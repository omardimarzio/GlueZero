---
quick_id: 260505-v1e
mode: quick
type: execute
wave: 1
depends_on: []
autonomous: true
language: it
files_modified:
  - "packages/sembridge/** (git mv → packages/gluezero/**)"
  - "packages/{core,mapper,routing,gateway,worker,cache,devtools,gluezero}/package.json"
  - "packages/*/src/**/*.ts (import path + identifier)"
  - "packages/*/src/**/*.test.ts (test asserzioni)"
  - "packages/*/README.md (8 file)"
  - "packages/gluezero/EXAMPLES.md"
  - "package.json (root scripts ci:publint/attw + size-limit)"
  - "pnpm-lock.yaml (rigenerato)"
  - "tsconfig.base.json (verifica path)"
  - "biome.json (verifica path)"
  - ".changeset/v1-0-0-release.md"
  - "README.md (root)"
  - "prd.md"
  - "CLAUDE.md"
  - ".planning/PROJECT.md"
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/TRACKER.md"
  - ".planning/phases/0?-*/*.md (160 file)"
requirements: ["RENAME-FS", "RENAME-NPM", "RENAME-IMPORTS", "RENAME-IDENT", "RENAME-METRIC", "RENAME-DOCS", "RENAME-PLANNING", "RENAME-CHANGESET"]

must_haves:
  truths:
    - "Il pacchetto pubblicabile aggregato si chiama @gluezero/gluezero (non più @sembridge/sembridge)"
    - "Tutti gli 8 sub-package hanno scope @gluezero/* (core, mapper, routing, gateway, worker, cache, devtools, gluezero)"
    - "Nessun import statement TS in packages/*/src/**/*.ts referenzia @sembridge/*"
    - "L'identifier pubblico è GlueZero/createGlueZero/GlueZeroConfig/GlueZeroAggregate (non più SemBridge*)"
    - "Le metric runtime usano prefisso gluezero.<package>.<metric> (D-163 invariato, solo rename del prefisso)"
    - "I README italiani citano GlueZero (non più SemBridge), preservando struttura e contatori @example/@see/@throws"
    - "I planning artefacts (prd.md, CLAUDE.md, ROADMAP, STATE, TRACKER, REQUIREMENTS, phases) citano GlueZero"
    - "Il changeset v1-0-0-release.md elenca @gluezero/* (8 package)"
    - "pnpm typecheck 8/8 passa senza errori"
    - "pnpm build:f3:cyclic completa ESM-only 8/8"
    - "pnpm test mantiene 1166/1169 passing (3 skip MSW V1.x F4 attesi)"
    - "pnpm biome check zero errors"
    - "pnpm publint 8/8 + pnpm attw 🟢 ESM su tutti gli 8 package"
    - "git grep finale di SemBridge/@sembridge/sembridge.<token> restituisce 0 hit non documentati"
  artifacts:
    - path: "packages/gluezero/package.json"
      provides: "Package aggregato pubblico rinominato @gluezero/gluezero con dependencies workspace su @gluezero/*"
      contains: '"name": "@gluezero/gluezero"'
    - path: "packages/gluezero/src/index.ts"
      provides: "Export createGlueZero + GlueZeroConfig + GlueZeroAggregate"
      contains: "createGlueZero"
    - path: "packages/{core,mapper,routing,gateway,worker,cache,devtools}/package.json"
      provides: "Sub-package rinominati @gluezero/<name> con workspace deps aggiornate"
      contains: '"name": "@gluezero/'
    - path: ".changeset/v1-0-0-release.md"
      provides: "Changeset entry allineato a 8 package @gluezero/*"
      contains: '"@gluezero/'
    - path: "package.json"
      provides: "Root scripts ci:publint + ci:attw + size-limit aggiornati a filtro @gluezero/*"
      contains: "@gluezero/core"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile rigenerato dopo rename scope"
      min_lines: 100
    - path: "CLAUDE.md"
      provides: "Project guide rinominato a GlueZero (eccetto path boundary root)"
      contains: "GlueZero"
    - path: "prd.md"
      provides: "PRD rinominato a GlueZero"
      contains: "GlueZero"
  key_links:
    - from: "packages/gluezero/package.json"
      to: "packages/{core,mapper,routing,gateway,worker,cache,devtools}/package.json"
      via: "workspace:* dependency su @gluezero/*"
      pattern: '@gluezero/(core|mapper|routing|gateway|worker|cache|devtools).*workspace:\*'
    - from: "packages/*/src/**/*.ts"
      to: "altri package @gluezero/*"
      via: "import statement"
      pattern: 'from\s+["'\'']@gluezero/'
    - from: "packages/devtools/src/metrics-collector.ts"
      to: "runtime metric naming"
      via: "string literal prefix"
      pattern: 'gluezero\.[a-z]+\.[a-z]+'
    - from: "package.json (root)"
      to: "size-limit + ci:publint + ci:attw"
      via: "filter @gluezero/* + path packages/gluezero/dist/index.js"
      pattern: '@gluezero/sembridge|packages/gluezero/dist'
---

<objective>
Rinominare il progetto da **SemBridge → GlueZero** in modo cross-cutting, atomic e pre-publish (nessun consumer pubblico esiste ancora — `pnpm release` non è stato eseguito, quindi il rename è breaking-change-free se fatto adesso).

**Scope coperto da questo PLAN:**
1. Rinomina filesystem `packages/sembridge/` → `packages/gluezero/` via `git mv` (preserva history).
2. Scope npm `@sembridge/*` → `@gluezero/*` su 8 `package.json` + workspace deps + root scripts (ci:publint, ci:attw, size-limit) + lockfile rigenerato.
3. Import statement cross-package `@sembridge/*` → `@gluezero/*` in tutti i `.ts`/`.test.ts` (~70 file source).
4. Identifier TypeScript pubblici/interni: `SemBridge` → `GlueZero`, `createSemBridge` → `createGlueZero`, `SemBridgeConfig` → `GlueZeroConfig`, `SemBridgeAggregate` → `GlueZeroAggregate` (case-sensitive).
5. Metric namespace runtime `sembridge.<package>.<metric>` → `gluezero.<package>.<metric>` (D-163 invariato — cambia SOLO il prefisso letterale; ~76 hit in 2 file source + test che asseriscono nomi metric).
6. JSDoc descrittivi italiani + commenti `SemBridge`/`sembridge` → `GlueZero`/`gluezero` (preserva @example/@see/@throws counts).
7. README italiani: root + 8 README package + `EXAMPLES.md` (sembridge → gluezero).
8. Planning artefacts: `prd.md`, `CLAUDE.md`, `.planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,TRACKER}.md`, ~160 file `.planning/phases/0?-*/*.md` — escluso commit hash storici.
9. Changeset `.changeset/v1-0-0-release.md` allineato a `@gluezero/*` (8 package).

**Regole di rename (case-sensitive, ordine di apply):**
- `@sembridge/`        → `@gluezero/`        (npm scope — non toccare in commit hash storici)
- `SemBridge`          → `GlueZero`          (PascalCase identifier + typo → tutti i symbol composti)
- `createSemBridge`    → `createGlueZero`    (sotto-caso di sopra ma esplicitato per chiarezza)
- `SemBridgeConfig`    → `GlueZeroConfig`
- `SemBridgeAggregate` → `GlueZeroAggregate`
- `packages/sembridge/`→ `packages/gluezero/` (path letterale)
- `sembridge.<lower>.<lower>` → `gluezero.<lower>.<lower>` (metric prefix Prometheus dot.case D-163 — usa pattern `sembridge\.[a-z_-]+` per ridurre falsi positivi; NON toccare la path boundary root in CLAUDE.md)
- `sembridge` lowercase nudo: SOLO nei contesti di package name nudo (es. heading `# @sembridge/sembridge` → `# @gluezero/gluezero`) e nel nome directory storico package — NON in identifier o variabili.
- **NON toccare:**
  - `pnpm-lock.yaml` (rigenerato a mano via `pnpm install`)
  - `graphify-out/**` (artefatto rigenerabile)
  - `node_modules/**`
  - `.git/**`
  - Commit hash menzionati nei SUMMARY.md storici (es. `3178103` resta com'è — il blob in git contiene ancora `@sembridge/*`, è corretto)
  - In `CLAUDE.md`: la string letterale del boundary `/Users/omarmarzio/programming/prova AI/SemBridge` finché l'utente non esegue `mv` root directory (istruzione manuale post-task).

**Hit count target post-rename:**
- `git grep -E "SemBridge|@sembridge|sembridge\\.[a-z]" -- ':!*.lock' ':!graphify-out/' ':!.git/' ':!node_modules/'` → ≈0 (eccezioni documentate nel SUMMARY).

**Stato pre-rename misurato:**
- Hit count totali: 3661 (3140 `@sembridge/` + 578 `SemBridge` identifier + 76 `sembridge.<lower>` metric — il restante è overlap regex)
- File con riferimenti: 375 totali (160 in `.planning/`, 70 TS source, 8 README, 8 package.json, 10 TS con identifier, 2 con metric prefix, 20 config tsup/vitest, etc.)
- File già monorepo: 8 package, ESM-only

Purpose: chiudere il rename atomico PRIMA di `pnpm release` v1.0.0. L'utente ha già il dominio `gluezero` libero. La milestone v1.0 è chiusa con verifier PASS, NESSUN package pubblicato → questo è l'ULTIMO momento sicuro per il rename senza dover gestire deprecation/redirect npm.
Output: 9 commit atomici (vedi struttura in &lt;tasks&gt;) + tutti i CI gates verdi + grep finale 0 hit + `260505-v1e-SUMMARY.md` italiano con istruzioni manuali post-task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@prd.md
@packages/sembridge/package.json
@packages/cache/package.json
@.changeset/v1-0-0-release.md
@package.json
@tsconfig.base.json
@pnpm-workspace.yaml

<interfaces>
<!-- Naming canonico target post-rename — l'executor deve trattare questi come contracts -->

Package scope: @gluezero/{core, mapper, routing, gateway, worker, cache, devtools, gluezero}
  └─ Aggregato pubblico: @gluezero/gluezero (era @sembridge/sembridge)

Identifier TypeScript pubblici (export):
  - createGlueZero            (era createSemBridge)
  - GlueZeroConfig            (era SemBridgeConfig)
  - GlueZeroAggregate         (era SemBridgeAggregate)
  - GlueZero (interface/type) (era SemBridge)

Metric naming (D-163 Prometheus dot.case):
  - gluezero.&lt;package&gt;.&lt;metric&gt;{&lt;labels&gt;}
    Es: gluezero.routing.routes.matched.count{routeId="weather-fetch"}
    Es: gluezero.gateway.http.requests.duration.histogram
  - Reservoir Algorithm R Vitter 1985 (D-165) invariato
  - Cardinality cap 100 (D-166) invariato

Directory layout:
  packages/gluezero/   (era packages/sembridge/)
  packages/gluezero/src/index.ts             — export createGlueZero
  packages/gluezero/src/augment.ts           — declaration merging
  packages/gluezero/EXAMPLES.md              — italiano 519 LOC
  packages/gluezero/README.md                — italiano 452 LOC

Boundary CLAUDE.md (path letterale root):
  - PRESERVA finché utente non esegue `mv "/Users/omarmarzio/programming/prova AI/SemBridge"`
  - Tutti gli altri riferimenti `SemBridge` nel project guide vanno rinominati
</interfaces>

<naming_invariants>
- L'unica `@gluezero/gluezero` è l'aggregato pubblico (l'unica path doppia: directory + scope = entrambi `gluezero`)
- `package.json` interno aggregato: `"name": "@gluezero/gluezero"`, `"description": "GlueZero aggregato pubblico ..."`
- Tutti gli altri 7 sub-package hanno `"name": "@gluezero/&lt;package&gt;"` (singolo segmento dopo lo scope)
- workspace deps: `"@gluezero/core": "workspace:*"` (mai `^x.y.z` finché non si pubblica)
- Mantieni: `"version": "0.0.0"` su tutti — la version diventa `1.0.0` SOLO via `changeset version` (dopo questo rename, in commit successivo del release real)
</naming_invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename filesystem + npm scope + lockfile (foundation)</name>
  <files>
    packages/sembridge/ → packages/gluezero/ (git mv ricorsivo),
    packages/gluezero/package.json,
    packages/{core,mapper,routing,gateway,worker,cache,devtools}/package.json,
    package.json (root scripts: build:f3, build:f3:cyclic, build:f3:dts:bootstrap, build:f3:dts:bundle, ci:publint, ci:attw, size-limit array),
    pnpm-lock.yaml (rigenerato),
    tsconfig.base.json (verifica zero hit @sembridge — è già pulito ma conferma),
    biome.json (verifica zero hit @sembridge),
    pnpm-workspace.yaml (è glob `packages/*` — NO change needed, conferma)
  </files>
  <action>
**Step 1.1 — git mv directory aggregato (CRITICO: usa `git mv` per preservare history):**
```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"
git mv packages/sembridge packages/gluezero
git status  # conferma rinomine ricorsive
```
**Step 1.2 — Rename `name` field nei package.json (8 file):**
- `packages/gluezero/package.json`: `"name": "@sembridge/sembridge"` → `"name": "@gluezero/gluezero"`; `"description"`: sostituisci `SemBridge` → `GlueZero`.
- `packages/{core,mapper,routing,gateway,worker,cache,devtools}/package.json`: `"name": "@sembridge/&lt;name&gt;"` → `"name": "@gluezero/&lt;name&gt;"`; `"description"`: sostituisci `SemBridge` → `GlueZero` e `sembridge` → `gluezero` (case-sensitive).

**Step 1.3 — Rename workspace deps in tutti gli 8 package.json:**
Sostituisci ogni occorrenza `"@sembridge/&lt;x&gt;": "workspace:*"` → `"@gluezero/&lt;x&gt;": "workspace:*"` in:
- `dependencies`, `devDependencies`, `peerDependencies` (se presenti)
- attenzione a `packages/cache/package.json` (deps su core+mapper+routing+gateway), `packages/worker/package.json`, `packages/devtools/package.json`, `packages/gluezero/package.json` (deps su tutti e 7).

**Step 1.4 — Root `package.json` scripts:**
Sostituisci nei comandi `--filter`:
- `--filter @sembridge/core` → `--filter @gluezero/core` (e per tutti i 7 + aggregato)
- `--filter @sembridge/sembridge` → `--filter @gluezero/gluezero`
Aggiorna anche `size-limit` array: ogni `name` da `"@sembridge/&lt;x&gt; (gzip)"` → `"@gluezero/&lt;x&gt; (gzip)"`; per l'aggregato anche path `packages/sembridge/dist/index.js` → `packages/gluezero/dist/index.js`.
Mantieni invariati: `"name": "sembridge-monorepo"` ⚠️ ANCHE QUESTO va rinominato a `"gluezero-monorepo"` (è il root package privato).

**Step 1.5 — Verifica config files:**
```bash
git grep -E "@sembridge|SemBridge|sembridge" -- 'tsconfig.base.json' 'biome.json' 'pnpm-workspace.yaml' 2>/dev/null
```
Atteso: 0 hit. Se hit → fix puntuale.

**Step 1.6 — Rigenera lockfile:**
```bash
pnpm install
```
Lockfile aggiornato con scope `@gluezero/*` per tutte le workspace ref.

**Step 1.7 — Sanity check immediato:**
```bash
pnpm typecheck   # DEVE fallire — il source TS ha ancora import @sembridge/* (questo è atteso, fixato in Task 2)
pnpm -r ls --depth=0 | grep -E "@gluezero|@sembridge"   # solo @gluezero/, nessun @sembridge/
```

**Commit 1:**
```
chore(rename): packages/sembridge → packages/gluezero (git mv + 8 package.json scope @gluezero/*)
```
File del commit: tutto il git mv ricorsivo + 8 package.json + root package.json + pnpm-lock.yaml.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" &amp;&amp; test ! -d packages/sembridge &amp;&amp; test -d packages/gluezero &amp;&amp; test "$(jq -r .name packages/gluezero/package.json)" = "@gluezero/gluezero" &amp;&amp; test "$(jq -r .name packages/core/package.json)" = "@gluezero/core" &amp;&amp; test -z "$(git grep -E '@sembridge/' -- 'package.json' 'packages/*/package.json' 'tsconfig.base.json' 'biome.json' 'pnpm-workspace.yaml' 2&gt;/dev/null)" &amp;&amp; pnpm install --frozen-lockfile=false 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
- packages/sembridge/ NON esiste più; packages/gluezero/ esiste con tutti i contenuti
- 8 package.json hanno `name: @gluezero/*`
- workspace deps tra package puntano a `@gluezero/*`
- Root package.json `name: gluezero-monorepo`, scripts ci:publint/ci:attw/size-limit citano `@gluezero/*`
- pnpm-lock.yaml rigenerato senza errori
- tsconfig.base.json + biome.json + pnpm-workspace.yaml: zero hit @sembridge
- Commit `chore(rename): packages/sembridge → packages/gluezero ...` creato
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename TS sources (import + identifier + metric prefix)</name>
  <files>
    packages/*/src/**/*.ts (~70 file con import @sembridge),
    packages/*/src/**/*.test.ts,
    packages/*/src/**/*.tsx (se presenti),
    packages/*/tsup.config.ts (verifica path letterali),
    packages/*/vitest.config.ts (verifica path letterali),
    packages/*/vitest.browser.config.ts (verifica path letterali),
    packages/devtools/src/metrics-collector.ts (metric prefix sembridge. → gluezero.),
    packages/devtools/src/devtools-broker.ts (metric prefix runtime emit),
    tutti i test che asseriscono `expect(metric.name).toBe('sembridge.routing....')` o pattern simile
  </files>
  <action>
**Step 2.1 — Trova tutti i file TS con riferimenti @sembridge:**
```bash
git grep -l "@sembridge/" -- 'packages/*/src/**/*.ts' 'packages/*/src/**/*.tsx' 'packages/*/src/**/*.test.ts' 2&gt;/dev/null &gt; /tmp/files-import.txt
wc -l /tmp/files-import.txt   # atteso ~70
```
Per ciascun file, sostituisci `@sembridge/` → `@gluezero/` (case-sensitive). USA `find/sed` o equivalente atomico — NO partial replace.

**Step 2.2 — Rename identifier TypeScript (case-sensitive):**
File target: tutti i `.ts`/`.tsx`/`.test.ts` sotto `packages/*/src/`.

Ordine di sostituzione (importante — più specifici prima):
1. `createSemBridge`     → `createGlueZero`
2. `SemBridgeAggregate`  → `GlueZeroAggregate`
3. `SemBridgeConfig`     → `GlueZeroConfig`
4. `SemBridge` (nudo, case-sensitive boundary `\bSemBridge\b`) → `GlueZero`

⚠️ NON toccare: 
- string literal `'@sembridge/...'` (già fixate a step 2.1, ma non è case-sensitive su SemBridge nudo, OK)
- token lowercase `sembridge` nelle string literal, ECCETTO le metric prefix gestite a step 2.3
- variabili locali a-caso che hanno la sequenza dei char: `\bSemBridge\b` previene falsi positivi.

**Step 2.3 — Rename metric prefix runtime + test asserzioni:**
Pattern: `sembridge.<lowercase>.<lowercase>...` (es. `sembridge.routing.routes.matched.count`).

File primari (runtime emit):
- `packages/devtools/src/metrics-collector.ts`
- `packages/devtools/src/devtools-broker.ts` (se contiene literal)
- ogni file con istanza letterale di `'sembridge.'` come prefisso

Pattern sed: `sembridge\.([a-z_-]+)` → `gluezero.\1` (preserva tutto il resto del nome metric).

Test che asseriscono nomi metric: cerca con `git grep "sembridge\." packages/*/src/**/*.test.ts` e applica stesso pattern.

**Step 2.4 — Verifica config tsup/vitest:**
```bash
git grep -E "@sembridge|SemBridge|sembridge\." -- 'packages/*/tsup.config.ts' 'packages/*/vitest.config.ts' 'packages/*/vitest.browser.config.ts' 2&gt;/dev/null
```
Sostituisci hit puntuali. Atteso: pochi hit (probabilmente nessuno), ma verifica.

**Step 2.5 — Verifica typecheck full:**
```bash
pnpm typecheck   # DEVE passare 8/8 ora
pnpm build:f3:cyclic   # DEVE passare ESM-only 8/8 — workflow ciclica routing↔gateway
pnpm test   # DEVE mantenere 1166/1169 (3 skip MSW V1.x F4 attesi)
pnpm biome check .   # DEVE passare zero errors
pnpm ci:publint   # DEVE passare 8/8
pnpm ci:attw   # DEVE passare 🟢 ESM su 8/8
```
Se uno qualsiasi fallisce → fix puntuale e re-run prima di committare.

**Step 2.6 — Commit (3 commit atomici):**
```
refactor(rename): import @sembridge/* → @gluezero/* (cross-package TS sources ~70 file)
refactor(rename): identifier SemBridge → GlueZero + createSemBridge → createGlueZero
refactor(rename): metric namespace sembridge.<package>.<metric> → gluezero.* (D-163 invariato)
```
File del commit: solo `.ts/.tsx/.test.ts` toccati + eventuali tsup/vitest config.

**Note metric (D-163):**
- D-163 lock: `<scope>.<package>.<metric>` con dot.case Prometheus.
- QUESTO PLAN cambia SOLO `<scope>` da `sembridge` a `gluezero`.
- La struttura del nome (segments, labels, reservoir D-165, cardinality cap D-166) NON cambia.
- Il SUMMARY del task dovrà annotare: "metric prefix flip 76 hit, D-163 unchanged".
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" &amp;&amp; test -z "$(git grep -E '@sembridge/' -- 'packages/*/src/' 2&gt;/dev/null)" &amp;&amp; test -z "$(git grep -E '\bSemBridge\b|createSemBridge' -- 'packages/*/src/' 2&gt;/dev/null)" &amp;&amp; test -z "$(git grep -E 'sembridge\.[a-z]' -- 'packages/*/src/' 2&gt;/dev/null)" &amp;&amp; pnpm typecheck 2&gt;&amp;1 | tail -10 &amp;&amp; pnpm test 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
- 0 hit `@sembridge/` in `packages/*/src/`
- 0 hit `\bSemBridge\b` o `createSemBridge` in `packages/*/src/` (case-sensitive identifier)
- 0 hit `sembridge\.<lower>` in `packages/*/src/` (metric prefix flipped)
- pnpm typecheck 8/8 PASS
- pnpm build:f3:cyclic ESM-only 8/8 PASS
- pnpm test 1166/1169 PASS (3 skip MSW V1.x F4 attesi)
- pnpm biome check zero errors
- pnpm ci:publint 8/8 + pnpm ci:attw 🟢 ESM 8/8
- 3 commit atomici creati (import + identifier + metric)
  </done>
</task>

<task type="auto">
  <name>Task 3: Rename docs (JSDoc + README + EXAMPLES + planning artefacts + changeset) + grep finale + SUMMARY</name>
  <files>
    packages/*/src/**/*.ts (JSDoc descrittivi italiani che citano "SemBridge"/"sembridge" — solo commenti, NON identifier già fatto in Task 2),
    README.md (root),
    packages/{core,mapper,routing,gateway,worker,cache,devtools,gluezero}/README.md (8 file),
    packages/gluezero/EXAMPLES.md,
    .changeset/v1-0-0-release.md,
    prd.md,
    CLAUDE.md (eccetto path letterale boundary `/Users/omarmarzio/programming/prova AI/SemBridge`),
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/TRACKER.md,
    .planning/phases/0?-*/*.md (~160 file),
    .planning/quick/260505-v1e-rinomina-sembridge-to-gluezero/260505-v1e-SUMMARY.md (NEW)
  </files>
  <action>
**Step 3.1 — JSDoc cleanup descrittivi italiani:**
Cerca `git grep -l "SemBridge\|sembridge" -- 'packages/*/src/**/*.ts'` (a questo punto solo commenti residui — gli identifier sono già stati flippati in Task 2). Casi tipici:
- JSDoc body: `/** SemBridge è la libreria browser-side ... */` → `/** GlueZero è la libreria browser-side ... */`
- @example che mostra `import { createSemBridge } from '@sembridge/sembridge'` (improbabile a questo punto, ma verifica)
- Inline `// note: SemBridge usa ...` → `// note: GlueZero usa ...`

⚠️ Preserva count: contare `@example`/`@see`/`@throws` PRIMA e DOPO. Atteso invariato (cambiamento solo testuale, non struttura).

**Step 3.2 — README package + root:**
- `README.md` (root): se cita SemBridge nei title/intro/install instructions, sostituisci. Verifica se `pnpm install @sembridge/sembridge` → `pnpm install @gluezero/gluezero`.
- `packages/{core,mapper,routing,gateway,worker,cache,devtools,gluezero}/README.md` (8 README italiani): sostituzione globale `SemBridge` → `GlueZero`, `@sembridge/` → `@gluezero/`, `sembridge.<metric>` → `gluezero.<metric>` (case-sensitive). Preserva struttura sezioni e contatori (Q&amp;A enumerate Q1-Q7 in devtools README WARNING-2 fix → preservato come-è, solo nome flippato).
- `packages/gluezero/EXAMPLES.md` (519 LOC): sostituzione globale stessa.

**Step 3.3 — Changeset:**
`.changeset/v1-0-0-release.md`: riallinea le 8 entry `"@sembridge/X": major` → `"@gluezero/X": major`. Sostituisci nel body del changeset:
- title `# SemBridge v1.0.0` → `# GlueZero v1.0.0`
- bullets che menzionano scope: `@sembridge/{core,...}` → `@gluezero/{core,...}`
- bundle size table: `@sembridge/X` → `@gluezero/X`
- V1.x roadmap: `@sembridge/cache-idb` → `@gluezero/cache-idb`, `@sembridge/metrics-prometheus` → `@gluezero/metrics-prometheus`, `@sembridge/metrics-otel` → `@gluezero/metrics-otel`

**Step 3.4 — Planning artefacts (alta priorità):**
- `prd.md`: sostituzione globale `SemBridge` → `GlueZero`, `@sembridge/` → `@gluezero/`, `sembridge.<metric>` → `gluezero.<metric>`. Verifica conteggio sezioni invariato.
- `CLAUDE.md`: sostituzione cautelosa:
  - `# SemBridge` (heading) → `# GlueZero`
  - `Cos'è SemBridge` → `Cos'è GlueZero`
  - Stack table cells `@sembridge/{core, mapper, ...}` → `@gluezero/{core, mapper, ...}`
  - Tutti gli altri letterali `SemBridge`/`@sembridge/`/`sembridge.<metric>`
  - ⚠️ **NON toccare:** la string letterale `/Users/omarmarzio/programming/prova AI/SemBridge` nel "Boundary di sicurezza" (la directory root NON viene mossa in questa sessione — istruzione manuale post-task). Se ne resterà 1 hit, è ATTESO e va documentato nel SUMMARY.
- `.planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,TRACKER}.md`: sostituzione globale.
- `.planning/phases/0?-*/*.md` (~160 file: SUMMARY, PLAN, CONTEXT, RESEARCH, PATTERNS, DISCUSSION-LOG, VERIFICATION, REVIEW, REVIEW-FIX): sostituzione globale `SemBridge` → `GlueZero`, `@sembridge/` → `@gluezero/`, `sembridge.<metric>` → `gluezero.<metric>`, `packages/sembridge/` → `packages/gluezero/`.
  - ⚠️ **Eccezione:** commit hash storici (es. `3178103`, `cf51c97`) NON vanno toccati — il blob in git contiene ancora `@sembridge/*`, ed è corretto storicamente.
  - ⚠️ **Eccezione:** se un planning file cita esplicitamente "il commit `3178103` ha messaggio `chore(rename): ...`" — il messaggio storico riferito a quel commit resta com'era; ma se il planning file NARRA il rename in prosa, la prosa va attualizzata.

Approccio raccomandato (sed in-place batched, atomic per categoria):
```bash
# fase 1: tutti i file .planning/**/*.md tranne path planning/quick/260505-v1e
find .planning -name '*.md' -not -path '*/quick/260505-v1e-*' \
  -exec sed -i.bak \
    -e 's|@sembridge/|@gluezero/|g' \
    -e 's|packages/sembridge/|packages/gluezero/|g' \
    -e 's|SemBridgeAggregate|GlueZeroAggregate|g' \
    -e 's|SemBridgeConfig|GlueZeroConfig|g' \
    -e 's|createSemBridge|createGlueZero|g' \
    -e 's|SemBridge|GlueZero|g' \
    -e 's|sembridge\.\([a-z_-]\+\)|gluezero.\1|g' \
    {} \;
find .planning -name '*.md.bak' -delete
```
(Adatta sed per macOS BSD: `sed -i ''` invece di `sed -i.bak` se preferito; comunque pulisci `.bak`.)

**Step 3.5 — CLAUDE.md custom (preserva boundary letterale):**
Strategia per CLAUDE.md:
1. Salva il letterale path boundary: `BOUNDARY="/Users/omarmarzio/programming/prova AI/SemBridge"` (DEVE restare intatto).
2. Applica sed globale `SemBridge` → `GlueZero`.
3. Ripristina manualmente la riga del boundary `Area di lavoro libera: ...` con il letterale originale `/Users/omarmarzio/programming/prova AI/SemBridge/`.
4. Verifica con `grep "/Users/omarmarzio/programming/prova AI/SemBridge" CLAUDE.md` — DEVE restituire ≥1 hit (riga boundary).

**Step 3.6 — Grep finale (gate di completion):**
```bash
git grep -E "SemBridge|@sembridge|sembridge\.[a-z]" -- ':!*.lock' ':!graphify-out/' ':!.git/' ':!node_modules/' ':!.planning/quick/260505-v1e-*' &gt; /tmp/grep-residual.txt
wc -l /tmp/grep-residual.txt
cat /tmp/grep-residual.txt
```
Atteso: hit residui SOLO in:
- `CLAUDE.md` riga boundary `Area di lavoro libera: /Users/omarmarzio/programming/prova AI/SemBridge` (1 hit atteso, documentato)
- Eventuali commit hash storici menzionati in planning narrative — verifica contesto, se sono "il commit XYZ con messaggio storico" ok, altrimenti fix.

Se grep ritorna hit non documentati → fix puntuale e re-run finché 0 hit non-documentati.

**Step 3.7 — Re-run CI gates POST docs/JSDoc (regressione zero):**
```bash
pnpm typecheck   # 8/8
pnpm build:f3:cyclic   # ESM-only 8/8 con dts JSDoc preservation
pnpm test   # 1166/1169
pnpm biome check .   # zero errors
pnpm ci:publint   # 8/8
pnpm ci:attw   # 🟢 ESM 8/8
```

**Step 3.8 — Verifica JSDoc preservation post-build:**
Dopo build, verifica che `dist/index.d.ts` contenga ancora `@example` / `@see` / `@throws` con i count attesi (target floor — vedi SUMMARY 06-09b). Se un caso scende → fix puntuale.

**Step 3.9 — Scrivi `260505-v1e-SUMMARY.md` italiano:**
Path: `.planning/quick/260505-v1e-rinomina-sembridge-to-gluezero/260505-v1e-SUMMARY.md`.

Contenuto (sezioni minime):
1. **Obiettivo** — rename pre-publish SemBridge → GlueZero
2. **Lista commit (9 atomici)** — hash + message
3. **Hit-count grep pre/post**:
   - PRE: 3661 hit totali (3140 `@sembridge/` + 578 `SemBridge` + 76 `sembridge.<lower>` metric, overlap rimosso)
   - POST: 0 hit non-documentati. Hit residui documentati: CLAUDE.md boundary path letterale (1 riga, post-rename root manuale)
4. **REQ matrix** — invariata (91/91 Complete). Nessun REQ-ID toccato. Nessuna nuova decisione D-XX (rename è meccanico, non architetturale).
5. **CI gates verde**:
   - pnpm typecheck 8/8 ✅
   - pnpm build:f3:cyclic ESM-only 8/8 ✅
   - pnpm test 1166/1169 (3 skip MSW V1.x F4 attesi) ✅
   - pnpm biome check zero errors ✅
   - pnpm ci:publint 8/8 ✅
   - pnpm ci:attw 🟢 ESM 8/8 ✅
   - JSDoc preservation: @example 36 / @see 55 / @throws 9 (sopra target floor) ✅
6. **Istruzioni manuali post-task (utente le esegue dopo il merge):**
   ```bash
   # 1. Rinomina root directory (sessione corrente cwd, non eseguibile da Claude)
   cd "/Users/omarmarzio/programming/prova AI"
   mv "SemBridge" "GlueZero"
   cd GlueZero

   # 2. Aggiorna CLAUDE.md "Area di lavoro libera" path letterale boundary
   sed -i '' 's|/programming/prova AI/SemBridge|/programming/prova AI/GlueZero|g' CLAUDE.md
   git add CLAUDE.md &amp;&amp; git commit -m "chore(rename): boundary path post-mv root directory"

   # 3. (Opzionale) Aggiorna git remote se anche il repo GitHub viene rinominato
   git remote -v
   git remote set-url origin git@github.com:&lt;org&gt;/gluezero.git   # nuovo nome repo

   # 4. (Opzionale) Copia memory dir GSD-Claude alla nuova path
   cp -r ~/.claude/projects/-Users-omarmarzio-programming-prova-AI-SemBridge \
         ~/.claude/projects/-Users-omarmarzio-programming-prova-AI-GlueZero

   # 5. Rigenera knowledge graph al nuovo path
   rm -rf graphify-out
   /graphify .
   ```
7. **Eccezioni grep documentate** — la riga residua in CLAUDE.md boundary è ATTESA (l'utente farà mv root dopo).
8. **Nessun breaking change pubblico** — nessuna versione npm pubblicata, scope flippato pre-`pnpm release` v1.0.0.

**Step 3.10 — Commit finali (4 commit atomici):**
```
docs(rename): JSDoc descrittivi italiani SemBridge → GlueZero (preserva @example/@see/@throws counts)
docs(rename): README + EXAMPLES italiani aggiornati a GlueZero (8 README package + root + EXAMPLES.md)
docs(rename): planning artefacts SemBridge → GlueZero (prd, CLAUDE eccetto boundary, ROADMAP, REQUIREMENTS, STATE, TRACKER, ~160 phases)
docs(rename): changeset v1-0-0-release.md @gluezero scope (8 entries) + 260505-v1e-SUMMARY.md
```
Total commit del PLAN: 9 (3 da Task 1+2 + 4 da Task 3 docs + 2 carryover identifier/metric).

⚠️ Aggiusta numerazione commit allineandola alla struttura raccomandata in `&lt;constraints&gt;` (9 commit atomici totali). Se logica suggerisce mergiare due commit docs in uno (es. README + EXAMPLES insieme), va bene — l'importante è ordine e atomicità.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" &amp;&amp; RESIDUAL=$(git grep -E "SemBridge|@sembridge|sembridge\.[a-z]" -- ':!*.lock' ':!graphify-out/' ':!.git/' ':!node_modules/' ':!.planning/quick/260505-v1e-*' 2&gt;/dev/null | grep -v "/Users/omarmarzio/programming/prova AI/SemBridge" | wc -l) &amp;&amp; test "$RESIDUAL" -eq 0 &amp;&amp; pnpm typecheck 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm test 2&gt;&amp;1 | tail -10 &amp;&amp; pnpm ci:publint 2&gt;&amp;1 | tail -10 &amp;&amp; pnpm ci:attw 2&gt;&amp;1 | tail -10 &amp;&amp; test -f .planning/quick/260505-v1e-rinomina-sembridge-to-gluezero/260505-v1e-SUMMARY.md</automated>
  </verify>
  <done>
- JSDoc descrittivi italiani flippati a GlueZero
- 8 README package + root README + EXAMPLES.md aggiornati a GlueZero
- prd.md, CLAUDE.md (eccetto boundary path), .planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,TRACKER}.md, ~160 file phases aggiornati
- .changeset/v1-0-0-release.md riallineato a `@gluezero/*`
- Grep finale: 0 hit non-documentati (1 hit atteso CLAUDE.md boundary path letterale)
- pnpm typecheck/test/build/biome/publint/attw tutti verdi (zero regressione)
- JSDoc preservation @example/@see/@throws counts above target floor
- `260505-v1e-SUMMARY.md` italiano scritto con 8 sezioni richieste + istruzioni manuali post-task
- 9 commit atomici totali del PLAN allineati alla struttura raccomandata
  </done>
</task>

</tasks>

<verification>
**Phase-level checks (eseguire DOPO tutti i 3 task):**

```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"

# 1. Filesystem
test ! -d packages/sembridge
test -d packages/gluezero
test -f packages/gluezero/package.json
test -f packages/gluezero/EXAMPLES.md

# 2. npm scope
for pkg in core mapper routing gateway worker cache devtools gluezero; do
  test "$(jq -r .name packages/$pkg/package.json)" = "@gluezero/$pkg" || echo "FAIL: $pkg"
done
test "$(jq -r .name package.json)" = "gluezero-monorepo"

# 3. Workspace deps consistency
git grep -E "@sembridge/" -- 'package.json' 'packages/*/package.json'   # 0 hit

# 4. TS source clean
git grep -E "@sembridge/|\bSemBridge\b|createSemBridge|sembridge\.[a-z]" -- 'packages/*/src/'   # 0 hit

# 5. Docs clean (eccetto boundary CLAUDE.md)
git grep -E "SemBridge|@sembridge|sembridge\.[a-z]" -- ':!*.lock' ':!graphify-out/' ':!node_modules/' ':!.planning/quick/260505-v1e-*' \
  | grep -v "/Users/omarmarzio/programming/prova AI/SemBridge" \
  | wc -l   # 0

# 6. CI gates
pnpm typecheck                  # 8/8 ✅
pnpm build:f3:cyclic            # ESM-only 8/8 ✅
pnpm test                       # 1166/1169 (3 skip MSW V1.x F4 attesi) ✅
pnpm biome check .              # zero errors ✅
pnpm ci:publint                 # 8/8 ✅
pnpm ci:attw                    # 🟢 ESM 8/8 ✅

# 7. SUMMARY presente
test -f .planning/quick/260505-v1e-rinomina-sembridge-to-gluezero/260505-v1e-SUMMARY.md

# 8. JSDoc preservation (sample check su gluezero aggregato)
grep -c "@example" packages/gluezero/dist/index.d.ts   # ≥27 (target floor)
grep -c "@see" packages/gluezero/dist/index.d.ts       # ≥30
grep -c "@throws" packages/gluezero/dist/index.d.ts    # ≥9
```

Se uno qualsiasi di questi check fallisce → fix puntuale e re-run, NON committare prima.
</verification>

<success_criteria>
- 8 package.json hanno `name: @gluezero/*` (core, mapper, routing, gateway, worker, cache, devtools, gluezero)
- root `package.json` ha `name: gluezero-monorepo` + scripts ci:publint/ci:attw/size-limit aggiornati
- pnpm-lock.yaml rigenerato senza errori
- 0 hit `@sembridge/` in tutto il repo (eccetto eventuali commit hash storici, già esclusi dal grep)
- 0 hit `SemBridge` (case-sensitive identifier) in `packages/*/src/`
- 0 hit `sembridge.<metric>` (Prometheus dot.case prefix) in tutto il repo runtime
- 1 hit ATTESO `/Users/omarmarzio/programming/prova AI/SemBridge` nel boundary CLAUDE.md (documentato in SUMMARY)
- pnpm typecheck/build:f3:cyclic/test/biome/publint/attw tutti verdi
- 1166/1169 test passing (3 skip MSW V1.x F4 invariati)
- JSDoc preservation @example/@see/@throws counts sopra target floor
- 9 commit atomici creati nell'ordine raccomandato (foundation → imports → identifier → metric → JSDoc → README → planning → changeset)
- `260505-v1e-SUMMARY.md` italiano scritto con: lista commit + hit count pre/post + REQ matrix invariata + CI gates green + istruzioni manuali post-task (mv root, remote update, memory dir copy, graphify regenerate)
</success_criteria>

<output>
After completion, create:
`.planning/quick/260505-v1e-rinomina-sembridge-to-gluezero/260505-v1e-SUMMARY.md`

Aggiorna anche:
- `.planning/TRACKER.md` — nuova entry: "Quick task 260505-v1e: rename SemBridge → GlueZero completato. Pronto per pnpm release v1.0.0 (eseguito a discrezione utente)."
- `.planning/STATE.md` — `last_updated` flippato; nessun cambio progress (milestone v1.0 resta CHIUSA, è solo rename pre-publish).
</output>
