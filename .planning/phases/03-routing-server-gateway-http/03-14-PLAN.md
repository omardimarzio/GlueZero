---
phase: 03-routing-server-gateway-http
plan: 14
type: execute
wave: 9
depends_on:
  - 03-12
  - 03-13
files_modified:
  - packages/routing/README.md
  - packages/gateway/README.md
  - packages/routing/package.json
  - packages/gateway/package.json
  - package.json
  - .size-limit.json
  - packages/routing/src/router-broker-wrapper.ts
  - packages/routing/src/public-factory.ts
  - packages/routing/src/types/route-definition.ts
  - packages/routing/src/route-resolver.ts
  - packages/gateway/src/http/http-gateway.ts
  - packages/gateway/src/http/public-factory.ts
  - packages/gateway/src/http/strategies/retry-strategy.ts
  - packages/gateway/src/http/strategies/auth-strategy.ts
autonomous: true
requirements:
  - DOC-04
  - PKG-04
  - TEST-01
  - TEST-02
  - TEST-03
tags:
  - final-gate
  - phase-3
  - ci
  - documentation
  - coverage
must_haves:
  truths:
    - "Coverage v8 ≥ 90% sui file `packages/routing/src/` (escluso test-utils + __integration__) e `packages/gateway/src/http/` (D-92)."
    - "`publint --strict` exit 0 su `@gluezero/routing` e `@gluezero/gateway`."
    - "`attw --pack --profile=esm-only` exit 0 su entrambi i package."
    - "`size-limit` budget rispettati: `@gluezero/routing` < 6 KB gz, `@gluezero/gateway/http` < 8 KB gz (budget RAISED da F2 lesson learned)."
    - "Bundle size effettivo riportato in SUMMARY (formato F2 plan 02-12: `routing: X KB / 6 KB budget = Y%`)."
    - "README italiano `packages/routing/README.md` + `packages/gateway/README.md` espansi con: cosa contiene, esempio scenario meteo HTTP end-to-end, link DOC-04 sezione interna del repo."
    - "JSDoc completo italiano su API pubblica: `RouterBroker`, `createRouterBroker`, `RouteDefinition`, `RouteResolver` (in routing) + `HttpGateway`, `createHttpGateway`, `createRetryStrategy`, `createAuthStrategy` (in gateway)."
    - "`pnpm --filter @gluezero/routing biome check` exit 0; idem gateway."
    - "Test totali F3 passing: ~145 unit + 14 integration = ~159 (cumulato F1 248 + F2 149 invariati)."
    - "`git diff --name-only packages/core/ packages/mapper/` empty (D-83 enforcement final check)."
  artifacts:
    - path: "packages/routing/README.md"
      provides: "Documentazione italiana @gluezero/routing"
      contains: "RouterBroker"
      min_lines: 100
    - path: "packages/gateway/README.md"
      provides: "Documentazione italiana @gluezero/gateway/http"
      contains: "HttpGateway"
      min_lines: 100
    - path: ".size-limit.json"
      provides: "Size budget routing + gateway/http"
      contains: "@gluezero/routing"
  key_links:
    - from: ".size-limit.json"
      to: "packages/routing/dist/index.js"
      via: "size-limit entry path"
      pattern: "packages/routing/dist/index.js"
    - from: ".size-limit.json"
      to: "packages/gateway/dist/http/index.js"
      via: "size-limit entry subpath"
      pattern: "packages/gateway/dist/http/index.js"
---

<objective>
**Final Gate F3** — chiude la Phase 3 con: coverage v8 ≥ 90% (D-92), CI gates extended (publint + attw + size-limit per i 2 package F3), README espansi italiano, JSDoc completo API pubblica (DOC-04 chiusura), regression check finale (test F1 + F2 invariati, ZERO modifiche `packages/core/` e `packages/mapper/`). Replica il pattern del plan 02-12 (final gate F2).

Verifica i 5 success criteria F3 ROADMAP via i 14 integration test del plan 03-13. Aggiorna ROADMAP.md / STATE.md per marcare Phase 3 complete con sintesi metriche.

Purpose: Phase 3 production-ready — `@gluezero/routing` e `@gluezero/gateway/http` pubblicabili (post-changeset). Plan 03-15+ NON esistono — F3 è chiusa qui.

Output: 4 doc + 2 package.json + 1 size-limit + JSDoc additions + ROADMAP/STATE update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/mapper/README.md
@packages/core/README.md
@packages/routing/package.json
@packages/gateway/package.json
@package.json
@packages/routing/src/router-broker-wrapper.ts
@packages/routing/src/public-factory.ts
@packages/routing/src/types/route-definition.ts
@packages/gateway/src/http/http-gateway.ts
@packages/gateway/src/http/public-factory.ts

<interfaces>
From plan 02-12 final gate F2:
- Pattern publint --strict + attw --pack --profile=esm-only + size-limit
- README JSDoc italiano espanso con scenari end-to-end
- Coverage v8 thresholds in vitest.config.ts (riusabile)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README italiano espansi (routing + gateway)</name>
  <read_first>
    - packages/mapper/README.md (template italiano espanso F2)
    - packages/core/README.md (template F1)
    - .planning/phases/03-routing-server-gateway-http/03-CONTEXT.md (D-60..D-99 sintesi)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 824-888 esempio config consumer pattern)
  </read_first>
  <files>
    packages/routing/README.md,
    packages/gateway/README.md
  </files>
  <action>
    **`packages/routing/README.md` (≥100 LOC):**
    Sezioni in italiano:
    1. Stato (Phase 3 complete, REQ-ID coperti)
    2. Cosa contiene (RouterBroker + createRouterBroker + RouteDefinition discriminated union 4 type)
    3. Vincoli (D-83 composition wrapper — 0 modifiche a `@gluezero/core` e `@gluezero/mapper` runtime)
    4. Esempio scenario meteo HTTP end-to-end (PRD §29) — copy da CONTEXT lines 374-428 con commenti italiano
    5. Open issues PRD §39 chiusi: #5 (ROUTE-16), #6 (ROUTE-15), #7 (LIFE-02 ext), #8 (ROUTE-09)
    6. Documentazione (link a DOC-04 sezione: route engine + gateway architecture)
    7. Pipeline §28 step 8/9/10 (riferimento ai tap)
    8. Licenza

    **`packages/gateway/README.md` (≥100 LOC):**
    Sezioni in italiano:
    1. Stato (Phase 3 complete sub-modulo HTTP, F4 sub-modulo SSE/WS riservato)
    2. Cosa contiene (HttpGateway + createHttpGateway + 7 Strategy primitives)
    3. Subpath exports `@gluezero/gateway/http` (Phase 3) e `@gluezero/gateway/sse-ws` (F4 placeholder)
    4. Esempio config gateway (auth + allowlist + defaults retry/idempotency + circuitBreaker)
    5. Policy chain order: allowlist → auth → idempotency → dedupe → backpressure → retry → fetch → parse → validation
    6. Open issue chiuso: #8 (ROUTE-09 retry differenziato 4xx/5xx/408/429)
    7. Stack: fetch nativo + AbortController/AbortSignal + nanoid (Idempotency-Key) + Valibot (response validation via riuso F2)
    8. Licenza

    Pattern documentazione "what + why + how" + JSDoc cross-link.
  </action>
  <verify>
    <automated>test $(wc -l < packages/routing/README.md) -ge 100 && test $(wc -l < packages/gateway/README.md) -ge 100 && grep -q "RouterBroker" packages/routing/README.md && grep -q "HttpGateway" packages/gateway/README.md && grep -q "scenario meteo" packages/routing/README.md</automated>
  </verify>
  <done>
    - README routing ≥ 100 LOC con sezione scenario meteo
    - README gateway ≥ 100 LOC con sezione policy chain
    - Italiano (no English content tranne identifier code)
  </done>
</task>

<task type="auto">
  <name>Task 2: JSDoc completo API pubblica + biome format</name>
  <read_first>
    - packages/routing/src/router-broker-wrapper.ts (aggiungi JSDoc a metodi pubblici)
    - packages/routing/src/public-factory.ts (createRouterBroker JSDoc)
    - packages/routing/src/types/route-definition.ts (RouteDefinition JSDoc già da plan 03-02)
    - packages/routing/src/route-resolver.ts (RouteResolver JSDoc)
    - packages/gateway/src/http/http-gateway.ts (HttpGateway methods)
    - packages/gateway/src/http/public-factory.ts (createHttpGateway)
    - packages/gateway/src/http/strategies/retry-strategy.ts (createRetryStrategy)
    - packages/gateway/src/http/strategies/auth-strategy.ts (createAuthStrategy)
  </read_first>
  <files>
    packages/routing/src/router-broker-wrapper.ts,
    packages/routing/src/public-factory.ts,
    packages/routing/src/types/route-definition.ts,
    packages/routing/src/route-resolver.ts,
    packages/gateway/src/http/http-gateway.ts,
    packages/gateway/src/http/public-factory.ts,
    packages/gateway/src/http/strategies/retry-strategy.ts,
    packages/gateway/src/http/strategies/auth-strategy.ts
  </files>
  <action>
    Per ogni file aggiungi/aggiorna JSDoc italiano:
    - Class/function description (cosa fa, REQ-ID coperto)
    - @param con tipo + descrizione
    - @returns
    - @example (per API pubbliche es. `createRouterBroker`, `createHttpGateway`)
    - @throws (per createBrokerError code documentati)
    - @see al PRD §X.Y o D-XX riferimento

    Esempio per `createRouterBroker`:
    ```typescript
    /**
     * Factory per creare un'istanza `RouterBroker` con validation Valibot strutturale.
     *
     * Il `RouterBroker` è un composition wrapper di `MapperBroker` (F2) che aggiunge
     * il routing engine dichiarativo + il Server Gateway HTTP centralizzato (Phase 3).
     *
     * Vincolo D-83: NESSUNA modifica al runtime di `@gluezero/core` o `@gluezero/mapper`.
     *
     * @param config — Config opzionale `RouterBrokerConfig`. Tutte le sezioni sono opzionali.
     * @returns Istanza `RouterBroker` pronta per `registerPlugin`/`registerRoute`/`publish`.
     * @throws Error con messaggio `Invalid RouterBrokerConfig: ...` se Valibot fallisce.
     *
     * @example
     * ```typescript
     * const broker = createRouterBroker({
     *   gateway: { allowlist: [/^\/api\//] },
     *   routes: [{ id: 'weather-http', type: 'http', topic: 'weather.requested', request: {...}, response: { canonical: 'weather' } }],
     * })
     * broker.publish('weather.requested', { location: 'Roma', forecast_date: '2026-04-30' })
     * ```
     *
     * @see PRD §17 Routing Engine
     * @see PRD §18 Server Gateway HTTP
     */
    export function createRouterBroker(config: RouterBrokerConfig = {}): RouterBroker { ... }
    ```

    **Biome format pass:**
    Esegui `pnpm --filter @gluezero/routing biome format --write` e `pnpm --filter @gluezero/gateway biome format --write` per allineare gli style.
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing biome check 2>&1 | tee /tmp/03-14-biome-r.log && pnpm --filter @gluezero/gateway biome check 2>&1 | tee /tmp/03-14-biome-g.log; grep -c "Found" /tmp/03-14-biome-r.log /tmp/03-14-biome-g.log | grep -q "0$"</automated>
  </verify>
  <done>
    - biome check exit 0 entrambi i package
    - JSDoc presenti su API pubblica (`grep -c "@example" packages/routing/src/public-factory.ts` ≥ 1)
    - `grep -c "@see" packages/routing/src/router-broker-wrapper.ts` ≥ 2
    - `grep -c "@example" packages/gateway/src/http/public-factory.ts` ≥ 1
  </done>
</task>

<task type="auto">
  <name>Task 3: CI gates extension — publint + attw + size-limit budget routing 6 KB / gateway/http 8 KB</name>
  <read_first>
    - packages/mapper/package.json (analogo F2 size-limit + scripts)
    - package.json (root — size-limit deps + script)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 922-945 CI gates extension Pattern S9 + bundle budget rationale A8/A9)
  </read_first>
  <files>
    packages/routing/package.json,
    packages/gateway/package.json,
    package.json,
    .size-limit.json
  </files>
  <action>
    **`packages/routing/package.json`:** aggiungi/conferma scripts:
    ```json
    "scripts": {
      "build": "tsup",
      "test": "vitest run",
      "typecheck": "tsc --noEmit",
      "publint": "publint --strict",
      "attw": "attw --pack --profile=esm-only"
    }
    ```

    **`packages/gateway/package.json`:** stessa struttura + verifica `exports['./http']` field.

    **`.size-limit.json` (root):** Aggiunge entries:
    ```json
    [
      { "name": "@gluezero/core (gzip)", "path": "packages/core/dist/index.js", "limit": "8 KB", "gzip": true },
      { "name": "@gluezero/mapper (gzip)", "path": "packages/mapper/dist/index.js", "limit": "12 KB", "gzip": true },
      { "name": "@gluezero/routing (gzip)", "path": "packages/routing/dist/index.js", "limit": "6 KB", "gzip": true },
      { "name": "@gluezero/gateway/http (gzip)", "path": "packages/gateway/dist/http/index.js", "limit": "8 KB", "gzip": true }
    ]
    ```

    Mantieni le entries F1+F2 esistenti (8KB core, 12KB mapper). Aggiungi le 2 nuove entries F3.

    **`package.json` (root) scripts:** aggiungi se mancano:
    ```json
    "publint:routing": "pnpm --filter @gluezero/routing publint",
    "publint:gateway": "pnpm --filter @gluezero/gateway publint",
    "attw:routing": "pnpm --filter @gluezero/routing attw",
    "attw:gateway": "pnpm --filter @gluezero/gateway attw",
    "ci:gate:f3": "pnpm publint:routing && pnpm publint:gateway && pnpm attw:routing && pnpm attw:gateway && pnpm size-limit"
    ```
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing build && pnpm --filter @gluezero/gateway build && pnpm --filter @gluezero/routing publint --strict 2>&1 | tee /tmp/03-14-publint-r.log && pnpm --filter @gluezero/gateway publint --strict 2>&1 | tee /tmp/03-14-publint-g.log && pnpm --filter @gluezero/routing exec attw --pack --profile=esm-only 2>&1 | tee /tmp/03-14-attw-r.log && pnpm --filter @gluezero/gateway exec attw --pack --profile=esm-only 2>&1 | tee /tmp/03-14-attw-g.log && pnpm size-limit 2>&1 | tee /tmp/03-14-size.log</automated>
  </verify>
  <done>
    - publint --strict exit 0 entrambi
    - attw --pack --profile=esm-only exit 0 entrambi
    - size-limit verifica routing < 6 KB gz e gateway/http < 8 KB gz
    - `.size-limit.json` ha 4 entries (core + mapper + routing + gateway/http)
    - Output size effettivo riportato (es. `routing: 4.2 KB / 6 KB = 70%`)
  </done>
</task>

<task type="auto">
  <name>Task 4: Coverage v8 ≥ 90% + verifiche regression</name>
  <read_first>
    - packages/routing/vitest.config.ts (verifica thresholds coverage v8 — replicare pattern F2 plan 02-12)
    - packages/gateway/vitest.config.ts
    - packages/mapper/vitest.config.ts (template thresholds)
  </read_first>
  <files>
    packages/routing/vitest.config.ts,
    packages/gateway/vitest.config.ts
  </files>
  <action>
    Aggiungi/conferma in `vitest.config.ts` di entrambi i package:
    ```typescript
    test: {
      coverage: {
        provider: 'v8',
        reporter: ['text', 'lcov', 'html'],
        include: ['src/**/*.ts'],
        exclude: ['src/**/*.test.ts', 'src/test-utils/**', 'src/__integration__/**'],
        thresholds: {
          lines: 90,
          functions: 90,
          branches: 85,
          statements: 90,
        },
      },
      // ... resto invariato
    }
    ```

    Esegui `pnpm --filter @gluezero/routing test --coverage` e `pnpm --filter @gluezero/gateway test --coverage`.

    Se coverage < 90%, aggiungi i test gap-filling (tipicamente edge case in route-resolver, http-gateway error path). Riporta in SUMMARY % effettivo + lista file < 90%.

    **Regression check finale (D-83 enforcement):**
    - `git diff --name-only main packages/core/` → vuoto
    - `git diff --name-only main packages/mapper/` → vuoto
    - `pnpm --filter @gluezero/core test` exit 0 (248 test invariati)
    - `pnpm --filter @gluezero/mapper test` exit 0 (149 test invariati)
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing test --coverage 2>&1 | tee /tmp/03-14-cov-r.log && pnpm --filter @gluezero/gateway test --coverage 2>&1 | tee /tmp/03-14-cov-g.log && pnpm --filter @gluezero/core test 2>&1 | tee /tmp/03-14-core.log && pnpm --filter @gluezero/mapper test 2>&1 | tee /tmp/03-14-mapper.log; grep -E "All files.*9[0-9]\." /tmp/03-14-cov-r.log && grep -E "All files.*9[0-9]\." /tmp/03-14-cov-g.log</automated>
  </verify>
  <done>
    - Coverage v8 ≥ 90% lines/functions/statements + ≥ 85% branches su entrambi
    - `pnpm --filter @gluezero/core test` 248/248 invariati
    - `pnpm --filter @gluezero/mapper test` 149/149 invariati
    - `git diff --name-only packages/core/ packages/mapper/` empty
    - Coverage % effettivo riportato in SUMMARY
  </done>
</task>

<task type="auto">
  <name>Task 5: Aggiornamento ROADMAP.md + STATE.md (Phase 3 complete)</name>
  <read_first>
    - .planning/ROADMAP.md (sezione Phase 3 — riempire status + closure date)
    - .planning/STATE.md (current_phase + completed phases)
    - .planning/phases/03-routing-server-gateway-http/03-13-SUMMARY.md (creato in 03-13 — coverage success criteria mapping)
  </read_first>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    **`.planning/ROADMAP.md`:**
    - Marca `[ ] Phase 3` → `[x] Phase 3 ✅ COMPLETE` con: closure date, plan count (14/14), test count, REQ-ID coperti (29/29)
    - Aggiorna sezione Phase 3 details con:
      ```
      **Status**: ✅ COMPLETE — ready for verification (gsd-verifier Phase 3)
      **Closure date**: <today>
      **Test coverage**: 14 routing test files / N test passing + N integration test
      **CI gates**: publint ✅, attw ESM-only ✅, size-limit (routing X KB / 6 KB, gateway/http Y KB / 8 KB) ✅, biome ✅, typecheck ✅
      **Open issues PRD §39 chiusi**: #5 (ROUTE-16), #6 (ROUTE-15), #7 (LIFE-02 ext F3), #8 (ROUTE-09)
      ```
    - Aggiorna tabella Progress: `Phase 3: 14/14 ✅ Complete & Verified <date>`

    **`.planning/STATE.md`:**
    - Aggiorna `current_phase`: 4 (next phase)
    - Aggiungi entry completed phases con metriche.

    Aggiorna anche `**Last updated:**` footer ROADMAP con la data corrente.
  </action>
  <verify>
    <automated>grep -E "Phase 3.*COMPLETE" /Users/omarmarzio/programming/prova\ AI/GlueZero/.planning/ROADMAP.md && grep -E "Phase 3.*14/14" /Users/omarmarzio/programming/prova\ AI/GlueZero/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 3 status = `[x] ✅ COMPLETE`
    - 14/14 plans
    - Closure date popolata
    - 4 open issue PRD §39 elencati come chiusi (#5/#6/#7/#8)
    - STATE.md current_phase = 4
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14-01 | Tampering | regression in F1/F2 modificato non intenzionale | mitigate | `git diff --name-only packages/core/ packages/mapper/` final check vuoto |
| T-03-14-02 | DoS | bundle bloat oltre budget | mitigate | size-limit hard fail in CI; budget 6/8 KB raised vs 5/6 RESEARCH (lesson learned F2 9.68 vs 5 stimato) |
| T-03-14-03 | Information Disclosure | type leak da attw failure | mitigate | attw --pack --profile=esm-only verifica tutte le risoluzioni Node16/bundler |
</threat_model>

<verification>
- 5 success criteria F3 ROADMAP coperti via 14 integration test (mapping in 03-13 SUMMARY)
- Coverage v8 ≥ 90% routing + gateway
- publint + attw + size-limit ✅ entrambi
- 0 changes a `packages/core/` e `packages/mapper/`
- 248 test core + 149 test mapper invariati
- ~145 test routing + gateway nuovi (cumulato F1+F2+F3 ~542 test)
- ROADMAP.md + STATE.md aggiornati
</verification>

<success_criteria>
1. Coverage v8 ≥ 90% routing + gateway (D-92)
2. publint --strict + attw --pack --profile=esm-only exit 0
3. size-limit budget rispettati (routing < 6 KB gz, gateway/http < 8 KB gz)
4. README italiano espansi entrambi
5. JSDoc completo API pubblica
6. Biome check exit 0
7. ROADMAP.md + STATE.md aggiornati
8. Phase 3 ufficialmente complete con 4 open issues PRD §39 chiusi
9. Regression F1/F2 zero (test passing + git diff vuoto)
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-14-SUMMARY.md` (final phase summary):
- Sezione "Phase 3 Status: ✅ COMPLETE"
- Sezione "Test totali F3" con breakdown (resolver: 17, executor: 13, handlers: 5, outcome: 8, gateway utils: 15, http-gateway+factory: 13, http-handler: 7, retry: 15, timeout: 4, idempotency: 8, dedupe: 8, backpressure: 10, auth: 9, circuit: 10, engine: 7, broker: 12, factory: 4, integration: 14)
- Sezione "Coverage v8" con % effettivo per package
- Sezione "Bundle size effettivo": routing X KB / 6 KB = Y%; gateway/http Z KB / 8 KB = W%
- Sezione "REQ-ID coperti F3" — 29 totali (ROUTE-01..16 + VAL-05 + ERR-02 + SEC-01..05 + TEST-01..03 + DOC-04 + LIFE-02)
- Sezione "Open Issues PRD §39 chiusi": #5, #6, #7, #8
- Sezione "Next steps": gsd-verifier Phase 3 → poi /gsd-discuss-phase 4 (Realtime SSE/WS)
- Sezione "5 success criteria F3 ROADMAP" → mapping con file integration test
</output>
