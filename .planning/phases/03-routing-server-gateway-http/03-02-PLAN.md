---
phase: 03-routing-server-gateway-http
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/routing/src/types/route-definition.ts
  - packages/routing/src/types/route-policies.ts
  - packages/routing/src/types/route-outcome.ts
  - packages/routing/src/types/routing-config.ts
  - packages/routing/src/types/index.ts
  - packages/gateway/src/http/types/gateway-config.ts
  - packages/gateway/src/http/types/http-strategies.ts
  - packages/gateway/src/http/types/http-error.ts
  - packages/gateway/src/http/types/index.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
  - ROUTE-08
  - ROUTE-10
  - ROUTE-11
  - ROUTE-13
  - VAL-05
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-05
tags:
  - types
  - phase-3
  - routing
  - gateway
  - foundation
must_haves:
  truths:
    - "Esiste una `RouteDefinition` discriminated union via `type: 'local' | 'http' | 'cache' | 'composite'` (D-60)."
    - "Tracks decisions D-62 (RoutingConfig+GatewayConfig type-only placeholder qui, declaration merging per BrokerConfig in 03-03/03-04), D-70 (IdempotencyConfig type con `mode:'auto'` + `headerName:'Idempotency-Key'`), D-77 (AbortSignal opzionale sui campi route/policy — F1 plan 04/05 baseline), D-98 (custom `request.serializer?: (canonical) => BodyInit` opt-in, default JSON content-type)."
    - "`RouteHttpDefinition` espone `request.method`, `request.url`, `request.queryMap`, `request.bodyMap`, `response.canonical`, `publishes.success/error`, `policies` (D-96, D-97)."
    - "`RouteCacheDefinition` e `RouteCompositeDefinition` definiti come tipi F3, con commento esplicito che adapter è F6."
    - "Esiste `RoutePolicies` con `timeout`, `retry: RetryPolicyConfig`, `dedupe: DedupePolicyConfig`, `concurrency: 'latest-only'|'serial'|'parallel'`, `backpressure: BackpressurePolicyConfig`, `auth`, `idempotency`, `error` (D-68, D-73, D-74, D-75)."
    - "`RouteOutcome` discriminata `{ ok: true; canonicalPayload; routeId } | { ok: false; error: BrokerError; routeId }` (analogo F2 `ValidationResult`)."
    - "`RoutingConfig` espone `multipleRoutesPolicy?: 'first-match' | 'priority-ordered' | 'all'` (D-66)."
    - "`GatewayConfig` espone `auth?`, `allowlist?: ReadonlyArray<string|RegExp>`, `defaults?` (timeout/retry/idempotency), `circuitBreaker?` (D-71, D-72, D-99)."
    - "`http-strategies.ts` definisce le 6 interface pluggable: `RetryStrategy`, `TimeoutStrategy`, `DedupeStrategy`, `BackpressureStrategy`, `AuthStrategy`, `IdempotencyStrategy` (D-68 Strategy Pattern)."
    - "`GatewayErrorCode` literal union con tutti i code D-80: `gateway.timeout`, `gateway.4xx`, `gateway.5xx`, `gateway.network`, `gateway.url.forbidden`, `response.validation.failed`, `route.required.missing`, `auth.expired`, `circuit.open`, `cache.not-implemented`."
    - "Type guards `isGatewayErrorCode(code)` esportati come pattern `isMappingErrorCode` di F2."
    - "Tutti i type sono in TS strict + `exactOptionalPropertyTypes: true` + `isolatedDeclarations: true` (PKG-02 invariato)."
    - "`pnpm --filter @gluezero/routing exec tsc --noEmit` exit 0; idem per gateway."
  artifacts:
    - path: "packages/routing/src/types/route-definition.ts"
      provides: "Discriminated union RouteDefinition + 4 sub-interface (Local/Http/Cache/Composite)"
      contains: "type: 'http'"
      min_lines: 80
    - path: "packages/routing/src/types/route-policies.ts"
      provides: "RoutePolicies + sub-config types (RetryPolicyConfig, DedupePolicyConfig, BackpressurePolicyConfig, ConcurrencyPolicy, AuthPolicyConfig, IdempotencyPolicyConfig, ErrorPolicyConfig)"
      contains: "RoutePolicies"
    - path: "packages/routing/src/types/route-outcome.ts"
      provides: "RouteOutcome discriminated + RouteResult/RouteError aliases"
      contains: "ok: true"
    - path: "packages/routing/src/types/routing-config.ts"
      provides: "RoutingConfig.multipleRoutesPolicy"
      contains: "multipleRoutesPolicy"
    - path: "packages/gateway/src/http/types/gateway-config.ts"
      provides: "GatewayConfig + AuthConfig + AllowlistEntry"
      contains: "GatewayConfig"
    - path: "packages/gateway/src/http/types/http-strategies.ts"
      provides: "Strategy interfaces (RetryStrategy/TimeoutStrategy/DedupeStrategy/BackpressureStrategy/AuthStrategy/IdempotencyStrategy)"
      contains: "RetryStrategy"
    - path: "packages/gateway/src/http/types/http-error.ts"
      provides: "GatewayErrorCode literal union + isGatewayErrorCode type guard"
      contains: "isGatewayErrorCode"
  key_links:
    - from: "packages/routing/src/types/route-definition.ts"
      to: "@gluezero/mapper"
      via: "import type { CanonicalSchemaId, OutputMap } from '@gluezero/mapper'"
      pattern: "from '@gluezero/mapper'"
    - from: "packages/routing/src/types/route-definition.ts"
      to: "@gluezero/gateway"
      via: "import type { GatewayConfig, RoutePolicies } reference"
      pattern: "RoutePolicies"
    - from: "packages/gateway/src/http/types/gateway-config.ts"
      to: "@gluezero/core"
      via: "import type { BrokerError } reference"
      pattern: "@gluezero/core"
---

<objective>
Pubblica i type contracts F3 per il routing engine e il gateway HTTP. Sono SOLO type-only file (zero runtime), sono il backbone su cui si baseranno tutti i plan successivi (resolver/executor/strategies/wrapper). Discriminated union `RouteDefinition` con i 4 type (`local`/`http`/`cache`/`composite`) chiude ROUTE-01..05 a livello tipo. `RoutePolicies` con i 7 sub-config copre ROUTE-08/10/11/13. `GatewayConfig` esporta superficie pubblica per consumer (allowlist + auth + defaults + circuitBreaker — chiude SEC-01..05). `http-strategies.ts` definisce le 6 interfaccia plug per Pattern 3 Strategy. `http-error.ts` codifica i 9+ error code D-80 come literal union per type-safety.

Purpose: il plan 03-03/03-04 (augment) e tutti i plan Wave 3-4 (resolver, executor, strategies) importano questi tipi. Definirli FIRST garantisce che i wave successivi possano partire in parallelo senza scavenger hunt sulla shape.

Output: 9 file type-only (4 routing + 4 gateway + 1 index opzionale per ognuno). `tsc --noEmit` deve passare. NO commit di runtime — questo plan è puro tipo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/mapper/src/types/canonical-schema.ts
@packages/mapper/src/types/input-output-map.ts
@packages/mapper/src/types/validator-adapter.ts
@packages/mapper/src/types/mapping-error.ts
@packages/core/src/types/broker-event.ts
@packages/core/src/types/error.ts

<interfaces>
<!-- Tipi F1/F2 da riusare nei type F3 -->

From packages/mapper/src/types/canonical-schema.ts:
```typescript
export type CanonicalSchemaId = string & { readonly [__canonicalSchemaIdBrand]: true }
export type FieldFailureMode = 'block' | 'skip' | 'fallback'
export interface FieldDescriptor { readonly type, readonly required?, readonly default?, readonly onFailure?, readonly description? }
export interface CanonicalSchema { readonly id: CanonicalSchemaId; readonly fields; readonly requires?; ... }
```

From packages/mapper/src/types/input-output-map.ts:
```typescript
export type InputMap = Readonly<Record<string, string | InputMapEntry>>
export type OutputMap = Readonly<Record<string, string | OutputMapEntry>>
```

From packages/mapper/src/types/validator-adapter.ts:
```typescript
export type ValidationResult = { ok: true } | { ok: false; issues: ReadonlyArray<ValidationIssue> }
export interface ValidatorAdapter { validate(schema: unknown, payload: unknown): ValidationResult }
```

From packages/mapper/src/types/mapping-error.ts (pattern per http-error.ts):
```typescript
export type MappingErrorCode = 'mapping.transform.failed' | 'mapping.field.missing' | ...
export function isMappingErrorCode(code: string): code is MappingErrorCode
```

From packages/core/src/types/error.ts:
```typescript
export interface BrokerError extends Error {
  readonly code: string
  readonly category: ErrorCategory  // include 'network' | 'auth' | 'config' | 'validation'
  readonly details?: Readonly<Record<string, unknown>>
  readonly originalError?: Error
  readonly routeId?: string
  readonly topic?: string
  readonly eventId?: string
}
```

From packages/core/src/types/broker-event.ts:
```typescript
export interface BrokerEvent { readonly id: EventId; readonly topic: string; readonly payload: unknown; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types routing — RouteDefinition discriminated union + RoutePolicies + RouteOutcome + RoutingConfig</name>
  <read_first>
    - packages/mapper/src/types/canonical-schema.ts (analogo: branded id, readonly fields, JSDoc)
    - packages/mapper/src/types/input-output-map.ts (per OutputMap reuse in queryMap/bodyMap)
    - packages/mapper/src/types/validator-adapter.ts (analogo discriminated `ValidationResult` per `RouteOutcome`)
    - packages/mapper/src/types/mapping-error.ts (analogo per literal union codes)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 494-501 CompiledRoute reference, lines 824-888 esempio config consumer)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione H — discriminated unions example)
    - .planning/phases/03-routing-server-gateway-http/03-CONTEXT.md (D-60..D-67, D-73..D-75, D-96..D-99)
  </read_first>
  <files>
    packages/routing/src/types/route-definition.ts,
    packages/routing/src/types/route-policies.ts,
    packages/routing/src/types/route-outcome.ts,
    packages/routing/src/types/routing-config.ts,
    packages/routing/src/types/index.ts
  </files>
  <action>
    Crea i 5 file type-only del package `@gluezero/routing` con header JSDoc italiano (replica stile `packages/mapper/src/types/canonical-schema.ts`).

    **`route-definition.ts` (≥ 80 LOC):**
    - Import `import type { CanonicalSchemaId, InputMap, OutputMap } from '@gluezero/mapper'` (NO type-only-loops — usa `import type`).
    - Import `import type { RoutePolicies } from './route-policies'`.
    - `interface RouteDefinitionBase { readonly id: string; readonly topic: string; readonly priority?: number; readonly policies?: RoutePolicies }`.
    - `interface RouteLocalDefinition extends RouteDefinitionBase { readonly type: 'local' }` (D-60, ROUTE-02).
    - `interface RouteHttpDefinition extends RouteDefinitionBase { readonly type: 'http'; readonly request: { readonly method: 'GET'|'POST'|'PATCH'|'PUT'|'DELETE'; readonly url: string; readonly queryMap?: OutputMap; readonly bodyMap?: OutputMap; readonly serializer?: (canonical: unknown) => BodyInit }; readonly response: { readonly canonical: CanonicalSchemaId | string }; readonly publishes?: { readonly success?: string; readonly error?: string } }` (ROUTE-03, D-96, D-97, D-98).
    - `interface RouteCacheDefinition extends RouteDefinitionBase { readonly type: 'cache'; readonly strategy: 'cache-first'|'network-first'|'cache-then-network'; readonly key?: (event: unknown) => string; readonly ttlMs?: number; /** F3 type-only — adapter F6 */ }` (ROUTE-04).
    - `interface RouteCompositeDefinition extends RouteDefinitionBase { readonly type: 'composite'; readonly steps: readonly { readonly type: 'cache'|'http'|'publish'; readonly route?: string }[]; /** F3 workflow — cache adapter F6 */ }` (ROUTE-05).
    - `export type RouteDefinition = RouteLocalDefinition | RouteHttpDefinition | RouteCacheDefinition | RouteCompositeDefinition`.
    - JSDoc esplicito su ogni interface (italiano) con riferimento al REQ-ID e D-XX.

    **`route-policies.ts`:**
    - `interface RetryPolicyConfig { readonly maxAttempts?: number; readonly baseDelayMs?: number; readonly maxDelayMs?: number; readonly retryOn?: ReadonlyArray<number> }` (D-69 default 3/300/10000).
    - `interface DedupePolicyConfig { readonly key?: (event: unknown) => string; readonly keyFrom?: ReadonlyArray<string> }` (D-74).
    - `type ConcurrencyPolicy = 'latest-only' | 'serial' | 'parallel'` (D-73).
    - `type BackpressurePolicyConfig = { readonly type: 'queue-bounded'; readonly max: number } | { readonly type: 'drop' } | { readonly type: 'throttle'; readonly perSec: number } | { readonly type: 'debounce'; readonly waitMs: number } | { readonly type: 'latest-only' } | { readonly type: 'merge' | 'coalesce' }` (D-75).
    - `interface AuthPolicyConfig { readonly bearer?: boolean; readonly custom?: (req: { url: string; headers: Record<string,string> }) => Promise<Record<string,string>> }` (SEC-01, D-72).
    - `interface IdempotencyPolicyConfig { readonly mode: 'auto' | 'manual' | false; readonly headerName?: string }` (D-70, SEC-03 default `'Idempotency-Key'`).
    - `interface ErrorPolicyConfig { readonly retryOn?: 'always'|'never'|'default' }` (override D-69).
    - `interface TimeoutPolicyConfig { readonly ms: number }` (default 30000ms).
    - `interface RoutePolicies { readonly timeout?: number | TimeoutPolicyConfig; readonly retry?: RetryPolicyConfig | false; readonly dedupe?: DedupePolicyConfig | false; readonly concurrency?: ConcurrencyPolicy; readonly backpressure?: BackpressurePolicyConfig; readonly auth?: AuthPolicyConfig | boolean; readonly idempotency?: IdempotencyPolicyConfig | false; readonly error?: ErrorPolicyConfig }` (D-68 Strategy Pattern container).

    **`route-outcome.ts`:**
    - Import `import type { BrokerError } from '@gluezero/core'`.
    - `type RouteOutcome = { readonly ok: true; readonly canonicalPayload: unknown; readonly routeId: string; readonly metadata?: { readonly httpStatus?: number; readonly attemptCount?: number; readonly origin?: 'cache' | 'remote' } } | { readonly ok: false; readonly error: BrokerError; readonly routeId: string }`.
    - `type RouteResult = Extract<RouteOutcome, { ok: true }>`.
    - `type RouteError = Extract<RouteOutcome, { ok: false }>`.

    **`routing-config.ts`:**
    - `type MultipleRoutesPolicy = 'first-match' | 'priority-ordered' | 'all'` (D-66).
    - `interface RoutingConfig { readonly multipleRoutesPolicy?: MultipleRoutesPolicy; readonly emitAmbiguousWarning?: boolean; readonly requiresRouteTopics?: ReadonlyArray<string> }` (default `'first-match'` + warning dev mode). Il campo `requiresRouteTopics` (BLOCKER 4 revision fix Plan 03-12, ROUTE-16/D-67) è l'opt-in esplicito per topic che richiedono route — bypassa il lookup canonical-registry quando il consumer NON usa la convenzione `<entity>.<action>.<status>` (PRD §11) per derivare schemaId dal topic. Vedi 03-12 `getCanonicalSchemaForTopic` rationale.

    **`index.ts` (barrel types):**
    - `export type * from './route-definition'`
    - `export type * from './route-policies'`
    - `export type * from './route-outcome'`
    - `export type * from './routing-config'`

    Conditional spread per `exactOptionalPropertyTypes` (Pattern S4): TUTTI i campi opzionali sono `readonly X?: T` (mai `readonly X: T | undefined`).

    NO export di runtime functions, NO valori, SOLO type-only.
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing exec tsc --noEmit 2>&1 | tee /tmp/route-types-tsc.log; grep -c "error TS" /tmp/route-types-tsc.log | grep -q '^0$'</automated>
  </verify>
  <done>
    - `tsc --noEmit` exit 0 sul package routing
    - 5 file creati con `grep -c "^export" packages/routing/src/types/*.ts` ≥ 14 export totali (RouteDefinition + 3 sub + RouteOutcome + RouteResult + RouteError + 7 policy types + RoutingConfig + MultipleRoutesPolicy)
    - `grep -E "type RouteDefinition" packages/routing/src/types/route-definition.ts` matcha la discriminated union
    - `grep "ConcurrencyPolicy" packages/routing/src/types/route-policies.ts` matcha la 3-string literal union
    - `grep "ok: true" packages/routing/src/types/route-outcome.ts` matcha
    - JSDoc italiano presente su ogni interface principale (`grep -E "/\*\*" packages/routing/src/types/route-definition.ts` ≥ 4)
    - NO `import` non `import type` (`grep -E "^import \{" packages/routing/src/types/*.ts` ritorna 0 — solo `import type`)
  </done>
</task>

<task type="auto">
  <name>Task 2: types gateway HTTP — GatewayConfig + Strategy interfaces + GatewayErrorCode</name>
  <read_first>
    - packages/mapper/src/types/validator-adapter.ts (analogo `ValidatorAdapter` interface — Pattern S6 per `AuthStrategy`/`RetryStrategy`)
    - packages/mapper/src/types/mapping-error.ts (analogo literal union + type guard `isMappingErrorCode`)
    - packages/core/src/types/error.ts (`BrokerError`, `ErrorCategory` da riusare con 'network'/'auth'/'config'/'validation')
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 60-74 BrokerError shape D-80, lines 537-548 GatewayContext, lines 670-682 Strategy pattern)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione H + sezione I — Strategy interface analogo `ValidatorAdapter`)
    - .planning/phases/03-routing-server-gateway-http/03-CONTEXT.md (D-68..D-72, D-99, D-80)
  </read_first>
  <files>
    packages/gateway/src/http/types/gateway-config.ts,
    packages/gateway/src/http/types/http-strategies.ts,
    packages/gateway/src/http/types/http-error.ts,
    packages/gateway/src/http/types/index.ts
  </files>
  <action>
    Crea 4 file type-only del package `@gluezero/gateway/http` con header JSDoc italiano.

    **`gateway-config.ts`:**
    - `type AllowlistEntry = string | RegExp` (D-71).
    - `interface AuthStrategyConfig { readonly getToken: () => Promise<string | undefined>; readonly refresh?: () => Promise<string>; readonly tokenCacheMs?: number }` (D-72, SEC-01, SEC-02).
    - `interface DefaultsConfig { readonly timeout?: number; readonly retry?: import('@gluezero/routing').RetryPolicyConfig; readonly idempotency?: import('@gluezero/routing').IdempotencyPolicyConfig; readonly dedupe?: import('@gluezero/routing').DedupePolicyConfig; readonly backpressure?: import('@gluezero/routing').BackpressurePolicyConfig }` (defaults applicati a tutte le route HTTP che non li overridano).
    - `interface CircuitBreakerConfig { readonly threshold: number; readonly cooldownMs: number; readonly halfOpenMaxRequests?: number }` (D-99 default 5/30000ms).
    - `interface GatewayConfig { readonly auth?: AuthStrategyConfig; readonly allowlist?: ReadonlyArray<AllowlistEntry>; readonly defaults?: DefaultsConfig; readonly circuitBreaker?: CircuitBreakerConfig | false }` (default `circuitBreaker: false`).
    - JSDoc esplicito su `AuthStrategyConfig.refresh`: "Single-flight (vedi RESEARCH §Pattern 5) — chiamato UNA volta su 401, le 4 fetch parallele coordinano".

    **`http-strategies.ts`:**
    - Import `import type { BrokerEvent, BrokerError } from '@gluezero/core'`.
    - Import `import type { RouteDefinition, RoutePolicies } from '@gluezero/routing'`.
    - `interface HttpRequestSpec { readonly method: string; readonly url: string; readonly headers: Record<string, string>; readonly body?: BodyInit }`.
    - `interface HttpResponseSpec { readonly ok: boolean; readonly status: number; readonly headers: Record<string,string>; readonly body: unknown }`.
    - `interface GatewayContext { readonly request: HttpRequestSpec; readonly route: RouteDefinition; readonly event: BrokerEvent; readonly signal: AbortSignal; attempt: number; response?: Response; parsedBody?: unknown; canonicalResponse?: unknown; error?: BrokerError }` (mutabile lungo la chain — RESEARCH lines 537-548).
    - `type GatewayMiddleware = (ctx: GatewayContext, next: () => Promise<void>) => Promise<void>` (Pattern Koa-compose).
    - `interface RetryStrategy { shouldRetry(response: Response | undefined, error: Error | undefined, attempt: number): boolean; delayMs(attempt: number, retryAfterHeader?: string | null): number }` (D-69).
    - `interface TimeoutStrategy { signal(timeoutMs: number): AbortSignal }` (D-68 wrapper su `AbortSignal.timeout`).
    - `interface DedupeStrategy { execute<T>(key: string, fn: () => Promise<T>): Promise<T>; size(): number; clear(): void }` (D-74 Promise singleton).
    - `interface BackpressureStrategy { schedule<T>(routeId: string, priority: 'critical'|'high'|'normal'|'low', task: () => Promise<T>): Promise<T>; queueLength(routeId: string): number }` (D-75 priority-aware).
    - `interface AuthStrategy { getToken(): Promise<string | undefined>; refresh?(): Promise<string>; isInflightRefresh(): boolean }` (D-72 single-flight).
    - `interface IdempotencyStrategy { generate(eventId: string): string; headerName(): string }` (D-70 nanoid + persistent on retry).
    - `interface CircuitBreakerStrategy { canExecute(routeId: string): boolean; recordSuccess(routeId: string): void; recordFailure(routeId: string): void; getState(routeId: string): 'closed' | 'open' | 'half-open' }` (D-99 opt-in).

    **`http-error.ts`:**
    - `type GatewayErrorCode = 'gateway.timeout' | 'gateway.4xx' | 'gateway.5xx' | 'gateway.network' | 'gateway.url.forbidden' | 'response.validation.failed' | 'route.required.missing' | 'auth.expired' | 'circuit.open' | 'cache.not-implemented' | 'route.id.duplicate'` (D-80 + D-87 unregister + cache stub).
    - `function isGatewayErrorCode(code: string): code is GatewayErrorCode` con type guard set-based (replica `isMappingErrorCode` di F2).
    - JSDoc per ogni code spiega: quando publishato + quale category + retry-eligibility (per riferimento D-69).

    **`index.ts` (barrel types):**
    - `export type * from './gateway-config'`
    - `export type * from './http-strategies'`
    - `export type * from './http-error'`
    - `export { isGatewayErrorCode } from './http-error'` (runtime — single guard ok)

    Conditional spread per `exactOptionalPropertyTypes`. Tutti type-only eccetto `isGatewayErrorCode` (runtime function).
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/gateway exec tsc --noEmit 2>&1 | tee /tmp/gateway-types-tsc.log; grep -c "error TS" /tmp/gateway-types-tsc.log | grep -q '^0$'</automated>
  </verify>
  <done>
    - `tsc --noEmit` exit 0 sul package gateway
    - 4 file creati
    - `grep -c "^export interface" packages/gateway/src/http/types/http-strategies.ts` ≥ 8 (le 7 strategy + GatewayContext)
    - `grep "type GatewayErrorCode" packages/gateway/src/http/types/http-error.ts` matcha
    - `grep "isGatewayErrorCode" packages/gateway/src/http/types/http-error.ts` matcha funzione + type guard
    - `grep "AuthStrategyConfig" packages/gateway/src/http/types/gateway-config.ts` matcha
    - `grep "circuitBreaker" packages/gateway/src/http/types/gateway-config.ts` matcha
    - JSDoc presenti (`grep -c "/\*\*" packages/gateway/src/http/types/*.ts` ≥ 12)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan boundary (no runtime) | I file di tipo non sono runtime — non c'è trust boundary attivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | RouteDefinition mutability post-register | mitigate | Tutti i field `readonly` (replica pattern F2 CanonicalSchema). Runtime `deepFreeze` al register sarà responsabilità del plan 03-05 (resolver) — qui solo type-level enforcement |
| T-03-02-02 | Tampering | type confusion via stringa generica | mitigate | `CanonicalSchemaId` branded type da F2 importato; `routeId` plain string ma sempre validato a runtime in plan 03-05 |
| T-03-02-03 | Information Disclosure | GatewayContext esponendo Authorization header | accept | Type permette `headers: Record<string,string>`. La sanitizzazione è responsabilità di Inspector F6 (deferred) |
| T-03-02-04 | DoS | RoutePolicies con `maxAttempts: Infinity` | accept | Type permette qualsiasi number; warning runtime in dev mode è plan 03-09 (retry-strategy) responsibility |
</threat_model>

<verification>
- `pnpm --filter @gluezero/routing exec tsc --noEmit` exit 0
- `pnpm --filter @gluezero/gateway exec tsc --noEmit` exit 0
- TUTTI gli export sono `export type` o `export interface` o `export function isGatewayErrorCode` — NESSUN export runtime extra
- Pattern S4 conditional spread NON serve qui (siamo in type-only — l'enforcement è sui plan runtime successivi)
- Bundle size unaffected (i tipi non producono runtime — dts only)
</verification>

<success_criteria>
1. 9 file type-only creati (5 routing + 4 gateway)
2. `tsc --noEmit` esce 0 su entrambi i package
3. `RouteDefinition` discriminated union completa con 4 type
4. `GatewayConfig`, `RetryStrategy`, `AuthStrategy`, `DedupeStrategy`, `BackpressureStrategy`, `IdempotencyStrategy`, `TimeoutStrategy`, `CircuitBreakerStrategy` interface esportate
5. `GatewayErrorCode` literal union ≥ 11 codes + `isGatewayErrorCode` type guard
6. JSDoc italiano con riferimento a REQ-ID/D-XX su ogni type principale
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-02-SUMMARY.md` con:
- Lista completa dei type esportati
- Conferma `tsc --noEmit` exit 0 entrambi i package
- Riferimenti REQ-ID coperti a livello tipo (ROUTE-01..05, ROUTE-08, ROUTE-10, ROUTE-11, ROUTE-13, VAL-05 (response.canonical), SEC-01..05)
- Note per i plan downstream (03-03 augment riusa questi tipi via declaration merging)
</output>
