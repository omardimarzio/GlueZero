---
phase: 03-routing-server-gateway-http
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/routing/package.json
  - packages/routing/tsup.config.ts
  - packages/routing/tsconfig.json
  - packages/routing/vitest.config.ts
  - packages/routing/README.md
  - packages/routing/src/index.ts
  - packages/gateway/package.json
  - packages/gateway/tsup.config.ts
  - packages/gateway/tsconfig.json
  - packages/gateway/vitest.config.ts
  - packages/gateway/README.md
  - packages/gateway/src/index.ts
  - packages/gateway/src/http/index.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - DOC-04
tags:
  - bootstrap
  - routing-package
  - gateway-package
  - foundation
  - phase-3-gate
  - subpath-exports
must_haves:
  truths:
    - "Il package `@gluezero/routing` ha tutti i file di config allineati a `@gluezero/mapper` con sideEffects array per `augment.ts`."
    - "Il package `@gluezero/gateway` ha config con SUBPATH EXPORTS (`./http` Phase 3) e `tsup` multi-entry."
    - "`pnpm --filter @gluezero/routing build` produce `dist/index.js` + `dist/index.d.ts`."
    - "`pnpm --filter @gluezero/gateway build` produce `dist/index.js` + `dist/http/index.js` (multi-entry)."
    - "Workspace pnpm linka `@gluezero/core` e `@gluezero/mapper` come dep di entrambi i package F3."
    - "Bundle size-limit per `@gluezero/routing` (6 KB gzip) e `@gluezero/gateway/http` (8 KB gzip) aggiunti al root `package.json`."
    - "README routing e gateway di skeleton creati in italiano (preparazione DOC-04 finale al plan 03-14)."
  artifacts:
    - path: "packages/routing/package.json"
      provides: "Package config @gluezero/routing con dep workspace:* su @gluezero/core e @gluezero/mapper, sideEffects array"
      contains: "@gluezero/routing"
    - path: "packages/gateway/package.json"
      provides: "Package config @gluezero/gateway con subpath exports `./http`"
      contains: "./http"
    - path: "packages/gateway/tsup.config.ts"
      provides: "Build config tsup multi-entry: index + http/index"
      contains: "http/index"
    - path: "packages/routing/src/index.ts"
      provides: "Barrel skeleton routing"
      contains: "@packageDocumentation"
    - path: "packages/gateway/src/http/index.ts"
      provides: "Barrel HTTP subpath"
      contains: "@packageDocumentation"
  key_links:
    - from: "packages/routing/package.json"
      to: "packages/mapper"
      via: "dependencies.@gluezero/mapper: workspace:*"
      pattern: "workspace:\\*"
    - from: "packages/gateway/package.json"
      to: "subpath exports"
      via: "exports['./http']"
      pattern: "\"\\./http\""
    - from: "package.json (root)"
      to: "packages/routing/dist/index.js"
      via: "size-limit entry per @gluezero/routing (6 KB gzip)"
      pattern: "packages/routing/dist/index.js"
---

<objective>
Bootstrap dei package `@gluezero/routing` e `@gluezero/gateway` (placeholder F1 plan 01-01 → package buildable F3). Replica precisa della struttura di `@gluezero/mapper` (proven F2) con tutti i file di config richiesti, dipendenze `workspace:*` su `@gluezero/core` E `@gluezero/mapper`, side-effects array per i futuri `augment.ts` (declaration merging — D-93/D-94/D-95), e SUBPATH EXPORTS per `@gluezero/gateway` (`./http` Phase 3, placeholder `./sse-ws` riservato a Phase 4).

Purpose: tutti i plan 03-02 in poi assumono che entrambi i package siano buildable, testabile, linkato al workspace pnpm con risoluzione `@gluezero/mapper` correttamente. Il subpath exports è cruciale per il budget separato (`gateway/http` 8 KB vs aggregato gateway dopo F4) e per dependency boundary chiaro (RESEARCH §"Subpath Exports Recommendation").

Output: 8 file config + 2 README skeleton + 3 file index.ts skeleton + 2 size-limit entries root. `pnpm install` aggiorna `pnpm-lock.yaml`. Build e test passano. Budget RAISED rispetto al RESEARCH iniziale (5/6 KB → 6/8 KB) per lesson learned F2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/mapper/package.json
@packages/mapper/tsup.config.ts
@packages/mapper/tsconfig.json
@packages/mapper/vitest.config.ts
@packages/mapper/src/index.ts
@packages/routing/package.json
@packages/gateway/package.json
@package.json
@pnpm-workspace.yaml
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap @gluezero/routing config + README + index skeleton</name>
  <read_first>
    - packages/mapper/package.json (template — replicare struttura completa)
    - packages/mapper/tsup.config.ts (template — entry+format+dts+target+platform+external)
    - packages/mapper/tsconfig.json (template — extends tsconfig.base.json)
    - packages/mapper/vitest.config.ts (template — name + jsdom + coverage v8 thresholds)
    - packages/mapper/src/index.ts (template — header @packageDocumentation italiano)
    - packages/routing/package.json (placeholder esistente — sostituire)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (Pattern S1 side-effect array)
  </read_first>
  <files>
    packages/routing/package.json,
    packages/routing/tsup.config.ts,
    packages/routing/tsconfig.json,
    packages/routing/vitest.config.ts,
    packages/routing/README.md,
    packages/routing/src/index.ts
  </files>
  <action>
    Replica precisa di `@gluezero/mapper` per `@gluezero/routing` (D-93 augment pattern).

    **`packages/routing/package.json`**:
    - `name: "@gluezero/routing"`, `version: "0.0.0"`, `type: "module"`, `license: "MIT"`
    - `main`/`module`/`types`/`exports` coerenti con mapper (single entry `./dist/index.js`)
    - `files: ["dist", "README.md", "LICENSE"]`
    - `sideEffects: ["./dist/augment.js", "./src/augment.ts", "**/augment.js", "**/augment.ts"]` (Pattern S1 anti tree-shaking)
    - `engines.node: ">=20"`, `publishConfig: { access: "public", provenance: true }`
    - `scripts`: build (`tsup`), test (`vitest run --passWithNoTests`), test:watch, test:coverage, typecheck (`tsc --noEmit`), clean
    - `dependencies`: `@gluezero/core: workspace:*`, `@gluezero/mapper: workspace:*`, `nanoid: 5.1.9`, `valibot: 1.3.1`
    - `devDependencies`: `tsup: 8.5.1`, `typescript: 6.0.3`, `vitest: 4.1.5`, `jsdom: 29.1.0`

    **`packages/routing/tsup.config.ts`** replica mapper:
    - `entry: { index: 'src/index.ts' }`
    - `format: ['esm']`, `dts: true`, `sourcemap: true`, `clean: true`, `target: 'es2022'`, `platform: 'browser'`, `splitting: false`, `treeshake: true`
    - `external: [/^node:/, '@gluezero/core', '@gluezero/mapper']`

    **`packages/routing/tsconfig.json`** — `extends: '../../tsconfig.base.json'`, `compilerOptions: { outDir: './dist', rootDir: './src' }`, `include: ['src/**/*']`, `exclude: ['node_modules', 'dist', '**/*.test.ts', 'src/test-utils', 'src/__integration__']`.

    **`packages/routing/vitest.config.ts`** — `name: '@gluezero/routing'`, `environment: 'jsdom'`, `globals: true`, `coverage: { provider: 'v8', thresholds: { lines: 90, functions: 90, branches: 90, statements: 90 } }`.

    **`packages/routing/README.md`** italiano skeleton:
    - `# @gluezero/routing` + tagline "Routing engine dichiarativo per GlueZero (Phase 3)"
    - "Stato": `Phase 3 in sviluppo. API non stabile.`
    - "Cosa contiene": RouteDefinition discriminata, RouteResolver dispatch table pre-compilata, RouteExecutor (local|http|cache|composite), RouterBroker composition wrapper di MapperBroker
    - "Vincolo D-83": `Zero modifiche a packages/core/ runtime e packages/mapper/ runtime. Estensione tramite composition wrapper + TS declaration merging.`
    - "Documentazione": `Vedi prd.md §17 e §28. DOC-04 completo al plan 03-14.`
    - "Licenza": `MIT`

    **`packages/routing/src/index.ts`** italiano skeleton:
    - Header `@packageDocumentation` con riferimento PRD §17, D-83, D-93/D-94/D-95
    - Commento `// Plan 03-02 riempirà i type exports; plan 03-03 il side-effect import './augment'; plan 03-12 il runtime export RouterBroker + createRouterBroker`
    - `export {}` (file modulare ESM)
  </action>
  <acceptance_criteria>
    - `grep -q '"@gluezero/mapper": "workspace:\\*"' packages/routing/package.json`
    - `grep -q '"./dist/augment.js"' packages/routing/package.json`
    - `grep -q "@gluezero/mapper" packages/routing/tsup.config.ts`
    - `grep -q "tsconfig.base.json" packages/routing/tsconfig.json`
    - `grep -q "@gluezero/routing" packages/routing/vitest.config.ts`
    - `pnpm --filter @gluezero/routing build` exit 0; `ls packages/routing/dist/index.js packages/routing/dist/index.d.ts` exit 0
    - `pnpm --filter @gluezero/routing test --passWithNoTests` exit 0
    - `pnpm --filter @gluezero/routing typecheck` exit 0
    - README routing in italiano contiene "Stato", "Cosa contiene", "Vincolo D-83"
  </acceptance_criteria>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @gluezero/routing build &amp;&amp; pnpm --filter @gluezero/routing typecheck &amp;&amp; pnpm --filter @gluezero/routing test --passWithNoTests</automated>
  </verify>
  <done>
    Package `@gluezero/routing` buildabile, testabile, typechecked. `dist/index.js` e `dist/index.d.ts` esistono.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap @gluezero/gateway con SUBPATH EXPORTS (./http) — multi-entry tsup</name>
  <read_first>
    - packages/mapper/package.json (template base)
    - packages/mapper/tsup.config.ts (template per estensione multi-entry)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (sezione "Subpath Exports Recommendation" lines 322-352)
    - packages/gateway/package.json (placeholder esistente)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (Pattern S1 + S9)
  </read_first>
  <files>
    packages/gateway/package.json,
    packages/gateway/tsup.config.ts,
    packages/gateway/tsconfig.json,
    packages/gateway/vitest.config.ts,
    packages/gateway/README.md,
    packages/gateway/src/index.ts,
    packages/gateway/src/http/index.ts
  </files>
  <action>
    Replica `@gluezero/mapper` con MULTI-ENTRY (subpath exports) per separare F3 HTTP da F4 SSE/WS.

    **`packages/gateway/package.json`**:
    - `name: "@gluezero/gateway"`, `version: "0.0.0"`, `type: "module"`, `license: "MIT"`
    - `main: "./dist/index.js"`, `module: "./dist/index.js"`, `types: "./dist/index.d.ts"`
    - `exports`:
      ```json
      {
        ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
        "./http": { "types": "./dist/http/index.d.ts", "import": "./dist/http/index.js" },
        "./package.json": "./package.json"
      }
      ```
    - `files: ["dist", "README.md", "LICENSE"]`
    - `sideEffects: ["./dist/augment.js", "./src/augment.ts", "**/augment.js", "**/augment.ts"]`
    - `engines.node: ">=20"`, `publishConfig: { access: "public", provenance: true }`
    - `scripts` identici a mapper
    - `dependencies`: `@gluezero/core: workspace:*`, `@gluezero/mapper: workspace:*`, `nanoid: 5.1.9`, `valibot: 1.3.1`
    - `devDependencies`: `tsup: 8.5.1`, `typescript: 6.0.3`, `vitest: 4.1.5`, `jsdom: 29.1.0`

    **`packages/gateway/tsup.config.ts`** MULTI-ENTRY:
    ```typescript
    import { defineConfig } from 'tsup'
    export default defineConfig({
      entry: { index: 'src/index.ts', 'http/index': 'src/http/index.ts' },
      format: ['esm'],
      dts: true,
      sourcemap: true,
      clean: true,
      target: 'es2022',
      platform: 'browser',
      splitting: false,
      treeshake: true,
      external: [/^node:/, '@gluezero/core', '@gluezero/mapper'],
    })
    ```

    **`packages/gateway/tsconfig.json`** replica mapper. **`packages/gateway/vitest.config.ts`** con `name: '@gluezero/gateway'`.

    **`packages/gateway/README.md`** italiano:
    - `# @gluezero/gateway` + tagline "HTTP Gateway centralizzato + adapter realtime per GlueZero (Phase 3 + 4)"
    - "Stato": `Phase 3 in sviluppo (HTTP). Phase 4 (SSE/WebSocket) successiva.`
    - "Subpath exports": `import { ... } from '@gluezero/gateway/http'`
    - "Cosa contiene (F3 — `/http`)": HttpGateway policy chain (auth/idempotency/dedupe/backpressure/retry/timeout), 7 Strategy primitives (Retry/Timeout/Dedupe/Backpressure/Auth/Idempotency/CircuitBreaker), URL allowlist, retry-after parser
    - "Vincolo D-83": `Zero modifiche a packages/core/ e packages/mapper/.`
    - "Documentazione": `Vedi prd.md §18, §23, §26. DOC-04 completo al plan 03-14.`
    - "Licenza": `MIT`

    **`packages/gateway/src/index.ts`** umbrella skeleton:
    - `@packageDocumentation` italiano + riferimento RESEARCH §"Subpath Exports"
    - Commento `// Plan 03-04 aggiunge augment.ts side-effect + re-export type da ./http`
    - `export {}`

    **`packages/gateway/src/http/index.ts`** HTTP subpath skeleton:
    - `@packageDocumentation` italiano + PRD §18 + §26
    - Commento `// Plan 03-04 popola con type re-export http-strategies, gateway-config, http-error`
    - Commento `// Plan 03-08+ popola con runtime export HttpGateway + Strategy implementations`
    - `export {}`
  </action>
  <acceptance_criteria>
    - `grep -q '"./http"' packages/gateway/package.json`
    - `grep -q '"./dist/http/index.d.ts"' packages/gateway/package.json`
    - `grep -q "http/index" packages/gateway/tsup.config.ts`
    - `grep -q "@gluezero/mapper" packages/gateway/tsup.config.ts`
    - `pnpm --filter @gluezero/gateway build` exit 0
    - `ls packages/gateway/dist/index.js packages/gateway/dist/http/index.js packages/gateway/dist/index.d.ts packages/gateway/dist/http/index.d.ts` exit 0
    - `pnpm --filter @gluezero/gateway typecheck` exit 0
    - `pnpm --filter @gluezero/gateway test --passWithNoTests` exit 0
    - README contiene "Subpath exports" + bullet `@gluezero/gateway/http`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gluezero/gateway build &amp;&amp; ls packages/gateway/dist/http/index.js &amp;&amp; pnpm --filter @gluezero/gateway typecheck</automated>
  </verify>
  <done>
    Package `@gluezero/gateway` buildabile con multi-entry. `dist/index.js` e `dist/http/index.js` (+ `.d.ts`) esistono. Subpath exports configurati. Workspace pnpm linka `@gluezero/mapper`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aggiungi size-limit entries root per @gluezero/routing (6 KB) e @gluezero/gateway/http (8 KB)</name>
  <read_first>
    - package.json (root — sezione `size-limit` esistente con entries F1/F2)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (sezione "Plan Topology" + "Assumptions A8/A9": budget RAISED da 5/6 a 6/8 KB per F2 lesson learned)
  </read_first>
  <files>package.json</files>
  <action>
    Estendi sezione `size-limit` del root `package.json` con due nuove entries F3:
    ```json
    {
      "name": "@gluezero/routing (gzip)",
      "path": "packages/routing/dist/index.js",
      "limit": "6 KB",
      "gzip": true
    },
    {
      "name": "@gluezero/gateway/http (gzip)",
      "path": "packages/gateway/dist/http/index.js",
      "limit": "8 KB",
      "gzip": true
    }
    ```

    Budget rationale (RESEARCH A8/A9): RAISED rispetto a stima iniziale per F2 lesson learned (mapper 5 KB stimato → 9.68 KB reale → raised 12 KB). Routing ha resolver + executor + 4 handler + 3 strategy + outcome collector. Gateway/http include 7 strategy + policy-chain + url-allowlist + retry-after-parser. Plan 03-14 verifica e raise se serve.

    NON modificare entries F1/F2 esistenti.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); const sl=p['size-limit']; const r=sl.find(e=&gt;e.name==='@gluezero/routing (gzip)'); const g=sl.find(e=&gt;e.name==='@gluezero/gateway/http (gzip)'); if(!r||!g||r.limit!=='6 KB'||g.limit!=='8 KB'||g.path!=='packages/gateway/dist/http/index.js') process.exit(1);"` exit 0
    - Entries F1/F2 esistenti per `@gluezero/core` e `@gluezero/mapper` invariate
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); const sl=p['size-limit']; const r=sl.find(e=>e.name==='@gluezero/routing (gzip)'); const g=sl.find(e=>e.name==='@gluezero/gateway/http (gzip)'); if(!r||!g||r.limit!=='6 KB'||g.limit!=='8 KB') process.exit(1);"</automated>
  </verify>
  <done>
    Root `package.json` ha 2 nuove size-limit entries per i package F3 con budget 6 KB / 8 KB gzip.
  </done>
</task>

</tasks>

<verification>
- Build entrambi i package F3: `pnpm --filter @gluezero/routing build && pnpm --filter @gluezero/gateway build`
- Multi-entry conferma: `ls packages/gateway/dist/http/index.js packages/gateway/dist/http/index.d.ts`
- Workspace risoluzione: `pnpm --filter @gluezero/routing exec -- node -e "console.log(require.resolve('@gluezero/mapper'))"` exit 0
- Size-limit entries root: 2 nuove entries F3 con budget 6/8 KB gzip
- Type check entrambi i package: `pnpm --filter @gluezero/routing typecheck && pnpm --filter @gluezero/gateway typecheck`
</verification>

<success_criteria>
- Plan 03-02..03-14 possono assumere `@gluezero/routing` e `@gluezero/gateway` buildable + linkati al workspace pnpm
- Subpath exports `@gluezero/gateway/http` funzionante (`import from '@gluezero/gateway/http'` non throw)
- Bundle budget 6 KB / 8 KB gzip configurati per CI gates plan 03-14
- Side-effects array protegge i futuri augment.ts plan 03-03 / 03-04 dal tree-shaking
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-01-SUMMARY.md` con:
- Lista artefatti creati (8 config + 2 README + 3 index)
- Comandi build/test/typecheck verificati
- Versioni dep installate (nanoid 5.1.9, valibot 1.3.1, tsup 8.5.1, vitest 4.1.5, typescript 6.0.3, jsdom 29.1.0)
- Bundle size baseline (anche se 0 KB perché vuoti) per confronto plan 03-14
- Subpath exports configurati e verificati
</output>
