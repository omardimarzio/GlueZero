---
phase: 03-routing-server-gateway-http
plan: 05
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
files_modified:
  - packages/routing/src/route-resolver.ts
  - packages/routing/src/route-resolver.test.ts
  - packages/routing/src/strategies/first-match.ts
  - packages/routing/src/strategies/priority-ordered.ts
  - packages/routing/src/strategies/all-broadcast.ts
  - packages/routing/src/strategies/index.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-15
  - ROUTE-16
tags:
  - resolver
  - dispatch-table
  - phase-3
  - routing
must_haves:
  truths:
    - "`RouteResolver.register(def, options?: { ownerId?, strict? })` pre-compila la `RouteDefinition` in `CompiledRoute` e la inserisce nel dispatch table — D-64."
    - "Il dispatch table riusa la wildcard machinery di F1 — `RouteResolver` istanzia un proprio `Map<topicPattern, CompiledRoute[]>` PIÙ un `TopicTrie<CompiledRoute>` riusato/duplicato per O(segments) match (decisione tactical: ricreare un trie standalone interno al package routing per evitare cross-package internal coupling — D-08 pattern già esposto nei test analogo, copia ≤120 LOC)."
    - "Throw `BrokerError 'route.id.duplicate'` su id duplicato in strict mode (default opt-in true)."
    - "`RouteResolver.unregister(routeId)` rimuove dal dispatch table E dal trie."
    - "`RouteResolver.unregisterByOwner(ownerId)` rimuove TUTTE le route registrate con quell'ownerId — chiusura LIFE-02 ext F3 (D-86)."
    - "`RouteResolver.resolve(topic, multipleRoutesPolicy)` ritorna `CompiledRoute[]` rispettando D-66: `'first-match'` (default), `'priority-ordered'`, `'all'`."
    - "In dev mode (`debug: true`) emette `routing.ambiguous` come BrokerEvent CORE quando matchano N>1 route con `'first-match'` (D-66)."
    - "Il `compile(def)` pre-curria `requestBuilder` (closure su `route.request.queryMap`) per route http — riuso `mapper.mapToShape` di F2 (D-96)."
    - "Test deterministici: register/unregister/resolve/wildcard match/priority/all/duplicate-id."
  artifacts:
    - path: "packages/routing/src/route-resolver.ts"
      provides: "RouteResolver class con register/unregister/unregisterByOwner/resolve + compile + dispatch table"
      contains: "class RouteResolver"
      min_lines: 180
    - path: "packages/routing/src/route-resolver.test.ts"
      provides: "Test deterministici per resolver"
      contains: "describe('RouteResolver'"
    - path: "packages/routing/src/strategies/first-match.ts"
      provides: "first-match strategy (default ROUTE-15)"
      contains: "firstMatch"
    - path: "packages/routing/src/strategies/priority-ordered.ts"
      provides: "priority-ordered strategy"
      contains: "priorityOrdered"
    - path: "packages/routing/src/strategies/all-broadcast.ts"
      provides: "all-broadcast strategy"
      contains: "allBroadcast"
  key_links:
    - from: "packages/routing/src/route-resolver.ts"
      to: "TopicTrie wildcard match"
      via: "internal segmented trie OR copy-of @sembridge/core topic-matcher pattern"
      pattern: "split\\('\\.'\\)"
    - from: "packages/routing/src/route-resolver.ts"
      to: "strategies/first-match"
      via: "import { firstMatch } from './strategies/first-match'"
      pattern: "from './strategies/first-match'"
    - from: "packages/routing/src/route-resolver.ts"
      to: "createBrokerError"
      via: "import { createBrokerError } from '@sembridge/core'"
      pattern: "createBrokerError"
---

<objective>
Implementa il `RouteResolver` (dispatch table pre-compilata D-64) + le 3 multi-route policy `'first-match'`/`'priority-ordered'`/`'all'` (D-66 — chiusura ROUTE-15 PRD §39 #6) + `unregisterByOwner` cascade per LIFE-02 ext F3 (D-86 — chiusura PRD §39 #7). Il resolver è il primo modulo runtime di `@sembridge/routing` (post-types/augment): trasforma `RouteDefinition` dichiarative in `CompiledRoute` con request builder pre-curried e fornisce O(segments) lookup via wildcard trie (riuso pattern `TopicTrie` segmentato di F1 D-08).

**Decisione TopicTrie reuse (Open Question Q1 RESEARCH):** poiché `TopicTrie` di F1 NON è esposto pubblicamente da `@sembridge/core`, copio l'implementazione interna in `packages/routing/src/internal/topic-trie.ts` (≤ 120 LOC, IDENTICA a `packages/core/src/core/topic-matcher.ts`) — preferisco questa rispetto a esporre internal di core (cross-package coupling internal). Pattern già provato in monorepo TS: piccolo duplicato accettabile per dependency boundary chiaro. Documenta nel JSDoc che è una copia mirror di F1 e va rimossa quando F1 esporrà `@sembridge/core/internal`.

Purpose: Plan 03-06 (executor) e 03-12 (RouterBroker wrapper) usano `RouteResolver.resolve(topic)` come primo passo del flow. Il pre-compile evita Pitfall 1 (compilation hot-path).

Output: 5 file (1 resolver + 1 test + 3 strategies + 1 index strategies + 1 internal trie copia mirror).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/core/src/core/topic-matcher.ts
@packages/core/src/core/broker-error.ts
@packages/mapper/src/canonical-registry.ts
@packages/mapper/src/alias-registry.ts
@packages/routing/src/types/route-definition.ts
@packages/routing/src/types/route-policies.ts
@packages/routing/src/types/routing-config.ts

<interfaces>
From packages/core/src/core/topic-matcher.ts:
```typescript
export class TopicTrie<T> {
  insert(pattern: string, item: T): void
  remove(pattern: string, item: T): boolean
  match(topic: string): T[]
  collectAllPatterns(): string[]
}
export function validateTopic(topic: string): void
export function validateTopicPattern(pattern: string): void
```

From packages/core/src/core/broker-error.ts:
```typescript
export function createBrokerError(params: { code, category, message, details?, originalError?, routeId?, topic?, eventId? }): BrokerError
```

From packages/mapper/src/canonical-registry.ts:
```typescript
register(schema: CanonicalSchema, options: RegisterOptions = {}): boolean
unregisterByOwner(ownerId: string): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal topic-trie copia mirror + RouteResolver con compile + dispatch table + ownership tracking</name>
  <read_first>
    - packages/core/src/core/topic-matcher.ts (template trie 155 LOC — copiare interamente come internal mirror)
    - packages/mapper/src/canonical-registry.ts (lines 100-160 — register/listener/ownership pattern)
    - packages/mapper/src/broker-mapper-wrapper.ts (lines 148-152 OwnershipEntry pattern — `Map<ownerId, Set<routeId>>`)
    - packages/core/src/core/broker-error.ts (createBrokerError factory)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 486-528 Pattern 2 dispatch table esempio code)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione D — resolver pattern)
  </read_first>
  <behavior>
    - Test 1: `register(def)` con id univoco ritorna `RouteRegistration { id, unregister }`
    - Test 2: `register(def)` con id duplicato in strict mode → throw `BrokerError 'route.id.duplicate'`
    - Test 3: `register(def)` con id duplicato in non-strict (default false) → ritorna idempotent (no throw, no insert duplicato)
    - Test 4: `resolve('weather.requested')` con 1 route registrata → ritorna `[CompiledRoute]`
    - Test 5: `resolve('weather.requested')` con 0 route → ritorna `[]`
    - Test 6: `resolve('weather.alert.failed')` con route `'weather.*.failed'` → match wildcard
    - Test 7: `resolve` con N=3 route + `multipleRoutesPolicy: 'first-match'` → ritorna `[primo]`
    - Test 8: `resolve` con N=3 route + `multipleRoutesPolicy: 'priority-ordered'` → ritorna `[priority più alta]`
    - Test 9: `resolve` con N=3 route + `multipleRoutesPolicy: 'all'` → ritorna `[tutte e 3]`
    - Test 10: `unregister(routeId)` rimuove dal dispatch table — `resolve(topic)` post-unregister ritorna `[]`
    - Test 11: `unregisterByOwner('plugin-A')` rimuove TUTTE le route con `ownerId: 'plugin-A'` (registra 3, unregister byOwner rimuove tutte e 3, lascia altre)
    - Test 12: `compile(def)` pre-curria `requestBuilder` per `RouteHttpDefinition` (verify: `compiledRoute.requestBuilder({location: 'Roma'})` ritorna oggetto con `queryMap` applicato — mock di mapper.mapToShape)
    - Test 13: dev mode con N>1 route + `'first-match'` → emette `routing.ambiguous` event (verify via mock callback)
  </behavior>
  <files>
    packages/routing/src/internal/topic-trie.ts,
    packages/routing/src/route-resolver.ts,
    packages/routing/src/route-resolver.test.ts
  </files>
  <action>
    **`packages/routing/src/internal/topic-trie.ts` (≤120 LOC, mirror di `packages/core/src/core/topic-matcher.ts`):**
    - Copia letterale di `TopicTrie<T>` da F1 (`insert/remove/match/collectAllPatterns`).
    - Copia `validateTopic` + `validateTopicPattern`.
    - Header italiano:
      ```
      // internal/topic-trie.ts — Mirror copy di packages/core/src/core/topic-matcher.ts.
      // RAGIONE: TopicTrie non è esposto pubblicamente da @sembridge/core (internal-only F1).
      // Per evitare cross-package internal coupling, F3 mantiene una copia interna ≤120 LOC.
      // RIMUOVERE quando F1 esporrà '@sembridge/core/internal' subpath o equivalente.
      // Vincolo D-83: ZERO modifiche a packages/core/.
      ```

    **`packages/routing/src/route-resolver.ts` (≥180 LOC):**

    Header JSDoc italiano:
    ```typescript
    // route-resolver.ts — Dispatch table pre-compilata per RouteDefinition (D-64, ROUTE-01).
    //
    // Riferimento decisioni:
    // - D-64: pre-compile al register; runtime O(segments) via TopicTrie<CompiledRoute>.
    // - D-66: 3 policy multi-route 'first-match' (default + warn dev) | 'priority-ordered' | 'all' (ROUTE-15, PRD §39 #6).
    // - D-86: unregisterByOwner cascade per LIFE-02 ext F3 (PRD §39 #7).
    //
    // Pitfall 1 mitigation: NESSUNA compilation hot-path. compile(def) avviene UNA volta al register.
    ```

    Imports:
    ```typescript
    import { createBrokerError } from '@sembridge/core'
    import type { BrokerError } from '@sembridge/core'
    import type { RouteDefinition } from './types/route-definition'
    import type { MultipleRoutesPolicy } from './types/routing-config'
    import { TopicTrie, validateTopicPattern } from './internal/topic-trie'
    import { firstMatch } from './strategies/first-match'
    import { priorityOrdered } from './strategies/priority-ordered'
    import { allBroadcast } from './strategies/all-broadcast'
    ```

    Tipo `CompiledRoute`:
    ```typescript
    export interface CompiledRoute {
      readonly id: string
      readonly definition: Readonly<RouteDefinition>
      readonly ownerId: string | undefined
      readonly priority: number  // default 0 se omesso
      /** Pre-curried per route http: (canonical) => HttpRequestSpec. undefined per local/cache/composite. */
      readonly requestBuilder?: (canonicalPayload: unknown) => unknown
    }
    ```

    Tipo `RouteRegistration`:
    ```typescript
    export interface RouteRegistration {
      readonly id: string
      unregister(): void
    }
    ```

    Tipo `AmbiguousRouteEvent`:
    ```typescript
    export interface AmbiguousRouteEvent {
      readonly topic: string
      readonly candidateRouteIds: readonly string[]
      readonly selectedRouteId: string
    }
    ```

    Class `RouteResolver`:
    ```typescript
    export interface RouteResolverOptions {
      /** Default false — register idempotente, true → throw on duplicate id (D-87 strict). */
      readonly strict?: boolean
      /** Callback opt-in per dev-mode warning (D-66). Default no-op (silent). */
      readonly onAmbiguousRoutes?: (event: AmbiguousRouteEvent) => void
    }

    export class RouteResolver {
      private readonly trie = new TopicTrie<CompiledRoute>()
      private readonly byId = new Map<string, CompiledRoute>()
      private readonly byOwner = new Map<string, Set<string>>()
      private readonly strict: boolean
      private readonly onAmbiguous: (event: AmbiguousRouteEvent) => void

      constructor(options: RouteResolverOptions = {}) {
        this.strict = options.strict ?? false
        this.onAmbiguous = options.onAmbiguousRoutes ?? (() => {})
      }

      register(def: RouteDefinition, options: { ownerId?: string } = {}): RouteRegistration {
        validateTopicPattern(def.topic)  // riuso F1 validation
        if (this.byId.has(def.id)) {
          if (this.strict) {
            throw createBrokerError({
              code: 'route.id.duplicate',
              category: 'config',
              message: `Route id "${def.id}" already registered`,
              details: { routeId: def.id, topic: def.topic },
            })
          }
          // idempotent return existing
          const existing = this.byId.get(def.id)!
          return { id: def.id, unregister: () => this.unregister(def.id) }
        }
        const compiled = this.compile(def, options.ownerId)
        this.byId.set(def.id, compiled)
        this.trie.insert(def.topic, compiled)
        if (options.ownerId !== undefined) {
          const set = this.byOwner.get(options.ownerId) ?? new Set<string>()
          set.add(def.id)
          this.byOwner.set(options.ownerId, set)
        }
        return { id: def.id, unregister: () => this.unregister(def.id) }
      }

      unregister(routeId: string): boolean {
        const compiled = this.byId.get(routeId)
        if (!compiled) return false
        this.byId.delete(routeId)
        this.trie.remove(compiled.definition.topic, compiled)
        if (compiled.ownerId !== undefined) {
          const set = this.byOwner.get(compiled.ownerId)
          if (set) {
            set.delete(routeId)
            if (set.size === 0) this.byOwner.delete(compiled.ownerId)
          }
        }
        return true
      }

      unregisterByOwner(ownerId: string): readonly string[] {
        const ids = Array.from(this.byOwner.get(ownerId) ?? new Set<string>())
        for (const id of ids) this.unregister(id)
        return ids
      }

      resolve(topic: string, policy: MultipleRoutesPolicy = 'first-match'): readonly CompiledRoute[] {
        const matches = this.trie.match(topic)
        if (matches.length === 0) return []
        // dev mode warn + select per first-match
        if (matches.length > 1 && policy === 'first-match') {
          this.onAmbiguous({
            topic,
            candidateRouteIds: matches.map((r) => r.id),
            selectedRouteId: matches[0]!.id,
          })
          return [matches[0]!]
        }
        if (policy === 'first-match') return [matches[0]!]
        if (policy === 'priority-ordered') return priorityOrdered(matches)
        return allBroadcast(matches)
      }

      countByOwner(ownerId: string): number {
        return this.byOwner.get(ownerId)?.size ?? 0
      }

      list(): readonly CompiledRoute[] {
        return Array.from(this.byId.values())
      }

      private compile(def: RouteDefinition, ownerId: string | undefined): CompiledRoute {
        const priority = def.priority ?? 0
        // requestBuilder per route http — closure pre-curried
        let requestBuilder: ((canonical: unknown) => unknown) | undefined
        if (def.type === 'http') {
          // Pre-curry: NB il mapper.mapToShape effettivo è invocato dal http-handler con il MapperEngine,
          // qui costruiamo solo un thunk closurizzato sul queryMap/bodyMap. Il vero mapping è risolto
          // dall'http-handler. Per ora il thunk è un placeholder che ritorna { url, method, queryMap, bodyMap }.
          requestBuilder = (canonical) => ({
            method: def.request.method,
            url: def.request.url,
            queryMap: def.request.queryMap,
            bodyMap: def.request.bodyMap,
            canonical,
          })
        }
        const compiled: CompiledRoute = {
          id: def.id,
          definition: def,
          ownerId,
          priority,
          ...(requestBuilder !== undefined && { requestBuilder }),
        }
        return compiled
      }
    }
    ```

    Conditional spread per `exactOptionalPropertyTypes` su tutti i campi opzionali.

    **`packages/routing/src/route-resolver.test.ts`:**
    13 test (uno per ogni behavior sopra). Setup: `const resolver = new RouteResolver({ strict: false })`. Crea `RouteDefinition` mock (es. `{ id: 'r1', type: 'local', topic: 'weather.requested' }`).
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/SemBridge && pnpm --filter @sembridge/routing test -- --run route-resolver.test.ts 2>&1 | tee /tmp/03-05-resolver.log; grep -E "Tests +13 +passed" /tmp/03-05-resolver.log</automated>
  </verify>
  <done>
    - 13/13 test passing
    - `grep -c "class RouteResolver" packages/routing/src/route-resolver.ts` ≥ 1
    - `grep "unregisterByOwner" packages/routing/src/route-resolver.ts` matcha
    - `grep "route.id.duplicate" packages/routing/src/route-resolver.ts` matcha
    - `grep "TopicTrie" packages/routing/src/internal/topic-trie.ts` matcha (mirror copy)
    - `wc -l packages/routing/src/route-resolver.ts` ≥ 180
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: strategies firstMatch + priorityOrdered + allBroadcast</name>
  <read_first>
    - packages/routing/src/route-resolver.ts (creato Task 1 — per CompiledRoute type)
    - packages/mapper/src/alias-registry.ts (analogo priority/scope resolution)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 521-528 esempio resolve dispatch)
  </read_first>
  <behavior>
    - Test 1: `firstMatch([r1, r2, r3])` → `[r1]`
    - Test 2: `priorityOrdered([{priority:1}, {priority:5}, {priority:2}])` → `[{priority:5}]` (più alto)
    - Test 3: `priorityOrdered([{priority:0}, {priority:0}])` → `[primo]` (tie-breaker insertion order)
    - Test 4: `allBroadcast([r1, r2, r3])` → `[r1, r2, r3]` (passthrough)
  </behavior>
  <files>
    packages/routing/src/strategies/first-match.ts,
    packages/routing/src/strategies/priority-ordered.ts,
    packages/routing/src/strategies/all-broadcast.ts,
    packages/routing/src/strategies/index.ts,
    packages/routing/src/strategies/strategies.test.ts
  </files>
  <action>
    **`first-match.ts`:**
    ```typescript
    import type { CompiledRoute } from '../route-resolver'

    /**
     * Strategy `'first-match'` (D-66, ROUTE-15 default — chiusura PRD §39 #6).
     * Ritorna [primo match] dell'array. In dev mode il warning ambiguous è gestito dal RouteResolver.resolve().
     */
    export function firstMatch(matches: readonly CompiledRoute[]): readonly CompiledRoute[] {
      return matches.length === 0 ? [] : [matches[0]!]
    }
    ```

    **`priority-ordered.ts`:**
    ```typescript
    import type { CompiledRoute } from '../route-resolver'

    /**
     * Strategy `'priority-ordered'` (D-66, ROUTE-15).
     * Ordina per priority numerica desc; tie-breaker: insertion order (sort stable JS ≥2019).
     * Ritorna [priority più alta] (1 elemento).
     */
    export function priorityOrdered(matches: readonly CompiledRoute[]): readonly CompiledRoute[] {
      if (matches.length === 0) return []
      const sorted = [...matches].sort((a, b) => b.priority - a.priority)
      return [sorted[0]!]
    }
    ```

    **`all-broadcast.ts`:**
    ```typescript
    import type { CompiledRoute } from '../route-resolver'

    /**
     * Strategy `'all'` (D-66, ROUTE-15) — opt-in fan-out: ritorna tutti i match (broadcast).
     */
    export function allBroadcast(matches: readonly CompiledRoute[]): readonly CompiledRoute[] {
      return matches
    }
    ```

    **`index.ts` (barrel):**
    ```typescript
    export { firstMatch } from './first-match'
    export { priorityOrdered } from './priority-ordered'
    export { allBroadcast } from './all-broadcast'
    ```

    **`strategies.test.ts`:** 4 test deterministici (uno per behavior).
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/SemBridge && pnpm --filter @sembridge/routing test -- --run strategies.test.ts 2>&1 | tee /tmp/03-05-strategies.log; grep -E "Tests +4 +passed" /tmp/03-05-strategies.log</automated>
  </verify>
  <done>
    - 4/4 test passing
    - 3 file strategies + 1 index + 1 test creati
    - `grep "firstMatch" packages/routing/src/strategies/first-match.ts` matcha
    - `grep "priorityOrdered" packages/routing/src/strategies/priority-ordered.ts` matcha + sort algoritmo presente
    - `grep "allBroadcast" packages/routing/src/strategies/all-broadcast.ts` matcha
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RouteResolver.register | Plugin/consumer dichiara RouteDefinition — input untrusted (potrebbe avere id collidenti, topic malformati) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | RouteDefinition mutation post-register | mitigate | `definition: Readonly<RouteDefinition>` in CompiledRoute; runtime deepFreeze deferred al wrapper plan 03-12 (pattern F2 D-04 esteso) |
| T-03-05-02 | Spoofing | route id collision/hijack | mitigate | strict-mode opt-in con throw `route.id.duplicate`; default idempotent ritorna esistente (no overwrite) |
| T-03-05-03 | DoS | trie senza limite numero pattern | accept | Bounded by registerRoute API; non c'è path unbounded — ogni register è esplicito |
| T-03-05-04 | Tampering | topic pattern injection | mitigate | `validateTopicPattern` riusato da F1 (regex strict `[a-z][a-z0-9*]*` segments) |
| T-03-05-05 | Information Disclosure | error includes route details | mitigate | `BrokerError.details` include solo `routeId` + `topic` (no payload) |
</threat_model>

<verification>
- 17/17 test totali passing (13 resolver + 4 strategies)
- `tsc --noEmit` exit 0
- Performance: 1000 register + 1000 resolve completati in < 100ms (test smoke se serve)
</verification>

<success_criteria>
1. RouteResolver con register/unregister/unregisterByOwner/resolve/compile
2. Dispatch table con TopicTrie mirror interno (≤120 LOC, documentato come copia)
3. 3 strategies (first-match/priority-ordered/all-broadcast) implementate
4. Cascade unregisterByOwner per LIFE-02 ext F3 (D-86)
5. Throw BrokerError 'route.id.duplicate' in strict mode
6. dev mode warn `routing.ambiguous` callback su N>1 first-match
7. requestBuilder pre-curried per route http (Pitfall 1 mitigation)
8. 17/17 test passing
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-05-SUMMARY.md` con:
- Conferma 17/17 test passing
- Note decisione internal/topic-trie mirror (rimuovere quando F1 esporrà internal subpath)
- Riferimenti REQ-ID (ROUTE-01, ROUTE-15, ROUTE-16 fondamentali ma chiusura ROUTE-16 è D-67 nel wrapper plan 03-12)
- API surface esposta: `RouteResolver`, `CompiledRoute`, `RouteRegistration`, `AmbiguousRouteEvent`, `RouteResolverOptions`, `firstMatch`, `priorityOrdered`, `allBroadcast`
</output>
