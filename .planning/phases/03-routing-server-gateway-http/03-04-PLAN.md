---
phase: 03-routing-server-gateway-http
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - packages/gateway/src/augment.ts
  - packages/gateway/src/augment.test.ts
  - packages/gateway/src/http/index.ts
  - packages/gateway/src/index.ts
  - packages/gateway/package.json
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - ROUTE-06
  - ROUTE-07
tags:
  - augment
  - declaration-merging
  - phase-3
  - gateway
  - subpath-exports
must_haves:
  truths:
    - "`packages/gateway/src/augment.ts` esiste e applica TS declaration merging a `@sembridge/core` per `BrokerConfig.gateway?: GatewayConfig` (D-93 — replica F2 D-56)."
    - "`packages/gateway/src/http/index.ts` espone tutti i type pubblici del sub-modulo HTTP (GatewayConfig, AuthStrategyConfig, AllowlistEntry, DefaultsConfig, CircuitBreakerConfig, GatewayErrorCode, le 7 Strategy interfaces, GatewayContext, GatewayMiddleware, HttpRequestSpec, HttpResponseSpec, isGatewayErrorCode)."
    - "`packages/gateway/src/index.ts` umbrella espone via `export * from './http'` PIÙ il side-effect import di `./augment` (Pattern S1)."
    - "`package.json` ha `\"sideEffects\": [\"./dist/augment.js\"]` (Pattern S1, T-02-09-01 mitigation)."
    - "`package.json` ha subpath exports `./http` con `dist/http/index.js`/`dist/http/index.d.ts` corretti (RESEARCH §Subpath Exports + plan 03-01 verifica)."
    - "Test compile-time: `augment.test.ts` con expectTypeOf valida `BrokerConfig.gateway` ed espone runtime symbol `__augmentGatewayLoaded`."
    - "Build produce `dist/index.js` + `dist/http/index.js` (multi-entry — già configurato in plan 03-01 tsup)."
  artifacts:
    - path: "packages/gateway/src/augment.ts"
      provides: "TS declaration merging F3 per BrokerConfig.gateway"
      contains: "declare module '@sembridge/core'"
      min_lines: 30
    - path: "packages/gateway/src/http/index.ts"
      provides: "Barrel HTTP subpath con tutti type pubblici"
      contains: "GatewayConfig"
    - path: "packages/gateway/src/index.ts"
      provides: "Umbrella barrel + side-effect"
      contains: "__augmentGatewayLoaded"
    - path: "packages/gateway/package.json"
      provides: "sideEffects array"
      contains: "./dist/augment.js"
  key_links:
    - from: "packages/gateway/src/index.ts"
      to: "augment side-effect"
      via: "export { __augmentGatewayLoaded } from './augment'"
      pattern: "__augmentGatewayLoaded"
    - from: "packages/gateway/src/augment.ts"
      to: "@sembridge/core declarations"
      via: "declare module '@sembridge/core'"
      pattern: "declare module '@sembridge/core'"
    - from: "packages/gateway/src/index.ts"
      to: "./http subpath"
      via: "export * from './http'"
      pattern: "from './http'"
---

<objective>
Augment del package `@sembridge/gateway` parallelo a 03-03 per `@sembridge/routing`. Estende `BrokerConfig.gateway?: GatewayConfig` via TS declaration merging (D-93). Configura barrel umbrella + barrel `./http` subpath (subpath exports già lockati nel plan 03-01 — qui solo wiring degli export). Ridondante con 03-03 sul tipo `BrokerConfig.gateway` (entrambi i package augmentano la stessa interface, TS merge li unifica) MA mantiene **dependency boundary chiaro**: il routing dipende dai tipi `RouteDefinition`, il gateway dai tipi `GatewayConfig` — eventuali consumer F4-only (SSE/WS) non hanno bisogno di routing.

Purpose: i plan Wave 3-4 (resolver, http-handler, strategies) assumono che `import type { GatewayConfig, AuthStrategy, ... } from '@sembridge/gateway/http'` funzioni. Augment è non-breaking — applica solo extension a tipi esistenti. Plan parallelo a 03-03 (file ownership disgiunta: routing vs gateway).

Output: 5 file modificati/creati (augment.ts, augment.test.ts, http/index.ts, index.ts, package.json). Test compile-time conferma `BrokerConfig.gateway` typed correttamente. Build genera multi-entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/mapper/src/augment.ts
@packages/mapper/src/index.ts
@packages/gateway/src/http/types/gateway-config.ts
@packages/gateway/src/http/types/http-strategies.ts
@packages/gateway/src/http/types/http-error.ts
@packages/gateway/package.json
@packages/core/src/types/config.ts

<interfaces>
From packages/gateway/src/http/types/index.ts (creato in plan 03-02):
```typescript
export type * from './gateway-config'
export type * from './http-strategies'
export type * from './http-error'
export { isGatewayErrorCode } from './http-error'
```

From packages/mapper/src/augment.ts (template):
```typescript
declare module '@sembridge/core' {
  interface BrokerConfig {
    canonicalModel?: { ... }
  }
}
export const __augmentLoaded: true = true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: augment.ts gateway (BrokerConfig.gateway extension)</name>
  <read_first>
    - packages/mapper/src/augment.ts (template completo)
    - packages/mapper/src/augment.test.ts (test pattern)
    - packages/gateway/src/http/types/gateway-config.ts (per GatewayConfig type da iniettare)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione B — augment pattern)
  </read_first>
  <behavior>
    - Test 1: `BrokerConfig.gateway` typed come `GatewayConfig | undefined` dopo `import '@sembridge/gateway'`
    - Test 2: `__augmentGatewayLoaded === true` runtime
    - Test 3: una config con `gateway: { allowlist: [/^\/api\//], auth: { getToken: async () => 'token' } }` non da errore TS
  </behavior>
  <files>
    packages/gateway/src/augment.ts,
    packages/gateway/src/augment.test.ts
  </files>
  <action>
    **`augment.ts`:**
    Header italiano (replica `packages/mapper/src/augment.ts`, sostituisci F2→F3 e mapper→gateway):
    ```typescript
    /**
     * augment.ts — TS declaration merging per estendere @sembridge/core con `BrokerConfig.gateway`.
     *
     * Vincolo D-83: NESSUNA modifica a packages/core/src/.
     * Replica pattern F2 D-56 (declaration merging non-breaking).
     */

    import type { GatewayConfig } from './http/types/gateway-config'

    declare module '@sembridge/core' {
      interface BrokerConfig {
        /** F3 augmentation (D-93/SEC-01..05): gateway HTTP config con auth/allowlist/defaults/circuitBreaker. */
        gateway?: GatewayConfig
      }
    }

    /** Side-effect runtime symbol — preserva augment.js dal tree-shaker (Pattern S1). */
    export const __augmentGatewayLoaded: true = true
    ```

    **`augment.test.ts`:**
    - 3 test:
      1. `expectTypeOf<BrokerConfig['gateway']>().toMatchTypeOf<GatewayConfig | undefined>()`
      2. `expect(__augmentGatewayLoaded).toBe(true)`
      3. Type-only assertion: `const cfg: BrokerConfig = { gateway: { allowlist: [/^\/api\//], auth: { getToken: async () => 'token' } } }` compila (no @ts-expect-error needed)

    NOTE: `BrokerConfig.gateway` viene aumentato sia da `@sembridge/routing/src/augment.ts` (plan 03-03) sia qui — TS unifica le declaration merging delle medesime interface (legal). Il test verifica che la versione finale è `GatewayConfig` typed (non `unknown`).
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/SemBridge && pnpm --filter @sembridge/gateway test -- --run augment.test.ts 2>&1 | tee /tmp/03-04-augment.log; grep -E "Tests +[0-9]+ +passed" /tmp/03-04-augment.log</automated>
  </verify>
  <done>
    - 3/3 test passing
    - `grep "declare module '@sembridge/core'" packages/gateway/src/augment.ts` matcha
    - `grep "__augmentGatewayLoaded" packages/gateway/src/augment.ts` matcha
    - `grep -c "/\*\*" packages/gateway/src/augment.ts` ≥ 2 JSDoc
  </done>
</task>

<task type="auto">
  <name>Task 2: barrel http subpath + umbrella + sideEffects</name>
  <read_first>
    - packages/mapper/src/index.ts (umbrella pattern)
    - packages/gateway/src/http/types/index.ts (creato in 03-02 — re-export type)
    - packages/gateway/src/augment.ts (creato Task 1)
    - packages/gateway/package.json (placeholder esistente da plan 03-01)
    - packages/gateway/tsup.config.ts (multi-entry config — verifica già presente da plan 03-01)
  </read_first>
  <files>
    packages/gateway/src/http/index.ts,
    packages/gateway/src/index.ts,
    packages/gateway/package.json
  </files>
  <action>
    **`packages/gateway/src/http/index.ts` (subpath barrel HTTP):**
    ```typescript
    /**
     * @packageDocumentation
     *
     * @sembridge/gateway/http — Server Gateway HTTP centralizzato (PRD §18, SEC-01..05).
     *
     * Espone tipi pubblici. Runtime (`HttpGateway`, `createHttpGateway`) arriva dal plan 03-08.
     */

    // Type re-export
    export type {
      GatewayConfig,
      AuthStrategyConfig,
      AllowlistEntry,
      DefaultsConfig,
      CircuitBreakerConfig,
    } from './types/gateway-config'

    export type {
      HttpRequestSpec,
      HttpResponseSpec,
      GatewayContext,
      GatewayMiddleware,
      RetryStrategy,
      TimeoutStrategy,
      DedupeStrategy,
      BackpressureStrategy,
      AuthStrategy,
      IdempotencyStrategy,
      CircuitBreakerStrategy,
    } from './types/http-strategies'

    export type { GatewayErrorCode } from './types/http-error'

    // Runtime export — type guard
    export { isGatewayErrorCode } from './types/http-error'
    ```

    **`packages/gateway/src/index.ts` (umbrella):**
    ```typescript
    /**
     * @packageDocumentation
     *
     * @sembridge/gateway — Aggregato umbrella che ri-esporta sub-moduli.
     *
     * Sub-moduli:
     * - `@sembridge/gateway/http` — Phase 3 (questo phase, SEC-01..05)
     * - `@sembridge/gateway/sse-ws` — Phase 4 (riservato)
     */

    // Side-effect import — abilita TS declaration merging per BrokerConfig.gateway.
    export { __augmentGatewayLoaded } from './augment'

    // Re-export sub-modulo HTTP per consumer che importano dall'umbrella
    export * from './http'
    ```

    **`packages/gateway/package.json`:**
    Aggiungi/conferma il field `sideEffects`:
    ```json
    "sideEffects": ["./dist/augment.js"]
    ```

    Conferma che `exports` field ha:
    ```json
    "exports": {
      ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
      "./http": { "types": "./dist/http/index.d.ts", "import": "./dist/http/index.js" }
    }
    ```

    Se manca o ha pattern diverso, correggi.
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/SemBridge && pnpm --filter @sembridge/gateway build 2>&1 | tee /tmp/03-04-build.log && test -f packages/gateway/dist/index.js && test -f packages/gateway/dist/http/index.js && test -f packages/gateway/dist/augment.js && grep '"sideEffects"' packages/gateway/package.json && grep '"./http"' packages/gateway/package.json</automated>
  </verify>
  <done>
    - Build exit 0
    - `dist/index.js`, `dist/http/index.js`, `dist/augment.js` presenti (multi-entry verificato)
    - `package.json.sideEffects` array `[".dist/augment.js"]`
    - `package.json.exports[".\/http"]` valido
    - `grep "GatewayConfig" packages/gateway/src/http/index.ts` matcha
    - `grep "isGatewayErrorCode" packages/gateway/src/http/index.ts` matcha (runtime)
    - `grep -c "export type" packages/gateway/src/http/index.ts` ≥ 3 (type re-export blocks)
    - `grep "export \* from './http'" packages/gateway/src/index.ts` matcha
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan boundary | Compile-time + barrel — nessun input runtime esterno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | tree-shaker rimuove augment.js | mitigate | sideEffects array con `./dist/augment.js` (Pattern S1) |
| T-03-04-02 | Spoofing | declaration merging dello stesso `BrokerConfig.gateway` da routing (03-03) e gateway (03-04) | accept | TS unifica le declaration merging della stessa interface; entrambi i package importano lo stesso `GatewayConfig` type da `@sembridge/gateway/http` (rotta mostrata in 03-03 augment.ts task) — nessuna divergenza |
</threat_model>

<verification>
- `pnpm --filter @sembridge/gateway test -- --run augment.test.ts` 3/3
- `pnpm --filter @sembridge/gateway build` produces `dist/index.js`, `dist/http/index.js`, `dist/augment.js`
- `pnpm --filter @sembridge/core test` continua a passare
- `pnpm --filter @sembridge/mapper test` continua a passare
</verification>

<success_criteria>
1. augment.ts con `declare module '@sembridge/core'` per `BrokerConfig.gateway`
2. `http/index.ts` ri-esporta tutti i type pubblici del sub-modulo
3. `index.ts` umbrella + side-effect re-export augment
4. `package.json.sideEffects` + `exports['./http']` validi
5. Build multi-entry funzionante
6. Test compile-time 3/3 passing
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-04-SUMMARY.md` con:
- Conferma test 3/3 passing
- Conferma build multi-entry
- Riferimento a SEC-01..05/ROUTE-06/ROUTE-07 coperti a livello tipo
- Note per plan downstream (03-08 implementerà runtime HttpGateway sopra questi tipi)
</output>
