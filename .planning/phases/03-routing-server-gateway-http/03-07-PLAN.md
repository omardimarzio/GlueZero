---
phase: 03-routing-server-gateway-http
plan: 07
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
files_modified:
  - packages/routing/src/outcome-collector.ts
  - packages/routing/src/outcome-collector.test.ts
autonomous: true
requirements:
  - ROUTE-12
  - ERR-02
  - LIFE-02
tags:
  - outcome-collector
  - phase-3
  - routing
  - error-handling
must_haves:
  truths:
    - "`OutcomeCollector.collect(outcome, route, event, publishFn)` pubblica `<topic>.loaded` o `<topic>.failed` UNA volta con shape D-80 (chiusura ROUTE-12, PRD §22.3)."
    - "`<topic>.failed` BrokerError ha shape D-80: `code, message, category, routeId, topic, eventId, originalError?, cause?, httpStatus?, retryAttempt?, retryAfterMs?`."
    - "`<topic>.loaded` payload contiene il `canonicalPayload` dell'outcome.ok PIÙ metadata `{ origin: 'cache' | 'remote', httpStatus?, attemptCount? }`."
    - "Per category `'network'` il collector pubblica DUE eventi: `<topic>.failed` AND `network.error` come BrokerEvent CORE separato (D-81)."
    - "NO double publish: recursion guard `Set<eventId>` previene publish multipli per stesso outcome (D-82)."
    - "Topic name resolution: `route.publishes.success` (esplicito) > convention `<topic-prefix>.loaded` (es. `weather.requested` → `weather.loaded`); idem `route.publishes.error` > `<topic-prefix>.failed`."
    - "Tap step `event.outcome.collected` emesso PRIMA del publish (D-85, Pattern S3)."
    - "Test deterministici: ok→loaded, error→failed, network→failed+network.error, double-call no double publish, custom publishes override convention."
  artifacts:
    - path: "packages/routing/src/outcome-collector.ts"
      provides: "OutcomeCollector con collect + recursion guard + topic resolution"
      contains: "class OutcomeCollector"
      min_lines: 120
  key_links:
    - from: "packages/routing/src/outcome-collector.ts"
      to: "publishFn callback"
      via: "deps.publishFn(topic, payload, options)"
      pattern: "publishFn"
    - from: "packages/routing/src/outcome-collector.ts"
      to: "BrokerError shape D-80"
      via: "outcome.error already structured by handlers"
      pattern: "originalError"
    - from: "packages/routing/src/outcome-collector.ts"
      to: "EventTap step 10"
      via: "safeTapStep('event.outcome.collected', ...)"
      pattern: "event.outcome.collected"
---

<objective>
Implementa l'`OutcomeCollector` (step 10 della pipeline §28 — D-84). Trasforma il `RouteOutcome` ritornato dall'executor in un BrokerEvent `<topic>.loaded` (success) o `<topic>.failed` (error), pubblicandolo UNA volta sola (D-82 — NO publish doppio durante retry; il collector è invocato dopo che retry/timeout sono esauriti). Per errori `category: 'network'` pubblica anche `network.error` come secondo evento CORE (D-81). Topic naming: rispetta `route.publishes.success/error` esplicito o convention `<topic-prefix>.loaded`/`<topic-prefix>.failed`. Recursion guard `Set<eventId+publishedSuffix>` previene loop. Emette tap step 10 prima del publish (Pattern S3).

Purpose: il plan 03-12 (RouterBroker wrapper) chiama `collector.collect(outcome, ...)` come step 10 dopo `executor.execute`. I plan 03-13 (integration test) verificano end-to-end che `<topic>.failed` arriva ai subscriber con shape D-80.

Output: 2 file (outcome-collector + test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/mapper/src/broker-mapper-wrapper.ts
@packages/core/src/core/broker-error.ts
@packages/core/src/core/event-tap.ts
@packages/routing/src/types/route-outcome.ts
@packages/routing/src/types/route-definition.ts

<interfaces>
From packages/routing/src/types/route-outcome.ts:
```typescript
export type RouteOutcome =
  | { readonly ok: true; readonly canonicalPayload: unknown; readonly routeId: string; readonly metadata? }
  | { readonly ok: false; readonly error: BrokerError; readonly routeId: string }
```

From packages/mapper/src/broker-mapper-wrapper.ts (analogo handleMappingError lines 1029-1068):
```typescript
private handleMappingError(err: BrokerError, sourceTopic: string, step: string): void {
  const key = `${sourceTopic}::${step}`
  if (this.inFlightMappingErrors.has(key)) return  // recursion guard
  this.inFlightMappingErrors.add(key)
  try {
    const safeError = { code: err.code, category: err.category, message: err.message, details: err.details }
    this.inner.publish('mapping.error', { error: safeError, sourceEvent: sourceTopic, step }, ...)
  } finally {
    this.inFlightMappingErrors.delete(key)
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outcome-collector.ts con collect + recursion guard + tap step 10</name>
  <read_first>
    - packages/mapper/src/broker-mapper-wrapper.ts (lines 1029-1068 — handleMappingError pattern recursion guard)
    - packages/core/src/core/broker-error.ts (BrokerError shape; isBrokerError)
    - packages/core/src/core/event-tap.ts (safeTapStep, startStep, PipelineStep)
    - packages/routing/src/types/route-outcome.ts
    - packages/routing/src/types/route-definition.ts (route.publishes.success/error)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione F — outcome collector pattern)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 70-76 BrokerError shape D-80, 81 D-81 network.error, 82 D-82 NO double publish)
  </read_first>
  <behavior>
    - Test 1: outcome.ok → publishFn invocato con `<topic-prefix>.loaded` + canonicalPayload + metadata
    - Test 2: outcome.ok con `route.publishes.success: 'weather.fresh'` → publishFn invocato con `'weather.fresh'` (override convention)
    - Test 3: outcome.error category='validation' → publishFn invocato con `<topic-prefix>.failed` PIÙ payload con `error` shape D-80
    - Test 4: outcome.error category='network' → DUE publishFn calls: `<topic-prefix>.failed` E `network.error`
    - Test 5: due `collect` consecutivi con stesso eventId → solo UNA publishFn call (recursion guard)
    - Test 6: tap.onPipelineStep invocato con `'event.outcome.collected'` PRIMA del publish (verifica order via spy)
    - Test 7: topic prefix resolution: `'weather.requested'` → loaded `'weather.loaded'`, failed `'weather.failed'`; `'weather.alert.requested'` → `'weather.alert.loaded'`/`'weather.alert.failed'`
    - Test 8: outcome.error con `originalError: new Error('test')` → published payload include sanitized shape SENZA stack trace ma CON code/category/message/details
  </behavior>
  <files>
    packages/routing/src/outcome-collector.ts,
    packages/routing/src/outcome-collector.test.ts
  </files>
  <action>
    **`outcome-collector.ts` (≥120 LOC):**

    Header italiano:
    ```typescript
    // outcome-collector.ts — Step 10 pipeline §28 (D-84): trasforma RouteOutcome in BrokerEvent
    // <topic>.loaded o <topic>.failed PIÙ network.error per category='network' (D-80, D-81, D-82).
    //
    // Vincoli chiave:
    // - D-80: shape BrokerError standard {code, message, category, routeId, topic, eventId, ...}
    // - D-81: network.error pubblicato AGGIUNTIVAMENTE per category='network'
    // - D-82: NO double publish — recursion guard Set<eventId+suffix>
    // - ROUTE-12: <topic>.failed automatico su errore route remota
    ```

    Imports:
    ```typescript
    import { isBrokerError, safeTapStep, startStep, type BrokerError, type BrokerEvent, type EventTap, type PipelineStep } from '@sembridge/core'
    import type { RouteOutcome } from './types/route-outcome'
    import type { CompiledRoute } from './route-resolver'
    ```

    Tipi:
    ```typescript
    export type PublishFn = (topic: string, payload: unknown, options?: { source?: { type: string; id: string }; correlationId?: string }) => void

    export interface OutcomeCollectorDeps {
      readonly publishFn: PublishFn
      readonly tap?: EventTap
    }
    ```

    Helpers:
    ```typescript
    function deriveLoadedTopic(originalTopic: string): string {
      // <prefix>.requested → <prefix>.loaded
      // <prefix>.alert.requested → <prefix>.alert.loaded
      // qualsiasi altro → originalTopic + '.loaded' (fallback)
      const idx = originalTopic.lastIndexOf('.')
      if (idx === -1) return `${originalTopic}.loaded`
      const suffix = originalTopic.slice(idx + 1)
      const prefix = originalTopic.slice(0, idx)
      if (suffix === 'requested') return `${prefix}.loaded`
      return `${originalTopic}.loaded`
    }

    function deriveFailedTopic(originalTopic: string): string {
      const idx = originalTopic.lastIndexOf('.')
      if (idx === -1) return `${originalTopic}.failed`
      const suffix = originalTopic.slice(idx + 1)
      const prefix = originalTopic.slice(0, idx)
      if (suffix === 'requested') return `${prefix}.failed`
      return `${originalTopic}.failed`
    }

    function sanitizeError(err: BrokerError): {
      readonly code: string
      readonly category: string
      readonly message: string
      readonly routeId?: string
      readonly topic?: string
      readonly eventId?: string
      readonly httpStatus?: number
      readonly retryAttempt?: number
      readonly retryAfterMs?: number
      readonly details?: Readonly<Record<string, unknown>>
    } {
      return {
        code: err.code,
        category: err.category,
        message: err.message,
        ...(err.routeId !== undefined && { routeId: err.routeId }),
        ...(err.topic !== undefined && { topic: err.topic }),
        ...(err.eventId !== undefined && { eventId: err.eventId }),
        ...((err as { httpStatus?: number }).httpStatus !== undefined && { httpStatus: (err as { httpStatus: number }).httpStatus }),
        ...((err as { retryAttempt?: number }).retryAttempt !== undefined && { retryAttempt: (err as { retryAttempt: number }).retryAttempt }),
        ...((err as { retryAfterMs?: number }).retryAfterMs !== undefined && { retryAfterMs: (err as { retryAfterMs: number }).retryAfterMs }),
        ...(err.details !== undefined && { details: err.details }),
      }
    }
    ```

    Class:
    ```typescript
    export class OutcomeCollector {
      private readonly inFlightPublishes = new Set<string>()

      constructor(private readonly deps: OutcomeCollectorDeps) {}

      collect(outcome: RouteOutcome, route: CompiledRoute, event: BrokerEvent): void {
        // Recursion guard
        const guardKey = `${event.id}::${outcome.ok ? 'loaded' : 'failed'}`
        if (this.inFlightPublishes.has(guardKey)) return
        this.inFlightPublishes.add(guardKey)

        const factory = startStep()

        try {
          // Tap step 10 PRE-publish
          if (this.deps.tap !== undefined) {
            safeTapStep(
              this.deps.tap,
              'event.outcome.collected' as PipelineStep,
              factory('event.outcome.collected' as PipelineStep, event.id, event.topic, {
                metadata: {
                  routeId: route.id,
                  ok: outcome.ok,
                  ...(outcome.ok ? {} : { errorCode: outcome.error.code, errorCategory: outcome.error.category }),
                },
              }),
            )
          }

          if (outcome.ok) {
            const successTopic =
              (route.definition as { publishes?: { success?: string } }).publishes?.success ?? deriveLoadedTopic(event.topic)
            const payload = {
              ...(outcome.canonicalPayload !== null && typeof outcome.canonicalPayload === 'object'
                ? (outcome.canonicalPayload as Record<string, unknown>)
                : { value: outcome.canonicalPayload }),
              ...(outcome.metadata !== undefined && { metadata: outcome.metadata }),
            }
            this.deps.publishFn(successTopic, payload, {
              source: { type: 'system', id: 'router' },
              correlationId: event.id,
            })
          } else {
            const failedTopic =
              (route.definition as { publishes?: { error?: string } }).publishes?.error ?? deriveFailedTopic(event.topic)
            const safeError = sanitizeError(outcome.error)
            this.deps.publishFn(
              failedTopic,
              { error: safeError, sourceEvent: { topic: event.topic, eventId: event.id }, routeId: route.id },
              {
                source: { type: 'system', id: 'router' },
                correlationId: event.id,
              },
            )
            // D-81: network.error secondario per category='network'
            if (outcome.error.category === 'network') {
              this.deps.publishFn(
                'network.error',
                { error: safeError, sourceEvent: { topic: event.topic, eventId: event.id }, routeId: route.id },
                {
                  source: { type: 'system', id: 'router' },
                  correlationId: event.id,
                },
              )
            }
          }
        } finally {
          this.inFlightPublishes.delete(guardKey)
        }
      }

      /** Per test/snapshot debug. */
      inFlightCount(): number {
        return this.inFlightPublishes.size
      }
    }
    ```

    Conditional spread per `exactOptionalPropertyTypes`. NO sanitization include `originalError` (Pitfall 9 mitigation — no recursive serialization).

    **`outcome-collector.test.ts`:**
    8 test (uno per behavior). Setup: `vi.fn()` per `publishFn`, mock `BrokerEvent` cast, mock `CompiledRoute`. Verifica chiamate via `expect(publishFn).toHaveBeenCalledWith(...)`.
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/SemBridge && pnpm --filter @sembridge/routing test -- --run outcome-collector.test.ts 2>&1 | tee /tmp/03-07-collector.log; grep -E "Tests +8 +passed" /tmp/03-07-collector.log</automated>
  </verify>
  <done>
    - 8/8 test passing
    - `grep "class OutcomeCollector" packages/routing/src/outcome-collector.ts` matcha
    - `grep "network.error" packages/routing/src/outcome-collector.ts` matcha
    - `grep "deriveLoadedTopic" packages/routing/src/outcome-collector.ts` matcha
    - `grep "deriveFailedTopic" packages/routing/src/outcome-collector.ts` matcha
    - `grep "inFlightPublishes" packages/routing/src/outcome-collector.ts` matcha (recursion guard)
    - `grep "event.outcome.collected" packages/routing/src/outcome-collector.ts` matcha (tap step 10)
    - `wc -l packages/routing/src/outcome-collector.ts` ≥ 120 LOC
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OutcomeCollector.collect | Riceve BrokerError potenzialmente con originalError (untrusted stack) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Information Disclosure | originalError stack trace nel published payload | mitigate | `sanitizeError` rimuove `originalError` e `cause` dalla shape published; CR-06 sanitization pattern F2 |
| T-03-07-02 | DoS | recursion infinita per loop publish→collect | mitigate | recursion guard `Set<eventId+suffix>` (D-82, pattern F2 handleMappingError) |
| T-03-07-03 | Spoofing | malicious route.publishes.success/error sostituisce topic core | accept | Topic naming validato dal bus.publish a valle (F1 validateTopic regex); collector non valida — è plugin authority |
</threat_model>

<verification>
- 8/8 test passing
- `tsc --noEmit` exit 0
- 0 changes a `packages/core/` e `packages/mapper/`
</verification>

<success_criteria>
1. OutcomeCollector con collect + recursion guard
2. Topic resolution prefisso convention + override `route.publishes`
3. Network.error secondario per category='network'
4. Sanitize error rimuove originalError/cause
5. Tap step 10 emit pre-publish
6. 8/8 test passing
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-07-SUMMARY.md` con:
- Conferma 8/8 test passing
- Riferimenti REQ-ID coperti (ROUTE-12, ERR-02 ext, LIFE-02 ext)
- Note D-80/D-81/D-82 chiusi
- Sanitization shape esplicita
</output>
