---
phase: 03-routing-server-gateway-http
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - packages/routing/src/augment.ts
  - packages/routing/src/augment.test.ts
  - packages/routing/src/index.ts
  - packages/routing/package.json
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-15
  - ROUTE-16
tags:
  - augment
  - declaration-merging
  - phase-3
  - routing
must_haves:
  truths:
    - "`packages/routing/src/augment.ts` esiste e applica TS declaration merging a `@gluezero/core` (PluginDescriptor.routes + BrokerConfig.routes) e `@gluezero/mapper` (CanonicalSchema.requiresRoute) — D-93/D-94/D-95."
    - "`PipelineStep` extension: `F3PipelineStep` literal union additive con i 3 nuovi step `event.route.resolved`/`event.route.executed`/`event.outcome.collected` — esposto da `index.ts` (D-85)."
    - "`PluginDescriptor.routes?: readonly RouteDefinition[]` è type-checkato (test importa un descriptor con routes e tsc non da errore)."
    - "`BrokerConfig.routes?: readonly RouteDefinition[]` e `BrokerConfig.gateway?: GatewayConfig` sono type-checkati."
    - "`CanonicalSchema.requiresRoute?: boolean` augmentation visibile dopo l'import di `'@gluezero/routing'` (D-95, ROUTE-16)."
    - "`packages/routing/src/index.ts` ha `export { __augmentLoaded } from './augment'` come prima riga runtime per garantire tree-shaker non lo elimini (Pattern S1)."
    - "`package.json` ha `\"sideEffects\": [\"./dist/augment.js\"]` (Pattern S1, T-02-09-01 mitigation)."
    - "Test compile-time: `augment.test.ts` con expectTypeOf/satisfies che valida l'estensione."
  artifacts:
    - path: "packages/routing/src/augment.ts"
      provides: "TS declaration merging F3 + F3PipelineStep export"
      contains: "declare module '@gluezero/core'"
      min_lines: 60
    - path: "packages/routing/src/index.ts"
      provides: "Barrel public con side-effect import augment + type re-export"
      contains: "__augmentLoaded"
    - path: "packages/routing/package.json"
      provides: "sideEffects array per preservare augment.js"
      contains: "./dist/augment.js"
  key_links:
    - from: "packages/routing/src/index.ts"
      to: "augment side-effect"
      via: "export { __augmentLoaded } from './augment'"
      pattern: "__augmentLoaded"
    - from: "packages/routing/src/augment.ts"
      to: "@gluezero/core declarations"
      via: "declare module '@gluezero/core'"
      pattern: "declare module '@gluezero/core'"
    - from: "packages/routing/src/augment.ts"
      to: "@gluezero/mapper declarations"
      via: "declare module '@gluezero/mapper'"
      pattern: "declare module '@gluezero/mapper'"
---

<objective>
Aggiunge l'estensione TS dichiarata da `@gluezero/routing` ai tipi pubblici di `@gluezero/core` (`PluginDescriptor.routes`, `BrokerConfig.routes`) e `@gluezero/mapper` (`CanonicalSchema.requiresRoute`) via TS declaration merging — pattern non-breaking identico a F2 D-49/D-56/D-57. Espone il `F3PipelineStep` literal union additivo per i 3 nuovi step della pipeline §28 (D-85). Configura il barrel `index.ts` con side-effect import e `package.json.sideEffects` array per garantire che il tree-shaker non elimini `augment.js`.

Purpose: senza augment, `BrokerConfig.routes` resta `unknown` placeholder F1 e i plugin non possono dichiarare `routes` nel descriptor. Senza `requiresRoute` augmentation, ROUTE-16 non è chiusa. Plan 03-04 augmenta gateway parallelamente; plan 03-05+ assume questi tipi attivi.

Output: 3 file modificati/creati (augment.ts, augment.test.ts, index.ts) + package.json sideEffects. Test compile-time conferma che le estensioni sono visibili dopo `import '@gluezero/routing'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-routing-server-gateway-http/03-CONTEXT.md
@.planning/phases/03-routing-server-gateway-http/03-RESEARCH.md
@.planning/phases/03-routing-server-gateway-http/03-PATTERNS.md
@packages/mapper/src/augment.ts
@packages/mapper/src/augment.test.ts
@packages/mapper/src/index.ts
@packages/mapper/package.json
@packages/routing/src/types/route-definition.ts
@packages/routing/src/types/routing-config.ts
@packages/core/src/types/plugin.ts
@packages/core/src/types/config.ts

<interfaces>
<!-- Pattern di declaration merging già provato in F2 -->

From packages/mapper/src/augment.ts (analogo da replicare):
```typescript
declare module '@gluezero/core' {
  interface PluginDescriptor {
    readonly inputMap?: InputMap | Readonly<Record<string, InputMap>>
    readonly outputMap?: OutputMap | Readonly<Record<string, OutputMap>>
  }
  interface BrokerConfig {
    canonicalModel?: { readonly schemas?: readonly CanonicalSchema[] }
    aliasRegistry?: { readonly global?: ... }
    transforms?: Readonly<Record<string, TransformFn>>
  }
}

export const __augmentLoaded: true = true
```

From packages/mapper/src/index.ts (barrel pattern):
```typescript
export { __augmentLoaded } from './augment'
// ... altri export
export type F2PipelineStep =
  | 'event.source.resolved'
  | 'event.mapped.canonical'
  | 'event.canonical.validated'
  | 'event.mapped.consumer'
  | 'event.final.validated'
```

From packages/mapper/package.json:
```json
{
  "sideEffects": ["./dist/augment.js"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: augment.ts + augment.test.ts (compile-time test) — F3 declaration merging</name>
  <read_first>
    - packages/mapper/src/augment.ts (template completo da replicare 109 LOC)
    - packages/mapper/src/augment.test.ts (template test pattern)
    - packages/mapper/src/index.ts (per F2PipelineStep literal union pattern)
    - packages/routing/src/types/route-definition.ts (RouteDefinition import target)
    - packages/routing/src/types/routing-config.ts (RoutingConfig import)
    - packages/core/src/types/plugin.ts (PluginDescriptor base — verifica che routes? sia placeholder unknown o assente)
    - packages/core/src/types/config.ts (BrokerConfig.routes/gateway placeholder unknown)
    - .planning/phases/03-routing-server-gateway-http/03-PATTERNS.md (sezione B — augment template line-by-line)
    - .planning/phases/03-routing-server-gateway-http/03-RESEARCH.md (lines 1045-1081 esempio augment F3)
  </read_first>
  <behavior>
    - Test 1: `import type { PluginDescriptor } from '@gluezero/core'` + un descriptor con `routes: [routeDef]` non da errore TS
    - Test 2: `import type { BrokerConfig } from '@gluezero/core'` + config con `routes: [routeDef]` e `gateway: gatewayConfig` non da errore TS
    - Test 3: `import type { CanonicalSchema } from '@gluezero/mapper'` + uno schema con `requiresRoute: true` non da errore TS
    - Test 4: `F3PipelineStep` literal include i 3 step
    - Test 5: `__augmentLoaded` runtime symbol vero (non strip dal tree-shaker)
  </behavior>
  <files>
    packages/routing/src/augment.ts,
    packages/routing/src/augment.test.ts
  </files>
  <action>
    **`augment.ts` (≥ 60 LOC):**
    - Header JSDoc italiano replica `packages/mapper/src/augment.ts` (sostituendo F2→F3 e D-49/56/57 → D-83/93/94/95).
    - `import type { RouteDefinition } from './types/route-definition'`.
    - `import type { GatewayConfig } from '@gluezero/gateway'` (cross-package import — tipo solo).

    Blocco 1 — augment `@gluezero/core`:
    ```typescript
    declare module '@gluezero/core' {
      interface PluginDescriptor {
        /** F3 augmentation (D-94, ROUTE-01): route auto-registrate al registerPlugin con ownerId=pluginId. */
        readonly routes?: readonly RouteDefinition[]
      }
      interface BrokerConfig {
        /** F3 augmentation (D-93/D-62): route registrate al boot via createRouterBroker. */
        routes?: readonly RouteDefinition[]
        /** F3 augmentation: gateway HTTP config (D-71/D-72/D-69). */
        gateway?: GatewayConfig
      }
    }
    ```

    Blocco 2 — augment `@gluezero/mapper`:
    ```typescript
    declare module '@gluezero/mapper' {
      interface CanonicalSchema {
        /**
         * F3 augmentation (D-95, ROUTE-16, chiusura PRD §39 #5).
         * Se `true`, topic con questo schema senza route registrata → BrokerError 'route.required.missing'.
         */
        readonly requiresRoute?: boolean
      }
    }
    ```

    Blocco 3 — `F3PipelineStep` literal union additive (D-85):
    ```typescript
    /**
     * Step pipeline §28 introdotti da F3 (D-84, D-85).
     * NB: TS NON supporta declaration merging di type alias; il barrel index.ts ri-esporta come literal additive.
     */
    export type F3PipelineStep =
      | 'event.route.resolved'
      | 'event.route.executed'
      | 'event.outcome.collected'
    ```

    Blocco 4 — symbol per side-effect preservation:
    ```typescript
    /** Side-effect runtime symbol — preserva augment.js dal tree-shaker (Pattern S1, T-02-09-01). */
    export const __augmentLoaded: true = true
    ```

    **`augment.test.ts`:**
    - Vitest con `import { expectTypeOf } from 'vitest'` (compile-time + runtime smoke).
    - 5 test:
      1. `expectTypeOf<PluginDescriptor>().toHaveProperty('routes')` (verifica routes opzionale array RouteDefinition)
      2. `expectTypeOf<BrokerConfig>().toHaveProperty('routes')` + `toHaveProperty('gateway')`
      3. `expectTypeOf<CanonicalSchema>().toHaveProperty('requiresRoute')`
      4. `expectTypeOf<F3PipelineStep>().toEqualTypeOf<'event.route.resolved' | 'event.route.executed' | 'event.outcome.collected'>()`
      5. Runtime: `expect(__augmentLoaded).toBe(true)`

    NB: import `import '@gluezero/routing/src/augment'` (relative `./augment`) per attivare le declaration merging — pattern F2.

    Conditional spread non serve qui (no costruzioni runtime).
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing test -- --run augment.test.ts 2>&1 | tee /tmp/03-03-augment.log; grep -E "Tests +[0-9]+ +passed" /tmp/03-03-augment.log</automated>
  </verify>
  <done>
    - 5 test passing
    - `grep -c "declare module" packages/routing/src/augment.ts` ≥ 2 (core + mapper)
    - `grep "F3PipelineStep" packages/routing/src/augment.ts` matcha
    - `grep "__augmentLoaded" packages/routing/src/augment.ts` matcha
    - `grep -c "/\*\*" packages/routing/src/augment.ts` ≥ 4 JSDoc entries
  </done>
</task>

<task type="auto">
  <name>Task 2: barrel index.ts + package.json sideEffects (Pattern S1 mitigation)</name>
  <read_first>
    - packages/mapper/src/index.ts (template barrel completo lines 64-181)
    - packages/mapper/package.json (sideEffects array example)
    - packages/routing/src/types/index.ts (per re-export type)
    - packages/routing/src/augment.ts (creato Task 1 — per __augmentLoaded export)
    - packages/routing/package.json (placeholder esistente da plan 03-01 — modificare sideEffects field)
  </read_first>
  <files>
    packages/routing/src/index.ts,
    packages/routing/package.json
  </files>
  <action>
    **`packages/routing/src/index.ts` (barrel completo F3):**
    Header italiano `@packageDocumentation` analogo a `@gluezero/mapper`:
    ```typescript
    /**
     * @packageDocumentation
     *
     * @gluezero/routing — Routing engine dichiarativo per GlueZero (PRD §17).
     *
     * Espone:
     * - `RouterBroker` (composition wrapper di `MapperBroker` di F2 — D-83)
     * - `createRouterBroker(config)` factory
     * - `RouteDefinition` discriminated union (`local`/`http`/`cache`/`composite`)
     * - `RoutePolicies` (timeout/retry/dedupe/concurrency/backpressure/auth/idempotency/error)
     * - `RouteOutcome` (discriminated success/error)
     * - `RoutingConfig` (multipleRoutesPolicy: 'first-match'/'priority-ordered'/'all')
     *
     * Estende via TS declaration merging:
     * - `PluginDescriptor.routes` (D-94)
     * - `BrokerConfig.routes`/`gateway` (D-93)
     * - `CanonicalSchema.requiresRoute` (D-95, ROUTE-16)
     */

    // Side-effect import — abilita TS declaration merging per @gluezero/core e @gluezero/mapper.
    // Pattern S1 — necessario perché tsup con sideEffects:false eliminerebbe augment.js.
    export { __augmentLoaded, type F3PipelineStep } from './augment'

    // Type exports public surface
    export type {
      RouteDefinition,
      RouteLocalDefinition,
      RouteHttpDefinition,
      RouteCacheDefinition,
      RouteCompositeDefinition,
    } from './types/route-definition'

    export type {
      RoutePolicies,
      RetryPolicyConfig,
      DedupePolicyConfig,
      BackpressurePolicyConfig,
      ConcurrencyPolicy,
      AuthPolicyConfig,
      IdempotencyPolicyConfig,
      ErrorPolicyConfig,
      TimeoutPolicyConfig,
    } from './types/route-policies'

    export type {
      RouteOutcome,
      RouteResult,
      RouteError,
    } from './types/route-outcome'

    export type {
      RoutingConfig,
      MultipleRoutesPolicy,
    } from './types/routing-config'
    ```

    **NO export di runtime ancora** — RouterBroker/createRouterBroker arrivano dal plan 03-12. Questo plan espone solo type + augment side-effect.

    **`packages/routing/package.json`:**
    Aggiungi/conferma il field `sideEffects`:
    ```json
    "sideEffects": ["./dist/augment.js"]
    ```
    NB: il plan 03-01 dovrebbe averlo già aggiunto. Verifica con `grep -A2 sideEffects packages/routing/package.json` e correggi se manca o se ha pattern diverso.

    Pattern S1 enforcement.
  </action>
  <verify>
    <automated>cd /Users/omarmarzio/programming/prova\ AI/GlueZero && pnpm --filter @gluezero/routing build 2>&1 | tee /tmp/03-03-build.log && test -f packages/routing/dist/augment.js && grep '"sideEffects"' packages/routing/package.json</automated>
  </verify>
  <done>
    - `pnpm --filter @gluezero/routing build` exit 0
    - `dist/index.js` e `dist/augment.js` generati (multi-entry o entry singolo dipende da tsup config — verifica entrambi presenti)
    - `package.json` ha `"sideEffects": ["./dist/augment.js"]`
    - `grep "F3PipelineStep" packages/routing/src/index.ts` matcha (re-export type)
    - `grep "__augmentLoaded" packages/routing/src/index.ts` matcha (re-export runtime)
    - `grep -c "export type" packages/routing/src/index.ts` ≥ 4 (4+ type re-export blocks)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan boundary (compile-time) | Augment è type-only + un simbolo runtime — nessun input esterno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | tree-shaker rimuove augment.js | mitigate | `package.json.sideEffects: ["./dist/augment.js"]` + side-effect re-export `__augmentLoaded` (Pattern S1, T-02-09-01 da F2) |
| T-03-03-02 | Spoofing | declaration merging accidentale collisioni con F2 | mitigate | Augment di routing modifica SOLO `PluginDescriptor.routes`, `BrokerConfig.routes`/`gateway`, `CanonicalSchema.requiresRoute` — campi NON usati da F2; verifica con tsc su mapper test (no errori incompatibility) |
</threat_model>

<verification>
- `pnpm --filter @gluezero/routing test -- --run augment.test.ts` 5/5 passing
- `pnpm --filter @gluezero/routing build` produces `dist/augment.js`
- `pnpm --filter @gluezero/mapper test` continua a passare (regression check — augment routing non rompe F2)
- `pnpm --filter @gluezero/core test` continua a passare
</verification>

<success_criteria>
1. augment.ts con declaration merging a `@gluezero/core` (PluginDescriptor.routes, BrokerConfig.routes/gateway) e `@gluezero/mapper` (CanonicalSchema.requiresRoute)
2. F3PipelineStep literal union esportato
3. `__augmentLoaded: true` runtime symbol esportato
4. Barrel `index.ts` espone tutti i tipi pubblici F3 (≥ 18 type export)
5. Test compile-time 5/5 passing
6. `package.json.sideEffects` array preserva `dist/augment.js`
</success_criteria>

<output>
Crea `.planning/phases/03-routing-server-gateway-http/03-03-SUMMARY.md` con:
- Conferma 5 test compile-time passing
- Conferma build produce `dist/augment.js`
- Conferma sideEffects array
- Lista delle estensioni applicate (PluginDescriptor.routes, BrokerConfig.routes/gateway, CanonicalSchema.requiresRoute, F3PipelineStep)
- Note per plan 03-04 (parallel): augment gateway segue stesso pattern ma augmenta SOLO `BrokerConfig.gateway` (ridondante con questo plan ma necessario per dependency boundary chiaro)
</output>
