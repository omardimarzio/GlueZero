---
phase: 02-canonical-model-mapper
plan: 08
type: execute
wave: 5
depends_on: [02-07]
files_modified:
  - packages/mapper/src/inspector.ts
  - packages/mapper/src/inspector.test.ts
autonomous: true
requirements:
  - MAP-15
  - MAP-16
tags:
  - inspector
  - tdd
  - tap-extension
  - debug-snapshot
must_haves:
  truths:
    - "`MappingInspector.recordSnapshot(step, snapshot)` registra ciascuna invocazione tap dei 5 step F2 (passi 4, 5, 6, 11, 12)"
    - "`MappingInspector.getSnapshot()` ritorna `{ canonicalSchemas: number, registeredAliases: number, registeredTransforms: number, lastMappingErrors: BrokerError[] }` (D-48)"
    - "`MappingInspector.lastErrors()` ritorna ring buffer degli ultimi N (default 10) errori `mapping.*` per debug"
    - "Il ring buffer è bounded (default 10) per evitare memory leak"
    - "`MappingInspector.recordError(error)` aggiunge un errore al ring buffer; eccede dimensione → drop FIFO"
    - "Il helper `wrapTap(originalTap, inspector)` produce un nuovo `EventTap` che chiama il tap originale + il recordSnapshot dell'inspector (composition pattern)"
    - "Tutti i test unit di `inspector.test.ts` passano"
  artifacts:
    - path: "packages/mapper/src/inspector.ts"
      provides: "MappingInspector class con recordSnapshot/recordError/getSnapshot/lastErrors + wrapTap helper"
      contains: "class MappingInspector"
    - path: "packages/mapper/src/inspector.test.ts"
      provides: "Test unit per MappingInspector coprendo D-46/47/48 + ring buffer"
      contains: "describe('MappingInspector'"
  key_links:
    - from: "packages/mapper/src/inspector.ts"
      to: "packages/core (via @gluezero/core)"
      via: "import type { EventTap, PipelineSnapshot, BrokerError }"
      pattern: "@gluezero/core"
    - from: "packages/mapper/src/inspector.ts"
      to: "packages/mapper/src/canonical-registry.ts"
      via: "lette stat via constructor injection (read-only)"
      pattern: "CanonicalRegistry"
---

<objective>
Implementare `MappingInspector` — estensione di `EventTap` di F1 (D-46) per i 5 nuovi step F2 della pipeline §28 (`event.source.resolved`, `event.mapped.canonical`, `event.canonical.validated`, `event.mapped.consumer`, `event.final.validated`). NON un'API parallela: il Broker wrapper (plan 02-10) compone questo inspector con il tap esistente via `wrapTap` helper.

Purpose: il `Broker.getDebugSnapshot()` di F1 viene esteso (in plan 02-10) con sezione `mappings: { canonicalSchemas, registeredAliases, registeredTransforms, lastMappingErrors }` (D-48). Il MappingInspector è la sorgente di queste statistiche. Inspector reale full-snapshot (payload before/after per evento) **deferred a F6** (TOOL-01) — F2 V1 espone solo counters + ring buffer degli ultimi errori.

Output: 1 module + 1 test file co-locato. TDD RED→GREEN. Nessun export pubblico ancora — sarà ri-esportato dal barrel in plan 02-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-model-mapper/02-CONTEXT.md
@.planning/phases/02-canonical-model-mapper/02-PATTERNS.md
@.planning/phases/02-canonical-model-mapper/02-07-SUMMARY.md
@packages/core/src/core/event-tap.ts
@packages/core/src/core/broker.ts
@packages/mapper/src/canonical-registry.ts
@packages/mapper/src/alias-registry.ts
@packages/mapper/src/transform-pipeline.ts
@prd.md
</context>

<interfaces>
<!-- Da @gluezero/core -->

```typescript
import type { EventTap, PipelineSnapshot, PipelineStep, BrokerError } from '@gluezero/core'
import { safeTapStep } from '@gluezero/core'  // utility per tap wrapping

// EventTap signature:
export interface EventTap {
  onPipelineStep(step: PipelineStep, snapshot: PipelineSnapshot): void
}

// Pattern wrapTap (composition):
function wrapTap(original: EventTap, inspector: MappingInspector): EventTap {
  return {
    onPipelineStep(step, snapshot) {
      original.onPipelineStep(step, snapshot)
      inspector.recordSnapshot(step, snapshot)
    },
  }
}
```

<!-- Wave 4 modules disponibili (read-only injection per stats) -->

```typescript
class CanonicalRegistry { list(): string[] }
class AliasRegistry { listGlobal(): Array<[string, string]> }
class TransformPipeline { list(): string[] }
```

<!-- Step F2 attesi (D-50): -->

```typescript
// I 5 nuovi step F2 (saranno aggiunti via TS declaration merging in plan 02-09 augment.ts):
type F2PipelineStep =
  | 'event.source.resolved'        // step 4
  | 'event.mapped.canonical'       // step 5
  | 'event.canonical.validated'    // step 6
  | 'event.mapped.consumer'        // step 11
  | 'event.final.validated'        // step 12
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inspector.test.ts (RED) + inspector.ts (GREEN)</name>
  <files>packages/mapper/src/inspector.ts, packages/mapper/src/inspector.test.ts</files>
  <read_first>
    - packages/core/src/core/event-tap.ts (riga 1-fine — modello + safeTapStep)
    - packages/core/src/core/broker.ts (riga 59-66 — modello BrokerDebugSnapshot)
    - .planning/phases/02-canonical-model-mapper/02-CONTEXT.md decisioni D-46, D-47, D-48
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §2.2 riga "inspector.ts"
    - prd.md §14.8 (Mapping Inspector requisiti)
  </read_first>
  <behavior>
    - Test 1: `MappingInspector` constructor accetta `{ canonicalRegistry, aliasRegistry, transformPipeline, errorBufferSize? }`
    - Test 2: `recordSnapshot(step, snapshot)` accetta i 5 step F2 (`event.source.resolved`, `event.mapped.canonical`, `event.canonical.validated`, `event.mapped.consumer`, `event.final.validated`) + i 5 F1 (no-op per F1 step — passa attraverso senza side effect)
    - Test 3: `recordSnapshot` con un F2 step incrementa il counter `f2StepInvocations[step]` (utile per stats)
    - Test 4: `recordError(error)` aggiunge l'errore al ring buffer
    - Test 5: Ring buffer bounded: dopo 11 recordError con `errorBufferSize: 10` → `lastErrors()` ritorna gli ultimi 10 (FIFO drop del primo)
    - Test 6: `lastErrors()` ritorna copia (mutation esterna non altera state)
    - Test 7: `getSnapshot()` ritorna `{ canonicalSchemas: 2, registeredAliases: 1, registeredTransforms: 3, lastMappingErrors: BrokerError[] }` quando 2 schema + 1 global alias + 3 transform sono registrati
    - Test 8: `wrapTap(originalTap, inspector)` ritorna un EventTap che chiama PRIMA il tap originale + POI inspector.recordSnapshot
    - Test 9: `wrapTap` con originalTap che throw → inspector NON throw + recordSnapshot comunque invocato (graceful: usa try/catch attorno al tap originale per non rompere il chain)
    - Test 10: `clearErrors()` svuota il ring buffer; `lastErrors()` post-clear ritorna `[]`
  </behavior>
  <action>
**1. Creare `packages/mapper/src/inspector.test.ts` (TDD RED):**

```ts
import { describe, expect, it, vi } from 'vitest'
import { createBrokerError } from '@gluezero/core'
import type { EventTap, PipelineSnapshot, PipelineStep } from '@gluezero/core'
import { CanonicalRegistry } from './canonical-registry'
import { AliasRegistry } from './alias-registry'
import { TransformPipeline } from './transform-pipeline'
import { MappingInspector, wrapTap } from './inspector'
import type { CanonicalSchemaId } from './types'

const makeSnap = (step: PipelineStep): PipelineSnapshot => ({
  eventId: 'evt-1',
  topic: 'test.topic',
  step,
  timestamp: Date.now(),
  durationMs: 0,
})

const makeInspector = (errorBufferSize = 10): {
  inspector: MappingInspector
  canonical: CanonicalRegistry
  alias: AliasRegistry
  transform: TransformPipeline
} => {
  const canonical = new CanonicalRegistry()
  const alias = new AliasRegistry()
  const transform = new TransformPipeline()
  const inspector = new MappingInspector({
    canonicalRegistry: canonical,
    aliasRegistry: alias,
    transformPipeline: transform,
    errorBufferSize,
  })
  return { inspector, canonical, alias, transform }
}

describe('MappingInspector', () => {
  it('constructor accepts dependencies', () => {
    const { inspector } = makeInspector()
    expect(inspector).toBeInstanceOf(MappingInspector)
  })

  it('recordSnapshot accepts F1 + F2 pipeline steps without throwing', () => {
    const { inspector } = makeInspector()
    const f1Steps: PipelineStep[] = ['event.received', 'event.delivered']
    for (const step of f1Steps) {
      expect(() => inspector.recordSnapshot(step, makeSnap(step))).not.toThrow()
    }
    const f2Steps = [
      'event.source.resolved',
      'event.mapped.canonical',
      'event.canonical.validated',
      'event.mapped.consumer',
      'event.final.validated',
    ] as PipelineStep[]
    for (const step of f2Steps) {
      expect(() => inspector.recordSnapshot(step, makeSnap(step))).not.toThrow()
    }
  })

  it('recordError appends error to ring buffer; lastErrors returns copy', () => {
    const { inspector } = makeInspector()
    const err1 = createBrokerError({ code: 'mapping.field.missing', category: 'mapping', message: 'm1' })
    const err2 = createBrokerError({ code: 'mapping.transform.failed', category: 'mapping', message: 'm2' })
    inspector.recordError(err1)
    inspector.recordError(err2)
    const errors = inspector.lastErrors()
    expect(errors).toHaveLength(2)
    expect(errors[0]?.code).toBe('mapping.field.missing')
    expect(errors[1]?.code).toBe('mapping.transform.failed')

    // Mutation esterna non altera state interno
    errors.length = 0
    expect(inspector.lastErrors()).toHaveLength(2)
  })

  it('ring buffer bounded: keeps last N errors (FIFO drop)', () => {
    const { inspector } = makeInspector(3)
    for (let i = 0; i < 5; i++) {
      inspector.recordError(
        createBrokerError({ code: 'mapping.field.missing', category: 'mapping', message: `m${i}` }),
      )
    }
    const errors = inspector.lastErrors()
    expect(errors).toHaveLength(3)
    expect(errors[0]?.message).toBe('m2')
    expect(errors[1]?.message).toBe('m3')
    expect(errors[2]?.message).toBe('m4')
  })

  it('getSnapshot returns counts from registries (D-48)', () => {
    const { inspector, canonical, alias, transform } = makeInspector()
    canonical.register({
      id: 'a' as CanonicalSchemaId,
      fields: { x: { type: 'string' } },
    })
    canonical.register({
      id: 'b' as CanonicalSchemaId,
      fields: { y: { type: 'string' } },
    })
    alias.registerGlobal('city', 'location')
    transform.register('t1', (x) => x)
    transform.register('t2', (x) => x)
    transform.register('t3', (x) => x)

    const snap = inspector.getSnapshot()
    expect(snap.canonicalSchemas).toBe(2)
    expect(snap.registeredAliases).toBe(1)
    expect(snap.registeredTransforms).toBe(3)
    expect(snap.lastMappingErrors).toEqual([])
  })

  it('clearErrors empties the buffer', () => {
    const { inspector } = makeInspector()
    inspector.recordError(
      createBrokerError({ code: 'mapping.field.missing', category: 'mapping', message: 'm' }),
    )
    expect(inspector.lastErrors()).toHaveLength(1)
    inspector.clearErrors()
    expect(inspector.lastErrors()).toHaveLength(0)
  })
})

describe('wrapTap', () => {
  it('wraps original tap + inspector recordSnapshot (composition)', () => {
    const { inspector } = makeInspector()
    const originalTap: EventTap = { onPipelineStep: vi.fn() }
    const wrapped = wrapTap(originalTap, inspector)
    const snap = makeSnap('event.received')
    wrapped.onPipelineStep('event.received', snap)
    expect(originalTap.onPipelineStep).toHaveBeenCalledWith('event.received', snap)
    // Inspector did not throw → ok
  })

  it('original tap throw is swallowed; recordSnapshot still invoked', () => {
    const { inspector } = makeInspector()
    const recordSpy = vi.spyOn(inspector, 'recordSnapshot')
    const originalTap: EventTap = {
      onPipelineStep: vi.fn(() => {
        throw new Error('original tap boom')
      }),
    }
    const wrapped = wrapTap(originalTap, inspector)
    expect(() => wrapped.onPipelineStep('event.received', makeSnap('event.received'))).not.toThrow()
    expect(recordSpy).toHaveBeenCalled()
  })
})
```

Eseguire `pnpm --filter @gluezero/mapper test inspector` — atteso FAIL.

**2. Creare `packages/mapper/src/inspector.ts`:**

```ts
// MappingInspector — estensione EventTap per i 5 step F2 (PRD §14.8, REQ MAP-15/MAP-16).
//
// Riferimento decisioni (CONTEXT 02):
// - D-46: estende EventTap di F1 — NON un'API parallela. wrapTap helper compose.
// - D-47: PipelineSnapshot esteso con `metadata.transformsApplied`/`metadata.ambiguityWarnings`
//   nei nuovi 5 step (popolati dal mapper-engine in plan 02-10).
// - D-48: getSnapshot() ritorna { canonicalSchemas, registeredAliases, registeredTransforms,
//   lastMappingErrors }. Inspector reale full-snapshot per evento → F6 (TOOL-01).
//
// Pattern F1 replicato:
// - safeTapStep di event-tap.ts (riga 23-34 — try/catch swallow per tap chain)
// - BrokerDebugSnapshot di broker.ts (riga 59-66 — shape per stats)
//
// Ring buffer bounded: il `lastErrors()` ritorna max N (default 10) errori con FIFO drop.
// Evita memory leak in caso di error storm.
//
// Threat coverage:
// - T-02-08-01 (DoS — ring buffer cresce indefinitamente): bounded buffer + FIFO drop.
// - T-02-08-02 (Information disclosure — error messages contengono PII): F2 V1 best-effort,
//   F6 redaction obbligatoria per produzione.

import type { BrokerError, EventTap, PipelineSnapshot, PipelineStep } from '@gluezero/core'
import type { AliasRegistry } from './alias-registry'
import type { CanonicalRegistry } from './canonical-registry'
import type { TransformPipeline } from './transform-pipeline'

export interface MappingInspectorOptions {
  readonly canonicalRegistry: CanonicalRegistry
  readonly aliasRegistry: AliasRegistry
  readonly transformPipeline: TransformPipeline
  readonly errorBufferSize?: number
}

export interface MappingInspectorSnapshot {
  readonly canonicalSchemas: number
  readonly registeredAliases: number
  readonly registeredTransforms: number
  readonly lastMappingErrors: BrokerError[]
}

export class MappingInspector {
  private readonly canonicalRegistry: CanonicalRegistry
  private readonly aliasRegistry: AliasRegistry
  private readonly transformPipeline: TransformPipeline
  private readonly errorBuffer: BrokerError[] = []
  private readonly errorBufferSize: number

  constructor(options: MappingInspectorOptions) {
    this.canonicalRegistry = options.canonicalRegistry
    this.aliasRegistry = options.aliasRegistry
    this.transformPipeline = options.transformPipeline
    this.errorBufferSize = options.errorBufferSize ?? 10
  }

  /**
   * Registra un'invocazione del tap (F1 + F2 step). Per F2 V1 è no-op runtime
   * (i counter sono lette via getSnapshot). F6 estenderà con full snapshot per evento.
   */
  recordSnapshot(_step: PipelineStep, _snapshot: PipelineSnapshot): void {
    // F2 V1: no-op. F6 popolerà ring buffer di snapshot per evento (TOOL-01).
    // F2 V1: counter incrementi già esposti via stats() del MapperEngine + size dei registry.
  }

  recordError(error: BrokerError): void {
    this.errorBuffer.push(error)
    if (this.errorBuffer.length > this.errorBufferSize) {
      this.errorBuffer.shift() // FIFO drop oldest
    }
  }

  lastErrors(): BrokerError[] {
    return [...this.errorBuffer]
  }

  clearErrors(): void {
    this.errorBuffer.length = 0
  }

  getSnapshot(): MappingInspectorSnapshot {
    return {
      canonicalSchemas: this.canonicalRegistry.list().length,
      registeredAliases: this.aliasRegistry.listGlobal().length,
      registeredTransforms: this.transformPipeline.list().length,
      lastMappingErrors: this.lastErrors(),
    }
  }
}

/**
 * Helper composition per wrappare un tap esistente con il MappingInspector.
 * Errori del tap originale sono swallowed (pattern safeTapStep F1) per non rompere il chain.
 */
export function wrapTap(original: EventTap, inspector: MappingInspector): EventTap {
  return {
    onPipelineStep(step, snapshot): void {
      try {
        original.onPipelineStep(step, snapshot)
      } catch {
        // Swallow — il tap originale non deve rompere il chain.
      }
      inspector.recordSnapshot(step, snapshot)
    },
  }
}
```

**3. Eseguire test:**

```bash
pnpm --filter @gluezero/mapper test inspector
```

Atteso: 9 test passano.

**4. Verificare typecheck:**

```bash
pnpm --filter @gluezero/mapper typecheck
```

Atteso: exit 0.
  </action>
  <verify>
    <automated>test -f packages/mapper/src/inspector.ts && test -f packages/mapper/src/inspector.test.ts && grep -q "export class MappingInspector" packages/mapper/src/inspector.ts && grep -q "export function wrapTap" packages/mapper/src/inspector.ts && grep -q "lastErrors" packages/mapper/src/inspector.ts && grep -q "errorBuffer" packages/mapper/src/inspector.ts && pnpm --filter @gluezero/mapper test inspector 2>&1 | grep -E "Test Files.*1.*passed" && pnpm --filter @gluezero/mapper typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `inspector.ts` esporta `class MappingInspector` + `wrapTap` helper + `MappingInspectorOptions` + `MappingInspectorSnapshot`
    - Class ha 5 metodi pubblici: `recordSnapshot`, `recordError`, `lastErrors`, `clearErrors`, `getSnapshot`
    - `getSnapshot()` ritorna `{ canonicalSchemas, registeredAliases, registeredTransforms, lastMappingErrors }`
    - Ring buffer bounded a `errorBufferSize` (default 10) con FIFO drop
    - `wrapTap(originalTap, inspector)` ritorna `EventTap` che chiama original + recordSnapshot
    - `wrapTap` swallow errori del tap originale (graceful chain)
    - File test ha almeno 9 test cases
    - `pnpm --filter @gluezero/mapper test inspector` esce 0
    - `pnpm --filter @gluezero/mapper typecheck` esce 0
  </acceptance_criteria>
  <done>MappingInspector + wrapTap creati. D-46/47/48 implementati (V1 — counter only; full snapshot per evento deferred F6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Original tap (consumer) → wrapTap | Tap originale potrebbe lanciare; inspector chain deve essere isolato |
| Error buffer → memory growth | Ring buffer bounded previene leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | DoS | errorBuffer cresce senza bound | mitigate | `errorBufferSize` default 10 + FIFO drop. Test 5 verifica. |
| T-02-08-02 | Information disclosure | error messages contengono PII | accept | F2 V1 best-effort; F6 redaction obbligatoria per produzione (DOC-03 documenterà best practice). |
| T-02-08-03 | DoS | original tap throw rompe wrapTap chain | mitigate | try/catch swallow pattern F1 (event-tap.ts:23-34). Test 9 verifica. |
| T-02-08-04 | Tampering | `lastErrors()` ritorna reference interno → mutation | mitigate | spread copy `[...this.errorBuffer]`. Test 6 verifica. |
| T-02-08-05 | Repudiation | `recordSnapshot` no-op per F1 step → debug confuso | accept | Intenzionale — F2 V1 espone solo stats; full per-event snapshot F6. JSDoc esplicita. |
</threat_model>

<verification>
- 1 file source + 1 file test esistenti in `packages/mapper/src/`
- `pnpm --filter @gluezero/mapper test inspector` esce 0 con `Test Files 1 passed (1)`
- `pnpm --filter @gluezero/mapper typecheck` esce 0
- Code coverage ≥ 90% su `inspector.ts` (verificato al merge wave plan 02-12)
</verification>

<success_criteria>
- REQ-IDs Phase 2 indirizzati:
  - **MAP-15** ✓ (Mapping Inspector ritorna canonicalSchemas/registeredAliases/registeredTransforms/lastMappingErrors — full payload before/after per evento deferred F6 come da D-48)
  - **MAP-16** ✓ (warning runtime via lastErrors — il mapper-engine plan 02-10 chiama recordError quando alias resolution è ambigua)
- D-46 (estende EventTap di F1, NON parallel API) implementato via wrapTap composition
- D-47 (snapshot esteso) — placeholder per F2 V1; metadata.transformsApplied/ambiguityWarnings popolati nel broker wrapper plan 02-10
- D-48 (getSnapshot section mappings) implementato
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-model-mapper/02-08-SUMMARY.md` con:
- Module + test creati (inspector.ts/.test.ts)
- 9+ test passing
- Decisioni: nessuna deviazione da PATTERNS.md §2.2; `recordSnapshot` no-op per F2 V1 documentato come scope (full snapshot deferred F6 — D-48); wrapTap come composition pattern (D-46)
- Pronto-per: plan 02-09 (augment.ts — TS declaration merging dei 5 nuovi step PipelineStep + InputMap/OutputMap su PluginDescriptor + sezioni BrokerConfig)
</output>
