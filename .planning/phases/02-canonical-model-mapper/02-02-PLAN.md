---
phase: 02-canonical-model-mapper
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/mapper/src/types/canonical-schema.ts
  - packages/mapper/src/types/input-output-map.ts
  - packages/mapper/src/types/transform.ts
  - packages/mapper/src/types/validator-adapter.ts
  - packages/mapper/src/types/mapping-error.ts
  - packages/mapper/src/types/index.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-12
  - VAL-03
  - VAL-04
tags:
  - types
  - foundation
  - public-surface
  - tdd-prep
must_haves:
  truths:
    - "`CanonicalSchema` interface dichiara `id: CanonicalSchemaId` (branded), `requires?: string[]`, `fields: Record<string, FieldDescriptor>` (D-36, D-42)"
    - "`FieldDescriptor` dichiara `required: boolean` (default false), `default?: unknown`, `onFailure: 'block' | 'skip' | 'fallback'` (default 'block') (D-42, D-44)"
    - "`InputMap` e `OutputMap` sono `Record<string, MappingRule>` con `MappingRule` discriminata per rename/nested/transform/derive/default (PRD §14.2)"
    - "`TransformFn` ha signature `(input: unknown, ctx: TransformContext) => unknown` (D-44)"
    - "`ValidatorAdapter` ritorna discriminated union `{ ok: true; value } | { ok: false; issues }` (D-38)"
    - "`MappingErrorCode` literal union dei 5 codici F2: 'mapping.cycle.detected', 'mapping.transform.failed', 'mapping.field.missing', 'mapping.canonical.validation.failed', 'mapping.consumer.validation.failed' (D-58)"
    - "`CanonicalSchemaId` e `TransformName` sono branded types con `unique symbol` (Pitfall #12)"
    - "Barrel `types/index.ts` esporta tutti i tipi pubblici via `export type *` (verbatimModuleSyntax)"
    - "Nessun `unknown` placeholder lasciato (D-32 chiude i placeholder F1)"
    - "`pnpm --filter @gluezero/mapper typecheck` esce 0 con `isolatedDeclarations: true` enforcement"
  artifacts:
    - path: "packages/mapper/src/types/canonical-schema.ts"
      provides: "CanonicalSchema, CanonicalSchemaId (branded), FieldDescriptor, FieldType"
      contains: "CanonicalSchema"
    - path: "packages/mapper/src/types/input-output-map.ts"
      provides: "InputMap, OutputMap, MappingRule, DeriveDescriptor"
      contains: "MappingRule"
    - path: "packages/mapper/src/types/transform.ts"
      provides: "TransformFn, TransformDescriptor, TransformContext, TransformName (branded)"
      contains: "TransformFn"
    - path: "packages/mapper/src/types/validator-adapter.ts"
      provides: "ValidatorAdapter, ValidationIssue, ValidationResult"
      contains: "ValidatorAdapter"
    - path: "packages/mapper/src/types/mapping-error.ts"
      provides: "MappingErrorCode literal union + helper isMappingErrorCode"
      contains: "MappingErrorCode"
    - path: "packages/mapper/src/types/index.ts"
      provides: "Barrel re-export type-only di tutti i tipi pubblici F2"
      contains: "export type"
  key_links:
    - from: "packages/mapper/src/types/canonical-schema.ts"
      to: "packages/mapper/src/types/input-output-map.ts"
      via: "(no import: i due tipi sono indipendenti, collegati semanticamente nel mapper-engine)"
      pattern: "MappingRule"
    - from: "packages/mapper/src/types/transform.ts"
      to: "packages/mapper/src/types/canonical-schema.ts"
      via: "TransformContext include riferimento opzionale a CanonicalSchemaId"
      pattern: "CanonicalSchemaId"
    - from: "packages/mapper/src/types/index.ts"
      to: "packages/mapper/src/index.ts (barrel mapper)"
      via: "export type * from './types' (attivato in 02-09 augment.ts plan)"
      pattern: "export type"
---

<objective>
Definire la **superficie tipi pubblici** del package `@gluezero/mapper` (Wave 2 — gating per Wave 3 paralleli). Tutti i moduli runtime dei plan successivi (02-03..02-08) importano da `./types/`. La definizione precisa qui è il "contract first" che permette la parallelizzazione di Wave 3 (canonical-registry || alias-registry || transform-pipeline || valibot-adapter — file ownership disgiunta).

Purpose: chiudere i `unknown` placeholder F1 (D-32 — `BrokerConfig.canonicalModel/aliasRegistry/transforms`, `PluginDescriptor.inputMap/outputMap`) con tipi specifici. Le sigle delle decisioni D-31..D-59 di CONTEXT.md determinano la shape esatta. NESSUN `unknown` rimanente nei tipi pubblici.

Output: 6 file `types/*.ts` (5 modulo + 1 barrel) con isolatedDeclarations + verbatimModuleSyntax enforcement. Nessun runtime — solo `interface`/`type`/branded `unique symbol`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-model-mapper/02-CONTEXT.md
@.planning/phases/02-canonical-model-mapper/02-PATTERNS.md
@.planning/phases/02-canonical-model-mapper/02-01-SUMMARY.md
@packages/core/src/types/broker-event.ts
@packages/core/src/types/error.ts
@packages/core/src/types/tap.ts
@packages/core/src/types/plugin.ts
@packages/core/src/types/index.ts
@prd.md
</context>

<interfaces>
<!-- Tipi disponibili da @gluezero/core (importabili in F2 via cross-package). -->

Da `@gluezero/core` (per validator-adapter.ts e mapping-error.ts):
```typescript
export type ErrorCategory = 'validation' | 'plugin' | 'mapping' | 'route' | 'network' | 'worker' | 'system' | 'config' | 'topic'
export interface BrokerError extends Error {
  readonly code: string
  readonly category: ErrorCategory
  readonly details?: Record<string, unknown>
  readonly originalError?: Error
  readonly routeId?: string
  readonly topic?: string
  readonly eventId?: string
}
```

Da `@gluezero/core` (per transform.ts TransformContext):
```typescript
export interface BrokerLogger {
  error(message: string, meta?: Record<string, unknown>): void
  warn(message: string, meta?: Record<string, unknown>): void
  info(message: string, meta?: Record<string, unknown>): void
  debug(message: string, meta?: Record<string, unknown>): void
  trace(message: string, meta?: Record<string, unknown>): void
}
```

Pattern enforcement F1 da replicare:
- Branded types con `declare const __brand: unique symbol` (vedi `broker-event.ts:54-61` `EventId`)
- `readonly` su tutti i field interface (vedi `broker-event.ts:37-52`)
- `exactOptionalPropertyTypes` policy (no `undefined` esplicito; usa `field?: T` solo)
- `verbatimModuleSyntax` policy (re-export con `export type` esplicito)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tipi canonical schema + transform + input/output map</name>
  <files>packages/mapper/src/types/canonical-schema.ts, packages/mapper/src/types/input-output-map.ts, packages/mapper/src/types/transform.ts</files>
  <read_first>
    - .planning/phases/02-canonical-model-mapper/02-CONTEXT.md decisioni D-32, D-33, D-36, D-42, D-43, D-44, D-45, D-57
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §2.1 "Tipi pubblici F2"
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §4.5 "Branded type pattern"
    - prd.md §13 (canonical model overview), §14.2 (esempi mapping), §14.5 ($derive), §14.6 (registerTransform)
    - packages/core/src/types/broker-event.ts (riga 54-61 — pattern EventId branded)
  </read_first>
  <action>
**1. Creare `packages/mapper/src/types/canonical-schema.ts`:**

```ts
// CanonicalSchema — vocabolario canonico del broker (PRD §13, REQ MAP-01/MAP-02).
//
// Riferimento decisioni (CONTEXT 02):
// - D-36: schema versioning via `requires?: string[]` (lista altri schema id richiesti).
//         Verificato al `registerCanonicalSchema` (throw se requires non risolti).
//         Plain string version (no SemVer parsing in F2).
// - D-42: VAL-08 chiusura PRD §39 #3 — `FieldDescriptor.required: boolean` (default false).
//         required:true + missing → throw 'validation.field.missing' + publish 'mapping.error'
//         required:false + missing → applica `default` se definito, altrimenti field omesso
// - D-43: `default` è valore statico (no funzioni). Per default dinamici → usa `$derive`.
// - D-44: VAL-09 chiusura PRD §39 #4 — `FieldDescriptor.onFailure: 'block' | 'skip' | 'fallback'`
//         (default 'block'). Specifica il comportamento del transform su throw.
//
// `CanonicalSchemaId` è branded (Pitfall #12 — type confusion prevention).
// Solo cast esplicito `as CanonicalSchemaId` permette di "instanziare" il tipo.

declare const __canonicalSchemaIdBrand: unique symbol

/**
 * Branded type per id canonical schema (Pitfall #12 — type confusion prevention).
 * Solo cast esplicito `as CanonicalSchemaId` permette di "instanziare" il tipo.
 */
export type CanonicalSchemaId = string & { readonly [__canonicalSchemaIdBrand]: true }

/** Field type primitivo del canonical schema. F2 supporta i tipi base + 'any' per estensioni. */
export type FieldType = 'string' | 'number' | 'boolean' | 'object' | 'array' | 'any'

/**
 * Comportamento del transform su throw (D-44 — chiusura PRD §39 #4).
 *
 * - `'block'` (default) — transform throw → mapping fallisce → publish `mapping.error` → no delivery
 * - `'skip'` — transform throw → field non valorizzato (come required:false + no default)
 * - `'fallback'` — transform throw → applica `default` se definito, altrimenti `'skip'`
 */
export type FieldFailureMode = 'block' | 'skip' | 'fallback'

/**
 * Descrittore di un campo canonico (D-42, D-43, D-44).
 *
 * `required: false` (default) + `default: T` definito → applica default se field assente.
 * `required: true` + field assente → throw `BrokerError 'validation.field.missing'`.
 *
 * Per i default dinamici (es. timestamp corrente, id generato) usa `$derive` con transform
 * registrato via `registerTransform`, NON un `default` valore (D-43).
 */
export interface FieldDescriptor {
  readonly type: FieldType
  readonly required?: boolean
  readonly default?: unknown
  readonly onFailure?: FieldFailureMode
  readonly description?: string
}

/**
 * Canonical schema definition (PRD §13, REQ MAP-01/MAP-02).
 *
 * `id` è branded — uso il pattern di `broker-event.ts:54-61` (EventId).
 * `requires` è una lista di schema id richiesti (D-36 — verificato al register).
 * `fields` è una mappa nome canonico → descrittore.
 *
 * @example
 * ```ts
 * const weatherSchema: CanonicalSchema = {
 *   id: 'weather' as CanonicalSchemaId,
 *   fields: {
 *     location: { type: 'string', required: true },
 *     forecast_date: { type: 'string', required: true },
 *     temperature_celsius: { type: 'number', required: false, onFailure: 'skip' },
 *   },
 * }
 * ```
 */
export interface CanonicalSchema {
  readonly id: CanonicalSchemaId
  readonly requires?: readonly string[]
  readonly fields: Readonly<Record<string, FieldDescriptor>>
  readonly description?: string
}
```

**2. Creare `packages/mapper/src/types/input-output-map.ts`:**

```ts
// InputMap / OutputMap / MappingRule — descrittori di mapping per plugin
// (PRD §14, §15.2; REQ MAP-03/MAP-04/MAP-05/MAP-06/MAP-07/MAP-08/MAP-09/MAP-10).
//
// Riferimento decisioni (CONTEXT 02):
// - D-32: `inputMap`/`outputMap` come campi opzionali del `PluginDescriptor`
//         (estensione tipo via TS declaration merging — vedi augment.ts plan 02-09).
// - D-40: resolution order — esplicito (inputMap/outputMap) PREVALE su alias automatici.
//
// `MappingRule` discriminata supporta i casi PRD §14.2:
//   1. Rename: `MappingRule = { source: 'città' }` → mappa `città` su nome canonico
//   2. Nested: `MappingRule = { source: 'address.city' }` → dot-path
//   3. Default: `MappingRule = { default: 'normal' }` → applica se assente
//   4. Format transform: `MappingRule = { source: 'data', transform: 'parseItalianDate' }`
//   5. Unit normalization: `MappingRule = { source: 'temp', transform: 'parseTempCelsius' }`
//   6. Derive: `MappingRule = { derive: { sources: ['firstName','lastName'], transform: 'concat' } }`
//   7. Partial: solo i campi dichiarati nella mappa vengono mappati (D-40)

/**
 * Descrittore di derive: produce un valore canonico combinando più field source via transform.
 *
 * Pattern PRD §14.5 (`fullName: $derive(['firstName','lastName'], (a,b) => `${a} ${b}`)`).
 * `transform` è il name registrato via `registerTransform` (D-31).
 */
export interface DeriveDescriptor {
  readonly sources: readonly string[]
  readonly transform: string
}

/**
 * Regola di mapping per un singolo campo canonico (PRD §14.2).
 *
 * Tutti i field sono opzionali ma almeno uno tra `source` / `derive` / `default` deve essere
 * presente per produrre un valore. Validazione runtime nel mapper-engine (plan 02-07).
 *
 * - `source`: nome del field locale (string semplice oppure dot-path per nested PRD §14.2.2)
 * - `transform`: nome del transform registrato (PRD §14.6, REQ MAP-12)
 * - `default`: valore statico applicato se source assente (PRD §14.2.3, D-43)
 * - `derive`: combinazione di più source via transform (PRD §14.2.6, REQ MAP-09)
 */
export interface MappingRule {
  readonly source?: string
  readonly transform?: string
  readonly default?: unknown
  readonly derive?: DeriveDescriptor
}

/**
 * Mappa locale → canonico per un plugin publisher (PRD §15.2).
 *
 * Chiave: nome canonico del field; valore: `MappingRule` che descrive come ottenerlo
 * dal payload locale.
 *
 * @example
 * ```ts
 * const weatherFormOutputMap: OutputMap = {
 *   location: { source: 'città' },
 *   forecast_date: { source: 'data', transform: 'parseItalianDate' },
 * }
 * ```
 */
export type OutputMap = Readonly<Record<string, MappingRule>>

/**
 * Mappa canonico → locale per un plugin consumer (PRD §15.2).
 *
 * Stessa shape di `OutputMap` ma applicata in direzione inversa al passo 11
 * della pipeline §28 (canonical → consumer).
 *
 * @example
 * ```ts
 * const weatherWidgetInputMap: InputMap = {
 *   'day-prevision': { source: 'forecast_date' },
 * }
 * ```
 */
export type InputMap = Readonly<Record<string, MappingRule>>
```

**3. Creare `packages/mapper/src/types/transform.ts`:**

```ts
// TransformFn / TransformDescriptor / TransformContext (PRD §14.6, REQ MAP-12).
//
// Riferimento decisioni (CONTEXT 02):
// - D-31: API pubblica `registerTransform(name, fn)` — coerente con `registerPlugin/Route` di F1.
// - D-44: `onFailure: 'block' | 'skip' | 'fallback'` decide cosa fare su throw del transform.
// - D-45: errore wrapped in `BrokerError 'mapping.transform.failed'` con `originalError`+`cause` ES2022.
//
// `TransformName` è branded (Pitfall #12) — solo cast esplicito permette di instanziare.
//
// `TransformContext` espone al transform un sotto-insieme readonly del runtime broker:
//   - logger (per debug log strutturati)
//   - pluginId (chi ha pubblicato l'evento — utile per error attribution)
//   - fieldName (quale field canonical si sta producendo)
//   - canonicalSchemaId (a quale schema il field appartiene — opzionale)

import type { BrokerLogger } from '@gluezero/core'
import type { CanonicalSchemaId } from './canonical-schema'

declare const __transformNameBrand: unique symbol

/** Branded type per nome transform (Pitfall #12). Cast esplicito `as TransformName`. */
export type TransformName = string & { readonly [__transformNameBrand]: true }

/**
 * Contesto readonly passato al transform a runtime (D-44).
 *
 * Il transform può loggare via `ctx.logger`, leggere `pluginId`/`fieldName`/`canonicalSchemaId`
 * per error attribution. NON può mutare nulla.
 */
export interface TransformContext {
  readonly logger: BrokerLogger
  readonly pluginId: string
  readonly fieldName: string
  readonly canonicalSchemaId?: CanonicalSchemaId
}

/**
 * Signature di un transform registrato.
 *
 * Input `unknown` per supportare i casi mixed (rename, format, derive — il caller passa
 * il valore del source field). Output `unknown` perché il mapper-engine validerà il tipo
 * canonico atteso al passo 6 (canonical validation).
 *
 * Su throw, il mapper-engine applica D-44 onFailure policy del field.
 */
export type TransformFn = (input: unknown, ctx: TransformContext) => unknown

/**
 * Descrittore di un transform registrato via `registerTransform(name, fn)` (D-31).
 *
 * `name` è il nome lookup (string per ergonomia; il cast a `TransformName` avviene
 * al register interno per audit branding). `description` opzionale per Inspector debug.
 */
export interface TransformDescriptor {
  readonly name: string
  readonly fn: TransformFn
  readonly description?: string
}
```

**4. Eseguire typecheck:**

```bash
pnpm --filter @gluezero/mapper typecheck
```

Atteso: exit 0. Nessun errore TS (`isolatedDeclarations` enforcement: ogni export ha return type esplicito).
  </action>
  <verify>
    <automated>test -f packages/mapper/src/types/canonical-schema.ts && test -f packages/mapper/src/types/input-output-map.ts && test -f packages/mapper/src/types/transform.ts && grep -q "export interface CanonicalSchema" packages/mapper/src/types/canonical-schema.ts && grep -q "unique symbol" packages/mapper/src/types/canonical-schema.ts && grep -q "FieldFailureMode" packages/mapper/src/types/canonical-schema.ts && grep -q "MappingRule" packages/mapper/src/types/input-output-map.ts && grep -q "DeriveDescriptor" packages/mapper/src/types/input-output-map.ts && grep -q "TransformFn" packages/mapper/src/types/transform.ts && grep -q "TransformContext" packages/mapper/src/types/transform.ts && grep -q "@gluezero/core" packages/mapper/src/types/transform.ts && pnpm --filter @gluezero/mapper typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `canonical-schema.ts` esporta `CanonicalSchema`, `CanonicalSchemaId` (branded), `FieldDescriptor`, `FieldFailureMode`, `FieldType`
    - File `input-output-map.ts` esporta `InputMap`, `OutputMap`, `MappingRule`, `DeriveDescriptor`
    - File `transform.ts` esporta `TransformFn`, `TransformDescriptor`, `TransformContext`, `TransformName` (branded)
    - Tutti i field readonly enforced (no mutable interface)
    - Branded types con `declare const __brand: unique symbol` (vedi `broker-event.ts:54-61` pattern)
    - Import `BrokerLogger` da `@gluezero/core` con `import type` esplicito (verbatimModuleSyntax)
    - `pnpm --filter @gluezero/mapper typecheck` esce 0
  </acceptance_criteria>
  <done>3 moduli tipi canonici + transform creati. Pronto per validator-adapter + mapping-error + barrel.</done>
</task>

<task type="auto">
  <name>Task 2: ValidatorAdapter + MappingErrorCode + barrel index</name>
  <files>packages/mapper/src/types/validator-adapter.ts, packages/mapper/src/types/mapping-error.ts, packages/mapper/src/types/index.ts</files>
  <read_first>
    - .planning/phases/02-canonical-model-mapper/02-CONTEXT.md decisioni D-37, D-38, D-39, D-58, D-59
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §2.1 (riga "validator-adapter.ts" + "mapping-error.ts")
    - packages/core/src/types/index.ts (riga 1-41 — modello barrel pattern)
    - prd.md §22.3 (eventi standard di errore — `mapping.error`)
  </read_first>
  <action>
**1. Creare `packages/mapper/src/types/validator-adapter.ts`:**

```ts
// ValidatorAdapter — interfaccia pluggable per la validazione (PRD §21.2, REQ VAL-03/VAL-04).
//
// Riferimento decisioni (CONTEXT 02):
// - D-37: Valibot 1.x come default (già installato in F1).
// - D-38: Adapter pluggable. Adapter Zod/Ajv deferred a V2.
// - D-39: validation injection 3 step pipeline §28 (3 sintassi F1, 6 canonical F2, 12 final F2).
//
// L'adapter è agnostic dello schema (Valibot, Zod, Ajv, custom) — il caller passa lo schema
// nel formato atteso dall'adapter. Il return è una discriminated union così il caller decide
// cosa fare con i risultati (publish `mapping.error`, applica fallback, ecc.).
//
// VINCOLO: NO throw. L'adapter ritorna `{ ok: false, issues }` su fail. Solo il caller
// (mapper-engine al passo 6/12) decide se publish `mapping.error` o applicare D-44 onFailure.
// Differenza con `validateEvent` di F1 (`event-validator.ts`) che invece THROW al fail —
// qui l'adapter è più granulare per supportare `onFailure: 'skip'/'fallback'` (D-44).

/** Issue di validazione (subset di Valibot.Issue per ergonomia). */
export interface ValidationIssue {
  readonly path?: readonly string[]
  readonly message: string
  readonly expected?: string
  readonly received?: string
}

/** Risultato discriminato di una validazione (D-38). */
export type ValidationResult<T = unknown> =
  | { readonly ok: true; readonly value: T }
  | { readonly ok: false; readonly issues: readonly ValidationIssue[] }

/**
 * ValidatorAdapter pluggable (D-37, D-38).
 *
 * Default `valibotAdapter` esportato dal barrel mapper (plan 02-06). V2 supporterà
 * Zod/Ajv come adapter alternativi senza breaking change al contract.
 *
 * NO throw — sempre ritorna ValidationResult.
 *
 * @example (uso interno mapper-engine plan 02-07)
 * ```ts
 * const result = validator.validate(canonicalSchema, payload)
 * if (!result.ok) {
 *   broker.publish('mapping.error', { issues: result.issues, sourceEvent: ev.id, step: 'event.canonical.validated' })
 *   return  // skip delivery
 * }
 * ```
 */
export interface ValidatorAdapter {
  validate<T = unknown>(schema: unknown, payload: unknown): ValidationResult<T>
}
```

**2. Creare `packages/mapper/src/types/mapping-error.ts`:**

```ts
// MappingErrorCode — literal union dei codici errore mapper F2 (D-58, REQ ERR-02 ext).
//
// Riferimento decisioni (CONTEXT 02):
// - D-58: i 5 codici emessi via `BrokerEvent 'mapping.error'` con payload `{ error, sourceEvent, step }`.
// - D-59: F2 NON pubblica `<topic>.failed` (quello è F3 route HTTP failure).
//
// I codici sono tutti `category: 'mapping'` (già definita in F1 `ErrorCategory` union).
// NON un nuovo BrokerError subclass — riusa `BrokerError` di F1 con `code` settato a uno
// di questi literal. Helper `isMappingErrorCode` permette runtime narrowing.

/**
 * Literal union dei codici errore F2 (D-58).
 *
 * - `'mapping.cycle.detected'` — circular mapping al `registerPlugin` (D-35)
 * - `'mapping.transform.failed'` — transform throw + onFailure 'block' (D-44, D-45)
 * - `'mapping.field.missing'` — field required:true mancante nel source (D-42)
 * - `'mapping.canonical.validation.failed'` — Valibot validation fail al passo 6 (D-39)
 * - `'mapping.consumer.validation.failed'` — Valibot validation fail al passo 12 (D-39)
 *
 * Tutti pubblicati via `BrokerEvent 'mapping.error'` (D-58, REQ ERR-02 extension).
 */
export type MappingErrorCode =
  | 'mapping.cycle.detected'
  | 'mapping.transform.failed'
  | 'mapping.field.missing'
  | 'mapping.canonical.validation.failed'
  | 'mapping.consumer.validation.failed'

const MAPPING_ERROR_CODES: ReadonlySet<string> = new Set<MappingErrorCode>([
  'mapping.cycle.detected',
  'mapping.transform.failed',
  'mapping.field.missing',
  'mapping.canonical.validation.failed',
  'mapping.consumer.validation.failed',
])

/**
 * Type guard runtime per `MappingErrorCode`.
 *
 * Utile per branchare comportamento basato su `error.code` quando si subscribe a `mapping.error`.
 *
 * @example
 * ```ts
 * broker.subscribe('mapping.error', (event) => {
 *   const code = event.payload.error.code
 *   if (isMappingErrorCode(code)) {
 *     // safe narrow: code is MappingErrorCode
 *   }
 * })
 * ```
 */
export function isMappingErrorCode(code: string): code is MappingErrorCode {
  return MAPPING_ERROR_CODES.has(code)
}
```

**3. Creare `packages/mapper/src/types/index.ts` (barrel):**

```ts
// Barrel re-export tipi pubblici @gluezero/mapper.
//
// Pattern F1 replicato (vedi packages/core/src/types/index.ts):
// - export type { ... } esplicito (verbatimModuleSyntax: true)
// - JSDoc 1-liner per ciascun export per IntelliSense + TypeDoc-ready
// - I tipi internal NON sono ri-esportati (es. eventuali helper compile-time del mapper-engine)

export type {
  /** Branded id canonical schema (Pitfall #12 — type confusion prevention). */
  CanonicalSchemaId,
  /** Canonical schema definition con `id`, `requires?`, `fields` (PRD §13, REQ MAP-01/MAP-02). */
  CanonicalSchema,
  /** Comportamento del transform su throw — D-44 (chiusura PRD §39 #4). */
  FieldFailureMode,
  /** Descrittore di un campo canonico (`type`, `required`, `default`, `onFailure`). */
  FieldDescriptor,
  /** Field type primitivo del canonical schema. */
  FieldType,
} from './canonical-schema'

export type {
  /** Mappa locale → canonico per plugin publisher (PRD §15.2). */
  OutputMap,
  /** Mappa canonico → locale per plugin consumer (PRD §15.2). */
  InputMap,
  /** Regola di mapping per un singolo campo canonico (rename/transform/default/derive). */
  MappingRule,
  /** Descrittore derive: combina più source via transform (PRD §14.5, REQ MAP-09). */
  DeriveDescriptor,
} from './input-output-map'

export type {
  /** Branded nome transform (Pitfall #12). */
  TransformName,
  /** Signature di un transform registrato (input/ctx → output). */
  TransformFn,
  /** Descrittore di un transform registrato via `registerTransform(name, fn)` (D-31). */
  TransformDescriptor,
  /** Contesto readonly passato al transform a runtime. */
  TransformContext,
} from './transform'

export type {
  /** Adapter pluggable per validazione (Valibot default; Zod/Ajv V2). */
  ValidatorAdapter,
  /** Issue di validazione (subset di Valibot.Issue). */
  ValidationIssue,
  /** Risultato discriminato di una validazione: `{ ok: true; value } | { ok: false; issues }`. */
  ValidationResult,
} from './validator-adapter'

export type {
  /** Literal union dei 5 codici errore mapper F2 (D-58, REQ ERR-02 extension). */
  MappingErrorCode,
} from './mapping-error'

export { isMappingErrorCode } from './mapping-error'
```

NOTA: `isMappingErrorCode` è una function (runtime) — esportata con `export {` (no `type`).

**4. Verificare typecheck:**

```bash
pnpm --filter @gluezero/mapper typecheck
```

Atteso: exit 0. `isolatedDeclarations: true` enforcement passa (ogni export ha return type esplicito).
  </action>
  <verify>
    <automated>test -f packages/mapper/src/types/validator-adapter.ts && test -f packages/mapper/src/types/mapping-error.ts && test -f packages/mapper/src/types/index.ts && grep -q "export interface ValidatorAdapter" packages/mapper/src/types/validator-adapter.ts && grep -q "ValidationResult" packages/mapper/src/types/validator-adapter.ts && grep -q "MappingErrorCode" packages/mapper/src/types/mapping-error.ts && grep -q "isMappingErrorCode" packages/mapper/src/types/mapping-error.ts && grep -q "export type" packages/mapper/src/types/index.ts && grep -q "CanonicalSchema" packages/mapper/src/types/index.ts && grep -q "ValidatorAdapter" packages/mapper/src/types/index.ts && pnpm --filter @gluezero/mapper typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `validator-adapter.ts` esporta `ValidatorAdapter`, `ValidationIssue`, `ValidationResult` (discriminated union)
    - File `mapping-error.ts` esporta `MappingErrorCode` (literal union 5 codici) + `isMappingErrorCode` (type guard runtime)
    - File `types/index.ts` ha 4 blocchi `export type` (canonical-schema + input-output-map + transform + validator-adapter) + 1 export type per mapping-error + 1 export runtime per isMappingErrorCode
    - JSDoc 1-liner su ogni export del barrel (pattern F1)
    - `pnpm --filter @gluezero/mapper typecheck` esce 0
    - File `mapping-error.ts` esporta literal `'mapping.cycle.detected'`, `'mapping.transform.failed'`, `'mapping.field.missing'`, `'mapping.canonical.validation.failed'`, `'mapping.consumer.validation.failed'` (5 codici esattamente — D-58)
  </acceptance_criteria>
  <done>Surface tipi pubblici F2 completata. 6 file types/*.ts. Barrel re-export pronto per import dai plan 02-03..02-08. `unknown` placeholder F1 chiusi (saranno wired in plan 02-09 augment.ts).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type-only export → consumer | I tipi pubblici sono visibili a tutti i consumer di `@gluezero/mapper` |
| Branded types → cast esplicito | `CanonicalSchemaId`/`TransformName` proteggono solo se i consumer non bypassano il branding via `as any` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Type confusion: passare uno schema id come transform name | mitigate | Branded types `CanonicalSchemaId` e `TransformName` con `unique symbol` distinto. Cast esplicito audit-able via grep. Pattern F1 replicato (`broker-event.ts:54-61` EventId). |
| T-02-02-02 | Information disclosure | `FieldDescriptor.default: unknown` accetta qualsiasi valore | accept | `unknown` è intenzionale per supportare default object/array/primitive senza generic explosion. `exactOptionalPropertyTypes` policy enforce field opzionale assente vs `undefined` esplicito. |
| T-02-02-03 | Tampering | Mutation di `CanonicalSchema.fields` post-register | mitigate | Tutti i field marcati `readonly`. Runtime `deepFreeze(schema)` in `CanonicalRegistry.register` (plan 02-03 — D-04 pattern F1). |
| T-02-02-04 | DoS | Adapter validator throw invece di ritornare ValidationResult | mitigate | `ValidatorAdapter.validate` ha contract NO-throw documentato in JSDoc; i 3 adapter ufficiali (Valibot/Zod/Ajv) wrappano internamente try/catch. Implementazione concreta in plan 02-06. |
| T-02-02-05 | Repudiation | `MappingErrorCode` aggiunti senza version bump rompono consumer | mitigate | Literal union è additive (aggiungere codici è non-breaking; rimuoverli sì). Documentazione DOC-03 al plan 02-12 specifica policy. |
</threat_model>

<verification>
- 6 file `packages/mapper/src/types/*.ts` esistono (5 modulo + 1 barrel)
- Nessun `any` o `unknown` non documentato (`unknown` consentito solo dove intenzionale: `FieldDescriptor.default`, `MappingRule.default`, `TransformFn input/output`, `ValidatorAdapter.validate(schema, payload)` — tutti documentati)
- Nessun import runtime cross-package eccetto `import type { BrokerLogger } from '@gluezero/core'` in `transform.ts`
- `pnpm --filter @gluezero/mapper typecheck` esce 0 con `isolatedDeclarations: true`
- `pnpm --filter @gluezero/mapper test` esce 0 con `passWithNoTests` (nessun test runtime ancora — plan 02-03 in poi aggiunge test)
- Barrel `types/index.ts` ri-esporta tutti i tipi pubblici (verifica grep di tutti i nomi)
</verification>

<success_criteria>
- Phase 2 ROADMAP success criteria: nessuno completato direttamente (è scaffolding tipi); precondizione per criterion 3 (mapper supporta tutti i casi PRD §14.2) e criterion 4 (open issues VAL-08/VAL-09 chiusi)
- REQ-IDs Phase 2 indirizzati al type-level (verranno completati al runtime nei plan successivi):
  - **MAP-01** type-level (CanonicalSchema interface)
  - **MAP-02** type-level (signature `registerCanonicalSchema(schemaDefinition)` definita in CanonicalSchema shape)
  - **MAP-03** type-level (InputMap/OutputMap shape — wired al PluginDescriptor in plan 02-09 augment.ts)
  - **MAP-12** type-level (TransformFn/TransformDescriptor signature definita)
  - **VAL-03** type-level (ValidatorAdapter usato in step 6 — implementato plan 02-06/02-07)
  - **VAL-04** type-level (ValidatorAdapter usato in step 12 — implementato plan 02-06/02-07)
- Open issues PRD §39 — type-level scaffold per chiusura nei plan successivi:
  - VAL-08 (`FieldDescriptor.required` definito; runtime in plan 02-07 mapper-engine)
  - VAL-09 (`FieldFailureMode` + `FieldDescriptor.onFailure` definiti; runtime in plan 02-07 + plan 02-12 robustness)
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-model-mapper/02-02-SUMMARY.md` con:
- 6 file types/*.ts creati
- Branded types definiti (`CanonicalSchemaId`, `TransformName`) con pattern F1 EventId
- 5 codici `MappingErrorCode` definiti (D-58)
- Decisioni: nessuna deviazione dalle decisioni D-31..D-59; tipi tutti `readonly` (immutabilità by-default); barrel re-export type-only per verbatimModuleSyntax
- Pronto-per: Wave 3 paralleli (plan 02-03 canonical-registry || 02-04 alias-registry || 02-05 transform-pipeline || 02-06 valibot-adapter)
</output>
