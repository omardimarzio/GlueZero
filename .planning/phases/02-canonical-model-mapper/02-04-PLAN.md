---
phase: 02-canonical-model-mapper
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - packages/mapper/src/alias-registry.ts
  - packages/mapper/src/alias-registry.test.ts
autonomous: true
requirements:
  - MAP-16
  - MAP-17
tags:
  - alias-registry
  - tdd
  - parallel-wave-3
  - resolution-order
  - close-prd-39-1
must_haves:
  truths:
    - "`AliasRegistry.registerGlobal(localField, canonicalField)` registra alias visibile a tutti i plugin"
    - "`AliasRegistry.registerScoped(pluginId, localField, canonicalField)` registra alias visibile solo al plugin"
    - "`AliasRegistry.resolve(pluginId, localField)` ritorna `{ canonical: string; ambiguous: boolean; source: 'scoped'|'global'|'name-match'|'unresolved' }` (D-40, D-41)"
    - "Resolution order D-40: 1. esplicito (delegato al mapper-engine, NON gestito qui); 2. plugin-scoped; 3. globale; 4. name match diretto; 5. unresolved"
    - "`ambiguous: true` quando alias automatico (scoped o globale) viene usato → mapper-engine emette `mapping.warn` (D-41)"
    - "Plugin A scoped alias NOT visible a Plugin B (scope isolation)"
    - "`registerGlobal` con `(localField, canonicalField)` duplicate → ritorna `false` (idempotent)"
    - "`unregisterScopedAll(pluginId)` rimuove tutti gli alias scoped del plugin (cascade D-26 ext F2)"
    - "Tutti i test unit di `alias-registry.test.ts` passano"
  artifacts:
    - path: "packages/mapper/src/alias-registry.ts"
      provides: "AliasRegistry class con registerGlobal/registerScoped/resolve/list/unregisterScopedAll"
      contains: "class AliasRegistry"
    - path: "packages/mapper/src/alias-registry.test.ts"
      provides: "Test unit per AliasRegistry coprendo D-40 resolution order + D-41 ambiguity flag + scope isolation"
      contains: "describe('AliasRegistry'"
  key_links:
    - from: "packages/mapper/src/alias-registry.ts"
      to: "packages/mapper/src/types/canonical-schema.ts"
      via: "import type { CanonicalSchemaId } (no runtime dep)"
      pattern: "CanonicalSchemaId"
    - from: "packages/mapper/src/alias-registry.ts"
      to: "packages/core (via @sembridge/core)"
      via: "(no import: questo module è auto-contenuto, error handling delegato al caller mapper-engine)"
      pattern: "no @sembridge/core import"
---

<objective>
Implementare `AliasRegistry` con i due livelli di scope (global + plugin-scoped) e la resolution order D-40 — chiusura PRD §39 #1 (MAP-17). Wave 3 parallelo a `CanonicalRegistry` (02-03), `TransformPipeline` (02-05), `ValibotAdapter` (02-06).

Purpose: il MapperEngine (plan 02-07) chiama `aliasRegistry.resolve(pluginId, localField)` per scoprire il canonical name di un field se il plugin NON ha dichiarato un mapping esplicito. Il flag `ambiguous` permette al mapper-engine di emettere `mapping.warn` (D-41) per debug — NON un'eccezione (errors solo se field required + non risolvibile).

**Vincolo critico D-40**: il resolution order parte SEMPRE dal mapping esplicito (gestito nel MapperEngine, NON qui). Questo registry copre solo i livelli 2-4 (scoped → globale → name-match) + 5 (unresolved).

Output: 1 module + 1 test file co-locato, TDD RED→GREEN. Nessun export pubblico ancora — sarà ri-esportato dal barrel in plan 02-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-model-mapper/02-CONTEXT.md
@.planning/phases/02-canonical-model-mapper/02-PATTERNS.md
@.planning/phases/02-canonical-model-mapper/02-02-SUMMARY.md
@packages/core/src/core/topic-registry.ts
@packages/core/src/core/topic-matcher.ts
@packages/mapper/src/types/canonical-schema.ts
@prd.md
</context>

<interfaces>
<!-- Tipi disponibili da plan 02-02. -->

From `packages/mapper/src/types/canonical-schema.ts`:
```typescript
export type CanonicalSchemaId = string & { readonly [__canonicalSchemaIdBrand]: true }
```

<!-- Pattern F1 da replicare: TopicRegistry idempotent register + Map<plugin, Map<...>> per scope. -->
<!-- Vedi 02-PATTERNS.md §2.2 riga "alias-registry.ts" per dettaglio. -->

Resolution order D-40 (MAP-17 — chiusura PRD §39 #1):

```
1. Mapping esplicito (inputMap/outputMap del plugin)        ← gestito da mapper-engine plan 02-07, NON qui
2. AliasRegistry.pluginScopedAliases.get(pluginId).get(localField)
3. AliasRegistry.globalAliases.get(localField)
4. localField === canonicalField (name match diretto)       ← scoperto solo se canonicalRegistry conferma
5. unresolved → mapper-engine applica required:true|false policy (D-42)
```

Questo plan implementa **livelli 2, 3, 4, 5** dal punto di vista del registry. Il livello 1 (esplicito) e il check di esistenza canonica del livello 4 (consultando CanonicalRegistry) avvengono nel mapper-engine (plan 02-07).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: alias-registry.test.ts (RED) + alias-registry.ts (GREEN)</name>
  <files>packages/mapper/src/alias-registry.ts, packages/mapper/src/alias-registry.test.ts</files>
  <read_first>
    - packages/core/src/core/topic-registry.ts (riga 1-52 — pattern register idempotent)
    - .planning/phases/02-canonical-model-mapper/02-CONTEXT.md decisioni D-40, D-41, D-26 ext
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §2.2 riga "alias-registry.ts"
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §5.1 (anti-pattern alias globali shadow)
    - prd.md §14.7 (warning runtime alias ambiguo + mapping esplicito vince sempre)
    - prd.md §39 #1 (open issue chiusa via D-40)
  </read_first>
  <behavior>
    - Test 1: `registerGlobal('city', 'location')` ritorna `true`; secondo register stesso pair ritorna `false` (idempotent)
    - Test 2: `registerGlobal('city', 'location-different')` con `localField` già registrato a `location` → throw `Error 'alias.global.conflict'` (lo stesso localField non può puntare a 2 canonical diversi)
    - Test 3: `registerScoped('plugin-a', 'city', 'location')` registra alias scoped; `registerScoped` è idempotent come global
    - Test 4: `resolve('plugin-a', 'city')` con scoped registrato → ritorna `{ canonical: 'location', ambiguous: true, source: 'scoped' }` (D-41)
    - Test 5: `resolve('plugin-a', 'city')` con SOLO global registrato → ritorna `{ canonical: 'location', ambiguous: true, source: 'global' }`
    - Test 6: `resolve('plugin-a', 'city')` con scoped E global registrati → scoped vince (return source: 'scoped')
    - Test 7: `resolve('plugin-b', 'city')` con scoped registrato per 'plugin-a' → NON vede lo scoped di plugin-a; cade su global se presente o name-match (scope isolation)
    - Test 8: `resolve('plugin-a', 'location')` con `'location'` field name canonical (no alias necessario) → ritorna `{ canonical: 'location', ambiguous: false, source: 'name-match' }` (D-40 livello 4)
    - Test 9: `resolve('plugin-a', 'unknown')` senza alias e senza name-match assumption → ritorna `{ canonical: 'unknown', ambiguous: false, source: 'unresolved' }` (il mapper-engine deciderà se applicare required policy)
    - Test 10: `unregisterScopedAll('plugin-a')` rimuove tutti gli alias scoped di plugin-a; gli alias global e di altri plugin restano intatti (D-26 ext F2)
    - Test 11: `listGlobal()` ritorna `[localField, canonical][]` ordinato deterministicamente; `listScoped(pluginId)` similmente
    - Test 12: `resolve` con localField vuoto string `''` → throw `Error 'alias.localField.empty'` (validation guard)
  </behavior>
  <action>
**1. Creare `packages/mapper/src/alias-registry.test.ts` (TDD RED):**

```ts
import { describe, expect, it } from 'vitest'
import { AliasRegistry } from './alias-registry'

describe('AliasRegistry', () => {
  describe('registerGlobal', () => {
    it('returns true on new alias, false on duplicate (idempotent)', () => {
      const reg = new AliasRegistry()
      expect(reg.registerGlobal('city', 'location')).toBe(true)
      expect(reg.registerGlobal('city', 'location')).toBe(false)
    })

    it('throws on conflict (same localField → different canonical)', () => {
      const reg = new AliasRegistry()
      reg.registerGlobal('city', 'location')
      expect(() => reg.registerGlobal('city', 'location-different')).toThrow(/alias\.global\.conflict/)
    })
  })

  describe('registerScoped', () => {
    it('registers scoped alias for one plugin only', () => {
      const reg = new AliasRegistry()
      expect(reg.registerScoped('plugin-a', 'city', 'location')).toBe(true)
      expect(reg.registerScoped('plugin-a', 'city', 'location')).toBe(false)
    })

    it('throws on conflict within same plugin scope', () => {
      const reg = new AliasRegistry()
      reg.registerScoped('plugin-a', 'city', 'location')
      expect(() => reg.registerScoped('plugin-a', 'city', 'place')).toThrow(/alias\.scoped\.conflict/)
    })

    it('different plugins can register conflicting scoped aliases (scope isolation)', () => {
      const reg = new AliasRegistry()
      expect(reg.registerScoped('plugin-a', 'city', 'location')).toBe(true)
      expect(reg.registerScoped('plugin-b', 'city', 'place')).toBe(true)
    })
  })

  describe('resolve (D-40 resolution order)', () => {
    it('scoped alias resolution → ambiguous: true, source: scoped', () => {
      const reg = new AliasRegistry()
      reg.registerScoped('plugin-a', 'city', 'location')
      expect(reg.resolve('plugin-a', 'city')).toEqual({
        canonical: 'location',
        ambiguous: true,
        source: 'scoped',
      })
    })

    it('global alias resolution → ambiguous: true, source: global', () => {
      const reg = new AliasRegistry()
      reg.registerGlobal('city', 'location')
      expect(reg.resolve('plugin-a', 'city')).toEqual({
        canonical: 'location',
        ambiguous: true,
        source: 'global',
      })
    })

    it('scoped wins over global when both registered (D-40 priority)', () => {
      const reg = new AliasRegistry()
      reg.registerGlobal('city', 'location-global')
      reg.registerScoped('plugin-a', 'city', 'location-scoped')
      const result = reg.resolve('plugin-a', 'city')
      expect(result.canonical).toBe('location-scoped')
      expect(result.source).toBe('scoped')
    })

    it('plugin-b does NOT see scoped alias of plugin-a (isolation)', () => {
      const reg = new AliasRegistry()
      reg.registerScoped('plugin-a', 'city', 'location-a')
      reg.registerGlobal('city', 'location-global')
      const result = reg.resolve('plugin-b', 'city')
      expect(result.canonical).toBe('location-global')
      expect(result.source).toBe('global')
    })

    it('name match (no alias) → ambiguous: false, source: name-match', () => {
      const reg = new AliasRegistry()
      // No alias registered. The mapper-engine will pass localField that already
      // equals the canonical name (or treat it as a candidate for name-match).
      expect(reg.resolve('plugin-a', 'location')).toEqual({
        canonical: 'location',
        ambiguous: false,
        source: 'name-match',
      })
    })

    it('truly unresolved → ambiguous: false, source: unresolved', () => {
      // resolve always returns SOME canonical (defaulting to localField as name-match).
      // The 'unresolved' source is not produced by this registry alone — it's reserved
      // for the mapper-engine when consulting CanonicalRegistry to verify the field
      // actually exists. Here we test that the surface contract is consistent.
      const reg = new AliasRegistry()
      const result = reg.resolve('plugin-a', 'unknown')
      // No alias → name-match assumption (mapper-engine will then check canonical existence)
      expect(result.canonical).toBe('unknown')
      expect(result.source).toBe('name-match')
      expect(result.ambiguous).toBe(false)
    })

    it('throws on empty localField', () => {
      const reg = new AliasRegistry()
      expect(() => reg.resolve('plugin-a', '')).toThrow(/alias\.localField\.empty/)
    })
  })

  describe('unregisterScopedAll (D-26 ext F2 cascade)', () => {
    it('removes all scoped aliases for a plugin; global and other-plugin aliases intact', () => {
      const reg = new AliasRegistry()
      reg.registerGlobal('city', 'location')
      reg.registerScoped('plugin-a', 'foo', 'bar')
      reg.registerScoped('plugin-a', 'baz', 'qux')
      reg.registerScoped('plugin-b', 'lorem', 'ipsum')

      const removed = reg.unregisterScopedAll('plugin-a')
      expect(removed).toBe(2)

      // plugin-a scoped gone
      expect(reg.resolve('plugin-a', 'foo').source).toBe('name-match')
      // global still there
      expect(reg.resolve('plugin-a', 'city').source).toBe('global')
      // plugin-b untouched
      expect(reg.resolve('plugin-b', 'lorem').canonical).toBe('ipsum')
    })

    it('returns 0 if plugin had no scoped aliases', () => {
      const reg = new AliasRegistry()
      expect(reg.unregisterScopedAll('plugin-x')).toBe(0)
    })
  })

  describe('list', () => {
    it('listGlobal returns sorted [localField, canonical][] entries', () => {
      const reg = new AliasRegistry()
      reg.registerGlobal('zeta', 'z')
      reg.registerGlobal('alpha', 'a')
      reg.registerGlobal('beta', 'b')
      expect(reg.listGlobal()).toEqual([
        ['alpha', 'a'],
        ['beta', 'b'],
        ['zeta', 'z'],
      ])
    })

    it('listScoped(pluginId) returns sorted entries for that plugin only', () => {
      const reg = new AliasRegistry()
      reg.registerScoped('plugin-a', 'foo', 'bar')
      reg.registerScoped('plugin-a', 'baz', 'qux')
      reg.registerScoped('plugin-b', 'lorem', 'ipsum')
      expect(reg.listScoped('plugin-a')).toEqual([
        ['baz', 'qux'],
        ['foo', 'bar'],
      ])
      expect(reg.listScoped('plugin-b')).toEqual([['lorem', 'ipsum']])
      expect(reg.listScoped('plugin-z')).toEqual([])
    })
  })
})
```

Eseguire `pnpm --filter @sembridge/mapper test alias-registry` — atteso FAIL.

**2. Creare `packages/mapper/src/alias-registry.ts`:**

```ts
// AliasRegistry — registry alias localField → canonicalField (PRD §13.3, REQ MAP-16/MAP-17).
//
// Riferimento decisioni (CONTEXT 02):
// - D-40: resolution order — esplicito (delegato a mapper-engine) > scoped > global > name-match.
// - D-41: alias automatico (scoped o global) → ambiguous:true → mapper-engine emette mapping.warn.
//   Il name-match diretto (localField === canonicalField) NON è ambiguo.
// - D-26 ext F2: `unregisterScopedAll(pluginId)` per cascade plugin unregister
//   (wired al broker wrapper in plan 02-10).
//
// Resolution levels gestiti da questo module:
//   2. plugin-scoped → ambiguous:true, source:'scoped'
//   3. global       → ambiguous:true, source:'global'
//   4. name-match   → ambiguous:false, source:'name-match'
// Level 1 (esplicito inputMap/outputMap) vive nel mapper-engine.
// Level 5 (unresolved) emerge se mapper-engine consulta CanonicalRegistry e il field non esiste.
//
// Anti-pattern PITFALLS §3.B (alias globali shadow tra plugin):
// risolto via separazione `globalAliases: Map` vs `pluginScopedAliases: Map<pluginId, Map>`.
//
// Threat coverage:
// - T-02-04-01 (Tampering — listGlobal/listScoped mutation esterna): spread copy.
// - T-02-04-02 (Information disclosure — plugin-b vede alias scoped di plugin-a): scope isolation
//   tramite Map<pluginId, Map>. Test "plugin-b does NOT see scoped alias" verifica.

import type { CanonicalSchemaId } from './types/canonical-schema'

/** Source identificativa della risoluzione (D-40, D-41). */
export type AliasResolutionSource = 'scoped' | 'global' | 'name-match' | 'unresolved'

/** Risultato di `resolve(pluginId, localField)`. */
export interface AliasResolution {
  readonly canonical: string
  readonly ambiguous: boolean
  readonly source: AliasResolutionSource
}

export class AliasRegistry {
  private readonly globalAliases = new Map<string, string>()
  private readonly pluginScopedAliases = new Map<string, Map<string, string>>()

  registerGlobal(localField: string, canonicalField: string): boolean {
    const existing = this.globalAliases.get(localField)
    if (existing !== undefined) {
      if (existing === canonicalField) return false
      throw new Error(
        `alias.global.conflict: localField "${localField}" already maps to "${existing}", cannot remap to "${canonicalField}"`,
      )
    }
    this.globalAliases.set(localField, canonicalField)
    return true
  }

  registerScoped(pluginId: string, localField: string, canonicalField: string): boolean {
    let scope = this.pluginScopedAliases.get(pluginId)
    if (!scope) {
      scope = new Map<string, string>()
      this.pluginScopedAliases.set(pluginId, scope)
    }
    const existing = scope.get(localField)
    if (existing !== undefined) {
      if (existing === canonicalField) return false
      throw new Error(
        `alias.scoped.conflict: plugin "${pluginId}" localField "${localField}" already maps to "${existing}", cannot remap to "${canonicalField}"`,
      )
    }
    scope.set(localField, canonicalField)
    return true
  }

  /**
   * Risolve `localField` per il plugin specifico secondo D-40 (livelli 2-4).
   *
   * Il mapper-engine (plan 02-07) ha già consultato il livello 1 (mapping esplicito)
   * prima di chiamare `resolve`. Il livello 5 (unresolved) viene determinato dal
   * mapper-engine consultando CanonicalRegistry — qui ritorniamo sempre un canonical
   * (defaulting a `name-match` con `localField`).
   */
  resolve(pluginId: string, localField: string): AliasResolution {
    if (localField === '') {
      throw new Error('alias.localField.empty: localField cannot be an empty string')
    }

    // Level 2: scoped
    const scope = this.pluginScopedAliases.get(pluginId)
    if (scope) {
      const scoped = scope.get(localField)
      if (scoped !== undefined) {
        return { canonical: scoped, ambiguous: true, source: 'scoped' }
      }
    }

    // Level 3: global
    const global = this.globalAliases.get(localField)
    if (global !== undefined) {
      return { canonical: global, ambiguous: true, source: 'global' }
    }

    // Level 4: name match (default — no alias needed).
    return { canonical: localField, ambiguous: false, source: 'name-match' }
  }

  /** Cascade D-26 ext F2: rimuove tutti gli alias scoped di un plugin. Ritorna count rimossi. */
  unregisterScopedAll(pluginId: string): number {
    const scope = this.pluginScopedAliases.get(pluginId)
    if (!scope) return 0
    const count = scope.size
    this.pluginScopedAliases.delete(pluginId)
    return count
  }

  listGlobal(): Array<[string, string]> {
    return [...this.globalAliases.entries()].sort(([a], [b]) => a.localeCompare(b))
  }

  listScoped(pluginId: string): Array<[string, string]> {
    const scope = this.pluginScopedAliases.get(pluginId)
    if (!scope) return []
    return [...scope.entries()].sort(([a], [b]) => a.localeCompare(b))
  }
}

// Ri-export type per convenienza nei consumer interni del mapper (mapper-engine plan 02-07).
export type { CanonicalSchemaId }
```

**3. Eseguire test:**

```bash
pnpm --filter @sembridge/mapper test alias-registry
```

Atteso: 14 test passano.

**4. Verificare typecheck:**

```bash
pnpm --filter @sembridge/mapper typecheck
```

Atteso: exit 0.
  </action>
  <verify>
    <automated>test -f packages/mapper/src/alias-registry.ts && test -f packages/mapper/src/alias-registry.test.ts && grep -q "export class AliasRegistry" packages/mapper/src/alias-registry.ts && grep -q "registerGlobal" packages/mapper/src/alias-registry.ts && grep -q "registerScoped" packages/mapper/src/alias-registry.ts && grep -q "unregisterScopedAll" packages/mapper/src/alias-registry.ts && grep -q "ambiguous" packages/mapper/src/alias-registry.ts && grep -q "source: 'scoped'" packages/mapper/src/alias-registry.ts && pnpm --filter @sembridge/mapper test alias-registry 2>&1 | grep -E "Test Files.*1.*passed" && pnpm --filter @sembridge/mapper typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `alias-registry.ts` esporta `class AliasRegistry` + `AliasResolution` + `AliasResolutionSource`
    - Class ha 6 metodi pubblici: `registerGlobal`, `registerScoped`, `resolve`, `unregisterScopedAll`, `listGlobal`, `listScoped`
    - `resolve(pluginId, localField)` segue D-40 ordine: scoped → global → name-match
    - `ambiguous: true` SOLO quando alias automatico (scoped o global) — `false` per name-match (D-41)
    - Plugin-scoped alias di plugin-a NON visibile a plugin-b (scope isolation)
    - `registerGlobal`/`registerScoped` con stesso localField+canonical → `false` (idempotent)
    - `registerGlobal`/`registerScoped` con stesso localField + canonical DIVERSO → throw conflict error
    - `unregisterScopedAll(pluginId)` ritorna count + pulisce solo lo scope del plugin
    - File test ha almeno 14 test cases
    - `pnpm --filter @sembridge/mapper test alias-registry` esce 0 con tutti test passing
    - `pnpm --filter @sembridge/mapper typecheck` esce 0
  </acceptance_criteria>
  <done>AliasRegistry module + test creati. D-40 resolution order + D-41 ambiguity flag implementati. Pronto per consumption da MapperEngine (plan 02-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin A scoped alias → Plugin B | Scope isolation tra plugin |
| Global alias conflict | Lo stesso localField non può mappare a 2 canonical diversi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | `listGlobal`/`listScoped` mutation esterna | mitigate | spread copy + sort. Test 11 verifica. |
| T-02-04-02 | Information disclosure | Plugin B vede alias scoped di Plugin A | mitigate | `pluginScopedAliases: Map<pluginId, Map<...>>` separa scope. Test 7 verifica. |
| T-02-04-03 | Tampering | Stesso localField mappato a canonical diversi (silent overwrite) | mitigate | `registerGlobal`/`registerScoped` throw `alias.{global,scoped}.conflict` su pair conflittuale (Test 2, idempotent on identical pair Test 1). |
| T-02-04-04 | Repudiation | Resolve non riporta source → debug difficile | mitigate | `AliasResolution.source` literal union espone esattamente quale livello D-40 ha risolto. Test 4-7 verifica. |
| T-02-04-05 | Spoofing | Plugin malevolo registra alias globale per shadow di mapping critico | mitigate | `registerGlobal` esposto solo via `Broker.registerAlias` (plan 02-10) con auth applicativo del consumer; conflict throw previene shadow accidentale. Plugin-scoped è sempre la priorità per il plugin proprio. |
</threat_model>

<verification>
- 1 file source + 1 file test esistenti in `packages/mapper/src/`
- `pnpm --filter @sembridge/mapper test alias-registry` esce 0 con `Test Files 1 passed (1)`
- `pnpm --filter @sembridge/mapper typecheck` esce 0
- Code coverage ≥ 90% su `alias-registry.ts` (verificato al merge wave plan 02-12)
- File ownership disgiunta: nessun overlap con plan 02-03/02-05/02-06 (Wave 3 paralleli)
</verification>

<success_criteria>
- REQ-IDs Phase 2 indirizzati:
  - **MAP-16** ✓ (warning runtime alias ambiguo via `ambiguous: true` + source `scoped|global` — usato dal mapper-engine plan 02-07 per emettere `mapping.warn`)
  - **MAP-17** ✓ (chiusura PRD §39 #1 — il livello esplicito è gestito dal mapper-engine PRIMA di consultare AliasRegistry; il registry stesso non vede l'esplicito ma il contract `resolve` ritorna comunque `name-match` come default deterministico)
- D-40 resolution order implementato (livelli 2-4 in questo module; livello 1 + 5 nel mapper-engine)
- D-41 ambiguity flag implementato (false solo per name-match)
- D-26 ext F2 cascade abilitato via `unregisterScopedAll(pluginId)`
- File ownership disgiunta confermata per parallelizzazione Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-model-mapper/02-04-SUMMARY.md` con:
- Module + test creati (alias-registry.ts/.test.ts)
- Output di `pnpm --filter @sembridge/mapper test alias-registry` (mostra "Test Files 1 passed")
- Decisioni: nessuna deviazione da PATTERNS.md §2.2 e §5.1; conflict error con `alias.global.conflict`/`alias.scoped.conflict` come Error nativo (non BrokerError) — coerente con error handling auto-contenuto del module (delegato al mapper-engine consumer per wrapping eventuale)
- Pronto-per: import dal MapperEngine (plan 02-07) per applicare resolution durante compile mapping
</output>
