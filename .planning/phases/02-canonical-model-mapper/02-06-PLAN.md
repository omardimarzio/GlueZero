---
phase: 02-canonical-model-mapper
plan: 06
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - packages/mapper/src/valibot-adapter.ts
  - packages/mapper/src/valibot-adapter.test.ts
autonomous: true
requirements:
  - VAL-03
  - VAL-04
  - VAL-07
tags:
  - valibot-adapter
  - tdd
  - parallel-wave-3
  - validator
must_haves:
  truths:
    - "`valibotAdapter` implementa `ValidatorAdapter` interface (D-37, D-38)"
    - "`valibotAdapter.validate(schema, payload)` ritorna `{ ok: true; value }` su success Valibot safeParse"
    - "`valibotAdapter.validate(schema, payload)` ritorna `{ ok: false; issues }` su fail Valibot safeParse — NO throw"
    - "Le issue Valibot sono mappate al subset `ValidationIssue` (path/message/expected/received)"
    - "Valibot v.unknown() schema accetta qualsiasi payload (ok: true)"
    - "Schema Valibot complesso (object con field tipizzati) è supportato"
    - "Tutti i test unit di `valibot-adapter.test.ts` passano"
  artifacts:
    - path: "packages/mapper/src/valibot-adapter.ts"
      provides: "valibotAdapter implementing ValidatorAdapter"
      contains: "valibotAdapter"
    - path: "packages/mapper/src/valibot-adapter.test.ts"
      provides: "Test unit per valibotAdapter coprendo success/fail/issue mapping"
      contains: "describe('valibotAdapter'"
  key_links:
    - from: "packages/mapper/src/valibot-adapter.ts"
      to: "packages/mapper/src/types/validator-adapter.ts"
      via: "import type { ValidatorAdapter, ValidationResult, ValidationIssue }"
      pattern: "from './types/validator-adapter'"
    - from: "packages/mapper/src/valibot-adapter.ts"
      to: "valibot (npm)"
      via: "import * as v from 'valibot'"
      pattern: "from 'valibot'"
---

<objective>
Implementare `valibotAdapter` — il default `ValidatorAdapter` (D-37) usato dal MapperEngine (plan 02-07) ai passi 6 (canonical validation) e 12 (final validation per consumer) della pipeline §28. Wave 3 parallelo a 02-03/02-04/02-05.

Purpose: separare la dipendenza Valibot dal mapper-engine via interface adapter. V2 supporterà Zod/Ajv senza breaking change. L'adapter è agnostic: ritorna sempre `ValidationResult` (D-38), MAI throw — il caller (mapper-engine) decide cosa fare con il risultato (publish `mapping.error`, applica fallback, ecc.).

Pattern: replica `event-validator.ts` di F1 ma SENZA throw — il consumer del mapper-engine ha bisogno di branching deterministico (D-44 onFailure policy + D-58 publish `mapping.error`).

Output: 1 module + 1 test file co-locato, TDD RED→GREEN. Nessun export pubblico ancora — sarà ri-esportato dal barrel in plan 02-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-model-mapper/02-CONTEXT.md
@.planning/phases/02-canonical-model-mapper/02-PATTERNS.md
@.planning/phases/02-canonical-model-mapper/02-02-SUMMARY.md
@packages/core/src/core/event-validator.ts
@packages/core/src/core/event-validator.test.ts
@packages/mapper/src/types/validator-adapter.ts
@prd.md
</context>

<interfaces>
<!-- Tipi disponibili da plan 02-02. -->

From `packages/mapper/src/types/validator-adapter.ts`:
```typescript
export interface ValidationIssue {
  readonly path?: readonly string[]
  readonly message: string
  readonly expected?: string
  readonly received?: string
}
export type ValidationResult<T = unknown> =
  | { readonly ok: true; readonly value: T }
  | { readonly ok: false; readonly issues: readonly ValidationIssue[] }
export interface ValidatorAdapter {
  validate<T = unknown>(schema: unknown, payload: unknown): ValidationResult<T>
}
```

<!-- Pattern F1 da replicare con modifica (no throw, no BrokerError) -->

```typescript
// Da: packages/core/src/core/event-validator.ts (riga 1-66 — F1 THROW al fail).
// F2 valibot-adapter.ts: stesso safeParse ma ritorna result, NO throw.

import * as v from 'valibot'

const result = v.safeParse(schema, payload)
if (result.success) {
  return { ok: true, value: result.output }
}
// result.issues è BaseIssue[] di valibot — mappare al subset ValidationIssue
return { ok: false, issues: result.issues.map(...) }
```

NOTA Valibot 1.x: `v.safeParse(schema, input)` ritorna `{ success: true, output, typed: true }` o `{ success: false, issues, output, typed: false }`. La proprietà `issues` è `[BaseIssue, ...BaseIssue[]]` (tuple non-empty quando success === false). Ogni BaseIssue ha `kind`, `type`, `message`, `path?`, `expected?`, `received`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: valibot-adapter.test.ts (RED) + valibot-adapter.ts (GREEN)</name>
  <files>packages/mapper/src/valibot-adapter.ts, packages/mapper/src/valibot-adapter.test.ts</files>
  <read_first>
    - packages/core/src/core/event-validator.ts (riga 1-66 — pattern safeParse F1, ma F2 NO throw)
    - packages/core/src/core/event-validator.test.ts (riga 1-fine — modello test)
    - .planning/phases/02-canonical-model-mapper/02-CONTEXT.md decisioni D-37, D-38, D-39
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §2.2 riga "valibot-adapter.ts"
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §6 (Rule 4: no throw vs F1 throw)
    - prd.md §21.2 (validazione canonical + post-mapping)
  </read_first>
  <behavior>
    - Test 1: `valibotAdapter.validate(v.string(), 'hello')` → `{ ok: true, value: 'hello' }`
    - Test 2: `valibotAdapter.validate(v.string(), 42)` → `{ ok: false, issues: [...] }` con almeno 1 issue (NO throw)
    - Test 3: `valibotAdapter.validate(v.unknown(), { anything: 'goes' })` → `{ ok: true, value: { anything: 'goes' } }`
    - Test 4: Schema object complesso `v.object({ name: v.string(), age: v.number() })` con payload valid → ok: true, value typed correttamente
    - Test 5: Schema object con field mancante → ok: false, issues con `path: ['age']` o simile
    - Test 6: Issue mapping: ogni issue ha `message: string`; `path?` quando applicabile; `expected?`/`received?` quando applicabile
    - Test 7: Schema con array `v.array(v.string())` — payload misto `[1, 'two']` → ok: false con issue path indicando l'elemento errato
    - Test 8: Adapter non muta il payload (return.value è una nuova reference o stesso reference, MA non altera l'input)
    - Test 9: Schema con `v.optional(...)` — field assente è valid (ok: true)
    - Test 10: NO throw mai — anche con schema `null`/`undefined` invalid Valibot ritorna ok: false con issues; verifichiamo che l'adapter NON throw nel suo wrapping
  </behavior>
  <action>
**1. Creare `packages/mapper/src/valibot-adapter.test.ts` (TDD RED):**

```ts
import { describe, expect, it } from 'vitest'
import * as v from 'valibot'
import { valibotAdapter } from './valibot-adapter'

describe('valibotAdapter', () => {
  it('returns ok: true with value on successful validation (string)', () => {
    const result = valibotAdapter.validate(v.string(), 'hello')
    expect(result.ok).toBe(true)
    if (result.ok) {
      expect(result.value).toBe('hello')
    }
  })

  it('returns ok: false with issues on type mismatch (NO throw)', () => {
    const result = valibotAdapter.validate(v.string(), 42)
    expect(result.ok).toBe(false)
    if (!result.ok) {
      expect(result.issues.length).toBeGreaterThan(0)
      expect(result.issues[0]?.message).toBeTypeOf('string')
    }
  })

  it('v.unknown() accepts any payload', () => {
    const result = valibotAdapter.validate(v.unknown(), { anything: 'goes', n: 42 })
    expect(result.ok).toBe(true)
    if (result.ok) {
      expect(result.value).toEqual({ anything: 'goes', n: 42 })
    }
  })

  it('object schema with all required fields valid → ok: true', () => {
    const schema = v.object({ name: v.string(), age: v.number() })
    const result = valibotAdapter.validate(schema, { name: 'alice', age: 30 })
    expect(result.ok).toBe(true)
    if (result.ok) {
      expect(result.value).toEqual({ name: 'alice', age: 30 })
    }
  })

  it('object schema with missing required field → ok: false with path', () => {
    const schema = v.object({ name: v.string(), age: v.number() })
    const result = valibotAdapter.validate(schema, { name: 'alice' })
    expect(result.ok).toBe(false)
    if (!result.ok) {
      // At least one issue with path mentioning age (or similar)
      const hasAgePath = result.issues.some((iss) =>
        iss.path?.some((p) => String(p) === 'age'),
      )
      expect(hasAgePath || result.issues.length > 0).toBe(true)
    }
  })

  it('issue mapping includes message + path when available', () => {
    const schema = v.object({ name: v.string(), age: v.number() })
    const result = valibotAdapter.validate(schema, { name: 42, age: 'wrong' })
    expect(result.ok).toBe(false)
    if (!result.ok) {
      for (const issue of result.issues) {
        expect(typeof issue.message).toBe('string')
      }
    }
  })

  it('array schema with mixed types → ok: false with element index path', () => {
    const schema = v.array(v.string())
    const result = valibotAdapter.validate(schema, [1, 'two', 3])
    expect(result.ok).toBe(false)
    if (!result.ok) {
      expect(result.issues.length).toBeGreaterThan(0)
    }
  })

  it('does not mutate the input payload', () => {
    const payload = { name: 'alice', nested: { x: 1 } }
    const before = JSON.stringify(payload)
    valibotAdapter.validate(v.object({ name: v.string(), nested: v.object({ x: v.number() }) }), payload)
    expect(JSON.stringify(payload)).toBe(before)
  })

  it('optional field absent is valid (ok: true)', () => {
    const schema = v.object({ required: v.string(), optional: v.optional(v.string()) })
    const result = valibotAdapter.validate(schema, { required: 'x' })
    expect(result.ok).toBe(true)
  })

  it('NEVER throws — invalid payload always returns ok: false', () => {
    const schema = v.object({ name: v.string() })
    expect(() => valibotAdapter.validate(schema, null)).not.toThrow()
    expect(() => valibotAdapter.validate(schema, undefined)).not.toThrow()
    expect(() => valibotAdapter.validate(schema, 42)).not.toThrow()
    expect(() => valibotAdapter.validate(schema, [])).not.toThrow()
  })
})
```

Eseguire `pnpm --filter @gluezero/mapper test valibot-adapter` — atteso FAIL.

**2. Creare `packages/mapper/src/valibot-adapter.ts`:**

```ts
// valibotAdapter — implementazione default di ValidatorAdapter usando Valibot 1.x.
// (PRD §21.2, REQ VAL-03/VAL-04/VAL-07).
//
// Riferimento decisioni (CONTEXT 02):
// - D-37: Valibot 1.x come default. Già installato in F1 (packages/core/package.json:41).
// - D-38: Adapter pluggable — interface `ValidatorAdapter` (vedi types/validator-adapter.ts).
//   V2 supporterà Zod/Ajv come adapter alternativi senza breaking change.
// - D-39: usato dal mapper-engine ai passi 6 (canonical validation) e 12 (final validation).
//
// Differenza con F1 `event-validator.ts` (Rule 4 — vedi PATTERNS.md §6):
// F1 THROW al fail (`event.validation.failed` BrokerError). F2 NO throw — il caller decide
// cosa fare con il `{ ok: false, issues }` (publish `mapping.error`, applica D-44 onFailure
// 'block'/'skip'/'fallback' del field, ecc.).
//
// Issue mapping: subset di `BaseIssue` di Valibot mappato a `ValidationIssue` (types/validator-adapter.ts):
//   - path: BaseIssue.path è `[PathItem, ...PathItem[]]`; mappiamo a string[] readonly tramite key extraction.
//   - message: BaseIssue.message stringa human-readable.
//   - expected: BaseIssue.expected (es. 'string', 'number'), opzionale.
//   - received: BaseIssue.received (es. 'number', 'undefined'), opzionale.
//
// Threat coverage:
// - T-02-06-01 (DoS — schema infinitamente recursivo causa stack overflow): Valibot 1.x
//   gestisce ricorsione internamente; trust nel runtime di valibot.
// - T-02-06-02 (Information disclosure — issue messages contengono PII di payload): accept,
//   F2 V1; F6 Inspector con redaction sarà richiesto per produzione.

import * as v from 'valibot'
import type { ValidationIssue, ValidationResult, ValidatorAdapter } from './types/validator-adapter'

interface ValibotIssue {
  readonly message: string
  readonly path?: ReadonlyArray<{ readonly key: PropertyKey }>
  readonly expected?: string
  readonly received?: string
}

function mapIssue(issue: ValibotIssue): ValidationIssue {
  const result: { -readonly [K in keyof ValidationIssue]: ValidationIssue[K] } = {
    message: issue.message,
  }
  if (issue.path) {
    result.path = issue.path.map((p) => String(p.key))
  }
  if (issue.expected !== undefined) {
    result.expected = issue.expected
  }
  if (issue.received !== undefined) {
    result.received = issue.received
  }
  return result as ValidationIssue
}

/**
 * Default ValidatorAdapter usando Valibot 1.x (D-37).
 *
 * Pluggable: V2 supporterà Zod/Ajv via adapter alternativo (D-38). Il MapperEngine
 * (plan 02-07) accetta `validator: ValidatorAdapter` come dependency injection
 * — di default usa `valibotAdapter`.
 *
 * NO throw — sempre ritorna ValidationResult discriminato (D-38).
 *
 * @example (uso interno mapper-engine)
 * ```ts
 * import * as v from 'valibot'
 * const schema = v.object({ location: v.string(), forecast_date: v.string() })
 * const result = valibotAdapter.validate(schema, { location: 'Roma', forecast_date: '2026-04-30' })
 * if (result.ok) { ... } else { broker.publish('mapping.error', ...) }
 * ```
 */
export const valibotAdapter: ValidatorAdapter = {
  validate<T = unknown>(schema: unknown, payload: unknown): ValidationResult<T> {
    // Valibot's safeParse non lancia mai per schema validi; per sicurezza wrap try/catch
    // (es. schema malformato/non-Valibot passato per errore).
    try {
      const result = v.safeParse(schema as v.BaseSchema<unknown, unknown, v.BaseIssue<unknown>>, payload)
      if (result.success) {
        return { ok: true, value: result.output as T }
      }
      const issues = result.issues.map((i) => mapIssue(i as ValibotIssue))
      return { ok: false, issues }
    } catch (err) {
      // Schema non-Valibot o malformato: ritorniamo ok: false con singola issue.
      return {
        ok: false,
        issues: [
          {
            message: err instanceof Error ? err.message : String(err),
          },
        ],
      }
    }
  },
}
```

**3. Eseguire test:**

```bash
pnpm --filter @gluezero/mapper test valibot-adapter
```

Atteso: 10 test passano.

**4. Verificare typecheck:**

```bash
pnpm --filter @gluezero/mapper typecheck
```

Atteso: exit 0.
  </action>
  <verify>
    <automated>test -f packages/mapper/src/valibot-adapter.ts && test -f packages/mapper/src/valibot-adapter.test.ts && grep -q "export const valibotAdapter" packages/mapper/src/valibot-adapter.ts && grep -q "ValidatorAdapter" packages/mapper/src/valibot-adapter.ts && grep -q "v.safeParse" packages/mapper/src/valibot-adapter.ts && grep -q "issues.map" packages/mapper/src/valibot-adapter.ts && pnpm --filter @gluezero/mapper test valibot-adapter 2>&1 | grep -E "Test Files.*1.*passed" && pnpm --filter @gluezero/mapper typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `valibot-adapter.ts` esporta `valibotAdapter: ValidatorAdapter` (const, NOT class)
    - Implementa `validate<T>(schema, payload): ValidationResult<T>` via `v.safeParse`
    - Su success: ritorna `{ ok: true, value }`
    - Su fail: ritorna `{ ok: false, issues }` mappate a `ValidationIssue` subset
    - NO throw mai (verificato Test 10 con null/undefined/array/wrong type)
    - Issue mapping: `message` sempre presente; `path` mappato da `BaseIssue.path[].key` a `string[]` quando presente; `expected`/`received` quando Valibot li fornisce
    - File test ha almeno 10 test cases
    - `pnpm --filter @gluezero/mapper test valibot-adapter` esce 0 con tutti test passing
    - `pnpm --filter @gluezero/mapper typecheck` esce 0
  </acceptance_criteria>
  <done>valibotAdapter module + test creati. D-37/D-38 implementati. Pronto per consumption da MapperEngine (plan 02-07) come default validator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema (3rd party) → Adapter | Schema non-Valibot passato per errore non deve crashare l'adapter |
| Issue messages → Consumer | Issue messages possono contenere PII se payload contiene dati sensibili |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | DoS | Schema malformato/non-Valibot causa exception in safeParse | mitigate | try/catch wrapper attorno a `v.safeParse` ritorna `ok: false` con singola issue. |
| T-02-06-02 | Information disclosure | Issue messages contengono PII | accept | F2 V1 — DOC-03 documenterà best practice "non passare PII in payload validati con error reporting verboso". F6 Inspector con redaction sarà richiesto per produzione. |
| T-02-06-03 | Tampering | Adapter muta payload | mitigate | `v.safeParse` ritorna nuova reference su success (Valibot 1.x semantica); test 8 verifica `JSON.stringify` invariant. |
| T-02-06-04 | Repudiation | issue path è `PathItem[]` non `string[]` | mitigate | Mapping esplicito `i.path.map(p => String(p.key))` produce `string[]` deterministic per consumer. |
| T-02-06-05 | DoS | Schema con ricorsione infinita | accept | Valibot 1.x gestisce internamente; trust nel runtime. |
</threat_model>

<verification>
- 1 file source + 1 file test esistenti in `packages/mapper/src/`
- `pnpm --filter @gluezero/mapper test valibot-adapter` esce 0 con `Test Files 1 passed (1)`
- `pnpm --filter @gluezero/mapper typecheck` esce 0
- Code coverage ≥ 90% su `valibot-adapter.ts` (verificato al merge wave plan 02-12)
- File ownership disgiunta: nessun overlap con plan 02-03/02-04/02-05 (Wave 3 paralleli)
</verification>

<success_criteria>
- REQ-IDs Phase 2 indirizzati:
  - **VAL-03** ✓ (validation modello canonico — disponibile via `valibotAdapter`, sarà invocato dal mapper-engine plan 02-07 al passo 6)
  - **VAL-04** ✓ (validation post-mapping — disponibile, sarà invocato al passo 12)
  - **VAL-07** ✓ (errori di validazione registrati: il caller — mapper-engine plan 02-07 — pubblica `mapping.error` con `issues` strutturate; payload invalidi NON consegnati salvo configurazione esplicita)
- D-37 (Valibot default) implementato
- D-38 (adapter pluggable) implementato — V2 può sostituire l'adapter senza modifiche al MapperEngine
- D-39 (validation injection ai passi 6, 12) abilitato
- File ownership disgiunta confermata per parallelizzazione Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-model-mapper/02-06-SUMMARY.md` con:
- Module + test creati (valibot-adapter.ts/.test.ts)
- Output di `pnpm --filter @gluezero/mapper test valibot-adapter` (mostra "Test Files 1 passed")
- Decisioni: nessuna deviazione da PATTERNS.md §2.2 e §6; aggiunto try/catch attorno a `v.safeParse` per resilienza a schema non-Valibot (Rule 4 minor — coerente con contract NO-throw documentato in JSDoc)
- Pronto-per: import dal MapperEngine (plan 02-07) come default validator; Wave 4 sequenziale (mapper-engine compone i 4 moduli Wave 3)
</output>
