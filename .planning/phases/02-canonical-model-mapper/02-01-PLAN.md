---
phase: 02-canonical-model-mapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mapper/package.json
  - packages/mapper/tsup.config.ts
  - packages/mapper/tsconfig.json
  - packages/mapper/vitest.config.ts
  - packages/mapper/README.md
  - packages/mapper/src/index.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - DOC-03
tags:
  - bootstrap
  - mapper-package
  - foundation
  - phase-2-gate
must_haves:
  truths:
    - "Il package `@sembridge/mapper` ha `package.json`/`tsup.config.ts`/`tsconfig.json`/`vitest.config.ts` allineati a `@sembridge/core` (replica D-15)"
    - "`pnpm --filter @sembridge/mapper build` esce 0 e produce `dist/index.js` + `dist/index.d.ts` (anche se l'index esporta solo placeholder)"
    - "`pnpm --filter @sembridge/mapper test --passWithNoTests` esce 0"
    - "`pnpm --filter @sembridge/mapper typecheck` esce 0 (no errori TS)"
    - "Workspace `pnpm install` linka `@sembridge/core: workspace:*` come dipendenza di `@sembridge/mapper`"
    - "`@vitest/coverage-v8` installato come devDep root (chiude open item F1, abilita D-55)"
    - "Bundle size-limit per `@sembridge/mapper` aggiunto al root `package.json` con limite 5 KB gzip"
    - "README mapper di skeleton creato in italiano (preparazione DOC-03 finale al plan 02-12)"
  artifacts:
    - path: "packages/mapper/package.json"
      provides: "Package config @sembridge/mapper con dep workspace:* su @sembridge/core, valibot, sideEffects array per augment.ts"
      contains: "@sembridge/mapper"
    - path: "packages/mapper/tsup.config.ts"
      provides: "Build config tsup ESM-only con external @sembridge/core (peer-like)"
      contains: "@sembridge/core"
    - path: "packages/mapper/tsconfig.json"
      provides: "TS config che estende tsconfig.base.json con strict + isolatedDeclarations"
      contains: "tsconfig.base.json"
    - path: "packages/mapper/vitest.config.ts"
      provides: "Vitest config con name @sembridge/mapper + jsdom env + coverage v8 thresholds"
      contains: "@sembridge/mapper"
    - path: "packages/mapper/src/index.ts"
      provides: "Barrel skeleton (verrà popolato nei plan successivi)"
      contains: "@packageDocumentation"
  key_links:
    - from: "packages/mapper/package.json"
      to: "packages/core"
      via: "dependencies.@sembridge/core: workspace:*"
      pattern: "workspace:\\*"
    - from: "packages/mapper/tsup.config.ts"
      to: "packages/core"
      via: "external: [/^node:/, '@sembridge/core']"
      pattern: "@sembridge/core"
    - from: "package.json (root)"
      to: "packages/mapper/dist/index.js"
      via: "size-limit entry per @sembridge/mapper (gzip)"
      pattern: "packages/mapper/dist/index.js"
---

<objective>
Bootstrap del package `@sembridge/mapper` (placeholder F1 plan 01-01 → package buildable F2). Replica precisa della struttura di `@sembridge/core` (vedi PATTERNS.md §3) con i 4 file di config richiesti, dipendenza `workspace:*` su `@sembridge/core`, side-effects array per il futuro `augment.ts` (declaration merging — D-49/D-56/D-57), e installazione di `@vitest/coverage-v8` come devDep root per chiudere l'open item F1 e abilitare D-55.

Purpose: tutti i plan 02-02 in poi assumono che il package sia buildable, testabile, e linkato al workspace pnpm. Senza questo, il TDD RED→GREEN dei moduli successivi non può partire (vitest non riconosce i nuovi `*.test.ts`).

Output: 4 file config + README skeleton + index.ts skeleton + 1 devDep root + 1 size-limit entry root. `pnpm install` aggiorna `pnpm-lock.yaml`. Build e test passano (anche se non c'è ancora codice runtime).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-model-mapper/02-CONTEXT.md
@.planning/phases/02-canonical-model-mapper/02-PATTERNS.md
@packages/core/package.json
@packages/core/tsup.config.ts
@packages/core/tsconfig.json
@packages/core/vitest.config.ts
@packages/mapper/package.json
@package.json
@pnpm-workspace.yaml
@tsconfig.base.json
</context>

<interfaces>
<!-- Surface package @sembridge/core già esistente (importabile da @sembridge/mapper). -->
<!-- Vedi 02-PATTERNS.md §1.6 per ordine import (external → cross-package types → cross-package runtime → local). -->

Da `@sembridge/core` (runtime exports):
```typescript
export { createBroker } from '@sembridge/core'
export { Broker } from '@sembridge/core'
export { createBrokerError, isBrokerError } from '@sembridge/core'
export { createConsoleLogger, silentLogger } from '@sembridge/core'
```

Da `@sembridge/core` (type exports):
```typescript
export type {
  BrokerEvent, EventSource, DeliveryMode, Priority, EventId,
  BrokerConfig, DeepReadonly,
  BrokerError, ErrorCategory,
  BrokerLogger, LogLevel,
  PluginContext, PluginDescriptor, PluginState,
  Subscription, SubscribeOptions,
  EventTap, PipelineSnapshot, PipelineStep,
} from '@sembridge/core'
```

NOTA: il tipo `CreateBrokerErrorParams` NON è ri-esportato dal barrel `@sembridge/core/src/index.ts` (deliberatamente — vedi `index.ts:33-86`). Se serve, importa via path interno `@sembridge/core/dist/types/error` (sconsigliato) oppure ricostruisci l'oggetto inline al `createBrokerError` call (pattern già usato in F1 — vedi `plugin-registry.ts:215-223`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replica config @sembridge/core in @sembridge/mapper (package.json + tsup + tsconfig + vitest)</name>
  <files>packages/mapper/package.json, packages/mapper/tsup.config.ts, packages/mapper/tsconfig.json, packages/mapper/vitest.config.ts</files>
  <read_first>
    - packages/core/package.json (riga 1-57 — modello da replicare)
    - packages/core/tsup.config.ts (riga 1-19)
    - packages/core/tsconfig.json (riga 1-11)
    - packages/core/vitest.config.ts (riga 1-25)
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §3 "Stack & build (replica da @sembridge/core)"
  </read_first>
  <action>
**1. Sostituire `packages/mapper/package.json` (placeholder corrente: 18 righe minimal) con replica `@sembridge/core` adattata:**

```json
{
  "name": "@sembridge/mapper",
  "version": "0.0.0",
  "description": "Canonical model + bidirectional mapper for SemBridge (Phase 2)",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": ["./dist/augment.js"],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sembridge/core": "workspace:*",
    "valibot": "1.3.1"
  },
  "devDependencies": {
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5",
    "jsdom": "29.1.0"
  },
  "size-limit": [
    {
      "name": "@sembridge/mapper (gzip)",
      "path": "dist/index.js",
      "limit": "5 KB",
      "gzip": true
    }
  ]
}
```

NOTE:
- `sideEffects: ["./dist/augment.js"]` (ARRAY non boolean) — preserva il side-effect di `augment.ts` (declaration merging registration F2 — D-49/D-56/D-57) dal tree-shaker, mantenendo tree-shaking del resto. Documentato come Rule 4 in PATTERNS.md §6.
- `dependencies.@sembridge/core: workspace:*` — pnpm workspace protocol; `pnpm install` linka simbolicamente.
- `valibot: 1.3.1` — versione locked F1 (D-37 default validator).
- `size-limit` budget 5 KB gzip (vs 8 KB core); +2 KB headroom per mapper engine + registries (vedi PATTERNS.md §3.1).

**2. Creare `packages/mapper/tsup.config.ts` (replica core con 2 diff documentate):**

```ts
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  sourcemap: true,
  clean: true,
  treeshake: true,
  splitting: false,
  minify: false,
  target: 'es2022',
  platform: 'browser',
  external: [/^node:/, '@sembridge/core'],
  banner: {
    js: '/* @sembridge/mapper — MIT — https://github.com/<TBD>/sembridge */',
  },
})
```

DIFF F1: `external` include `'@sembridge/core'` (peer-like — non bundla `@sembridge/core` dentro mapper); banner string aggiornato con name `@sembridge/mapper`.

**3. Creare `packages/mapper/tsconfig.json` (replica esatta core):**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "ignoreDeprecations": "6.0"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "**/*.test.ts", "**/*.spec.ts"]
}
```

Eredita `tsconfig.base.json` con `strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes + isolatedDeclarations + verbatimModuleSyntax`.

**4. Creare `packages/mapper/vitest.config.ts` (replica core con name diverso):**

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: '@sembridge/mapper',
    environment: 'jsdom',
    globals: false,
    include: ['src/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/index.ts'],
      thresholds: {
        statements: 90,
        branches: 85,
        functions: 90,
        lines: 90,
      },
    },
    typecheck: {
      enabled: false,
    },
  },
})
```

**5. Verificare typecheck (parziale — index ancora vuoto):**

```bash
pnpm --filter @sembridge/mapper typecheck
```

Atteso: exit 0 (nessun file `.ts` in src oltre il futuro index, `tsc --noEmit` passa).
  </action>
  <verify>
    <automated>test -f packages/mapper/package.json && test -f packages/mapper/tsup.config.ts && test -f packages/mapper/tsconfig.json && test -f packages/mapper/vitest.config.ts && grep -q "@sembridge/core" packages/mapper/package.json && grep -q "workspace:\\*" packages/mapper/package.json && grep -q "valibot" packages/mapper/package.json && grep -q "augment.js" packages/mapper/package.json && grep -q "@sembridge/core" packages/mapper/tsup.config.ts && grep -q "tsconfig.base.json" packages/mapper/tsconfig.json && grep -q "@sembridge/mapper" packages/mapper/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/mapper/package.json` esiste con name `@sembridge/mapper`, dep `@sembridge/core: workspace:*` + `valibot: 1.3.1`, `sideEffects: ["./dist/augment.js"]` (array — Rule 4)
    - File contiene scripts identici a core (`build`, `test`, `test:watch`, `test:coverage`, `typecheck`, `clean`)
    - File contiene `size-limit` entry con budget 5 KB gzip
    - File `packages/mapper/tsup.config.ts` esiste con `external: [/^node:/, '@sembridge/core']` (peer-like)
    - File `packages/mapper/tsconfig.json` estende `../../tsconfig.base.json` con outDir/rootDir/lib coerenti a core
    - File `packages/mapper/vitest.config.ts` ha `name: '@sembridge/mapper'` + jsdom env + coverage thresholds 90/85/90/90
    - Comando `pnpm --filter @sembridge/mapper typecheck` esce 0
  </acceptance_criteria>
  <done>4 file config creati. Package è strutturalmente buildable (manca solo il codice runtime del barrel index, prossimo task).</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton barrel + README + install workspace deps + size-limit root + coverage v8</name>
  <files>packages/mapper/src/index.ts, packages/mapper/README.md, package.json</files>
  <read_first>
    - packages/core/src/index.ts (riga 1-87 — modello @packageDocumentation block)
    - packages/core/README.md (struttura README espanso)
    - .planning/phases/02-canonical-model-mapper/02-PATTERNS.md §7 "Coverage & quality gate"
    - package.json root (size-limit array attuale, devDependencies)
  </read_first>
  <action>
**1. Creare `packages/mapper/src/index.ts` (skeleton barrel — verrà popolato dai plan 02-02 → 02-12):**

```ts
/**
 * @sembridge/mapper — Canonical model + bidirectional mapper per SemBridge.
 *
 * Phase 2 di SemBridge V1. Estende `@sembridge/core` (Phase 1) con:
 * - Canonical Vocabulary Registry (campi tipizzati, alias riconosciuti, requires)
 * - Mapper bidirezionale (locale → canonico → locale per ogni consumer)
 * - Transform Pipeline (rename, nested, default, transform, derive, partial)
 * - Validation adapter (Valibot 1.x default; Zod/Ajv deferred a V2)
 * - Mapping Inspector (estensione EventTap con 5 nuovi step pipeline §28)
 *
 * Vincolo architetturale (D-49 — vedi 02-CONTEXT.md): NON modifica `bus.ts` di F1.
 * Estende la pipeline via composition wrapper + TS declaration merging del
 * `PipelineStep` union (file `augment.ts`, importato per side-effect).
 *
 * Surface pubblica: progressivamente popolata dai plan 02-02 → 02-12.
 *
 * @packageDocumentation
 */

// Side-effect import — abilita TS declaration merging per PipelineStep,
// PluginDescriptor.inputMap/outputMap, BrokerConfig sezioni canonicalModel/aliasRegistry/transforms.
// Vedi packages/mapper/src/augment.ts (creato nel plan 02-09).
// import './augment'  — verrà attivato in 02-09 quando augment.ts esisterà

// Runtime exports — popolati dai plan successivi:
// export { CanonicalRegistry } from './canonical-registry'      // plan 02-03
// export { AliasRegistry } from './alias-registry'              // plan 02-04
// export { TransformPipeline } from './transform-pipeline'      // plan 02-05
// export { valibotAdapter } from './valibot-adapter'            // plan 02-06
// export { MapperEngine } from './mapper-engine'                // plan 02-07

// Type exports — popolati dai plan successivi:
// export type * from './types'                                  // plan 02-02
```

NOTA: il barrel è inizialmente VUOTO eseguibile (solo header @packageDocumentation + commenti placeholder). Questo permette a `tsup build` e `vitest run --passWithNoTests` di passare. I plan 02-02..02-12 incrementeranno questo file.

**2. Creare `packages/mapper/README.md` (skeleton in italiano — preparazione DOC-03):**

```md
# @sembridge/mapper

Canonical model + mapper bidirezionale per SemBridge (Phase 2).

> Pacchetto della famiglia [`@sembridge`](https://github.com/<TBD>/sembridge) — V1, ESM-only, target browser evergreen (ES2022).

## Stato

Phase 2 in sviluppo. La superficie pubblica del package viene popolata progressivamente dai plan 02-02 → 02-12. Vedi `.planning/phases/02-canonical-model-mapper/` per il piano dettagliato.

## Cosa contiene (a regime — fine F2)

- **Canonical Vocabulary Registry** — campi canonici tipizzati, alias riconosciuti, schema versioning (`requires`).
- **Mapper bidirezionale** — pipeline pre-compilata locale → canonico (input) e canonico → consumer (output) per ogni plugin sottoscritto.
- **Transform Pipeline** — rename, nested, default, format transform, unit normalization, derive (`$derive`), partial mapping; `registerTransform(name, fn)` per trasformazioni custom.
- **Mapping Inspector** — estensione di `EventTap` ai 5 nuovi step della pipeline §28 (step 4, 5, 6, 11, 12).
- **Validation adapter** — Valibot 1.x default; adapter pluggable (Zod/Ajv deferred a V2).

## Vincolo architetturale (D-49)

Il package `@sembridge/mapper` NON modifica `bus.ts` di `@sembridge/core` (Phase 1). La pipeline è estesa via composition wrapper + TS declaration merging del `PipelineStep` union. Vedi `02-CONTEXT.md` per il dettaglio.

## Documentazione

La documentazione utente completa (DOC-03 — REQ ROADMAP F2) è prodotta nel plan finale 02-12 con scenario meteo PRD §29 end-to-end senza HTTP.

## Licenza

MIT.
```

**3. Aggiungere `@vitest/coverage-v8` come devDep root (chiude open item F1, abilita D-55):**

Modificare `package.json` (root) sezione `devDependencies` aggiungendo:

```json
"@vitest/coverage-v8": "4.1.5"
```

(stessa versione di vitest, vincolo plugin Vitest).

**4. Aggiungere entry `size-limit` root per `@sembridge/mapper`:**

Modificare `package.json` (root) sezione `size-limit` aggiungendo:

```json
{
  "name": "@sembridge/mapper (gzip)",
  "path": "packages/mapper/dist/index.js",
  "limit": "5 KB",
  "gzip": true
}
```

L'array finale ha 2 entry: core (8 KB) + mapper (5 KB) — budget mapper allineato a STACK.md §"size-limit" + ROADMAP Phase 6 stack.

**5. Eseguire `pnpm install` per linkare `@sembridge/core: workspace:*` + installare la nuova devDep coverage:**

```bash
pnpm install
```

Atteso:
- `pnpm-lock.yaml` aggiornato
- `node_modules/@sembridge/mapper` simlink al package locale
- `node_modules/@vitest/coverage-v8` installato

**6. Verificare la pipeline build/test/typecheck:**

```bash
pnpm --filter @sembridge/mapper typecheck
pnpm --filter @sembridge/mapper test
pnpm --filter @sembridge/mapper build
```

Atteso:
- typecheck exit 0
- test exit 0 (no test files yet, `--passWithNoTests`)
- build exit 0 — `dist/index.js` (~250 byte ESM con solo header) + `dist/index.d.ts` (~50 byte)
  </action>
  <verify>
    <automated>test -f packages/mapper/src/index.ts && test -f packages/mapper/README.md && grep -q "@packageDocumentation" packages/mapper/src/index.ts && grep -q "Canonical model" packages/mapper/README.md && grep -q "@vitest/coverage-v8" package.json && grep -q "@sembridge/mapper (gzip)" package.json && grep -q "packages/mapper/dist/index.js" package.json && pnpm --filter @sembridge/mapper typecheck 2>&1 | tail -5 && pnpm --filter @sembridge/mapper test 2>&1 | tail -5 && pnpm --filter @sembridge/mapper build 2>&1 | tail -10 && test -f packages/mapper/dist/index.js && test -f packages/mapper/dist/index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/mapper/src/index.ts` esiste con header `@packageDocumentation` + commenti placeholder per export futuri
    - File `packages/mapper/README.md` esiste in italiano con sezione "Stato", "Cosa contiene", "Vincolo architetturale (D-49)"
    - `package.json` (root) ha `@vitest/coverage-v8: 4.1.5` in `devDependencies`
    - `package.json` (root) ha entry `size-limit` per `@sembridge/mapper (gzip)` con path `packages/mapper/dist/index.js` e limit 5 KB
    - `pnpm install` ha aggiornato `pnpm-lock.yaml` e linkato `@sembridge/mapper` al workspace
    - `pnpm --filter @sembridge/mapper typecheck` esce 0
    - `pnpm --filter @sembridge/mapper test` esce 0 (passWithNoTests)
    - `pnpm --filter @sembridge/mapper build` produce `dist/index.js` + `dist/index.d.ts` (anche se minimal)
  </acceptance_criteria>
  <done>Package `@sembridge/mapper` buildable + testable + typed-checked. Coverage v8 disponibile. Pronto per popolamento da plan 02-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm workspace → @sembridge/mapper | Workspace symlink potrebbe esporre `@sembridge/core` non buildato (build out-of-order) |
| Tree-shaker → augment.ts | Tree-shaking troppo aggressivo eliminerebbe il side-effect di declaration merging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `sideEffects: false` (boolean) eliminerebbe `augment.ts` da tree-shake | mitigate | `sideEffects: ["./dist/augment.js"]` (array) preserva il file specifico, mantenendo tree-shaking del resto. Verificato in PATTERNS.md §6 e §3.1 (Rule 4 documentato). |
| T-02-01-02 | Repudiation | Build out-of-order (mapper prima di core) → import error | mitigate | `pnpm -r --filter='./packages/*' --if-present run build` rispetta dipendenze workspace; `tsup external: '@sembridge/core'` non bundla, quindi `dist/index.js` di mapper risolve a runtime via consumer's `node_modules`. |
| T-02-01-03 | DoS | Coverage v8 plugin add a runtime overhead | accept | Coverage attivo solo in `test:coverage` script; `test` standard non lo invoca. |
| T-02-01-04 | Information disclosure | `pnpm-lock.yaml` aggiornato espone snapshot dependency tree | accept | Comportamento standard pnpm, file committato come da workflow F1; nessun secret. |
| T-02-01-05 | Tampering | Versione `valibot: 1.3.1` divergente da F1 → runtime mismatch | mitigate | Versione locked identica a F1 (verificata in `packages/core/package.json:41`). `pnpm install` deduplicherà l'installazione. |
</threat_model>

<verification>
- 4 file config + index.ts + README.md esistenti in `packages/mapper/`
- `pnpm install` esce 0 e aggiorna `pnpm-lock.yaml`
- `pnpm --filter @sembridge/mapper build` esce 0 e produce `dist/index.js` + `dist/index.d.ts`
- `pnpm --filter @sembridge/mapper test` esce 0 con `passWithNoTests` (nessun test file ancora)
- `pnpm --filter @sembridge/mapper typecheck` esce 0
- `pnpm typecheck` workspace esce 0 (no regression core)
- `pnpm --filter @sembridge/core test` esce 0 con i 248 test F1 invariati
- `@vitest/coverage-v8: 4.1.5` presente in `package.json` root
- `size-limit` array root contiene 2 entry (core + mapper)
</verification>

<success_criteria>
- Phase 2 ROADMAP success criteria: nessuno completato direttamente da questo plan (è bootstrap)
- REQ-IDs Phase 2 indirizzati: PKG-01 (ESM build mapper), PKG-02 (TypeScript), PKG-03 (target evergreen ES2022), DOC-03 (README skeleton — completamento al plan 02-12)
- Open item F1 chiuso: `@vitest/coverage-v8` installato (D-55 abilitato per misurazione coverage F2 finale)
- `@sembridge/mapper` linkato al workspace pnpm; tutti i 7 sub-package F1 + il populated mapper coesistono
- Build artifact mapper esiste (`dist/index.js`/`dist/index.d.ts`), pre-requisito per future invocazioni `ci:size`/`ci:publint`/`ci:attw` (estese al mapper nel plan 02-12)
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-model-mapper/02-01-SUMMARY.md` con:
- 4 file config + 2 file workspace creati/modificati
- Output di `pnpm --filter @sembridge/mapper build` (mostra dist/index.js + dist/index.d.ts size)
- Conferma `pnpm install` linkato `@sembridge/core: workspace:*`
- Conferma `@vitest/coverage-v8` installato
- Decisioni: sideEffects array per augment.ts (Rule 4 - documentato in PATTERNS.md §6); external @sembridge/core (peer-like) in tsup
- Pronto-per: plan 02-02 (Public types F2)
</output>
