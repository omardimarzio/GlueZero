---
phase: 05-worker-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/worker/package.json
  - packages/worker/tsconfig.json
  - packages/worker/tsup.config.ts
  - packages/worker/vitest.config.ts
  - packages/worker/vitest.browser.config.ts
  - packages/worker/biome.json
  - packages/worker/src/index.ts
  - packages/worker/src/augment.ts
  - packages/worker/src/augment.test.ts
  - packages/worker/src/types/worker-descriptor.ts
  - packages/worker/src/types/worker-config.ts
  - packages/worker/src/types/route-worker-definition.ts
  - packages/worker/src/types/progress-payload.ts
  - packages/worker/src/types/task-state.ts
  - packages/worker/src/types/internal-topics.ts
  - packages/worker/src/types/index.ts
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - WK-01
  - WK-02
  - WK-03
  - WK-04
  - WK-05
  - WK-06
  - WK-07
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
must_haves:
  truths:
    - "Il pacchetto @gluezero/worker compila con tsup ESM-only e produce dist/index.{js,d.ts} importabili"
    - "L'augment.ts estende BrokerConfig.workers e PluginDescriptor.workers via declaration merging senza modificare runtime di @gluezero/{core,mapper,routing,gateway}"
    - "I type pubblici (WorkerDescriptor, WorkerConfig, RouteWorkerDefinition, ProgressPayload, TaskState, INTERNAL_TOPICS_WORKER) sono esportati dal barrel src/index.ts e tree-shake-safe"
    - "Test smoke decl merging passa (8 test) e __augmentWorkerLoaded export è presente nel dist"
    - "Comlink 4.4.2 è installato come runtime dependency con version pin esatto"
  artifacts:
    - path: "packages/worker/package.json"
      provides: "Manifest con name @gluezero/worker, type module, sideEffects glob augment, deps comlink/@gluezero/{core,mapper,routing}/nanoid/valibot"
    - path: "packages/worker/tsup.config.ts"
      provides: "Build config ESM-only target ES2022 con dts integrato e clean: true"
    - path: "packages/worker/vitest.config.ts"
      provides: "Tier-1 jsdom env + coverage v8 thresholds 90/80/90/90 + exclude __browser__"
    - path: "packages/worker/vitest.browser.config.ts"
      provides: "Tier-3 Playwright Chromium config per __browser__/**"
    - path: "packages/worker/src/augment.ts"
      provides: "Declaration merging BrokerConfig.workers + PluginDescriptor.workers + F5PipelineStep + __augmentWorkerLoaded const"
    - path: "packages/worker/src/types/worker-descriptor.ts"
      provides: "WorkerDescriptor interface (id, factory, tasks, mode, size, allowUnboundedPool, workerType, cancelGraceMs)"
    - path: "packages/worker/src/types/route-worker-definition.ts"
      provides: "RouteWorkerDefinition con type='worker' e subset RoutePolicies (timeout/concurrency/backpressure/dedupe — D-143)"
    - path: "packages/worker/src/types/progress-payload.ts"
      provides: "ProgressPayload canonical schema { value, message?, partialResult? } — D-136"
    - path: "packages/worker/src/types/task-state.ts"
      provides: "TaskState union 'pending'|'done'|'timeout'|'cancelled'|'error' — D-133"
    - path: "packages/worker/src/types/internal-topics.ts"
      provides: "INTERNAL_TOPICS_WORKER frozen const { CANCEL: '__cancel__', PROGRESS: '__progress__' } + isInternalWorkerTopic helper STRICT match"
    - path: "packages/worker/src/index.ts"
      provides: "Barrel pubblico con export augment + types + (placeholder runtime exports per W2-W4)"
  key_links:
    - from: "packages/worker/src/index.ts"
      to: "packages/worker/src/augment.ts"
      via: "side-effect re-export __augmentWorkerLoaded"
      pattern: "__augmentWorkerLoaded"
    - from: "packages/worker/src/augment.ts"
      to: "@gluezero/core BrokerConfig + PluginDescriptor"
      via: "declare module '@gluezero/core'"
      pattern: "declare module '@gluezero/core'"
    - from: "packages/worker/package.json"
      to: "comlink + @gluezero/core/mapper/routing"
      via: "dependencies"
      pattern: "\"comlink\""
---

<objective>
Bootstrap del pacchetto `@gluezero/worker` come scaffold completo: package.json, build/test config (tsup + Vitest 3-tier), TypeScript config, biome inheritance, augment.ts con declaration merging additive (D-126 PluginDescriptor.workers + D-122 BrokerConfig.workers + F5PipelineStep), tutti i type pubblici (WorkerDescriptor, WorkerConfig, RouteWorkerDefinition, ProgressPayload, TaskState, INTERNAL_TOPICS_WORKER), e barrel index.ts. Wave 1 sequential gate per W2/W3/W4. Vincolo D-83 strict: zero modifiche runtime a `packages/{core,mapper,routing}/src/` e `packages/gateway/src/{http,sse-ws}/` — l'augment.ts F5 estende solo i type via declaration merging, NON modifica runtime.

Purpose: stabilire l'infrastruttura minima compilabile che permetta a W2 (assert-serializable, transferable-extractor, task-tracker) e W3 (worker-bridge, worker-pool) di importare i type pubblici e procedere in parallelo TDD. Coperto WK-01 (Worker Registry creazione/riuso pool — type) + WK-02 (tipo route worker — RouteWorkerDefinition) + WK-03 (Task correlation — correlationId in ProgressPayload) + WK-04..WK-07 (placeholder type) + PKG-01..PKG-04 (build ESM, dts, target evergreen, monorepo).

Output: `packages/worker/` popolato con scaffolding production-ready (analog F4 04-01 04-CONTEXT D-101+D-102), `pnpm -F @gluezero/worker build` produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}`, tsc --noEmit clean su tutto il monorepo (5 package), 8 test smoke decl merging passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-worker-runtime/05-CONTEXT.md
@.planning/phases/05-worker-runtime/05-RESEARCH.md
@.planning/phases/05-worker-runtime/05-PATTERNS.md
@CLAUDE.md
@prd.md
@packages/gateway/package.json
@packages/gateway/tsup.config.ts
@packages/gateway/tsconfig.json
@packages/gateway/vitest.config.ts
@packages/gateway/vitest.browser.config.ts
@packages/gateway/src/sse-ws/augment.ts
@packages/gateway/src/sse-ws/augment.test.ts
@packages/gateway/src/sse-ws/index.ts
@packages/gateway/src/sse-ws/types/realtime-channel-def.ts
@packages/gateway/src/sse-ws/types/realtime-config.ts
@packages/gateway/src/sse-ws/types/frame-envelope.ts
@packages/gateway/src/sse-ws/types/index.ts

<interfaces>
<!-- Type pubblici da @gluezero/core e @gluezero/routing che augment.ts estende -->
<!-- Estratti dal codebase F1+F3 — l'executor li usa direttamente, NO scavenger hunt -->

From packages/core/src/types/plugin.ts (PluginDescriptor F1):
```typescript
export interface PluginDescriptor {
  readonly id: string;
  readonly version?: string;
  readonly subscriptions?: readonly SubscriptionDescriptor[];
  readonly meta?: Readonly<Record<string, unknown>>;
  // F2/F3/F4 already extended via declare module — F5 adds .workers
}
```

From packages/core/src/types/config.ts (BrokerConfig F1):
```typescript
export interface BrokerConfig {
  readonly logger?: BrokerLogger;
  readonly logLevel?: BrokerLogLevel;
  readonly defaultDedupeWindow?: number;
  // F2/F3/F4 already extended via declare module — F5 adds .workers
}
```

From packages/routing/src/types/route-policies.ts (RoutePolicies F3):
```typescript
export interface RoutePolicies {
  readonly timeout?: number;
  readonly concurrency?: 'serial' | 'parallel' | 'latest-only';
  readonly backpressure?: BackpressureStrategy;
  readonly dedupe?: { key?: (event: BrokerEvent) => string };
  readonly retry?: RetryConfig;       // NOT used by worker route (D-143)
  readonly auth?: AuthConfig;         // NOT used by worker route (D-143)
  readonly circuitBreaker?: CBConfig; // NOT used by worker route (D-143)
}
```

From packages/gateway/src/sse-ws/augment.ts (pattern target da replicare):
```typescript
declare module '@gluezero/core' {
  interface BrokerConfig {
    realtime?: RealtimeConfig
  }
  interface PluginDescriptor {
    readonly realtimeChannels?: readonly RealtimeChannelDef[]
  }
}
export type F4PipelineStep =
  | 'event.realtime.received'
  | 'event.realtime.frame-parsed'
  | 'event.realtime.reconnecting'
export const __augmentSseWsLoaded: true = true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json + build/test config + tsconfig + biome (Wave 1 scaffold)</name>
  <files>packages/worker/package.json, packages/worker/tsconfig.json, packages/worker/tsup.config.ts, packages/worker/vitest.config.ts, packages/worker/vitest.browser.config.ts, packages/worker/biome.json, pnpm-workspace.yaml</files>
  <read_first>
    - packages/gateway/package.json (analog manifest verbatim — copy struttura sostituendo nome e exports)
    - packages/gateway/tsup.config.ts (analog build verbatim)
    - packages/gateway/tsconfig.json (analog typecheck verbatim)
    - packages/gateway/vitest.config.ts (analog Tier-1 verbatim)
    - packages/gateway/vitest.browser.config.ts (analog Tier-3 Playwright verbatim)
    - packages/gateway/biome.json (analog se esistente — altrimenti workspace root inherit)
    - pnpm-workspace.yaml (verifica che packages/worker sia già incluso)
    - .planning/phases/05-worker-runtime/05-PATTERNS.md sezioni "package.json" e "Wave 1 — Bootstrap" (LOC 99-141)
    - .planning/phases/05-worker-runtime/05-RESEARCH.md §2.3 "Cosa va INSTALLATO" (deps lockate) e §9.3 (vitest.browser.config.ts esempio)
    - .planning/phases/05-worker-runtime/05-CONTEXT.md D-122/D-125/D-147/D-150
    - packages/worker/package.json (stato corrente — placeholder F1 da popolare in F5)
  </read_first>
  <action>
**1.1 — `packages/worker/package.json` (REPLACE intero file):**
Copia struttura da `packages/gateway/package.json` (analog) e adatta a F5 single-purpose (no `./http`/`./sse-ws` subpath):
```json
{
  "name": "@gluezero/worker",
  "version": "0.0.0",
  "description": "Worker Runtime per GlueZero (Phase 5 — Comlink-based RPC + bounded pool + state machine atomico Pitfall 2C + WK-07 closure serializzazione documentata)",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": [
    "./dist/augment.js",
    "./src/augment.ts",
    "**/augment.js",
    "**/augment.ts"
  ],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:browser": "vitest run --config vitest.browser.config.ts --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@gluezero/core": "workspace:*",
    "@gluezero/mapper": "workspace:*",
    "@gluezero/routing": "workspace:*",
    "@gluezero/gateway": "workspace:*",
    "comlink": "4.4.2",
    "nanoid": "5.1.9",
    "valibot": "1.3.1"
  },
  "devDependencies": {
    "@vitest/browser": "4.1.5",
    "@vitest/browser-playwright": "4.1.5",
    "@vitest/coverage-v8": "4.1.5",
    "playwright": "1.59.1",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
```

Nota: `@gluezero/gateway` come dep runtime per riusare BackpressureStrategy F3 da `@gluezero/gateway/http` (D-130 carryover). Versioni esatte da `pnpm view comlink version` (4.4.2) — RESEARCH §14.

**1.2 — `packages/worker/tsconfig.json` (CREATE):**
Copia verbatim da `packages/gateway/tsconfig.json`. Verifica che extends il root e che `compilerOptions.composite` sia configurato (workspace project references). Output `./dist`, target ES2022, module ESNext, moduleResolution Bundler, strict, isolatedDeclarations true.

**1.3 — `packages/worker/tsup.config.ts` (CREATE):**
Copia da `packages/gateway/tsup.config.ts` adattando a F5 single-entry:
```ts
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: {
    index: 'src/index.ts',
    augment: 'src/augment.ts',
  },
  format: ['esm'],
  dts: true,
  clean: true,
  sourcemap: true,
  target: 'es2022',
  outDir: 'dist',
  splitting: false,
})
```

**1.4 — `packages/worker/vitest.config.ts` (CREATE):**
Copia verbatim da `packages/gateway/vitest.config.ts` con coverage threshold 90/80/90/90 v8:
```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.ts', 'src/__integration__/**/*.test.ts'],
    exclude: ['src/__browser__/**', 'node_modules', 'dist'],
    coverage: {
      provider: 'v8',
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/__browser__/**', 'src/test-utils/**', 'src/types/**'],
      thresholds: { statements: 90, branches: 80, functions: 90, lines: 90 },
    },
  },
})
```

**1.5 — `packages/worker/vitest.browser.config.ts` (CREATE):**
Riuso pattern F4 (RESEARCH §9.3):
```ts
import { defineConfig } from 'vitest/config'
import playwright from '@vitest/browser-playwright'

export default defineConfig({
  test: {
    browser: {
      enabled: true,
      provider: playwright(),
      instances: [{ browser: 'chromium' }],
      headless: true,
    },
    include: ['src/__browser__/**/*.test.ts'],
  },
})
```

**1.6 — `packages/worker/biome.json` (CREATE)** se non c'è inheritance root:
```json
{ "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json", "extends": ["../../biome.json"] }
```

**1.7 — `pnpm-workspace.yaml` (VERIFICA — patch solo se manca `packages/worker`):**
Run `grep -q '"packages/worker"' pnpm-workspace.yaml` o equivalente; se assente, aggiungi `- "packages/worker"`. (Probabilmente già presente da F1 bootstrap — verifica e basta.)

**Verification post-Task 1:**
- `pnpm install` from monorepo root completa senza errori
- `pnpm -F @gluezero/worker typecheck` exit 0 (anche se src/ è vuoto — il pacchetto compila con i type files che aggiungiamo in Task 2-3)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm -F @gluezero/worker exec test -f package.json -a -f tsup.config.ts -a -f tsconfig.json -a -f vitest.config.ts -a -f vitest.browser.config.ts &amp;&amp; grep -q '"comlink": "4.4.2"' packages/worker/package.json &amp;&amp; grep -q '"@gluezero/gateway": "workspace:\*"' packages/worker/package.json &amp;&amp; grep -q '"sideEffects"' packages/worker/package.json</automated>
  </verify>
  <done>
    - `packages/worker/package.json` contiene `name: @gluezero/worker`, `type: module`, deps `comlink: 4.4.2` + `@gluezero/{core,mapper,routing,gateway}: workspace:*` + `nanoid` + `valibot`, sideEffects glob `**/augment.{ts,js}`, scripts build/test/typecheck/test:browser/test:coverage/lint.
    - `tsup.config.ts` produce 2 entry: `index` + `augment`, ESM-only, dts true, target es2022.
    - `vitest.config.ts` ha jsdom env, include `src/**/*.test.ts` + `src/__integration__/**`, exclude `src/__browser__/**`, coverage v8 thresholds 90/80/90/90.
    - `vitest.browser.config.ts` ha browser provider Playwright Chromium, include `src/__browser__/**`.
    - `pnpm install` completa, lockfile aggiornato.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type files (worker-descriptor, worker-config, route-worker-definition, progress-payload, task-state, internal-topics, types/index)</name>
  <files>packages/worker/src/types/worker-descriptor.ts, packages/worker/src/types/worker-config.ts, packages/worker/src/types/route-worker-definition.ts, packages/worker/src/types/progress-payload.ts, packages/worker/src/types/task-state.ts, packages/worker/src/types/internal-topics.ts, packages/worker/src/types/index.ts</files>
  <behavior>
    - Test 1 (smoke type-only): `import type { WorkerDescriptor } from '@gluezero/worker'` compila con `factory: () => new Worker(new URL('./x.ts', import.meta.url))`, `tasks: ['parseCsv'] as const`, `mode: 'pool'`
    - Test 2: `import { INTERNAL_TOPICS_WORKER, isInternalWorkerTopic } from '@gluezero/worker'` — `INTERNAL_TOPICS_WORKER.CANCEL === '__cancel__'`, `isInternalWorkerTopic('__cancel__') === true`, `isInternalWorkerTopic('weather.__cancel__') === false` (STRICT match no prefix — D-111 carryover)
    - Test 3: `RouteWorkerDefinition` ammette `policies?: { timeout, concurrency, backpressure, dedupe }` ma RIFIUTA a livello TS `policies.retry`/`policies.auth`/`policies.circuitBreaker` (D-143 — verifica via `Pick<RoutePolicies, 'timeout' | 'concurrency' | 'backpressure' | 'dedupe'>`)
  </behavior>
  <read_first>
    - packages/gateway/src/sse-ws/types/realtime-channel-def.ts (analog WorkerDescriptor — replica pattern `id`/factory + readonly + JSDoc decisionali)
    - packages/gateway/src/sse-ws/types/realtime-config.ts (analog WorkerConfig)
    - packages/gateway/src/sse-ws/types/frame-envelope.ts (analog ProgressPayload + INTERNAL_TOPICS pattern STRICT match)
    - packages/gateway/src/sse-ws/types/index.ts (analog barrel — verbatim pattern)
    - packages/routing/src/types/route-definition.ts (analog RouteHttpDefinition per RouteWorkerDefinition struttura)
    - packages/routing/src/types/route-policies.ts (interface RoutePolicies F3 — sorgente per Pick subset D-143)
    - .planning/phases/05-worker-runtime/05-PATTERNS.md sezioni "worker-descriptor" e "route-worker-definition" (LOC 243-316)
    - .planning/phases/05-worker-runtime/05-RESEARCH.md §7.1 "RouteDefinition union extension" (LOC 884-942) e §6.4 "Transferable JSONPath"
    - .planning/phases/05-worker-runtime/05-CONTEXT.md D-123/D-124/D-125/D-127/D-128/D-130/D-131/D-133/D-136/D-137/D-141/D-143/D-144/D-145/D-146/D-147
  </read_first>
  <action>
**2.1 — `packages/worker/src/types/worker-descriptor.ts` (CREATE):**
Define D-123 + D-124 + D-127 + D-128 + D-131 + D-147 (analog `RealtimeChannelDef`):
```ts
/**
 * F5 Worker Descriptor — shape entry per WorkerRegistry (D-123, D-124, D-127, D-128, D-131, D-147).
 * @see .planning/phases/05-worker-runtime/05-CONTEXT.md (decisioni D-121..D-154)
 */
export type WorkerMode = 'dedicated' | 'pool'
export type WorkerType = 'module' | 'classic'

export interface WorkerDescriptor {
  /** Chiave registry univoca (es. 'report-worker'). Usata da RouteWorkerDefinition.worker. */
  readonly id: string
  /**
   * D-123 — Factory lazy: invocata al PRIMO dispatch (D-129). Pattern bundler-friendly:
   * `factory: () => new Worker(new URL('./x.worker.ts', import.meta.url), { type: 'module' })`.
   * @see D-148
   */
  readonly factory: () => Worker
  /**
   * D-124 — Tasks dichiarate esplicite per fail-fast 'worker.task.unknown' al register.
   * @see D-124
   */
  readonly tasks: readonly string[]
  /** D-127 — Default 'pool' con size = min(navigator.hardwareConcurrency, 4). */
  readonly mode?: WorkerMode
  /** D-127 — Pool size override. Cap hard 8 (D-128) salvo allowUnboundedPool. */
  readonly size?: number
  /** D-128 — Opt-in per pool size > 8 con console.warn dev mode. */
  readonly allowUnboundedPool?: boolean
  /** D-147 — Default 'module'. Classic opt-in per legacy bundler senza module-worker support. */
  readonly workerType?: WorkerType
  /**
   * D-131 — Cancellation pool cooperative: grace timeout prima di terminate fallback.
   * Default 2000ms. Solo per `mode: 'pool'` — `mode: 'dedicated'` chiama terminate immediato.
   */
  readonly cancelGraceMs?: number
}
```

**2.2 — `packages/worker/src/types/worker-config.ts` (CREATE):**
Define D-122 + D-139 (broker config sezione):
```ts
/**
 * F5 Worker Config — sezione `BrokerConfig.workers` aggiunta via declaration merging in augment.ts.
 * @see D-122, D-139
 */
export interface WorkerConfig {
  /**
   * D-139 — assertSerializable mode default `'dev'` (auto NODE_ENV !== 'production').
   * `'always'` runtime cost in prod (debug only); `'off'` disabilita.
   */
  readonly assertSerializable?: 'always' | 'dev' | 'off'
  /** Override default progressThrottleMs globale (D-137 default 100). */
  readonly defaultProgressThrottleMs?: number
  /** Override default timeout globale (D-145 default 30000). */
  readonly defaultTimeoutMs?: number
}
```

**2.3 — `packages/worker/src/types/route-worker-definition.ts` (CREATE):**
Define D-143 subset + D-141 transferable + D-137 progressThrottleMs + D-146 publishes override (analog RouteHttpDefinition):
```ts
import type { RoutePolicies } from '@gluezero/routing'

/**
 * F5 RouteWorkerDefinition — discriminator type='worker' per route worker (D-143, D-146, D-141, D-137).
 *
 * NOTA R4 (RESEARCH §17): TS non supporta declaration merging di type alias (RouteDefinition).
 * Il consumer può dichiarare: `type AllRoutes = RouteDefinition | RouteWorkerDefinition` localmente.
 * @see D-152, D-143
 */
export interface RouteWorkerDefinition {
  readonly type: 'worker'
  readonly id: string
  /** Topic intercettato (es. 'report.generation.requested'). */
  readonly topic: string
  /** WorkerId da registry (deve essere registrato via registerWorker o PluginDescriptor.workers). */
  readonly worker: string
  /** Task name — DEVE essere in `WorkerDescriptor.tasks` (D-124 fail-fast al register). */
  readonly task: string
  /**
   * D-146 — Topic publishing override. Default auto-derive da `<topic>` con regola suffix:
   *   `<entity>.<action>.requested` → success: `<entity>.<action>.completed`, error: `.failed`, progress: `.progress`
   */
  readonly publishes?: {
    readonly success?: string
    readonly progress?: string
    readonly error?: string
  }
  /**
   * D-141 — JSONPath-like array per transferable extraction. Wildcard `[*]` supportato.
   * Esempio: `['payload.audioBuffer', 'payload.images[*].buffer']`.
   * @see Pitfall 7.E DOC-05
   */
  readonly transferable?: readonly string[]
  /** D-137 — Default 100ms. Throttle latest-only window per onProgress events. */
  readonly progressThrottleMs?: number
  readonly priority?: number
  /**
   * D-143 — Subset rilevante delle RoutePolicies F3:
   * - timeout (WK-06 obbligatorio, D-145 default 30s)
   * - concurrency (D-144 default 'latest-only')
   * - backpressure (D-130 carryover F3)
   * - dedupe (D-143)
   *
   * NIENTE: retry, auth, circuitBreaker (deferred V1.x — D-143).
   */
  readonly policies?: Pick<RoutePolicies, 'timeout' | 'concurrency' | 'backpressure' | 'dedupe'>
}
```

**2.4 — `packages/worker/src/types/progress-payload.ts` (CREATE):**
Define D-136 schema canonical (analog FrameEnvelope):
```ts
/**
 * F5 Progress Payload canonical schema (D-136). Validato via Valibot a runtime.
 * Registrato come canonical type nel CanonicalRegistry F2 al register del worker route.
 */
export interface ProgressPayload {
  /** Progress fraction 0..1. */
  readonly value: number
  /** Optional human-readable label (es. 'Parsing CSV row 5000/10000'). */
  readonly message?: string
  /** Optional partial result preview (es. partial parsed records). */
  readonly partialResult?: unknown
}
```

**2.5 — `packages/worker/src/types/task-state.ts` (CREATE):**
Define D-133 state machine atomico union:
```ts
/**
 * F5 TaskState — state machine atomico per Pitfall 2C closure (D-133).
 * Transizioni esclusive: una volta `state ≠ 'pending'`, ogni successivo message dal worker viene
 * scartato silenziosamente (counter `lateResponses` incrementato — Claude's Discretion).
 */
export type TaskState = 'pending' | 'done' | 'timeout' | 'cancelled' | 'error'

/**
 * Outcome del task post-state-transition. Letto da WorkerHandler per costruire OutcomeCollector
 * (D-152 + D-153).
 */
export interface WorkerTaskOutcome {
  readonly taskId: string
  readonly correlationId: string
  readonly state: TaskState
  readonly result?: unknown
  readonly errorCode?: string
  readonly errorMessage?: string
  readonly elapsedMs: number
}
```

**2.6 — `packages/worker/src/types/internal-topics.ts` (CREATE):**
D-131 + D-137 reserved internal topics, S5 STRICT match no prefix (analog INTERNAL_TOPICS F4):
```ts
/**
 * F5 Reserved internal topics (D-131 cancel + D-137 progress). Filtrati STRICT (no prefix
 * `__`) per non collidere con topic legittimi che potrebbero iniziare con `__` (es. `weather.__cancel__`
 * NON è un internal topic — solo `__cancel__` esatto). Pattern S5 carryover F4 (D-111 PING/PONG).
 */
export const INTERNAL_TOPICS_WORKER: Readonly<{
  readonly CANCEL: '__cancel__'
  readonly PROGRESS: '__progress__'
}> = Object.freeze({ CANCEL: '__cancel__', PROGRESS: '__progress__' } as const)

/** Match STRICT — solo le 2 stringhe esatte (no startsWith '__'). */
export function isInternalWorkerTopic(topic: string): boolean {
  return topic === INTERNAL_TOPICS_WORKER.CANCEL || topic === INTERNAL_TOPICS_WORKER.PROGRESS
}
```

**2.7 — `packages/worker/src/types/index.ts` (CREATE — barrel):**
```ts
export type { WorkerDescriptor, WorkerMode, WorkerType } from './worker-descriptor'
export type { WorkerConfig } from './worker-config'
export type { RouteWorkerDefinition } from './route-worker-definition'
export type { ProgressPayload } from './progress-payload'
export type { TaskState, WorkerTaskOutcome } from './task-state'
export { INTERNAL_TOPICS_WORKER, isInternalWorkerTopic } from './internal-topics'
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" &amp;&amp; pnpm -F @gluezero/worker tsc --noEmit &amp;&amp; grep -q "export type { WorkerDescriptor" packages/worker/src/types/index.ts &amp;&amp; grep -q "INTERNAL_TOPICS_WORKER" packages/worker/src/types/internal-topics.ts &amp;&amp; grep -q "Pick&lt;RoutePolicies, 'timeout' | 'concurrency' | 'backpressure' | 'dedupe'&gt;" packages/worker/src/types/route-worker-definition.ts &amp;&amp; grep -q "type WorkerMode = 'dedicated' | 'pool'" packages/worker/src/types/worker-descriptor.ts</automated>
  </verify>
  <done>
    - 7 file `packages/worker/src/types/*.ts` creati con shape esatte di D-123/124/127/128/131/133/136/137/141/143/146/147
    - `tsc --noEmit` passa su `@gluezero/worker` (non ci sono ancora source consumer ma i type compilano)
    - `INTERNAL_TOPICS_WORKER` frozen const + `isInternalWorkerTopic` STRICT match esportati
    - `RouteWorkerDefinition.policies` usa `Pick<RoutePolicies, 'timeout' | 'concurrency' | 'backpressure' | 'dedupe'>` (D-143 enforced a livello TS)
    - Barrel `types/index.ts` re-esporta tutto
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: augment.ts declaration merging + barrel index.ts + smoke tests</name>
  <files>packages/worker/src/augment.ts, packages/worker/src/augment.test.ts, packages/worker/src/index.ts</files>
  <behavior>
    - Test 1: `import { __augmentWorkerLoaded } from '@gluezero/worker'` — `__augmentWorkerLoaded === true` (Pattern S1 anti tree-shake)
    - Test 2: `BrokerConfig` accetta property `workers?: WorkerConfig` con `assertSerializable: 'dev'` (decl merging)
    - Test 3: `PluginDescriptor` accetta property `workers?: readonly WorkerDescriptor[]` (D-126 cascade)
    - Test 4: `F5PipelineStep` literal union ha 4 valori esatti: `event.worker.dispatched | event.worker.progress | event.worker.completed | event.worker.failed`
    - Test 5: Type-level coexistence — config con `realtime` (F4) + `workers` (F5) + `gateway` (F3) + `mapper` (F2) compila in singolo `BrokerConfig` (declaration merging F2/F3/F4/F5 cumulativo)
    - Test 6: `import './augment'` come side-effect è preservato dopo build tsup (grep `__augmentWorkerLoaded` in `dist/`)
    - Test 7: PluginDescriptor con sia `realtimeChannels` (F4) che `workers` (F5) compila simultaneamente
    - Test 8: Smoke barrel — `import { __augmentWorkerLoaded, INTERNAL_TOPICS_WORKER, isInternalWorkerTopic } from '@gluezero/worker'` risolve runtime
  </behavior>
  <read_first>
    - packages/gateway/src/sse-ws/augment.ts (analog verbatim — copy struttura `declare module` + const literal true)
    - packages/gateway/src/sse-ws/augment.test.ts (analog smoke test verbatim — adapt names)
    - packages/gateway/src/sse-ws/index.ts (analog barrel — verbatim ordine import side-effect)
    - packages/routing/src/augment.ts (vedi quale pattern PipelineStep export literal union usa)
    - .planning/phases/05-worker-runtime/05-PATTERNS.md sezione "augment.ts" (LOC 177-241) e "Shared Patterns S1" (LOC 1094-1106)
    - .planning/phases/05-worker-runtime/05-RESEARCH.md §7.1 (LOC 884-942 RouteDefinition R4 limitation)
    - .planning/phases/05-worker-runtime/05-CONTEXT.md D-122, D-126, D-152
    - packages/worker/src/types/index.ts (creato in Task 2 — sorgente type per import)
  </read_first>
  <action>
**3.1 — `packages/worker/src/augment.ts` (CREATE):**
Copia struttura verbatim da `packages/gateway/src/sse-ws/augment.ts`, sostituisci `realtime`/`realtimeChannels` con `workers`/`workers`, sostituisci `F4PipelineStep` con `F5PipelineStep`:
```ts
/**
 * F5 augmentation (D-122, D-126, D-152): estende `BrokerConfig` e `PluginDescriptor` di
 * `@gluezero/core` via declaration merging additive, NON modifica runtime di F1/F2/F3/F4
 * (D-83 strict carryover D-121).
 *
 * **Pattern S1 anti tree-shaking (CRITICAL):** il file espone `__augmentWorkerLoaded`
 * come const literal `true`. Il barrel `index.ts` lo re-esporta per garantire che il modulo
 * `augment` sia evaluated come side-effect anche da consumer che non usano TypeScript-only
 * import. `package.json#sideEffects` glob `**/augment.{ts,js}` blocca tree-shake bundler.
 *
 * @see .planning/phases/05-worker-runtime/05-CONTEXT.md (D-121..D-154)
 * @see .planning/research/ARCHITECTURE.md §3.2 (declaration merging pattern)
 */

import type { WorkerConfig, WorkerDescriptor } from './types'

declare module '@gluezero/core' {
  /** F5 augmentation (D-122, PRD §27): aggiunge la sezione `workers` a `BrokerConfig`. */
  interface BrokerConfig {
    /**
     * F5 sezione `workers` (D-122, PRD §19): config runtime worker (assertSerializable mode,
     * defaults globali). Auto-merge con `realtime` (F4), `gateway` (F3), `mapper` (F2).
     */
    workers?: WorkerConfig
  }

  /** F5 augmentation (D-126, WK-01): PluginDescriptor.workers per cascade D-26 ext F5. */
  interface PluginDescriptor {
    /**
     * F5 (D-126): worker auto-registrati al `registerPlugin` con `ownerId = pluginId`.
     * Cascade `unregisterByOwner` su `unregisterPlugin` (LIFE-02 ext F5).
     */
    readonly workers?: readonly WorkerDescriptor[]
  }
}

/**
 * F5 PipelineStep — eventi step §28 emessi dal WorkerHandler (D-152 step 9 dispatch worker).
 *
 * **Limitazione TS (R4 RESEARCH §17):** `PipelineStep` di `@gluezero/core` è un type alias literal union,
 * NON un'interface — TS non supporta declaration merging di type alias. Soluzione: il consumer
 * che dichiara tap F5 importa questo super-set additive e dichiara localmente:
 *
 * ```ts
 * import type { F5PipelineStep } from '@gluezero/worker'
 * import type { PipelineStep as F1PipelineStep } from '@gluezero/core'
 * type AllSteps = F1PipelineStep | F5PipelineStep
 * ```
 *
 * @see .planning/phases/05-worker-runtime/05-RESEARCH.md §7.4 (Tap mapping)
 */
export type F5PipelineStep =
  | 'event.worker.dispatched'
  | 'event.worker.progress'
  | 'event.worker.completed'
  | 'event.worker.failed'

/**
 * Marker const esportato per detection runtime del side-effect import (Pattern S1).
 * Re-esportato dal barrel `src/index.ts` per garantire evaluation del modulo augment.
 */
export const __augmentWorkerLoaded: true = true
```

**3.2 — `packages/worker/src/augment.test.ts` (CREATE):**
Copy struttura da `packages/gateway/src/sse-ws/augment.test.ts` adattando names. 8 test richiesti:
```ts
import { describe, expect, it } from 'vitest'
import type { BrokerConfig, PluginDescriptor } from '@gluezero/core'
import { __augmentWorkerLoaded, type F5PipelineStep } from './augment'
import {
  INTERNAL_TOPICS_WORKER,
  isInternalWorkerTopic,
  type WorkerConfig,
  type WorkerDescriptor,
  type RouteWorkerDefinition,
  type ProgressPayload,
  type TaskState,
} from './types'

describe('augment.ts — F5 declaration merging smoke', () => {
  it('Test 1: __augmentWorkerLoaded const true exported (Pattern S1 anti tree-shake)', () => {
    expect(__augmentWorkerLoaded).toBe(true)
  })

  it('Test 2: BrokerConfig.workers field accepts WorkerConfig (D-122 decl merging)', () => {
    const config: BrokerConfig = {
      workers: { assertSerializable: 'dev', defaultProgressThrottleMs: 100, defaultTimeoutMs: 30_000 },
    }
    expect(config.workers?.assertSerializable).toBe('dev')
  })

  it('Test 3: PluginDescriptor.workers field accepts WorkerDescriptor[] (D-126 cascade)', () => {
    const descriptor: PluginDescriptor = {
      id: 'p1',
      workers: [
        {
          id: 'report-worker',
          factory: () => new Worker('about:blank' as unknown as URL),
          tasks: ['generateReport'] as const,
          mode: 'pool',
        },
      ],
    }
    expect(descriptor.workers?.[0]?.id).toBe('report-worker')
  })

  it('Test 4: F5PipelineStep literal union has 4 exact members', () => {
    const steps: readonly F5PipelineStep[] = [
      'event.worker.dispatched',
      'event.worker.progress',
      'event.worker.completed',
      'event.worker.failed',
    ] as const
    expect(steps).toHaveLength(4)
  })

  it('Test 5: Coexistence with F2/F3/F4 augmentations (cumulative decl merging)', () => {
    // Type-level test — se compila, il decl merging F2/F3/F4/F5 coesiste in BrokerConfig
    const config: BrokerConfig = { workers: { assertSerializable: 'off' } }
    expect(config).toBeDefined()
  })

  it('Test 6: INTERNAL_TOPICS_WORKER frozen + STRICT match (Pattern S5)', () => {
    expect(INTERNAL_TOPICS_WORKER.CANCEL).toBe('__cancel__')
    expect(INTERNAL_TOPICS_WORKER.PROGRESS).toBe('__progress__')
    expect(Object.isFrozen(INTERNAL_TOPICS_WORKER)).toBe(true)
    expect(isInternalWorkerTopic('__cancel__')).toBe(true)
    expect(isInternalWorkerTopic('__progress__')).toBe(true)
    // STRICT: weather.__cancel__ NOT internal
    expect(isInternalWorkerTopic('weather.__cancel__')).toBe(false)
    expect(isInternalWorkerTopic('__cancelxxx__')).toBe(false)
  })

  it('Test 7: PluginDescriptor coesiste realtimeChannels (F4) + workers (F5) simultanei', () => {
    const desc: PluginDescriptor = {
      id: 'multi',
      realtimeChannels: [],
      workers: [],
    }
    expect(desc.realtimeChannels).toEqual([])
    expect(desc.workers).toEqual([])
  })

  it('Test 8: ProgressPayload + TaskState + RouteWorkerDefinition + WorkerTaskOutcome importable', () => {
    const p: ProgressPayload = { value: 0.5, message: 'half' }
    const s: TaskState = 'pending'
    const r: RouteWorkerDefinition = {
      type: 'worker', id: 'r1', topic: 't.requested', worker: 'w', task: 'tk',
      policies: { timeout: 30_000, concurrency: 'latest-only' },
    }
    expect(p.value).toBe(0.5)
    expect(s).toBe('pending')
    expect(r.type).toBe('worker')
  })
})
```

**3.3 — `packages/worker/src/index.ts` (CREATE — barrel pubblico):**
Pattern S1 — re-export side-effect augment + types + (placeholder runtime exports per Wave 2-4):
```ts
/**
 * @gluezero/worker — Worker Runtime entry point pubblico (Phase 5).
 *
 * **Pattern S1 anti tree-shake:** il side-effect import `./augment` è preservato dal
 * package.json#sideEffects glob `**\/augment.{ts,js}` + dal re-export `__augmentWorkerLoaded`.
 *
 * Wave 2-4 popolerà runtime exports (assertSerializable, transferableExtractor, taskTracker,
 * WorkerBridge, WorkerPool, WorkerRegistry, WorkerHandler, WorkerBroker, createWorkerBroker).
 */

// Side-effect import — abilita declaration merging F5 in BrokerConfig.workers + PluginDescriptor.workers
export { __augmentWorkerLoaded, type F5PipelineStep } from './augment'

// ---------- Public types ----------
export {
  INTERNAL_TOPICS_WORKER,
  isInternalWorkerTopic,
  type WorkerDescriptor,
  type WorkerMode,
  type WorkerType,
  type WorkerConfig,
  type RouteWorkerDefinition,
  type ProgressPayload,
  type TaskState,
  type WorkerTaskOutcome,
} from './types'
```

**Verification post-Task 3:**
- `pnpm -F @gluezero/worker test` exit 0 con 8 test passing
- `pnpm -F @gluezero/worker build` produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}`
- `grep -q "__augmentWorkerLoaded" packages/worker/dist/index.js` (Pattern S1 audit)
- `pnpm -F @gluezero/core typecheck && pnpm -F @gluezero/mapper typecheck && pnpm -F @gluezero/routing typecheck && pnpm -F @gluezero/gateway typecheck` tutti passing (zero regression — D-83 strict)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" &amp;&amp; pnpm -F @gluezero/worker test --run &amp;&amp; pnpm -F @gluezero/worker build &amp;&amp; test -f packages/worker/dist/index.js &amp;&amp; test -f packages/worker/dist/index.d.ts &amp;&amp; test -f packages/worker/dist/augment.js &amp;&amp; grep -q "__augmentWorkerLoaded" packages/worker/dist/index.js &amp;&amp; grep -q "F5PipelineStep" packages/worker/dist/index.d.ts &amp;&amp; pnpm -F @gluezero/core typecheck &amp;&amp; pnpm -F @gluezero/mapper typecheck &amp;&amp; pnpm -F @gluezero/routing typecheck &amp;&amp; pnpm -F @gluezero/gateway typecheck</automated>
  </verify>
  <done>
    - `augment.ts` estende `BrokerConfig` e `PluginDescriptor` via `declare module '@gluezero/core'` con `workers?: WorkerConfig` e `workers?: readonly WorkerDescriptor[]`
    - `F5PipelineStep` literal union 4 valori esatti esportato
    - `__augmentWorkerLoaded: true` const literal esportato
    - `augment.test.ts` 8 test passing (smoke decl merging F5 + coexistence F2/F3/F4 + INTERNAL_TOPICS strict)
    - Barrel `src/index.ts` re-esporta side-effect augment + types
    - Build tsup produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}`
    - Pattern S1 audit: `__augmentWorkerLoaded` presente in `dist/index.js`
    - Tutti gli altri 4 package del monorepo type-check senza regression (D-83 strict ✓)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer config → BrokerConfig.workers | Config passato a `createWorkerBroker(config)` può contenere descriptor malformati |
| Bundler → tsup output | Tree-shaker bundler potrebbe eliminare side-effect import `./augment` se non protetto |
| Cross-phase decl merging | F2/F3/F4/F5 augmentano stessa interface BrokerConfig — collisione field names rischio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | augment.ts decl merging | mitigate | Const literal `true` non runtime-mutabile (`__augmentWorkerLoaded: true`); freeze interno tramite `Object.freeze` su `INTERNAL_TOPICS_WORKER`. Verifica: grep `Object.freeze` in internal-topics.ts ≥ 1. |
| T-05-01-02 | DoS | tree-shake elimina augment import | mitigate | Pattern S1: package.json#sideEffects glob `**/augment.{ts,js}` + barrel re-export `__augmentWorkerLoaded`. Verifica: grep `__augmentWorkerLoaded` in `dist/index.js` exit 0. |
| T-05-01-03 | Information Disclosure | error category leakage type-level | mitigate | RouteWorkerDefinition.policies usa `Pick<RoutePolicies, 'timeout' \| 'concurrency' \| 'backpressure' \| 'dedupe'>` — no exposure di RetryConfig/AuthConfig/CircuitBreakerConfig types (D-143). Verifica: grep `Pick<RoutePolicies,` in route-worker-definition.ts ≥ 1. |
| T-05-01-04 | Logic flaw | self-invalidating grep gate (Nyquist) | mitigate | Tutti i grep gate usano stringhe specifiche con quote escaped o pattern non-self-matching (es. `INTERNAL_TOPICS_WORKER` token uniquely identifica il source code, non un commento header). |
| T-05-01-05 | Logic flaw | spoofing source `type: 'worker'` | accept | Source descriptor frozen const arriverà in 05-06 worker-handler — Wave 1 expone solo type-level, runtime threat in plan dedicato (T-05-06-XX). Plan checker propaga al plan downstream. |
| T-05-01-06 | Tampering | INTERNAL_TOPICS_WORKER mutation | mitigate | `Object.freeze(... as const)` blocca runtime mutation; STRICT match `topic === '__cancel__'` no `startsWith('__')` blocca prefix-spoofing (Pattern S5 carryover F4 anti-AP-6). Verifica: grep `topic === INTERNAL_TOPICS_WORKER` esatto, NO `startsWith('__')`. |
| T-05-01-07 | DoS | unbounded type recursion da declaration merging | accept | TS compiler protegge nativamente; nessun pattern Mapped Types ricorsivo F5. Verifica: tsc --noEmit completa < 10s. |

**Severity assessment:** Tutti LOW (Wave 1 è scaffold type-level + config). Nessun threat HIGH+ richiede block.
</threat_model>

<verification>
**Plan-level checks (acceptance criteria aggregate):**
- `pnpm install` from monorepo root completa senza errori (lockfile coerente)
- `pnpm -F @gluezero/worker test --run` exit 0, 8 test passing (smoke decl merging)
- `pnpm -F @gluezero/worker build` produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}`
- `pnpm -F @gluezero/worker typecheck` exit 0
- `grep -c "__augmentWorkerLoaded" packages/worker/dist/index.js` ≥ 1 (Pattern S1 audit anti tree-shake)
- `grep -c "F5PipelineStep" packages/worker/dist/index.d.ts` ≥ 1 (export tap step literal union)
- `grep -c "INTERNAL_TOPICS_WORKER" packages/worker/dist/index.d.ts` ≥ 1
- **D-83 strict gate:** `git diff main...HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/ packages/gateway/src/http/ packages/gateway/src/sse-ws/` produce zero output (zero modifiche runtime fuori da `packages/worker/`). Nota: `packages/worker/src/augment.ts` è in scope perché è il pacchetto F5.
- Cross-package typecheck: `pnpm -F @gluezero/core typecheck && pnpm -F @gluezero/mapper typecheck && pnpm -F @gluezero/routing typecheck && pnpm -F @gluezero/gateway typecheck` tutti exit 0
- Coverage non applicabile a Wave 1 (only type files + config — coverage thresholds attivi da Wave 2)
</verification>

<success_criteria>
- [ ] `packages/worker/package.json` contiene name `@gluezero/worker`, type module, deps comlink 4.4.2 + workspace deps + valibot + nanoid, sideEffects glob, scripts build/test/typecheck/test:browser/test:coverage/lint
- [ ] `packages/worker/tsconfig.json` strict + isolatedDeclarations + ES2022/ESNext/Bundler
- [ ] `packages/worker/tsup.config.ts` ESM-only 2 entries (index + augment) con dts true clean true
- [ ] `packages/worker/vitest.config.ts` jsdom + coverage v8 thresholds 90/80/90/90 + exclude `__browser__`
- [ ] `packages/worker/vitest.browser.config.ts` Playwright Chromium include `__browser__/**`
- [ ] 7 type files in `src/types/` (worker-descriptor, worker-config, route-worker-definition, progress-payload, task-state, internal-topics, index) con shape esatte di D-123/124/127/128/131/133/136/137/141/143/146/147
- [ ] `INTERNAL_TOPICS_WORKER` frozen const + `isInternalWorkerTopic` STRICT match no prefix
- [ ] `RouteWorkerDefinition.policies` enforced via `Pick<RoutePolicies, ...>` D-143 (no retry/auth/circuitBreaker)
- [ ] `augment.ts` con `declare module '@gluezero/core'` per BrokerConfig.workers + PluginDescriptor.workers + F5PipelineStep + `__augmentWorkerLoaded: true`
- [ ] `augment.test.ts` 8 test smoke decl merging passing
- [ ] `index.ts` barrel re-esporta side-effect augment + tutti i type pubblici
- [ ] Build tsup produce `dist/{index,augment}.{js,d.ts}` con `__augmentWorkerLoaded` preservato (Pattern S1 audit)
- [ ] D-83 strict ✓ — zero modifiche a `packages/{core,mapper,routing}/src/` e `packages/gateway/src/{http,sse-ws}/`
- [ ] Tutti gli altri 4 package del monorepo type-check senza regression
</success_criteria>

<output>
After completion, create `.planning/phases/05-worker-runtime/05-01-SUMMARY.md` documentando:
- Files created (count + lista path)
- Test results (8/8 smoke decl merging)
- Build artifacts (dist/{index,augment}.{js,d.ts} sizes KB)
- Pattern S1 audit (grep result)
- D-83 strict verification (`git diff` zero output)
- REQ progress: WK-01..WK-07 type-level scaffold + PKG-01..PKG-04 done
- Building blocks pronti per Wave 2 (05-02 + 05-03)
</output>
