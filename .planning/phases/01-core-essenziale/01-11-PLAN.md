---
phase: 01-core-essenziale
plan: 11
type: execute
wave: 8
depends_on: [01-09, 01-10]
files_modified:
  - packages/core/README.md
  - packages/core/src/types/broker-event.ts
  - packages/core/src/types/subscription.ts
  - packages/core/src/types/plugin.ts
  - packages/core/src/types/error.ts
  - packages/core/src/types/logger.ts
  - packages/core/src/types/tap.ts
  - packages/core/src/types/config.ts
  - packages/core/src/public-factory.ts
  - packages/core/src/core/broker.ts
  - packages/core/src/core/broker-error.ts
  - packages/core/src/index.ts
  - packages/core/dist/
autonomous: true
requirements:
  - PKG-04
  - DOC-01
tags:
  - documentation
  - build-verification
  - ci-gates
must_haves:
  truths:
    - "`packages/core/README.md` documenta l'API pubblica completa F1 (createBroker, Broker class methods, types pubblici, EventTap pre-instrumented constraint)"
    - "Tutti gli export pubblici da `src/index.ts` hanno JSDoc con descrizione, params, return, esempio breve"
    - "`pnpm --filter @sembridge/core build` produce `dist/index.js` (ESM) + `dist/index.d.ts` (declaration) + sourcemaps"
    - "`pnpm ci:publint` esce 0 (package shape valida — exports, types, sideEffects)"
    - "`pnpm ci:attw` esce 0 (.d.ts resolution corretta per node16/bundler)"
    - "`pnpm ci:size` esce 0 (bundle gzip <= 8 KB budget)"
    - "Smoke import: `node -e \"import('@sembridge/core').then(m => console.log(Object.keys(m).sort()))\"` stampa esports pubblici inclusi `Broker`, `createBroker`, `createBrokerError`, `isBrokerError`"
    - "Suite completa `pnpm --filter @sembridge/core test` esce 0"
    - "`pnpm biome check .` esce 0 (lint pulito workspace-wide)"
    - "`pnpm typecheck` esce 0 (workspace-wide)"
  artifacts:
    - path: "packages/core/README.md"
      provides: "DOC-01 completo: install, getting started, API surface, scenario meteo example, EventTap constraint, vincoli architetturali"
      min_lines: 80
    - path: "packages/core/src/index.ts"
      provides: "Public entry point con JSDoc summary"
      contains: "createBroker"
    - path: "packages/core/dist/index.js"
      provides: "Build artifact ESM bundled"
    - path: "packages/core/dist/index.d.ts"
      provides: "TypeScript declarations (PKG-04)"
  key_links:
    - from: "packages/core/README.md"
      to: "src/index.ts public API"
      via: "Documentazione di ogni export pubblico"
      pattern: "createBroker"
    - from: "JSDoc on public exports"
      to: "TypeDoc (future F6)"
      via: "Skeleton per generazione docs automatica in F6"
      pattern: "@param"
---

<objective>
Finalizzare DOC-01 (skeleton API documentation) e verificare i CI gates di packaging (PKG-04 .d.ts + publint + attw + size-limit). Aggiungere JSDoc dettagliato a tutti gli export pubblici di `src/index.ts` per generare documentazione utile per il consumer e abilitare TypeDoc generation in F6.

Coverage: PKG-04 (.d.ts generate per API pubblica), DOC-01 (documentazione API pubblica skeleton).

Purpose: chiudere Phase 1 con build verde, package shape verificato, e documentazione minima ma completa. Il consumer di `@sembridge/core` può fare `pnpm add @sembridge/core` e iniziare a usare il broker leggendo SOLO il README.

Output: README.md espanso (~100+ righe), JSDoc su tutti gli export pubblici, CI gates verdi (publint, attw, size-limit), build artifacts presenti, smoke test passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-09-SUMMARY.md
@.planning/phases/01-core-essenziale/01-10-SUMMARY.md
@packages/core/src/index.ts
@packages/core/src/public-factory.ts
@packages/core/src/core/broker.ts
@packages/core/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: JSDoc sui public exports + README.md DOC-01 completo</name>
  <files>packages/core/README.md, packages/core/src/index.ts, packages/core/src/public-factory.ts, packages/core/src/core/broker.ts, packages/core/src/core/broker-error.ts, packages/core/src/types/broker-event.ts, packages/core/src/types/subscription.ts, packages/core/src/types/plugin.ts, packages/core/src/types/error.ts, packages/core/src/types/logger.ts, packages/core/src/types/tap.ts, packages/core/src/types/config.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "DOC-01 (skeleton API documentation)" + "DOC-06 documentazione debug tooling skeleton"
    - prd.md §29 (scenario meteo per esempio end-to-end) + §41.3 (DOC-01 requirement)
    - packages/core/src/index.ts (creato in plan 08)
  </read_first>
  <action>
**1. Aggiungere JSDoc completo a `packages/core/src/index.ts`:**

```ts
/**
 * @sembridge/core — Browser-side event broker (pub/sub, plugin registry,
 * lifecycle, BrokerEvent model, EventTap pre-instrumentation).
 *
 * ESM-only library, target ES2022 (browser evergreen).
 * Phase 1 of SemBridge V1.
 *
 * Architectural constraint (per ARCHITECTURE.md §3.2):
 * `EventTap` is pre-instrumented at the 5 F1 pipeline steps with no-op
 * implementation by default. Phase 6 substitutes the no-op with real
 * Inspector implementations without retrofit.
 *
 * @packageDocumentation
 */

export { createBroker } from './public-factory'
export { Broker } from './core/broker'

export type {
  /** Event envelope flowing through the pipeline. */
  BrokerEvent,
  /** Originator descriptor (plugin/component/server/worker/system). */
  EventSource,
  /** Delivery semantics: 'sync' | 'async' (F1 default) | 'worker' | 'remote' (F1 fallback to async). */
  DeliveryMode,
  /** Event priority: 'low' | 'normal' (default) | 'high' | 'critical'. */
  Priority,
  /** Branded type for unique event IDs. */
  EventId,
} from './types/broker-event'

export type {
  /** Subscription handle returned by `Broker.subscribe`. Idempotent unsubscribe. */
  Subscription,
  /** Options accepted by `Broker.subscribe`: signal, priority, deliveryMode, once. */
  SubscribeOptions,
} from './types/subscription'

export type {
  /** Plugin descriptor with optional lifecycle hooks. */
  PluginDescriptor,
  /** Context passed to plugin hooks: id, logger, broker, signal. */
  PluginContext,
  /** Lifecycle state: unregistered → registered → mounting → mounted → unmounting → unmounted → destroyed (or → failed). */
  PluginState,
} from './types/plugin'

export type {
  /** Error type thrown by the broker, extending Error with code/category/details. */
  BrokerError,
  /** Error category enum: validation | plugin | mapping | route | network | worker | system | config | topic. */
  ErrorCategory,
} from './types/error'

export {
  /** Type guard for BrokerError. */
  isBrokerError,
  /** Factory for BrokerError instances. */
  createBrokerError,
} from './core/broker-error'

export type {
  /** Logger interface: 5 methods (error, warn, info, debug, trace). */
  BrokerLogger,
  /** Log level: silent | error | warn | info | debug | trace. */
  LogLevel,
} from './types/logger'

export type {
  /** Wire Tap pattern for pipeline observation. F1 no-op default; F6 real Inspector. */
  EventTap,
  /** Pipeline step identifier (5 steps in F1). */
  PipelineStep,
  /** Pipeline state snapshot at each step. */
  PipelineSnapshot,
} from './types/tap'

export type {
  /** Broker configuration with sections: runtime, debug, topicSchemas (F2+), routes (F3+), etc. */
  BrokerConfig,
} from './types/config'

export type {
  /** Recursive readonly utility type. */
  DeepReadonly,
} from './types/deep-readonly'
```

**2. Aggiungere JSDoc a `packages/core/src/public-factory.ts`:**

Anteporre alla function `createBroker`:

```ts
/**
 * Creates a new Broker instance with the given configuration.
 *
 * Phase 1 implements the `runtime` and `debug` config sections; Phase 2-6
 * sections (`topicSchemas`, `canonicalModel`, `routes`, etc.) are accepted
 * as `unknown` and ignored at runtime in F1.
 *
 * @param config - Optional broker configuration (default: empty object).
 * @returns A fresh Broker instance. Each call returns an independent instance (no singleton).
 * @throws {Error} `Invalid BrokerConfig: ...` if config validation (Valibot) fails.
 *
 * @example
 * ```ts
 * import { createBroker } from '@sembridge/core'
 *
 * const broker = createBroker({
 *   runtime: { logLevel: 'info', debug: false },
 * })
 *
 * const sub = broker.subscribe('weather.requested', (event) => {
 *   console.log('received:', event.payload)
 * })
 *
 * broker.publish('weather.requested', { city: 'Roma' }, {
 *   source: { type: 'plugin', id: 'weather-form' },
 * })
 * ```
 */
export function createBroker(config: BrokerConfig = {}): Broker {
  // ... existing impl
}
```

**3. Aggiungere JSDoc agli altri public exports:**

In `core/broker.ts`, anteporre JSDoc alla classe `Broker` e ai metodi pubblici:

```ts
/**
 * Core event broker. Composition of EventBus + PluginRegistry + TopicRegistry.
 *
 * Created via `createBroker(config)` factory. No singleton pattern (D-30).
 *
 * Implements pub/sub with wildcard subscribe, plugin lifecycle anti-leak,
 * deep-freeze in dev mode, EventTap pre-instrumented on 5 F1 pipeline steps.
 */
export class Broker {
  /**
   * Publish an event to the broker.
   *
   * Builds a BrokerEvent via factory (with id from nanoid, timestamp from Date.now()
   * if not provided), validates the event shape (VAL-01), and dispatches to
   * matching subscribers.
   *
   * Default deliveryMode is 'async' (D-01) via queueMicrotask — handler invocation
   * is deferred to next microtask, preventing re-entrancy stack overflow.
   *
   * @param topic - Topic name. Must match `<entity>.<action>.<status>` (lowercase,
   *   dot-separated). Validated against regex `/^[a-z][a-z0-9]*(\.[a-z][a-z0-9*]*)*$/`.
   * @param payload - Event payload. Frozen recursively in dev mode (D-04).
   * @param options - Publish options: source (required for `event.source.missing` to
   *   not throw), deliveryMode, priority, correlationId, ttlMs, dedupeKey, etc.
   * @throws {BrokerError} `topic.invalid` if topic name fails regex.
   * @throws {BrokerError} `event.source.missing` if no source provided.
   * @throws {BrokerError} `event.validation.failed` if BrokerEvent shape invalid.
   */
  publish<T>(topic: string, payload: T, options: Omit<PublishParams<T>, 'topic' | 'payload'> = {}): void {
    // ... existing impl
  }

  /**
   * Subscribe to a topic or topic pattern.
   *
   * Patterns support wildcards: `weather.*` (single segment), `*.failed` (leading),
   * `weather.*.failed` (multi-position, D-11).
   *
   * @param pattern - Topic or pattern to subscribe to.
   * @param handler - Function invoked when matching event published. Errors are isolated
   *   (CORE-12) and re-published as `system.error` BrokerEvent.
   * @param options - signal (AbortSignal for auto-unsubscribe), once, priority, deliveryMode.
   * @returns A Subscription handle with idempotent `.unsubscribe()`.
   *
   * @example
   * ```ts
   * const sub = broker.subscribe('weather.*', (event) => console.log(event))
   * sub.unsubscribe()
   * ```
   */
  subscribe(...): Subscription { /* ... */ }

  /**
   * Register a plugin with lifecycle hooks.
   *
   * Lifecycle execution order (D-25):
   *   1. transitionState: unregistered → registered
   *   2. await onRegister(ctx)
   *   3. transitionState: registered → mounting
   *   4. await onMount(ctx)
   *   5. transitionState: mounting → mounted
   *
   * @throws {BrokerError} `plugin.id.duplicate` if a plugin with the same id is already registered.
   * @throws {BrokerError} `plugin.lifecycle.failed` if onRegister or onMount throws.
   */
  registerPlugin(descriptor: PluginDescriptor): Promise<void> { /* ... */ }

  /**
   * Unregister a plugin with mandatory cascade cleanup (D-26, LIFE-02 — closes PRD §39 #7).
   *
   * Cleanup order:
   *   1. await onUnmount(ctx) — errors caught + logged but cascade proceeds
   *   2. bus.unsubscribeByOwner(id) — removes all subscriptions registered with `signal: ctx.signal`
   *   3. abortController.abort() — fires AbortSignal for in-flight async handlers
   *   4. onDestroy(ctx) — sync, errors logged
   *
   * After unregister: `getDebugSnapshot()` counters return to pre-registration baseline.
   *
   * @throws {BrokerError} `plugin.not-found` if no plugin with the given id.
   */
  unregisterPlugin(id: string): Promise<void> { /* ... */ }

  /** @returns Readonly array of all topic names ever published or registered (CORE-03). */
  getTopicRegistry(): readonly string[] { /* ... */ }

  /** Replace the runtime logger. */
  setLogger(logger: BrokerLogger): void { /* ... */ }

  /** Enable debug mode: deep-freeze runtime + verbose tap snapshots (D-29). */
  enableDebug(): void { /* ... */ }

  /** Disable debug mode (production default). */
  disableDebug(): void { /* ... */ }

  /**
   * Get a snapshot of the current broker state for debugging (D-28).
   *
   * In F1 returns: topics, subscriberCount per topic, pluginIds, pendingAsyncDelivery,
   * logLevel, pipelineSteps. F6 will extend with full metrics.
   */
  getDebugSnapshot(): { /* ... */ } { /* ... */ }
}
```

In `core/broker-error.ts`, anteporre JSDoc:

```ts
/**
 * Create a BrokerError with the given parameters.
 *
 * @param params - Error parameters: code, category, message + optional fields.
 * @returns BrokerError instance (Error subclass with code, category, details, etc.).
 *
 * @example
 * ```ts
 * throw createBrokerError({
 *   code: 'topic.invalid',
 *   category: 'topic',
 *   message: `Invalid topic name: "${topic}"`,
 *   details: { topic },
 * })
 * ```
 */
export function createBrokerError(...) { /* ... */ }

/**
 * Type guard for BrokerError.
 *
 * @returns `true` if the value is an Error with `code` and `category` properties.
 */
export function isBrokerError(...) { /* ... */ }
```

NOTA: aggiungere JSDoc anche a tutti i tipi pubblici nelle file `types/*.ts` se non già presenti dal commento `/** @sembridge */`. Tipicamente 1 riga JSDoc per interfaccia/type alias.

**4. Espandere `packages/core/README.md` (DOC-01 completo):**

```md
# @sembridge/core

> Core event broker for SemBridge — pub/sub in-page, plugin registry with anti-leak lifecycle, structured `BrokerEvent` model, `EventTap` pre-instrumented for Phase 6 Inspector.

ESM-only TypeScript library. Browser evergreen target (ES2022). Two runtime dependencies: [`nanoid`](https://github.com/ai/nanoid) (event IDs) and [`valibot`](https://valibot.dev) (config + event shape validation).

## Installazione

```sh
pnpm add @sembridge/core
# oppure
npm install @sembridge/core
```

## Quick start

```ts
import { createBroker } from '@sembridge/core'

const broker = createBroker({
  runtime: { logLevel: 'info' },
})

// Subscribe — wildcard supportato
const sub = broker.subscribe('weather.*', (event) => {
  console.log(`Received ${event.topic}:`, event.payload)
})

// Publish (default: deliveryMode='async' via queueMicrotask)
broker.publish('weather.requested', { city: 'Roma' }, {
  source: { type: 'plugin', id: 'weather-form' },
})

// Cleanup
sub.unsubscribe()
```

## API pubblica

### `createBroker(config?: BrokerConfig): Broker`

Crea una nuova istanza del broker. Niente singleton globale (D-30); ogni chiamata ritorna istanza indipendente.

`config.runtime.logLevel`: `'silent' | 'error' | 'warn' | 'info' | 'debug' | 'trace'` (default `'info'`)
`config.runtime.debug`: boolean (default `false` in production, `true` in dev via `import.meta.env.DEV`) — abilita deep-freeze runtime del payload
`config.runtime.logger`: `BrokerLogger` custom (default `ConsoleLogger`)
`config.runtime.tap`: `EventTap` custom (default no-op; F6 sostituirà con Inspector reale)
`config.topicSchemas`, `config.canonicalModel`, `config.routes`, `config.transport`, `config.workers`, `config.cache`: placeholder per Phase 2-6 (accettati come `unknown` in F1).

### `Broker`

Classe principale. Metodi pubblici:

- `publish<T>(topic, payload, options?)` — pubblica un evento. `options.source` deve essere fornito o l'evento ha `event.source.missing`.
- `subscribe(pattern, handler, options?)` — sottoscrive un pattern (wildcard supportati: `weather.*`, `*.failed`, `weather.*.failed`). Ritorna `Subscription` con `.unsubscribe()` idempotente.
- `registerPlugin(descriptor)` — registra un plugin con lifecycle hooks (`onRegister`, `onMount`, `onUnmount`, `onDestroy`). Cascade cleanup automatico al `unregisterPlugin`.
- `unregisterPlugin(id)` — rimuove plugin + tutte le subscription registrate con `signal: ctx.signal` (cascade D-26 — chiusura PRD §39 #7).
- `getTopicRegistry(): readonly string[]` — elenco dei topic noti.
- `getDebugSnapshot()` — snapshot dello stato corrente (topics, subscriberCount, pluginIds, pipelineSteps, ecc.)
- `enableDebug() / disableDebug()` — toggle deep-freeze runtime e verbose tap snapshot.
- `setLogger(logger)` — sostituisce logger runtime.

## Naming convention dei topic (CORE-08)

I topic devono rispettare il pattern `<entity>.<action>.<status>` lowercase dot-separated. Regex enforcement: `/^[a-z][a-z0-9]*(\.[a-z][a-z0-9*]*)*$/`.

Esempi validi: `weather.requested`, `auth.user.login`, `form.customer.submitted`.
Esempi invalidi: `Weather.Requested` (uppercase), `weather/requested` (slash), `weather..requested` (segmento vuoto).

Wildcard supportati nei pattern di subscribe (single-segment match):
- `weather.*` matcha `weather.requested`, `weather.loaded`
- `*.failed` matcha `weather.failed`, `auth.failed`
- `weather.*.failed` matcha `weather.alert.failed`, `weather.danger.failed`

## Plugin lifecycle (CORE-04, CORE-05)

```ts
await broker.registerPlugin({
  id: 'weather-plugin',
  onRegister: (ctx) => {
    // sync, runs first
  },
  onMount: async (ctx) => {
    // async, runs after onRegister
    broker.subscribe('weather.requested', handler, { signal: ctx.signal })
  },
  onUnmount: async (ctx) => {
    // async, runs on unregisterPlugin
    // ctx.signal.aborted === false here
  },
  onDestroy: (ctx) => {
    // sync, last step
    // ctx.signal.aborted === true here (cascade fired)
  },
})
```

**Cascade cleanup (D-26, LIFE-02 — chiusura PRD §39 #7):** `unregisterPlugin(id)` invoca `onUnmount`, poi rimuove tutte le subscription registrate con `signal: ctx.signal`, fires `AbortController.abort()`, e infine `onDestroy`. Anche se `onUnmount` lancia eccezione, la cascade procede comunque (D-26 must always run).

## Handler isolation (CORE-12, ERR-03)

Un handler che lancia eccezione (sync o async via Promise rejected) è catturato dal broker:
1. L'errore viene loggato via `logger.error`.
2. Il broker pubblica `system.error` come `BrokerEvent` con `BrokerError.category: 'plugin'`, `code: 'plugin.handler.failed'`, includendo `originalEventId` e `originalTopic`.
3. Altri handler iscritti allo stesso topic continuano a essere invocati.
4. Il broker rimane funzionante per publish successivi.

```ts
broker.subscribe('system.error', (event) => {
  console.error('Plugin error:', event.payload.error)
})
```

## EventTap pre-instrumented (CORE-13, vincolo critico)

`EventTap` è instrumentato sui 5 step pipeline F1:
1. `event.received`
2. `event.metadata.enriched`
3. `event.validated`
4. `event.dedupe.checked` (placeholder F1, dedupe completo in F3)
5. `event.delivered`

Implementazione default: no-op (zero overhead). F2-F5 estendono la pipeline con step aggiuntivi. F6 sostituisce il no-op con Event/Mapping/Route Inspector reali — senza retrofit (vincolo architetturale ARCHITECTURE.md §3.2).

```ts
const broker = createBroker({
  runtime: {
    tap: {
      onPipelineStep: (step, snapshot) => {
        console.log(`[${step}]`, snapshot.eventId, snapshot.topic)
      },
    },
  },
})
```

Errori dal tap sono swallowed (D-20) — un tap che fallisce non rompe la pipeline.

## Delivery semantics (D-01..D-03)

- **`async` (default)** — consegna via `queueMicrotask`. Garantisce FIFO, previene re-entrancy stack overflow, isola publisher/subscriber.
- **`sync` (opt-in)** — consegna immediata. Use case: `system.error` per fail-fast.
- **`worker` / `remote`** — dichiarati nel tipo ma no-op in F1. Mappati su `async` con warn `mapping.delivery.fallback`. Implementazione completa in F3 (route HTTP) e F5 (worker).

## Deep-freeze runtime (D-04, D-05)

Quando `debug: true` (default in dev), il payload viene `Object.freeze` ricorsivamente prima della consegna ai subscriber. Tentativi di mutation lanciano `TypeError` in strict mode.

In produzione (`debug: false`), il freeze è skippato per performance — il contratto type-level `Readonly<T>` resta enforce-d a compile-time.

`Date`, `Promise`, `TypedArray` skippati per default (Date è freezable ma non immutabile; TypedArray view freeze rompe iteration).

## BrokerError (ERR-01)

```ts
import { isBrokerError } from '@sembridge/core'

try {
  broker.publish('Invalid.Topic', {}, { source: { type: 'plugin', id: 'p' } })
} catch (err) {
  if (isBrokerError(err)) {
    console.log(err.code)        // 'topic.invalid'
    console.log(err.category)    // 'topic'
    console.log(err.details)     // { topic: 'Invalid.Topic', regex: ... }
  }
}
```

## Roadmap

Phase 1 (questa fase): Core broker + EventTap pre-instrumented.
Phase 2: Canonical Model + Mapper bidirezionale (`@sembridge/mapper`).
Phase 3: Routing engine + HTTP gateway (`@sembridge/routing`, `@sembridge/gateway`).
Phase 4: Realtime SSE/WS adapter.
Phase 5: Worker runtime (`@sembridge/worker`).
Phase 6: Cache + Tooling avanzato (`@sembridge/cache`, `@sembridge/devtools`) — Inspector reale sostituisce no-op tap di F1.

## Vincoli architetturali

1. **`EventTap` instrumentato in F1** (no retrofit in F6).
2. **Canonicalizzazione interna completa V1** (PRD §13.5) — implementata in F2.
3. **Niente singleton globale** — `createBroker` ritorna istanze indipendenti.
4. **ESM-only V1** — niente CJS (no dual-package hazard).

## Licenza

MIT
```
  </action>
  <verify>
    <automated>test -f packages/core/README.md && grep -q "createBroker" packages/core/README.md && grep -q "EventTap pre-instrumented" packages/core/README.md && grep -q "LIFE-02" packages/core/README.md && grep -q "Cascade cleanup" packages/core/README.md && grep -q "@param" packages/core/src/core/broker.ts && grep -q "@example" packages/core/src/public-factory.ts && pnpm --filter @sembridge/core typecheck && pnpm biome check packages/core/src/</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/README.md` ha almeno 80 righe e sezioni: Installazione, Quick start, API pubblica, Naming convention, Plugin lifecycle, Handler isolation, EventTap pre-instrumented, Delivery semantics, Deep-freeze, BrokerError, Roadmap, Vincoli architetturali, Licenza
    - JSDoc presente su `createBroker` (in public-factory.ts) con `@param`, `@returns`, `@throws`, `@example`
    - JSDoc presente sulla classe `Broker` e sui metodi pubblici (`publish`, `subscribe`, `registerPlugin`, `unregisterPlugin`, `getDebugSnapshot`, ecc.)
    - JSDoc presente su `createBrokerError` e `isBrokerError`
    - JSDoc presente sui re-export di types in `src/index.ts` (almeno 1 riga descrittiva per ogni type)
    - `pnpm --filter @sembridge/core typecheck` esce 0 (JSDoc non rompe TS)
    - `pnpm biome check packages/core/src/` esce 0 (JSDoc non viola lint rules)
    - Il README documenta tutti i 5 success criteria di Phase 1 (pub/sub e2e, cascade unregister, naming validation, wildcard, EventTap pre-instrumented)
  </acceptance_criteria>
  <done>JSDoc su tutti gli export pubblici + README.md DOC-01 completo. Pronto per build verification.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build + CI gates (publint, attw, size-limit) + smoke test final</name>
  <files>packages/core/dist/, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Pitfall mitigation table" §13 (Build/distribution CI gates)
    - packages/core/package.json (size-limit budget, scripts)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md "Validation Architecture" Phase Gate
  </read_first>
  <action>
**1. Build pulito da zero:**

```bash
# Clean dist
rm -rf packages/core/dist

# Build
pnpm --filter @sembridge/core build
```

Atteso output: `dist/index.js` (ESM bundled) + `dist/index.d.ts` (declarations) + `dist/index.js.map` (sourcemap).

**2. Run typecheck workspace-wide:**

```bash
pnpm typecheck
```

Atteso: 0 errori. Se errori → fix prima di procedere.

**3. Run biome lint workspace-wide:**

```bash
pnpm biome check .
```

Atteso: 0 errori, 0 warning bloccanti. Se warning su style minori → accettabili; se errori → fix.

**4. Run suite test completa:**

```bash
pnpm --filter @sembridge/core test
```

Atteso: `Test Files >= 24 passed`, tutti i test passing.

**5. Run CI gate publint:**

```bash
pnpm ci:publint
```

publint controlla: `package.json#exports` corretto, `types` presente, `sideEffects: false`, niente file estranei in `files`. Atteso: exit 0 con eventuali suggestion ma niente errori bloccanti.

**6. Run CI gate attw (Are The Types Wrong):**

```bash
pnpm ci:attw
```

attw controlla: `.d.ts` resolution corretta per `node16`/`bundler`/`commonjs`/`esm`. Atteso: ESM check passes; CJS check skipped (non pubblichiamo CJS in V1, RESEARCH §13).

NOTA: `attw --pack` può fallire su CJS resolution per package ESM-only. Se fail solo su CJS check, è atteso. Se fail su ESM check, fix.

**7. Run CI gate size-limit:**

```bash
pnpm ci:size
```

size-limit controlla che `dist/index.js` gzipped sia <= 8 KB (budget definito in `packages/core/package.json#size-limit`). Atteso: PASS con misura attuale (dovrebbe essere ben sotto 8 KB con bundle minimal).

**8. Smoke import final:**

```bash
cd packages/core && node -e "import('./dist/index.js').then(m => { const keys = Object.keys(m).sort(); console.log('Exports:', keys.join(', ')); if (!keys.includes('Broker') || !keys.includes('createBroker') || !keys.includes('createBrokerError') || !keys.includes('isBrokerError')) { console.error('MISSING EXPORTS'); process.exit(1) } })"
```

Atteso: stampa `Exports: Broker, createBroker, createBrokerError, isBrokerError` (l'ordine alfabetico dovrebbe essere stabile).

**9. End-to-end smoke usage (verifica che il bundle funzioni davvero, non solo che esponga le funzioni):**

```bash
cd packages/core && node --input-type=module -e "
import { createBroker } from './dist/index.js'
const broker = createBroker({ runtime: { logLevel: 'silent' } })
const received = []
broker.subscribe('test.topic', (e) => received.push(e.payload))
broker.publish('test.topic', { ok: true }, { source: { type: 'plugin', id: 'smoke' }, deliveryMode: 'sync' })
if (received.length !== 1 || !received[0].ok) { console.error('FAIL', received); process.exit(1) }
console.log('Smoke test OK:', received)
"
```

Atteso: `Smoke test OK: [ { ok: true } ]`.

**10. Verifica `dist/index.d.ts` (PKG-04):**

```bash
test -f packages/core/dist/index.d.ts
head -20 packages/core/dist/index.d.ts
```

Atteso: file esiste; contiene `export declare class Broker` o simili type declarations.

**11. Riepilogo phase gate finale:**

```bash
pnpm --filter @sembridge/core test \
  && pnpm typecheck \
  && pnpm biome check . \
  && pnpm --filter @sembridge/core build \
  && pnpm ci:publint \
  && pnpm ci:size
```

Atteso: tutti exit 0. Se uno fallisce, identificare e fixare prima di chiudere la fase.
  </action>
  <verify>
    <automated>rm -rf packages/core/dist && pnpm --filter @sembridge/core build && test -f packages/core/dist/index.js && test -f packages/core/dist/index.d.ts && pnpm --filter @sembridge/core typecheck && pnpm biome check . && pnpm --filter @sembridge/core test 2>&1 | grep -qE "Test Files.*[0-9]+.*passed" && pnpm ci:publint && pnpm ci:size && cd packages/core && node -e "import('./dist/index.js').then(m => { const keys = Object.keys(m).sort(); if (!keys.includes('Broker') || !keys.includes('createBroker') || !keys.includes('createBrokerError') || !keys.includes('isBrokerError')) { console.error('MISSING'); process.exit(1) } })"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sembridge/core build` esce 0 dopo clean — produce `dist/index.js`, `dist/index.d.ts`, `dist/index.js.map`
    - `pnpm typecheck` workspace-wide esce 0
    - `pnpm biome check .` esce 0
    - `pnpm --filter @sembridge/core test` esce 0 con `Test Files >= 24 passed`
    - `pnpm ci:publint` esce 0
    - `pnpm ci:attw` esce 0 (almeno per ESM resolution; CJS check può essere skippato per ESM-only package)
    - `pnpm ci:size` esce 0 (bundle <= 8 KB gzip)
    - Smoke import di `dist/index.js` espone `Broker`, `createBroker`, `createBrokerError`, `isBrokerError`
    - End-to-end smoke (publish + subscribe da bundle) ritorna risultato corretto
    - File `packages/core/dist/index.d.ts` esiste (PKG-04 verified)
  </acceptance_criteria>
  <done>Build verification completa. Tutti i CI gates verdi. Phase 1 pronta per `/gsd-verify-work`. Fine plan 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build → npm tarball | Output `dist/` è ciò che vedono i consumer; CI gates assicurano correttezza |
| Documentazione → consumer expectations | README definisce il contratto pubblico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | publint identifica `package.json#exports` malformed | mitigate | `pnpm ci:publint` come CI gate; fail-fast |
| T-11-02 | Tampering | `.d.ts` resolution rotta per consumer node16 | mitigate | `pnpm ci:attw` come CI gate; ESM resolution corretta verificata |
| T-11-03 | DoS | Bundle ingrassa oltre 8 KB → adoption frizionata | mitigate | `pnpm ci:size` budget 8 KB gzip; fail-fast |
| T-11-04 | Information disclosure | Sourcemap include path locali developer | accept | Trade-off DX vs privacy; sourcemap generated by tsup default; consumer può strippare in production build |
| T-11-05 | Tampering | README documenta API non implementata | mitigate | JSDoc accanto al codice + smoke test end-to-end verifica le API documentate |
| T-11-06 | Tampering | npm publish accidentale di package privato | mitigate | `"private": true` su 7 placeholder; `@sembridge/core` ha `provenance: true` ma `version: "0.0.0"` (no actual publish in F1, solo dist artifact locale) |
</threat_model>

<verification>
- `packages/core/README.md` >= 80 righe, documenta tutti i success criteria F1
- JSDoc su tutti i public exports
- `pnpm --filter @sembridge/core build` esce 0
- `dist/index.js` e `dist/index.d.ts` esistono
- `pnpm typecheck` workspace esce 0
- `pnpm biome check .` esce 0
- `pnpm --filter @sembridge/core test` esce 0 con tutte le 24+ suite
- `pnpm ci:publint` esce 0
- `pnpm ci:attw` esce 0 (ESM resolution OK)
- `pnpm ci:size` esce 0 (<=8 KB gz)
- Smoke import + end-to-end smoke usage entrambi pass
</verification>

<success_criteria>
- DOC-01 ✓ (README skeleton completo + JSDoc su public exports)
- PKG-04 ✓ (`.d.ts` generate per API pubblica, verificato da attw)
- Tutti i CI gates verdi: publint + attw + size-limit
- Build artifact pronto per release (anche se NO publish in F1 — RESEARCH Open Q 4)
- Coverage v8 finale ≥ 90% sull'intero `src/` (eccetto `index.ts` che è solo re-export)
- Phase 1 pronta per `/gsd-verify-work` e per inizio Phase 2 (canonical model in `@sembridge/mapper`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-11-SUMMARY.md` con:
- README.md espanso (riga count effettivo)
- JSDoc presenti su tutti i public exports
- Output di tutti i CI gates (publint, attw, size-limit, build, test, typecheck, biome)
- Bundle size finale (gzip)
- Coverage v8 finale
- Decisioni:
  - NO npm publish in F1 (RESEARCH Open Q 4 — primo release fine F2)
  - JSDoc minimal ma sufficiente per TypeDoc generation in F6
- Phase 1 COMPLETA. Pronto-per: `/gsd-verify-work 1` e successivamente `/gsd-discuss-phase 2` (Canonical Model & Mapper).
</output>
