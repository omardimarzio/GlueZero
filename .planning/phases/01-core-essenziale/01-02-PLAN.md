---
phase: 01-core-essenziale
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/tsup.config.ts
  - packages/core/vitest.config.ts
  - packages/core/README.md
  - packages/core/src/index.ts
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
tags:
  - core-package
  - build-config
  - test-config
must_haves:
  truths:
    - "`pnpm --filter @sembridge/core build` produce `dist/index.js` (ESM) e `dist/index.d.ts` (declaration) senza errori"
    - "`pnpm --filter @sembridge/core typecheck` esce 0 (anche se index.ts è quasi vuoto)"
    - "`pnpm --filter @sembridge/core test` esce 0 con output `no test files found` (nessun test ancora — atteso)"
    - "Le dipendenze runtime `nanoid@5.1.9` e `valibot@1.3.1` sono installate in `packages/core/node_modules` (o hoisted) e dichiarate in `packages/core/package.json#dependencies`"
    - "`packages/core/package.json#exports` punta a `./dist/index.js` con `types` PRIMA di `import`"
  artifacts:
    - path: "packages/core/package.json"
      provides: "Package config @sembridge/core con runtime deps lockate, scripts build/test/typecheck, exports field, size-limit budget 8 KB gz"
      contains: "@sembridge/core"
    - path: "packages/core/tsconfig.json"
      provides: "TS config che extends tsconfig.base.json con outDir/rootDir per `@sembridge/core`"
      contains: "../../tsconfig.base.json"
    - path: "packages/core/tsup.config.ts"
      provides: "Build config ESM-only, dts true, target es2022, platform browser"
      contains: "format: ['esm']"
    - path: "packages/core/vitest.config.ts"
      provides: "Test runner config con jsdom environment, coverage v8 90/85/90/90"
      contains: "environment: 'jsdom'"
    - path: "packages/core/README.md"
      provides: "Skeleton DOC-01 + API surface placeholder"
      contains: "@sembridge/core"
    - path: "packages/core/src/index.ts"
      provides: "Public entry point — vuoto in F1 wave 1, popolato da plan 08"
      min_lines: 1
  key_links:
    - from: "packages/core/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig\\.base\\.json"
    - from: "packages/core/package.json"
      to: "packages/core/tsup.config.ts"
      via: "scripts.build invoca tsup"
      pattern: "\"build\": \"tsup\""
    - from: "packages/core/package.json"
      to: "packages/core/vitest.config.ts"
      via: "scripts.test invoca vitest run"
      pattern: "\"test\": \"vitest run\""
---

<objective>
Configurare il pacchetto `@sembridge/core` come UNICO sub-package con codice attivo in F1: package.json con runtime deps (`nanoid@5.1.9`, `valibot@1.3.1`) e devDeps locali (`tsup@8.5.1`, `typescript@6.0.3`, `vitest@4.1.5`, `jsdom@29.1.0`), tsconfig che extends il base, tsup config ESM-only, vitest config con jsdom environment + coverage thresholds, README skeleton DOC-01, e `src/index.ts` placeholder vuoto.

Purpose: avere un build pipeline funzionante (build + test + typecheck) PRIMA di scrivere codice TypeScript. I plan 03-08 popoleranno `src/types/`, `src/core/`, `src/test-utils/` aderendo a questa configurazione.

Output: `pnpm --filter @sembridge/core build` produce `dist/index.js` + `dist/index.d.ts`. `pnpm --filter @sembridge/core test` esce 0 (no tests yet, atteso). Tutte le 5 verifiche packaging (PKG-01 ESM, PKG-02 TS source, PKG-03 ES2022 target, PKG-04 .d.ts) sono in atto come baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-01-SUMMARY.md
@CLAUDE.md
@package.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare package.json + tsconfig + tsup config + README per @sembridge/core</name>
  <files>packages/core/package.json, packages/core/tsconfig.json, packages/core/tsup.config.ts, packages/core/README.md</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Config files" sotto-sezioni `packages/core/package.json`, `packages/core/tsconfig.json`, `packages/core/tsup.config.ts`
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Pitfall mitigation table" §13 (Build / distribution) — ESM-only V1 rationale
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-15
    - tsconfig.base.json (creato in plan 01)
  </read_first>
  <action>
**1. Creare `packages/core/package.json` (verbatim da RESEARCH.md):**

```json
{
  "name": "@sembridge/core",
  "version": "0.0.0",
  "description": "Core event broker (pub/sub, plugin registry, lifecycle, BrokerEvent) for SemBridge",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "nanoid": "5.1.9",
    "valibot": "1.3.1"
  },
  "devDependencies": {
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5",
    "jsdom": "29.1.0"
  },
  "size-limit": [
    {
      "name": "@sembridge/core (gzip)",
      "path": "dist/index.js",
      "limit": "8 KB",
      "gzip": true
    }
  ]
}
```

CRITICO (RESEARCH.md §"Note critiche"):
- `"type": "module"` — package ESM puro
- `"sideEffects": false` — abilita tree-shaking aggressivo dal consumer
- `exports` con `types` PRIMA di `import` (ordine richiesto da TS resolver per `node16`/`bundler`)
- `publishConfig.provenance: true` — npm provenance attestations (sicurezza supply chain)
- `size-limit` budget 8 KB gz allineato a STACK.md

**2. Creare `packages/core/tsconfig.json` (verbatim da RESEARCH.md):**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "**/*.test.ts", "**/*.spec.ts"]
}
```

NOTA: non usare `references` in F1 (RESEARCH.md "Nota: non usiamo `references` in F1").

**3. Creare `packages/core/tsup.config.ts` (verbatim da RESEARCH.md):**

```ts
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  sourcemap: true,
  clean: true,
  treeshake: true,
  splitting: false,
  minify: false,
  target: 'es2022',
  platform: 'browser',
  external: [/^node:/],
  banner: {
    js: '/* @sembridge/core — MIT — https://github.com/<TBD>/sembridge */',
  },
})
```

CRITICO: `format: ['esm']` solo (NO CJS in V1 — dual-package hazard letale per broker con state singleton, RESEARCH.md "Decisione su CJS dual-package").

**4. Creare `packages/core/README.md` (skeleton DOC-01):**

```md
# @sembridge/core

> Core event broker (pub/sub, plugin registry, lifecycle, BrokerEvent) for SemBridge.

ESM-only TypeScript browser library. Browser evergreen target (ES2022). Zero runtime dependencies beyond `nanoid` (ID generation) and `valibot` (schema validation).

## Installazione

```sh
pnpm add @sembridge/core
```

## API pubblica (skeleton)

L'API completa viene definita nei plan 03-08 di Phase 1. Surface attesa:

- `createBroker(config: BrokerConfig): Broker` — factory imperativa (D-19, D-30)
- `Broker` — istanza con `publish`, `subscribe`, `registerPlugin`, `unregisterPlugin`, `getTopicRegistry`, `getDebugSnapshot`, `enableDebug`, `disableDebug`, `setLogger`
- `BrokerEvent`, `EventSource`, `Subscription`, `PluginDescriptor`, `BrokerError`, `BrokerLogger`, `EventTap`, `PipelineStep`, `PipelineSnapshot`, `BrokerConfig`, `LogLevel`, `DeliveryMode`, `Priority`, `EventId`, `DeepReadonly`, `ErrorCategory`, `PluginContext`, `PluginState` — public types
- `createBrokerError`, `isBrokerError` — error utilities

## Vincolo architetturale

`EventTap` interface viene instrumentata già in F1 con implementazione no-op; F6 sostituirà no-op con Inspector reali senza retrofit. Vedi `.planning/research/SUMMARY.md` "Vincolo critico architetturale".

## Stato Phase 1

Plan 01: monorepo bootstrap ✅ (plan 01-01)
Plan 02: package config ⏳ (corrente)
Plan 03-08: types + core modules + EventBus + PluginRegistry + Broker (TBD)
Plan 09-10: integration + robustness tests (TBD)
Plan 11: build verification + DOC-01 finalizzato (TBD)

## Riferimenti

- `prd.md` (root) sezioni §10, §11, §12, §15, §16, §22, §24, §25.4, §27, §28, §31, §33.2, §39, §42
- `.planning/phases/01-core-essenziale/01-CONTEXT.md` — 30 decisioni locked (D-01..D-30)
- `.planning/phases/01-core-essenziale/01-RESEARCH.md` — implementation patterns

## Licenza

MIT
```
  </action>
  <verify>
    <automated>test -f packages/core/package.json && test -f packages/core/tsconfig.json && test -f packages/core/tsup.config.ts && test -f packages/core/README.md && grep -q "\"@sembridge/core\"" packages/core/package.json && grep -q "\"sideEffects\": false" packages/core/package.json && grep -q "\"nanoid\": \"5.1.9\"" packages/core/package.json && grep -q "\"valibot\": \"1.3.1\"" packages/core/package.json && grep -q "\"provenance\": true" packages/core/package.json && grep -q "extends.*\\.\\./\\.\\./tsconfig\\.base\\.json" packages/core/tsconfig.json && grep -q "format: \\['esm'\\]" packages/core/tsup.config.ts && grep -q "platform: 'browser'" packages/core/tsup.config.ts && grep -q "dts: true" packages/core/tsup.config.ts && node -e "JSON.parse(require('fs').readFileSync('packages/core/package.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/package.json` esiste e contiene `"name": "@sembridge/core"`, `"type": "module"`, `"sideEffects": false`
    - `packages/core/package.json` contiene runtime deps `"nanoid": "5.1.9"` e `"valibot": "1.3.1"` in `dependencies`
    - `packages/core/package.json` contiene devDeps `"tsup": "8.5.1"`, `"typescript": "6.0.3"`, `"vitest": "4.1.5"`, `"jsdom": "29.1.0"`
    - `packages/core/package.json` ha `exports.["."]` con `types` (chiave) PRIMA di `import` (chiave) — ordine verificabile guardando l'oggetto JSON
    - `packages/core/package.json` ha `publishConfig.provenance: true`
    - `packages/core/package.json` ha `scripts.build: "tsup"`, `scripts.test: "vitest run"`, `scripts.typecheck: "tsc --noEmit"`
    - `packages/core/package.json` ha `size-limit` con budget `8 KB` gzip
    - `packages/core/tsconfig.json` extends `../../tsconfig.base.json`
    - `packages/core/tsconfig.json` ha `outDir: "./dist"` e `rootDir: "./src"`
    - `packages/core/tsup.config.ts` ha `format: ['esm']`, `dts: true`, `target: 'es2022'`, `platform: 'browser'`, `splitting: false`, `treeshake: true`, `clean: true`
    - `packages/core/README.md` esiste, contiene `# @sembridge/core`, menziona `EventTap` come vincolo architetturale F1, e elenca l'API surface skeleton
    - Tutti i JSON parsano senza errori (`node -e "JSON.parse(...)"` esce 0)
  </acceptance_criteria>
  <done>Package config files creati. Pronto per `pnpm install` ricorsivo (Task 2) e creazione `vitest.config.ts` + `src/index.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: vitest.config.ts + src/index.ts placeholder + install runtime deps</name>
  <files>packages/core/vitest.config.ts, packages/core/src/index.ts, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Config files" sotto-sezione `vitest.config.ts per @sembridge/core`
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Test patterns" tabella livelli + Strategia 3-livelli
    - packages/core/package.json (creato in Task 1)
  </read_first>
  <action>
**1. Creare `packages/core/vitest.config.ts` (verbatim da RESEARCH.md):**

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: '@sembridge/core',
    environment: 'jsdom',
    globals: false,
    include: ['src/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/index.ts'],
      thresholds: {
        statements: 90,
        branches: 85,
        functions: 90,
        lines: 90,
      },
    },
    typecheck: {
      enabled: false,
    },
  },
})
```

Decisioni lockate (RESEARCH.md):
- `environment: 'jsdom'` — browser-like DOM per integration mid-level (3-livelli strategy)
- `globals: false` — `import { describe, it, expect } from 'vitest'` esplicito (DX migliore in libreria pubblicata)
- `coverage.provider: 'v8'` — più veloce di istanbul
- Soglie 90/85/90/90 — sane default

**2. Creare `packages/core/src/index.ts` placeholder vuoto:**

```ts
// @sembridge/core public API surface.
// Phase 1 Plan 02 (Wave 1) — placeholder. Plans 03-08 popolano types e moduli core,
// Plan 08 espone l'API pubblica completa (createBroker, Broker, types, errori, ecc.).

export {}
```

NOTA: deve essere un modulo ESM valido. `export {}` lo dichiara come modulo (non script globale). Plan 08 sostituisce questo file con i re-export pubblici.

**3. Eseguire `pnpm install` per risolvere e installare le runtime deps `@sembridge/core`:**

```bash
pnpm install
```

Atteso: `pnpm-lock.yaml` aggiornato con `nanoid@5.1.9` e `valibot@1.3.1`; `node_modules/.pnpm/nanoid@5.1.9` e `valibot@1.3.1` (hoisted o nested) presenti.

**4. Verifica build + typecheck + test:**

```bash
# Build deve produrre dist/index.js e dist/index.d.ts (anche se index.ts è quasi vuoto)
pnpm --filter @sembridge/core build

# Typecheck workspace-aware
pnpm --filter @sembridge/core typecheck

# Test deve uscire 0 con "no test files found" (atteso in wave 1)
pnpm --filter @sembridge/core test
```

NOTA: `vitest run` con `include: ['src/**/*.test.ts']` e nessun file `.test.ts` ESCE 0 con messaggio "no test files found, exiting with code 0" (Vitest 4 default). Se invece esce con error, controllare il config.

**5. Smoke test del bundle:**

```bash
# Verifica che dist/index.js sia valido ESM importabile
cd packages/core && node -e "import('./dist/index.js').then(m => console.log('exports:', Object.keys(m)))"
```

Atteso: `exports: []` (placeholder vuoto, ma il modulo si importa senza errori).
  </action>
  <verify>
    <automated>test -f packages/core/vitest.config.ts && test -f packages/core/src/index.ts && grep -q "environment: 'jsdom'" packages/core/vitest.config.ts && grep -q "statements: 90" packages/core/vitest.config.ts && grep -q "export {}" packages/core/src/index.ts && pnpm --filter @sembridge/core build && test -f packages/core/dist/index.js && test -f packages/core/dist/index.d.ts && pnpm --filter @sembridge/core typecheck && pnpm --filter @sembridge/core test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/vitest.config.ts` esiste e contiene `environment: 'jsdom'`, `globals: false`, `provider: 'v8'`, `statements: 90`, `branches: 85`
    - `packages/core/src/index.ts` esiste e contiene `export {}` (modulo ESM valido placeholder)
    - `pnpm install` ha aggiornato `pnpm-lock.yaml` con `nanoid@5.1.9` e `valibot@1.3.1`
    - Comando `pnpm --filter @sembridge/core build` esce 0
    - File `packages/core/dist/index.js` esiste post-build
    - File `packages/core/dist/index.d.ts` esiste post-build (PKG-04 baseline)
    - Comando `pnpm --filter @sembridge/core typecheck` esce 0
    - Comando `pnpm --filter @sembridge/core test` esce 0 (con "no test files found" — atteso)
    - Comando `cd packages/core && node -e "import('./dist/index.js').then(m => console.log(Object.keys(m)))"` esce 0 e stampa `[]`
  </acceptance_criteria>
  <done>Build pipeline `@sembridge/core` operativa: build (tsup) + test (vitest) + typecheck (tsc) tutti verdi. Runtime deps installate. Pronto per Plan 03 (types).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → npm tarball | Output `dist/` è quello pubblicato; configurazione `tsup` controlla cosa entra |
| Consumer → @sembridge/core | Downstream consuma `package.json#exports`; surface non controllata può esporre internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Dual-package hazard (ESM+CJS state divergence) | mitigate | `format: ['esm']` solo in tsup.config.ts; nessun CJS output (RESEARCH.md "Decisione su CJS dual-package") |
| T-02-02 | Information disclosure | `dist/` espone file non destinati al pubblico | mitigate | `files: ["dist", "README.md", "LICENSE"]` in package.json; `tsup entry: ['src/index.ts']` solo (no glob) — solo ciò che esce da index.ts entra nel bundle |
| T-02-03 | Tampering | Bundle ingrassa con polyfill Node | mitigate | `external: [/^node:/]` in tsup.config.ts; `platform: 'browser'` |
| T-02-04 | Tampering | Versione runtime dep diversa al consumer | mitigate | `save-exact=true` in .npmrc + lockfile committato; `nanoid: "5.1.9"` esatto (no caret/tilde) |
| T-02-05 | Information disclosure | TypeScript declarations leak `any` | mitigate | `noExplicitAny: error` in biome.json (plan 01) + tsconfig strict completo |
| T-02-06 | DoS | Coverage threshold troppo alto blocca CI | accept | 90/85/90/90 sane default; rilassabile in plan 09/10 se serve |
</threat_model>

<verification>
- `pnpm --filter @sembridge/core build` esce 0
- `pnpm --filter @sembridge/core typecheck` esce 0
- `pnpm --filter @sembridge/core test` esce 0 (no tests yet)
- `packages/core/dist/index.js` e `packages/core/dist/index.d.ts` esistono post-build
- `packages/core/package.json` contiene runtime deps esatte (`nanoid@5.1.9`, `valibot@1.3.1`) e size-limit budget
- `packages/core/tsconfig.json` extends il base config
- `packages/core/vitest.config.ts` ha `environment: 'jsdom'`
- `packages/core/src/index.ts` è un modulo ESM valido (`export {}`)
- Smoke import di `dist/index.js` funziona da Node 20+
</verification>

<success_criteria>
- Package `@sembridge/core` configurato con tutti i tooling per-package: tsup (build ESM+dts), vitest (test jsdom), tsconfig (extends base), package.json (deps + scripts + exports + size-limit)
- Runtime deps `nanoid@5.1.9` e `valibot@1.3.1` installate
- Pipeline build+test+typecheck verde con `index.ts` placeholder
- README.md skeleton (DOC-01 baseline) presente
- Pronto per Plan 03: definizione dei tipi pubblici in `src/types/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-02-SUMMARY.md` con:
- Output di `pnpm --filter @sembridge/core build` (deve mostrare ESM + DTS generati)
- Path dei file dist generati
- Versioni runtime deps installate (verificabili in pnpm-lock.yaml)
- Pronto-per: Plan 03 (public types in `src/types/`)
</output>
