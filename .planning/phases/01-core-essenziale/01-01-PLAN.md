---
phase: 01-core-essenziale
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - .gitignore
  - tsconfig.base.json
  - biome.json
  - .changeset/config.json
  - packages/mapper/package.json
  - packages/mapper/README.md
  - packages/gateway/package.json
  - packages/gateway/README.md
  - packages/routing/package.json
  - packages/routing/README.md
  - packages/worker/package.json
  - packages/worker/README.md
  - packages/cache/package.json
  - packages/cache/README.md
  - packages/devtools/package.json
  - packages/devtools/README.md
  - packages/gluezero/package.json
  - packages/gluezero/README.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
tags:
  - monorepo
  - bootstrap
  - tooling
must_haves:
  truths:
    - "`corepack enable && corepack prepare pnpm@10.33.2 --activate` attivano pnpm 10.33.2 senza install globale"
    - "`pnpm install` dalla root completa senza errori e senza warning bloccanti"
    - "`pnpm typecheck` non riporta errori (workspace-wide, anche se i sub-package sono vuoti)"
    - "`pnpm biome check .` esce 0"
    - "I 7 sotto-pacchetti `@gluezero/{core,mapper,gateway,routing,worker,cache,devtools}` + aggregato `@gluezero/gluezero` esistono come placeholder con `package.json` privati e README"
  artifacts:
    - path: "package.json"
      provides: "Root workspace orchestrator con devDeps lockate (TypeScript 6.0.3, tsup 8.5.1, Vitest 4.1.5, Biome 2.4.13, ecc.) e scripts build/test/lint/typecheck"
      contains: "\"name\": \"gluezero-monorepo\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace declaration `packages/*`"
      contains: "packages/*"
    - path: "tsconfig.base.json"
      provides: "TS strict config target ES2022 + isolatedDeclarations + verbatimModuleSyntax"
      contains: "isolatedDeclarations"
    - path: "biome.json"
      provides: "Biome 2.4.13 config con schema URL aggiornato + linter rules (noExplicitAny error, useImportType error)"
      contains: "biomejs.dev/schemas/2.4.13/schema.json"
    - path: ".changeset/config.json"
      provides: "Changesets config con ignore list per i 7 placeholder"
      contains: "@changesets/config"
    - path: "packages/mapper/package.json"
      provides: "Placeholder package privato per F2"
      contains: "@gluezero/mapper"
    - path: "packages/gluezero/package.json"
      provides: "Placeholder package aggregato per release pubblico"
      contains: "@gluezero/gluezero"
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "packageManager + workspaces protocol"
      pattern: "packageManager.*pnpm@10\\.33\\.2"
    - from: "tsconfig.base.json"
      to: "packages/*/tsconfig.json"
      via: "extends ../../tsconfig.base.json (predisposto, applicato in plan 02)"
      pattern: "isolatedDeclarations.*true"
---

<objective>
Bootstrap del monorepo `pnpm` workspaces con i 7 sotto-pacchetti `@gluezero/{core,mapper,gateway,routing,worker,cache,devtools}` + aggregato `@gluezero/gluezero`. Tooling root completo: TypeScript 6.0.3 (strict + isolatedDeclarations), Biome 2.4.13 (lint + format), Changesets 2.31.0 (versioning), pnpm 10.33.2 via corepack. In F1 solo `@gluezero/core` riceverà codice (plan 02+); gli altri 7 restano placeholder privati con `package.json` minimale + README.

Purpose: stabilire il fondamento di build/lint/test workspace-wide prima che qualunque codice TypeScript venga scritto. Conformità a D-15 (sub-package layout monorepo) e a CORE-14 + PKG-01..PKG-03.

Output: workspace pnpm pronto per `pnpm install`, `pnpm typecheck`, `pnpm lint` senza errori; 8 directory di package create; tooling config files lockati su versioni esatte verificate live in RESEARCH.md §Stack versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap pnpm + monorepo root files</name>
  <files>package.json, pnpm-workspace.yaml, .npmrc, .gitignore, tsconfig.base.json</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Setup commands (scaffolding monorepo)" steps 1-6 e sezione "Config files" sotto-sezioni `tsconfig.base.json`
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-15 (sub-package layout)
    - CLAUDE.md sezione "Stack raccomandato" + "Vincoli architetturali"
  </read_first>
  <action>
Eseguire i comandi shell e creare i file root nell'ordine seguente:

**1. Attivazione pnpm via corepack (NON installare pnpm globalmente):**

```bash
corepack enable
corepack prepare pnpm@10.33.2 --activate
pnpm --version  # deve stampare 10.33.2
```

**2. Creare `package.json` root (workspace orchestrator, NON publicabile):**

Contenuto esatto (copiato verbatim da RESEARCH.md §Setup commands step 2):

```json
{
  "name": "gluezero-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "test": "pnpm -r --filter='./packages/*' run test",
    "test:watch": "pnpm -r --filter='./packages/*' run test:watch",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r --filter='./packages/*' run typecheck",
    "ci:publint": "pnpm -r --filter='./packages/*' exec publint",
    "ci:attw": "pnpm -r --filter='./packages/*' exec attw --pack",
    "ci:size": "size-limit",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "@biomejs/biome": "2.4.13",
    "@changesets/cli": "2.31.0",
    "vitest": "4.1.5",
    "@vitest/browser": "4.1.5",
    "playwright": "1.59.1",
    "jsdom": "29.1.0",
    "happy-dom": "20.9.0",
    "msw": "2.13.6",
    "tsup": "8.5.1",
    "typedoc": "0.28.19",
    "publint": "0.3.18",
    "@arethetypeswrong/cli": "0.18.2",
    "size-limit": "12.1.0",
    "@size-limit/preset-small-lib": "12.1.0"
  }
}
```

**3. Creare `pnpm-workspace.yaml`:**

```yaml
packages:
  - 'packages/*'
```

**4. Creare `.npmrc`:**

```
auto-install-peers=true
strict-peer-dependencies=false
prefer-workspace-packages=true
shared-workspace-lockfile=true
save-exact=true
```

**5. Creare `.gitignore` (verbatim da RESEARCH.md §Setup commands step 5):**

```
# deps
node_modules
.pnpm-store

# build output
dist
*.tsbuildinfo

# test
coverage
.vitest

# tooling
.turbo
.cache

# editor
.vscode
.idea
.DS_Store

# OS
Thumbs.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*
```

**6. Creare `tsconfig.base.json` (verbatim da RESEARCH.md §Config files):**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true,
    "noPropertyAccessFromIndexSignature": true,

    "isolatedModules": true,
    "isolatedDeclarations": true,
    "verbatimModuleSyntax": true,

    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "useDefineForClassFields": true,
    "resolveJsonModule": true,
    "allowSyntheticDefaultImports": true
  },
  "exclude": ["**/node_modules", "**/dist", "**/*.test.ts", "**/*.spec.ts"]
}
```

Nessuna abbreviazione: tutti i campi sono lockati come da RESEARCH (rationale dei flag in §Config files: `isolatedDeclarations`, `verbatimModuleSyntax`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` sono critici per la libreria).
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f .npmrc && test -f .gitignore && test -f tsconfig.base.json && pnpm --version | grep -q "10.33.2" && grep -q "\"packageManager\": \"pnpm@10.33.2\"" package.json && grep -q "\"typescript\": \"6.0.3\"" package.json && grep -q "isolatedDeclarations" tsconfig.base.json && grep -q "verbatimModuleSyntax" tsconfig.base.json</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` esiste e contiene `"name": "gluezero-monorepo"`, `"private": true`, `"packageManager": "pnpm@10.33.2"`
    - File `package.json` contiene tutte le 16 devDependencies con versioni esatte (typescript 6.0.3, tsup 8.5.1, vitest 4.1.5, @biomejs/biome 2.4.13, @changesets/cli 2.31.0, jsdom 29.1.0, happy-dom 20.9.0, msw 2.13.6, playwright 1.59.1, typedoc 0.28.19, publint 0.3.18, @arethetypeswrong/cli 0.18.2, size-limit 12.1.0, @size-limit/preset-small-lib 12.1.0, @vitest/browser 4.1.5)
    - File `pnpm-workspace.yaml` contiene la riga `- 'packages/*'`
    - File `.npmrc` contiene `auto-install-peers=true` e `save-exact=true`
    - File `.gitignore` esclude `node_modules`, `dist`, `coverage`, `.DS_Store`
    - File `tsconfig.base.json` contiene `"target": "ES2022"`, `"strict": true`, `"isolatedDeclarations": true`, `"verbatimModuleSyntax": true`, `"moduleResolution": "Bundler"`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`
    - Comando `pnpm --version` stampa esattamente `10.33.2`
    - Nessun file ha trailing whitespace o errori JSON syntax (`node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` esce 0)
  </acceptance_criteria>
  <done>Root workspace files creati. pnpm 10.33.2 attivo. Pronto per scaffolding sub-package.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold dei 7 sub-package + aggregato (placeholder privati)</name>
  <files>packages/core/, packages/mapper/package.json, packages/mapper/README.md, packages/gateway/package.json, packages/gateway/README.md, packages/routing/package.json, packages/routing/README.md, packages/worker/package.json, packages/worker/README.md, packages/cache/package.json, packages/cache/README.md, packages/devtools/package.json, packages/devtools/README.md, packages/gluezero/package.json, packages/gluezero/README.md</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Stub `package.json` per gli altri 6 sub-package + aggregato"
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-15
  </read_first>
  <action>
**1. Creare le directory dei package + sub-directory `src` (8 totali):**

```bash
mkdir -p packages/core/src
mkdir -p packages/mapper/src
mkdir -p packages/gateway/src
mkdir -p packages/routing/src
mkdir -p packages/worker/src
mkdir -p packages/cache/src
mkdir -p packages/devtools/src
mkdir -p packages/gluezero/src
```

NOTA: `packages/core/` riceverà package.json + tooling completi nel Plan 02 (next wave). Qui creiamo SOLO la directory + `src/` (vuoto). Non aggiungere `package.json` per `core` in questo plan — è gestito da plan 02.

**2. Creare il `package.json` placeholder per ciascuno dei 6 sub-package non-core (`mapper`, `gateway`, `routing`, `worker`, `cache`, `devtools`).**

Per ogni `<pkg>` in `[mapper, gateway, routing, worker, cache, devtools]`, creare `packages/<pkg>/package.json` con il pattern esatto da RESEARCH.md §Stub package.json. Esempio per `mapper` (sostituire `@gluezero/mapper` e la description per gli altri):

```json
{
  "name": "@gluezero/mapper",
  "version": "0.0.0",
  "description": "Canonical model + bidirectional mapper for GlueZero (Phase 2 — placeholder in Phase 1)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md"],
  "publishConfig": {
    "access": "public"
  }
}
```

Description per ciascuno:
- `mapper`: "Canonical model + bidirectional mapper for GlueZero (Phase 2 — placeholder in Phase 1)"
- `gateway`: "HTTP gateway + realtime adapters for GlueZero (Phase 3/4 — placeholder in Phase 1)"
- `routing`: "Declarative routing engine for GlueZero (Phase 3 — placeholder in Phase 1)"
- `worker`: "Worker registry + pool for GlueZero (Phase 5 — placeholder in Phase 1)"
- `cache`: "In-memory cache adapters for GlueZero (Phase 6 — placeholder in Phase 1)"
- `devtools`: "Event/Mapping/Route Inspector + Metrics for GlueZero (Phase 6 — placeholder in Phase 1)"

CRITICO: tutti hanno `"private": true` per impedire `pnpm changeset publish` accidentale finché non hanno codice (D-15 + RESEARCH.md note critiche).

**3. Creare il `package.json` per il pacchetto aggregato `@gluezero/gluezero`:**

```json
{
  "name": "@gluezero/gluezero",
  "version": "0.0.0",
  "description": "GlueZero aggregate package (re-exports core + mapper + gateway + routing + worker + cache + devtools — placeholder in Phase 1)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md"],
  "publishConfig": {
    "access": "public"
  }
}
```

**4. Creare un `README.md` placeholder per ciascuno dei 7 sub-package (incluso aggregato, escluso core):**

Per ciascuno scrivere un README minimo seguendo questo template (sostituire `<pkg>` e fase di apertura):

```md
# @gluezero/<pkg>

> Placeholder. Sviluppo previsto in Phase <N>. In Phase 1 questo pacchetto esiste solo come scaffold per il monorepo `pnpm workspaces`.

## Stato

- [x] Scaffold (Phase 1)
- [ ] Implementazione (Phase <N>)

## Riferimenti

- `prd.md` (root) sezioni rilevanti per <pkg>
- `.planning/ROADMAP.md` Phase <N>
```

Mappatura fase di apertura per package:
- `mapper` → Phase 2
- `gateway` → Phase 3 (HTTP) / Phase 4 (realtime)
- `routing` → Phase 3
- `worker` → Phase 5
- `cache` → Phase 6
- `devtools` → Phase 6
- `sembridge` → Phase 2 (primo release pubblico aggregato)
  </action>
  <verify>
    <automated>test -d packages/core/src && test -d packages/mapper/src && test -d packages/gateway/src && test -d packages/routing/src && test -d packages/worker/src && test -d packages/cache/src && test -d packages/devtools/src && test -d packages/gluezero/src && for pkg in mapper gateway routing worker cache devtools sembridge; do test -f "packages/$pkg/package.json" || exit 1; test -f "packages/$pkg/README.md" || exit 1; grep -q "\"@gluezero/$pkg\"" "packages/$pkg/package.json" || exit 1; grep -q "\"private\": true" "packages/$pkg/package.json" || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 8 directory esistono: `packages/core/src`, `packages/mapper/src`, `packages/gateway/src`, `packages/routing/src`, `packages/worker/src`, `packages/cache/src`, `packages/devtools/src`, `packages/gluezero/src`
    - 7 file `packages/<pkg>/package.json` esistono per `mapper, gateway, routing, worker, cache, devtools, sembridge`
    - Nessun file `packages/core/package.json` esiste in questo plan (responsabilità di Plan 02)
    - Ogni placeholder `package.json` contiene `"private": true`, `"type": "module"`, `"version": "0.0.0"`, `"name": "@gluezero/<pkg>"`
    - 7 file `packages/<pkg>/README.md` esistono con titolo `# @gluezero/<pkg>` e marker `Placeholder`
    - Comando `for pkg in mapper gateway routing worker cache devtools sembridge; do node -e "JSON.parse(require('fs').readFileSync('packages/$pkg/package.json','utf8'))"; done` esce 0 (JSON valido)
  </acceptance_criteria>
  <done>I 7 sub-package non-core esistono come placeholder privati con package.json + README. La directory `packages/core/` esiste con `src/` vuoto, pronta per Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Configurare Biome 2.4.13, Changesets 2.31.0 e installare devDeps root</name>
  <files>biome.json, .changeset/config.json, .changeset/README.md, pnpm-lock.yaml, node_modules/</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Config files" sotto-sezioni `biome.json` e `.changeset/config.json`
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Setup commands" steps 8, 9, 10
  </read_first>
  <action>
**1. Creare `biome.json` (Biome 2.4.13 — schema URL aggiornato vs Biome 1.x):**

Contenuto esatto (verbatim da RESEARCH.md §Config files §biome.json):

```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.13/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": [
      "**/*.ts",
      "**/*.tsx",
      "**/*.js",
      "**/*.json",
      "!**/dist/**",
      "!**/node_modules/**",
      "!**/coverage/**",
      "!**/*.d.ts"
    ]
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "complexity": { "noForEach": "off" },
      "style": {
        "useImportType": "error",
        "noNonNullAssertion": "warn",
        "useConsistentArrayType": {
          "level": "error",
          "options": { "syntax": "shorthand" }
        }
      },
      "suspicious": { "noExplicitAny": "error" }
    }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "asNeeded",
      "trailingCommas": "all",
      "arrowParentheses": "always"
    }
  },
  "assist": {
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
```

NOTA D-13/RESEARCH: `noExplicitAny: error` è obbligatorio (libreria pubblicata non deve mai esporre `any` in `.d.ts`). `useImportType: error` è coerente con `verbatimModuleSyntax: true` in tsconfig.

**2. Creare `.changeset/` directory + config:**

```bash
mkdir -p .changeset
```

Creare `.changeset/config.json` (verbatim da RESEARCH.md §Config files §.changeset/config.json):

```json
{
  "$schema": "https://unpkg.com/@changesets/config@3.0.5/schema.json",
  "changelog": "@changesets/cli/changelog",
  "commit": false,
  "fixed": [],
  "linked": [],
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": [
    "@gluezero/mapper",
    "@gluezero/gateway",
    "@gluezero/routing",
    "@gluezero/worker",
    "@gluezero/cache",
    "@gluezero/devtools",
    "@gluezero/gluezero"
  ]
}
```

Creare `.changeset/README.md` con il contenuto standard generato da `changeset init`:

```md
# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works with multi-package repos, or single-package repos to help you version and publish your code. You can find the full documentation for it in our repository.

We have a quick list of common questions to get you started engaging with this project in the docs:
https://github.com/changesets/changesets/blob/main/docs/common-questions.md
```

NOTA: NON eseguire `pnpm changeset init` — può sovrascrivere il config con default. Creare i file direttamente.

**3. Installare devDeps root:**

```bash
pnpm install
```

Atteso: lockfile `pnpm-lock.yaml` creato; tutte le 16 devDeps risolte; node_modules popolato. Nessun warning bloccante (peer deps warning OK; deprecation warning OK; warning su provenance OK).

**4. Verifica installazione:**

```bash
# Biome installato e funzionante
pnpm biome --version  # deve stampare 2.4.13

# Changesets installato
pnpm changeset --version  # deve stampare 2.31.0

# Vitest installato (anche se nessun test trovato)
pnpm vitest --version  # deve stampare 4.1.5

# TypeScript installato
pnpm tsc --version  # deve stampare 6.0.3

# Lint passes
pnpm biome check .  # exit 0 — solo file root, nessun TS ancora

# Workspace-wide typecheck (atteso "no test files" o no script trovato — ma niente errori)
pnpm typecheck  # OK con warning "no script found" su placeholder
```

NOTA: `pnpm typecheck` è ricorsivo; i placeholder non hanno `typecheck` script → `pnpm` salta con warning. Atteso, non un errore.
  </action>
  <verify>
    <automated>test -f biome.json && test -f .changeset/config.json && test -f .changeset/README.md && test -f pnpm-lock.yaml && test -d node_modules && grep -q "biomejs.dev/schemas/2.4.13/schema.json" biome.json && grep -q "\"noExplicitAny\": \"error\"" biome.json && grep -q "\"useImportType\": \"error\"" biome.json && grep -q "@gluezero/mapper" .changeset/config.json && pnpm biome --version | grep -q "2.4.13" && pnpm tsc --version | grep -q "6.0.3" && pnpm vitest --version | grep -q "4.1.5" && pnpm biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `biome.json` esiste e contiene `https://biomejs.dev/schemas/2.4.13/schema.json` (NON 1.9.4 — verifica anti-regressione)
    - `biome.json` contiene `"noExplicitAny": "error"`, `"useImportType": "error"`, `"useConsistentArrayType"` con `"shorthand"`
    - `.changeset/config.json` esiste e contiene `@gluezero/mapper`, `@gluezero/gateway`, `@gluezero/routing`, `@gluezero/worker`, `@gluezero/cache`, `@gluezero/devtools`, `@gluezero/gluezero` nella lista `ignore`
    - `.changeset/config.json` contiene `"access": "public"` e `"baseBranch": "main"`
    - `pnpm-lock.yaml` esiste (lockfile generato da pnpm install)
    - Directory `node_modules/` esiste alla root
    - Comando `pnpm biome --version` stampa esattamente `2.4.13`
    - Comando `pnpm tsc --version` stampa una versione che inizia con `6.0.3` (es. `Version 6.0.3`)
    - Comando `pnpm vitest --version` stampa una versione che inizia con `4.1.5`
    - Comando `pnpm changeset --version` stampa una versione che inizia con `2.31.0`
    - Comando `pnpm biome check .` esce 0 (nessun lint error sui file root)
  </acceptance_criteria>
  <done>Tooling root completo. Biome 2.4.13, Changesets 2.31.0, devDeps installate, lint pulito. Pronto per Plan 02 (configurazione `@gluezero/core`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → published artifact | npm tarball generato da `tsup` consumato da downstream developers |
| Local FS → tooling configs | I file root (package.json, tsconfig.base.json, biome.json) sono letti da TS compiler, Biome CLI, pnpm install — input fidato, ma uno schema malformato può degradare il dev loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Supply chain pnpm install | mitigate | `save-exact=true` in `.npmrc` (versioni esatte non range), lockfile `pnpm-lock.yaml` committato in Wave 1 (Plan 02 pre-commit), provenance preparato (`publishConfig.provenance: true` in plan 02) |
| T-01-02 | Tampering | Phantom deps | mitigate | pnpm strict hoisting (default in pnpm 10) impedisce dep fantasma; `prefer-workspace-packages=true` in .npmrc forza workspace resolution prima di npm registry |
| T-01-03 | Information disclosure | Accidental publish dei placeholder | mitigate | `"private": true` su tutti i 7 placeholder + `ignore` list in `.changeset/config.json`; `pnpm changeset publish` rifiuta package privati senza override |
| T-01-04 | DoS | tsconfig misconfiguration → IDE/tsc lento | accept | tsconfig.base.json verificato manualmente; `incremental: false` (no `tsBuildInfo`) per package vuoti accettato |
| T-01-05 | Spoofing | Import path resolution `@gluezero/*` → wrong package | mitigate | `pnpm-workspace.yaml` definisce `packages/*` come unica scope; pnpm hoist-protocol assicura che `@gluezero/foo` risolva al workspace package, non a npm registry |
</threat_model>

<verification>
- `pnpm install` completa senza errori bloccanti
- `pnpm biome check .` esce 0
- `pnpm --version` stampa `10.33.2`
- `pnpm biome --version` stampa `2.4.13`
- `pnpm tsc --version` inizia con `6.0.3`
- `pnpm vitest --version` inizia con `4.1.5`
- 8 directory `packages/<pkg>/` esistono (core, mapper, gateway, routing, worker, cache, devtools, sembridge)
- 7 placeholder package hanno `package.json` privati e README
- `tsconfig.base.json` contiene tutti i flag strict richiesti (isolatedDeclarations, verbatimModuleSyntax, noUncheckedIndexedAccess, exactOptionalPropertyTypes)
- `biome.json` riferisce schema 2.4.13 (non 1.9.4)
- `.changeset/config.json` ignora i 7 placeholder
</verification>

<success_criteria>
- Workspace pnpm bootstrappato con `corepack enable && corepack prepare pnpm@10.33.2 --activate`
- Tutti i 7 sotto-pacchetti scaffoldati come placeholder privati (PKG-01, PKG-02, PKG-03 — packaging foundation)
- Tooling root completo e verificato: TypeScript 6.0.3, tsup 8.5.1, Vitest 4.1.5, Biome 2.4.13, Changesets 2.31.0
- `pnpm install` riuscito; `pnpm biome check .` pulito; `pnpm typecheck` non riporta errori (anche se i package sono vuoti)
- Stato pronto per Plan 02: configurazione del solo `@gluezero/core` con runtime deps + tooling per-package
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-01-SUMMARY.md` con:
- Versioni esatte installate (verificate via `pnpm <tool> --version`)
- Path completo dei file root creati
- Path delle 8 directory di package
- Eventuali warning non-bloccanti incontrati durante `pnpm install`
- Pronto-per: Plan 02 (`@gluezero/core` package config)
</output>
