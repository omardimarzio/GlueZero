---
phase: 01-core-essenziale
plan: 05
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - packages/core/src/core/topic-matcher.ts
  - packages/core/src/core/topic-matcher.test.ts
  - packages/core/src/core/event-factory.ts
  - packages/core/src/core/event-factory.test.ts
  - packages/core/src/core/event-validator.ts
  - packages/core/src/core/event-validator.test.ts
autonomous: true
requirements:
  - CORE-08
  - CORE-09
  - CORE-07
  - VAL-01
  - VAL-06
tags:
  - matcher
  - validation
  - tdd
must_haves:
  truths:
    - "`validateTopic('weather.requested')` non lancia; `validateTopic('Weather.Requested')` lancia BrokerError code='topic.invalid'"
    - "`TopicTrie.match('weather.requested')` ritorna subscribers registrati su `weather.*` e `weather.requested`, ma NON su `weather.alert.*`"
    - "`TopicTrie.match('weather.alert.failed')` matcha pattern `weather.*.failed` (D-11)"
    - "`createBrokerEvent({topic, payload, source})` ritorna BrokerEvent con id nanoid se assente, timestamp Date.now() se assente, deliveryMode 'async' default, priority 'normal' default"
    - "`createBrokerEvent` SENZA source e SENZA defaultSource lancia BrokerError code='event.source.missing'"
    - "`validateEvent(invalidShape)` lancia BrokerError code='event.validation.failed'"
  artifacts:
    - path: "packages/core/src/core/topic-matcher.ts"
      provides: "validateTopic + validateTopicPattern + TopicTrie<T> con insert/remove/match/collectAllPatterns"
      contains: "TopicTrie"
    - path: "packages/core/src/core/event-factory.ts"
      provides: "createBrokerEvent factory + PublishParams interface"
      contains: "createBrokerEvent"
    - path: "packages/core/src/core/event-validator.ts"
      provides: "validateEvent (Valibot schema BrokerEvent shape — VAL-01)"
      contains: "validateEvent"
  key_links:
    - from: "packages/core/src/core/topic-matcher.ts"
      to: "packages/core/src/core/broker-error.ts"
      via: "import { createBrokerError }"
      pattern: "createBrokerError"
    - from: "packages/core/src/core/event-factory.ts"
      to: "nanoid"
      via: "import { nanoid }"
      pattern: "from 'nanoid'"
    - from: "packages/core/src/core/event-validator.ts"
      to: "valibot"
      via: "import * as v from 'valibot'"
      pattern: "from 'valibot'"
---

<objective>
Implementare il batch B delle utility foundation di `@gluezero/core`: topic-matcher (CORE-08 naming validation + CORE-09 wildcard trie con D-08..D-11), event-factory (CORE-07 default id nanoid/timestamp/source obbligatorio), event-validator (VAL-01 Valibot schema BrokerEvent shape). File ownership disgiunta da plan 04 e 06 — eseguibile in parallelo. Coverage: CORE-08, CORE-09, CORE-07, VAL-01, VAL-06.

Output: 3 moduli + 3 file test che passano `pnpm --filter @gluezero/core test`. Test deterministici per la regex CORE-08 (D-24), il trie D-08..D-11 (incluso edge case D-11 `weather.*.failed` matcha `weather.alert.failed`), il default id/timestamp/source-required, e la Valibot validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-03-SUMMARY.md
@packages/core/src/types/broker-event.ts
@packages/core/src/types/error.ts
</context>

<interfaces>
From packages/core/src/types/broker-event.ts (plan 03):
```typescript
export type DeliveryMode = 'sync' | 'async' | 'worker' | 'remote'
export type Priority = 'low' | 'normal' | 'high' | 'critical'
export interface EventSource { readonly type: 'plugin' | 'component' | 'server' | 'worker' | 'system'; readonly id: string; readonly name?: string; readonly version?: string }
export interface BrokerEvent<TPayload = unknown> { readonly id: string; readonly topic: string; readonly timestamp: number; readonly source: EventSource; readonly payload: DeepReadonly<TPayload>; ... }
```

From packages/core/src/core/broker-error.ts (plan 04, parallelo):
```typescript
export function createBrokerError(params: CreateBrokerErrorParams): BrokerError
export function isBrokerError(value: unknown): value is BrokerError
```

NOTA inter-plan: questo plan è parallelo a plan 04. Se al momento del build `broker-error.ts` non esiste ancora, attendere il merge della wave (orchestrator gestisce ordinamento).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: topic-matcher.ts + test (CORE-08, CORE-09, D-08..D-11, D-24)</name>
  <files>packages/core/src/core/topic-matcher.ts, packages/core/src/core/topic-matcher.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/topic-matcher.ts — Trie segmentato (D-08)" (snippet completo)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Test essenziali (TopicTrie)"
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisioni D-08..D-11, D-24
  </read_first>
  <behavior>
    - Test: `validateTopic` accetta `weather.requested`, `weather.requested.success`, `a.b.c.d.e.f` e rifiuta `Weather.Requested`, `weather/requested`, `weather..requested`, `1weather.x`, vuoto, leading/trailing dot
    - Test: `validateTopicPattern` accetta `weather.*`, `*.failed`, `weather.*.failed`, `*`, `form.customer.*`
    - Test: `TopicTrie.match('weather.requested')` ritorna subscribers di `weather.*` E `weather.requested`
    - Test (D-11 critico): `insert('weather.*.failed', X)`, `match('weather.alert.failed')` → [X]
    - Test: `*.failed` matcha `weather.failed`/`auth.failed` ma NON `failed` singleton
    - Test: insertion idempotente (Set semantics)
    - Test: `remove` cleanup nodi vuoti
    - Test: 10000 wildcard subscribers, single match in < 50ms (perf safety margin vs RESEARCH 5ms)
  </behavior>
  <action>
**1. Creare `packages/core/src/core/topic-matcher.test.ts`** con i casi sopra. Schema verbose:

```ts
import { describe, it, expect } from 'vitest'
import { validateTopic, validateTopicPattern, TopicTrie } from './topic-matcher'
import { isBrokerError } from './broker-error'

describe('validateTopic', () => {
  it.each([['weather.requested'], ['weather.requested.success'], ['weather'], ['a.b.c.d.e.f']])(
    'accepts valid topic %s',
    (topic) => { expect(() => validateTopic(topic)).not.toThrow() },
  )
  it.each([['Weather.Requested'], ['weather/requested'], ['weather..requested'], ['1weather.x'], ['weather.'], ['.weather'], ['']])(
    'rejects invalid topic %s',
    (topic) => {
      let caught: unknown = null
      try { validateTopic(topic) } catch (e) { caught = e }
      expect(caught).not.toBeNull()
      expect(isBrokerError(caught)).toBe(true)
      expect((caught as { code: string }).code).toBe('topic.invalid')
    },
  )
})

describe('validateTopicPattern', () => {
  it.each([['weather.*'], ['*.failed'], ['weather.*.failed'], ['weather.requested'], ['*'], ['form.customer.*']])(
    'accepts %s', (p) => { expect(() => validateTopicPattern(p)).not.toThrow() },
  )
  it.each([['Weather.*'], ['weather/*'], ['weather..*']])(
    'rejects %s', (p) => { expect(() => validateTopicPattern(p)).toThrow() },
  )
})

describe('TopicTrie', () => {
  it('matches exact subscribed topic', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.requested', 'A')
    expect(trie.match('weather.requested')).toEqual(['A'])
  })

  it('matches wildcard `weather.*` against `weather.requested` and `weather.loaded`', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.*', 'W')
    expect(trie.match('weather.requested')).toEqual(['W'])
    expect(trie.match('weather.loaded')).toEqual(['W'])
  })

  it('combines exact + wildcard subscribers', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.*', 'W')
    trie.insert('weather.requested', 'E')
    const result = trie.match('weather.requested')
    expect(result).toContain('W')
    expect(result).toContain('E')
    expect(result).toHaveLength(2)
  })

  it('does NOT match wildcard with different segment count', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.alert.*', 'A')
    expect(trie.match('weather.requested')).toEqual([])
  })

  it('matches `weather.*.failed` against `weather.alert.failed` (D-11)', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.*.failed', 'F')
    expect(trie.match('weather.alert.failed')).toEqual(['F'])
    expect(trie.match('weather.danger.failed')).toEqual(['F'])
  })

  it('matches `*.failed` against `weather.failed` and `auth.failed`', () => {
    const trie = new TopicTrie<string>()
    trie.insert('*.failed', 'F')
    expect(trie.match('weather.failed')).toEqual(['F'])
    expect(trie.match('auth.failed')).toEqual(['F'])
  })

  it('does NOT match `*.failed` against single-segment `failed`', () => {
    const trie = new TopicTrie<string>()
    trie.insert('*.failed', 'F')
    expect(trie.match('failed')).toEqual([])
  })

  it('insertion is idempotent (Set semantics)', () => {
    const trie = new TopicTrie<string>()
    trie.insert('x.y', 'A')
    trie.insert('x.y', 'A')
    expect(trie.match('x.y')).toEqual(['A'])
  })

  it('remove returns true on present, false on absent', () => {
    const trie = new TopicTrie<string>()
    trie.insert('a.b', 'A')
    expect(trie.remove('a.b', 'A')).toBe(true)
    expect(trie.remove('a.b', 'A')).toBe(false)
  })

  it('remove cleans empty branches', () => {
    const trie = new TopicTrie<string>()
    trie.insert('a.b.c', 'A')
    trie.remove('a.b.c', 'A')
    expect(trie.collectAllPatterns()).toEqual([])
  })

  it('collectAllPatterns returns subscribed patterns', () => {
    const trie = new TopicTrie<string>()
    trie.insert('weather.*', 'W')
    trie.insert('auth.failed', 'A')
    const patterns = trie.collectAllPatterns()
    expect(patterns).toContain('weather.*')
    expect(patterns).toContain('auth.failed')
  })

  it('performance: 10000 wildcard subscribers, single match < 50ms', () => {
    const trie = new TopicTrie<string>()
    for (let i = 0; i < 10000; i++) trie.insert(`ns${i}.*`, `S${i}`)
    trie.insert('weather.*', 'W')
    const start = performance.now()
    const result = trie.match('weather.requested')
    const elapsed = performance.now() - start
    expect(result).toContain('W')
    expect(elapsed).toBeLessThan(50)
  })
})
```

**2. Creare `packages/core/src/core/topic-matcher.ts`** verbatim da RESEARCH.md sezione "core/topic-matcher.ts — Trie segmentato (D-08)" (regex `^[a-z][a-z0-9]*(\.[a-z][a-z0-9*]*)*$` per validateTopic; regex `^([a-z][a-z0-9]*|\*)(\.([a-z][a-z0-9]*|\*))*$` per validateTopicPattern; TopicTrie con insert/remove/match/collectAllPatterns).

**3. Eseguire `pnpm --filter @gluezero/core test topic-matcher`** — atteso ≥ 11 test groups, tutti passing.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/topic-matcher.ts && test -f packages/core/src/core/topic-matcher.test.ts && grep -q "export function validateTopic" packages/core/src/core/topic-matcher.ts && grep -q "export function validateTopicPattern" packages/core/src/core/topic-matcher.ts && grep -q "export class TopicTrie" packages/core/src/core/topic-matcher.ts && pnpm --filter @gluezero/core test topic-matcher 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/topic-matcher.ts` esporta `validateTopic`, `validateTopicPattern`, `TopicTrie`
    - Regex `validateTopic` rifiuta `Weather.Requested` (lancia BrokerError code='topic.invalid')
    - Regex `validateTopicPattern` accetta `weather.*.failed` (D-11)
    - `TopicTrie.match('weather.alert.failed')` ritorna subscribers di pattern `weather.*.failed` (D-11 esplicito)
    - `TopicTrie.insert` è idempotente (Set semantics, no duplicate)
    - `pnpm --filter @gluezero/core test topic-matcher` esce 0
    - Output del test contiene `Test Files  1 passed`
    - Performance test: 10000 subscriber + match in < 50ms
  </acceptance_criteria>
  <done>topic-matcher module + test creati. CORE-08, CORE-09, D-08..D-11, D-24 coperti.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: event-factory.ts + test (CORE-07, D-21..D-23)</name>
  <files>packages/core/src/core/event-factory.ts, packages/core/src/core/event-factory.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/event-factory.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisioni D-21 (id nanoid), D-22 (timestamp Date.now), D-23 (source obbligatorio)
  </read_first>
  <behavior>
    - Test: `createBrokerEvent({topic, payload, source})` (no id, no timestamp) ritorna evento con id non vuoto e timestamp ≥ Date.now() iniziale
    - Test: il default id ha lunghezza ≥ 21 (nanoid default 21 char URL-safe)
    - Test: due chiamate consecutive hanno id distinti (uniqueness probabilistica)
    - Test: `createBrokerEvent` con `id: 'custom-id'` rispetta l'id custom
    - Test: `createBrokerEvent` con `timestamp: 12345` rispetta il timestamp custom
    - Test: `createBrokerEvent({topic, payload, source})` ritorna `deliveryMode: 'async'` di default (D-01)
    - Test: `createBrokerEvent({topic, payload, source})` ritorna `priority: 'normal'` di default
    - Test: `createBrokerEvent` SENZA source e SENZA defaultSource lancia BrokerError code='event.source.missing'
    - Test: `createBrokerEvent` con defaultSource=`{type:'plugin',id:'p1'}` e SENZA source param usa il defaultSource
    - Test: source param vince su defaultSource quando entrambi presenti
    - Test: campi opzionali (correlationId, causationId, traceId, schemaVersion, ttlMs, dedupeKey, metadata) sono inclusi nel result solo se forniti (no `undefined` esplicito — `'foo' in event === false` se non fornito)
  </behavior>
  <action>
**1. Creare `packages/core/src/core/event-factory.test.ts`:**

```ts
import { describe, it, expect } from 'vitest'
import { createBrokerEvent } from './event-factory'
import { isBrokerError } from './broker-error'
import type { EventSource } from '../types/broker-event'

const SRC: EventSource = { type: 'plugin', id: 'plugin-a' }

describe('createBrokerEvent', () => {
  it('generates a default id (nanoid, length >= 21)', () => {
    const evt = createBrokerEvent({ topic: 'weather.requested', payload: { city: 'Roma' }, source: SRC }, undefined)
    expect(typeof evt.id).toBe('string')
    expect(evt.id.length).toBeGreaterThanOrEqual(21)
  })

  it('generates unique ids across calls', () => {
    const a = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    const b = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    expect(a.id).not.toBe(b.id)
  })

  it('respects custom id when provided', () => {
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC, id: 'custom-id' }, undefined)
    expect(evt.id).toBe('custom-id')
  })

  it('generates default timestamp via Date.now()', () => {
    const before = Date.now()
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    const after = Date.now()
    expect(evt.timestamp).toBeGreaterThanOrEqual(before)
    expect(evt.timestamp).toBeLessThanOrEqual(after)
  })

  it('respects custom timestamp', () => {
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC, timestamp: 12345 }, undefined)
    expect(evt.timestamp).toBe(12345)
  })

  it('default deliveryMode is "async"', () => {
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    expect(evt.deliveryMode).toBe('async')
  })

  it('default priority is "normal"', () => {
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    expect(evt.priority).toBe('normal')
  })

  it('throws BrokerError event.source.missing when no source and no defaultSource', () => {
    let caught: unknown = null
    try {
      createBrokerEvent({ topic: 't.a', payload: {} }, undefined)
    } catch (e) {
      caught = e
    }
    expect(isBrokerError(caught)).toBe(true)
    expect((caught as { code: string }).code).toBe('event.source.missing')
  })

  it('uses defaultSource when source param absent', () => {
    const def: EventSource = { type: 'system', id: 'broker' }
    const evt = createBrokerEvent({ topic: 't.a', payload: {} }, def)
    expect(evt.source).toEqual(def)
  })

  it('source param wins over defaultSource', () => {
    const def: EventSource = { type: 'system', id: 'broker' }
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, def)
    expect(evt.source).toEqual(SRC)
  })

  it('omits optional fields when not provided', () => {
    const evt = createBrokerEvent({ topic: 't.a', payload: {}, source: SRC }, undefined)
    expect('correlationId' in evt).toBe(false)
    expect('causationId' in evt).toBe(false)
    expect('traceId' in evt).toBe(false)
    expect('schemaVersion' in evt).toBe(false)
    expect('ttlMs' in evt).toBe(false)
    expect('dedupeKey' in evt).toBe(false)
    expect('metadata' in evt).toBe(false)
  })

  it('includes optional fields when provided', () => {
    const evt = createBrokerEvent({
      topic: 't.a', payload: {}, source: SRC,
      correlationId: 'c-1', causationId: 'c-0', traceId: 't-1', schemaVersion: '1.0',
      ttlMs: 5000, dedupeKey: 'k-1', metadata: { foo: 'bar' },
    }, undefined)
    expect(evt.correlationId).toBe('c-1')
    expect(evt.causationId).toBe('c-0')
    expect(evt.traceId).toBe('t-1')
    expect(evt.schemaVersion).toBe('1.0')
    expect(evt.ttlMs).toBe(5000)
    expect(evt.dedupeKey).toBe('k-1')
    expect(evt.metadata).toEqual({ foo: 'bar' })
  })
})
```

**2. Creare `packages/core/src/core/event-factory.ts`** verbatim da RESEARCH.md sezione "core/event-factory.ts" (con `nanoid` import + `createBrokerError` import + signature `createBrokerEvent<T>(params, defaultSource)` come da RESEARCH).

**3. Eseguire `pnpm --filter @gluezero/core test event-factory`** — atteso 12 test passing.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/event-factory.ts && test -f packages/core/src/core/event-factory.test.ts && grep -q "export function createBrokerEvent" packages/core/src/core/event-factory.ts && grep -q "from 'nanoid'" packages/core/src/core/event-factory.ts && grep -q "createBrokerError" packages/core/src/core/event-factory.ts && grep -q "event.source.missing" packages/core/src/core/event-factory.ts && pnpm --filter @gluezero/core test event-factory 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/event-factory.ts` esporta `createBrokerEvent` e `PublishParams<T>` interface
    - Import `nanoid` da `'nanoid'`
    - Throw BrokerError code='event.source.missing' se né params.source né defaultSource forniti
    - Default `deliveryMode: 'async'`, `priority: 'normal'` (D-01 + standard)
    - Campi opzionali NON aggiunti come `undefined` se non forniti (`exactOptionalPropertyTypes`)
    - `pnpm --filter @gluezero/core test event-factory` esce 0 con 12 test passing
  </acceptance_criteria>
  <done>event-factory module + test creati. CORE-07 + D-21/D-22/D-23 coperti.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: event-validator.ts + test (VAL-01, VAL-06)</name>
  <files>packages/core/src/core/event-validator.ts, packages/core/src/core/event-validator.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/event-validator.ts (VAL-01 con Valibot)" (snippet completo)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-18
  </read_first>
  <behavior>
    - Test: `validateEvent(validBrokerEvent)` non lancia
    - Test: `validateEvent({})` lancia BrokerError code='event.validation.failed'
    - Test: `validateEvent({id:'', topic:'t.a', timestamp:0, source:{type:'plugin',id:'p'}, payload:{}})` lancia (id min length 1)
    - Test: `validateEvent({id:'x', topic:'Weather.X', ...})` lancia (topic regex fail — uppercase)
    - Test: `validateEvent({id:'x', topic:'t.a', timestamp:-1, source:{...}, payload:{}})` lancia (timestamp deve essere ≥ 0)
    - Test: `validateEvent({id:'x', topic:'t.a', timestamp:0, source:{type:'plugin',id:''}, payload:{}})` lancia (source.id min length 1)
    - Test: `validateEvent({id:'x', topic:'t.a', timestamp:0, source:{type:'invalid',id:'p'}, payload:{}})` lancia (type non in picklist)
    - Test: `validateEvent({id:'x', topic:'t.a', timestamp:0, source:{type:'plugin',id:'p'}, payload:{}, deliveryMode:'invalid'})` lancia (deliveryMode picklist)
    - Test: `validateEvent({id:'x', topic:'t.a', timestamp:0, source:{type:'plugin',id:'p'}, payload:{}, priority:'critical'})` accetta (critical valid)
    - Test: BrokerError lanciato include `details.issues` con array di Valibot issues
  </behavior>
  <action>
**1. Creare `packages/core/src/core/event-validator.test.ts`:**

```ts
import { describe, it, expect } from 'vitest'
import { validateEvent } from './event-validator'
import { isBrokerError } from './broker-error'

const baseValid = {
  id: 'evt-1',
  topic: 'weather.requested',
  timestamp: 1700000000000,
  source: { type: 'plugin', id: 'plugin-a' },
  payload: { city: 'Roma' },
}

describe('validateEvent', () => {
  it('accepts a minimal valid event', () => {
    expect(() => validateEvent(baseValid)).not.toThrow()
  })

  it('accepts event with all optional fields', () => {
    expect(() => validateEvent({
      ...baseValid,
      metadata: { x: 1 },
      correlationId: 'c-1',
      causationId: 'c-0',
      traceId: 't-1',
      schemaVersion: '1.0',
      deliveryMode: 'async',
      priority: 'critical',
      ttlMs: 5000,
      dedupeKey: 'k-1',
    })).not.toThrow()
  })

  it('rejects empty object', () => {
    let caught: unknown = null
    try { validateEvent({}) } catch (e) { caught = e }
    expect(isBrokerError(caught)).toBe(true)
    expect((caught as { code: string }).code).toBe('event.validation.failed')
  })

  it('rejects empty id', () => {
    expect(() => validateEvent({ ...baseValid, id: '' })).toThrow()
  })

  it('rejects invalid topic (uppercase)', () => {
    expect(() => validateEvent({ ...baseValid, topic: 'Weather.X' })).toThrow()
  })

  it('rejects negative timestamp', () => {
    expect(() => validateEvent({ ...baseValid, timestamp: -1 })).toThrow()
  })

  it('rejects empty source.id', () => {
    expect(() => validateEvent({ ...baseValid, source: { type: 'plugin', id: '' } })).toThrow()
  })

  it('rejects invalid source.type', () => {
    expect(() => validateEvent({ ...baseValid, source: { type: 'invalid', id: 'p' } })).toThrow()
  })

  it('rejects invalid deliveryMode', () => {
    expect(() => validateEvent({ ...baseValid, deliveryMode: 'invalid' })).toThrow()
  })

  it('accepts priority "critical"', () => {
    expect(() => validateEvent({ ...baseValid, priority: 'critical' })).not.toThrow()
  })

  it('attached error includes details.issues', () => {
    let caught: unknown = null
    try { validateEvent({}) } catch (e) { caught = e }
    expect(isBrokerError(caught)).toBe(true)
    const details = (caught as { details: { issues: unknown[] } }).details
    expect(Array.isArray(details.issues)).toBe(true)
    expect(details.issues.length).toBeGreaterThan(0)
  })
})
```

**2. Creare `packages/core/src/core/event-validator.ts`** verbatim da RESEARCH.md sezione "core/event-validator.ts (VAL-01 con Valibot)".

CRITICO (RESEARCH): import `createBrokerError` da `./broker-error`. Senza questo, il throw non genera BrokerError. Verificare nel snippet RESEARCH:

```ts
import * as v from 'valibot'
import { createBrokerError } from './broker-error'

const EventSourceSchema = v.object({
  type: v.picklist(['plugin', 'component', 'server', 'worker', 'system']),
  id: v.pipe(v.string(), v.minLength(1)),
  name: v.optional(v.string()),
  version: v.optional(v.string()),
})

const BrokerEventSchema = v.object({
  id: v.pipe(v.string(), v.minLength(1)),
  topic: v.pipe(v.string(), v.regex(/^[a-z][a-z0-9]*(\.[a-z][a-z0-9*]*)*$/)),
  timestamp: v.pipe(v.number(), v.integer(), v.minValue(0)),
  source: EventSourceSchema,
  payload: v.unknown(),
  metadata: v.optional(v.record(v.string(), v.unknown())),
  correlationId: v.optional(v.string()),
  causationId: v.optional(v.string()),
  traceId: v.optional(v.string()),
  schemaVersion: v.optional(v.string()),
  deliveryMode: v.optional(v.picklist(['sync', 'async', 'worker', 'remote'])),
  priority: v.optional(v.picklist(['low', 'normal', 'high', 'critical'])),
  ttlMs: v.optional(v.pipe(v.number(), v.minValue(0))),
  dedupeKey: v.optional(v.string()),
})

export function validateEvent(event: unknown): void {
  const result = v.safeParse(BrokerEventSchema, event)
  if (!result.success) {
    throw createBrokerError({
      code: 'event.validation.failed',
      category: 'validation',
      message: `BrokerEvent validation failed: ${result.issues.map((i) => i.message).join('; ')}`,
      details: { issues: result.issues },
    })
  }
}
```

NOTA: `payload: v.unknown()` perché payload schema è VAL-02 territorio F2.

**3. Eseguire `pnpm --filter @gluezero/core test event-validator`** — atteso 11 test passing.

**4. Eseguire suite completa di Wave 3:**

```bash
pnpm --filter @gluezero/core test
```

Atteso (dopo merge con plan 04 e plan 06): `Test Files 9 passed` (4 da plan 04 + 3 da plan 05 + 2 da plan 06).
  </action>
  <verify>
    <automated>test -f packages/core/src/core/event-validator.ts && test -f packages/core/src/core/event-validator.test.ts && grep -q "export function validateEvent" packages/core/src/core/event-validator.ts && grep -q "from 'valibot'" packages/core/src/core/event-validator.ts && grep -q "createBrokerError" packages/core/src/core/event-validator.ts && grep -q "event.validation.failed" packages/core/src/core/event-validator.ts && pnpm --filter @gluezero/core test event-validator 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/event-validator.ts` esporta `validateEvent(event: unknown): void`
    - Import `* as v from 'valibot'`
    - Schema include picklist per `source.type`, `deliveryMode`, `priority` (con `'critical'`)
    - Throw BrokerError code='event.validation.failed' con `details.issues` come array
    - File `packages/core/src/core/event-validator.test.ts` con almeno 11 test cases
    - `pnpm --filter @gluezero/core test event-validator` esce 0 con tutti i test passing
  </acceptance_criteria>
  <done>event-validator module + test creati. VAL-01 + VAL-06 coperti. Plan 05 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publisher → Broker (publish) | Topic name, source.id, payload shape sono input non fidati |
| Plugin → Broker (subscribe) | Pattern wildcard può essere malformato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Topic injection (es. `weather.../proc/etc`) | mitigate | Regex `validateTopic` D-24 rifiuta caratteri non `[a-z0-9.]`; validateTopicPattern parimenti restrittiva |
| T-05-02 | Spoofing | Plugin spoof `source.id` per impersonare altro plugin | accept | F1 non enforce ownership di source.id (deferred a F2 con plugin-scoped publish); test esistenza source obbligatorio (D-23) |
| T-05-03 | DoS | Wildcard `*` malformato → infinite loop nel match | mitigate | Validate al `insert` (validateTopicPattern), match ricorsivo `O(segments)` (D-09 lookup limitato dal numero di segmenti del topic, non dei subscriber) |
| T-05-04 | Information disclosure | Valibot issues leak internal schema info | accept | `details.issues` esposto al developer; PII non in schema (è meta-info di shape, non payload reali) |
| T-05-05 | DoS | Publish con timestamp futuro estremo (Number.MAX_SAFE) | accept | Schema accetta tutti i numeri ≥ 0; eventuali check semantici delegati a F2 (canonical model) |
</threat_model>

<verification>
- 3 file source + 3 file test esistenti in `packages/core/src/core/`
- `pnpm --filter @gluezero/core test topic-matcher` esce 0
- `pnpm --filter @gluezero/core test event-factory` esce 0
- `pnpm --filter @gluezero/core test event-validator` esce 0
- `pnpm --filter @gluezero/core typecheck` esce 0
- `pnpm biome check packages/core/src/core/topic-matcher.ts packages/core/src/core/event-factory.ts packages/core/src/core/event-validator.ts` esce 0
</verification>

<success_criteria>
- CORE-08 ✓ (regex naming D-24 enforcement)
- CORE-09 ✓ (TopicTrie D-08..D-11 incluso D-11 multi-position wildcard)
- CORE-07 ✓ (id nanoid + timestamp Date.now + source obbligatorio)
- VAL-01 ✓ (Valibot schema BrokerEvent shape)
- VAL-06 ✓ (Valibot tipizzato come scelta locked)
- File ownership disgiunta da plan 04 e 06 — eseguibile in parallelo senza conflitti
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-05-SUMMARY.md` con:
- 3 moduli + 3 test creati
- Output di `pnpm --filter @gluezero/core test` per i 3 nuovi pattern
- Verificato D-11 (`weather.*.failed` matcha `weather.alert.failed`)
- Performance test trie 10000 subscriber documentato
- Pronto-per: completamento Wave 3 (plan 06 parallelo) → Wave 4 (bus.ts in plan 07)
</output>
