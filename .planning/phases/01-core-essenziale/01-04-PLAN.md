---
phase: 01-core-essenziale
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - packages/core/src/core/broker-error.ts
  - packages/core/src/core/broker-error.test.ts
  - packages/core/src/core/deep-freeze.ts
  - packages/core/src/core/deep-freeze.test.ts
  - packages/core/src/core/logger.ts
  - packages/core/src/core/logger.test.ts
  - packages/core/src/core/event-tap.ts
  - packages/core/src/core/event-tap.test.ts
autonomous: true
requirements:
  - ERR-01
  - CORE-10
  - CORE-13
tags:
  - utilities
  - foundation
  - tdd
must_haves:
  truths:
    - "`createBrokerError({code, category, message})` ritorna un Error con tutti i campi BrokerError settati e `Error.cause` se `originalError` fornito"
    - "`isBrokerError(value)` type guard riconosce errori creati da `createBrokerError` e rifiuta Error generici"
    - "`deepFreeze(obj)` propaga `Object.freeze` ricorsivamente, gestisce cycle protection via WeakSet, e rispetta opt-out per Date/Promise/TypedArray"
    - "`createConsoleLogger('debug')` filtra messaggi sotto-livello (silent no-op, error invoca console.error, ecc.) con namespace `[gluezero]`"
    - "`silentLogger` espone i 5 metodi BrokerLogger come no-op (utility per test)"
    - "`noopEventTap` implementa EventTap con `onPipelineStep` no-op (D-20)"
    - "`safeTapStep(tap, step, snapshot)` swallow errori dal tap senza rompere la pipeline (D-20)"
    - "Tutte le suite unit (broker-error.test.ts, deep-freeze.test.ts, logger.test.ts, event-tap.test.ts) passano con 0 errori"
  artifacts:
    - path: "packages/core/src/core/broker-error.ts"
      provides: "createBrokerError factory + isBrokerError type guard"
      contains: "createBrokerError"
    - path: "packages/core/src/core/deep-freeze.ts"
      provides: "deepFreeze recursive con cycle protection + opt-out per Date/Promise/TypedArray/Map/Set"
      contains: "deepFreeze"
    - path: "packages/core/src/core/logger.ts"
      provides: "createConsoleLogger(level) + silentLogger utility"
      contains: "createConsoleLogger"
    - path: "packages/core/src/core/event-tap.ts"
      provides: "noopEventTap + safeTapStep + startStep helper"
      contains: "noopEventTap"
  key_links:
    - from: "packages/core/src/core/broker-error.ts"
      to: "packages/core/src/types/error.ts"
      via: "import type { BrokerError, CreateBrokerErrorParams }"
      pattern: "from '../types/error'"
    - from: "packages/core/src/core/deep-freeze.ts"
      to: "(no external deps)"
      via: "self-contained utility"
      pattern: "WeakSet"
    - from: "packages/core/src/core/logger.ts"
      to: "packages/core/src/types/logger.ts"
      via: "import type { BrokerLogger, LogLevel }"
      pattern: "from '../types/logger'"
    - from: "packages/core/src/core/event-tap.ts"
      to: "packages/core/src/types/tap.ts"
      via: "import type { EventTap, PipelineStep, PipelineSnapshot }"
      pattern: "from '../types/tap'"
---

<objective>
Implementare il batch A delle utility foundation di `@gluezero/core`: error factory (ERR-01), deep-freeze runtime (D-04, D-05), console-based logger (CORE-10, D-12, D-14), e EventTap no-op + helpers (CORE-13, D-20). Ogni modulo ha i suoi test unit co-locati. File ownership disgiunta da plan 05 e 06 → eseguibile in parallelo.

Purpose: questi 4 moduli sono dipendenze utility di `bus.ts` (plan 07). Devono esistere, essere testati, e i loro contratti devono essere stabili PRIMA che bus.ts li componga.

Output: 4 moduli + 4 file test che passano `pnpm --filter @gluezero/core test` per i pattern `broker-error|deep-freeze|logger|event-tap`. Coverage v8 ≥ 90% su questi 4 file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-03-SUMMARY.md
@packages/core/src/types/error.ts
@packages/core/src/types/logger.ts
@packages/core/src/types/tap.ts
@packages/core/vitest.config.ts
</context>

<interfaces>
<!-- Tipi disponibili da plan 03 (creati in packages/core/src/types/) -->
From packages/core/src/types/error.ts:
```typescript
export type ErrorCategory = 'validation' | 'plugin' | 'mapping' | 'route' | 'network' | 'worker' | 'system' | 'config' | 'topic'
export interface BrokerError extends Error {
  readonly code: string
  readonly category: ErrorCategory
  readonly details?: Record<string, unknown>
  readonly originalError?: Error
  readonly routeId?: string
  readonly topic?: string
  readonly eventId?: string
}
export interface CreateBrokerErrorParams { code: string; category: ErrorCategory; message: string; details?: Record<string, unknown>; originalError?: Error; routeId?: string; topic?: string; eventId?: string }
```

From packages/core/src/types/logger.ts:
```typescript
export type LogLevel = 'silent' | 'error' | 'warn' | 'info' | 'debug' | 'trace'
export interface BrokerLogger {
  error(message: string, meta?: Record<string, unknown>): void
  warn(message: string, meta?: Record<string, unknown>): void
  info(message: string, meta?: Record<string, unknown>): void
  debug(message: string, meta?: Record<string, unknown>): void
  trace(message: string, meta?: Record<string, unknown>): void
}
```

From packages/core/src/types/tap.ts:
```typescript
export type PipelineStep = 'event.received' | 'event.metadata.enriched' | 'event.validated' | 'event.dedupe.checked' | 'event.delivered'
export interface PipelineSnapshot {
  readonly eventId: string
  readonly topic: string
  readonly step: PipelineStep
  readonly timestamp: number
  readonly durationMs: number
  readonly payloadBefore?: unknown
  readonly payloadAfter?: unknown
  readonly metadata?: Record<string, unknown>
}
export interface EventTap { onPipelineStep(step: PipelineStep, snapshot: PipelineSnapshot): void }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: broker-error.ts + test (ERR-01)</name>
  <files>packages/core/src/core/broker-error.ts, packages/core/src/core/broker-error.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/broker-error.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "broker-error.test.ts" (test cases)
    - packages/core/src/types/error.ts (creato in plan 03)
  </read_first>
  <behavior>
    - Test 1: `createBrokerError({ code: 'foo.bar', category: 'plugin', message: 'oops' })` ritorna un Error con `name === 'BrokerError'`, `code === 'foo.bar'`, `category === 'plugin'`, `message === 'oops'`
    - Test 2: `createBrokerError` con `details`, `originalError`, `routeId`, `topic`, `eventId` valorizza tutti i campi opzionali
    - Test 3: `createBrokerError` con `originalError` setta `Error.cause` (ES2022) sul valore
    - Test 4: `createBrokerError` SENZA un campo opzionale (es. `details`) NON aggiunge la proprietà all'errore (no `details: undefined` esplicito; verifica con `'details' in err`)
    - Test 5: `isBrokerError(broker_error)` ritorna `true`
    - Test 6: `isBrokerError(new Error('plain'))` ritorna `false`
    - Test 7: `isBrokerError(null)` e `isBrokerError(undefined)` ritornano `false`
    - Test 8: `isBrokerError({code: 'x', category: 'system', message: 'y'})` (oggetto plain non Error) ritorna `false`
  </behavior>
  <action>
**1. Creare `packages/core/src/core/broker-error.test.ts` PRIMA dell'implementazione (TDD RED):**

```ts
import { describe, it, expect } from 'vitest'
import { createBrokerError, isBrokerError } from './broker-error'

describe('createBrokerError', () => {
  it('builds a BrokerError with required fields', () => {
    const err = createBrokerError({ code: 'foo.bar', category: 'plugin', message: 'oops' })
    expect(err).toBeInstanceOf(Error)
    expect(err.name).toBe('BrokerError')
    expect(err.code).toBe('foo.bar')
    expect(err.category).toBe('plugin')
    expect(err.message).toBe('oops')
  })

  it('attaches optional fields when provided', () => {
    const original = new Error('underlying')
    const err = createBrokerError({
      code: 'route.failed',
      category: 'route',
      message: 'route x failed',
      details: { attempt: 2 },
      originalError: original,
      routeId: 'r1',
      topic: 'weather.requested',
      eventId: 'evt-1',
    })
    expect(err.details).toEqual({ attempt: 2 })
    expect(err.originalError).toBe(original)
    expect(err.routeId).toBe('r1')
    expect(err.topic).toBe('weather.requested')
    expect(err.eventId).toBe('evt-1')
  })

  it('sets Error.cause when originalError is provided (ES2022)', () => {
    const original = new Error('underlying')
    const err = createBrokerError({ code: 'x', category: 'system', message: 'y', originalError: original })
    expect((err as Error & { cause?: unknown }).cause).toBe(original)
  })

  it('omits optional fields entirely when not provided (no undefined property)', () => {
    const err = createBrokerError({ code: 'x', category: 'system', message: 'y' })
    expect('details' in err).toBe(false)
    expect('originalError' in err).toBe(false)
    expect('routeId' in err).toBe(false)
    expect('topic' in err).toBe(false)
    expect('eventId' in err).toBe(false)
  })
})

describe('isBrokerError', () => {
  it('returns true for a BrokerError', () => {
    const err = createBrokerError({ code: 'x', category: 'system', message: 'y' })
    expect(isBrokerError(err)).toBe(true)
  })

  it('returns false for a plain Error', () => {
    expect(isBrokerError(new Error('plain'))).toBe(false)
  })

  it('returns false for null/undefined', () => {
    expect(isBrokerError(null)).toBe(false)
    expect(isBrokerError(undefined)).toBe(false)
  })

  it('returns false for a plain object with code+category but not an Error', () => {
    expect(isBrokerError({ code: 'x', category: 'system', message: 'y' })).toBe(false)
  })

  it('returns false for a string', () => {
    expect(isBrokerError('error string')).toBe(false)
  })
})
```

Eseguire `pnpm --filter @gluezero/core test broker-error` — atteso FAIL (modulo non esiste ancora).

**2. Creare `packages/core/src/core/broker-error.ts` (verbatim da RESEARCH.md):**

```ts
import type { BrokerError, CreateBrokerErrorParams } from '../types/error'

export function createBrokerError(params: CreateBrokerErrorParams): BrokerError {
  const err = new Error(params.message) as BrokerError & { -readonly [K in keyof BrokerError]: BrokerError[K] }
  err.name = 'BrokerError'
  err.code = params.code
  err.category = params.category
  if (params.details) err.details = params.details
  if (params.originalError) {
    err.originalError = params.originalError
    ;(err as Error & { cause?: unknown }).cause = params.originalError
  }
  if (params.routeId) err.routeId = params.routeId
  if (params.topic) err.topic = params.topic
  if (params.eventId) err.eventId = params.eventId
  return err
}

export function isBrokerError(value: unknown): value is BrokerError {
  return (
    value instanceof Error &&
    (value as BrokerError).code !== undefined &&
    (value as BrokerError).category !== undefined
  )
}
```

NOTA: pattern conditional assignment (`if (params.details) err.details = params.details`) è intenzionale per evitare `details: undefined` che fallirebbe `'details' in err === false` (richiesto da Test 4 dove `exactOptionalPropertyTypes: true` fa la differenza).

**3. Eseguire test (TDD GREEN):**

```bash
pnpm --filter @gluezero/core test broker-error
```

Atteso: 9 test passano (4 createBrokerError + 5 isBrokerError).
  </action>
  <verify>
    <automated>test -f packages/core/src/core/broker-error.ts && test -f packages/core/src/core/broker-error.test.ts && grep -q "export function createBrokerError" packages/core/src/core/broker-error.ts && grep -q "export function isBrokerError" packages/core/src/core/broker-error.ts && grep -q "Error.cause" packages/core/src/core/broker-error.test.ts && pnpm --filter @gluezero/core test broker-error 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/broker-error.ts` esiste e esporta `createBrokerError` e `isBrokerError`
    - File `packages/core/src/core/broker-error.test.ts` esiste con almeno 9 test cases (4 per createBrokerError, 5 per isBrokerError)
    - Comando `pnpm --filter @gluezero/core test broker-error` esce 0 con tutti i test passing
    - Output del test contiene `Test Files  1 passed`
    - `createBrokerError` setta `Error.cause` (ES2022) quando `originalError` fornito
    - `createBrokerError` NON aggiunge proprietà `details/originalError/routeId/topic/eventId` se non fornite (verifica `'details' in err === false`)
    - `isBrokerError` ritorna `false` per: plain Error, null, undefined, plain object, string
  </acceptance_criteria>
  <done>broker-error module + test creati. Pattern TDD RED→GREEN dimostrato. Pronto per moduli paralleli.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deep-freeze.ts + test (D-04, D-05)</name>
  <files>packages/core/src/core/deep-freeze.ts, packages/core/src/core/deep-freeze.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/deep-freeze.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "deep-freeze.test.ts" (test cases)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisioni D-04, D-05
  </read_first>
  <behavior>
    - Test 1: `deepFreeze({a: {b: {c: 1}}})` rende frozen tutti i livelli (`Object.isFrozen` = true su outer + inner)
    - Test 2: `deepFreeze([{a: 1}, {b: 2}])` rende frozen array + ogni elemento
    - Test 3: Cycle protection: `const o: any = {}; o.self = o; deepFreeze(o)` non lancia stack overflow
    - Test 4: Date `deepFreeze(new Date())` di default NON congela (skipDates: true default), ma `deepFreeze(d, { skipDates: false })` la congela
    - Test 5: TypedArray `deepFreeze(new Uint8Array([1,2,3]))` di default NON congela (skipTypedArrays: true default)
    - Test 6: Map `deepFreeze(new Map([['k', {v: 1}]]))` di default congela la map e ogni valore (skipMaps: false default)
    - Test 7: Mutazione su frozen object in strict mode (file TS è strict by default in Vitest 4 con TS) lancia TypeError
    - Test 8: Performance — `deepFreeze` su oggetto con 1000 chiavi piatte completa in < 50ms (smoke perf check)
    - Test 9: `deepFreeze(null)` e `deepFreeze(undefined)` ritornano l'input senza errori
    - Test 10: `deepFreeze(42)` (primitive) ritorna l'input senza errori
  </behavior>
  <action>
**1. Creare `packages/core/src/core/deep-freeze.test.ts`:**

```ts
import { describe, it, expect } from 'vitest'
import { deepFreeze } from './deep-freeze'

describe('deepFreeze', () => {
  it('freezes nested object recursively', () => {
    const obj = { a: { b: { c: 1 } } }
    deepFreeze(obj)
    expect(Object.isFrozen(obj)).toBe(true)
    expect(Object.isFrozen(obj.a)).toBe(true)
    expect(Object.isFrozen(obj.a.b)).toBe(true)
  })

  it('freezes array and its elements', () => {
    const arr = [{ a: 1 }, { b: 2 }]
    deepFreeze(arr)
    expect(Object.isFrozen(arr)).toBe(true)
    expect(Object.isFrozen(arr[0])).toBe(true)
    expect(Object.isFrozen(arr[1])).toBe(true)
  })

  it('handles circular references without stack overflow', () => {
    const o: { self?: unknown; v: number } = { v: 1 }
    o.self = o
    expect(() => deepFreeze(o)).not.toThrow()
    expect(Object.isFrozen(o)).toBe(true)
  })

  it('skips Date by default', () => {
    const d = new Date()
    deepFreeze(d)
    expect(Object.isFrozen(d)).toBe(false)
  })

  it('freezes Date when skipDates: false', () => {
    const d = new Date()
    deepFreeze(d, { skipDates: false })
    expect(Object.isFrozen(d)).toBe(true)
  })

  it('skips TypedArray by default (would break iteration)', () => {
    const ta = new Uint8Array([1, 2, 3])
    deepFreeze(ta)
    expect(Object.isFrozen(ta)).toBe(false)
  })

  it('freezes Map and its values by default', () => {
    const m = new Map<string, { v: number }>([['k', { v: 1 }]])
    deepFreeze(m)
    expect(Object.isFrozen(m)).toBe(true)
    expect(Object.isFrozen(m.get('k'))).toBe(true)
  })

  it('freezes Set and its elements by default', () => {
    const s = new Set([{ a: 1 }, { b: 2 }])
    deepFreeze(s)
    expect(Object.isFrozen(s)).toBe(true)
    for (const v of s) expect(Object.isFrozen(v)).toBe(true)
  })

  it('throws TypeError on mutation in strict mode', () => {
    const obj = { a: 1 }
    deepFreeze(obj)
    expect(() => {
      ;(obj as { a: number }).a = 2
    }).toThrow(TypeError)
  })

  it('handles null and undefined gracefully', () => {
    expect(() => deepFreeze(null)).not.toThrow()
    expect(() => deepFreeze(undefined)).not.toThrow()
    expect(deepFreeze(null)).toBe(null)
    expect(deepFreeze(undefined)).toBe(undefined)
  })

  it('handles primitives gracefully', () => {
    expect(deepFreeze(42)).toBe(42)
    expect(deepFreeze('str')).toBe('str')
    expect(deepFreeze(true)).toBe(true)
  })

  it('completes in <50ms for object with 1000 flat keys', () => {
    const big: Record<string, number> = {}
    for (let i = 0; i < 1000; i++) big[`k${i}`] = i
    const start = performance.now()
    deepFreeze(big)
    const elapsed = performance.now() - start
    expect(elapsed).toBeLessThan(50)
    expect(Object.isFrozen(big)).toBe(true)
  })

  it('skips already-frozen objects (idempotent + perf optimization)', () => {
    const obj = { a: { b: 1 } }
    deepFreeze(obj)
    const start = performance.now()
    deepFreeze(obj)
    const elapsed = performance.now() - start
    expect(elapsed).toBeLessThan(5)
  })
})
```

**2. Creare `packages/core/src/core/deep-freeze.ts` (verbatim da RESEARCH.md):**

```ts
const FROZEN = new WeakSet<object>()

export interface DeepFreezeOptions {
  skipDates?: boolean
  skipMaps?: boolean
  skipSets?: boolean
  skipPromises?: boolean
  skipTypedArrays?: boolean
}

export function deepFreeze<T>(value: T, options: DeepFreezeOptions = {}): T {
  const opts = {
    skipDates: options.skipDates ?? true,
    skipMaps: options.skipMaps ?? false,
    skipSets: options.skipSets ?? false,
    skipPromises: options.skipPromises ?? true,
    skipTypedArrays: options.skipTypedArrays ?? true,
  }
  freezeRecursive(value, opts)
  return value
}

function freezeRecursive(value: unknown, opts: Required<DeepFreezeOptions>): void {
  if (value === null || value === undefined) return
  if (typeof value !== 'object') return
  if (FROZEN.has(value as object)) return
  if (Object.isFrozen(value)) return

  if (value instanceof Date) {
    if (!opts.skipDates) Object.freeze(value)
    return
  }
  if (value instanceof Promise) {
    if (!opts.skipPromises) Object.freeze(value)
    return
  }
  if (ArrayBuffer.isView(value)) {
    if (!opts.skipTypedArrays) {
      // Do NOT freeze TypedArray views (breaks iteration)
    }
    return
  }
  if (value instanceof Map) {
    if (!opts.skipMaps) {
      for (const [k, v] of value) {
        freezeRecursive(k, opts)
        freezeRecursive(v, opts)
      }
      Object.freeze(value)
    }
    return
  }
  if (value instanceof Set) {
    if (!opts.skipSets) {
      for (const v of value) freezeRecursive(v, opts)
      Object.freeze(value)
    }
    return
  }

  FROZEN.add(value as object)
  Object.freeze(value)
  for (const key of Object.getOwnPropertyNames(value)) {
    freezeRecursive((value as Record<string, unknown>)[key], opts)
  }
}
```

NOTA critica (RESEARCH §"Note critiche"):
- `WeakSet` per cycle protection (gestisce `obj.self = obj`)
- Skip se già frozen (perf hot path)
- Errori in strict mode JS: `Object.freeze` + assignment throw `TypeError` → test 9 verifica
- TypedArray skip default (freeze breaks iteration)

**3. Eseguire test:**

```bash
pnpm --filter @gluezero/core test deep-freeze
```

Atteso: 12 test passano.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/deep-freeze.ts && test -f packages/core/src/core/deep-freeze.test.ts && grep -q "export function deepFreeze" packages/core/src/core/deep-freeze.ts && grep -q "WeakSet" packages/core/src/core/deep-freeze.ts && grep -q "skipDates" packages/core/src/core/deep-freeze.ts && pnpm --filter @gluezero/core test deep-freeze 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/deep-freeze.ts` esporta `deepFreeze<T>(value, options?)` e `DeepFreezeOptions`
    - `deepFreeze` usa `WeakSet` per cycle protection (grep `WeakSet` in source)
    - File `packages/core/src/core/deep-freeze.test.ts` esiste con almeno 12 test cases (nested object, array, circular, Date skip, Date freeze opt-in, TypedArray skip, Map freeze, Set freeze, mutation TypeError, null/undefined, primitives, perf 1000 keys, idempotent)
    - Comando `pnpm --filter @gluezero/core test deep-freeze` esce 0 con tutti i test passing
    - Default `skipDates: true`, `skipMaps: false`, `skipSets: false`, `skipPromises: true`, `skipTypedArrays: true` (verifica via test cases)
    - Performance test (1000 keys < 50ms) passa
  </acceptance_criteria>
  <done>deep-freeze module + test creati. D-04 (deep-freeze runtime) + D-05 (Date/Map/Set semantics) coperti.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: logger.ts + test (CORE-10, D-12, D-14)</name>
  <files>packages/core/src/core/logger.ts, packages/core/src/core/logger.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/logger.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "logger.test.ts" (test cases)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisioni D-12, D-13, D-14
  </read_first>
  <behavior>
    - Test 1: `createConsoleLogger('info')` invoca `console.info` per `.info(msg)`, `console.warn` per `.warn`, `console.error` per `.error`, MA NON invoca `console.debug` per `.debug` (sotto-livello)
    - Test 2: `createConsoleLogger('silent')` non invoca alcun console method (no-op completo)
    - Test 3: `createConsoleLogger('error')` invoca solo `console.error`, non warn/info/debug/trace
    - Test 4: `createConsoleLogger('trace')` invoca tutti i metodi (max verbosity)
    - Test 5: Ogni log ha namespace prefix `[gluezero]` come primo argomento
    - Test 6: Quando `meta` è fornito, viene passato come terzo argomento; quando assente, console viene chiamato con 2 argomenti
    - Test 7: Trace log usa `console.debug` (non `console.trace`) con prefisso `TRACE` (D-12)
    - Test 8: `silentLogger` esporta i 5 metodi come no-op che non lanciano errori
    - Test 9: Default level è `'info'` quando non specificato in `createConsoleLogger()`
  </behavior>
  <action>
**1. Creare `packages/core/src/core/logger.test.ts`:**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { createConsoleLogger, silentLogger } from './logger'

describe('createConsoleLogger', () => {
  let errorSpy: ReturnType<typeof vi.spyOn>
  let warnSpy: ReturnType<typeof vi.spyOn>
  let infoSpy: ReturnType<typeof vi.spyOn>
  let debugSpy: ReturnType<typeof vi.spyOn>

  beforeEach(() => {
    errorSpy = vi.spyOn(console, 'error').mockImplementation(() => {})
    warnSpy = vi.spyOn(console, 'warn').mockImplementation(() => {})
    infoSpy = vi.spyOn(console, 'info').mockImplementation(() => {})
    debugSpy = vi.spyOn(console, 'debug').mockImplementation(() => {})
  })

  it('respects the info level: info/warn/error invoked, debug/trace not', () => {
    const logger = createConsoleLogger('info')
    logger.error('e')
    logger.warn('w')
    logger.info('i')
    logger.debug('d')
    logger.trace('t')
    expect(errorSpy).toHaveBeenCalledTimes(1)
    expect(warnSpy).toHaveBeenCalledTimes(1)
    expect(infoSpy).toHaveBeenCalledTimes(1)
    expect(debugSpy).not.toHaveBeenCalled()
  })

  it('silent level: no console method invoked at all', () => {
    const logger = createConsoleLogger('silent')
    logger.error('e')
    logger.warn('w')
    logger.info('i')
    logger.debug('d')
    logger.trace('t')
    expect(errorSpy).not.toHaveBeenCalled()
    expect(warnSpy).not.toHaveBeenCalled()
    expect(infoSpy).not.toHaveBeenCalled()
    expect(debugSpy).not.toHaveBeenCalled()
  })

  it('error level: only error invoked', () => {
    const logger = createConsoleLogger('error')
    logger.error('e')
    logger.warn('w')
    logger.info('i')
    logger.debug('d')
    logger.trace('t')
    expect(errorSpy).toHaveBeenCalledTimes(1)
    expect(warnSpy).not.toHaveBeenCalled()
    expect(infoSpy).not.toHaveBeenCalled()
    expect(debugSpy).not.toHaveBeenCalled()
  })

  it('trace level: all methods invoked', () => {
    const logger = createConsoleLogger('trace')
    logger.error('e')
    logger.warn('w')
    logger.info('i')
    logger.debug('d')
    logger.trace('t')
    expect(errorSpy).toHaveBeenCalledTimes(1)
    expect(warnSpy).toHaveBeenCalledTimes(1)
    expect(infoSpy).toHaveBeenCalledTimes(1)
    // debug AND trace both go to console.debug (D-12)
    expect(debugSpy).toHaveBeenCalledTimes(2)
  })

  it('includes [gluezero] namespace prefix as first arg', () => {
    const logger = createConsoleLogger('info')
    logger.info('hello')
    expect(infoSpy).toHaveBeenCalledWith(expect.stringContaining('[gluezero]'), 'hello')
  })

  it('passes meta as third arg when provided', () => {
    const logger = createConsoleLogger('info')
    logger.info('hello', { key: 'value' })
    expect(infoSpy).toHaveBeenCalledWith(expect.any(String), 'hello', { key: 'value' })
  })

  it('passes only 2 args when meta absent', () => {
    const logger = createConsoleLogger('info')
    logger.info('hello')
    const callArgs = infoSpy.mock.calls[0]!
    expect(callArgs.length).toBe(2)
  })

  it('trace uses console.debug with TRACE prefix (D-12)', () => {
    const logger = createConsoleLogger('trace')
    logger.trace('msg')
    expect(debugSpy).toHaveBeenCalledWith(expect.stringContaining('TRACE'), 'msg')
  })

  it('default level is info when not specified', () => {
    const logger = createConsoleLogger()
    logger.info('i')
    logger.debug('d')
    expect(infoSpy).toHaveBeenCalledTimes(1)
    expect(debugSpy).not.toHaveBeenCalled()
  })
})

describe('silentLogger', () => {
  it('all 5 methods are no-ops that do not throw', () => {
    expect(() => silentLogger.error('x')).not.toThrow()
    expect(() => silentLogger.warn('x')).not.toThrow()
    expect(() => silentLogger.info('x')).not.toThrow()
    expect(() => silentLogger.debug('x')).not.toThrow()
    expect(() => silentLogger.trace('x')).not.toThrow()
  })

  it('does not invoke console methods', () => {
    const errorSpy = vi.spyOn(console, 'error').mockImplementation(() => {})
    silentLogger.error('x')
    expect(errorSpy).not.toHaveBeenCalled()
  })
})
```

**2. Creare `packages/core/src/core/logger.ts` (verbatim da RESEARCH.md):**

```ts
import type { BrokerLogger, LogLevel } from '../types/logger'

const LEVEL_ORDER: Record<LogLevel, number> = {
  silent: 0,
  error: 1,
  warn: 2,
  info: 3,
  debug: 4,
  trace: 5,
}

const PREFIX = '[gluezero]'

export function createConsoleLogger(level: LogLevel = 'info'): BrokerLogger {
  const enabled = (target: LogLevel): boolean => LEVEL_ORDER[level] >= LEVEL_ORDER[target]

  const fmt = (label: string, msg: string, meta?: Record<string, unknown>): unknown[] =>
    meta ? [`${PREFIX} [${label}]`, msg, meta] : [`${PREFIX} [${label}]`, msg]

  return {
    error(message, meta) {
      if (enabled('error')) console.error(...fmt('ERROR', message, meta))
    },
    warn(message, meta) {
      if (enabled('warn')) console.warn(...fmt('WARN', message, meta))
    },
    info(message, meta) {
      if (enabled('info')) console.info(...fmt('INFO', message, meta))
    },
    debug(message, meta) {
      if (enabled('debug')) console.debug(...fmt('DEBUG', message, meta))
    },
    trace(message, meta) {
      if (enabled('trace')) console.debug(...fmt('TRACE', message, meta))
    },
  }
}

export const silentLogger: BrokerLogger = {
  error: () => {},
  warn: () => {},
  info: () => {},
  debug: () => {},
  trace: () => {},
}
```

NOTA D-12 enforcement: `trace` invoca `console.debug` con prefisso `TRACE` (NON `console.trace`); il test 8 lo verifica via `expect.stringContaining('TRACE')`.

**3. Eseguire test:**

```bash
pnpm --filter @gluezero/core test logger
```

Atteso: 11 test passano.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/logger.ts && test -f packages/core/src/core/logger.test.ts && grep -q "export function createConsoleLogger" packages/core/src/core/logger.ts && grep -q "export const silentLogger" packages/core/src/core/logger.ts && grep -q "\\[sembridge\\]" packages/core/src/core/logger.ts && grep -q "TRACE" packages/core/src/core/logger.ts && pnpm --filter @gluezero/core test logger 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/logger.ts` esporta `createConsoleLogger(level?: LogLevel): BrokerLogger` e `silentLogger: BrokerLogger`
    - File contiene la costante `LEVEL_ORDER` con tutti e 6 i livelli (silent=0, error=1, warn=2, info=3, debug=4, trace=5)
    - File contiene il PREFIX `[gluezero]`
    - File contiene il label `TRACE` (per D-12: trace usa console.debug con prefisso TRACE)
    - File `packages/core/src/core/logger.test.ts` ha almeno 11 test cases (9 createConsoleLogger + 2 silentLogger)
    - Comando `pnpm --filter @gluezero/core test logger` esce 0 con tutti i test passing
    - Test verifica: livello `silent` non invoca alcun console method, `trace` invoca tutti, `info` non invoca debug
    - Test verifica: namespace `[gluezero]` presente nel primo arg
    - Test verifica: meta passato come terzo arg quando fornito
  </acceptance_criteria>
  <done>logger module + test creati. CORE-10 (6 livelli configurabili) + D-12 (console mapping) + D-14 (BrokerLogger surface) coperti.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: event-tap.ts + test (CORE-13, D-20)</name>
  <files>packages/core/src/core/event-tap.ts, packages/core/src/core/event-tap.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/event-tap.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-20 (EventTap surface F1)
  </read_first>
  <behavior>
    - Test 1: `noopEventTap.onPipelineStep(step, snapshot)` non lancia, non ha return value, è veramente no-op
    - Test 2: `safeTapStep` invoca `tap.onPipelineStep(step, snapshot)` con i parametri corretti
    - Test 3: Se il tap lancia eccezione, `safeTapStep` la swallowa (D-20: errors swallowed; un tap che fallisce non rompe la pipeline)
    - Test 4: Se il tap lancia eccezione e `onError` è fornito, viene invocato con l'errore
    - Test 5: Se il tap lancia eccezione e `onError` non è fornito, non viene fatto nulla (silent swallow)
    - Test 6: `startStep()` ritorna una factory function che genera PipelineSnapshot con timestamp + durationMs
    - Test 7: Il `durationMs` ritornato dalla factory è ≥ 0 (e tipicamente piccolo, < 100ms in test)
  </behavior>
  <action>
**1. Creare `packages/core/src/core/event-tap.test.ts`:**

```ts
import { describe, it, expect, vi } from 'vitest'
import { noopEventTap, safeTapStep, startStep } from './event-tap'
import type { PipelineSnapshot, PipelineStep } from '../types/tap'

const makeSnapshot = (step: PipelineStep): PipelineSnapshot => ({
  eventId: 'evt-1',
  topic: 'test.topic',
  step,
  timestamp: Date.now(),
  durationMs: 0,
})

describe('noopEventTap', () => {
  it('onPipelineStep is a no-op that does not throw', () => {
    const snap = makeSnapshot('event.received')
    expect(() => noopEventTap.onPipelineStep('event.received', snap)).not.toThrow()
  })

  it('returns undefined (no return value)', () => {
    const snap = makeSnapshot('event.delivered')
    const ret = noopEventTap.onPipelineStep('event.delivered', snap)
    expect(ret).toBeUndefined()
  })
})

describe('safeTapStep', () => {
  it('invokes tap.onPipelineStep with provided step and snapshot', () => {
    const tap = { onPipelineStep: vi.fn() }
    const snap = makeSnapshot('event.received')
    safeTapStep(tap, 'event.received', snap)
    expect(tap.onPipelineStep).toHaveBeenCalledWith('event.received', snap)
  })

  it('swallows errors thrown by the tap (D-20)', () => {
    const tap = {
      onPipelineStep: vi.fn(() => {
        throw new Error('tap blew up')
      }),
    }
    const snap = makeSnapshot('event.validated')
    expect(() => safeTapStep(tap, 'event.validated', snap)).not.toThrow()
  })

  it('invokes onError callback when tap throws', () => {
    const onError = vi.fn()
    const err = new Error('boom')
    const tap = {
      onPipelineStep: vi.fn(() => {
        throw err
      }),
    }
    safeTapStep(tap, 'event.delivered', makeSnapshot('event.delivered'), onError)
    expect(onError).toHaveBeenCalledWith(err)
  })

  it('silently swallows when onError absent', () => {
    const tap = {
      onPipelineStep: vi.fn(() => {
        throw new Error('silent')
      }),
    }
    expect(() =>
      safeTapStep(tap, 'event.dedupe.checked', makeSnapshot('event.dedupe.checked')),
    ).not.toThrow()
  })
})

describe('startStep', () => {
  it('returns a factory that produces PipelineSnapshot', () => {
    const factory = startStep()
    const snap = factory('event.received', 'evt-1', 'test.topic')
    expect(snap.step).toBe('event.received')
    expect(snap.eventId).toBe('evt-1')
    expect(snap.topic).toBe('test.topic')
    expect(typeof snap.timestamp).toBe('number')
    expect(typeof snap.durationMs).toBe('number')
  })

  it('durationMs >= 0', () => {
    const factory = startStep()
    const snap = factory('event.delivered', 'evt-2', 'foo.bar')
    expect(snap.durationMs).toBeGreaterThanOrEqual(0)
  })

  it('factory accepts extras to merge into snapshot', () => {
    const factory = startStep()
    const snap = factory('event.metadata.enriched', 'evt-3', 'x.y', {
      metadata: { subscriberCount: 5 },
    })
    expect(snap.metadata).toEqual({ subscriberCount: 5 })
  })
})
```

**2. Creare `packages/core/src/core/event-tap.ts` (verbatim da RESEARCH.md):**

```ts
import type { EventTap, PipelineStep, PipelineSnapshot } from '../types/tap'

export const noopEventTap: EventTap = {
  onPipelineStep: () => {},
}

export function safeTapStep(
  tap: EventTap,
  step: PipelineStep,
  snapshot: PipelineSnapshot,
  onError?: (e: unknown) => void,
): void {
  try {
    tap.onPipelineStep(step, snapshot)
  } catch (e) {
    onError?.(e)
  }
}

export function startStep(): (
  step: PipelineStep,
  eventId: string,
  topic: string,
  extras?: Partial<PipelineSnapshot>,
) => PipelineSnapshot {
  const start = performance.now()
  return (step, eventId, topic, extras = {}) => ({
    eventId,
    topic,
    step,
    timestamp: Date.now(),
    durationMs: performance.now() - start,
    ...extras,
  })
}
```

**3. Eseguire test:**

```bash
pnpm --filter @gluezero/core test event-tap
```

Atteso: 9 test passano.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/event-tap.ts && test -f packages/core/src/core/event-tap.test.ts && grep -q "export const noopEventTap" packages/core/src/core/event-tap.ts && grep -q "export function safeTapStep" packages/core/src/core/event-tap.ts && grep -q "export function startStep" packages/core/src/core/event-tap.ts && grep -q "try {" packages/core/src/core/event-tap.ts && grep -q "catch (e)" packages/core/src/core/event-tap.ts && pnpm --filter @gluezero/core test event-tap 2>&1 | grep -E "Test Files.*1.*passed" && pnpm --filter @gluezero/core test 2>&1 | grep -E "Test Files.*4.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/event-tap.ts` esporta `noopEventTap`, `safeTapStep`, `startStep`
    - `noopEventTap` implementa interface `EventTap` con `onPipelineStep` che è una funzione vuota `() => {}`
    - `safeTapStep` ha try/catch che cattura errori dal tap (grep `try {` + `catch (e)`)
    - File `packages/core/src/core/event-tap.test.ts` ha almeno 9 test cases (2 noopEventTap + 4 safeTapStep + 3 startStep)
    - Comando `pnpm --filter @gluezero/core test event-tap` esce 0
    - Comando `pnpm --filter @gluezero/core test` (suite completa fino a qui) esce 0 e riporta `Test Files  4 passed` (broker-error + deep-freeze + logger + event-tap)
    - Test verifica: errore lanciato dal tap NON propaga (D-20 enforcement)
    - Test verifica: `onError` callback invocato se fornito quando tap throws
  </acceptance_criteria>
  <done>event-tap module + test creati. CORE-13 (EventTap interface implementata con no-op default) + D-20 (errors swallowed) coperti. Plan 04 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tap callback → Pipeline | Tap di terze parti può lanciare eccezioni; pipeline deve essere isolata |
| Subscriber → Frozen payload | Subscriber tenta mutazione → strict mode TypeError (volutamente non catchato) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | EventTap di terze parti throw → broker collassa | mitigate | `safeTapStep` cattura tutte le eccezioni con try/catch (D-20); test 3 verifica il no-throw |
| T-04-02 | Tampering | Subscriber muta payload condiviso → race condition | mitigate | `deepFreeze` ricorsivo + cycle protection; in dev mode TypeError throw immediato; in production type-level `DeepReadonly` (compile-time) |
| T-04-03 | Information disclosure | Logger logga PII in dev mode | accept | DOC-01 documenterà best practice "non loggare PII"; runtime mitigation deferred a F6 (Inspector con redaction) |
| T-04-04 | DoS | deepFreeze lento su payload grandi | accept | D-04: solo in dev mode (production skip); cycle protection via WeakSet evita worst-case; perf test 1000 keys < 50ms |
| T-04-05 | Tampering | Logger custom (setLogger) può corrompere log | accept | Adapter slot intenzionale (D-13); responsabilità del consumer; firma `BrokerLogger` enforcement type-level |
</threat_model>

<verification>
- 4 file source + 4 file test esistenti in `packages/core/src/core/`
- `pnpm --filter @gluezero/core test` esce 0 e riporta esattamente `Test Files  4 passed` (broker-error + deep-freeze + logger + event-tap)
- `pnpm --filter @gluezero/core typecheck` esce 0
- `pnpm biome check packages/core/src/core/` esce 0
- Coverage v8 (eseguito al merge wave) ≥ 90% su questi 4 file
</verification>

<success_criteria>
- ERR-01 ✓ (BrokerError factory con tutti gli 8 campi + ES2022 cause)
- CORE-10 ✓ (6 livelli configurabili, console mapping D-12)
- CORE-13 ✓ (EventTap interface implementata con no-op default + safeTapStep helper)
- D-04, D-05 ✓ (deep-freeze runtime con Date/Map/Set/Promise/TypedArray semantics)
- D-12, D-13, D-14 ✓ (logger surface + console mapping)
- D-20 ✓ (errors swallowed in tap)
- Tutte le suite unit passano deterministicamente
- File ownership disgiunta: nessun file in plan 05/06/07 sovrappone a questi 4 source + 4 test
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-04-SUMMARY.md` con:
- 4 moduli + 4 test creati
- Output di `pnpm --filter @gluezero/core test` (mostra "Test Files 4 passed")
- Coverage parziale post-plan (target ≥ 90% su questi 4 file)
- Decisioni: nessuna deviazione dalla research
- Pronto-per: il completamento di Wave 3 (plan 05 + 06 paralleli) e successivamente Wave 4 (bus.ts in plan 07 importa da questi 4 moduli)
</output>
