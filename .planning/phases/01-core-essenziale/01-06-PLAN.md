---
phase: 01-core-essenziale
plan: 06
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - packages/core/src/core/topic-registry.ts
  - packages/core/src/core/topic-registry.test.ts
  - packages/core/src/core/lifecycle.ts
  - packages/core/src/core/lifecycle.test.ts
autonomous: true
requirements:
  - CORE-03
  - CORE-05
tags:
  - registry
  - lifecycle
  - tdd
must_haves:
  truths:
    - "`TopicRegistry.register('weather.requested')` ritorna true alla prima invocazione, false alla seconda (idempotente)"
    - "`TopicRegistry.list()` ritorna i topic registrati ordinati alfabeticamente"
    - "`TopicRegistry.onRegistered(listener)` invoca il listener su nuova registrazione e ritorna unsubscribe function"
    - "`transitionState(reg, 'mounting', logger)` avanza stato da 'registered' → 'mounting' senza errori"
    - "`transitionState(reg, 'destroyed', logger)` da stato 'registered' lancia BrokerError code='plugin.lifecycle.invalid-transition'"
    - "Tutte le transizioni di VALID_TRANSITIONS (D-25) sono coperte dai test"
  artifacts:
    - path: "packages/core/src/core/topic-registry.ts"
      provides: "TopicRegistry class con register/has/list/onRegistered"
      contains: "TopicRegistry"
    - path: "packages/core/src/core/lifecycle.ts"
      provides: "VALID_TRANSITIONS map + transitionState() (state machine D-25)"
      contains: "VALID_TRANSITIONS"
  key_links:
    - from: "packages/core/src/core/lifecycle.ts"
      to: "packages/core/src/core/broker-error.ts"
      via: "import { createBrokerError } per BrokerError code='plugin.lifecycle.invalid-transition'"
      pattern: "createBrokerError"
    - from: "packages/core/src/core/lifecycle.ts"
      to: "packages/core/src/types/plugin.ts"
      via: "import type { PluginRegistration, PluginState }"
      pattern: "PluginState"
---

<objective>
Implementare il batch C delle utility foundation di `@sembridge/core`: TopicRegistry (CORE-03 — traccia topic noti via `Set<string>` con observer pattern) e Lifecycle state machine (CORE-05 — VALID_TRANSITIONS + `transitionState` che valida le transizioni dello stato plugin secondo D-25). File ownership disgiunta da plan 04 e 05 → eseguibile in parallelo.

Purpose: questi 2 moduli sono dipendenze utility di `bus.ts`/`broker.ts` (plan 07) e `plugin-registry.ts` (plan 08). State machine atomico previene transizioni invalide (es. `unregistered → destroyed` salta 6 stati). TopicRegistry abilita CORE-03 e supporta `getDebugSnapshot().topics`.

Output: 2 moduli + 2 file test che passano `pnpm --filter @sembridge/core test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-03-SUMMARY.md
@packages/core/src/types/plugin.ts
@packages/core/src/types/error.ts
@packages/core/src/types/logger.ts
</context>

<interfaces>
From packages/core/src/types/plugin.ts (plan 03):
```typescript
export type PluginState = 'unregistered' | 'registered' | 'mounting' | 'mounted' | 'unmounting' | 'unmounted' | 'failed' | 'destroyed'
export interface PluginDescriptor { readonly id: string; ... onRegister?(ctx); onMount?(ctx); onUnmount?(ctx); onDestroy?(ctx) }
export interface PluginRegistration {
  descriptor: PluginDescriptor
  state: PluginState
  subscriptions: Set<Subscription>
  abortController: AbortController
  registeredAt: number
  mountedAt?: number
  unmountedAt?: number
  failureReason?: BrokerError
}
```

From packages/core/src/types/logger.ts (plan 03):
```typescript
export interface BrokerLogger { error(msg, meta?); warn(msg, meta?); info(msg, meta?); debug(msg, meta?); trace(msg, meta?) }
```

From packages/core/src/core/broker-error.ts (plan 04, parallelo):
```typescript
export function createBrokerError(params): BrokerError
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: topic-registry.ts + test (CORE-03)</name>
  <files>packages/core/src/core/topic-registry.ts, packages/core/src/core/topic-registry.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/topic-registry.ts" (snippet completo)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md "Open Questions §2" (decisione: niente emit di system.topic.registered in F1)
  </read_first>
  <behavior>
    - Test: `register('weather.requested')` ritorna `true` (nuovo topic), `register('weather.requested')` di nuovo ritorna `false` (già presente — idempotente)
    - Test: `has('weather.requested')` ritorna true dopo register, false prima
    - Test: `list()` ritorna array ordinato alfabeticamente di topic registrati
    - Test: `onRegistered(listener)` invoca listener su nuovo register; non invoca su register duplicato (no-op idempotent)
    - Test: `onRegistered` ritorna unsubscribe; dopo unsubscribe il listener non viene più invocato
    - Test: Multipli listener: tutti vengono invocati; uno che lancia eccezione non rompe gli altri (try/catch swallow)
    - Test: `list()` su registry vuoto ritorna `[]`
  </behavior>
  <action>
**1. Creare `packages/core/src/core/topic-registry.test.ts`:**

```ts
import { describe, it, expect, vi } from 'vitest'
import { TopicRegistry } from './topic-registry'

describe('TopicRegistry', () => {
  it('register returns true on new topic, false on duplicate (idempotent)', () => {
    const reg = new TopicRegistry()
    expect(reg.register('weather.requested')).toBe(true)
    expect(reg.register('weather.requested')).toBe(false)
  })

  it('has returns correct presence', () => {
    const reg = new TopicRegistry()
    expect(reg.has('weather.requested')).toBe(false)
    reg.register('weather.requested')
    expect(reg.has('weather.requested')).toBe(true)
  })

  it('list returns alphabetically sorted topics', () => {
    const reg = new TopicRegistry()
    reg.register('weather.requested')
    reg.register('auth.failed')
    reg.register('form.submit')
    expect(reg.list()).toEqual(['auth.failed', 'form.submit', 'weather.requested'])
  })

  it('list on empty registry returns []', () => {
    const reg = new TopicRegistry()
    expect(reg.list()).toEqual([])
  })

  it('onRegistered listener invoked on new register only', () => {
    const reg = new TopicRegistry()
    const listener = vi.fn()
    reg.onRegistered(listener)
    reg.register('a.b')
    reg.register('a.b')   // duplicate, listener should NOT fire again
    expect(listener).toHaveBeenCalledTimes(1)
    expect(listener).toHaveBeenCalledWith('a.b')
  })

  it('onRegistered returns unsubscribe', () => {
    const reg = new TopicRegistry()
    const listener = vi.fn()
    const unsub = reg.onRegistered(listener)
    reg.register('a.b')
    unsub()
    reg.register('c.d')
    expect(listener).toHaveBeenCalledTimes(1)
  })

  it('multiple listeners all invoked; throwing listener does not break others', () => {
    const reg = new TopicRegistry()
    const ok1 = vi.fn()
    const ok2 = vi.fn()
    const bad = vi.fn(() => { throw new Error('oops') })
    reg.onRegistered(ok1)
    reg.onRegistered(bad)
    reg.onRegistered(ok2)
    expect(() => reg.register('x.y')).not.toThrow()
    expect(ok1).toHaveBeenCalled()
    expect(ok2).toHaveBeenCalled()
  })
})
```

**2. Creare `packages/core/src/core/topic-registry.ts`** verbatim da RESEARCH.md sezione "core/topic-registry.ts":

```ts
export class TopicRegistry {
  private topics = new Set<string>()
  private listeners = new Set<(topic: string) => void>()

  register(topic: string): boolean {
    if (this.topics.has(topic)) return false
    this.topics.add(topic)
    for (const l of this.listeners) {
      try { l(topic) } catch {}
    }
    return true
  }

  has(topic: string): boolean {
    return this.topics.has(topic)
  }

  list(): string[] {
    return [...this.topics].sort()
  }

  onRegistered(listener: (topic: string) => void): () => void {
    this.listeners.add(listener)
    return () => { this.listeners.delete(listener) }
  }
}
```

NOTA Open Questions §2: in F1 NON emette `system.topic.registered` come BrokerEvent. Il TopicRegistry è "soft" — solo expose via `getTopicRegistry()`. F6 può estendere se Inspector lo richiede.

**3. Eseguire `pnpm --filter @sembridge/core test topic-registry`** — atteso 7 test passing.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/topic-registry.ts && test -f packages/core/src/core/topic-registry.test.ts && grep -q "export class TopicRegistry" packages/core/src/core/topic-registry.ts && grep -q "register(topic" packages/core/src/core/topic-registry.ts && grep -q "onRegistered" packages/core/src/core/topic-registry.ts && pnpm --filter @sembridge/core test topic-registry 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/topic-registry.ts` esporta `TopicRegistry` class con metodi `register(topic): boolean`, `has(topic): boolean`, `list(): string[]`, `onRegistered(listener): () => void`
    - `register` ritorna `true` solo alla prima registrazione (idempotente)
    - `list()` ritorna sempre array ordinato (uso `.sort()`)
    - `onRegistered` ritorna funzione unsubscribe
    - Listener throwing non propaga (try/catch swallow)
    - File `packages/core/src/core/topic-registry.test.ts` con almeno 7 test cases
    - `pnpm --filter @sembridge/core test topic-registry` esce 0
  </acceptance_criteria>
  <done>topic-registry module + test creati. CORE-03 coperto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lifecycle.ts + test (CORE-05, D-25 state machine)</name>
  <files>packages/core/src/core/lifecycle.ts, packages/core/src/core/lifecycle.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/lifecycle.ts" (snippet completo con VALID_TRANSITIONS)
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisione D-25 (lifecycle execution order)
    - packages/core/src/types/plugin.ts (PluginRegistration + PluginState)
  </read_first>
  <behavior>
    - Test: `transitionState(reg, 'registered', logger)` da `unregistered` non lancia, aggiorna `reg.state` a `'registered'`
    - Test: `transitionState(reg, 'mounting', logger)` da `registered` OK
    - Test: `transitionState(reg, 'mounted', logger)` da `mounting` OK
    - Test: `transitionState(reg, 'unmounting', logger)` da `mounted` OK
    - Test: `transitionState(reg, 'unmounted', logger)` da `unmounting` OK
    - Test: `transitionState(reg, 'destroyed', logger)` da `unmounted` OK
    - Test: `transitionState(reg, 'failed', logger)` da `mounting` OK (mount fail path)
    - Test: `transitionState(reg, 'unmounting', logger)` da `failed` OK (recovery path)
    - Test: `transitionState(reg, 'destroyed', logger)` da `unregistered` lancia BrokerError code='plugin.lifecycle.invalid-transition'
    - Test: `transitionState(reg, 'mounted', logger)` da `unregistered` lancia (skip stati intermedi)
    - Test: `transitionState(reg, 'unregistered', logger)` da qualunque altro stato lancia (no rollback to unregistered)
    - Test: errore include `details.from`, `details.to`, `details.pluginId`
    - Test: errore loggato via `logger.error` prima del throw
    - Test: `destroyed` è terminal — no transizioni outbound (VALID_TRANSITIONS.destroyed === [])
  </behavior>
  <action>
**1. Creare `packages/core/src/core/lifecycle.test.ts`:**

```ts
import { describe, it, expect, vi } from 'vitest'
import { transitionState } from './lifecycle'
import { isBrokerError } from './broker-error'
import { silentLogger } from './logger'
import type { PluginRegistration, PluginState } from '../types/plugin'

const makeReg = (state: PluginState): PluginRegistration => ({
  descriptor: { id: 'p1' },
  state,
  subscriptions: new Set(),
  abortController: new AbortController(),
  registeredAt: Date.now(),
})

describe('transitionState — valid transitions', () => {
  it.each([
    ['unregistered', 'registered'],
    ['registered', 'mounting'],
    ['registered', 'unmounted'],
    ['mounting', 'mounted'],
    ['mounting', 'failed'],
    ['mounted', 'unmounting'],
    ['unmounting', 'unmounted'],
    ['unmounting', 'failed'],
    ['unmounted', 'destroyed'],
    ['failed', 'unmounting'],
    ['failed', 'destroyed'],
  ] as const)('allows %s → %s', (from, to) => {
    const reg = makeReg(from)
    expect(() => transitionState(reg, to, silentLogger)).not.toThrow()
    expect(reg.state).toBe(to)
  })
})

describe('transitionState — invalid transitions', () => {
  it.each([
    ['unregistered', 'mounted'],
    ['unregistered', 'destroyed'],
    ['registered', 'destroyed'],
    ['mounted', 'mounting'],
    ['mounted', 'destroyed'],
    ['destroyed', 'mounted'],
    ['destroyed', 'registered'],
  ] as const)('rejects %s → %s', (from, to) => {
    const reg = makeReg(from)
    let caught: unknown = null
    try { transitionState(reg, to, silentLogger) } catch (e) { caught = e }
    expect(isBrokerError(caught)).toBe(true)
    expect((caught as { code: string }).code).toBe('plugin.lifecycle.invalid-transition')
  })
})

describe('transitionState — error details', () => {
  it('error.details contains from, to, pluginId', () => {
    const reg = makeReg('unregistered')
    let caught: unknown = null
    try { transitionState(reg, 'destroyed', silentLogger) } catch (e) { caught = e }
    const details = (caught as { details: Record<string, unknown> }).details
    expect(details.from).toBe('unregistered')
    expect(details.to).toBe('destroyed')
    expect(details.pluginId).toBe('p1')
  })

  it('error logged via logger.error before throw', () => {
    const reg = makeReg('unregistered')
    const errorSpy = vi.fn()
    const logger = { ...silentLogger, error: errorSpy }
    try { transitionState(reg, 'destroyed', logger) } catch {}
    expect(errorSpy).toHaveBeenCalled()
  })
})

describe('transitionState — destroyed is terminal', () => {
  it.each(['registered', 'mounting', 'mounted', 'unmounting', 'unmounted', 'failed', 'unregistered'] as const)(
    'destroyed cannot transition to %s',
    (target) => {
      const reg = makeReg('destroyed')
      expect(() => transitionState(reg, target, silentLogger)).toThrow()
    },
  )
})
```

**2. Creare `packages/core/src/core/lifecycle.ts`** verbatim da RESEARCH.md sezione "core/lifecycle.ts":

```ts
import type { PluginRegistration, PluginState } from '../types/plugin'
import type { BrokerLogger } from '../types/logger'
import { createBrokerError } from './broker-error'

const VALID_TRANSITIONS: Record<PluginState, PluginState[]> = {
  unregistered: ['registered'],
  registered: ['mounting', 'unmounted'],
  mounting: ['mounted', 'failed'],
  mounted: ['unmounting'],
  unmounting: ['unmounted', 'failed'],
  unmounted: ['destroyed'],
  failed: ['unmounting', 'destroyed'],
  destroyed: [],
}

export function transitionState(reg: PluginRegistration, target: PluginState, logger: BrokerLogger): void {
  const allowed = VALID_TRANSITIONS[reg.state]
  if (!allowed.includes(target)) {
    const err = createBrokerError({
      code: 'plugin.lifecycle.invalid-transition',
      category: 'plugin',
      message: `Invalid plugin lifecycle transition: ${reg.state} → ${target} (plugin: ${reg.descriptor.id})`,
      details: { from: reg.state, to: target, pluginId: reg.descriptor.id },
    })
    logger.error(err.message, { error: err })
    throw err
  }
  reg.state = target
}
```

NOTA D-25: ordine `unregistered → registered → mounting → mounted → unmounting → unmounted → destroyed`. Path `failed` raggiungibile da `mounting` o `unmounting` (failure during async hook). Da `failed` → `unmounting` (recovery) o `destroyed` (force cleanup).

NOTA: `registered → unmounted` ammesso (registerPlugin che fallisce su `onRegister` rollback verso unmounted senza passare per mounting).

**3. Eseguire `pnpm --filter @sembridge/core test lifecycle`** — atteso ~25 test cases (it.each genera multipli).
  </action>
  <verify>
    <automated>test -f packages/core/src/core/lifecycle.ts && test -f packages/core/src/core/lifecycle.test.ts && grep -q "VALID_TRANSITIONS" packages/core/src/core/lifecycle.ts && grep -q "export function transitionState" packages/core/src/core/lifecycle.ts && grep -q "plugin.lifecycle.invalid-transition" packages/core/src/core/lifecycle.ts && pnpm --filter @sembridge/core test lifecycle 2>&1 | grep -E "Test Files.*1.*passed" && pnpm --filter @sembridge/core test 2>&1 | grep -E "Test Files.*[0-9]+.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/lifecycle.ts` esporta `transitionState(reg, target, logger): void`
    - File contiene mappa `VALID_TRANSITIONS` con tutti gli 8 stati come keys (`unregistered, registered, mounting, mounted, unmounting, unmounted, failed, destroyed`)
    - `destroyed` ha array `[]` (terminale)
    - Throw BrokerError code='plugin.lifecycle.invalid-transition' su transizione non permessa
    - Errore include `details.from`, `details.to`, `details.pluginId`
    - Logger invocato con `error(msg, meta)` prima del throw
    - File `packages/core/src/core/lifecycle.test.ts` ha test cases per: 11 transizioni valide + 7 transizioni invalide + error.details + logger.error invoked + destroyed terminale
    - `pnpm --filter @sembridge/core test lifecycle` esce 0
  </acceptance_criteria>
  <done>lifecycle module + test creati. CORE-05 + D-25 state machine coperti. Plan 06 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin code → Broker (lifecycle hook) | onRegister/onMount/onUnmount/onDestroy possono throw o trovarsi in stato corrotto |
| Public API (TopicRegistry) → Internal state | `getTopicRegistry()` espone Set di topic; non deve permettere mutation esterna |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | TopicRegistry mutato dall'esterno (pubblicazione `weather.../malicious`) | mitigate | `list()` ritorna copia spread `[...this.topics].sort()` (non riferimento al Set interno); `register` valida implicitamente via `validateTopic` chiamato dal `bus.ts` PRIMA di delegare al registry |
| T-06-02 | DoS | Lifecycle state corruption (race tra register/unregister concorrenti) | mitigate | `transitionState` è sync — race-free se chiamato da single-threaded JS. Eventuale pattern async via microtask in plan 08 (PluginRegistry); state machine atomico previene salti illegittimi |
| T-06-03 | DoS | Topic registry listener malevolo throw | mitigate | `try { l(topic) } catch {}` swallow gli errori dai listener |
| T-06-04 | Information disclosure | Errori lifecycle leak pluginId in details | accept | Per design — pluginId è internal, non PII; necessario per debug |
</threat_model>

<verification>
- 2 file source + 2 file test esistenti in `packages/core/src/core/`
- `pnpm --filter @sembridge/core test topic-registry` esce 0
- `pnpm --filter @sembridge/core test lifecycle` esce 0
- `pnpm --filter @sembridge/core typecheck` esce 0
- `pnpm biome check packages/core/src/core/topic-registry.ts packages/core/src/core/lifecycle.ts` esce 0
- Suite Wave 3 completa (post merge plan 04 + 05 + 06): `pnpm --filter @sembridge/core test` riporta `Test Files 9 passed` (4 plan04 + 3 plan05 + 2 plan06)
</verification>

<success_criteria>
- CORE-03 ✓ (TopicRegistry con register/has/list/onRegistered)
- CORE-05 ✓ (state machine D-25 con VALID_TRANSITIONS map + transitionState validator)
- File ownership disgiunta da plan 04 e 05 — eseguibile in parallelo
- Tutte le 11 transizioni valide + 7 invalide + edge cases coperti dai test
- `destroyed` è stato terminale verificato dai test
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-06-SUMMARY.md` con:
- 2 moduli + 2 test creati
- Output di `pnpm --filter @sembridge/core test` (post merge wave 3)
- Conferma del state machine D-25 enforcement
- Pronto-per: Wave 4 — plan 07 (bus.ts) che importa TopicTrie (plan 05), createBrokerEvent (plan 05), validateEvent (plan 05), createConsoleLogger (plan 04), noopEventTap+safeTapStep (plan 04), deepFreeze (plan 04), createBrokerError (plan 04)
</output>
