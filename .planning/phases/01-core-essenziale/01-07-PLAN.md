---
phase: 01-core-essenziale
plan: 07
type: execute
wave: 4
depends_on: [01-04, 01-05, 01-06]
files_modified:
  - packages/core/src/core/bus.ts
  - packages/core/src/core/bus.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-09
  - CORE-12
  - ERR-03
tags:
  - event-bus
  - dispatch
  - handler-isolation
must_haves:
  truths:
    - "`EventBus.publish(brokerEvent)` invoca `validateEvent` (step 3) e poi consegna ai subscriber matched (step 13)"
    - "Tutti i 5 step F1 (`event.received`, `event.metadata.enriched`, `event.validated`, `event.dedupe.checked`, `event.delivered`) chiamano `tap.onPipelineStep(step, snapshot)` (CORE-13 + D-20)"
    - "Default `deliveryMode: 'async'` consegna via `queueMicrotask` (D-01) — publish ritorna prima della consegna ai handler"
    - "Override `deliveryMode: 'sync'` consegna immediatamente (D-02)"
    - "`deliveryMode: 'worker'` o `'remote'` in F1 viene mappato su async + warn `mapping.delivery.fallback` (D-03)"
    - "`subscribe(pattern, handler)` ritorna Subscription con .unsubscribe() idempotente (D-27)"
    - "Handler che lancia eccezione → caught + logged + `system.error` pubblicato (CORE-12 + D-16); broker non collassa"
    - "Handler async rejected Promise → stesso treatment (caught via .catch())"
    - "`subscribe` con `signal: AbortSignal` → unsubscribe automatico su abort"
    - "`unsubscribeByOwner(pluginId)` rimuove tutte le subscription con `ownerId === pluginId` e ritorna il count"
    - "`SubscribeOptions.once: true` → handler invocato max una volta, poi auto-unsubscribe (decisione plan 03)"
  artifacts:
    - path: "packages/core/src/core/bus.ts"
      provides: "EventBus class con publish/subscribe/unsubscribe/dispatch + handler isolation + tap orchestration"
      contains: "EventBus"
      min_lines: 150
  key_links:
    - from: "packages/core/src/core/bus.ts"
      to: "packages/core/src/core/topic-matcher.ts"
      via: "import { TopicTrie, validateTopic } per subscribe/match"
      pattern: "TopicTrie"
    - from: "packages/core/src/core/bus.ts"
      to: "packages/core/src/core/deep-freeze.ts"
      via: "import { deepFreeze } per freezeForDelivery in dev mode"
      pattern: "deepFreeze"
    - from: "packages/core/src/core/bus.ts"
      to: "packages/core/src/core/event-tap.ts"
      via: "import { safeTapStep } per ogni step pipeline"
      pattern: "safeTapStep"
    - from: "packages/core/src/core/bus.ts"
      to: "packages/core/src/core/event-validator.ts"
      via: "import { validateEvent } per step 3"
      pattern: "validateEvent"
    - from: "packages/core/src/core/bus.ts"
      to: "packages/core/src/core/broker-error.ts"
      via: "import { createBrokerError, isBrokerError } per system.error pubblicato in handler isolation"
      pattern: "isBrokerError"
---

<objective>
Implementare `EventBus` (bus.ts) — il cuore del broker: dispatch pub/sub con handler isolation, tap orchestration sui 5 step F1, deep-freeze in dev mode, supporto a `deliveryMode: 'async'/'sync'/'worker'/'remote'` (D-01..D-03), AbortSignal-first subscribe, e `unsubscribeByOwner` per cascade plugin cleanup (LIFE-02 in plan 08).

Coverage: CORE-01 (event bus pub/sub), CORE-09 (wildcard delivery via TopicTrie), CORE-12 (handler isolation try/catch + system.error publish), ERR-03 (errori non collassano runtime).

Purpose: questo è il modulo centrale; tutti i moduli di plan 04/05/06 confluiscono qui. Plan 08 (Broker class + PluginRegistry + factory) compone EventBus con TopicRegistry e PluginRegistry.

Output: `bus.ts` con classe `EventBus` + suite unit test che verifica dispatch sync/async, handler isolation con system.error publish, tap chiamati nei 5 step, subscription idempotente, AbortSignal hookup, unsubscribeByOwner. Coverage v8 ≥ 90%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-essenziale/01-CONTEXT.md
@.planning/phases/01-core-essenziale/01-RESEARCH.md
@.planning/phases/01-core-essenziale/01-04-SUMMARY.md
@.planning/phases/01-core-essenziale/01-05-SUMMARY.md
@.planning/phases/01-core-essenziale/01-06-SUMMARY.md
@packages/core/src/types/broker-event.ts
@packages/core/src/types/subscription.ts
@packages/core/src/types/tap.ts
@packages/core/src/core/topic-matcher.ts
@packages/core/src/core/deep-freeze.ts
@packages/core/src/core/event-tap.ts
@packages/core/src/core/event-validator.ts
@packages/core/src/core/broker-error.ts
@packages/core/src/core/logger.ts
</context>

<interfaces>
From plan 03 (types):
```typescript
export interface BrokerEvent<T> { id; topic; timestamp; source; payload; deliveryMode?; priority?; ... }
export interface Subscription { readonly id: string; readonly topic: string; readonly active: boolean; unsubscribe(): void }
export interface SubscribeOptions { signal?: AbortSignal; priority?; deliveryMode?: 'sync' | 'async'; once?: boolean }
export type PipelineStep = 'event.received' | 'event.metadata.enriched' | 'event.validated' | 'event.dedupe.checked' | 'event.delivered'
export interface EventTap { onPipelineStep(step, snapshot): void }
export interface BrokerLogger { error(msg, meta?); warn(msg, meta?); info(msg, meta?); debug(msg, meta?); trace(msg, meta?) }
```

From plan 04 (utilities):
```typescript
export function createBrokerError(params): BrokerError
export function isBrokerError(value): value is BrokerError
export function deepFreeze<T>(value, options?): T
export const noopEventTap: EventTap
export function safeTapStep(tap, step, snapshot, onError?): void
```

From plan 05 (matcher + factory + validator):
```typescript
export class TopicTrie<T> { insert(pattern, item); remove(pattern, item); match(topic): T[]; collectAllPatterns(): string[] }
export function validateTopic(topic): void
export function validateEvent(event: unknown): void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bus.ts EventBus implementation + dispatch logic</name>
  <files>packages/core/src/core/bus.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "core/bus.ts — EventBus principale" (snippet completo, ~ 250 righe)
    - .planning/phases/01-core-essenziale/01-RESEARCH.md "Note critiche" sotto bus.ts
    - .planning/phases/01-core-essenziale/01-CONTEXT.md decisioni D-01..D-03 (delivery), D-16 (handler error isolation), D-27 (Subscription idempotente)
  </read_first>
  <action>
**1. Creare `packages/core/src/core/bus.ts`** verbatim da RESEARCH.md sezione "core/bus.ts — EventBus principale":

```ts
import { nanoid } from 'nanoid'
import type { BrokerEvent } from '../types/broker-event'
import type { Subscription, SubscribeOptions } from '../types/subscription'
import type { BrokerLogger } from '../types/logger'
import type { EventTap, PipelineSnapshot } from '../types/tap'
import { TopicTrie, validateTopic } from './topic-matcher'
import { deepFreeze } from './deep-freeze'
import { validateEvent } from './event-validator'
import { createBrokerError, isBrokerError } from './broker-error'
import { safeTapStep } from './event-tap'

interface InternalSubscription {
  id: string
  topic: string                  // pattern (may contain '*')
  handler: (event: BrokerEvent) => void | Promise<void>
  active: boolean
  ownerId?: string
  options: SubscribeOptions
  abortListener?: () => void
}

export class EventBus {
  private trie = new TopicTrie<InternalSubscription>()
  private byId = new Map<string, InternalSubscription>()
  private pendingAsync = 0
  private debugMode: boolean

  constructor(
    private readonly logger: BrokerLogger,
    private readonly tap: EventTap,
    options: { debug: boolean },
  ) {
    this.debugMode = options.debug
  }

  setDebugMode(enabled: boolean): void {
    this.debugMode = enabled
  }

  publish<T>(event: BrokerEvent<T>): void {
    // Step 1: received
    safeTapStep(this.tap, 'event.received', this.snap('event.received', event))

    // Step 2: enrich (id/timestamp already set by event-factory)
    safeTapStep(this.tap, 'event.metadata.enriched', this.snap('event.metadata.enriched', event))

    // Step 3: validate (VAL-01)
    try {
      validateEvent(event)
    } catch (err) {
      this.logger.error('Event validation failed', { eventId: event.id, error: err })
      throw err
    }
    safeTapStep(this.tap, 'event.validated', this.snap('event.validated', event))

    // Step 7-base: dedupe check (placeholder — full dedupe in F3)
    safeTapStep(this.tap, 'event.dedupe.checked', this.snap('event.dedupe.checked', event))

    // Step 13: delivery
    const matches = this.trie.match(event.topic)
    const frozenEvent = this.debugMode ? this.freezeForDelivery(event) : event

    const mode = event.deliveryMode ?? 'async'
    if (mode === 'sync') {
      this.dispatchSync(matches, frozenEvent)
    } else {
      if (mode === 'worker' || mode === 'remote') {
        this.logger.warn('mapping.delivery.fallback', { mode, fallback: 'async' })
      }
      this.dispatchAsync(matches, frozenEvent)
    }

    safeTapStep(this.tap, 'event.delivered', this.snap('event.delivered', frozenEvent, {
      metadata: { subscriberCount: matches.length },
    }))
  }

  subscribe(
    pattern: string,
    handler: (event: BrokerEvent) => void | Promise<void>,
    options: SubscribeOptions = {},
    ownerId?: string,
  ): Subscription {
    // Light validation: replace '*' with placeholder for validateTopic
    // Full pattern validation done by trie.insert via validateTopicPattern
    const id = nanoid()
    const sub: InternalSubscription = {
      id,
      topic: pattern,
      handler,
      active: true,
      ...(ownerId !== undefined && { ownerId }),
      options,
    }

    this.trie.insert(pattern, sub)
    this.byId.set(id, sub)

    if (options.signal) {
      const listener = (): void => this.unsubscribeInternal(id)
      options.signal.addEventListener('abort', listener)
      sub.abortListener = listener
    }

    const handle: Subscription = {
      get id() { return id },
      get topic() { return pattern },
      get active() { return sub.active },
      unsubscribe: () => this.unsubscribeInternal(id),
    }
    return handle
  }

  unsubscribeInternal(id: string): void {
    const sub = this.byId.get(id)
    if (!sub || !sub.active) return  // idempotent (D-27)
    sub.active = false
    this.trie.remove(sub.topic, sub)
    this.byId.delete(id)
    if (sub.abortListener && sub.options.signal) {
      sub.options.signal.removeEventListener('abort', sub.abortListener)
    }
  }

  unsubscribeByOwner(ownerId: string): number {
    let count = 0
    for (const [id, sub] of this.byId) {
      if (sub.ownerId === ownerId) {
        this.unsubscribeInternal(id)
        count++
      }
    }
    return count
  }

  getStats(): {
    topics: string[]
    subscriberCount: Record<string, number>
    pendingAsyncDelivery: number
  } {
    const topics = this.trie.collectAllPatterns()
    const subscriberCount: Record<string, number> = {}
    for (const sub of this.byId.values()) {
      subscriberCount[sub.topic] = (subscriberCount[sub.topic] ?? 0) + 1
    }
    return { topics, subscriberCount, pendingAsyncDelivery: this.pendingAsync }
  }

  private dispatchSync(matches: InternalSubscription[], event: BrokerEvent): void {
    for (const sub of matches) {
      if (!sub.active) continue
      this.runHandler(sub, event)
    }
  }

  private dispatchAsync(matches: InternalSubscription[], event: BrokerEvent): void {
    if (matches.length === 0) return
    this.pendingAsync++
    queueMicrotask(() => {
      this.pendingAsync--
      for (const sub of matches) {
        if (!sub.active) continue
        this.runHandler(sub, event)
      }
    })
  }

  private runHandler(sub: InternalSubscription, event: BrokerEvent): void {
    try {
      const result = sub.handler(event)
      if (result && typeof (result as Promise<void>).catch === 'function') {
        (result as Promise<void>).catch((err: unknown) => this.handleHandlerError(sub, event, err))
      }
      // Apply { once: true } AFTER successful invocation (D-27 + plan 03 decision)
      if (sub.options.once && sub.active) {
        this.unsubscribeInternal(sub.id)
      }
    } catch (err) {
      this.handleHandlerError(sub, event, err)
    }
  }

  private handleHandlerError(sub: InternalSubscription, event: BrokerEvent, err: unknown): void {
    this.logger.error('Plugin handler threw', {
      subscriptionId: sub.id,
      topic: event.topic,
      eventId: event.id,
      ownerId: sub.ownerId,
      error: err,
    })
    const sysError = isBrokerError(err)
      ? err
      : createBrokerError({
          code: 'plugin.handler.failed',
          category: 'plugin',
          message: err instanceof Error ? err.message : String(err),
          ...(err instanceof Error && { originalError: err }),
          eventId: event.id,
          topic: event.topic,
        })
    // Defer system.error publish to next microtask to avoid recursion
    queueMicrotask(() => {
      try {
        this.publish({
          id: nanoid(),
          topic: 'system.error',
          timestamp: Date.now(),
          source: { type: 'system', id: 'broker' },
          payload: { error: sysError, originalEventId: event.id, originalTopic: event.topic } as never,
          priority: 'critical',
          deliveryMode: 'async',
        })
      } catch {
        this.logger.error('Failed to publish system.error', { error: sysError })
      }
    })
  }

  private freezeForDelivery<T>(event: BrokerEvent<T>): BrokerEvent<T> {
    deepFreeze(event.payload)
    if (event.metadata) deepFreeze(event.metadata)
    Object.freeze(event)
    return event
  }

  private snap(
    step: PipelineSnapshot['step'],
    event: BrokerEvent,
    extras: Partial<PipelineSnapshot> = {},
  ): PipelineSnapshot {
    return {
      eventId: event.id,
      topic: event.topic,
      step,
      timestamp: Date.now(),
      durationMs: 0,
      ...(this.debugMode && { payloadAfter: event.payload }),
      ...extras,
    }
  }
}
```

NOTE CRITICHE (RESEARCH.md):
- `dispatchAsync` usa `queueMicrotask` (D-01) — più veloce di `setTimeout(0)`, garantisce publisher esce dallo stack prima della consegna
- `runHandler` cattura sia errori sync (try/catch) sia rejected Promise (`.catch()` su return value) — pattern necessario per D-16 + CORE-12
- `system.error` pubblicato in `queueMicrotask` per evitare recursion durante consegna stessa
- `unsubscribeByOwner` è il metodo che `plugin-registry.ts` (plan 08) chiama in cascade D-26
- `{ once: true }` (plan 03 decision) implementato dopo successful invocation prima del prossimo dispatch

DEVIAZIONE da RESEARCH.md (motivata da `exactOptionalPropertyTypes`):
- L'originale assegna `ownerId: ownerId ?? ''` come stringa vuota; con `exactOptionalPropertyTypes: true` questo è un bug (`ownerId === ''` non è equivalente a undefined). Sostituire con conditional spread `...(ownerId !== undefined && { ownerId })`.
- Stesso pattern per `originalError: err instanceof Error ? err : undefined` → `...(err instanceof Error && { originalError: err })`.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/bus.ts && grep -q "export class EventBus" packages/core/src/core/bus.ts && grep -q "queueMicrotask" packages/core/src/core/bus.ts && grep -q "safeTapStep(this.tap, 'event.received'" packages/core/src/core/bus.ts && grep -q "safeTapStep(this.tap, 'event.metadata.enriched'" packages/core/src/core/bus.ts && grep -q "safeTapStep(this.tap, 'event.validated'" packages/core/src/core/bus.ts && grep -q "safeTapStep(this.tap, 'event.dedupe.checked'" packages/core/src/core/bus.ts && grep -q "safeTapStep(this.tap, 'event.delivered'" packages/core/src/core/bus.ts && grep -q "unsubscribeByOwner" packages/core/src/core/bus.ts && grep -q "system.error" packages/core/src/core/bus.ts && grep -q "mapping.delivery.fallback" packages/core/src/core/bus.ts && pnpm --filter @gluezero/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/bus.ts` esiste e esporta `EventBus` class
    - File contiene `queueMicrotask` (D-01)
    - File chiama `safeTapStep` con tutti e 5 i step F1: `event.received`, `event.metadata.enriched`, `event.validated`, `event.dedupe.checked`, `event.delivered`
    - File contiene metodo `unsubscribeByOwner(ownerId): number` (per cascade in plan 08)
    - File pubblica `system.error` in handler isolation (CORE-12)
    - File logga warning `mapping.delivery.fallback` per `worker`/`remote` deliveryMode (D-03)
    - Idempotenza unsubscribe: `unsubscribeInternal` ha early return se `!sub.active` (D-27)
    - `pnpm --filter @gluezero/core typecheck` esce 0 (no TS errors)
  </acceptance_criteria>
  <done>EventBus class implementata. Pronto per suite unit test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bus.test.ts unit suite</name>
  <files>packages/core/src/core/bus.test.ts</files>
  <read_first>
    - .planning/phases/01-core-essenziale/01-RESEARCH.md sezione "Test patterns" tabella `bus.integration.test.ts`
    - packages/core/src/core/bus.ts (creato in Task 1)
  </read_first>
  <behavior>
    - Test (CORE-01 pub/sub): publish + subscribe end-to-end con deliveryMode 'sync' → handler invocato con event payload corretto
    - Test (CORE-09 wildcard): subscribe a `weather.*`, publish `weather.requested` → handler invocato
    - Test (D-01 async default): publish con deliveryMode 'async' → handler NON invocato sincronamente; dopo `await Promise.resolve()` → invocato
    - Test (D-03 worker/remote fallback): publish con deliveryMode 'worker' → logger.warn invocato con `mapping.delivery.fallback`; handler comunque invocato (async)
    - Test (FIFO): publish A, B, C async → handler riceve nell'ordine A, B, C
    - Test (D-27 idempotente): `subscription.unsubscribe()` due volte non rompe nulla; second call no-op
    - Test: dopo unsubscribe, publish stesso topic non invoca più l'handler
    - Test (AbortSignal hookup): subscribe con `signal: ac.signal`, `ac.abort()` → automatic unsubscribe; publish dopo abort non invoca handler
    - Test (CORE-12 handler isolation sync): handler che lancia eccezione → broker non collassa; logger.error chiamato; system.error pubblicato dopo microtask
    - Test (CORE-12 handler isolation async): handler che ritorna Promise rejected → idem
    - Test (system.error payload): system.error pubblicato include `error: BrokerError`, `originalEventId`, `originalTopic`
    - Test (5 tap step): publish event con tap spy → tutti 5 step invocati nell'ordine corretto
    - Test (debug mode freeze): debug:true → payload frozen prima della consegna; subscriber tenta `event.payload.x = 'y'` → TypeError
    - Test (debug mode off): debug:false → payload non frozen
    - Test (unsubscribeByOwner): subscribe 3 con ownerId='p1' + 2 con ownerId='p2'; `unsubscribeByOwner('p1')` ritorna 3; getStats mostra solo 2 subscriber attivi
    - Test (once option): subscribe con `once: true` → handler invocato solo una volta; seconda publish non invoca
    - Test (validateEvent fail at publish): publish con event invalid → throws BrokerError code='event.validation.failed'; nessun handler invocato; tap.event.delivered NON chiamato
  </behavior>
  <action>
**1. Creare `packages/core/src/core/bus.test.ts`:**

```ts
import { describe, it, expect, vi } from 'vitest'
import { EventBus } from './bus'
import { silentLogger } from './logger'
import { noopEventTap } from './event-tap'
import { isBrokerError } from './broker-error'
import type { BrokerEvent } from '../types/broker-event'
import type { EventTap, PipelineSnapshot, PipelineStep } from '../types/tap'

const makeBus = (opts: { debug?: boolean; tap?: EventTap; logger?: ReturnType<typeof vi.fn> | typeof silentLogger } = {}) => {
  return new EventBus(
    (opts.logger ?? silentLogger) as ReturnType<typeof silentLogger.error> extends void ? typeof silentLogger : never,
    opts.tap ?? noopEventTap,
    { debug: opts.debug ?? false },
  )
}

const makeEvent = <T>(topic: string, payload: T, overrides: Partial<BrokerEvent<T>> = {}): BrokerEvent<T> => ({
  id: 'evt-1',
  topic,
  timestamp: Date.now(),
  source: { type: 'plugin', id: 'p1' },
  payload: payload as never,
  deliveryMode: 'sync',
  priority: 'normal',
  ...overrides,
} as BrokerEvent<T>)

const flush = () => new Promise<void>((r) => queueMicrotask(() => r()))

describe('EventBus.publish + subscribe (CORE-01)', () => {
  it('delivers sync event to subscriber', () => {
    const bus = makeBus()
    const handler = vi.fn()
    bus.subscribe('weather.requested', handler)
    bus.publish(makeEvent('weather.requested', { city: 'Roma' }))
    expect(handler).toHaveBeenCalledTimes(1)
    expect((handler.mock.calls[0]![0] as BrokerEvent<{city:string}>).payload).toEqual({ city: 'Roma' })
  })

  it('delivers wildcard match (CORE-09)', () => {
    const bus = makeBus()
    const handler = vi.fn()
    bus.subscribe('weather.*', handler)
    bus.publish(makeEvent('weather.requested', { x: 1 }))
    bus.publish(makeEvent('weather.loaded', { y: 2 }))
    expect(handler).toHaveBeenCalledTimes(2)
  })

  it('async delivery (D-01) defers handler to microtask', async () => {
    const bus = makeBus()
    const handler = vi.fn()
    bus.subscribe('a.b', handler)
    bus.publish(makeEvent('a.b', {}, { deliveryMode: 'async' }))
    expect(handler).not.toHaveBeenCalled()  // not yet
    await flush()
    expect(handler).toHaveBeenCalledTimes(1)
  })

  it('worker/remote fallback to async + warn (D-03)', async () => {
    const warn = vi.fn()
    const logger = { ...silentLogger, warn }
    const bus = new EventBus(logger, noopEventTap, { debug: false })
    const handler = vi.fn()
    bus.subscribe('a.b', handler)
    bus.publish(makeEvent('a.b', {}, { deliveryMode: 'worker' }))
    expect(warn).toHaveBeenCalledWith('mapping.delivery.fallback', expect.objectContaining({ mode: 'worker', fallback: 'async' }))
    await flush()
    expect(handler).toHaveBeenCalledTimes(1)
  })

  it('preserves FIFO order under async delivery', async () => {
    const bus = makeBus()
    const log: number[] = []
    bus.subscribe('a.b', (e) => { log.push((e.payload as { n: number }).n) })
    bus.publish(makeEvent('a.b', { n: 1 }, { deliveryMode: 'async' }))
    bus.publish(makeEvent('a.b', { n: 2 }, { deliveryMode: 'async' }))
    bus.publish(makeEvent('a.b', { n: 3 }, { deliveryMode: 'async' }))
    await flush()
    await flush()
    expect(log).toEqual([1, 2, 3])
  })
})

describe('Subscription handle (D-27, CORE-02)', () => {
  it('unsubscribe is idempotent', () => {
    const bus = makeBus()
    const handler = vi.fn()
    const sub = bus.subscribe('a.b', handler)
    sub.unsubscribe()
    sub.unsubscribe()  // second call no-op
    bus.publish(makeEvent('a.b', {}))
    expect(handler).not.toHaveBeenCalled()
  })

  it('exposes id, topic, active', () => {
    const bus = makeBus()
    const sub = bus.subscribe('a.b', () => {})
    expect(typeof sub.id).toBe('string')
    expect(sub.topic).toBe('a.b')
    expect(sub.active).toBe(true)
    sub.unsubscribe()
    expect(sub.active).toBe(false)
  })
})

describe('AbortSignal hookup', () => {
  it('aborts the subscription when signal fires', () => {
    const bus = makeBus()
    const ac = new AbortController()
    const handler = vi.fn()
    const sub = bus.subscribe('a.b', handler, { signal: ac.signal })
    ac.abort()
    bus.publish(makeEvent('a.b', {}))
    expect(handler).not.toHaveBeenCalled()
    expect(sub.active).toBe(false)
  })
})

describe('Handler isolation (CORE-12, ERR-03, D-16)', () => {
  it('sync handler that throws does not crash broker', async () => {
    const errorLog = vi.fn()
    const logger = { ...silentLogger, error: errorLog }
    const bus = new EventBus(logger, noopEventTap, { debug: false })
    const sysHandler = vi.fn()
    bus.subscribe('system.error', sysHandler)
    bus.subscribe('a.b', () => { throw new Error('boom') })
    expect(() => bus.publish(makeEvent('a.b', {}))).not.toThrow()
    expect(errorLog).toHaveBeenCalled()
    await flush()
    await flush()
    expect(sysHandler).toHaveBeenCalled()
    const sysEvent = sysHandler.mock.calls[0]![0] as BrokerEvent<{ error: { code: string }; originalEventId: string; originalTopic: string }>
    expect(sysEvent.topic).toBe('system.error')
    expect(sysEvent.payload.error.code).toBe('plugin.handler.failed')
    expect(sysEvent.payload.originalTopic).toBe('a.b')
  })

  it('async handler with rejected Promise also caught', async () => {
    const errorLog = vi.fn()
    const logger = { ...silentLogger, error: errorLog }
    const bus = new EventBus(logger, noopEventTap, { debug: false })
    bus.subscribe('a.b', async () => { throw new Error('async boom') })
    bus.publish(makeEvent('a.b', {}))
    await flush()
    await flush()
    expect(errorLog).toHaveBeenCalled()
  })
})

describe('Tap orchestration — 5 F1 steps (CORE-13, D-20)', () => {
  it('invokes tap on all 5 F1 steps in order', () => {
    const steps: PipelineStep[] = []
    const tap: EventTap = {
      onPipelineStep: (step) => { steps.push(step) },
    }
    const bus = makeBus({ tap })
    bus.subscribe('a.b', () => {})
    bus.publish(makeEvent('a.b', {}))
    expect(steps).toEqual([
      'event.received',
      'event.metadata.enriched',
      'event.validated',
      'event.dedupe.checked',
      'event.delivered',
    ])
  })

  it('event.delivered snapshot includes subscriberCount', () => {
    const captured: PipelineSnapshot[] = []
    const tap: EventTap = {
      onPipelineStep: (_step, snap) => { if (snap.step === 'event.delivered') captured.push(snap) },
    }
    const bus = makeBus({ tap })
    bus.subscribe('a.b', () => {})
    bus.subscribe('a.b', () => {})
    bus.publish(makeEvent('a.b', {}))
    expect(captured[0]?.metadata).toEqual({ subscriberCount: 2 })
  })

  it('a tap that throws does not break the pipeline (D-20)', () => {
    const tap: EventTap = {
      onPipelineStep: () => { throw new Error('tap blew up') },
    }
    const bus = makeBus({ tap })
    const handler = vi.fn()
    bus.subscribe('a.b', handler)
    expect(() => bus.publish(makeEvent('a.b', {}))).not.toThrow()
    expect(handler).toHaveBeenCalled()
  })
})

describe('Debug mode + deep-freeze', () => {
  it('debug:true freezes payload before delivery', () => {
    const bus = makeBus({ debug: true })
    bus.subscribe('a.b', (e) => {
      expect(() => {
        ;(e.payload as { x: number }).x = 999
      }).toThrow(TypeError)
    })
    bus.publish(makeEvent('a.b', { x: 1 }))
  })

  it('debug:false does not freeze (production performance)', () => {
    const bus = makeBus({ debug: false })
    let mutated = false
    bus.subscribe('a.b', (e) => {
      try {
        ;(e.payload as { x: number }).x = 999
        mutated = true
      } catch {
        // unexpected in non-debug
      }
    })
    bus.publish(makeEvent('a.b', { x: 1 }))
    expect(mutated).toBe(true)
  })
})

describe('unsubscribeByOwner (cascade for plan 08)', () => {
  it('removes all subscriptions for given ownerId, returns count', () => {
    const bus = makeBus()
    bus.subscribe('a.b', () => {}, {}, 'p1')
    bus.subscribe('c.d', () => {}, {}, 'p1')
    bus.subscribe('e.f', () => {}, {}, 'p1')
    bus.subscribe('g.h', () => {}, {}, 'p2')
    bus.subscribe('i.j', () => {}, {}, 'p2')
    expect(bus.unsubscribeByOwner('p1')).toBe(3)
    expect(bus.getStats().topics.length).toBe(2)
  })
})

describe('once option (plan 03 decision)', () => {
  it('invokes handler at most once then auto-unsubscribes', () => {
    const bus = makeBus()
    const handler = vi.fn()
    bus.subscribe('a.b', handler, { once: true })
    bus.publish(makeEvent('a.b', { n: 1 }))
    bus.publish(makeEvent('a.b', { n: 2 }))
    expect(handler).toHaveBeenCalledTimes(1)
  })
})

describe('validation failure at publish (VAL-01)', () => {
  it('invalid event throws and skips delivery', () => {
    const bus = makeBus()
    const handler = vi.fn()
    bus.subscribe('a.b', handler)
    const invalidEvent = makeEvent('a.b', {})
    // Force invalid: empty id
    const broken = { ...invalidEvent, id: '' } as BrokerEvent
    let caught: unknown = null
    try { bus.publish(broken) } catch (e) { caught = e }
    expect(isBrokerError(caught)).toBe(true)
    expect((caught as { code: string }).code).toBe('event.validation.failed')
    expect(handler).not.toHaveBeenCalled()
  })
})
```

**2. Eseguire suite:**

```bash
pnpm --filter @gluezero/core test bus
```

Atteso: ~ 18-20 test passing. Se uno fallisce per timing async, aggiungere ulteriore `await flush()` (microtask flush) come pattern.
  </action>
  <verify>
    <automated>test -f packages/core/src/core/bus.test.ts && grep -q "EventBus" packages/core/src/core/bus.test.ts && grep -q "system.error" packages/core/src/core/bus.test.ts && grep -q "mapping.delivery.fallback" packages/core/src/core/bus.test.ts && grep -q "5 F1 steps" packages/core/src/core/bus.test.ts && pnpm --filter @gluezero/core test bus 2>&1 | grep -E "Test Files.*1.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/core/src/core/bus.test.ts` esiste con almeno 16 test cases coprenti tutti i comportamenti elencati nel `<behavior>`
    - Test verifica: 5 step pipeline F1 invocati in ordine via tap spy
    - Test verifica: handler throw → broker continua + system.error pubblicato dopo microtask
    - Test verifica: async handler rejected Promise → caught
    - Test verifica: deliveryMode 'worker'/'remote' → warn 'mapping.delivery.fallback'
    - Test verifica: AbortSignal hookup (auto-unsubscribe su abort)
    - Test verifica: unsubscribeByOwner ritorna count
    - Test verifica: `once: true` (single invocation)
    - Test verifica: deep-freeze in debug mode, NOT in production
    - Test verifica: validateEvent fail al publish
    - `pnpm --filter @gluezero/core test bus` esce 0 con tutti i test passing
    - Suite completa post-plan 07: `pnpm --filter @gluezero/core test` riporta `Test Files 10 passed` (4 plan04 + 3 plan05 + 2 plan06 + 1 plan07)
  </acceptance_criteria>
  <done>EventBus testato unitariamente. Tutti i comportamenti chiave coperti. CORE-01, CORE-09, CORE-12, ERR-03 verificati. Pronto per Wave 5 (plan 08 — Broker class composition).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publisher → Bus | Event input non fidato (validato da step 3) |
| Plugin handler → Bus | Handler può throw, infinite loop, mutate event payload |
| EventTap (3rd party) → Bus | Tap può throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | DoS | Plugin handler infinite loop blocca event loop | accept | F1 non implementa timeout su handler (deferred a F3 routes); broker non può proteggere da CPU-bound infinite loop (limite single-threaded JS) |
| T-07-02 | DoS | Plugin handler ricorsivamente publica stesso topic | mitigate | Default `deliveryMode: 'async'` via queueMicrotask (D-01) — la consegna avviene su microtask separato dal publish, lo stack si svuota; non c'è stack overflow |
| T-07-03 | DoS | system.error pubblicato in handler isolation causa nuova handler error → recursion | mitigate | `system.error` pubblicato in `queueMicrotask`; se la pubblicazione stessa fallisce (es. validateEvent throw) il fallback è solo `logger.error('Failed to publish system.error')` (no retry) |
| T-07-04 | Tampering | Subscriber muta event.payload condiviso | mitigate | `freezeForDelivery` in dev mode con deepFreeze (D-04); production type-level `DeepReadonly` (compile-time) + accept dei rischi runtime per performance |
| T-07-05 | Information disclosure | system.error payload include originalError che può contenere PII | accept | Per design — debugging utility; il consumer è responsabile di filtrare PII prima di esporre |
| T-07-06 | DoS | Tap throw nel hot-path delivery degrada performance | mitigate | `safeTapStep` con try/catch esplicito (D-20); errori swallow non propagano |
| T-07-07 | Tampering | Plugin handler unsubscribe altri handler (cross-plugin tampering) | accept | F1 expose `unsubscribeByOwner(ownerId)` come metodo internal del bus (chiamato da plan 08); plugin handler non hanno accesso diretto al bus interno via PluginContext (deferred a F2 per access control) |
| T-07-08 | Spoofing | Plugin handler publica con `source.type: 'system'` impersonando broker | accept | F1 non enforce ownership di source.id; deferred a F2 con plugin-scoped publish wrapper |
</threat_model>

<verification>
- `packages/core/src/core/bus.ts` esiste con `EventBus` class (~ 200+ righe)
- `packages/core/src/core/bus.test.ts` esiste con 16+ test cases
- `pnpm --filter @gluezero/core test bus` esce 0
- `pnpm --filter @gluezero/core typecheck` esce 0
- `pnpm biome check packages/core/src/core/bus.ts packages/core/src/core/bus.test.ts` esce 0
- Suite completa post-Wave-4: `Test Files 10 passed`
- Coverage v8 ≥ 90% su bus.ts
</verification>

<success_criteria>
- CORE-01 ✓ (event bus pub/sub publish/subscribe)
- CORE-09 ✓ (wildcard delivery via TopicTrie integrato)
- CORE-12 ✓ (handler isolation try/catch sync + .catch() async + system.error publish)
- ERR-03 ✓ (errori non collassano runtime — broker continua dopo handler throw)
- D-01..D-03 ✓ (delivery semantics: async default, sync override, worker/remote fallback con warn)
- D-16 ✓ (handler error → system.error con BrokerError.category='plugin')
- D-20 ✓ (tap errors swallowed)
- D-27 ✓ (Subscription idempotente)
- 5 step pipeline F1 emessi sui tap
- Pronto per Plan 08 — Broker class composition
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-essenziale/01-07-SUMMARY.md` con:
- bus.ts implementato (~200+ righe)
- Output di `pnpm --filter @gluezero/core test bus`
- Suite completa "Test Files 10 passed"
- Coverage v8 sul bus.ts
- Decisioni tecniche:
  - `exactOptionalPropertyTypes` deviation: conditional spread invece di `: undefined` (motivato)
  - `once` option implementato dopo successful invocation (non prima del run, per dare ai subscriber l'evento finale)
  - system.error sempre via queueMicrotask per evitare recursion
- Pronto-per: Plan 08 — Broker class composition (PluginRegistry + Broker + public-factory + index.ts)
</output>
