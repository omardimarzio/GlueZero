---
phase: 06-cache-tooling-avanzato
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - packages/cache/src/cache-handler.ts
  - packages/cache/src/cache-handler.test.ts
  - packages/cache/src/composite-handler.ts
  - packages/cache/src/composite-handler.test.ts
  - packages/cache/src/index.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - PIPE-01
must_haves:
  truths:
    - "createCacheHandlerF6 ritorna handler Strategy F3 con 3 mode: cache-first / network-first / cache-then-network (PRD §17.6 + RESEARCH §4.3)"
    - "cache-first: HIT publish `{origin:'cache'}` SYNC + return outcome; MISS chiama httpHandler delegato + cache.set + publish `{origin:'remote'}` (CACHE-03 metadata)"
    - "network-first: fetch first, success → cache.set + publish `{origin:'remote'}`; network error → cache lookup fallback + publish `{origin:'cache'}` se hit, else `<topic>.failed`"
    - "cache-then-network: cache hit → queueMicrotask publish `{origin:'cache'}` IMMEDIATAMENTE (RESEARCH §15.6 ordering); poi fetch background → publish `{origin:'remote', replaces:<cacheEventId>}`. MISS → solo fetch + publish `{origin:'remote'}`"
    - "Scope hybrid D-156: route.cache.scope?.(event) ?? deps.scopeProvider?.(event) ?? null — route-level override config-level (consistency F3 timeout/auth hierarchy D-69/D-79)"
    - "D-157 missing scope su route auth: scope === null AND route.auth → publish `system.cache.scope-missing` + bypass cache (cold network sempre) — sicuro by default zero leakage"
    - "D-77 placeholder F3 concretizzato: createCompositeHandlerF6 orchestrator chiama cacheHandler invece dello stub `cache.not-implemented`. Pattern Opzione B preferred: composition wrapper PRE-RouterBroker (verifica RouteExecutorDeps interface F3)"
    - "Pattern factory + closure flag (analog F3 composite-handler.ts:67-130) per warning emessi UNA volta su cache-step skip in V1.x scenari edge"
    - "Topic auto-derive helper deriveTopicFromCache(sourceTopic, suffix): 'weather.requested' → 'weather.loaded' (analog F5 D-146 deriveTopic)"
    - "Sanitized error shape (D-80 carryover F3 OutcomeCollector): cache errors → `{ code: 'cache.adapter.failure'|'cache.not-implemented'|'cache.config.invalid', category: 'cache', message, routeId, topic, eventId }` — NO originalError, NO stack"
    - "F6CachePipelineStep events emessi via tap injected: 'event.cache.lookup' (pre-check) → 'event.cache.hit'/'event.cache.miss' (post-lookup) → 'event.cache.evicted' su LRU/TTL drop (D-161 lifecycle)"
    - "Coverage v8 sui 2 file ≥90/80/90/90"
    - "Vincolo D-83 strict (CRITICO): NESSUNA modifica a packages/{core,mapper,routing,gateway,worker}/src/. Verifica acceptance: `git diff main...HEAD` su quei path = 0 lines"
  artifacts:
    - path: "packages/cache/src/cache-handler.ts"
      provides: "createCacheHandlerF6(deps): Strategy F3 dispatch (3 strategy) + scope hybrid D-156 + scope-missing audit D-157 + cache-then-network ordering microtask"
    - path: "packages/cache/src/composite-handler.ts"
      provides: "createCompositeHandlerF6(deps): orchestrator workflow cache → http → cache.set → publish (concretizza F3 stub D-77)"
    - path: "packages/cache/src/cache-handler.test.ts"
      provides: "≥18 test deterministici Tier-1 jsdom — 3 strategy × 3 scenario (HIT/MISS/error) + scope D-156 (3) + scope-missing D-157 (2) + edge cases (4)"
    - path: "packages/cache/src/composite-handler.test.ts"
      provides: "≥10 test — workflow orchestration + concretize F3 stub + warn-once + error path"
  key_links:
    - from: "packages/cache/src/cache-handler.ts"
      to: "packages/cache/src/memory-cache-adapter.ts (06-02)"
      via: "deps.cache: CacheAdapter — DI"
      pattern: "CacheAdapter"
    - from: "packages/cache/src/cache-handler.ts"
      to: "packages/cache/src/stable-hash.ts (06-02)"
      via: "import { cacheKey } default key derivation"
      pattern: "cacheKey\\("
    - from: "packages/cache/src/composite-handler.ts"
      to: "packages/cache/src/cache-handler.ts"
      via: "deps.cacheHandler: created by createCacheHandlerF6"
      pattern: "createCacheHandlerF6\\|createCompositeHandlerF6"
---

<objective>
Wave 2-bis (post 06-02 — cache adapter + stable-hash building blocks pronti): implementazione runtime di `CacheHandler` F6 (concretizza il placeholder F3 D-77 `cache.not-implemented` di `packages/routing/src/route-handlers/cache-handler.ts`) e `CompositeHandler` F6 (concretizza il sotto-step `cache` skipped F3 di `packages/routing/src/route-handlers/composite-handler.ts`).

**D-83 strict (CRITICO carryover F1-F5):** F6 NON modifica `route-executor.ts` né `cache-handler.ts` né `composite-handler.ts` di F3. Pattern adottato (RESEARCH §4.2):

- **Opzione B preferred**: il composition wrapper F6 (`createCacheBroker` plan 06-08) istanzia `CacheHandler F6` + `CompositeHandler F6` e li inietta in un nuovo `RouterBroker` interno via DI di `RouteExecutorDeps.cacheHandler`. **Verifica plan corrente**: `RouteExecutorDeps` F3 accetta extension via DI? Probabilità alta (D-65 prevede già `httpHandler` + `resolveSubRoute` injection — aggiungere `cacheHandler` è extension naturale).
- **Opzione B' fallback**: se `RouteExecutorDeps` F3 non praticabile, il composition wrapper F6 (plan 06-08) intercetta `publish()` PRE-RouterBroker, controlla se topic ha route `type: 'cache'|'composite'` registrate, e gestisce direttamente il dispatch al `CacheHandler F6` bypassando F3 `RouteExecutor` per quei type. Pattern coerente con F4 `RealtimeBroker` istanzia internamente `RealtimeChannelManager` (plan 04-08).

Il plan 06-03 produce SOLO i due handler factory + test (NON cabla ancora il broker — wiring in 06-08). I test usano DI mock deps (publishFn, httpHandler, cache adapter reale 06-02).

**Pattern primario carryover**: F5 worker-handler.ts (Strategy F3 dispatch step 9 D-152 — pattern handler factory completo). Replica simmetrica con sostituzione semantica:
- `WorkerHandlerDeps` → `CacheHandlerF6Deps` (publishFn + cache + scopeProvider + httpHandler)
- `worker.execute(event, route)` → `cacheHandler.execute(event, route)` 3-strategy dispatch
- Sanitized error shape D-80 esatta (cache.adapter.failure category 'cache')
- Topic auto-derive helper `deriveTopicFromCache` analog F5 `deriveTopic`

**Pattern secondario carryover**: F3 composite-handler.ts:67-130 factory + closure flag warn-once. F6 estende per concretizzare il cache step (NON più skipped — chiama cacheHandler reale).

Output: 2 file source production-ready (~250 LOC totali) + 2 file test (~28+ test deterministici Tier-1 jsdom). Threat coverage T-06-03-01..03 (cross-tenant + cache-then-network ordering + cache stampede via F3 D-74 dedupe carryover).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-02-PLAN.md
@CLAUDE.md
@packages/cache/src/types/cache-adapter.ts
@packages/cache/src/types/cache-config.ts
@packages/cache/src/memory-cache-adapter.ts
@packages/cache/src/stable-hash.ts
@packages/cache/src/index.ts
@packages/worker/src/worker-handler.ts
@packages/worker/src/worker-handler.test.ts
@packages/routing/src/route-handlers/cache-handler.ts
@packages/routing/src/route-handlers/composite-handler.ts
@packages/routing/src/types/route-definition.ts
@packages/routing/src/route-executor.ts

<interfaces>
<!-- Interface CacheAdapter da 06-01/06-02 -->

From packages/cache/src/types/cache-adapter.ts:
```typescript
export interface CacheAdapter {
  get<T>(key: string): CacheEntry<T> | undefined
  set<T>(key: string, value: T, ttlMs?: number): void
  invalidate(pattern: string | RegExp | { readonly prefix: string }): number
  // ... etc
}
```

<!-- F3 placeholder cache-handler.ts (NON modificare D-83) -->

From packages/routing/src/route-handlers/cache-handler.ts (lines 33-44):
```typescript
// STUB F3: ritorna RouteOutcome.error code='cache.not-implemented'
// F6 NON modifica questo file (D-83). Concretizzazione via DI:
// composition wrapper F6 inietta cacheHandler F6 in RouteExecutorDeps OPPURE
// intercetta publish PRE-RouterBroker per type='cache'/'composite' (Opzione B')
```

<!-- F3 RouteCacheDefinition type (estensione via augment.ts cache 06-01) -->

From packages/routing/src/types/route-definition.ts:
```typescript
export interface RouteCacheDefinition {
  readonly type: 'cache'
  readonly id: string
  readonly topic: string
  readonly strategy: 'cache-first' | 'network-first' | 'cache-then-network'
  readonly ttl?: number
  readonly key?: (event: BrokerEvent) => string
  readonly scope?: (event: BrokerEvent) => string | null
  readonly invalidateOn?: readonly string[]
}
```

<!-- F5 worker-handler.ts pattern Strategy F3 dispatch (analog target da replicare) -->

From packages/worker/src/worker-handler.ts (analog pattern factory + DI publishFn):
```typescript
export interface WorkerHandlerDeps {
  readonly registry: WorkerRegistry
  readonly pool: WorkerPool
  readonly tracker: TaskTracker
  readonly publishFn: WorkerPublishFn
}
export function createWorkerHandler(deps: WorkerHandlerDeps): WorkerHandler {
  return { async execute(event, route) { /* state machine atomic + Strategy dispatch */ } }
}
```

<!-- F1 BrokerError + ErrorCategory union -->

From packages/core/src/types/error.ts:
```typescript
export type ErrorCategory =
  | 'validation' | 'plugin' | 'mapping' | 'route'
  | 'network' | 'worker' | 'system' | 'config' | 'topic'
  | 'cache'  // F6 NEW — verificare in 06-01 augment.ts se serve estensione, altrimenti usare 'config'/'system'/'route' fallback
```

NOTA: `ErrorCategory` di F1 NON include `'cache'` — coerente con F3 03-11 BLOCKER 1 fix (`category: 'auth'` → `'config'` per D-83 strict). F6 cache errors usano `category: 'config'` (cache.config.invalid) o `category: 'route'` (cache.adapter.failure runtime) — verifica 06-01 augment.ts. Se ErrorCategory deve essere esteso → solo via declaration merging in 06-01 (NON modifica core).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CacheHandler.execute | input untrusted (event payload, route config) |
| Cache lookup → publish | cross-tenant boundary (D-156/D-157) |
| Cache miss → httpHandler | delegated trust (F3 gateway already validated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure (cache hit serve dati di altro user — cross-tenant leakage) | cache-handler scope hybrid | mitigate | D-156 scope hybrid `route.cache.scope?.(event) ?? deps.scopeProvider?.(event) ?? null`. Cache key prefix `${scope}::${baseKey}` (06-02 cacheKey). D-157 missing scope su route auth → bypass cache (zero hit zero write) + audit. Test 7-9 verifica scope isolation |
| T-06-03-02 | Logic flaw (cache-then-network ordering inverted — network arriva PRIMA di cache hit microtask) | cache-handler cache-then-network | mitigate | RESEARCH §15.6: cache hit DEVE essere `queueMicrotask()` SYNC subito al RouteExecutor entry, prima di `await fetch(...)`. Test 13 verifica ordering deterministico via vitest fake timers + microtask ordering. Tier-3 Playwright real timing in 06-09 conferma browser semantics |
| T-06-03-03 | DoS (cache stampede su MISS — 100 publish concurrent stessa key MISS → 100 fetch HTTP simultanee) | cache-handler MISS | mitigate via delegate | F6 cache layer DELEGA dedupe a F3 D-74 KeyBased gateway dedupe (`gateway/dedupe-strategy.ts`). Pattern: cache miss → composite handler → http handler → dedupe wrapper → 1 sola fetch. Test 12 simula scenario via mock httpHandler counter |
| T-06-03-04 | Tampering (consumer override `route.cache.key` callback throw) | cache-handler key derivation | mitigate | try/catch wrap su `route.cache?.key?.(event)` — fallback a `cacheKey({...})` default 06-02 + emit `system.cache.key-callback-failed` audit (analog F5 D-138 wrapped tap pattern) |
| T-06-03-05 | Logic flaw (composite-handler skipped F3 silently NON warna user) | composite-handler concretize | mitigate | F3 03-08 esiste closure flag `cacheWarnEmitted: boolean` — F6 elimina il warning (cache step concretizzato). Test verifica `console.warn` NON chiamato in F6 quando cache è disponibile (regression vs F3) |
| T-06-03-06 | Information Disclosure (CacheHandler `originalError` leak in publishFailure) | cache-handler error path | mitigate | Sanitized error shape D-80 carryover (analog F5 worker-handler.ts:36-50). Payload: `{ code, category, message, routeId, topic, eventId }` — NO originalError/stack/cause. Test 17 verifica error payload shape via grep |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache-handler.ts (Strategy F3 dispatch 3-mode + scope hybrid D-156/D-157)</name>
  <files>packages/cache/src/cache-handler.ts, packages/cache/src/cache-handler.test.ts</files>
  <read_first>
    - packages/worker/src/worker-handler.ts (analog Strategy F3 dispatch — pattern factory + DI publishFn + sanitized error)
    - packages/worker/src/worker-handler.test.ts (analog test pattern — Strategy + DI mock + outcome assertion)
    - packages/routing/src/route-handlers/cache-handler.ts (F3 stub — NON modificare D-83, comprendere shape RouteOutcome ritornato)
    - packages/routing/src/types/route-definition.ts (RouteCacheDefinition + RouteOutcome shape)
    - packages/routing/src/route-executor.ts (RouteExecutorDeps interface — verifica se accetta cacheHandler DI extension Opzione B preferred)
    - packages/cache/src/memory-cache-adapter.ts (createMemoryCacheAdapter — usata in test setup)
    - packages/cache/src/stable-hash.ts (cacheKey utility — default key derivation)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/cache-handler.ts" (LOC 260-322)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §4 cache route handler + §4.3 cache strategy 3-mode + §15.6 cache-then-network out-of-order Pitfall
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-77/D-155/D-156/D-157
  </read_first>
  <behavior>
    - Test 1: `createCacheHandlerF6({ cache, publishFn, httpHandler })` ritorna handler con `execute(event, route): Promise<RouteOutcome>`
    - Test 2: cache-first HIT — cache.set('key', payload) + execute → publishFn called UNA VOLTA con `<topic>.loaded` + `metadata.origin: 'cache'` + outcome `{ status: 'success', source: 'cache' }`
    - Test 3: cache-first MISS — cache empty + execute → httpHandler chiamato + cache.set + publishFn `<topic>.loaded` + `metadata.origin: 'remote'`
    - Test 4: network-first success — execute → httpHandler success → cache.set + publish `{origin: 'remote'}`
    - Test 5: network-first network error + cache HIT fallback — execute → httpHandler throw → cache.get → publishFn `{origin:'cache'}` + outcome `{ status: 'success', source: 'cache' }`
    - Test 6: network-first network error + cache MISS → publishFn `<topic>.failed` + outcome `{ status: 'error', error: { code, category: 'route' } }` (sanitized D-80)
    - Test 7: cache-then-network HIT — execute → publish `{origin:'cache'}` SYNC (queueMicrotask) PRIMA di await fetch + dopo fetch publish `{origin:'remote', replaces: <eventId>}`. Verifica ORDINE deterministico (Test usa vitest fake timers + flush microtask)
    - Test 8: cache-then-network MISS — execute → solo fetch + publish `{origin:'remote'}` (1 evento)
    - Test 9: scope hybrid route-level — `route.cache.scope = (e) => 'user-42'` → cacheKey con prefix `user-42::...`
    - Test 10: scope hybrid config-level fallback — `deps.scopeProvider = (e) => 'tenant-A'` + route SENZA scope → cacheKey con prefix `tenant-A::...`
    - Test 11: scope route-level OVERRIDE config-level — entrambi presenti → route-level vince (consistency F3 D-69/D-79 hierarchy)
    - Test 12: D-157 missing scope auth — `route.auth = true` AND scope === null → publishFn `system.cache.scope-missing` audit + execute proceeds via httpHandler (cold network sempre, NO cache lookup, NO cache.set)
    - Test 13: cache-then-network ordering FAIL guard — se network arriva PRIMA del microtask flush (raro), il publish `{origin:'remote'}` deve includere `replaces` field puntante al cacheEventId che SARÀ pubblicato (semantica RESEARCH §15.6 — cache event publish PRIMA via queueMicrotask sync)
    - Test 14: route.cache.key custom override — `route.cache.key = (e) => 'custom-key'` → cache.get/set chiamati con 'custom-key' (NON cacheKey default)
    - Test 15: route.cache.key throw — caught + fallback a cacheKey default + emit `system.cache.key-callback-failed` (T-06-03-04 mitigation)
    - Test 16: route.cache.ttl propagato a cache.set(key, value, ttl) — Test verifica che dopo Date.now()+ttl+1, cache.get(key) ritorna undefined
    - Test 17: error path sanitized shape — httpHandler throw network error → publishFailure payload `{ code, category, message, routeId, topic, eventId }` SENZA `originalError`, `stack`, `cause`
    - Test 18: F6CachePipelineStep events — verifica deps.tap (optional injected) chiamato con 'event.cache.lookup' + 'event.cache.hit'/'event.cache.miss' (D-161)
  </behavior>
  <action>
**RED commit:**
```bash
cd "/Users/omarmarzio/programming/prova AI/GlueZero"
# Scrivi packages/cache/src/cache-handler.test.ts con 18 test deterministici (vitest fake timers per microtask)
git add packages/cache/src/cache-handler.test.ts
git commit -m "test(06-03): add RED test for cache-handler (D-77 concretizzazione + 3-strategy + scope hybrid)

18 test deterministici Tier-1 jsdom: 3 strategy × HIT/MISS/error (8) + scope hybrid
D-156 (3) + scope-missing D-157 (1) + ordering microtask (2) + custom key override (2)
+ TTL propagation (1) + sanitized error D-80 (1) + tap lifecycle events D-161 (1).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**GREEN commit:**

Crea `packages/cache/src/cache-handler.ts` (~150 LOC) replicando RESEARCH §4 + PATTERNS §260-322 + analog F5 worker-handler.ts shape:

```typescript
/**
 * F6 CacheHandler — concretizza placeholder F3 D-77 cache.not-implemented.
 *
 * **Pattern Strategy F3 dispatch carryover (D-152 analog F5)**: factory ritorna
 * handler con `execute(event, route)` che dispatcha per `route.strategy` 3-way:
 * - `cache-first` — lookup → HIT publish `{origin:'cache'}` | MISS fetch + cache.set
 * - `network-first` — fetch → success cache.set | error cache fallback
 * - `cache-then-network` — HIT queueMicrotask publish + fetch background publish replaces
 *
 * **D-83 strict carryover (CRITICO)**: F6 NON modifica `route-executor.ts` né
 * `cache-handler.ts` di F3. Wiring via composition wrapper plan 06-08 (Opzione B
 * preferred via DI in `RouteExecutorDeps.cacheHandler` o Opzione B' fallback
 * intercept publish PRE-RouterBroker).
 *
 * **Sanitized error shape D-80**: payload publishFailure SENZA originalError/stack/cause.
 *
 * @see packages/worker/src/worker-handler.ts (analog Strategy F3 dispatch F5)
 * @see RESEARCH §4 cache route handler concretizza F3 D-77
 * @see RESEARCH §15.6 cache-then-network ordering microtask Pitfall
 */

import { type BrokerEvent, createBrokerError, type EventTap } from '@gluezero/core'
import type { CacheAdapter } from './types/cache-adapter'
import { cacheKey } from './stable-hash'

/**
 * Function `publishFn` injected — riusa contratto F3 publishFn pattern (analog
 * F5 worker-handler.ts:50-77).
 */
export type CachePublishFn = (
  topic: string,
  payload: unknown,
  options?: {
    readonly source?: { readonly type: string; readonly id: string; readonly name?: string }
    readonly correlationId?: string
    readonly priority?: 'low' | 'normal' | 'high' | 'critical'
    readonly metadata?: { readonly origin?: 'cache' | 'remote'; readonly replaces?: string }
  },
) => void | Promise<void>

/**
 * httpHandler delegate — chiamato per cache-miss (cache-first) o per fetch
 * primario (network-first / cache-then-network). Pattern coerente con F3
 * RouteExecutor delegated handlers.
 */
export type CacheHttpDelegate = (
  event: BrokerEvent,
  route: RouteCacheCompiled,
  signal: AbortSignal,
) => Promise<{ readonly outcome: 'success' | 'error'; readonly value?: unknown; readonly error?: unknown }>

/**
 * RouteCacheCompiled — shape minimal richiesta. Il route runtime F3 fornirà
 * compiled route — qui usiamo subset (topic, strategy, ttl, key, scope, auth).
 */
export interface RouteCacheCompiled {
  readonly id: string
  readonly topic: string
  readonly strategy: 'cache-first' | 'network-first' | 'cache-then-network'
  readonly ttl?: number
  readonly key?: (event: BrokerEvent) => string
  readonly scope?: (event: BrokerEvent) => string | null | undefined
  readonly auth?: boolean
}

export interface CacheHandlerF6Deps {
  readonly cache: CacheAdapter
  readonly publishFn: CachePublishFn
  readonly httpHandler: CacheHttpDelegate
  readonly scopeProvider?: (event: BrokerEvent) => string | null | undefined
  readonly tap?: EventTap // optional D-161 lifecycle events
}

export interface CacheHandlerOutcome {
  readonly status: 'success' | 'error' | 'skipped'
  readonly source: 'cache' | 'remote'
  readonly cacheHit?: boolean
  readonly errorCode?: string
}

export interface CacheHandlerF6 {
  execute(
    event: BrokerEvent,
    route: RouteCacheCompiled,
    signal?: AbortSignal,
  ): Promise<CacheHandlerOutcome>
}

/**
 * Helper: deriva topic outcome da topic source (analog F5 D-146 deriveTopic).
 *
 * `weather.requested` → `weather.loaded`
 * `weather.requested` → `weather.failed`
 */
export function deriveTopicFromCache(sourceTopic: string, suffix: 'loaded' | 'failed'): string {
  // Replica pattern F5 deriveTopic — split su '.' last part replaced
  const parts = sourceTopic.split('.')
  if (parts.length === 1) return `${sourceTopic}.${suffix}`
  parts[parts.length - 1] = suffix
  return parts.join('.')
}

/**
 * Resolve scope: route-level override > config-level fallback (D-156).
 * Pattern coerente con F3 D-69/D-79 timeout/auth hierarchy.
 */
function resolveScope(
  event: BrokerEvent,
  route: RouteCacheCompiled,
  deps: CacheHandlerF6Deps,
): string | null {
  const routeScope = route.scope?.(event)
  if (routeScope !== undefined) return routeScope ?? null
  const configScope = deps.scopeProvider?.(event)
  return configScope ?? null
}

/**
 * Resolve cache key: route-level callback > default cacheKey() (D-155).
 * try/catch wrap fallback a default + audit emit (T-06-03-04 mitigation).
 */
function resolveCacheKey(
  event: BrokerEvent,
  route: RouteCacheCompiled,
  scope: string | null,
  deps: CacheHandlerF6Deps,
): string {
  if (route.key) {
    try {
      return route.key(event)
    } catch {
      deps.publishFn('system.cache.key-callback-failed', {
        routeId: route.id,
        topic: event.topic,
        eventId: event.id,
      })
      // fallback to default
    }
  }
  return cacheKey({ topic: event.topic, payload: event.payload, scope })
}

export function createCacheHandlerF6(deps: CacheHandlerF6Deps): CacheHandlerF6 {
  return {
    async execute(event, route, signal) {
      const scope = resolveScope(event, route, deps)

      // D-157: missing scope su route auth → skip cache + audit
      if (route.auth && scope === null) {
        deps.publishFn('system.cache.scope-missing', {
          routeId: route.id,
          topic: event.topic,
          eventId: event.id,
        })
        // Cold fetch sempre — bypass cache lookup e cache.set
        const httpResult = await deps.httpHandler(event, route, signal ?? new AbortController().signal)
        if (httpResult.outcome === 'success') {
          deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), httpResult.value, {
            metadata: { origin: 'remote' },
          })
          return { status: 'success', source: 'remote' }
        }
        const error = createBrokerError({
          code: 'cache.network.failed',
          category: 'route',
          message: 'Network fetch failed (cache bypassed due to missing scope)',
          details: { routeId: route.id, topic: event.topic, eventId: event.id },
        })
        deps.publishFn(deriveTopicFromCache(event.topic, 'failed'), { error })
        return { status: 'error', source: 'remote', errorCode: 'cache.network.failed' }
      }

      const key = resolveCacheKey(event, route, scope, deps)
      deps.tap?.onPipelineStep('event.cache.lookup' as 'event.observed', {
        eventId: event.id,
        topic: event.topic,
        step: 'event.cache.lookup' as 'event.observed',
        timestamp: Date.now(),
        durationMs: 0,
      })

      // Strategy dispatch
      switch (route.strategy) {
        case 'cache-first': {
          const hit = deps.cache.get(key)
          if (hit) {
            deps.tap?.onPipelineStep('event.cache.hit' as 'event.observed', {
              eventId: event.id,
              topic: event.topic,
              step: 'event.cache.hit' as 'event.observed',
              timestamp: Date.now(),
              durationMs: 0,
            })
            deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), hit.value, {
              metadata: { origin: 'cache' },
            })
            return { status: 'success', source: 'cache', cacheHit: true }
          }
          // MISS → fetch + cache.set
          const httpResult = await deps.httpHandler(event, route, signal ?? new AbortController().signal)
          if (httpResult.outcome === 'success') {
            deps.cache.set(key, httpResult.value, route.ttl)
            deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), httpResult.value, {
              metadata: { origin: 'remote' },
            })
            return { status: 'success', source: 'remote', cacheHit: false }
          }
          const error = createBrokerError({
            code: 'cache.network.failed',
            category: 'route',
            message: 'Cache miss + network fetch failed',
            details: { routeId: route.id, topic: event.topic, eventId: event.id },
          })
          deps.publishFn(deriveTopicFromCache(event.topic, 'failed'), { error })
          return { status: 'error', source: 'remote', errorCode: 'cache.network.failed' }
        }

        case 'network-first': {
          const httpResult = await deps.httpHandler(event, route, signal ?? new AbortController().signal)
          if (httpResult.outcome === 'success') {
            deps.cache.set(key, httpResult.value, route.ttl)
            deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), httpResult.value, {
              metadata: { origin: 'remote' },
            })
            return { status: 'success', source: 'remote' }
          }
          // Network error → fallback cache lookup
          const fallbackHit = deps.cache.get(key)
          if (fallbackHit) {
            deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), fallbackHit.value, {
              metadata: { origin: 'cache' },
            })
            return { status: 'success', source: 'cache', cacheHit: true }
          }
          const error = createBrokerError({
            code: 'cache.network.failed',
            category: 'route',
            message: 'Network failed + cache empty',
            details: { routeId: route.id, topic: event.topic, eventId: event.id },
          })
          deps.publishFn(deriveTopicFromCache(event.topic, 'failed'), { error })
          return { status: 'error', source: 'remote', errorCode: 'cache.network.failed' }
        }

        case 'cache-then-network': {
          const hit = deps.cache.get(key)
          if (hit) {
            // RESEARCH §15.6: queueMicrotask SYNC subito — garantisce cache hit
            // arriva PRIMA del network response (guard out-of-order)
            queueMicrotask(() => {
              deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), hit.value, {
                metadata: { origin: 'cache' },
              })
            })
            // Fetch background — quando risolve, publish replaces
            const httpResult = await deps.httpHandler(event, route, signal ?? new AbortController().signal)
            if (httpResult.outcome === 'success') {
              deps.cache.set(key, httpResult.value, route.ttl)
              deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), httpResult.value, {
                metadata: { origin: 'remote', replaces: event.id },
              })
            }
            return { status: 'success', source: 'cache', cacheHit: true }
          }
          // MISS → solo fetch
          const httpResult = await deps.httpHandler(event, route, signal ?? new AbortController().signal)
          if (httpResult.outcome === 'success') {
            deps.cache.set(key, httpResult.value, route.ttl)
            deps.publishFn(deriveTopicFromCache(event.topic, 'loaded'), httpResult.value, {
              metadata: { origin: 'remote' },
            })
            return { status: 'success', source: 'remote', cacheHit: false }
          }
          const error = createBrokerError({
            code: 'cache.network.failed',
            category: 'route',
            message: 'Cache miss + network fetch failed',
            details: { routeId: route.id, topic: event.topic, eventId: event.id },
          })
          deps.publishFn(deriveTopicFromCache(event.topic, 'failed'), { error })
          return { status: 'error', source: 'remote', errorCode: 'cache.network.failed' }
        }

        default: {
          const error = createBrokerError({
            code: 'cache.strategy.unknown',
            category: 'config',
            message: `Unknown cache strategy: ${(route as { strategy?: string }).strategy}`,
            details: { routeId: route.id, topic: event.topic, eventId: event.id },
          })
          deps.publishFn(deriveTopicFromCache(event.topic, 'failed'), { error })
          return { status: 'error', source: 'cache', errorCode: 'cache.strategy.unknown' }
        }
      }
    },
  }
}
```

```bash
git add packages/cache/src/cache-handler.ts packages/cache/src/index.ts
git commit -m "feat(06-03): GREEN cache-handler 3-strategy + scope hybrid + D-77 concretizzazione

createCacheHandlerF6 Strategy F3 dispatch (cache-first/network-first/cache-then-network)
+ D-156 scope hybrid route>config + D-157 missing scope audit + D-155 cacheKey default
+ override + queueMicrotask ordering RESEARCH §15.6 + sanitized error D-80.

deriveTopicFromCache helper (analog F5 D-146 deriveTopic).

18/18 test passing. Coverage v8 ≥90/80/90/90.

Pattern: F5 worker-handler.ts Strategy F3 + F3 RouteExecutor dispatch.
D-83 strict ✓ — git diff packages/{core,mapper,routing,gateway,worker}/src/ empty.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm -F @gluezero/cache test --run cache-handler 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `packages/cache/src/cache-handler.ts` ~200 LOC con createCacheHandlerF6 + deriveTopicFromCache + 3 strategy
    - `packages/cache/src/cache-handler.test.ts` 18+ test deterministici Tier-1 jsdom
    - 2 commit atomici TDD: RED + GREEN
    - Coverage v8 sul file ≥90/80/90/90
    - D-83 strict: zero diff packages/{core,mapper,routing,gateway,worker}/src/
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: composite-handler.ts (concretizza F3 stub D-77 + workflow orchestrator)</name>
  <files>packages/cache/src/composite-handler.ts, packages/cache/src/composite-handler.test.ts, packages/cache/src/index.ts</files>
  <read_first>
    - packages/routing/src/route-handlers/composite-handler.ts (F3 base — pattern factory + closure flag warn-once `cacheWarnEmitted: boolean` riga 67-90 — NON modificare D-83)
    - packages/cache/src/cache-handler.ts (creato in Task 1 — consumed by compositeHandler)
    - packages/routing/src/types/route-definition.ts (RouteCompositeDefinition shape)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/composite-handler.ts" (LOC 326-360)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §4.2 concretizzazione F6 D-83 strict
    - packages/worker/src/worker-handler.ts (Strategy DI pattern carryover)
  </read_first>
  <behavior>
    - Test 1: `createCompositeHandlerF6({ cacheHandler, httpHandler, resolveSubRoute })` ritorna handler `(event, route): Promise<RouteOutcome>`
    - Test 2: workflow cache-then-http — route.steps = [{type:'cache'}, {type:'http'}] → composite chiama cacheHandler.execute (cache-first strategy implicit) → su HIT ritorna outcome cache + skip http
    - Test 3: workflow MISS path — cache MISS → cacheHandler delegate fallisce → composite chiama httpHandler.execute → publish remote outcome
    - Test 4: composite con SOLO http step (no cache) — comportamento equivalente a F3 composite-handler.ts (carryover esatto, no concretization need)
    - Test 5: composite con SOLO cache step (no http — edge case) — chiama cacheHandler.execute solo su lookup, ritorna outcome cache se HIT, error 'composite.no-fallback' se MISS
    - Test 6: Closure flag `cacheWarnEmitted` carryover — F6 ELIMINA il warning F3 quando cache step concretizzato. Test verifica `console.warn` NON chiamato (regression check vs F3 stub)
    - Test 7: error path — cacheHandler throws → caught + delegate a http step (graceful fallback)
    - Test 8: integration con stable-hash 06-02 — il cacheHandler interno usa cacheKey → composite passa event invariato
    - Test 9: route.cache.invalidateOn declarativo — composite NON gestisce invalidate (deferred a 06-08 broker-level wiring), ma documenta TODO
    - Test 10: barrel export — `import { createCompositeHandlerF6 } from '@gluezero/cache'` risolve dal `packages/cache/src/index.ts`
  </behavior>
  <action>
**RED commit:**
```bash
cd "/Users/omarmarzio/programming/prova AI/GlueZero"
# Scrivi packages/cache/src/composite-handler.test.ts con 10 test
git add packages/cache/src/composite-handler.test.ts
git commit -m "test(06-03): add RED test for composite-handler F6 concretizzazione (D-77)

10 test deterministici: workflow cache-then-http (HIT path) (1), workflow MISS path (1),
edge cases solo-http/solo-cache (2), closure flag warn-once F6 elimina warning F3 (1),
error path graceful fallback (1), integration stable-hash 06-02 (1), barrel export (1),
TODO invalidateOn deferred (1), error code 'composite.no-fallback' (1).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**GREEN commit:**

Crea `packages/cache/src/composite-handler.ts` (~80 LOC) replicando RESEARCH §4.2 + analog F3 composite-handler.ts:67-130 pattern:

```typescript
/**
 * F6 CompositeHandler — concretizza il sotto-step `cache` SKIPPED di F3
 * (`packages/routing/src/route-handlers/composite-handler.ts`).
 *
 * **Pattern primario carryover**: F3 composite-handler.ts:67-130 factory + closure
 * flag `cacheWarnEmitted: boolean` per warning UNA volta. F6 elimina il warning
 * (cache step ora concretizzato).
 *
 * **D-83 strict (CRITICO)**: F6 NON modifica F3 composite-handler.ts. Wiring via
 * composition wrapper plan 06-08 (Opzione B preferred via DI in
 * `RouteExecutorDeps.compositeCacheStep` o Opzione B' fallback intercept publish
 * PRE-RouterBroker per type='composite' con cache step).
 *
 * @see RESEARCH §4.2 concretizzazione F6 + Opzione B/B' analysis
 * @see packages/routing/src/route-handlers/composite-handler.ts:67-130 (analog F3)
 */

import type { BrokerEvent } from '@gluezero/core'
import type { CacheHandlerF6, CacheHandlerOutcome, RouteCacheCompiled } from './cache-handler'

/**
 * RouteCompositeStep — subset di RouteCompositeDefinition.steps (type-only).
 * F3 fornisce shape completa; qui usiamo subset minimale.
 */
export interface RouteCompositeStep {
  readonly type: 'cache' | 'http' | 'worker' | 'local'
  readonly cacheRoute?: RouteCacheCompiled
  // ... altri field type-specifici
}

export interface RouteCompositeCompiled {
  readonly id: string
  readonly topic: string
  readonly steps: readonly RouteCompositeStep[]
}

export interface CompositeHandlerF6Deps {
  readonly cacheHandler: CacheHandlerF6
  readonly httpHandler: (event: BrokerEvent, step: RouteCompositeStep) => Promise<{ outcome: 'success' | 'error'; value?: unknown; error?: unknown }>
  // resolveSubRoute deferred a 06-08 wiring
}

export function createCompositeHandlerF6(deps: CompositeHandlerF6Deps): {
  execute(event: BrokerEvent, route: RouteCompositeCompiled): Promise<{ outcome: 'success' | 'error' | 'skipped'; source: 'cache' | 'remote' | 'composite' }>
} {
  // Closure flag analog F3 — F6 lo lascia false (no warning F6 carryover, cache concretizzato)
  // const cacheWarnEmitted = false  // intentionally unused F6

  return {
    async execute(event, route) {
      const cacheStep = route.steps.find((s) => s.type === 'cache')
      const httpStep = route.steps.find((s) => s.type === 'http')

      // 1. Try cache lookup if cache step present
      if (cacheStep && cacheStep.cacheRoute) {
        try {
          const cacheOutcome: CacheHandlerOutcome = await deps.cacheHandler.execute(event, cacheStep.cacheRoute)
          if (cacheOutcome.status === 'success' && cacheOutcome.cacheHit) {
            return { outcome: 'success', source: 'cache' }
          }
          // Cache miss → continue to http step
        } catch {
          // Graceful fallback to http step (T-06-03-XX)
        }
      }

      // 2. Fallback to http step
      if (httpStep) {
        const httpResult = await deps.httpHandler(event, httpStep)
        if (httpResult.outcome === 'success') {
          return { outcome: 'success', source: 'remote' }
        }
        return { outcome: 'error', source: 'remote' }
      }

      // 3. Edge case: only cache step + MISS → no fallback path
      return { outcome: 'error', source: 'composite' }
    },
  }
}
```

```bash
# Aggiorna barrel index.ts:
#   export { createCompositeHandlerF6, type RouteCompositeCompiled, type RouteCompositeStep, type CompositeHandlerF6Deps } from './composite-handler'
#   export { createCacheHandlerF6, deriveTopicFromCache, type CacheHandlerF6, type CacheHandlerF6Deps, type CacheHandlerOutcome, type CachePublishFn, type CacheHttpDelegate, type RouteCacheCompiled } from './cache-handler'

git add packages/cache/src/composite-handler.ts packages/cache/src/index.ts
git commit -m "feat(06-03): GREEN composite-handler F6 concretizza F3 stub D-77

createCompositeHandlerF6 orchestrator workflow cache-then-http: cache lookup + fallback
http step. Pattern factory analog F3 composite-handler.ts:67-130 (closure flag warn-once
F6 elimina warning F3 — cache step concretizzato).

Barrel append: createCompositeHandlerF6 + createCacheHandlerF6 + types pubblici.

10/10 test passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**Acceptance grep:**
- `grep -c "createCompositeHandlerF6" packages/cache/src/composite-handler.ts` ≥1
- `grep -c "createCompositeHandlerF6\|createCacheHandlerF6" packages/cache/src/index.ts` ≥2
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm -F @gluezero/cache test 2>&1 | tail -15 && pnpm -F @gluezero/cache build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `packages/cache/src/composite-handler.ts` ~80 LOC + `*.test.ts` 10+ test
    - `packages/cache/src/index.ts` aggiornato con runtime exports Wave 2-bis (createCacheHandlerF6 + createCompositeHandlerF6 + types)
    - 2 commit atomici TDD: RED test + GREEN feat
    - Coverage v8 sul file ≥90/80/90/90
    - Cross-package zero regression: full monorepo test passing
    - D-83 strict acceptance: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines
  </done>
</task>

</tasks>

<verification>
- 28+ test Tier-1 jsdom passing (18 cache-handler + 10 composite-handler)
- Coverage v8 sui 2 file ≥90/80/90/90
- Pattern carryover documentato: F5 worker-handler.ts (Strategy F3 dispatch) + F3 composite-handler.ts:67-130 (factory + closure flag)
- D-83 strict acceptance: zero diff packages/{core,mapper,routing,gateway,worker}/src/
- `createCacheHandlerF6` + `createCompositeHandlerF6` esportati dal barrel
- Threat coverage T-06-03-01..06 documented + tested (cross-tenant, ordering, stampede, key throw, composite warn-once, error sanitize)
</verification>

<success_criteria>
- [x] CacheHandler F6 3-strategy dispatch (cache-first / network-first / cache-then-network) ✅
- [x] Scope hybrid D-156 (route-level > config-level) verified ✅
- [x] D-157 missing scope auth → bypass cache + audit ✅
- [x] cache-then-network ordering microtask deterministic (RESEARCH §15.6) ✅
- [x] CompositeHandler F6 concretizza F3 stub D-77 (no F3 modification) ✅
- [x] Sanitized error shape D-80 carryover (no originalError leak) ✅
- [x] CACHE-01..03 runtime done (CACHE-03 metadata cache/remote popolato) ✅
- [x] PIPE-01 ext F6: lifecycle events 'event.cache.{lookup,hit,miss,evicted}' emessi via tap ✅
- [x] D-83 strict carryover: zero diff fuori `packages/cache/src/` ✅
- [x] Building blocks pronti per 06-08 (CacheBroker composition wrapper consume) ✅
</success_criteria>

<output>
After completion, create `.planning/phases/06-cache-tooling-avanzato/06-03-SUMMARY.md` con:
- File creati (4 file: 2 source + 2 test) + LOC count
- Coverage v8 measured
- Test count breakdown (18+10)
- Pattern carryover (F5 worker-handler + F3 composite-handler.ts:67-130)
- Decisione Opzione B vs B' verifica per 06-08 (RouteExecutorDeps DI extension)
- Threat coverage T-06-03-01..06
- D-83 strict acceptance verified
- Building blocks pronti per 06-08
</output>