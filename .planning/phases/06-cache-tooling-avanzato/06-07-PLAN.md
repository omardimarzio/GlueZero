---
phase: 06-cache-tooling-avanzato
plan: 07
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - packages/devtools/src/pause-controller.ts
  - packages/devtools/src/pause-controller.test.ts
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "createPauseController({ maxQueueSize, publishFn }) ritorna PauseController { pauseTopic, resumeTopic, flushQueue, isPaused, intercept, getSnapshot } — D-168/D-169/D-170"
    - "pauseTopic(topic) → blocca publish del topic specifico. Eventi accodati FIFO in Map<topic, BrokerEvent[]>"
    - "resumeTopic(topic) → flush queue FIFO + delete topic da paused Map (replay automatico via publishFn iniettato)"
    - "flushQueue(topic?) → drop silenzioso + emit `system.queue.flushed { topic, droppedCount, droppedEventIds }` (D-169) + retain paused state (queue empty ma topic ancora paused)"
    - "flushQueue() (no arg) → flush TUTTE le queue paused"
    - "intercept(event) → ritorna 'pass' (no pause / critical bypass) | 'queued' (in pause + cap not reached) | 'dropped' (cap reached, drop-oldest FIFO)"
    - "Critical bypass D-170: event.priority === 'critical' → ritorna 'pass' (consistency Pitfall 4.C — pattern F3 D-75 + F5 D-130 carryover esatto)"
    - "Cap drop-oldest D-170: maxQueueSize=1000 default. Cap raggiunto → drop oldest via queue.shift() + emit `system.queue.overflow { topic, droppedEventId }`"
    - "Coverage v8 sul file ≥90/80/90/90"
    - "File ownership disgiunta da 06-05 e 06-06 (parallel Wave 3): 06-07 owns SOLO pause-controller source+test. NESSUN plan W3 tocca packages/devtools/src/index.ts"
    - "Barrel append packages/devtools/src/index.ts gestito da 06-08b Wave 4b (sequential gate cumulative). 06-07 NON modifica index.ts (BLOCKER-1 fix)."
    - "Vincolo D-83 strict: zero modifiche fuori `packages/devtools/src/`"
  artifacts:
    - path: "packages/devtools/src/pause-controller.ts"
      provides: "createPauseController flow controller pauseTopic/resumeTopic/flushQueue + intercept (D-168/D-169/D-170 + critical bypass D-130/D-75 carryover)"
    - path: "packages/devtools/src/pause-controller.test.ts"
      provides: "≥15 test deterministici Tier-1 jsdom — pauseTopic block (3) + queue FIFO order (3) + resumeTopic replay (2) + flushQueue audit (3) + cap drop-oldest (2) + critical bypass (2)"
  key_links:
    - from: "packages/devtools/src/pause-controller.ts"
      to: "packages/gateway/src/http/strategies/backpressure-strategy.ts"
      via: "pattern carryover D-75 cap+critical bypass (NON modificare F3 — analog only)"
      pattern: "priority === 'critical'\\|drop-oldest"
    - from: "packages/devtools/src/pause-controller.ts"
      to: "packages/devtools/src/types/pause-state.ts"
      via: "PauseAction + FlushQueueResult contract"
      pattern: "PauseAction\\|FlushQueueResult"
---

<objective>
Wave 3 building block C (parallel con 06-05 ∥ 06-06 — file ownership disgiunta verified): implementazione di **PauseController** (`pauseTopic/resumeTopic/flushQueue/intercept` D-168/D-169/D-170 + critical bypass).

**Pattern primario carryover**: F3 `gateway/src/http/strategies/backpressure-strategy.ts:127-160` (D-75 queue-bounded + drop-oldest + critical bypass). Il pattern algoritmico è **identico**, ma la semantica è diversa:
- F3 backpressure = automatic load shedding
- F6 pauseTopic = explicit user-driven block

F6 NON riusa direttamente `BackpressureStrategy` di F3 (semantica diversa) ma replica il pattern algoritmico inline. **D-83 strict carryover esatto**: nessuna modifica a `packages/gateway/`.

**Pattern carryover F5 D-130**: critical priority bypass — pattern uniforme cross-fase (F3 D-75 backpressure-strategy.ts:131 + F5 D-130 worker-pool.ts:39 + F6 D-170 pause-controller).

Output: 1 file source production-ready (~80 LOC) + 1 file test (~15+ test deterministici Tier-1 jsdom). Threat coverage T-06-07-01..03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@CLAUDE.md
@packages/gateway/src/http/strategies/backpressure-strategy.ts
@packages/gateway/src/http/strategies/backpressure-strategy.test.ts
@packages/worker/src/worker-pool.ts
@packages/devtools/src/types/pause-state.ts
@packages/devtools/src/index.ts

<interfaces>
<!-- F3 backpressure-strategy.ts pattern (analog NON modificare D-83) -->

From packages/gateway/src/http/strategies/backpressure-strategy.ts (lines 127-160):
```typescript
// CRITICAL bypass — Pitfall 4 fix.
if (priority === 'critical') {
  return await task()
}
// queue-bounded drop-oldest
case 'queue-bounded': {
  const max = state.policy.max
  if (state.inFlight >= max) {
    if (dropOldest && state.pending.length > 0) {
      const oldest = state.pending.shift()
      // ... drop + audit emit
    }
  }
}
```

<!-- F6 type da 06-01 -->

From packages/devtools/src/types/pause-state.ts:
```typescript
export type PauseAction = 'pass' | 'queued' | 'dropped'
export interface FlushQueueResult {
  readonly topic: string
  readonly droppedCount: number
  readonly droppedEventIds: readonly string[]
}
```

<!-- F1 BrokerEvent shape (per intercept signature) -->

From packages/core/src/types/broker-event.ts:
```typescript
export interface BrokerEvent<T = unknown> {
  readonly id: string
  readonly topic: string
  readonly payload: T
  readonly priority?: 'low' | 'normal' | 'high' | 'critical'
  readonly timestamp: number
  readonly source: { readonly type: string; readonly id: string; readonly name?: string }
  // ...
}
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | DoS (queue cresce illimitato) | pause-controller | mitigate | D-170 cap default 1000 + drop-oldest FIFO. Test 11 verifica cap rispettato |
| T-06-07-02 | Information Disclosure (flushQueue audit leak event ids) | pause-controller flushQueue | accept | eventIds sono nanoid random non-PII. Documented in DOC-06 |
| T-06-07-03 | Logic flaw (pauseTopic + critical bypass race) | pause-controller intercept | mitigate | D-170 critical pass — atomic single-thread JS event loop (D-133 carryover). Test 14 verifica priority='critical' bypassa anche con cap=0 |
| T-06-07-04 | Logic flaw (resumeTopic replay infinite loop — replay event triggera nuovo intercept che ri-accodera) | pause-controller resumeTopic | mitigate | resumeTopic delete topic da paused Map PRIMA di replay → replay events vedono `paused.has(topic) === false` → 'pass'. Test 6 verifica no infinite loop |
| T-06-07-05 | Logic flaw (flushQueue su topic non-paused → no-op silente) | pause-controller flushQueue | accept | Behavior corretto — flushQueue di topic mai paused è semantically no-op (zero entries, zero audit). Test 12 verifica |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pause-controller.ts (pauseTopic + queue + flushQueue + intercept + critical bypass)</name>
  <files>packages/devtools/src/pause-controller.ts, packages/devtools/src/pause-controller.test.ts</files>
  <read_first>
    - packages/gateway/src/http/strategies/backpressure-strategy.ts (analog D-75 cap+critical bypass riga 127-160 — pattern carryover ESATTO inline)
    - packages/gateway/src/http/strategies/backpressure-strategy.test.ts (analog test pattern — cap + drop-oldest + critical bypass)
    - packages/worker/src/worker-pool.ts (analog F5 D-130 critical bypass riga 39)
    - packages/devtools/src/types/pause-state.ts (creato 06-01 — PauseAction + FlushQueueResult)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/pause-controller.ts" (LOC 700-744)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §10 pauseTopic queue impl + §10.2 critical bypass D-170 carryover
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-168/D-169/D-170
  </read_first>
  <behavior>
    - Test 1: createPauseController({ publishFn }) ritorna PauseController { pauseTopic, resumeTopic, flushQueue, isPaused, intercept, getSnapshot }
    - Test 2: pauseTopic('weather.requested') → isPaused === true + queue empty initialized
    - Test 3: intercept(event di topic non-paused) → ritorna 'pass'
    - Test 4: pauseTopic + intercept(event same topic) → ritorna 'queued' + queue.length === 1
    - Test 5: resumeTopic('weather.requested') → publishFn chiamato N volte (1 per ogni event in queue) FIFO order + isPaused === false
    - Test 6: resumeTopic NO infinite loop — events replayed durante resume vedono topic non-paused (T-06-07-04 mitigation)
    - Test 7: flushQueue('topic') con 5 event in queue → publishFn chiamato 1 volta con `system.queue.flushed` + payload `{ topic, droppedCount: 5, droppedEventIds: ['id1',...] }` (D-169)
    - Test 8: flushQueue('topic') → topic ancora paused (queue empty ma `isPaused === true`)
    - Test 9: flushQueue() (no arg) → tutte le queue paused flushed + N audit events (uno per topic)
    - Test 10: flushQueue su topic non-paused → no-op silente, zero audit events (T-06-07-05)
    - Test 11: cap drop-oldest D-170 — maxQueueSize=3, intercept di 4 eventi sequenziali → 4° evento accodato, 1° (oldest) droppato + publishFn `system.queue.overflow { topic, droppedEventId }` chiamato
    - Test 12: cap drop multiple times — 10 eventi con cap=3 → ultimi 3 in queue + 7 audit events `system.queue.overflow`
    - Test 13: critical bypass D-170 — pauseTopic + intercept(event con priority='critical') → ritorna 'pass' (NO queue, NO drop)
    - Test 14: critical bypass + cap raggiunto — pauseTopic, intercept di 1000 normal, intercept di 1 critical → critical passes through (NO drop)
    - Test 15: getSnapshot() ritorna `{ pausedTopics: [...], queueSizes: { topic: count }, maxQueueSize }`
  </behavior>
  <action>
**RED commit:**
```bash
cd "/Users/omarmarzio/programming/prova AI/GlueZero"
git add packages/devtools/src/pause-controller.test.ts
git commit -m "test(06-07): add RED test for pause-controller (D-168/D-169/D-170 + critical bypass)

15 test deterministici: pauseTopic block (3) + queue FIFO order (3) + resumeTopic
replay no-loop (2) + flushQueue audit (3) + cap drop-oldest (2) + critical bypass (2).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**GREEN commit (~80 LOC):**

```typescript
/**
 * F6 PauseController — flow controller `pauseTopic/resumeTopic/flushQueue` (D-168/D-169/D-170).
 *
 * **Pattern primario carryover**: F3 backpressure-strategy.ts:127-160 (D-75
 * queue-bounded + drop-oldest + critical bypass) + F5 worker-pool.ts:39 (D-130
 * critical bypass). Replica algoritmica inline (semantica diversa: F6 = explicit
 * user pause vs F3 = automatic load shedding).
 *
 * **D-170 critical bypass**: `event.priority === 'critical'` → return 'pass'
 * (consistency Pitfall 4.C cross-fase F3+F5+F6).
 *
 * **D-169 flushQueue audit**: emit `system.queue.flushed { topic, droppedCount,
 * droppedEventIds }` SENZA re-publish (replay solo via resumeTopic).
 *
 * @see packages/gateway/src/http/strategies/backpressure-strategy.ts:127-160 (analog F3 D-75)
 * @see RESEARCH §10 pauseTopic queue impl
 */

import type { BrokerEvent } from '@gluezero/core'
import type { FlushQueueResult, PauseAction, PauseControllerSnapshot } from './types/pause-state'

const DEFAULT_MAX_QUEUE_SIZE = 1000 // D-170

export type PausePublishFn = (topic: string, payload: unknown) => void | Promise<void>

export interface PauseController {
  pauseTopic(topic: string): void
  resumeTopic(topic: string): void
  /**
   * @param topic Optional — undefined → flush TUTTE le queue paused
   * @returns Array di FlushQueueResult per ogni topic flushed
   */
  flushQueue(topic?: string): readonly FlushQueueResult[]
  isPaused(topic: string): boolean
  /**
   * @returns 'pass' (no pause / critical bypass) | 'queued' (in pause + cap ok) | 'dropped' (cap raggiunto, oldest droppato)
   */
  intercept(event: BrokerEvent): PauseAction
  getSnapshot(): PauseControllerSnapshot
}

export interface PauseControllerOptions {
  readonly maxQueueSize?: number
  readonly publishFn: PausePublishFn
}

export function createPauseController(opts: PauseControllerOptions): PauseController {
  const cap = opts.maxQueueSize ?? DEFAULT_MAX_QUEUE_SIZE
  const paused = new Map<string, BrokerEvent[]>()

  return {
    pauseTopic(topic) {
      if (!paused.has(topic)) paused.set(topic, [])
    },
    resumeTopic(topic) {
      const queue = paused.get(topic)
      if (!queue) return
      // T-06-07-04 mitigation: delete BEFORE replay (replay events vedono topic non-paused)
      paused.delete(topic)
      // Replay FIFO order
      for (const event of queue) {
        opts.publishFn(event.topic, event.payload)
      }
    },
    flushQueue(topic): readonly FlushQueueResult[] {
      const result: FlushQueueResult[] = []
      const topics = topic ? [topic] : Array.from(paused.keys())
      for (const t of topics) {
        const queue = paused.get(t)
        if (!queue) continue // T-06-07-05: topic non-paused → no-op silente
        const droppedEventIds = queue.map((e) => e.id)
        paused.set(t, []) // empty queue but keep paused state
        const entry: FlushQueueResult = {
          topic: t,
          droppedCount: droppedEventIds.length,
          droppedEventIds,
        }
        result.push(entry)
        // D-169 emit audit
        opts.publishFn('system.queue.flushed', entry)
      }
      return result
    },
    isPaused(topic) {
      return paused.has(topic)
    },
    intercept(event): PauseAction {
      const queue = paused.get(event.topic)
      if (!queue) return 'pass' // topic non-paused → pass through
      // D-170 critical bypass — Pitfall 4.C carryover F3+F5
      if (event.priority === 'critical') return 'pass'
      // D-170 cap + drop-oldest FIFO
      let dropped: PauseAction = 'queued'
      if (queue.length >= cap) {
        const oldest = queue.shift()
        if (oldest) {
          opts.publishFn('system.queue.overflow', {
            topic: event.topic,
            droppedEventId: oldest.id,
          })
          dropped = 'dropped'
        }
      }
      queue.push(event)
      return dropped === 'dropped' ? 'dropped' : 'queued'
    },
    getSnapshot(): PauseControllerSnapshot {
      const queueSizes: Record<string, number> = {}
      for (const [topic, queue] of paused) {
        queueSizes[topic] = queue.length
      }
      return {
        pausedTopics: Array.from(paused.keys()),
        queueSizes,
        maxQueueSize: cap,
      }
    },
  }
}
```

**BLOCKER-1 fix**: questo task NON modifica `packages/devtools/src/index.ts`. Il barrel append cumulativo è gestito esclusivamente in 06-08b Wave 4b sequential gate.

```bash
git add packages/devtools/src/pause-controller.ts
git commit -m "feat(06-07): GREEN pause-controller pauseTopic/resumeTopic/flushQueue (D-168/D-169/D-170)

createPauseController { pauseTopic, resumeTopic, flushQueue, isPaused, intercept,
getSnapshot } + critical bypass D-170 (Pitfall 4.C carryover F3 D-75 + F5 D-130) +
cap drop-oldest + audit emit system.queue.{flushed,overflow}.

Pattern F3 backpressure-strategy.ts:127-160 algoritmico carryover (semantica diversa).
~80 LOC zero deps esterne.

resumeTopic delete-before-replay anti infinite-loop (T-06-07-04).
flushQueue su topic non-paused = no-op silente (T-06-07-05).

NB: barrel packages/devtools/src/index.ts NON modificato qui (BLOCKER-1 fix — append
cumulativo gestito da 06-08b Wave 4b sequential gate).

15/15 test passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**Acceptance grep:**
- `grep -c "priority === 'critical'" packages/devtools/src/pause-controller.ts` ≥1 (D-170 critical bypass verified)
- `grep -c "queue.shift()" packages/devtools/src/pause-controller.ts` ≥1 (drop-oldest FIFO)
- `grep -c "paused.delete(topic)" packages/devtools/src/pause-controller.ts` ≥1 (anti-loop guard T-06-07-04)
- `grep -c "system.queue.flushed\|system.queue.overflow" packages/devtools/src/pause-controller.ts` ≥2 (audit events)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm -F @gluezero/devtools test 2>&1 | tail -15 && pnpm -F @gluezero/devtools build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `pause-controller.ts` ~80 LOC + `*.test.ts` 15+ test
    - 2 commit TDD (RED + GREEN)
    - Coverage v8 sul file ≥90/80/90/90
    - Pattern carryover F3 D-75 + F5 D-130 critical bypass documentato in commit
    - NESSUNA modifica a packages/devtools/src/index.ts (BLOCKER-1 fix — barrel append in 06-08b)
  </done>
</task>

</tasks>

<verification>
- 15+ test Tier-1 jsdom passing
- Coverage v8 sul file ≥90/80/90/90
- File ownership disgiunta da 06-05 e 06-06 verified
- D-83 strict: zero modifiche fuori `packages/devtools/src/`
- Critical bypass pattern uniforme cross-fase verified (F3+F5+F6)
- Anti infinite-loop resumeTopic verified (T-06-07-04)
</verification>

<success_criteria>
- [x] PauseController { pauseTopic, resumeTopic, flushQueue, intercept, isPaused, getSnapshot } ✅
- [x] D-168 pauseTopic block + queue FIFO ✅
- [x] D-169 flushQueue audit emit + retain paused state ✅
- [x] D-170 cap drop-oldest + critical bypass ✅
- [x] Pattern carryover F3 D-75 + F5 D-130 critical bypass ✅
- [x] TOOL-04 runtime done ✅
- [x] D-83 strict ✓ ✅
</success_criteria>

<output>
Crea `.planning/phases/06-cache-tooling-avanzato/06-07-SUMMARY.md` con file/test/coverage/pattern carryover/threat coverage T-06-07-01..05/blocks pronti per 06-08.
</output>