---
phase: 06-cache-tooling-avanzato
plan: 06
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - packages/devtools/src/metrics-collector.ts
  - packages/devtools/src/metrics-collector.test.ts
  - packages/devtools/src/reservoir-sampling.ts
  - packages/devtools/src/reservoir-sampling.test.ts
  - packages/devtools/src/cardinality-cap.ts
  - packages/devtools/src/cardinality-cap.test.ts
autonomous: true
requirements:
  - TOOL-02
  - TOOL-03
  - TOOL-05
must_haves:
  truths:
    - "createMetricsCollector({ histogramSamples, maxLabelCombinations, onCardinalityOverflow }) ritorna MetricsCollector { increment, setGauge, observe, getMetrics, getMetricsDelta } — D-163/D-164/D-165/D-166"
    - "Counter atomic JS event loop (D-133 carryover): `counters.set(key, (counters.get(key) ?? 0) + by)` thread-safe per costruzione single-thread"
    - "getMetrics() ritorna MetricsSnapshot { counters, gauges, histograms } readonly cumulative dal boot (D-164)"
    - "getMetricsDelta(prev) calcola counter delta (current - prev), gauges = current, histograms = current snapshot (D-164 helper)"
    - "Histogram via reservoir sampling Algorithm R Vitter 1985 (D-165): cap default 1024 samples per metric. computeSummary ritorna { count, sum, p50, p90, p99 }"
    - "Cardinality cap (D-166): cap default 100 distinct label combinations per metric base name. Overflow → drop + emit `system.metrics.cardinality-overflow` audit (T-06-06-01 mitigation)"
    - "Labels Prometheus-style flatten: `flatLabels({route_id:'weather',status:'200'})` → `{route_id=\"weather\",status=\"200\"}` (key alphabetical sort + double quote values)"
    - "Naming Prometheus-friendly D-163: `sembridge.<package>.<metric>{<labels>}` formato — verifica via grep nei test"
    - "createReservoir(capacity) + reservoirAdd(state, value) + computeSummary(state) — Algorithm R inline ~30 LOC zero deps (RESEARCH §8.2 cite C2 Vitter 1985)"
    - "Coverage v8 sui 3 file ≥90/80/90/90"
    - "File ownership disgiunta da 06-05 e 06-07 (parallel Wave 3): 06-06 owns SOLO metrics-collector + reservoir-sampling + cardinality-cap"
    - "Vincolo D-83 strict: zero modifiche fuori `packages/devtools/src/`"
  artifacts:
    - path: "packages/devtools/src/metrics-collector.ts"
      provides: "createMetricsCollector counters/gauges/histograms + cardinality cap + tap (D-163/D-164/D-165/D-166) — chiude PRD §39 #10 TOOL-05"
    - path: "packages/devtools/src/reservoir-sampling.ts"
      provides: "createReservoir + reservoirAdd + computeSummary — Algorithm R Vitter 1985 ~30 LOC inline (D-165)"
    - path: "packages/devtools/src/cardinality-cap.ts"
      provides: "checkCardinality + flatLabels helper — cap 100 distinct combinations per base name (D-166)"
    - path: "packages/devtools/src/metrics-collector.test.ts"
      provides: "≥18 test deterministici Tier-1 jsdom — counter atomic (3) + gauge (2) + histogram observe + summary (4) + getMetricsDelta (2) + cardinality cap + audit (3) + flatLabels (2) + naming convention (2)"
    - path: "packages/devtools/src/reservoir-sampling.test.ts"
      provides: "≥10 test — Algorithm R correctness su 10k samples uniform distribution (3) + edge cases (3) + computeSummary p50/p90/p99 vs known dist (4)"
    - path: "packages/devtools/src/cardinality-cap.test.ts"
      provides: "≥8 test — flatLabels alphabetical sort (2) + cardinality limit + drop new combo (3) + audit emit (2) + edge cases empty labels (1)"
  key_links:
    - from: "packages/devtools/src/metrics-collector.ts"
      to: "packages/devtools/src/reservoir-sampling.ts"
      via: "histogram observe consumes ReservoirState"
      pattern: "createReservoir\\|reservoirAdd"
    - from: "packages/devtools/src/metrics-collector.ts"
      to: "packages/devtools/src/cardinality-cap.ts"
      via: "checkCardinality call before insert"
      pattern: "checkCardinality\\|flatLabels"
    - from: "packages/devtools/src/metrics-collector.ts"
      to: "packages/devtools/src/types/metrics.ts"
      via: "MetricsSnapshot + HistogramSummary contract"
      pattern: "MetricsSnapshot\\|HistogramSummary"
---

<objective>
Wave 3 building block B (parallel con 06-05 ∥ 06-07 — file ownership disgiunta verified): implementazione di **MetricsCollector** (counters cumulative + gauges + histograms via reservoir sampling D-163/D-164/D-165) + **ReservoirSampling** (Algorithm R Vitter 1985 inline) + **CardinalityCap** (cap 100 distinct label combinations + audit D-166).

**Plan chiude PRD §39 #10 (TOOL-05 metrics format)**: schema `{ counters, gauges, histograms }` simil-OpenMetrics — naming `sembridge.<package>.<metric>{<labels>}` Prometheus-friendly.

**Pattern primario carryover**: F5 worker-pool.ts:113-160 (counter atomic JS event loop single-threaded) + F5 task-tracker.ts:140-220 (Map<key, state>) + F5 worker-registry.ts:39+104-180 (D-128 cap pattern + console.warn audit).

**NEW pattern**: Reservoir Algorithm R Vitter 1985 inline ~30 LOC (RESEARCH §8.2 cite C2). NON adottato t-digest (~3-5 KB minified, deferred V1.x se profiling richiede p999).

Output: 3 file source production-ready (~150 LOC totali) + 3 file test (~36+ test deterministici Tier-1 jsdom). Threat coverage T-06-06-01..04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-04-PLAN.md
@CLAUDE.md
@packages/devtools/src/types/metrics.ts
@packages/devtools/src/types/devtools-config.ts
@packages/devtools/src/index.ts
@packages/worker/src/worker-pool.ts
@packages/worker/src/worker-registry.ts
@packages/worker/src/task-tracker.ts

<interfaces>
<!-- F6 type da 06-01 -->
From packages/devtools/src/types/metrics.ts:
```typescript
export interface MetricsSnapshot {
  readonly counters: Readonly<Record<string, number>>
  readonly gauges: Readonly<Record<string, number>>
  readonly histograms: Readonly<Record<string, HistogramSummary>>
}
export interface HistogramSummary {
  readonly count: number
  readonly sum: number
  readonly p50: number
  readonly p90: number
  readonly p99: number
}
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | DoS (cardinality explosion via dynamic labels) | metrics-collector + cardinality-cap | mitigate | D-166 cap 100 + audit emit `system.metrics.cardinality-overflow`. Test 11-12 explicit |
| T-06-06-02 | DoS (histogram samples cresce illimitato) | reservoir-sampling | mitigate | D-165 ring buffer 1024 capacity. Test 5 fill 5000 → samples.length === 1024 |
| T-06-06-03 | Logic flaw (counter overflow Number.MAX_SAFE_INTEGER) | metrics-collector | accept | Pattern Prometheus standard. JS number 53-bit ~9e15 — sufficient per anni di traffico web |
| T-06-06-04 | Information Disclosure (metric labels include payload sensibile PII) | metrics-collector | accept | Consumer responsibility. DOC-06 esempio anti-pattern userId label (RESEARCH §15.1) |
| T-06-06-05 | Logic flaw (Math.random predictable seed in test) | reservoir-sampling | mitigate | Test usa fixed seed via `Math.random` mock (vitest spyOn) per determinismo. Production non sensibile a entropia (cache key NON crypto) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reservoir-sampling.ts (Algorithm R Vitter 1985 inline D-165)</name>
  <files>packages/devtools/src/reservoir-sampling.ts, packages/devtools/src/reservoir-sampling.test.ts</files>
  <read_first>
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/reservoir-sampling.ts" (LOC 656-694)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §8 reservoir sampling Algorithm R vs t-digest + §8.2 inline impl
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-165
  </read_first>
  <behavior>
    - Test 1: createReservoir(10) ritorna { samples: Array(10), capacity: 10, count: 0, sum: 0 }
    - Test 2: reservoirAdd fase fill — primi 10 add → samples[0..9] popolati direttamente, count=10
    - Test 3: reservoirAdd fase replace — add 11° → Math.random()*11 → se index<10 sostituisce, altrimenti scarta. count=11
    - Test 4: count cresce indefinitamente (non capped da capacity), samples cap a capacity
    - Test 5: 5000 add con capacity 1024 → state.samples.length === 1024 (max capacity)
    - Test 6: sum cumulative — sum di 100 add { 1, 2, 3, ..., 100 } === 5050
    - Test 7: computeSummary su 0 samples → { count: 0, sum: 0, p50: 0, p90: 0, p99: 0 }
    - Test 8: computeSummary p50 su [1..100] sorted → ~50 (entry index 50 in sorted array of 100)
    - Test 9: computeSummary p90 su [1..100] → ~90
    - Test 10: computeSummary p99 su [1..100] → ~99
    - Test 11: Determinismo via Math.random mock — seed fissato → reservoir deterministico (regression test)
    - Test 12: Distribuzione uniforme (soft assertion) — 10000 add 1..10000 + capacity 1000 → mean(samples) ≈ 5000 (±10%)
  </behavior>
  <action>
GREEN code (~40 LOC):

```typescript
/**
 * F6 ReservoirSampling — Algorithm R (Vitter 1985) inline ~30 LOC.
 *
 * Reservoir mantiene cap N samples random uniform da uno stream illimitato.
 * Trade-off vs t-digest: ~5% errore p50/p90/p99 vs ~1% (RESEARCH §8.1) ma
 * zero deps + ~30 LOC vs +3-5 KB minified. V1.x se profiling richiede p999.
 *
 * @see [Vitter 1985 ACM Trans. Math. Software 11(1)] (cite C2)
 * @see RESEARCH §8 reservoir sampling Algorithm R vs t-digest
 */

import type { HistogramSummary } from './types/metrics'

export interface ReservoirState {
  readonly samples: number[]
  readonly capacity: number
  count: number
  sum: number
}

export function createReservoir(capacity: number): ReservoirState {
  return { samples: new Array(capacity), capacity, count: 0, sum: 0 }
}

export function reservoirAdd(state: ReservoirState, value: number): void {
  state.sum += value
  if (state.count < state.capacity) {
    // Fase fill
    state.samples[state.count] = value
    state.count++
  } else {
    // Fase replace: replace random index con probabilità capacity/(count+1)
    const j = Math.floor(Math.random() * (state.count + 1))
    if (j < state.capacity) {
      state.samples[j] = value
    }
    state.count++
  }
}

export function computeSummary(state: ReservoirState): HistogramSummary {
  const n = Math.min(state.count, state.capacity)
  if (n === 0) return { count: 0, sum: 0, p50: 0, p90: 0, p99: 0 }
  const sorted = state.samples.slice(0, n).sort((a, b) => a - b)
  const pickIdx = (p: number) => Math.min(n - 1, Math.floor(n * p))
  return {
    count: state.count, // total observations (NON capacity!)
    sum: state.sum,
    p50: sorted[pickIdx(0.5)] ?? 0,
    p90: sorted[pickIdx(0.9)] ?? 0,
    p99: sorted[pickIdx(0.99)] ?? 0,
  }
}
```

```bash
git add packages/devtools/src/reservoir-sampling.{ts,test.ts}
git commit -m "feat(06-06): GREEN reservoir-sampling Algorithm R Vitter 1985 inline (D-165)

createReservoir + reservoirAdd + computeSummary ~40 LOC zero deps. Trade-off
~5% errore p50/p90/p99 vs t-digest ~1%, ma zero deps prioritario per budget bundle
stretto (RESEARCH §8.1).

12/12 test passing (incl. soft uniform distribution + determinism via Math.random mock).
Coverage v8 ≥90/80/90/90.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test --run reservoir-sampling 2>&1 | tail -10</automated>
  </verify>
  <done>~40 LOC + 12 test, 2 commit TDD, coverage ≥90/80/90/90</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cardinality-cap.ts (cap 100 distinct combinations + audit D-166)</name>
  <files>packages/devtools/src/cardinality-cap.ts, packages/devtools/src/cardinality-cap.test.ts</files>
  <read_first>
    - packages/worker/src/worker-registry.ts (analog F5 D-128 cap pattern + audit)
    - packages/worker/src/worker-pool.ts (analog cap pool 8 + console.warn)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/cardinality-cap.ts" (LOC 70)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §9 cardinality cap + §9.2 wiring all'audit event + §9.3 mitigation explosion
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-166
  </read_first>
  <behavior>
    - Test 1: flatLabels(undefined) ritorna ''
    - Test 2: flatLabels({}) ritorna ''
    - Test 3: flatLabels({ route_id: 'weather' }) ritorna `{route_id="weather"}`
    - Test 4: flatLabels({ status: '200', route_id: 'weather' }) ritorna `{route_id="weather",status="200"}` (alphabetical sort)
    - Test 5: flatLabels values escape — `{ msg: 'has "quotes"' }` ritorna `{msg="has \"quotes\""}` (JSON-escape)
    - Test 6: createCardinalityTracker({ cap: 5, onOverflow }) — accept fino a 5 distinct combinations per metric base name
    - Test 7: 6° distinct combo → drop + onOverflow chiamato con `{ baseName, droppedLabels: <flatSig> }`
    - Test 8: ripetuta combinazione esistente → accept (no count incremento + no overflow)
    - Test 9: empty labelSig (no labels) → sempre accept (cardinality cap NON applica a metric senza labels)
    - Test 10: base names diversi → cardinality count separato (cap per base name, NON globale)
  </behavior>
  <action>
GREEN code (~50 LOC):

```typescript
/**
 * F6 CardinalityCap — guard contro metric explosion (D-166).
 *
 * Pattern carryover F5 worker-registry.ts:39+104-180 (D-128 cap pool 8 + audit
 * console.warn) + F5 worker-pool.ts:113-160.
 *
 * @see RESEARCH §9 cardinality cap + audit
 */

const DEFAULT_CARDINALITY_CAP = 100 // D-166

/**
 * Prometheus-style flatten labels in deterministic alphabetical sort.
 *
 * @example
 * flatLabels({ route_id: 'weather', status: '200' })
 *   // → '{route_id="weather",status="200"}'
 */
export function flatLabels(labels?: Readonly<Record<string, string>>): string {
  if (!labels) return ''
  const keys = Object.keys(labels).sort()
  if (keys.length === 0) return ''
  return `{${keys.map((k) => `${k}=${JSON.stringify(labels[k] ?? '')}`).join(',')}}`
}

export interface CardinalityTrackerOptions {
  readonly cap?: number
  readonly onOverflow?: (info: { readonly baseName: string; readonly droppedLabels: string }) => void
}

export interface CardinalityTracker {
  /**
   * @returns true se accept (combo new o esistente), false se drop (cap raggiunto)
   */
  check(baseName: string, labelSig: string): boolean
}

export function createCardinalityTracker(opts: CardinalityTrackerOptions = {}): CardinalityTracker {
  const cap = opts.cap ?? DEFAULT_CARDINALITY_CAP
  const cardinality = new Map<string, Set<string>>()

  return {
    check(baseName, labelSig) {
      if (!labelSig) return true // empty labels = no cardinality concern
      let set = cardinality.get(baseName)
      if (!set) {
        set = new Set()
        cardinality.set(baseName, set)
      }
      if (set.has(labelSig)) return true
      if (set.size >= cap) {
        // D-166 emit audit
        opts.onOverflow?.({ baseName, droppedLabels: labelSig })
        return false
      }
      set.add(labelSig)
      return true
    },
  }
}
```

```bash
git add packages/devtools/src/cardinality-cap.{ts,test.ts}
git commit -m "feat(06-06): GREEN cardinality-cap + flatLabels Prometheus-style (D-166)

createCardinalityTracker { check } + flatLabels helper. Cap default 100 distinct
combinations per base name. Overflow → drop + onOverflow audit callback.

Pattern F5 worker-registry D-128 cap + audit carryover. ~50 LOC.

10/10 test passing. Coverage v8 ≥90/80/90/90.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test --run cardinality-cap 2>&1 | tail -10</automated>
  </verify>
  <done>~50 LOC + 10 test, 2 commit TDD, coverage ≥90/80/90/90</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: metrics-collector.ts (counters/gauges/histograms + cardinality wiring + chiude PRD §39 #10)</name>
  <files>packages/devtools/src/metrics-collector.ts, packages/devtools/src/metrics-collector.test.ts, packages/devtools/src/index.ts</files>
  <read_first>
    - packages/devtools/src/reservoir-sampling.ts (creato Task 1)
    - packages/devtools/src/cardinality-cap.ts (creato Task 2)
    - packages/devtools/src/types/metrics.ts (06-01 — MetricsSnapshot + HistogramSummary contract)
    - packages/worker/src/worker-pool.ts (analog F5 counter atomic event loop)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/metrics-collector.ts" (LOC 611-652)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §7 MetricsCollector impl deep dive + §7.2 metriche standard pre-definite + §7.3 counter atomic update
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-163/D-164/D-165/D-166
  </read_first>
  <behavior>
    - Test 1: createMetricsCollector({}) ritorna MetricsCollector { increment, setGauge, observe, getMetrics, getMetricsDelta }
    - Test 2: increment('m', undefined, 1) chiamato 5 volte → counters.m === 5
    - Test 3: increment('m', { topic: 'a' }, 2) + increment('m', { topic: 'b' }, 3) → counters['m{topic="a"}'] === 2, counters['m{topic="b"}'] === 3
    - Test 4: setGauge('g', 42) → gauges.g === 42
    - Test 5: setGauge poi setGauge stessa key → ultimo valore vince (gauge semantics)
    - Test 6: observe('h', 100) + observe('h', 200) → histograms.h.count === 2, sum === 300, p50 ≈ 150
    - Test 7: getMetrics() ritorna snapshot readonly { counters, gauges, histograms }
    - Test 8: getMetricsDelta(prev) — counter delta (current - prev), gauges = current snapshot, histograms = current
    - Test 9: cardinality cap — opts.maxLabelCombinations=2 + 3 distinct combo → 3° combo droppata + onCardinalityOverflow chiamato
    - Test 10: cardinality cap onOverflow → publishFn injected publica `system.metrics.cardinality-overflow` audit (analog F5 cap+console.warn)
    - Test 11: Naming convention `sembridge.<package>.<metric>` verificata via test setup keys (es. `sembridge.cache.hits_total{route_id="weather"}`)
    - Test 12: Histogram summary monotonicity — p50 <= p90 <= p99 sempre (sorted property)
    - Test 13: getMetrics() su empty collector → counters={}, gauges={}, histograms={}
    - Test 14: getMetricsDelta su empty prev → tutti i counters delta === current
    - Test 15: Counter overflow Number.MAX_SAFE_INTEGER edge — accept docs only, test omesso per perf (T-06-06-03)
    - Test 16: increment con `by` negativo — accept (decrement) — pattern Prometheus discourages ma JS Math.imul-like semantics
    - Test 17: observe con value negativo — accept (latency negativi raro, ma non block)
    - Test 18: F6PipelineStep tap chiamato (optional) per step 14 'event.observed' (D-161 attivazione)
  </behavior>
  <action>
GREEN code (~120 LOC):

```typescript
/**
 * F6 MetricsCollector — counters cumulative + gauges + histograms simil-OpenMetrics
 * (D-163/D-164/D-165/D-166). **Chiude PRD §39 #10 (TOOL-05 metrics format)**.
 *
 * Pattern primario carryover F5 worker-pool.ts:113-160 (counter atomic JS event
 * loop) + F5 task-tracker.ts:140-220 (Map<key, state>).
 *
 * Naming convention D-163: `sembridge.<package>.<metric>{<labels>}` Prometheus
 * friendly. Cumulative-only counters (D-164) — `getMetricsDelta(prev)` opzionale.
 *
 * @see RESEARCH §7 MetricsCollector impl deep dive
 */

import type { EventTap } from '@sembridge/core'
import { createCardinalityTracker, flatLabels } from './cardinality-cap'
import { computeSummary, createReservoir, type ReservoirState, reservoirAdd } from './reservoir-sampling'
import type { HistogramSummary, MetricsDelta, MetricsSnapshot } from './types/metrics'

export interface MetricsCollector {
  increment(name: string, labels?: Readonly<Record<string, string>>, by?: number): void
  setGauge(name: string, value: number, labels?: Readonly<Record<string, string>>): void
  observe(name: string, value: number, labels?: Readonly<Record<string, string>>): void
  getMetrics(): MetricsSnapshot
  getMetricsDelta(prev: MetricsSnapshot): MetricsDelta
  /** D-161 lifecycle tap — exposed for wiring in 06-08 composition wrapper */
  readonly tap: EventTap
}

export interface MetricsCollectorOptions {
  readonly histogramSamples?: number
  readonly maxLabelCombinations?: number
  /** Wired al broker.publish in plan 06-08 — emette `system.metrics.cardinality-overflow` */
  readonly onCardinalityOverflow?: (info: { baseName: string; droppedLabels: string }) => void
}

const DEFAULT_HISTOGRAM_SAMPLES = 1024 // D-165
const DEFAULT_MAX_LABEL_COMBINATIONS = 100 // D-166

export function createMetricsCollector(opts: MetricsCollectorOptions = {}): MetricsCollector {
  const samplesCap = opts.histogramSamples ?? DEFAULT_HISTOGRAM_SAMPLES
  const cardinality = createCardinalityTracker({
    cap: opts.maxLabelCombinations ?? DEFAULT_MAX_LABEL_COMBINATIONS,
    onOverflow: opts.onCardinalityOverflow,
  })

  const counters = new Map<string, number>()
  const gauges = new Map<string, number>()
  const histograms = new Map<string, ReservoirState>()

  return {
    increment(name, labels, by = 1) {
      const sig = flatLabels(labels)
      if (!cardinality.check(name, sig)) return
      const key = `${name}${sig}`
      counters.set(key, (counters.get(key) ?? 0) + by)
    },
    setGauge(name, value, labels) {
      const sig = flatLabels(labels)
      if (!cardinality.check(name, sig)) return
      gauges.set(`${name}${sig}`, value)
    },
    observe(name, value, labels) {
      const sig = flatLabels(labels)
      if (!cardinality.check(name, sig)) return
      const key = `${name}${sig}`
      let state = histograms.get(key)
      if (!state) {
        state = createReservoir(samplesCap)
        histograms.set(key, state)
      }
      reservoirAdd(state, value)
    },
    getMetrics(): MetricsSnapshot {
      return {
        counters: Object.fromEntries(counters),
        gauges: Object.fromEntries(gauges),
        histograms: Object.fromEntries(
          Array.from(histograms.entries()).map(([k, v]) => [k, computeSummary(v)] as [string, HistogramSummary]),
        ),
      }
    },
    getMetricsDelta(prev): MetricsDelta {
      const cur = this.getMetrics()
      const counterDelta: Record<string, number> = {}
      for (const [k, v] of Object.entries(cur.counters)) {
        counterDelta[k] = v - (prev.counters[k] ?? 0)
      }
      return { counters: counterDelta, gauges: cur.gauges, histograms: cur.histograms }
    },
    // Pass-through tap (no-op default — wiring downstream 06-08 per step 14
    // attivazione D-161). Lazy mode early-return analog F6 D-160.
    tap: {
      onPipelineStep() {
        // No-op default — il broker 06-08 può collegare automaticamente increment
        // su lifecycle events (route.dispatched, cache.hit, etc) per wiring metric.
      },
    },
  }
}
```

Aggiorna `packages/devtools/src/index.ts`:
```typescript
export { createMetricsCollector, type MetricsCollector, type MetricsCollectorOptions } from './metrics-collector'
export { createReservoir, reservoirAdd, computeSummary, type ReservoirState } from './reservoir-sampling'
export { createCardinalityTracker, flatLabels, type CardinalityTracker, type CardinalityTrackerOptions } from './cardinality-cap'
```

```bash
git add packages/devtools/src/metrics-collector.{ts,test.ts} packages/devtools/src/index.ts
git commit -m "feat(06-06): GREEN metrics-collector counters/gauges/histograms + cardinality (D-163/D-164/D-165/D-166)

**Closes PRD §39 #10 (TOOL-05 metrics format)** — schema { counters, gauges, histograms }
simil-OpenMetrics naming sembridge.<package>.<metric>{<labels>} Prometheus-friendly.

createMetricsCollector { increment, setGauge, observe, getMetrics, getMetricsDelta, tap }.
Cumulative-only D-164 + helper delta + reservoir Algorithm R D-165 + cardinality cap
D-166 + audit emit hook.

Pattern F5 worker-pool counter atomic event loop carryover. ~120 LOC.

Barrel append: createMetricsCollector + createReservoir + createCardinalityTracker + types.

18/18 test passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test 2>&1 | tail -15 && pnpm -F @sembridge/devtools build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `metrics-collector.ts` ~120 LOC + `*.test.ts` 18+ test
    - Barrel export aggiornato (Wave 3 close — Inspector + Metrics + Pause exports)
    - 2 commit TDD
    - Coverage v8 sui 3 file ≥90/80/90/90
  </done>
</task>

</tasks>

<verification>
- 36+ test Tier-1 jsdom passing (12 reservoir + 10 cardinality + 18 metrics-collector)
- Coverage v8 sui 3 file ≥90/80/90/90
- Pattern carryover documentato (F5 worker-pool counter atomic + F5 worker-registry D-128 cap+audit)
- D-83 strict: zero modifiche fuori `packages/devtools/src/`
- File ownership disgiunta da 06-05 e 06-07 verified
- Closes PRD §39 #10 (TOOL-05 metrics format) — annotation in commit message
</verification>

<success_criteria>
- [x] MetricsCollector counters/gauges/histograms con schema MetricsSnapshot D-163 ✅
- [x] Reservoir Algorithm R Vitter 1985 inline D-165 ✅
- [x] Cardinality cap 100 + audit emit D-166 ✅
- [x] Naming Prometheus-friendly `sembridge.<package>.<metric>{<labels>}` ✅
- [x] Cumulative-only counters + getMetricsDelta helper D-164 ✅
- [x] **PRD §39 #10 (TOOL-05) chiuso** ✅
- [x] TOOL-02 + TOOL-05 runtime done ✅
- [x] D-83 strict ✓ ✅
</success_criteria>

<output>
Crea `.planning/phases/06-cache-tooling-avanzato/06-06-SUMMARY.md` con file/test/coverage/closure PRD §39 #10/blocks pronti per 06-08.
</output>