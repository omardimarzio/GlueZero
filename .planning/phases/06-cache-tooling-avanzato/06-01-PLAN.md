---
phase: 06-cache-tooling-avanzato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cache/package.json
  - packages/cache/tsconfig.json
  - packages/cache/tsup.config.ts
  - packages/cache/vitest.config.ts
  - packages/cache/vitest.browser.config.ts
  - packages/cache/biome.json
  - packages/cache/src/index.ts
  - packages/cache/src/augment.ts
  - packages/cache/src/augment.test.ts
  - packages/cache/src/types/cache-config.ts
  - packages/cache/src/types/cache-adapter.ts
  - packages/cache/src/types/cache-entry.ts
  - packages/cache/src/types/index.ts
  - packages/devtools/package.json
  - packages/devtools/tsconfig.json
  - packages/devtools/tsup.config.ts
  - packages/devtools/vitest.config.ts
  - packages/devtools/vitest.browser.config.ts
  - packages/devtools/biome.json
  - packages/devtools/src/index.ts
  - packages/devtools/src/augment.ts
  - packages/devtools/src/augment.test.ts
  - packages/devtools/src/types/devtools-config.ts
  - packages/devtools/src/types/metrics.ts
  - packages/devtools/src/types/inspector-entry.ts
  - packages/devtools/src/types/pause-state.ts
  - packages/devtools/src/types/index.ts
  - packages/sembridge/package.json
  - packages/sembridge/tsconfig.json
  - packages/sembridge/tsup.config.ts
  - packages/sembridge/vitest.config.ts
  - packages/sembridge/biome.json
  - packages/sembridge/src/index.ts
  - packages/sembridge/src/types/sembridge-config.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05
  - PIPE-01
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
must_haves:
  truths:
    - "I pacchetti @sembridge/cache, @sembridge/devtools, @sembridge/sembridge compilano con tsup ESM-only e producono dist/index.{js,d.ts} importabili"
    - "augment.ts di cache estende BrokerConfig.cache e RouteDefinition.cache via declaration merging senza modificare runtime di @sembridge/{core,mapper,routing,gateway,worker} (D-83 strict carryover)"
    - "augment.ts di devtools estende BrokerConfig.taps (D-159) + BrokerConfig.devtools + esporta F6PipelineStep literal union ('event.observed' step 14 §28 + 'event.cache.lookup'/'hit'/'miss'/'evicted' lifecycle D-161) senza modificare runtime upstream"
    - "I type pubblici (CacheAdapter, CacheEntry, CacheConfig, MetricsSnapshot, HistogramSummary, PauseControllerSnapshot, EventInspectorEntry, RouteInspectorEntry) sono esportati dai barrel src/index.ts e tree-shake-safe"
    - "Test smoke decl merging passing per cache e devtools (≥8 test ciascuno) e __augmentCacheLoaded + __augmentDevtoolsLoaded esportati nel dist (audit `grep \"__augmentCacheLoaded\\|__augmentDevtoolsLoaded\" packages/{cache,devtools}/dist/`)"
    - "Workspace deps lockate: @sembridge/cache dipende da @sembridge/{core,mapper,routing,gateway} workspace:* + valibot 1.3.1 + nanoid 5.1.11; @sembridge/devtools dipende da @sembridge/{core,mapper,routing} + valibot; @sembridge/sembridge dipende da TUTTI (core/mapper/routing/gateway/worker/cache/devtools)"
    - "Vincolo D-83 strict carryover (CRITICO): `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines per tutto il plan 06-01"
  artifacts:
    - path: "packages/cache/package.json"
      provides: "Manifest @sembridge/cache: type module, sideEffects glob augment, deps workspace:* + valibot + nanoid; scripts build/test/typecheck"
    - path: "packages/cache/src/augment.ts"
      provides: "Declaration merging BrokerConfig.cache (D-155/D-156/D-158) + RouteDefinition.cache (D-77 ext F6) + F6CachePipelineStep literal union + __augmentCacheLoaded const literal"
    - path: "packages/cache/src/types/cache-adapter.ts"
      provides: "Interface CacheAdapter { get/set/delete/invalidate/size/clear/stats } — contract per MemoryCacheAdapter (06-02) + V1.x adapter swap"
    - path: "packages/cache/src/types/cache-config.ts"
      provides: "CacheConfig type (maxEntries default 1000 D-158 + adapter passthrough + scopeProvider callback D-156)"
    - path: "packages/cache/src/types/cache-entry.ts"
      provides: "CacheEntry<T> readonly { value, expiresAt, setAt }"
    - path: "packages/devtools/package.json"
      provides: "Manifest @sembridge/devtools: type module, sideEffects glob augment, deps workspace:* + valibot"
    - path: "packages/devtools/src/augment.ts"
      provides: "Declaration merging BrokerConfig.taps (D-159) + BrokerConfig.devtools + F6PipelineStep ('event.observed' step 14 + lifecycle events D-161) + __augmentDevtoolsLoaded const literal"
    - path: "packages/devtools/src/types/metrics.ts"
      provides: "MetricsSnapshot { counters, gauges, histograms } + HistogramSummary { count, sum, p50, p90, p99 } — D-163/D-165"
    - path: "packages/devtools/src/types/inspector-entry.ts"
      provides: "RouteInspectorEntry readonly + Inspector buffer types (D-167)"
    - path: "packages/devtools/src/types/pause-state.ts"
      provides: "PauseControllerSnapshot + PauseAction union 'pass'|'queued'|'dropped' (D-168/D-170)"
    - path: "packages/sembridge/package.json"
      provides: "Manifest @sembridge/sembridge: type module, deps su tutti i 7 sub-package workspace:*; aggregato pubblico finale"
  key_links:
    - from: "packages/cache/src/index.ts"
      to: "packages/cache/src/augment.ts"
      via: "side-effect re-export __augmentCacheLoaded"
      pattern: "__augmentCacheLoaded"
    - from: "packages/devtools/src/index.ts"
      to: "packages/devtools/src/augment.ts"
      via: "side-effect re-export __augmentDevtoolsLoaded + F6PipelineStep"
      pattern: "__augmentDevtoolsLoaded"
    - from: "packages/cache/src/augment.ts"
      to: "@sembridge/core BrokerConfig"
      via: "declare module '@sembridge/core'"
      pattern: "declare module '@sembridge/core'"
    - from: "packages/devtools/src/augment.ts"
      to: "@sembridge/core BrokerConfig"
      via: "declare module '@sembridge/core' (campi cache + devtools + taps disgiunti)"
      pattern: "declare module '@sembridge/core'"
---

<objective>
Bootstrap dei 3 pacchetti F6 (`@sembridge/cache`, `@sembridge/devtools`, `@sembridge/sembridge`) come scaffold compilabile. Wave 1 sequential gate per W2/W2-bis/W3/W4/W5. Plan replica meccanicamente la struttura di 05-01 (bootstrap @sembridge/worker) — analog F5 verbatim su:

- `package.json` (deps workspace + comlink/valibot/nanoid + sideEffects glob — Pattern S1 anti tree-shake)
- `tsup.config.ts` ESM-only target ES2022 + dts integrato + clean
- `vitest.config.ts` Tier-1 jsdom + coverage v8 thresholds 90/80/90/90 + exclude `__browser__`
- `vitest.browser.config.ts` Tier-3 Playwright Chromium factory provider Vitest 4.x
- `tsconfig.json` extends workspace base + lib WebWorker (per cache adapter API surface)
- `biome.json` workspace root inherit (omettibile, come gateway/mapper/routing/core/worker)
- 6 type files per cache + 5 type files per devtools (interfaces + literal union)
- `augment.ts` con `declare module '@sembridge/core'` ESTENSIONE additive disgiunta
- `src/index.ts` barrel re-export side-effect augment + tutti i type pubblici

**Vincolo D-83 strict (CRITICO acceptance gate):** F6 vive SOLO in `packages/cache/src/`, `packages/devtools/src/`, `packages/sembridge/src/`, e i rispettivi `augment.ts`. ZERO modifiche runtime a `packages/{core,mapper,routing,gateway,worker}/src/`. Verifica acceptance esplicita: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines.

Purpose: stabilire l'infrastruttura minima compilabile che permetta a W2 (06-02 MemoryCacheAdapter ∥ 06-04 MultiplexTap) di importare i type pubblici e procedere in parallelo TDD. Coperto CACHE-01..03 + TOOL-01..05 (type-level) + PIPE-01 ext F6 (`event.observed` step 14 declared) + PKG-01..PKG-04 (build ESM-only, dts, target evergreen, monorepo).

Output: `packages/{cache,devtools,sembridge}/` popolati con scaffolding production-ready (analog F5 05-01 04-CONTEXT D-122+D-126 carryover meccanico), `pnpm -F @sembridge/cache build` + `pnpm -F @sembridge/devtools build` + `pnpm -F @sembridge/sembridge build` producono `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}` per cache e devtools, tsc --noEmit clean su tutti gli 8 package monorepo, ≥16 test smoke decl merging passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@CLAUDE.md
@prd.md
@.planning/phases/05-worker-runtime/05-01-PLAN.md
@.planning/phases/05-worker-runtime/05-01-SUMMARY.md
@packages/worker/package.json
@packages/worker/tsconfig.json
@packages/worker/tsup.config.ts
@packages/worker/vitest.config.ts
@packages/worker/vitest.browser.config.ts
@packages/worker/src/augment.ts
@packages/worker/src/augment.test.ts
@packages/worker/src/index.ts
@packages/worker/src/types/index.ts
@packages/cache/package.json
@packages/devtools/package.json
@packages/sembridge/package.json
@pnpm-workspace.yaml

<interfaces>
<!-- Type pubblici da @sembridge/core / @sembridge/routing che augment.ts F6 estende -->
<!-- Estratti dal codebase F1+F3 — l'executor li usa direttamente, NO scavenger hunt -->

From packages/core/src/types/config.ts (BrokerConfig F1, già esteso da F2/F3/F4/F5):
```typescript
export interface BrokerConfig {
  readonly logger?: BrokerLogger;
  readonly logLevel?: BrokerLogLevel;
  readonly defaultDedupeWindow?: number;
  readonly runtime?: { readonly tap?: EventTap; /* ... */ };
  // F2: canonicalModel/aliasRegistry/transforms
  // F3: routes/gateway/routing
  // F4: realtime
  // F5: workers
  // F6 NEW (questo plan): cache + devtools + taps
}
```

From packages/core/src/types/tap.ts (EventTap interface F1 pre-instrumentato):
```typescript
export interface EventTap {
  onPipelineStep(step: PipelineStep, snapshot: PipelineSnapshot): void;
}
export type PipelineStep =
  | 'event.received' | 'event.metadata.enriched' | 'event.validated'
  | 'event.dedupe.checked' | 'event.delivered'
  // F2 step: 'event.source.resolved' | 'event.mapped.canonical' | ...
  // F3 step: 'event.route.resolved' | 'event.route.executed' | 'event.outcome.collected' | ...
  // F4-F5 lifecycle events
  // F6 NEW (questo plan): 'event.observed' (step 14) + cache lifecycle
```

From packages/routing/src/types/route-definition.ts (RouteCacheDefinition F3 type-only):
```typescript
export interface RouteCacheDefinition {
  readonly type: 'cache';
  readonly id: string;
  readonly topic: string;
  readonly strategy: 'cache-first' | 'network-first' | 'cache-then-network';
  readonly ttl?: number;
  readonly key?: (event: BrokerEvent) => string;
  readonly scope?: (event: BrokerEvent) => string | null;
  readonly invalidateOn?: readonly string[];
  // F6 può estendere via declaration merging (route-level cache config)
}
```

From packages/worker/src/augment.ts (pattern target da replicare 1:1):
```typescript
declare module '@sembridge/core' {
  interface BrokerConfig {
    workers?: WorkerConfig
  }
  interface PluginDescriptor {
    readonly workers?: readonly WorkerDescriptor[]
  }
}
export type F5PipelineStep =
  | 'event.worker.dispatched' | 'event.worker.progress'
  | 'event.worker.completed' | 'event.worker.failed'
export const __augmentWorkerLoaded: true = true
```

</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json sideEffects | tree-shaker rimuove augment se sideEffects non glob |
| augment.ts declare module | namespace pollution se campi collidono con F1-F5 |
| barrel index.ts exports | tree-shake elimina side-effect import senza re-export S1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | DoS (tree-shake elimina augment) | package.json sideEffects | mitigate | sideEffects array `["./dist/augment.js","./src/augment.ts","**/augment.{js,ts}"]` glob (Pattern S1 carryover F2/F3/F4/F5) — verifica audit `grep "__augmentCacheLoaded" packages/cache/dist/index.js` ≥1 hit |
| T-06-01-02 | Tampering (augment.ts decl merging mutation) | augment.ts | mitigate | const literal `__augmentCacheLoaded: true` non runtime-mutabile (Pattern S1) |
| T-06-01-03 | Logic flaw (collision F2/F3/F4/F5 fields) | augment.ts | accept | F6 augmenta SOLO `BrokerConfig.cache`, `BrokerConfig.devtools`, `BrokerConfig.taps`, `RouteDefinition.cache` — campi DISGIUNTI verificati. Test 5/6 augment.test.ts verifica coexistenza (analog F5 05-01 Test 5) |
| T-06-01-04 | DoS (tsc --noEmit fail su 8 package monorepo) | tsconfig.json | mitigate | `lib WebWorker` aggiunto a tsconfig (cache adapter potrebbe usare web standard API); `extends ../../tsconfig.base.json` workspace root |
| T-06-01-05 | Information Disclosure (workspace deps non lockate genera mismatch versione) | package.json deps | mitigate | tutte le workspace dep usano `workspace:*` protocol; valibot 1.3.1, nanoid 5.1.11 lockate exact (analog F5) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap @sembridge/cache — package.json + build/test config + tsconfig + biome + augment + 4 type files</name>
  <files>packages/cache/package.json, packages/cache/tsconfig.json, packages/cache/tsup.config.ts, packages/cache/vitest.config.ts, packages/cache/vitest.browser.config.ts, packages/cache/biome.json, packages/cache/src/index.ts, packages/cache/src/augment.ts, packages/cache/src/augment.test.ts, packages/cache/src/types/cache-config.ts, packages/cache/src/types/cache-adapter.ts, packages/cache/src/types/cache-entry.ts, packages/cache/src/types/index.ts</files>
  <read_first>
    - packages/worker/package.json (analog manifest verbatim — copy struttura sostituendo nome `@sembridge/cache` e omettendo deps `comlink`)
    - packages/worker/tsconfig.json (analog verbatim — include `lib WebWorker`)
    - packages/worker/tsup.config.ts (analog verbatim — 2 entry: index + augment)
    - packages/worker/vitest.config.ts (analog verbatim — Tier-1 jsdom + coverage v8 90/80/90/90 + exclude `__browser__/**`)
    - packages/worker/vitest.browser.config.ts (analog verbatim — Tier-3 Playwright)
    - packages/worker/src/augment.ts (analog STRUTTURA verbatim — sostituire BrokerConfig.workers → BrokerConfig.cache + RouteDefinition.cache; F5PipelineStep → F6CachePipelineStep)
    - packages/worker/src/augment.test.ts (analog verbatim — ~8 test smoke decl merging)
    - packages/worker/src/index.ts (analog verbatim — barrel pattern S1)
    - packages/worker/src/types/index.ts (analog barrel pattern)
    - packages/cache/package.json (stato corrente — placeholder F1 da popolare in F6)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/augment.ts" (LOC 106-146) + "packages/cache/package.json" (LOC 19-22) — analog F5 worker pattern map
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §1 Executive Summary + §16.1 budget proposta + §17.2 file ownership Wave 1
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-155/D-156/D-158/D-77 (cache section schema) + D-83 strict carryover
    - .planning/phases/05-worker-runtime/05-01-PLAN.md (struttura plan analog completo)
  </read_first>
  <action>
**1.1 — `packages/cache/package.json` (REPLACE intero file):**

Replica struttura `packages/worker/package.json` adattandola:

```json
{
  "name": "@sembridge/cache",
  "version": "0.0.0",
  "description": "Cache layer per SemBridge (Phase 6 — MemoryCacheAdapter LRU bounded + scope user-aware + cache-then-network ordering — D-155/D-156/D-157/D-158)",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "sideEffects": [
    "./dist/augment.js",
    "./src/augment.ts",
    "**/augment.js",
    "**/augment.ts"
  ],
  "engines": { "node": ">=20" },
  "publishConfig": { "access": "public", "provenance": true },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:browser": "vitest run --config vitest.browser.config.ts --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sembridge/core": "workspace:*",
    "@sembridge/mapper": "workspace:*",
    "@sembridge/routing": "workspace:*",
    "@sembridge/gateway": "workspace:*",
    "nanoid": "5.1.11",
    "valibot": "1.3.1"
  },
  "devDependencies": {
    "@vitest/browser": "4.1.5",
    "@vitest/browser-playwright": "^4.1.5",
    "@vitest/coverage-v8": "4.1.5",
    "jsdom": "29.1.0",
    "playwright": "1.59.1",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
```

NOTA: `nanoid` 5.1.11 incluso come precaution per Inspector entry id se necessario (researcher §1 dice "probabilmente non necessario — riuso event.id existing", ma include per compat con W4 senza modifiche package.json).

**1.2 — `packages/cache/tsconfig.json` (REPLACE):**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "lib": ["ES2022", "DOM", "WebWorker"]
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**1.3 — `packages/cache/tsup.config.ts` (REPLACE):**

Replica VERBATIM `packages/worker/tsup.config.ts` (2 entry: `src/index.ts` + `src/augment.ts`, ESM-only, dts true, target es2022, clean true, sourcemap true, treeshake true).

**1.4 — `packages/cache/vitest.config.ts` (REPLACE):**

Replica VERBATIM `packages/worker/vitest.config.ts` (Tier-1 jsdom env, coverage v8 thresholds 90/80/90/90, include `src/**/*.test.ts`, exclude `**/__browser__/**`).

**1.5 — `packages/cache/vitest.browser.config.ts` (REPLACE):**

Replica VERBATIM `packages/worker/vitest.browser.config.ts` (Tier-3 Playwright Chromium factory provider, include `src/**/__browser__/**/*.test.ts`).

**1.6 — `packages/cache/biome.json` (OMIT — workspace root inherit):**

Non creare il file (analog F5 worker che NON ha `biome.json` proprio — workspace root `biome.json` inherit OK).

**1.7 — `packages/cache/src/types/cache-entry.ts` (NEW):**

```typescript
/**
 * F6 CacheEntry — readonly entry restituita da `CacheAdapter.get()`.
 *
 * Pattern di serializzazione: TTL via `expiresAt` epoch ms (Infinity se TTL non
 * settato). `setAt` per audit/Inspector.
 *
 * **D-158 carryover**: LRU eviction tracked via Map insertion order (idiomatic JS
 * since ECMAScript 2015 spec). TTL ortogonale a LRU — entry può essere evicted
 * prima della scadenza TTL se cache piena.
 *
 * @see RESEARCH §2.2 Map insertion order LRU.
 */
export interface CacheEntry<T = unknown> {
  /** Valore canonicalizzato cachato (post-mapper F2). */
  readonly value: T
  /** Epoch ms scadenza (Number.POSITIVE_INFINITY se TTL non settato). */
  readonly expiresAt: number
  /** Epoch ms timestamp di set — audit/Inspector. */
  readonly setAt: number
}
```

**1.8 — `packages/cache/src/types/cache-adapter.ts` (NEW):**

```typescript
import type { CacheEntry } from './cache-entry'

/**
 * F6 CacheAdapter — contract per cache implementations.
 *
 * Default V1: `MemoryCacheAdapter` (LRU bounded `maxEntries=1000` D-158, plan 06-02).
 *
 * Swap V1.x: `@sembridge/cache-idb` IndexedDB adapter (deferred — CONTEXT.md
 * Deferred Ideas).
 *
 * @see RESEARCH §2 architettura cache + LRU implementation deep dive.
 */
export interface CacheAdapter {
  get<T = unknown>(key: string): CacheEntry<T> | undefined
  set<T = unknown>(key: string, value: T, ttlMs?: number): void
  delete(key: string): boolean
  invalidate(pattern: string | RegExp | { readonly prefix: string }): number
  size(): number
  clear(): void
  stats(): CacheStats
}

/**
 * Statistiche cumulative dal boot dell'adapter (D-164 cumulative-only).
 *
 * Consumate dal MetricsCollector F6 (plan 06-06) via gauge
 * `sembridge.cache.entries_count` + counter `sembridge.cache.{hits,misses,evictions}_total`.
 */
export interface CacheStats {
  readonly hits: number
  readonly misses: number
  readonly evictions: number
  readonly entries: number
}
```

**1.9 — `packages/cache/src/types/cache-config.ts` (NEW):**

```typescript
import type { BrokerEvent } from '@sembridge/core'
import type { CacheAdapter } from './cache-adapter'

/**
 * F6 CacheConfig — sezione `BrokerConfig.cache` (D-155/D-156/D-158).
 *
 * Pattern decl merging via `augment.ts` (analog `WorkerConfig` F5).
 *
 * @see RESEARCH §3 stable hash + §4 cache route handler concretizza F3 D-77.
 */
export interface CacheConfig {
  /**
   * D-158: cap entries LRU eviction. Default 1000.
   * Override per swap completo via `adapter`.
   */
  readonly maxEntries?: number
  /**
   * D-158: adapter custom (V1.x — `@sembridge/cache-idb` o consumer-defined).
   * Default: `createMemoryCacheAdapter({ maxEntries })` plan 06-02.
   */
  readonly adapter?: CacheAdapter
  /**
   * D-156 hybrid: scope provider config-level (default per tutte le route).
   * Route-level override via `RouteDefinition.cache.scope`. Cache key finale:
   * `${scope}::${baseKey}` (anti cross-tenant leakage).
   *
   * Ritorna `null`/`undefined` → D-157 missing scope su route auth: skip cache
   * (zero hit, zero write) + emit `system.cache.scope-missing` audit.
   */
  readonly scopeProvider?: (event: BrokerEvent) => string | null | undefined
}
```

**1.10 — `packages/cache/src/types/index.ts` (NEW barrel):**

```typescript
export type { CacheAdapter, CacheStats } from './cache-adapter'
export type { CacheEntry } from './cache-entry'
export type { CacheConfig } from './cache-config'
```

**1.11 — `packages/cache/src/augment.ts` (NEW):**

Replica STRUTTURA `packages/worker/src/augment.ts` (analog F5 verbatim) sostituendo:
- `BrokerConfig.workers` → `BrokerConfig.cache`
- `PluginDescriptor.workers` → NON aggiunto in F6 (cache è broker-scoped, non plugin-scoped — cascade via `unregisterPlugin` cascade scope-by-owner pattern, plan 06-02 + 06-08)
- `F5PipelineStep` → `F6CachePipelineStep` literal union 4 valori: `'event.cache.lookup'` | `'event.cache.hit'` | `'event.cache.miss'` | `'event.cache.evicted'` (D-161 lifecycle events)
- `__augmentWorkerLoaded` → `__augmentCacheLoaded`

Header doc commento esplicito:
```typescript
// augment.ts — TS declaration merging per estendere @sembridge/core con tipi F6 Cache.
//
// (D-155/D-156/D-158/D-83 strict carryover in 06-CONTEXT.md — replica simmetrica
// di worker/augment.ts di F5 + gateway/sse-ws/augment.ts di F4 + gateway/augment.ts
// di F3 + routing/augment.ts di F3 + mapper/augment.ts di F2)
//
// Vincolo D-83 strict (carryover F1-F5): NESSUNA modifica a packages/{core,mapper,
// routing,gateway,worker}/src/ runtime. Questo file estende solo i type via
// declaration merging additive disgiunta.
//
// Cosa estende:
//   - BrokerConfig (interface, da @sembridge/core) — aggiunge `cache?: CacheConfig`
//     come campo opzionale (D-155/D-156/D-158). Sezione complementare a `workers`
//     (F5), `realtime` (F4), `gateway`/`routes`/`routing` (F3), `canonicalModel`/
//     `aliasRegistry`/`transforms` (F2).
//
// Threat coverage:
// - T-06-01-01 (DoS tree-shake elimina augment): mitigate via sideEffects glob +
//   __augmentCacheLoaded re-export dal barrel.
// - T-06-01-02 (Tampering decl merging mutation): mitigate via const literal `true`.
// - T-06-01-03 (Logic flaw collision F2-F5 fields): accept — F6 cache augmenta
//   SOLO `BrokerConfig.cache` — campo DISGIUNTO da F2-F5.

import type { CacheConfig } from './types/cache-config'

declare module '@sembridge/core' {
  interface BrokerConfig {
    /** F6 sezione `cache` (D-155, PRD §20): config cache layer LRU + scope hybrid. */
    cache?: CacheConfig
  }
}

/**
 * F6 PipelineStep — eventi lifecycle cache emessi dal CacheHandler (D-161).
 * Pattern carryover F2/F3/F4/F5 (`F2PipelineStep` di @sembridge/mapper, etc).
 */
export type F6CachePipelineStep =
  | 'event.cache.lookup'   // pre-cache check (analog F3 dispatch step 9)
  | 'event.cache.hit'      // cache.hit lifecycle (D-161)
  | 'event.cache.miss'     // cache.miss lifecycle
  | 'event.cache.evicted'  // LRU/TTL/invalidate eviction

export const __augmentCacheLoaded: true = true
```

**1.12 — `packages/cache/src/augment.test.ts` (NEW):**

Replica STRUTTURA `packages/worker/src/augment.test.ts` (analog F5). 8 test smoke decl merging:
1. `__augmentCacheLoaded === true` (boot side-effect)
2. `F6CachePipelineStep` literal union accetta i 4 valori previsti
3. `BrokerConfig.cache?: CacheConfig` accetta shape (`{ maxEntries: 500 }`)
4. `BrokerConfig.cache.adapter` accetta CacheAdapter custom
5. `BrokerConfig.cache.scopeProvider` accetta callback
6. coexistence con sezioni F2/F3/F4/F5 (`canonicalModel`, `routes`, `realtime`, `workers`) — verifica typecheck simultaneo
7. CacheEntry shape verifica readonly campi
8. CacheStats shape verifica readonly campi cumulative

**1.13 — `packages/cache/src/index.ts` (NEW barrel):**

```typescript
// @sembridge/cache — F6 cache layer barrel pubblico.
//
// Pattern S1 anti tree-shake: re-export side-effect augment.
export { __augmentCacheLoaded, type F6CachePipelineStep } from './augment'

// Type pubblici
export type { CacheAdapter, CacheStats, CacheEntry, CacheConfig } from './types'

// Runtime exports — placeholder per Wave 2/W2-bis/W4.
// Wave 2 (06-02): export { createMemoryCacheAdapter } from './memory-cache-adapter'
//                 export { stableHash, cacheKey } from './stable-hash'
// Wave 2-bis (06-03): export { createCacheHandlerF6 } from './cache-handler'
//                     export { createCompositeHandlerF6 } from './composite-handler'
// Wave 4 (06-08): export { CacheBroker } from './cache-broker'
//                 export { createCacheBroker } from './public-factory'
```

**Acceptance grep:**
- `grep -c "__augmentCacheLoaded" packages/cache/src/index.ts` ≥ 1
- `grep -c "F6CachePipelineStep" packages/cache/src/augment.ts` ≥ 1
- `grep -c "declare module '@sembridge/core'" packages/cache/src/augment.ts` = 1
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm install && pnpm -F @sembridge/cache build && pnpm -F @sembridge/cache test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - File `packages/cache/{package.json,tsconfig.json,tsup.config.ts,vitest.config.ts,vitest.browser.config.ts}` creati
    - File `packages/cache/src/{augment.ts,augment.test.ts,index.ts,types/{cache-adapter,cache-entry,cache-config,index}.ts}` creati
    - `pnpm -F @sembridge/cache build` produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}` ESM-only
    - `pnpm -F @sembridge/cache test` ≥8 test smoke decl merging passing
    - `pnpm -F @sembridge/cache typecheck` zero errori
    - `grep "__augmentCacheLoaded" packages/cache/dist/index.js` ≥1 hit (Pattern S1 verified)
    - `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines (D-83 strict)
  </done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap @sembridge/devtools — package.json + build/test config + augment + 5 type files</name>
  <files>packages/devtools/package.json, packages/devtools/tsconfig.json, packages/devtools/tsup.config.ts, packages/devtools/vitest.config.ts, packages/devtools/vitest.browser.config.ts, packages/devtools/src/index.ts, packages/devtools/src/augment.ts, packages/devtools/src/augment.test.ts, packages/devtools/src/types/devtools-config.ts, packages/devtools/src/types/metrics.ts, packages/devtools/src/types/inspector-entry.ts, packages/devtools/src/types/pause-state.ts, packages/devtools/src/types/index.ts</files>
  <read_first>
    - packages/worker/package.json (analog manifest verbatim)
    - packages/worker/src/augment.ts (analog STRUTTURA + sostituzione field — vedi Task 1.11 simmetria)
    - packages/cache/package.json (creato in Task 1 — pattern coerente)
    - packages/cache/src/augment.ts (creato in Task 1 — pattern coerente)
    - packages/core/src/types/tap.ts (EventTap interface F1 — augment.ts devtools deve aggiungere `BrokerConfig.taps?: readonly EventTap[]` D-159)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/augment.ts" + "packages/devtools/src/types/metrics.ts"
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §5 (tap registry D-159) + §7 (MetricsCollector schema D-163/D-165) + §10 (pauseTopic D-168/D-170) + §12 (step 14 attivazione)
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-159/D-160/D-161/D-163/D-165/D-166/D-167/D-168/D-170
  </read_first>
  <action>
**2.1 — `packages/devtools/package.json` (REPLACE intero file):**

Replica struttura `packages/cache/package.json` (creato in Task 1) adattando nome/desc:
- `name`: `@sembridge/devtools`
- `description`: `"Developer tooling per SemBridge (Phase 6 — Tap registry chain D-159 + EventInspector + RouteInspector + MetricsCollector simil-OpenMetrics + pauseTopic queue — D-159..D-170 / chiude PRD §39 #10 TOOL-05)"`
- `dependencies`: `@sembridge/core`, `@sembridge/mapper`, `@sembridge/routing` workspace:* + `valibot 1.3.1` + `nanoid 5.1.11` (NO `@sembridge/gateway` — devtools non dipende dal gateway HTTP/SSE-WS direttamente)
- `devDependencies`: identiche a cache (vitest, jsdom, playwright, tsup, typescript)
- `sideEffects`: identico glob augment Pattern S1

**2.2 — `packages/devtools/tsconfig.json` (REPLACE):**

Identico a `packages/cache/tsconfig.json` (Task 1.2).

**2.3 — `packages/devtools/tsup.config.ts` (REPLACE):**

Identico a `packages/cache/tsup.config.ts` (2 entry: index + augment).

**2.4 — `packages/devtools/vitest.config.ts` (REPLACE):**

Identico a `packages/cache/vitest.config.ts` (Tier-1 jsdom, coverage v8 90/80/90/90, exclude `__browser__`).

**2.5 — `packages/devtools/vitest.browser.config.ts` (REPLACE):**

Identico a `packages/cache/vitest.browser.config.ts` (Tier-3 Playwright).

**2.6 — `packages/devtools/biome.json` (OMIT):**

Non creare (workspace root inherit).

**2.7 — `packages/devtools/src/types/metrics.ts` (NEW):**

```typescript
/**
 * F6 MetricsSnapshot — schema simil-OpenMetrics ritornato da `getMetrics()`
 * (TOOL-05 closure PRD §39 #10).
 *
 * **Naming convention** (D-163): `sembridge.<package>.<metric>{<labels>}` formato
 * Prometheus-style flatten. Esempi:
 * - Counter: `sembridge.broker.events_published_total{topic="weather.requested"}`
 * - Gauge: `sembridge.cache.entries_count`
 * - Histogram: `sembridge.http.duration_ms{route_id="weather"}`
 *
 * **Cumulative-only** (D-164): counters crescono dal boot, mai resettati.
 * Helper opzionale `getMetricsDelta(prev)` calcola differenze lato consumer.
 *
 * @see RESEARCH §7 MetricsCollector impl deep dive.
 */
export interface MetricsSnapshot {
  /** D-163: counters cumulative `_total` suffix (Prometheus convention). */
  readonly counters: Readonly<Record<string, number>>
  /** D-163: gauge current value (no suffix Prometheus convention). */
  readonly gauges: Readonly<Record<string, number>>
  /** D-165: histogram quantile summary `_ms` suffix per duration. */
  readonly histograms: Readonly<Record<string, HistogramSummary>>
}

/**
 * F6 HistogramSummary — quantile summary (D-165 reservoir Algorithm R Vitter 1985,
 * plan 06-06).
 *
 * `count` = total observations (NOT capacity reservoir). `sum` per calcolo media.
 * `p50/p90/p99` quantile sample-based.
 */
export interface HistogramSummary {
  readonly count: number
  readonly sum: number
  readonly p50: number
  readonly p90: number
  readonly p99: number
}

/**
 * F6 MetricsDelta — ritornato da `getMetricsDelta(prev: MetricsSnapshot)`.
 *
 * Counters delta (current - prev). Gauges = current snapshot. Histograms =
 * current snapshot (no delta semantica per quantile aggregati).
 */
export interface MetricsDelta {
  readonly counters: Readonly<Record<string, number>>
  readonly gauges: Readonly<Record<string, number>>
  readonly histograms: Readonly<Record<string, HistogramSummary>>
}
```

**2.8 — `packages/devtools/src/types/inspector-entry.ts` (NEW):**

```typescript
import type { PipelineSnapshot } from '@sembridge/core'

/**
 * F6 RouteInspectorEntry — entry capture da RouteInspector (plan 06-05, D-167).
 *
 * Cattura via tap che ascolta step 9 (`event.route.executed`) + step 10
 * (`event.outcome.collected`) F3, aggrega per `eventId + routeId`.
 *
 * @see RESEARCH §6.2 RouteInspector focus route-level.
 */
export interface RouteInspectorEntry {
  readonly eventId: string
  readonly routeId: string
  readonly topic: string
  readonly type: 'local' | 'http' | 'cache' | 'composite' | 'worker' | 'realtime-inbound'
  readonly outcome: 'success' | 'error' | 'skipped' | 'cached' | 'pending'
  readonly durationMs: number
  readonly retryCount?: number
  readonly cacheHit?: boolean
  readonly policiesApplied?: readonly string[]
  readonly timestamp: number
  readonly errorCode?: string
}

/**
 * F6 EventInspectorSnapshot — meta-info esposta via getDebugSnapshot (D-162).
 *
 * Buffer eventi è array `PipelineSnapshot[]` deep-cloned via structuredClone.
 * Ring buffer cap default 500 (D-167).
 */
export interface EventInspectorSnapshot {
  readonly enabled: boolean
  readonly bufferEntries: number
  readonly bufferSize: number
}

/**
 * Re-export PipelineSnapshot per compat consumer devtools (single import surface).
 */
export type { PipelineSnapshot }
```

**2.9 — `packages/devtools/src/types/pause-state.ts` (NEW):**

```typescript
/**
 * F6 PauseAction — risultato di `PauseController.intercept(event)` (D-168).
 *
 * - `pass`: topic NON in pausa o priority='critical' (D-170 critical bypass) →
 *   delegato downstream RouterBroker
 * - `queued`: topic in pausa, evento accodato FIFO
 * - `dropped`: topic in pausa + queue piena (cap default 1000 D-170) → dropped
 *   il più vecchio (drop-oldest FIFO) + emit `system.queue.overflow`
 */
export type PauseAction = 'pass' | 'queued' | 'dropped'

/**
 * F6 PauseControllerSnapshot — meta-info esposta da `getPauseControllerSnapshot()`.
 */
export interface PauseControllerSnapshot {
  readonly pausedTopics: readonly string[]
  readonly queueSizes: Readonly<Record<string, number>>
  readonly maxQueueSize: number
}

/**
 * F6 FlushQueueResult — ritornato da `flushQueue(topic?)` (D-169).
 */
export interface FlushQueueResult {
  readonly topic: string
  readonly droppedCount: number
  readonly droppedEventIds: readonly string[]
}
```

**2.10 — `packages/devtools/src/types/devtools-config.ts` (NEW):**

```typescript
/**
 * F6 DevtoolsConfig — sezione `BrokerConfig.devtools` (D-160/D-167/D-170).
 *
 * Default-driven: tutti i field sono opzionali con default sensati (CONTEXT.md
 * Claude's Discretion).
 *
 * @see RESEARCH §16.1 budget defaults proposta.
 */
export interface DevtoolsConfig {
  /**
   * D-160: Toggle automatico `enableDebug()` al boot.
   * Default: `NODE_ENV !== 'production'` → true (DX dev-friendly).
   * Production build: false (zero overhead).
   */
  readonly enableByDefault?: boolean
  /** D-167: ring buffer EventInspector cap (default 500 eventi). */
  readonly eventBufferSize?: number
  /** D-167: ring buffer RouteInspector cap (default 500 routes). */
  readonly routeBufferSize?: number
  /** D-165: reservoir samples per histogram (default 1024). */
  readonly histogramSamples?: number
  /** D-166: cap distinct label combinations per metric base name (default 100). */
  readonly maxLabelCombinations?: number
  /** D-170: cap pauseTopic queue per topic (default 1000). */
  readonly pauseQueueMaxSize?: number
}
```

**2.11 — `packages/devtools/src/types/index.ts` (NEW barrel):**

```typescript
export type { MetricsSnapshot, HistogramSummary, MetricsDelta } from './metrics'
export type {
  RouteInspectorEntry,
  EventInspectorSnapshot,
  PipelineSnapshot,
} from './inspector-entry'
export type { PauseAction, PauseControllerSnapshot, FlushQueueResult } from './pause-state'
export type { DevtoolsConfig } from './devtools-config'
```

**2.12 — `packages/devtools/src/augment.ts` (NEW):**

```typescript
// augment.ts — TS declaration merging per estendere @sembridge/core con tipi F6
// Devtools (taps registry chain D-159 + DevtoolsConfig + F6PipelineStep step 14
// reale).
//
// (D-159/D-160/D-161/D-163/D-165/D-166/D-167/D-168/D-170/D-83 strict carryover
// in 06-CONTEXT.md — replica simmetrica di cache/augment.ts nello stesso plan.)
//
// Vincolo D-83 strict (carryover F1-F5): NESSUNA modifica a packages/{core,mapper,
// routing,gateway,worker}/src/ runtime. Questo file estende SOLO type via
// declaration merging additive disgiunta.
//
// Cosa estende:
//   - BrokerConfig (interface, da @sembridge/core) — aggiunge:
//     * `taps?: readonly EventTap[]` (D-159) — sostituisce semanticamente
//       `runtime.tap` F1 single-tap con multiplex chain. Il `createDevtoolsBroker`
//       F6 (plan 06-08) auto-wrappa eventuale `runtime.tap` legacy per
//       backward-compat.
//     * `devtools?: DevtoolsConfig` (D-160/D-167/D-170) — config Inspector,
//       MetricsCollector, PauseController.
//
// Cosa NON estende:
//   - F1 `runtime.tap` resta intatto (single value) — F6 lo legge e auto-wrappa
//     in `MultiplexTap` aggregator. NON modifichiamo packages/core/.
//
// Threat coverage:
// - T-06-01-01 (DoS tree-shake): mitigate via sideEffects glob.
// - T-06-04-01 (DoS tap throw blocca pipeline): mitigate downstream plan 06-04
//   (MultiplexTap try/catch isolato).
// - T-06-01-03 (Logic flaw collision F2-F5): accept — F6 devtools augmenta SOLO
//   `BrokerConfig.taps` e `BrokerConfig.devtools` — campi DISGIUNTI da F1-F5.

import type { EventTap } from '@sembridge/core'
import type { DevtoolsConfig } from './types/devtools-config'

declare module '@sembridge/core' {
  interface BrokerConfig {
    /**
     * F6 D-159: chain di tap (registry multiplex). Sostituisce `runtime.tap`
     * single-tap di F1 con array. `createDevtoolsBroker` (plan 06-08) auto-wrappa
     * `runtime.tap` legacy per backward-compat.
     */
    taps?: readonly EventTap[]
    /**
     * F6 D-160/D-167/D-170: config Inspector + Metrics + PauseController.
     */
    devtools?: DevtoolsConfig
  }
}

/**
 * F6 PipelineStep — step 14 (D-161 attivazione step 14 reale, "logging/metrics/
 * debug snapshot") + lifecycle events emessi dal CacheHandler / WorkerHandler /
 * RealtimeAdapter / RouteExecutor.
 *
 * **Limitazione TS R4 (RESEARCH §17 + F5 augment.ts pattern)**: `PipelineStep` di
 * `@sembridge/core` è type alias literal union — TS non supporta declaration
 * merging di type alias. Soluzione: super-set additive importato dal consumer
 * tap F6:
 *
 * ```ts
 * import type { PipelineStep } from '@sembridge/core'
 * import type { F2PipelineStep } from '@sembridge/mapper'
 * import type { F3PipelineStep } from '@sembridge/routing'
 * import type { F4PipelineStep } from '@sembridge/gateway/sse-ws'
 * import type { F5PipelineStep } from '@sembridge/worker'
 * import type { F6CachePipelineStep } from '@sembridge/cache'
 * import type { F6PipelineStep } from '@sembridge/devtools'
 * type AllSteps = PipelineStep | F2PipelineStep | F3PipelineStep | F4PipelineStep
 *                | F5PipelineStep | F6CachePipelineStep | F6PipelineStep
 * ```
 *
 * Lifecycle events (D-161): `route.dispatched`, `cache.{hit,miss,evicted}`,
 * `worker.{spawned,terminated}`, `realtime.{connected,disconnected}` sono già
 * coperti da F4/F5 step + F6CachePipelineStep di `@sembridge/cache/augment`.
 * Qui dichiariamo solo lo step 14 reale + system events audit.
 */
export type F6PipelineStep =
  | 'event.observed'                  // step 14 §28 reale (D-161 attivazione)
  | 'system.queue.flushed'            // D-169 audit
  | 'system.queue.overflow'           // D-170 audit
  | 'system.metrics.cardinality-overflow' // D-166 audit
  | 'system.cache.scope-missing'      // D-157 audit (anche emesso da @sembridge/cache)

export const __augmentDevtoolsLoaded: true = true
```

**2.13 — `packages/devtools/src/augment.test.ts` (NEW):**

Replica STRUTTURA augment.test.ts di Task 1.12. ≥8 test smoke decl merging:
1. `__augmentDevtoolsLoaded === true`
2. `F6PipelineStep` literal union accetta i 5 valori previsti
3. `BrokerConfig.taps?: readonly EventTap[]` accetta array di tap
4. `BrokerConfig.devtools?: DevtoolsConfig` accetta shape
5. Coexistence con `BrokerConfig.cache` (F6 cache nello stesso plan)
6. Coexistence con `BrokerConfig.workers/realtime/routes/canonicalModel` (F2-F5)
7. MetricsSnapshot / HistogramSummary readonly enforcement
8. PauseAction discriminated union 3 valori

**2.14 — `packages/devtools/src/index.ts` (NEW barrel):**

```typescript
// @sembridge/devtools — F6 developer tooling barrel pubblico.
//
// Pattern S1 anti tree-shake: re-export side-effect augment.
export { __augmentDevtoolsLoaded, type F6PipelineStep } from './augment'

// Type pubblici
export type {
  MetricsSnapshot,
  HistogramSummary,
  MetricsDelta,
  RouteInspectorEntry,
  EventInspectorSnapshot,
  PipelineSnapshot,
  PauseAction,
  PauseControllerSnapshot,
  FlushQueueResult,
  DevtoolsConfig,
} from './types'

// Runtime exports — placeholder per Wave 2/3/4.
// Wave 2 (06-04): export { createMultiplexTap, createTapRegistry } from ...
// Wave 3 (06-05): export { createEventInspector, createRouteInspector } from ...
// Wave 3 (06-06): export { createMetricsCollector, createReservoir, ... } from ...
// Wave 3 (06-07): export { createPauseController } from ...
// Wave 4 (06-08): export { DevtoolsBroker, createDevtoolsBroker } from ...
```

**Acceptance grep:**
- `grep -c "__augmentDevtoolsLoaded" packages/devtools/src/index.ts` ≥1
- `grep -c "BrokerConfig.taps\|taps?: readonly EventTap" packages/devtools/src/augment.ts` ≥1
- `grep -c "F6PipelineStep" packages/devtools/src/augment.ts` ≥1
- `grep -c "'event.observed'" packages/devtools/src/augment.ts` ≥1
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm install && pnpm -F @sembridge/devtools build && pnpm -F @sembridge/devtools test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - File `packages/devtools/{package.json,tsconfig.json,tsup.config.ts,vitest.config.ts,vitest.browser.config.ts}` creati
    - File `packages/devtools/src/{augment.ts,augment.test.ts,index.ts,types/{metrics,inspector-entry,pause-state,devtools-config,index}.ts}` creati
    - `pnpm -F @sembridge/devtools build` produce `dist/index.{js,d.ts}` + `dist/augment.{js,d.ts}` ESM-only
    - `pnpm -F @sembridge/devtools test` ≥8 test smoke decl merging passing
    - `pnpm -F @sembridge/devtools typecheck` zero errori
    - `grep "__augmentDevtoolsLoaded" packages/devtools/dist/index.js` ≥1 hit
    - `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines (D-83 strict)
  </done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap @sembridge/sembridge (aggregato) + cross-package typecheck full monorepo + audit acceptance</name>
  <files>packages/sembridge/package.json, packages/sembridge/tsconfig.json, packages/sembridge/tsup.config.ts, packages/sembridge/vitest.config.ts, packages/sembridge/src/index.ts, packages/sembridge/src/types/sembridge-config.ts</files>
  <read_first>
    - packages/worker/package.json (analog manifest)
    - packages/cache/package.json (creato in Task 1)
    - packages/devtools/package.json (creato in Task 2)
    - packages/sembridge/package.json (stato corrente — placeholder F1)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/sembridge/" (LOC 82-89)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §11 composition wrapper topology + §17.2 file ownership
  </read_first>
  <action>
**3.1 — `packages/sembridge/package.json` (REPLACE intero file):**

```json
{
  "name": "@sembridge/sembridge",
  "version": "0.0.0",
  "description": "SemBridge aggregato pubblico (Phase 6 — re-exports core + mapper + routing + gateway + worker + cache + devtools + factory createSemBridge milestone v1.0)",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "sideEffects": [
    "./dist/augment.js",
    "**/augment.{js,ts}"
  ],
  "engines": { "node": ">=20" },
  "publishConfig": { "access": "public", "provenance": true },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sembridge/core": "workspace:*",
    "@sembridge/mapper": "workspace:*",
    "@sembridge/routing": "workspace:*",
    "@sembridge/gateway": "workspace:*",
    "@sembridge/worker": "workspace:*",
    "@sembridge/cache": "workspace:*",
    "@sembridge/devtools": "workspace:*",
    "valibot": "1.3.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.5",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
```

**3.2 — `packages/sembridge/tsconfig.json` (REPLACE):**

Identico a `packages/cache/tsconfig.json`.

**3.3 — `packages/sembridge/tsup.config.ts` (REPLACE):**

Single entry `src/index.ts` (no augment — il pacchetto è puro re-export aggregato + factory `createSemBridge`):

```typescript
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  target: 'es2022',
  dts: true,
  clean: true,
  sourcemap: true,
  treeshake: true,
  splitting: false,
  external: [
    '@sembridge/core',
    '@sembridge/mapper',
    '@sembridge/routing',
    '@sembridge/gateway',
    '@sembridge/worker',
    '@sembridge/cache',
    '@sembridge/devtools',
    'valibot',
  ],
})
```

**3.4 — `packages/sembridge/vitest.config.ts` (REPLACE):**

Vitest config minimal jsdom + coverage v8 thresholds 90/80/90/90 + `passWithNoTests` (placeholder Wave 4 test 06-08).

**3.5 — `packages/sembridge/src/types/sembridge-config.ts` (NEW):**

```typescript
/**
 * F6 SemBridgeConfig — config per `createSemBridge(config)` aggregato (D-30 no
 * singleton + research §11 Opzione B factory aggregato).
 *
 * Estende `BrokerConfig` di @sembridge/core (con augmentation F2-F6 attive via
 * side-effect import dei rispettivi augment.ts). Aggiunge `features` flag per
 * opt-out di feature opzionali (cache/devtools/worker/realtime).
 *
 * @see RESEARCH §11 composition wrapper topology — Opzione A chain explicit vs
 *  Opzione B factory aggregato.
 */
import type { BrokerConfig } from '@sembridge/core'

export interface SemBridgeFeatures {
  readonly cache?: boolean
  readonly devtools?: boolean
  readonly worker?: boolean
  readonly realtime?: boolean
}

export interface SemBridgeConfig extends BrokerConfig {
  /**
   * Opt-out di feature opzionali. Default: tutte enabled (true).
   *
   * Esempio: `features: { cache: false }` esclude `createCacheBroker` dalla
   * chain — il broker risultante è equivalente a `createDevtoolsBroker(...)`
   * senza cache layer (zero costo bundle se tree-shake aggressivo).
   */
  readonly features?: SemBridgeFeatures
}
```

**3.6 — `packages/sembridge/src/index.ts` (NEW barrel placeholder):**

```typescript
// @sembridge/sembridge — aggregato pubblico (re-export 7 sub-package + factory).
//
// **Wave 1 placeholder**: questo file viene popolato in Wave 4 (plan 06-08) con:
// - re-export pubblico di tutti i sub-package (createBroker, createMapperBroker,
//   createRouterBroker, createRealtimeBroker, createWorkerBroker,
//   createCacheBroker, createDevtoolsBroker)
// - `createSemBridge(config)` factory aggregato chain composition (D-30 no
//   singleton + RESEARCH §11.3 Opzione B)
//
// **Plan 06-09 final gate** popola anche `packages/sembridge/README.md` (DOC-02 +
// DOC-05) e `packages/sembridge/EXAMPLES.md` (DOC-05 esempi end-to-end).

// Type re-export (disponibile da Wave 1 per consumer typecheck early-stage)
export type { SemBridgeConfig, SemBridgeFeatures } from './types/sembridge-config'

// Side-effect re-export per attivare augment di tutti i sub-package (deferred a
// Wave 4 plan 06-08 — qui placeholder).
// Wave 4 (06-08):
// import '@sembridge/mapper/augment'
// import '@sembridge/routing/augment'
// import '@sembridge/gateway/augment'
// import '@sembridge/gateway/sse-ws/augment'  (se esiste subpath)
// import '@sembridge/worker/augment'
// import '@sembridge/cache/augment'
// import '@sembridge/devtools/augment'
//
// Runtime factory (Wave 4 06-08):
// export { createSemBridge, type SemBridge } from './sem-bridge'
// export * from '@sembridge/core'
// export * from '@sembridge/mapper'
// export * from '@sembridge/routing'
// export * from '@sembridge/gateway/http'
// export * from '@sembridge/gateway/sse-ws'  (se esiste)
// export * from '@sembridge/worker'
// export * from '@sembridge/cache'
// export * from '@sembridge/devtools'
```

**3.7 — Aggiorna `pnpm-workspace.yaml` (verifica copertura):**

Verifica via `Read` che `pnpm-workspace.yaml` glob `packages/*` copra cache/devtools/sembridge (probabile già OK come per worker in F5 — analog 05-01 SUMMARY note).

**3.8 — Cross-package typecheck full monorepo + acceptance audit:**

Esegui:
```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"
pnpm install
pnpm -r typecheck
pnpm -F @sembridge/cache build
pnpm -F @sembridge/devtools build
pnpm -F @sembridge/sembridge build
pnpm -F @sembridge/cache test
pnpm -F @sembridge/devtools test
pnpm -r test --passWithNoTests
```

Atteso:
- `pnpm -r typecheck` zero errori su tutti gli 8 pacchetti (core/mapper/routing/gateway/worker/cache/devtools/sembridge)
- `pnpm -F @sembridge/{cache,devtools,sembridge} build` produce `dist/` per tutti
- ≥16 test smoke (8 cache + 8 devtools) passing
- Cross-package zero regression: 248 core + 183 mapper + 103 routing + 222 gateway (3 skip MSW V1.x F4) + 121 worker + nuovi 16 = 893+/896 monorepo full

**3.9 — D-83 strict acceptance verification (CRITICO):**

```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"
git diff main...HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/ packages/gateway/src/ packages/worker/src/ | wc -l
# atteso: 0 (zero modifiche runtime upstream — D-83 strict carryover F1-F5 → F6)
```

Se non-zero: diagnose, rollback runtime changes — il bootstrap F6 deve essere hermetic (solo `packages/{cache,devtools,sembridge}/`).

**Acceptance grep finale:**
- `grep -rn "__augmentCacheLoaded" packages/cache/src/` ≥2 hits (augment + index)
- `grep -rn "__augmentDevtoolsLoaded" packages/devtools/src/` ≥2 hits
- `grep "F6CachePipelineStep" packages/cache/dist/index.d.ts` ≥1 hit (preserved in dts)
- `grep "F6PipelineStep" packages/devtools/dist/index.d.ts` ≥1 hit
- `grep -c "BrokerConfig" packages/cache/src/augment.ts` ≥1
- `grep -c "BrokerConfig" packages/devtools/src/augment.ts` ≥1

**Decisioni minori autonome (precedente F5):**
- Aggiunto `lib WebWorker` in tsconfig.json di cache + devtools + sembridge per coerenza con worker (browser API surface)
- Omesso `biome.json` per cache/devtools/sembridge (workspace root inherit OK)
- `external` in tsup config sembridge include TUTTI i workspace dep + valibot per garantire bundle aggregato
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm install && pnpm -r typecheck && pnpm -F @sembridge/cache build && pnpm -F @sembridge/devtools build && pnpm -F @sembridge/sembridge build && DIFF=$(git diff main...HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/ packages/gateway/src/ packages/worker/src/ | wc -l); echo "D-83 strict diff lines: $DIFF (atteso: 0)"</automated>
  </verify>
  <done>
    - File `packages/sembridge/{package.json,tsconfig.json,tsup.config.ts,vitest.config.ts}` + `src/{index.ts,types/sembridge-config.ts}` creati
    - `pnpm -r typecheck` zero errori su 8 pacchetti
    - `pnpm -F @sembridge/sembridge build` produce `dist/index.{js,d.ts}` ESM-only
    - Cross-package full monorepo: zero regression (≥893/896 test passing inclusi nuovi 16 smoke)
    - D-83 strict acceptance: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines (CRITICO)
    - Pattern S1 verified per cache + devtools augment via grep `__augment*Loaded` ≥1 hit nel `dist/index.js`
  </done>
</task>

</tasks>

<verification>
- Tutti i 3 pacchetti (`@sembridge/cache`, `@sembridge/devtools`, `@sembridge/sembridge`) compilano ESM-only via tsup
- 16+ test smoke decl merging passing (8 cache + 8 devtools)
- `pnpm -r typecheck` exit 0 su 8 pacchetti monorepo
- `__augmentCacheLoaded` + `__augmentDevtoolsLoaded` preservati in `dist/index.js` (Pattern S1 anti tree-shake)
- D-83 strict carryover: zero diff runtime fuori `packages/{cache,devtools,sembridge}/src/`
- Tutti i type pubblici (CacheAdapter, CacheConfig, MetricsSnapshot, HistogramSummary, RouteInspectorEntry, PauseAction, DevtoolsConfig, SemBridgeConfig) esportati e tree-shake-safe
</verification>

<success_criteria>
- [x] @sembridge/cache: package.json + tsconfig + tsup + vitest config + augment + 4 type files + barrel + 8 augment.test.ts smoke ✅
- [x] @sembridge/devtools: package.json + tsconfig + tsup + vitest config + augment + 5 type files + barrel + 8 augment.test.ts smoke ✅
- [x] @sembridge/sembridge: package.json + tsconfig + tsup + vitest + barrel placeholder + SemBridgeConfig type ✅
- [x] tsc --noEmit clean su 8 pacchetti monorepo ✅
- [x] Pattern S1 anti tree-shake: `__augmentCacheLoaded` + `__augmentDevtoolsLoaded` preservati nel dist (audit grep ≥1 hit) ✅
- [x] D-83 strict carryover acceptance gate: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines ✅
- [x] Building blocks pronti per Wave 2 parallel: 06-02 (MemoryCacheAdapter+stable-hash) ∥ 06-04 (MultiplexTap+tap-registry) — file ownership disgiunta verificata
</success_criteria>

<output>
After completion, create `.planning/phases/06-cache-tooling-avanzato/06-01-SUMMARY.md` con:
- File creati (count + path)
- Cross-package typecheck/build/test result
- D-83 strict carryover verification report
- REQ progress: CACHE-01..03 + TOOL-01..05 type-level scaffold + PIPE-01 ext F6 (step 14 declared) + PKG-01..04 done
- Decisioni minori autonome documentate (lib WebWorker, biome inherit, etc)
- Building blocks pronti per Wave 2 parallel
</output>