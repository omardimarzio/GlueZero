---
phase: 06-cache-tooling-avanzato
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - packages/devtools/src/event-inspector.ts
  - packages/devtools/src/event-inspector.test.ts
  - packages/devtools/src/route-inspector.ts
  - packages/devtools/src/route-inspector.test.ts
autonomous: true
requirements:
  - TOOL-01
  - TOOL-03
  - PIPE-01
must_haves:
  truths:
    - "createEventInspector({ bufferSize, initiallyEnabled }) ritorna { tap: EventTap, enable, disable, getBuffer, clear } — D-167 ring buffer 500 default"
    - "createRouteInspector({ bufferSize, initiallyEnabled }) ritorna { tap: EventTap, enable, disable, getBuffer, clear } — D-167 routeBufferSize 500 default"
    - "Lazy-mode early-return D-160: state.enabled === false → onPipelineStep ritorna senza alloc/capture (zero overhead in production verified via mock counter)"
    - "Default automatico D-160 inline (consistency cross-component): se opts.initiallyEnabled undefined → fallback a `(typeof process !== 'undefined' ? process.env.NODE_ENV !== 'production' : true)` — DX dev-friendly auto, production zero overhead"
    - "Buffer FIFO drop-oldest: cap=10, push 11 entries → primo entry droppato (Array.shift), bufferSize=10 (Test 5)"
    - "getBuffer() ritorna deep-clone via structuredClone (D-162) — mutation di return value NON corrompe state.buffer interno (Test 8)"
    - "disable() clear-buffer: state.enabled = false + state.buffer = [] (memory hygiene — RESEARCH §6.3 Pitfall ring buffer leak)"
    - "RouteInspector cattura step 9 ('event.route.executed') + step 10 ('event.outcome.collected') aggregando per `eventId + routeId` in RouteInspectorEntry"
    - "RouteInspectorEntry shape: readonly { eventId, routeId, topic, type, outcome, durationMs, retryCount?, cacheHit?, policiesApplied?, timestamp, errorCode? } — match tipo da 06-01"
    - "Coverage v8 sui 2 file ≥90/80/90/90"
    - "File ownership disgiunta da 06-06 e 06-07 (parallel Wave 3): 06-05 owns SOLO event-inspector + route-inspector source+test. 06-06 owns metrics-collector + reservoir + cardinality-cap. 06-07 owns pause-controller. NESSUN plan W3 tocca packages/devtools/src/index.ts"
    - "Barrel append packages/devtools/src/index.ts gestito da 06-08b Wave 4b (sequential gate cumulative). 06-05 NON modifica index.ts."
    - "Vincolo D-83 strict: zero modifiche fuori `packages/devtools/src/`"
  artifacts:
    - path: "packages/devtools/src/event-inspector.ts"
      provides: "createEventInspector closure factory: tap + enable/disable + getBuffer (deep-clone) + clear (D-160 lazy mode + default NODE_ENV inline + D-162 deep clone + D-167 ring buffer 500)"
    - path: "packages/devtools/src/route-inspector.ts"
      provides: "createRouteInspector closure factory: tap che cattura step 9+10 aggregando per eventId+routeId (D-167 routeBufferSize)"
    - path: "packages/devtools/src/event-inspector.test.ts"
      provides: "≥12 test deterministici Tier-1 jsdom — buffer cap (3) + enable/disable lazy (3) + getBuffer deep-clone (2) + clear (1) + step coverage F1+F6 (2) + default NODE_ENV inline (1)"
    - path: "packages/devtools/src/route-inspector.test.ts"
      provides: "≥10 test — capture step 9+10 (3) + aggregate eventId+routeId (2) + retry/cacheHit/policies (3) + buffer cap (2)"
  key_links:
    - from: "packages/devtools/src/event-inspector.ts"
      to: "packages/devtools/src/types/inspector-entry.ts (EventInspectorSnapshot)"
      via: "type only"
      pattern: "EventInspectorSnapshot"
    - from: "packages/devtools/src/route-inspector.ts"
      to: "packages/devtools/src/types/inspector-entry.ts (RouteInspectorEntry)"
      via: "shape contract"
      pattern: "RouteInspectorEntry"
    - from: "packages/devtools/src/event-inspector.ts + route-inspector.ts"
      to: "structuredClone"
      via: "deep clone pattern D-162"
      pattern: "structuredClone\\("
---

<objective>
Wave 3 building block A (parallel con 06-06 ∥ 06-07 — file ownership disgiunta verified): implementazione di **EventInspector** (ring buffer 500 PipelineSnapshot D-167) e **RouteInspector** (ring buffer 500 RouteInspectorEntry D-167).

**Pattern primario carryover**: F5 `task-tracker.ts:140-220` (state Map atomic + bounded counter) + F4 `realtime-channel-manager.ts:90-120` (Map registry pattern). Closure factory ritorna { tap, enable, disable, getBuffer, clear }.

**D-160 lazy-mode early-return**: tap sempre registrato ma `if (!state.enabled) return` first-line — zero overhead in production. Pattern coerente con F5 D-139 assertSerializable dev-mode auto. **Default automatico inline (uniformità D-160 cross-component): `state.enabled = opts.initiallyEnabled ?? (typeof process !== 'undefined' ? process.env.NODE_ENV !== 'production' : true)`**. Browser default true (DX dev-friendly), production false.

**D-162 getDebugSnapshot deep-clone via structuredClone**: ritorna copia immutable buffer per zero side-effect. RESEARCH §15.3 documenta perf: ~5-10ms su buffer pieno (500 × 5KB) — accettabile rare-call.

**D-167 ring buffer cap default 500 + drop-oldest FIFO**: Array.push + Array.shift se length > cap. Memory footprint predictable ~2.5MB con payload medi.

**File ownership Wave 3 (BLOCKER-1 fix)**: 06-05/06-06/06-07 parallel **NON toccano** `packages/devtools/src/index.ts`. Il barrel append cumulativo (Inspector + Metrics + Pause + composition wrappers) è centralizzato in 06-08b Wave 4b sequential gate post-W3.

Output: 2 file source production-ready (~120 LOC totali) + 2 file test (~22+ test deterministici Tier-1 jsdom). Threat coverage T-06-05-01..03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-04-PLAN.md
@CLAUDE.md
@packages/core/src/types/tap.ts
@packages/devtools/src/augment.ts
@packages/devtools/src/types/inspector-entry.ts
@packages/devtools/src/types/devtools-config.ts
@packages/worker/src/task-tracker.ts

<interfaces>
<!-- F1 EventTap + PipelineSnapshot (NON modificare) -->
From packages/core/src/types/tap.ts:
```typescript
export interface PipelineSnapshot {
  readonly eventId: string
  readonly topic: string
  readonly step: PipelineStep
  readonly timestamp: number
  readonly durationMs: number
  readonly payloadBefore?: unknown
  readonly payloadAfter?: unknown
  readonly metadata?: Readonly<Record<string, unknown>>
}
```

<!-- F6 types da 06-01 -->
From packages/devtools/src/types/inspector-entry.ts:
```typescript
export interface RouteInspectorEntry {
  readonly eventId: string
  readonly routeId: string
  readonly topic: string
  readonly type: 'local' | 'http' | 'cache' | 'composite' | 'worker' | 'realtime-inbound'
  readonly outcome: 'success' | 'error' | 'skipped' | 'cached' | 'pending'
  readonly durationMs: number
  readonly retryCount?: number
  readonly cacheHit?: boolean
  readonly policiesApplied?: readonly string[]
  readonly timestamp: number
  readonly errorCode?: string
}
export interface EventInspectorSnapshot {
  readonly enabled: boolean
  readonly bufferEntries: number
  readonly bufferSize: number
}
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | DoS (buffer cresce illimitato) | event-inspector + route-inspector | mitigate | D-167 cap default 500 + drop-oldest FIFO. Test 5 verifica cap rispettato |
| T-06-05-02 | Information Disclosure (getBuffer leak via mutation) | event-inspector + route-inspector | mitigate | D-162 deep-clone via structuredClone. Test 8 verifica mutation return value NON corrompe state interno |
| T-06-05-03 | Logic flaw (disable non clear-buffer leak memoria) | event-inspector + route-inspector | mitigate | disable() clear state.buffer (memory hygiene RESEARCH §6.3). Test 11 verifica buffer===0 post-disable |
| T-06-05-04 | DoS (structuredClone perf disclose su buffer pieno) | getBuffer deep-clone | accept | RESEARCH §15.3 ~5-10ms su 500 entries. Documented DOC-06 "rare-call". Tier-3 perf benchmark in 06-09a |
| T-06-05-05 | Logic flaw (production debug accidentale leak via initiallyEnabled default) | event-inspector default | mitigate | Default inline `process.env.NODE_ENV !== 'production'` → production false (zero leak), browser/dev true. Test 12 verifica fallback NODE_ENV detection |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event-inspector.ts (ring buffer 500 D-167 + lazy mode D-160 + default NODE_ENV inline + deep-clone D-162)</name>
  <files>packages/devtools/src/event-inspector.ts, packages/devtools/src/event-inspector.test.ts</files>
  <read_first>
    - packages/worker/src/task-tracker.ts (analog state Map atomic + closure factory pattern)
    - packages/devtools/src/types/inspector-entry.ts (creato in 06-01)
    - packages/devtools/src/types/devtools-config.ts (creato in 06-01 — DevtoolsConfig.eventBufferSize)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/event-inspector.ts" (LOC 521-580)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §6 Inspector ring buffer impl + §5.3 lazy-mode + §6.3 memory footprint
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-160 + D-162 + D-167
  </read_first>
  <behavior>
    - Test 1: createEventInspector({}) ritorna { tap, enable, disable, getBuffer, clear, getSnapshot } default bufferSize=500. Default initiallyEnabled segue `process.env.NODE_ENV !== 'production'` (in jsdom test env: true)
    - Test 2: tap.onPipelineStep chiamato con state.enabled=false → no-op (state.buffer.length === 0 dopo 100 chiamate). Setup: opts.initiallyEnabled=false explicit
    - Test 3: enable() + 5 onPipelineStep → state.buffer.length === 5
    - Test 4: disable() → state.enabled === false + state.buffer === [] (memory hygiene)
    - Test 5: cap rispettato — bufferSize=10, 15 onPipelineStep → state.buffer.length === 10 + first 5 evicted (FIFO via shift)
    - Test 6: getBuffer() ritorna deep clone (verify via structuredClone identity check — clone !== original reference)
    - Test 7: Mutation safety — getBuffer() = clone; clone.push(fakeEntry) → state.buffer length unchanged
    - Test 8: clear() → state.buffer === [] ma state.enabled inalterato
    - Test 9: Multiple PipelineStep types F1 ('event.received', 'event.delivered') + F6 ('event.observed') tutti catturati
    - Test 10: PipelineSnapshot con payloadBefore/After preservato in buffer (verify via deep equal)
    - Test 11: initiallyEnabled=true → state.enabled === true al boot
    - Test 12: Default NODE_ENV inline detection — mock `process.env.NODE_ENV='production'` + opts={} → state.enabled === false; mock NODE_ENV='development' + opts={} → state.enabled === true; missing process global (browser sim) → state.enabled === true
  </behavior>
  <action>
**RED + GREEN TDD pattern.**

GREEN code (~70 LOC):

```typescript
/**
 * F6 EventInspector — ring buffer 500 PipelineSnapshot + lazy-mode toggle (D-160 + D-167).
 *
 * Pattern primario carryover F5 task-tracker.ts:46-220 (state closure + factory).
 * Pattern lazy-mode early-return D-160 + deep-clone D-162.
 *
 * **D-160 default NODE_ENV inline (uniformità cross-component WARNING-5 fix)**:
 * se `opts.initiallyEnabled` undefined → fallback a
 * `(typeof process !== 'undefined' ? process.env.NODE_ENV !== 'production' : true)`.
 * Browser/dev → true (DX), production → false (zero overhead).
 *
 * @see RESEARCH §6 Inspector ring buffer impl
 */

import type { EventTap, PipelineSnapshot, PipelineStep } from '@sembridge/core'
import type { EventInspectorSnapshot } from './types/inspector-entry'

interface EventInspectorState {
  enabled: boolean
  buffer: PipelineSnapshot[]
  readonly bufferSize: number
}

export interface EventInspector {
  readonly tap: EventTap
  enable(): void
  disable(): void
  getBuffer(): readonly PipelineSnapshot[]
  clear(): void
  getSnapshot(): EventInspectorSnapshot
}

export interface EventInspectorOptions {
  readonly bufferSize?: number
  readonly initiallyEnabled?: boolean
}

const DEFAULT_BUFFER_SIZE = 500 // D-167

/**
 * Default inline detection NODE_ENV (D-160 uniformità cross-component).
 * Production → false (zero overhead). Browser/dev → true (DX dev-friendly).
 */
function detectDefaultEnabled(): boolean {
  try {
    if (typeof process !== 'undefined' && process.env != null) {
      return process.env.NODE_ENV !== 'production'
    }
  } catch {
    /* fallthrough */
  }
  return true
}

export function createEventInspector(opts: EventInspectorOptions = {}): EventInspector {
  const state: EventInspectorState = {
    enabled: opts.initiallyEnabled ?? detectDefaultEnabled(),
    buffer: [],
    bufferSize: opts.bufferSize ?? DEFAULT_BUFFER_SIZE,
  }

  return {
    tap: {
      onPipelineStep(step: PipelineStep, snapshot: PipelineSnapshot): void {
        if (!state.enabled) return // hot-path early return D-160 — zero overhead production
        state.buffer.push(snapshot)
        if (state.buffer.length > state.bufferSize) {
          state.buffer.shift() // FIFO drop oldest
        }
      },
    },
    enable() {
      state.enabled = true
    },
    disable() {
      state.enabled = false
      state.buffer = [] // memory hygiene RESEARCH §6.3
    },
    getBuffer() {
      // D-162 deep-clone via structuredClone — caller mutation NON corrompe state interno
      return structuredClone(state.buffer)
    },
    clear() {
      state.buffer = []
    },
    getSnapshot() {
      return {
        enabled: state.enabled,
        bufferEntries: state.buffer.length,
        bufferSize: state.bufferSize,
      }
    },
  }
}
```

**Decisione minore (Claude's discretion CONTEXT)**: aggiunto metodo `getSnapshot()` per esporre lo state senza dover deep-cloning l'intero buffer (DX-friendly per metrics gauge). Pattern coerente con F5 task-tracker.ts:71-81.

**WARNING-5 fix verbatim**: `detectDefaultEnabled()` gestisce 3 scenari (production / development / browser senza process) per consistency D-160 cross-component (EventInspector standalone NON delega più a wrapper DevtoolsBroker).

**Importante (BLOCKER-1 fix)**: questo task NON modifica `packages/devtools/src/index.ts`. Il barrel append (esportazione `createEventInspector + types`) è gestito esclusivamente in 06-08b Wave 4b sequential gate.

```bash
git add packages/devtools/src/event-inspector.{ts,test.ts}
git commit -m "feat(06-05): GREEN event-inspector ring buffer 500 + lazy mode + default NODE_ENV inline + deep-clone (D-160/D-162/D-167)

createEventInspector closure factory: tap + enable/disable + getBuffer (structuredClone) +
clear + getSnapshot. ~70 LOC zero deps esterne.

Pattern F5 task-tracker state closure carryover. D-160 lazy-mode early-return zero
overhead. Default initiallyEnabled fallback NODE_ENV inline (production false, dev/browser
true) — uniformità cross-component WARNING-5 fix. D-167 cap 500 default + drop-oldest FIFO.

NB: barrel packages/devtools/src/index.ts NON modificato qui (BLOCKER-1 fix — append
cumulativo gestito da 06-08b Wave 4b sequential gate).

12/12 test passing. Coverage v8 ≥90/80/90/90.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test --run event-inspector 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `event-inspector.ts` ~70 LOC + `*.test.ts` 12+ test
    - 2 commit TDD
    - Coverage v8 ≥90/80/90/90
    - NESSUNA modifica a packages/devtools/src/index.ts (BLOCKER-1 fix)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: route-inspector.ts (capture step 9+10 + aggregate per eventId+routeId D-167)</name>
  <files>packages/devtools/src/route-inspector.ts, packages/devtools/src/route-inspector.test.ts</files>
  <read_first>
    - packages/devtools/src/event-inspector.ts (creato in Task 1 — pattern closure factory)
    - packages/devtools/src/types/inspector-entry.ts (RouteInspectorEntry shape)
    - packages/routing/src/outcome-collector.ts (analog step 10 capture pattern F3)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/route-inspector.ts" (LOC 583-606)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §6.2 RouteInspector focus route-level
  </read_first>
  <behavior>
    - Test 1: createRouteInspector({}) ritorna { tap, enable, disable, getBuffer, clear, getSnapshot }
    - Test 2: tap.onPipelineStep('event.route.executed', snapshot) con metadata.routeId + metadata.type → entry creata in buffer
    - Test 3: tap.onPipelineStep('event.outcome.collected', snapshot) con outcome='success' aggrega all'entry esistente per eventId+routeId
    - Test 4: Step diversi da 9+10 → IGNORATI (no entry creata)
    - Test 5: cap rispettato — bufferSize=5, 7 distinct routes → 5 entry + 2 evicted FIFO
    - Test 6: getBuffer() ritorna deep-clone via structuredClone D-162
    - Test 7: retryCount aggregato da metadata se presente (analog F3 03-09 retry)
    - Test 8: cacheHit aggregato da metadata.origin === 'cache' se presente
    - Test 9: policiesApplied aggregato da metadata.policies array
    - Test 10: errorCode catturato per outcome='error'
  </behavior>
  <action>
GREEN code (~80 LOC):

```typescript
/**
 * F6 RouteInspector — cattura step 9 (`event.route.executed`) + step 10
 * (`event.outcome.collected`) F3, aggrega per eventId+routeId in RouteInspectorEntry.
 *
 * Pattern carryover F5 task-tracker (state Map atomic) + F3 outcome-collector
 * (step 10 capture). Default NODE_ENV inline coerente con event-inspector
 * (D-160 uniformità cross-component).
 *
 * @see RESEARCH §6.2 RouteInspector focus route-level
 */

import type { EventTap, PipelineSnapshot, PipelineStep } from '@sembridge/core'
import type { RouteInspectorEntry } from './types/inspector-entry'

interface RouteInspectorState {
  enabled: boolean
  buffer: RouteInspectorEntry[]
  readonly bufferSize: number
  readonly pending: Map<string, RouteInspectorEntry> // eventId+routeId → partial entry
}

export interface RouteInspector {
  readonly tap: EventTap
  enable(): void
  disable(): void
  getBuffer(): readonly RouteInspectorEntry[]
  clear(): void
  getSnapshot(): { readonly enabled: boolean; readonly bufferEntries: number; readonly bufferSize: number }
}

export interface RouteInspectorOptions {
  readonly bufferSize?: number
  readonly initiallyEnabled?: boolean
}

const DEFAULT_BUFFER_SIZE = 500
const STEP_ROUTE_EXECUTED: PipelineStep = 'event.route.executed' as PipelineStep
const STEP_OUTCOME_COLLECTED: PipelineStep = 'event.outcome.collected' as PipelineStep

function detectDefaultEnabled(): boolean {
  try {
    if (typeof process !== 'undefined' && process.env != null) {
      return process.env.NODE_ENV !== 'production'
    }
  } catch {
    /* fallthrough */
  }
  return true
}

function aggregateKey(eventId: string, routeId: string): string {
  return `${eventId}::${routeId}`
}

function pushBuffer(state: RouteInspectorState, entry: RouteInspectorEntry): void {
  state.buffer.push(entry)
  if (state.buffer.length > state.bufferSize) state.buffer.shift()
}

export function createRouteInspector(opts: RouteInspectorOptions = {}): RouteInspector {
  const state: RouteInspectorState = {
    enabled: opts.initiallyEnabled ?? detectDefaultEnabled(),
    buffer: [],
    bufferSize: opts.bufferSize ?? DEFAULT_BUFFER_SIZE,
    pending: new Map(),
  }

  return {
    tap: {
      onPipelineStep(step: PipelineStep, snapshot: PipelineSnapshot): void {
        if (!state.enabled) return
        const meta = snapshot.metadata ?? {}
        const routeId = (meta as { routeId?: string }).routeId
        if (!routeId) return

        const key = aggregateKey(snapshot.eventId, routeId)

        if (step === STEP_ROUTE_EXECUTED) {
          const partial: RouteInspectorEntry = {
            eventId: snapshot.eventId,
            routeId,
            topic: snapshot.topic,
            type: ((meta as { type?: RouteInspectorEntry['type'] }).type ?? 'local') as RouteInspectorEntry['type'],
            outcome: 'pending',
            durationMs: snapshot.durationMs,
            retryCount: (meta as { retryCount?: number }).retryCount,
            policiesApplied: (meta as { policies?: readonly string[] }).policies,
            timestamp: snapshot.timestamp,
          }
          state.pending.set(key, partial)
          return
        }

        if (step === STEP_OUTCOME_COLLECTED) {
          const partial = state.pending.get(key)
          const outcome: RouteInspectorEntry['outcome'] = ((meta as { outcome?: RouteInspectorEntry['outcome'] }).outcome ?? 'success') as RouteInspectorEntry['outcome']
          const final: RouteInspectorEntry = {
            ...(partial ?? {
              eventId: snapshot.eventId,
              routeId,
              topic: snapshot.topic,
              type: 'local',
              durationMs: snapshot.durationMs,
              timestamp: snapshot.timestamp,
              outcome: 'pending',
            }),
            outcome,
            cacheHit: (meta as { origin?: 'cache' | 'remote' }).origin === 'cache' ? true : partial?.cacheHit,
            errorCode: (meta as { errorCode?: string }).errorCode,
            durationMs: snapshot.durationMs,
          }
          state.pending.delete(key)
          pushBuffer(state, final)
        }
      },
    },
    enable() { state.enabled = true },
    disable() { state.enabled = false; state.buffer = []; state.pending.clear() },
    getBuffer() { return structuredClone(state.buffer) },
    clear() { state.buffer = []; state.pending.clear() },
    getSnapshot() {
      return { enabled: state.enabled, bufferEntries: state.buffer.length, bufferSize: state.bufferSize }
    },
  }
}
```

**Importante (BLOCKER-1 fix)**: questo task NON modifica `packages/devtools/src/index.ts`. Barrel append cumulativo in 06-08b.

```bash
git add packages/devtools/src/route-inspector.{ts,test.ts}
git commit -m "feat(06-05): GREEN route-inspector capture step 9+10 + aggregate eventId+routeId (D-167)

createRouteInspector closure factory: tap che cattura \`event.route.executed\` (step 9)
+ \`event.outcome.collected\` (step 10) aggregando per (eventId, routeId) in
RouteInspectorEntry. Pending Map cleanup post-completion. Default NODE_ENV inline
coerente con event-inspector (D-160).

NB: barrel packages/devtools/src/index.ts NON modificato qui (BLOCKER-1 fix).

10/10 test passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `route-inspector.ts` ~80 LOC + `*.test.ts` 10+ test
    - 2 commit TDD
    - Coverage v8 ≥90/80/90/90
    - NESSUNA modifica a packages/devtools/src/index.ts (BLOCKER-1 fix)
  </done>
</task>

</tasks>

<verification>
- 22+ test Tier-1 jsdom passing (12 event-inspector + 10 route-inspector)
- Coverage v8 sui 2 file ≥90/80/90/90
- Pattern carryover F5 task-tracker (state closure) + F3 outcome-collector (step 10 capture)
- D-83 strict: zero modifiche fuori `packages/devtools/src/`
- structuredClone API verified disponibile in jsdom env (Baseline 2022)
- BLOCKER-1 fix: ZERO modifiche a packages/devtools/src/index.ts (verifica `git diff packages/devtools/src/index.ts` = 0 lines per Wave 3)
</verification>

<success_criteria>
- [x] EventInspector ring buffer 500 + lazy-mode + default NODE_ENV inline + deep-clone D-162 ✅
- [x] RouteInspector capture step 9+10 + aggregate eventId+routeId ✅
- [x] disable() clear-buffer (memory hygiene) ✅
- [x] TOOL-01 partial (Inspector pronto per wiring 06-08b) ✅
- [x] TOOL-03 partial (enable/disable API esposta — wiring 06-08b) ✅
- [x] PIPE-01 ext F6 partial (step 9+10 captured + step 14 ready) ✅
- [x] D-83 strict ✓ ✅
- [x] BLOCKER-1 fix: file ownership Wave 3 disgiunta su devtools/src/index.ts ✅
</success_criteria>

<output>
Crea `.planning/phases/06-cache-tooling-avanzato/06-05-SUMMARY.md` con file/test/coverage/pattern carryover/threat coverage T-06-05-01..05/blocks pronti per 06-08b (barrel append cumulative + DevtoolsBroker composition wrapper).
</output>
