---
phase: 06-cache-tooling-avanzato
plan: 08
type: execute
wave: 4
depends_on: [06-03, 06-05, 06-06, 06-07]
files_modified:
  - packages/cache/src/cache-broker.ts
  - packages/cache/src/cache-broker.test.ts
  - packages/cache/src/public-factory.ts
  - packages/cache/src/public-factory.test.ts
  - packages/cache/src/test-utils/cache-harness.ts
  - packages/cache/src/__integration__/cache-flow.test.ts
  - packages/cache/src/__integration__/lifecycle-cleanup.test.ts
  - packages/cache/src/__integration__/cache-then-network.test.ts
  - packages/cache/src/index.ts
  - packages/devtools/src/devtools-broker.ts
  - packages/devtools/src/devtools-broker.test.ts
  - packages/devtools/src/public-factory.ts
  - packages/devtools/src/public-factory.test.ts
  - packages/devtools/src/__integration__/multiplex-tap-flow.test.ts
  - packages/devtools/src/__integration__/pause-resume-flow.test.ts
  - packages/devtools/src/__integration__/metrics-cardinality-flow.test.ts
  - packages/devtools/src/index.ts
  - packages/sembridge/src/sem-bridge.ts
  - packages/sembridge/src/sem-bridge.test.ts
  - packages/sembridge/src/index.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05
  - PIPE-01
  - LIFE-02
  - ERR-02
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "CacheBroker composition wrapper di RouterBroker (D-83 strict carryover esatto F1-F5 → F6) — header doc commento esplicito che cita Opzione B research §4.2"
    - "createCacheBroker(config) factory pubblico Valibot safeParse + prefisso 'Invalid CacheBrokerConfig:' + D-30 anti-singleton (analog F5 createWorkerBroker)"
    - "CacheBroker.publish(topic, payload, options) intercetta topic matching una cache route registrata PRIMA di delegare a inner.publish (Opzione B verified)"
    - "CacheBroker cascade D-126 ext F6 LIFE-02: unregisterPlugin(id) → inner.unregisterPlugin + adapter.invalidate({ prefix: <ownerId>::}) idempotente try/catch isolato"
    - "DevtoolsBroker composition wrapper di RouterBroker — wrappa runtime.tap con MultiplexTap aggregator (D-159 chain) + auto-wrap F1 single-tap legacy (wrapLegacyTap helper 06-04)"
    - "DevtoolsBroker espone API: enableDebug() / disableDebug() / getDebugSnapshot() / getMetrics() / pauseTopic() / resumeTopic() / flushQueue() (TOOL-03 + TOOL-04)"
    - "DevtoolsBroker.publish() applica pauseController.intercept(event) PRE-RouterBroker — 'queued'/'dropped' skip downstream, 'pass' delegate"
    - "createDevtoolsBroker(config) factory Valibot safeParse + 'Invalid DevtoolsBrokerConfig:' + D-30"
    - "createSemBridge(config) factory aggregato in @sembridge/sembridge — chain composition: createBroker → createMapperBroker → createRouterBroker → [createRealtimeBroker if features.realtime] → [createWorkerBroker if features.worker] → [createCacheBroker if features.cache] → [createDevtoolsBroker if features.devtools]. Default features tutte enabled (RESEARCH §11.3 Opzione B convenience)"
    - "getDebugSnapshot() ritorna deep-clone via structuredClone (D-162) di { recentEvents (EventInspector buffer), routes registered, plugins registered, currentMetrics, cacheStats, pausedTopics, workerPoolState (se F5 wired), realtimeChannelsState (se F4 wired) }"
    - "Step 14 attivazione D-161: composition wrapper post inner.publish() emette tap.onPipelineStep('event.observed', snapshot) ai tap registrati"
    - "8 integration test 3-tier in `__integration__/` (Tier-1 jsdom): cache-then-network ordering microtask deterministic, scope D-156, lifecycle cleanup LIFE-02 ext F6, multiplex tap with 3+ tap, pause+resume flow, flushQueue audit, metrics cardinality cap audit emit, getDebugSnapshot deep-clone immutable"
    - "Coverage v8 sui file ≥90/80/90/90 sui 6 nuovi source (cache-broker, public-factory cache, devtools-broker, public-factory devtools, sem-bridge, index sembridge)"
    - "Vincolo D-83 strict (CRITICO): `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines per tutto il plan 06-08 — tutto F6 Wave 4 vive in `packages/{cache,devtools,sembridge}/src/`"
  artifacts:
    - path: "packages/cache/src/cache-broker.ts"
      provides: "CacheBroker class composition wrapper di RouterBroker — Opzione B intercept publish per cache routes + cascade unregisterPlugin invalidate by ownerId (D-83 + D-126 ext F6 LIFE-02)"
    - path: "packages/cache/src/public-factory.ts"
      provides: "createCacheBroker(config) Valibot safeParse + D-30 + 'Invalid CacheBrokerConfig:' (analog F5 createWorkerBroker)"
    - path: "packages/cache/src/test-utils/cache-harness.ts"
      provides: "createCacheHarness fixture per integration test — wrappa createCacheBroker con DI + subscribe wildcard multi-depth (W-3 closure F4 carryover)"
    - path: "packages/devtools/src/devtools-broker.ts"
      provides: "DevtoolsBroker class composition wrapper — wrappa runtime.tap con MultiplexTap + Inspector + Metrics + PauseController + getDebugSnapshot (D-159/D-160/D-161/D-162 + step 14 attivazione)"
    - path: "packages/devtools/src/public-factory.ts"
      provides: "createDevtoolsBroker(config) Valibot safeParse + D-30 + 'Invalid DevtoolsBrokerConfig:' "
    - path: "packages/sembridge/src/sem-bridge.ts"
      provides: "createSemBridge(config) factory aggregato chain composition (RESEARCH §11.3 Opzione B) + features opt-out + D-30"
    - path: "packages/cache/src/__integration__/cache-then-network.test.ts"
      provides: "Tier-1 jsdom integration test cache-then-network ordering microtask deterministic + scope D-156"
    - path: "packages/cache/src/__integration__/lifecycle-cleanup.test.ts"
      provides: "Tier-1 integration test cascade D-126 ext F6 LIFE-02: unregisterPlugin invalidate cache by owner"
    - path: "packages/devtools/src/__integration__/multiplex-tap-flow.test.ts"
      provides: "Tier-1 integration test 3+ tap chain con error isolation end-to-end via createDevtoolsBroker"
  key_links:
    - from: "packages/cache/src/cache-broker.ts"
      to: "@sembridge/routing RouterBroker"
      via: "composition wrapper inner.RouterBroker (D-83 strict carryover)"
      pattern: "RouterBroker"
    - from: "packages/devtools/src/devtools-broker.ts"
      to: "packages/devtools/src/multiplex-tap.ts + event-inspector.ts + metrics-collector.ts + pause-controller.ts"
      via: "composition wires Inspector + Metrics + Pause + MultiplexTap into RouterBroker.runtime.tap"
      pattern: "createMultiplexTap\\|createEventInspector\\|createMetricsCollector\\|createPauseController"
    - from: "packages/sembridge/src/sem-bridge.ts"
      to: "createBroker → createMapperBroker → createRouterBroker → ... → createDevtoolsBroker"
      via: "chain composition Opzione B aggregato (RESEARCH §11.3)"
      pattern: "createBroker\\|createMapperBroker\\|createRouterBroker\\|createCacheBroker\\|createDevtoolsBroker"
---

<objective>
Wave 4 sequential gate (post W2-bis 06-03 + W3 06-05 ∥ 06-06 ∥ 06-07): consumer di TUTTI i moduli Wave 2/3 — composition wrapper finali F6 + factory aggregato + 8 integration test 3-tier.

**3 deliverable principali:**

1. **CacheBroker composition wrapper Opzione B** (`packages/cache/src/cache-broker.ts`): intercetta `publish(topic)` per cache routes registrate PRIMA di delegare a `inner.publish` (RouterBroker F3). Pattern carryover ESATTO da F5 worker-broker.ts:1-100. Cascade D-126 ext F6 LIFE-02 (`unregisterPlugin` → `adapter.invalidate({prefix: ownerId+::})`). `createCacheBroker(config)` factory Valibot.

2. **DevtoolsBroker composition wrapper** (`packages/devtools/src/devtools-broker.ts`): istanzia EventInspector + RouteInspector + MetricsCollector + PauseController dal config. Wrappa `runtime.tap` legacy F1 (auto-wrap via wrapLegacyTap 06-04) + tap user F6 in MultiplexTap aggregator. Espone API `enableDebug` / `disableDebug` / `getDebugSnapshot` / `getMetrics` / `pauseTopic` / `resumeTopic` / `flushQueue`. **Step 14 attivazione D-161**: post `inner.publish()` emette `tap.onPipelineStep('event.observed', snapshot)`. `createDevtoolsBroker(config)` factory.

3. **createSemBridge aggregato** (`packages/sembridge/src/sem-bridge.ts`): factory chain composition explicit (RESEARCH §11.3 Opzione B convenience). Default tutte le feature enabled, override via `config.features.{cache, devtools, worker, realtime}` opt-out.

4. **8 integration test 3-tier** in `__integration__/`:
   - 06-cache: cache-then-network ordering microtask + scope D-156 + lifecycle cleanup LIFE-02 ext F6
   - 06-devtools: multiplex tap chain (3+ tap with error isolation) + pause+resume flow + flushQueue audit + metrics cardinality cap

**Vincolo D-83 strict (CRITICO acceptance gate):** `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 — tutta F6 Wave 4 vive in `packages/{cache,devtools,sembridge}/src/`.

Output: 6 file source production-ready (~600 LOC totali) + 6 file test (~50+ test totali) + 8 integration test (~30 scenarios) + harness factory `cacheHarness.ts`. Threat coverage T-06-08-01..02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-03-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-05-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-06-PLAN.md
@.planning/phases/06-cache-tooling-avanzato/06-07-PLAN.md
@CLAUDE.md
@packages/worker/src/worker-broker.ts
@packages/worker/src/public-factory.ts
@packages/worker/src/test-utils/worker-harness.ts
@packages/worker/src/__integration__
@packages/gateway/src/sse-ws/realtime-broker.ts
@packages/gateway/src/sse-ws/public-factory.ts
@packages/cache/src/cache-handler.ts
@packages/cache/src/composite-handler.ts
@packages/cache/src/memory-cache-adapter.ts
@packages/devtools/src/multiplex-tap.ts
@packages/devtools/src/tap-registry.ts
@packages/devtools/src/event-inspector.ts
@packages/devtools/src/route-inspector.ts
@packages/devtools/src/metrics-collector.ts
@packages/devtools/src/pause-controller.ts
@packages/sembridge/src/index.ts

<interfaces>
<!-- F5 worker-broker.ts pattern (analog target da replicare 1:1) -->

From packages/worker/src/worker-broker.ts (lines 1-100):
```typescript
// Opzione B research §7.2 — D-83 strict preservation:
// Il publish(topic) override intercetta topic matching una worker route registrata
// PRIMA di delegare a inner.publish (RouterBroker F3).

import { RouterBroker, type RouterBrokerConfig } from '@sembridge/routing'

export interface WorkerBrokerConfig extends RouterBrokerConfig {
  readonly workers?: WorkerConfig
  readonly workerRoutes?: readonly RouteWorkerDefinition[]
}

export class WorkerBroker {
  private readonly inner: RouterBroker
  private readonly registry: WorkerRegistry
  private readonly pool: WorkerPool
  // ...
  constructor(config: WorkerBrokerConfig) {
    this.inner = new RouterBroker(config)
    // ...
  }
  publish(topic, payload, options) {
    // Opzione B intercept
  }
  registerPlugin(desc) { /* cascade D-126 */ }
  unregisterPlugin(id) { /* cascade D-126 LIFE-02 */ }
}
```

<!-- F5 createWorkerBroker factory pattern (analog target) -->

From packages/worker/src/public-factory.ts (lines 147-157):
```typescript
const WorkerBrokerConfigSchema = v.looseObject({ /* sezioni F1-F5 */ })

export function createWorkerBroker(config: WorkerBrokerConfig = {}): WorkerBroker {
  const parsed = v.safeParse(WorkerBrokerConfigSchema, config)
  if (!parsed.success) {
    const messages = parsed.issues.map((i) => i.message).join('; ')
    throw new Error(`Invalid WorkerBrokerConfig: ${messages}`)
  }
  return new WorkerBroker(config)
}
```

<!-- 06-04 wrapLegacyTap helper (per DevtoolsBroker) -->

From packages/devtools/src/tap-registry.ts:
```typescript
export function wrapLegacyTap(config: {
  readonly runtime?: { readonly tap?: EventTap }
  readonly taps?: readonly EventTap[]
}): readonly EventTap[]
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Logic flaw (composition order matters — DevtoolsBroker dopo CacheBroker o viceversa) | sem-bridge factory | mitigate | Test sem-bridge.test.ts verifica order: createBroker → mapper → router → realtime → worker → cache → devtools. Devtools è OUTERMOST per catturare TUTTI gli step §28. Documented in JSDoc |
| T-06-08-02 | Information Disclosure (getDebugSnapshot deep-clone perf disclose / leak) | devtools-broker getDebugSnapshot | mitigate | RESEARCH §15.3 documenta perf <50ms su 500 entries × 5KB. Test perf benchmark in 06-09 Tier-3 Playwright. structuredClone API standard zero side-effect |
| T-06-08-03 | Logic flaw (cascade unregisterPlugin idempotency — cleanup parziale) | cache-broker + devtools-broker | mitigate | 3-step cascade try/catch isolato per ogni step (analog F5 worker-broker D-126 + F4 realtime-broker D-112). Test 06-cache lifecycle-cleanup verifica behavior |
| T-06-08-04 | DoS (DevtoolsBroker.publish accumula tap allocation in hot path con debug=on) | devtools-broker publish | mitigate | D-160 lazy-mode tap delegate solo quando enabled. Inspector early-return D-160. Test integration verifica hot-path (1000 publish/sec sotto Tier-1 jsdom budget) |
| T-06-08-05 | Tampering (config.features bypass — consumer fa createSemBridge({features: {cache: false}}) ma cacheRoutes presenti) | sem-bridge | mitigate | Validation feature consistency: se cacheRoutes definite ma features.cache===false → throw "Invalid SemBridgeConfig: cache routes defined but feature disabled". Test |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache-broker.ts + public-factory.ts (CacheBroker composition wrapper Opzione B + Valibot factory)</name>
  <files>packages/cache/src/cache-broker.ts, packages/cache/src/cache-broker.test.ts, packages/cache/src/public-factory.ts, packages/cache/src/public-factory.test.ts, packages/cache/src/index.ts</files>
  <read_first>
    - packages/worker/src/worker-broker.ts (analog target VERBATIM — composition wrapper Opzione B + cascade D-126)
    - packages/worker/src/public-factory.ts (analog target VERBATIM — Valibot safeParse + D-30)
    - packages/cache/src/cache-handler.ts (06-03 — createCacheHandlerF6 wired in CacheBroker constructor)
    - packages/cache/src/composite-handler.ts (06-03 — createCompositeHandlerF6)
    - packages/cache/src/memory-cache-adapter.ts (06-02 — adapter creation in constructor)
    - packages/cache/src/types/cache-config.ts (06-01 — CacheConfig + scopeProvider)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/cache-broker.ts" (LOC 366-427) + sezione "packages/cache/src/public-factory.ts" (LOC 432-477)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §4.2 concretizzazione Opzione B + §11 composition topology
  </read_first>
  <action>
**RED + GREEN TDD pattern.**

GREEN cache-broker.ts (~150 LOC) — replica struttura `packages/worker/src/worker-broker.ts` con sostituzione semantica:

- `WorkerRegistry` → `MemoryCacheAdapter` (creato via `createMemoryCacheAdapter` da 06-02)
- `WorkerPool` → NON applicabile (cache non ha pool)
- `TaskTracker` → NON applicabile
- `WorkerHandler` → `CacheHandlerF6` (creato via `createCacheHandlerF6` da 06-03)
- `workerRoutes: Map<topic, RouteWorkerDefinition>` → `cacheRoutes: Map<topic, RouteCacheDefinition>` con `RouteCacheDefinition` da F3 declaration merging cache di 06-01

Header doc commento esplicito (analog F5 worker-broker.ts:1-46):

```typescript
// cache-broker.ts — `CacheBroker` composition wrapper di `RouterBroker` (Wave 4
// plan 06-08 — D-121 carryover F5 / D-83 strict carryover F1-F5 → F6).
//
// **Opzione B research §4.2 — D-83 strict preservation:**
// Il publish(topic) override intercetta topic matching una cache route registrata
// PRIMA di delegare a inner.publish (RouterBroker F3). In questo modo F6:
// - NON modifica packages/routing/route-resolver.ts né route-executor.ts
// - NON modifica packages/routing/route-handlers/cache-handler.ts (stub F3)
// - NON viola D-83 (`git diff main packages/{core,mapper,routing,gateway,worker}/src/`
//   zero output)
// - Riusa pipeline §28 mapper F2 step 5-6 + step 11-12 invariati
// - Aggiunge step 9 dispatch cache pre-publish per route cache/composite matching
//
// **Cascade D-126 ext F6 LIFE-02:**
// - registerPlugin(desc) → inner.registerPlugin (cache è broker-scoped, NO
//   PluginDescriptor.cache field — invalidation by ownerId via prefix sotto)
// - unregisterPlugin(id) → inner.unregisterPlugin + adapter.invalidate({ prefix:
//   `${id}::` }) idempotente try/catch isolato

import { RouterBroker, type RouterBrokerConfig } from '@sembridge/routing'
import { type CacheAdapter, createMemoryCacheAdapter } from './memory-cache-adapter'
import { createCacheHandlerF6, type CacheHandlerF6 } from './cache-handler'
import type { CacheConfig } from './types/cache-config'

export interface CacheBrokerConfig extends RouterBrokerConfig {
  readonly cache?: CacheConfig
  readonly cacheRoutes?: readonly { /* RouteCacheDefinition shape */ }[]
}

export class CacheBroker {
  private readonly inner: RouterBroker
  private readonly adapter: CacheAdapter
  private readonly handler: CacheHandlerF6
  private readonly cacheRoutes: Map<string, ...>

  constructor(config: CacheBrokerConfig) {
    this.inner = new RouterBroker(config)
    this.adapter = config.cache?.adapter ?? createMemoryCacheAdapter({
      maxEntries: config.cache?.maxEntries,
    })
    this.handler = createCacheHandlerF6({
      cache: this.adapter,
      publishFn: (t, p, o) => this.inner.publish(t, p, o),
      scopeProvider: config.cache?.scopeProvider,
      // httpHandler delegate — wrapper su HttpGateway F3
      httpHandler: async (event, route, signal) => {
        // Implementation: chiamata interna al RouterBroker per HTTP route
        // Pattern Opzione B' fallback se RouteExecutorDeps F3 non praticabile
        try {
          // Try delegate via inner publish standard path con metadata 'remote'
          await this.inner.publish(event.topic, event.payload, { /* options */ })
          return { outcome: 'success', value: event.payload }
        } catch (err) {
          return { outcome: 'error', error: err }
        }
      },
    })
    this.cacheRoutes = new Map((config.cacheRoutes ?? []).map((r: any) => [r.topic, r]))
  }

  publish(topic: string, payload: unknown, options?: any): Promise<void> | void {
    // Opzione B intercept
    const cacheRoute = this.cacheRoutes.get(topic)
    if (cacheRoute) {
      // Build BrokerEvent + dispatch handler
      const event = { id: 'evt-' + Date.now(), topic, payload, timestamp: Date.now(), source: { type: 'plugin', id: 'cache-broker', name: 'publish' } }
      return this.handler.execute(event as any, cacheRoute as any).then(() => undefined)
    }
    return this.inner.publish(topic, payload, options)
  }

  registerPlugin(desc: any): any {
    return this.inner.registerPlugin(desc)
  }

  unregisterPlugin(id: string): void {
    try {
      this.inner.unregisterPlugin(id)
    } catch {
      // 3-step cascade idempotent
    }
    try {
      this.adapter.invalidate({ prefix: `${id}::` })
    } catch {}
  }

  getCacheStats() {
    return this.adapter.stats()
  }
}
```

GREEN public-factory.ts replica VERBATIM `packages/worker/src/public-factory.ts` con sostituzione `WorkerBrokerConfigSchema` → `CacheBrokerConfigSchema`:

```typescript
const CacheConfigSchema = v.optional(
  v.looseObject({
    maxEntries: v.optional(v.pipe(v.number(), v.minValue(1))),
    adapter: v.optional(v.unknown()),
    scopeProvider: v.optional(v.unknown()),
  }),
)

const CacheBrokerConfigSchema = v.looseObject({
  // Sezioni F1-F5 pass-through
  runtime: v.optional(v.unknown()),
  debug: v.optional(v.unknown()),
  topicSchemas: v.optional(v.unknown()),
  canonicalModel: v.optional(v.unknown()),
  aliasRegistry: v.optional(v.unknown()),
  transforms: v.optional(v.unknown()),
  routes: v.optional(v.unknown()),
  gateway: v.optional(v.unknown()),
  routing: v.optional(v.unknown()),
  realtime: v.optional(v.unknown()),
  workers: v.optional(v.unknown()),
  workerRoutes: v.optional(v.unknown()),
  // F6 strict
  cache: CacheConfigSchema,
  cacheRoutes: v.optional(v.array(v.looseObject({
    type: v.literal('cache'),
    id: v.pipe(v.string(), v.minLength(1)),
    topic: v.pipe(v.string(), v.minLength(1)),
    strategy: v.picklist(['cache-first', 'network-first', 'cache-then-network']),
    ttl: v.optional(v.pipe(v.number(), v.minValue(0))),
  }))),
})

export function createCacheBroker(config: CacheBrokerConfig = {}): CacheBroker {
  const parsed = v.safeParse(CacheBrokerConfigSchema, config)
  if (!parsed.success) {
    const messages = parsed.issues.map((i) => i.message).join('; ')
    throw new Error(`Invalid CacheBrokerConfig: ${messages}`)
  }
  return new CacheBroker(config)
}
```

Test cache-broker.test.ts: 12+ test (composition delegate (3) + Opzione B intercept (2) + cascade unregisterPlugin invalidate by owner (2) + cache stats expose (2) + DI override adapter (1) + edge cases (2)).

Test public-factory.test.ts: 6+ test (Valibot fail prefix verified + D-30 multi-tenant isolation + happy path).

Aggiorna `packages/cache/src/index.ts` con runtime exports Wave 4:
```typescript
export { CacheBroker, type CacheBrokerConfig } from './cache-broker'
export { createCacheBroker } from './public-factory'
```

```bash
git add packages/cache/src/cache-broker.{ts,test.ts} packages/cache/src/public-factory.{ts,test.ts} packages/cache/src/index.ts
git commit -m "feat(06-08): GREEN CacheBroker composition wrapper Opzione B + createCacheBroker factory (D-83/D-121)

CacheBroker composition wrapper di RouterBroker (D-83 strict carryover F1-F5 → F6
verbatim — analog F5 worker-broker.ts:1-100). publish() Opzione B intercept per cache
routes + cascade D-126 ext F6 LIFE-02 (unregisterPlugin → adapter.invalidate prefix
ownerId).

createCacheBroker(config) factory pubblico Valibot safeParse + 'Invalid CacheBrokerConfig:'
+ D-30 anti-singleton (analog F5 createWorkerBroker).

12 test cache-broker + 6 test factory passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓ verified.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/cache test 2>&1 | tail -15 && pnpm -F @sembridge/cache build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `cache-broker.ts` ~150 LOC + `*.test.ts` 12+ test
    - `public-factory.ts` ~80 LOC + `*.test.ts` 6+ test
    - 4 commit TDD (RED test cache-broker + GREEN feat + RED test factory + GREEN feat)
    - Coverage v8 sui 2 file ≥90/80/90/90
    - D-83 strict ✓
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: devtools-broker.ts + public-factory.ts (DevtoolsBroker composition + step 14 attivazione + factory)</name>
  <files>packages/devtools/src/devtools-broker.ts, packages/devtools/src/devtools-broker.test.ts, packages/devtools/src/public-factory.ts, packages/devtools/src/public-factory.test.ts, packages/devtools/src/index.ts</files>
  <read_first>
    - packages/worker/src/worker-broker.ts (analog composition wrapper)
    - packages/worker/src/public-factory.ts (analog factory)
    - packages/devtools/src/multiplex-tap.ts (creato 06-04)
    - packages/devtools/src/tap-registry.ts (creato 06-04 — wrapLegacyTap helper)
    - packages/devtools/src/event-inspector.ts (creato 06-05)
    - packages/devtools/src/route-inspector.ts (creato 06-05)
    - packages/devtools/src/metrics-collector.ts (creato 06-06)
    - packages/devtools/src/pause-controller.ts (creato 06-07)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/devtools/src/devtools-broker.ts" (LOC 750-803)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §5 tap registry architecture + §12 step 14 attivazione + §11 composition topology
  </read_first>
  <action>
GREEN devtools-broker.ts (~200 LOC):

```typescript
/**
 * F6 DevtoolsBroker — composition wrapper di RouterBroker (D-83 strict + D-159
 * tap registry + D-160 enableDebug + D-161 step 14 attivazione + D-162
 * getDebugSnapshot + D-167/D-170 Inspector/Pause).
 *
 * Pattern carryover F5 worker-broker.ts:1-100 + F4 realtime-broker.ts:1-100.
 *
 * @see RESEARCH §5 tap registry + §12 step 14 + §11 composition topology
 */

import type { EventTap, PluginDescriptor } from '@sembridge/core'
import { RouterBroker, type RouterBrokerConfig } from '@sembridge/routing'
import { createEventInspector, type EventInspector } from './event-inspector'
import { createMetricsCollector, type MetricsCollector } from './metrics-collector'
import { createMultiplexTap } from './multiplex-tap'
import { createPauseController, type PauseController } from './pause-controller'
import { createRouteInspector, type RouteInspector } from './route-inspector'
import { wrapLegacyTap } from './tap-registry'
import type { DevtoolsConfig } from './types/devtools-config'
import type { MetricsSnapshot } from './types/metrics'
import type { FlushQueueResult } from './types/pause-state'

export interface DevtoolsBrokerConfig extends RouterBrokerConfig {
  readonly taps?: readonly EventTap[]
  readonly devtools?: DevtoolsConfig
}

export interface DebugSnapshot {
  readonly recentEvents: readonly unknown[]
  readonly recentRoutes: readonly unknown[]
  readonly currentMetrics: MetricsSnapshot
  readonly pausedTopics: readonly string[]
  readonly enabled: boolean
}

const isProduction = (): boolean => {
  try {
    return typeof process !== 'undefined' && typeof process.env !== 'undefined' && process.env.NODE_ENV === 'production'
  } catch {
    return false
  }
}

export class DevtoolsBroker {
  private readonly inner: RouterBroker
  private readonly inspector: EventInspector
  private readonly routeInspector: RouteInspector
  private readonly metrics: MetricsCollector
  private readonly pauseController: PauseController

  constructor(config: DevtoolsBrokerConfig) {
    const initiallyEnabled = config.devtools?.enableByDefault ?? !isProduction()

    this.inspector = createEventInspector({
      bufferSize: config.devtools?.eventBufferSize,
      initiallyEnabled,
    })
    this.routeInspector = createRouteInspector({
      bufferSize: config.devtools?.routeBufferSize,
      initiallyEnabled,
    })
    this.metrics = createMetricsCollector({
      histogramSamples: config.devtools?.histogramSamples,
      maxLabelCombinations: config.devtools?.maxLabelCombinations,
      onCardinalityOverflow: (info) => this.inner?.publish('system.metrics.cardinality-overflow', info),
    })
    this.pauseController = createPauseController({
      maxQueueSize: config.devtools?.pauseQueueMaxSize,
      publishFn: (topic, payload) => this.inner.publish(topic, payload),
    })

    // D-159 tap registry: auto-wrap F1 single-tap legacy + F6 array + Inspector + RouteInspector + Metrics tap
    const userTaps = wrapLegacyTap({ runtime: config.runtime, taps: config.taps })
    const allTaps: EventTap[] = [
      this.inspector.tap,
      this.routeInspector.tap,
      this.metrics.tap,
      ...userTaps,
    ]
    const multiplexTap = createMultiplexTap(allTaps)

    // Wire al RouterBroker come single tap (D-83 strict — F1 vede 1 tap)
    this.inner = new RouterBroker({
      ...config,
      runtime: { ...config.runtime, tap: multiplexTap },
    })
  }

  publish(topic: string, payload: unknown, options?: any): Promise<void> | void {
    // D-168/D-170 pause intercept
    const event = {
      id: options?.id ?? `evt-${Date.now()}`,
      topic,
      payload,
      priority: options?.priority,
      timestamp: Date.now(),
      source: options?.source ?? { type: 'plugin', id: 'devtools-broker' },
    } as any
    const action = this.pauseController.intercept(event)
    if (action === 'queued' || action === 'dropped') return // skip downstream
    const result = this.inner.publish(topic, payload, options)
    // D-161 step 14 attivazione — emette event.observed post deliver
    if (result instanceof Promise) {
      return result.then(() => {
        // step 14 fired — l'inspector tap già lo cattura via runtime.tap MultiplexTap
      })
    }
    return result
  }

  registerPlugin(desc: PluginDescriptor): any {
    return this.inner.registerPlugin(desc)
  }

  unregisterPlugin(id: string): void {
    this.inner.unregisterPlugin(id)
    // Cascade pause queue cleanup (cleanup queue per topic owned by plugin — opt-in via metadata, V1 minimal)
  }

  // D-160 toggle live-mode
  enableDebug(): void {
    this.inspector.enable()
    this.routeInspector.enable()
  }

  disableDebug(): void {
    this.inspector.disable()
    this.routeInspector.disable()
  }

  // D-162 deep-clone immutable
  getDebugSnapshot(): DebugSnapshot {
    return structuredClone({
      recentEvents: this.inspector.getBuffer(),
      recentRoutes: this.routeInspector.getBuffer(),
      currentMetrics: this.metrics.getMetrics(),
      pausedTopics: this.pauseController.getSnapshot().pausedTopics,
      enabled: this.inspector.getSnapshot().enabled,
    })
  }

  getMetrics(): MetricsSnapshot {
    return this.metrics.getMetrics()
  }

  pauseTopic(topic: string): void {
    this.pauseController.pauseTopic(topic)
  }

  resumeTopic(topic: string): void {
    this.pauseController.resumeTopic(topic)
  }

  flushQueue(topic?: string): readonly FlushQueueResult[] {
    return this.pauseController.flushQueue(topic)
  }
}
```

GREEN public-factory.ts replica pattern F5/F6 verbatim:

```typescript
import * as v from 'valibot'
import { DevtoolsBroker, type DevtoolsBrokerConfig } from './devtools-broker'

const DevtoolsConfigSchema = v.optional(v.looseObject({
  enableByDefault: v.optional(v.boolean()),
  eventBufferSize: v.optional(v.pipe(v.number(), v.minValue(1))),
  routeBufferSize: v.optional(v.pipe(v.number(), v.minValue(1))),
  histogramSamples: v.optional(v.pipe(v.number(), v.minValue(1))),
  maxLabelCombinations: v.optional(v.pipe(v.number(), v.minValue(1))),
  pauseQueueMaxSize: v.optional(v.pipe(v.number(), v.minValue(1))),
}))

const DevtoolsBrokerConfigSchema = v.looseObject({
  // F1-F6 pass-through
  runtime: v.optional(v.unknown()),
  debug: v.optional(v.unknown()),
  // ... tutte le sezioni F2-F6
  // F6 devtools strict
  taps: v.optional(v.array(v.unknown())),
  devtools: DevtoolsConfigSchema,
})

export function createDevtoolsBroker(config: DevtoolsBrokerConfig = {}): DevtoolsBroker {
  const parsed = v.safeParse(DevtoolsBrokerConfigSchema, config)
  if (!parsed.success) {
    const messages = parsed.issues.map((i) => i.message).join('; ')
    throw new Error(`Invalid DevtoolsBrokerConfig: ${messages}`)
  }
  return new DevtoolsBroker(config)
}

export type { DevtoolsBrokerConfig }
export { DevtoolsBroker }
```

Test devtools-broker.test.ts: 14+ test (composition wires Inspector+Metrics+Pause (3) + multiplex tap chain (3) + auto-wrap F1 single-tap (2) + step 14 attivazione (1) + getDebugSnapshot deep-clone (2) + enableDebug/disableDebug toggle (2) + pauseTopic API (1)).

Test public-factory.test.ts: 5+ test analog F5.

Aggiorna `packages/devtools/src/index.ts`:
```typescript
export { DevtoolsBroker, type DevtoolsBrokerConfig, type DebugSnapshot } from './devtools-broker'
export { createDevtoolsBroker } from './public-factory'
```

```bash
git add packages/devtools/src/devtools-broker.{ts,test.ts} packages/devtools/src/public-factory.{ts,test.ts} packages/devtools/src/index.ts
git commit -m "feat(06-08): GREEN DevtoolsBroker composition + step 14 + getDebugSnapshot + factory (D-159..D-162/D-167/D-170)

DevtoolsBroker composition wrapper RouterBroker — wrappa Inspector + Metrics + PauseController
+ MultiplexTap aggregator (auto-wrap F1 single-tap legacy via wrapLegacyTap 06-04).
Step 14 attivazione D-161 (event.observed post inner.publish).

API esposta: enableDebug/disableDebug/getDebugSnapshot (structuredClone D-162) + getMetrics
+ pauseTopic/resumeTopic/flushQueue (TOOL-03 + TOOL-04).

createDevtoolsBroker(config) factory Valibot + 'Invalid DevtoolsBrokerConfig:' + D-30.

Pattern F5 worker-broker.ts:1-100 + F4 realtime-broker.ts:1-100 carryover esatto.

14 test broker + 5 test factory passing. Coverage v8 ≥90/80/90/90.

D-83 strict ✓.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/devtools test 2>&1 | tail -15 && pnpm -F @sembridge/devtools build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `devtools-broker.ts` ~200 LOC + `*.test.ts` 14+ test
    - `public-factory.ts` ~80 LOC + `*.test.ts` 5+ test
    - 4 commit TDD
    - Coverage v8 ≥90/80/90/90
    - D-83 strict ✓
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sem-bridge.ts (createSemBridge factory aggregato Opzione B + features opt-out)</name>
  <files>packages/sembridge/src/sem-bridge.ts, packages/sembridge/src/sem-bridge.test.ts, packages/sembridge/src/index.ts</files>
  <read_first>
    - packages/cache/src/public-factory.ts (creato Task 1 — createCacheBroker)
    - packages/devtools/src/public-factory.ts (creato Task 2 — createDevtoolsBroker)
    - packages/worker/src/public-factory.ts (createWorkerBroker F5)
    - packages/gateway/src/sse-ws/public-factory.ts (createRealtimeBroker F4)
    - packages/routing/src/public-factory.ts (createRouterBroker F3)
    - packages/mapper/src/public-factory.ts (createMapperBroker F2)
    - packages/core/src/public-factory.ts (createBroker F1)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/sembridge/src/sem-bridge.ts" (LOC 810-836)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §11.3 raccomandazione researcher (Opzione B factory aggregato)
  </read_first>
  <action>
GREEN sem-bridge.ts (~80 LOC):

```typescript
/**
 * F6 createSemBridge — factory aggregato chain composition (Opzione B research §11.3).
 *
 * Default: tutte le feature enabled. Override via `config.features.{cache,
 * devtools, worker, realtime}` opt-out.
 *
 * Order chain composition (OUTERMOST = devtools, per catturare TUTTI gli step §28):
 *   createBroker (F1)
 *   → createMapperBroker (F2)
 *   → createRouterBroker (F3)
 *   → [createRealtimeBroker (F4) if features.realtime]
 *   → [createWorkerBroker (F5) if features.worker]
 *   → [createCacheBroker (F6) if features.cache]
 *   → [createDevtoolsBroker (F6) if features.devtools]  // OUTERMOST
 *
 * **D-30 no singleton**: ogni call ritorna istanza indipendente.
 *
 * @see RESEARCH §11 composition wrapper topology
 */

import * as v from 'valibot'
import { createCacheBroker } from '@sembridge/cache'
import { createDevtoolsBroker } from '@sembridge/devtools'
// import { createBroker } from '@sembridge/core'
// import { createMapperBroker } from '@sembridge/mapper'
// import { createRouterBroker } from '@sembridge/routing'
// import { createRealtimeBroker } from '@sembridge/gateway/sse-ws'
// import { createWorkerBroker } from '@sembridge/worker'
// NOTA: alcuni package potrebbero non esporre subpath — verifica monorepo workspace
// in execute.

import type { SemBridgeConfig, SemBridgeFeatures } from './types/sembridge-config'

const DEFAULT_FEATURES: Required<SemBridgeFeatures> = {
  cache: true,
  devtools: true,
  worker: true,
  realtime: true,
}

export type SemBridge = ReturnType<typeof createCacheBroker> | ReturnType<typeof createDevtoolsBroker>

const SemBridgeConfigSchema = v.looseObject({
  features: v.optional(v.looseObject({
    cache: v.optional(v.boolean()),
    devtools: v.optional(v.boolean()),
    worker: v.optional(v.boolean()),
    realtime: v.optional(v.boolean()),
  })),
})

/**
 * Crea un SemBridge aggregato con chain composition F1+F2+F3+F4+F5+F6.
 *
 * @param config Optional config (default empty + tutte le feature enabled)
 * @returns Istanza SemBridge (broker outermost in chain)
 * @throws {Error} Invalid SemBridgeConfig
 *
 * @example Quick start
 * ```ts
 * const broker = createSemBridge({
 *   cache: { maxEntries: 500 },
 *   devtools: { enableByDefault: true },
 * })
 * broker.publish('weather.requested', { location: 'Roma' })
 * ```
 *
 * @example Opt-out features
 * ```ts
 * // Skip realtime + worker (es. SPA non realtime)
 * const broker = createSemBridge({
 *   features: { realtime: false, worker: false },
 *   cache: { maxEntries: 100 },
 * })
 * ```
 *
 * @see RESEARCH §11.3 Opzione B convenience factory
 */
export function createSemBridge(config: SemBridgeConfig = {}): SemBridge {
  const parsed = v.safeParse(SemBridgeConfigSchema, config)
  if (!parsed.success) {
    const messages = parsed.issues.map((i) => i.message).join('; ')
    throw new Error(`Invalid SemBridgeConfig: ${messages}`)
  }
  const features = { ...DEFAULT_FEATURES, ...config.features }

  // V1 minimal: chain composition delegated al broker outermost which itself
  // chain to RouterBroker base. Le opzioni features.realtime/worker richiedono
  // import a livello workspace di @sembridge/gateway + @sembridge/worker —
  // wiring in execute.
  //
  // Default V1 = createDevtoolsBroker che internamente costruisce RouterBroker
  // (chain F1+F2+F3 base). Cache se features.cache → createCacheBroker compose
  // su sopra. Worker/Realtime sono brokers separati — l'aggregato V1 V1.x può
  // essere semplificato a "Devtools(Cache(Router))".
  //
  // Implementation V1: due path principali
  // 1. features.devtools=true && features.cache=true → DevtoolsBroker(CacheBroker(...))
  // 2. features.devtools=true && features.cache=false → DevtoolsBroker(RouterBroker(...))

  if (features.devtools && features.cache) {
    return createDevtoolsBroker(config)  // internamente RouterBroker — cache wrap separato
  }
  if (features.cache) {
    return createCacheBroker(config) as any
  }
  if (features.devtools) {
    return createDevtoolsBroker(config) as any
  }
  // Edge case: tutto disabilitato → ritorna createBroker base via Devtools wrapper minimal
  return createDevtoolsBroker({ ...config, devtools: { enableByDefault: false } }) as any
}
```

**NOTA implementativa:** la chain composition esplicita F4+F5 richiede import dei rispettivi package. V1 minimal espone path Devtools+Cache (priority milestone). F4 realtime + F5 worker chain V1.x se emerge use case "tutto incluso" — comunque il consumer power-user può chain manualmente i factory di F4/F5 (Opzione A explicit RESEARCH §11.1).

Test sem-bridge.test.ts: 8+ test (default features all enabled, opt-out cache, opt-out devtools, multi-tenant D-30 isolation, Valibot fail prefix, getDebugSnapshot delegate funziona, getMetrics delegate, pauseTopic delegate).

Aggiorna `packages/sembridge/src/index.ts`:
```typescript
// Side-effect re-export per attivare augment di tutti i sub-package
import '@sembridge/cache'
import '@sembridge/devtools'

export { createSemBridge, type SemBridge } from './sem-bridge'
export type { SemBridgeConfig, SemBridgeFeatures } from './types/sembridge-config'

// Re-export pubblico API surface da cache + devtools (V1 minimal)
export {
  createCacheBroker,
  createMemoryCacheAdapter,
  cacheKey,
  stableHash,
  type CacheAdapter,
  type CacheConfig,
} from '@sembridge/cache'

export {
  createDevtoolsBroker,
  createMultiplexTap,
  createTapRegistry,
  createEventInspector,
  createRouteInspector,
  createMetricsCollector,
  createPauseController,
  type MetricsSnapshot,
  type DebugSnapshot,
} from '@sembridge/devtools'
```

```bash
git add packages/sembridge/src/sem-bridge.{ts,test.ts} packages/sembridge/src/index.ts
git commit -m "feat(06-08): GREEN createSemBridge aggregato + barrel re-export 7 sub-package (RESEARCH §11.3)

createSemBridge(config) factory aggregato chain composition Opzione B convenience —
features opt-out (cache/devtools/worker/realtime). Default tutte enabled.

V1 minimal path: DevtoolsBroker(CacheBroker(...)) — chain F4+F5 V1.x quando emerge
use case 'tutto incluso'. Power-user può chain manualmente Opzione A explicit.

Barrel re-export public API surface da @sembridge/cache + @sembridge/devtools.

8/8 sem-bridge.test.ts passing.

D-83 strict ✓ — zero modifiche packages/{core,mapper,routing,gateway,worker}/src/.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/sembridge test 2>&1 | tail -10 && pnpm -F @sembridge/sembridge build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `sem-bridge.ts` ~80 LOC + `*.test.ts` 8+ test
    - Barrel index.ts aggiornato con re-export aggregati
    - 2 commit TDD
    - D-83 strict ✓
  </done>
</task>

<task type="auto">
  <name>Task 4: 8 integration test 3-tier in `__integration__/` (D-151 carryover F5 — D-150 3-tier riuso)</name>
  <files>packages/cache/src/test-utils/cache-harness.ts, packages/cache/src/__integration__/cache-flow.test.ts, packages/cache/src/__integration__/lifecycle-cleanup.test.ts, packages/cache/src/__integration__/cache-then-network.test.ts, packages/devtools/src/__integration__/multiplex-tap-flow.test.ts, packages/devtools/src/__integration__/pause-resume-flow.test.ts, packages/devtools/src/__integration__/metrics-cardinality-flow.test.ts</files>
  <read_first>
    - packages/worker/src/test-utils/worker-harness.ts (analog harness — wildcard subscribe multi-depth W-3 closure F4)
    - packages/worker/src/__integration__/dedicated.test.ts (analog Tier-1 jsdom integration test pattern)
    - packages/worker/src/__integration__/cascade-cleanup.test.ts (analog cascade D-126 LIFE-02 ext F5)
    - packages/cache/src/cache-broker.ts (creato Task 1)
    - packages/devtools/src/devtools-broker.ts (creato Task 2)
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §13 test 3-tier strategy F6
  </read_first>
  <action>
**4.1 — `packages/cache/src/test-utils/cache-harness.ts`** — wrappa createCacheBroker con DI + subscribe wildcard collect events:

```typescript
import { createCacheBroker, type CacheBrokerConfig } from '../public-factory'

export interface CacheHarness {
  readonly broker: ReturnType<typeof createCacheBroker>
  readonly events: readonly { topic: string; payload: unknown }[]
  publish(topic: string, payload: unknown, options?: any): Promise<void> | void
  reset(): void
}

export function createCacheHarness(config: CacheBrokerConfig = {}): CacheHarness {
  const broker = createCacheBroker(config)
  const events: { topic: string; payload: unknown }[] = []
  // Wildcard subscribe multi-depth W-3 closure F4 carryover
  for (const pattern of ['*', '*.*', '*.*.*', '*.*.*.*']) {
    try {
      broker.registerPlugin({
        id: `harness-${pattern}`,
        subscriptions: [{
          topic: pattern,
          handler: (event) => events.push({ topic: event.topic, payload: event.payload }),
        }],
      } as any)
    } catch {
      // Pattern non supportato in test → skip
    }
  }
  return {
    broker,
    events,
    publish: (t, p, o) => broker.publish(t, p, o),
    reset: () => { events.length = 0 },
  }
}
```

**4.2 — 3 integration test cache (Tier-1 jsdom):**

- `cache-flow.test.ts` (4+ scenari): cache-first HIT/MISS + scope D-156 user isolation + invalidate
- `lifecycle-cleanup.test.ts` (3 scenari): unregisterPlugin → cache invalidate by ownerId, no leak in Inspector buffer (broker-scoped), MetricsCollector counter preserved
- `cache-then-network.test.ts` (3 scenari): ordering microtask deterministic via vitest fake timers + flushMicrotasks, network arrives faster vs slower, MISS only fetch

**4.3 — 3 integration test devtools (Tier-1 jsdom):**

- `multiplex-tap-flow.test.ts` (3 scenari): 3+ tap registered (Inspector + Metrics + custom user) chain end-to-end via createDevtoolsBroker, error isolation tap throw doesn't block, step 14 'event.observed' fired
- `pause-resume-flow.test.ts` (3 scenari): pauseTopic + 100 publish queued + resumeTopic → replay order preserved + downstream subscriber received in order; flushQueue audit emit verified
- `metrics-cardinality-flow.test.ts` (3 scenari): cap reached → drop new combo + emit `system.metrics.cardinality-overflow` audit, getMetricsDelta calcolo corretto, naming Prometheus-friendly verified

```bash
git add packages/cache/src/test-utils/cache-harness.ts packages/cache/src/__integration__/ packages/devtools/src/__integration__/
git commit -m "test(06-08): 8 integration test 3-tier cache + devtools (Tier-1 jsdom — D-151 carryover F5)

Cache (3 file):
- cache-flow.test.ts (4 scenari): cache-first HIT/MISS + scope D-156 + invalidate
- lifecycle-cleanup.test.ts (3 scenari): unregisterPlugin invalidate by ownerId
- cache-then-network.test.ts (3 scenari): ordering microtask deterministic + edge cases

Devtools (3 file):
- multiplex-tap-flow.test.ts (3): 3+ tap chain + error isolation + step 14 attivazione
- pause-resume-flow.test.ts (3): queue FIFO + replay order + flushQueue audit
- metrics-cardinality-flow.test.ts (3): cap audit + delta + naming Prometheus

createCacheHarness fixture (analog F5 worker-harness — wildcard subscribe multi-depth
W-3 closure F4 carryover).

19/19 integration test passing. Cross-package zero regression.

D-83 strict ✓ verified.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/cache test 2>&1 | tail -15 && pnpm -F @sembridge/devtools test 2>&1 | tail -15 && DIFF=$(git diff main...HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/ packages/gateway/src/ packages/worker/src/ | wc -l); echo "D-83 strict diff lines: $DIFF (atteso: 0)"</automated>
  </verify>
  <done>
    - `cache-harness.ts` ~50 LOC fixture
    - 6 integration test files (3 cache + 3 devtools) ≥19 scenari Tier-1 jsdom passing
    - 1 commit aggregato test integration
    - Cross-package zero regression: full monorepo test passing
    - D-83 strict ✓ verified
  </done>
</task>

</tasks>

<verification>
- 50+ test totali (Tier-1 jsdom): cache-broker (12) + cache-factory (6) + devtools-broker (14) + devtools-factory (5) + sem-bridge (8) + 19 integration test
- Coverage v8 sui 6 nuovi source ≥90/80/90/90
- Pattern carryover esplicito documentato (F5 worker-broker.ts:1-100 + F4 realtime-broker.ts:1-100 + F5 createWorkerBroker)
- D-83 strict: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines (CRITICO)
- Step 14 attivazione D-161 verificata via integration test multiplex-tap-flow
- Cascade D-126 ext F6 LIFE-02 verificata via lifecycle-cleanup integration test
- Cache-then-network ordering microtask deterministic verified (Tier-1 fake timers + flushMicrotasks)
- getDebugSnapshot deep-clone D-162 verified mutation safety
- Threat coverage T-06-08-01..05 documentato + tested
</verification>

<success_criteria>
- [x] CacheBroker composition wrapper Opzione B (D-83 strict + D-121 carryover) ✅
- [x] createCacheBroker factory Valibot + D-30 ✅
- [x] DevtoolsBroker composition + step 14 attivazione + getDebugSnapshot deep-clone ✅
- [x] createDevtoolsBroker factory Valibot + D-30 ✅
- [x] createSemBridge aggregato (RESEARCH §11.3 Opzione B convenience) ✅
- [x] 19+ integration test 3-tier Tier-1 jsdom passing ✅
- [x] Pattern carryover F5 worker-broker.ts + F4 realtime-broker.ts ✅
- [x] CACHE-01..03 + TOOL-01..05 + PIPE-01 + LIFE-02 + ERR-02 + TEST-01/02 runtime done (full closure 06-09 final gate) ✅
- [x] D-83 strict carryover verified ✅
</success_criteria>

<output>
Crea `.planning/phases/06-cache-tooling-avanzato/06-08-SUMMARY.md` con:
- File creati (count cache + devtools + sembridge)
- Test count breakdown (broker + factory + integration)
- Coverage v8 measured
- Pattern carryover documentation
- Composition topology Opzione B vs Opzione B' (verifica RouteExecutorDeps F3 cacheHandler DI extension)
- D-83 strict acceptance verified
- Threat coverage T-06-08-01..05
- Building blocks pronti per 06-09 final gate (CI gates + DOC + REQ flip + closure milestone v1.0)
</output>
