---
phase: 06-cache-tooling-avanzato
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - packages/cache/src/memory-cache-adapter.ts
  - packages/cache/src/memory-cache-adapter.test.ts
  - packages/cache/src/stable-hash.ts
  - packages/cache/src/stable-hash.test.ts
  - packages/cache/src/index.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
must_haves:
  truths:
    - "createMemoryCacheAdapter ritorna un CacheAdapter LRU bounded con maxEntries default 1000 (D-158): get/set/delete/invalidate/size/clear/stats funzionanti"
    - "Eviction LRU verified: con cap=10, dopo 11 set sequenziali la prima entry inserita è evicted (Map insertion order)"
    - "TTL expiry verified: entry con ttlMs=100 dopo Date.now()+200 → cache.get ritorna undefined + entry rimossa + counter evictions++"
    - "LRU touch on get: cache.get(k1) re-ordina k1 in coda Map (insertion order) — la PROSSIMA eviction droppa k2 (next-oldest)"
    - "invalidate(string) invalida exact key + ritorna 1; invalidate(RegExp) invalida tutti i match + ritorna count; invalidate({prefix}) invalida startsWith + ritorna count"
    - "stats() ritorna readonly { hits, misses, evictions, entries } cumulative dal boot adapter"
    - "stableHash(value) determinismo: stesso payload → stesso 8-char hex hash su 1000 iterazioni"
    - "stableStringify key ordering invariance: { a: 1, b: 2 } produce identica stringa di { b: 2, a: 1 }"
    - "FNV-1a 32-bit collision rate <1e-3 su 10k random payload (citato C1 RESEARCH §3.2)"
    - "cacheKey({topic, payload}) ritorna formato `${topic}::${hash8}` (es. `weather.requested::a1b2c3d4`)"
    - "cacheKey({topic, payload, scope: 'user-42'}) ritorna `user-42::weather.requested::a1b2c3d4` (D-156 anti cross-tenant)"
    - "Coverage v8 sui 2 file ≥90/80/90/90 (statements/branches/functions/lines)"
    - "Vincolo D-83 strict: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines"
    - "File ownership disgiunta da 06-04 (parallel Wave 2): 06-02 owns `packages/cache/src/{memory-cache-adapter,stable-hash}.{ts,test.ts}` + index.ts append section. 06-04 owns `packages/devtools/src/{multiplex-tap,tap-registry}.{ts,test.ts}` + devtools/index.ts append. Zero overlap."
  artifacts:
    - path: "packages/cache/src/memory-cache-adapter.ts"
      provides: "createMemoryCacheAdapter({ maxEntries }): CacheAdapter — LRU bounded Map insertion order + TTL expiry + invalidate dispatch + stats cumulative (D-158)"
    - path: "packages/cache/src/stable-hash.ts"
      provides: "stableStringify, fnv1a32, stableHash, cacheKey utility per cache key derivation (D-155 + D-156 prefix scope)"
    - path: "packages/cache/src/memory-cache-adapter.test.ts"
      provides: "≥15 test deterministici Tier-1 jsdom — LRU eviction (5), TTL expiry (3), invalidate (4), stats (2), edge cases (1)"
    - path: "packages/cache/src/stable-hash.test.ts"
      provides: "≥10 test deterministici — determinismo (3), key ordering invariance (2), collision rate (2), cacheKey format (3)"
  key_links:
    - from: "packages/cache/src/memory-cache-adapter.ts"
      to: "packages/cache/src/types/cache-adapter.ts"
      via: "implements CacheAdapter interface"
      pattern: "CacheAdapter"
    - from: "packages/cache/src/stable-hash.ts"
      to: "packages/cache/src/cache-handler.ts (06-03)"
      via: "consumed by cache-handler default key derivation"
      pattern: "cacheKey\\("
    - from: "packages/cache/src/index.ts"
      to: "createMemoryCacheAdapter + cacheKey + stableHash"
      via: "public barrel export Wave 2"
      pattern: "createMemoryCacheAdapter\\|cacheKey\\|stableHash"
---

<objective>
Wave 2 building block A (parallel con 06-04 — file ownership disgiunta): implementazione runtime di `MemoryCacheAdapter` LRU bounded e utility `stable-hash` riusato dal CacheHandler (06-03) per la cache key default `${topic}::${stableHash(canonicalPayload)}` (D-155).

**Pattern primario carryover**: F3 D-74 KeyBased dedupe (`packages/gateway/src/http/strategies/dedupe-strategy.ts:65-90`) — generalizzato qui in `stableStringify` + `fnv1a32` + `cacheKey`. Inline ~30 LOC, zero dep esterna (RESEARCH §3.2 — `json-stable-stringify` valutato 2 KB ma rigettato; FNV-1a sufficient per cache key NON crypto).

**Pattern secondario NEW**: Map insertion order LRU (RESEARCH §2.2 — idiomatic JS dal 2015 ECMAScript spec, zero LOC overhead vs library, Baseline universale). `lru-cache@11.3.6` valutato ma rigettato per zero-dep priority + budget bundle stretto.

**TDD RED→GREEN co-located** (D-149 carryover F5): per ogni file source `*.ts` il `*.test.ts` è committato PRIMA (RED) — feat commit successivo (GREEN). Pattern verbatim 05-02/05-03/05-04/05-05.

Output: 2 file source production-ready (memory-cache-adapter.ts ~120 LOC + stable-hash.ts ~60 LOC), 2 file test (~25+ test totali Tier-1 jsdom deterministici), `packages/cache/src/index.ts` aggiornato con runtime exports.

Threat coverage: T-06-02-01 (DoS cache illimitata) → cap maxEntries; T-06-02-02 (Information Disclosure cross-tenant) → cacheKey scope prefix D-156 verified test; T-06-02-04 (Logic flaw TTL race) → atomic single-thread JS event loop verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md
@.planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md
@.planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md
@.planning/phases/06-cache-tooling-avanzato/06-01-PLAN.md
@CLAUDE.md
@packages/cache/src/types/cache-adapter.ts
@packages/cache/src/types/cache-entry.ts
@packages/cache/src/index.ts
@packages/gateway/src/http/strategies/dedupe-strategy.ts
@packages/gateway/src/http/strategies/dedupe-strategy.test.ts

<interfaces>
<!-- Type pubblici creati in 06-01 da consumare in 06-02 -->

From packages/cache/src/types/cache-adapter.ts:
```typescript
export interface CacheAdapter {
  get<T = unknown>(key: string): CacheEntry<T> | undefined
  set<T = unknown>(key: string, value: T, ttlMs?: number): void
  delete(key: string): boolean
  invalidate(pattern: string | RegExp | { readonly prefix: string }): number
  size(): number
  clear(): void
  stats(): CacheStats
}
export interface CacheStats {
  readonly hits: number
  readonly misses: number
  readonly evictions: number
  readonly entries: number
}
```

From packages/cache/src/types/cache-entry.ts:
```typescript
export interface CacheEntry<T = unknown> {
  readonly value: T
  readonly expiresAt: number  // Number.POSITIVE_INFINITY se TTL non set
  readonly setAt: number
}
```

<!-- Pattern analog F3 dedupe strategy KeyBased (D-74 carryover esatto) -->

From packages/gateway/src/http/strategies/dedupe-strategy.ts (riferimento — NON modificare D-83):
```typescript
// L'analog F3 usa Map<string, Promise<unknown>> + cap maxInflight + cleanup finally
// F6 generalizza il pattern KEY DERIVATION (stable hash) e cap LRU eviction
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory-cache-adapter | input untrusted (key string, value unknown) |
| stable-hash | input untrusted (payload arbitrary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | DoS (cache cresce illimitato → memory exhaustion) | memory-cache-adapter | mitigate | D-158 cap `maxEntries=1000` default + LRU eviction (Map.keys().next().value primo key droppato) — Test 5 fill 1001 entries verifica cap rispettato + first eviction |
| T-06-02-02 | Information Disclosure (cache cross-tenant — utente A serve dati utente B) | stable-hash cacheKey | mitigate | D-156 scope hybrid prefix — `cacheKey({topic, payload, scope: 'user-42'})` produce `user-42::...`. Test 9 verifica due scope diversi → key diverse anche con stesso payload |
| T-06-02-03 | Tampering (consumer muta `cache.get()` value reference) | memory-cache-adapter | accept | Cache restituisce reference (perf consideration RESEARCH §14.2). Documentato in JSDoc: consumer responsabile freeze in dev (D-29 pattern F1 carryover). Deep clone su get rejected (5-10ms overhead 500 entries — RESEARCH §15.3) |
| T-06-02-04 | Logic flaw (TTL expiry race con concurrent get/set) | memory-cache-adapter | mitigate | TTL check `entry.expiresAt < Date.now()` su get atomic single-thread JS event loop (D-133 carryover F5 pattern). Auto-evict expired su read (lazy expiration, NO proactive sweeper — RESEARCH §15.7) — Test 7 |
| T-06-02-05 | Tampering (stable-hash collision crafted attack — payload diversi → stesso hash) | stable-hash fnv1a32 | accept | FNV-1a NON crypto-grade. Cache key NON è security boundary — collision worst case = wrong cache hit (servire dati di altro request) ma scope D-156 mitigation rende difficile sfruttare. Documentato in JSDoc + RESEARCH §3.2 caveat |
| T-06-02-06 | DoS (stableStringify ricorsione infinita su cyclic structure) | stable-hash | mitigate | Caveat documentato: "NO cyclic-detection — assume canonical payload acyclic (pattern F2 invariante)". Test 11 verifica throw `RangeError: Maximum call stack` su cyclic input — caller responsabile (mapper F2 garantisce acyclic) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stable-hash.ts (RED→GREEN — D-149 carryover F5)</name>
  <files>packages/cache/src/stable-hash.ts, packages/cache/src/stable-hash.test.ts</files>
  <read_first>
    - packages/gateway/src/http/strategies/dedupe-strategy.ts (analog D-74 KeyBased — pattern key derivation inline)
    - packages/cache/src/types/cache-adapter.ts (creato in 06-01 — non modificare)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/stable-hash.ts" (LOC 150-194) — pattern excerpt verbatim
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §3 cache key stable hash impl + §3.2 stable hash 3 opzioni (FNV-1a adopted) + §3.3 override route-level
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-155 + D-156
  </read_first>
  <behavior>
    - Test 1: `stableStringify(null)` ritorna `'null'`
    - Test 2: `stableStringify(42)` ritorna `'42'`
    - Test 3: `stableStringify('foo')` ritorna `'"foo"'`
    - Test 4: `stableStringify({ b: 2, a: 1 })` === `stableStringify({ a: 1, b: 2 })` (key ordering invariance — D-155 determinismo)
    - Test 5: `stableStringify([3, 1, 2])` ritorna `'[3,1,2]'` (array NON re-ordinato — preserva semantica)
    - Test 6: `stableStringify({ nested: { z: 1, a: 2 } })` ricorsione corretta — output stable
    - Test 7: `fnv1a32('foo')` ritorna stringa esadecimale 8-char (lowercase, padded)
    - Test 8: `fnv1a32('foo') === fnv1a32('foo')` su 1000 iterazioni (determinismo)
    - Test 9: `fnv1a32('foo') !== fnv1a32('bar')` (diverso input → diverso hash atteso)
    - Test 10: `stableHash({ a: 1, b: 2 })` === `stableHash({ b: 2, a: 1 })` (D-155 determinismo end-to-end)
    - Test 11: `cacheKey({ topic: 'weather.requested', payload: { city: 'Roma' } })` ritorna formato `weather.requested::<8 hex chars>` (regex `/^weather\.requested::[0-9a-f]{8}$/`)
    - Test 12: `cacheKey({ topic: 't', payload: {}, scope: 'user-42' })` ritorna `user-42::t::<hex>` (D-156 prefix scope)
    - Test 13: `cacheKey({ topic: 't', payload: {}, scope: null })` === `cacheKey({ topic: 't', payload: {} })` (null scope = no prefix)
    - Test 14: collision rate — 10000 random payload `{ id: i, value: random }` per i in 0..9999 → collision count <10 (≤0.1%) — soft assertion documentata
    - Test 15: `stableStringify(cyclic)` throw RangeError — caller responsibility caveat documentato (T-06-02-06)
  </behavior>
  <action>
**RED commit (test scritto PRIMA):**
```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"
# Scrivi packages/cache/src/stable-hash.test.ts con 15 test descritti in <behavior>
# vitest run --filter stable-hash → tutti FAIL (no source)
git add packages/cache/src/stable-hash.test.ts
git commit -m "test(06-02): add RED test for stable-hash (D-155 cache key default + FNV-1a + stableStringify)

15 test deterministici Tier-1 jsdom: determinismo (3), key ordering invariance (2),
FNV-1a determinism + collision soft (3), cacheKey format (3), scope prefix D-156 (2),
acyclic invariant caveat (1), edge cases (1).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**GREEN commit (source minimal pass):**

Crea `packages/cache/src/stable-hash.ts` (~60 LOC) replicando esattamente il pattern di RESEARCH §3.2 + PATTERNS §150-194:

```typescript
/**
 * F6 stable-hash — utility pure per derivazione cache key deterministica
 * (D-155 default + D-156 scope prefix).
 *
 * **Pattern primario carryover**: F3 D-74 KeyBased dedupe strategy
 * (`packages/gateway/src/http/strategies/dedupe-strategy.ts`) — generalizzato
 * qui in funzione pubblica esposta al CacheHandler (06-03).
 *
 * **NO crypto-grade** — FNV-1a 32-bit è hash deterministico per cache key,
 * NON sicurezza (collision worst case = wrong cache hit, mitigato da scope
 * D-156). Cite C1 RESEARCH §3.2 — collision rate ~1e-6 per 100k entries.
 *
 * **Acyclic invariant**: `stableStringify` NON gestisce ciclic structure.
 * Caller responsabile garantire payload acyclic — Mapper F2 invariante per
 * canonical payload (V1 garantisce). Cyclic input → RangeError stack overflow
 * (T-06-02-06 documented).
 *
 * @see RESEARCH §3 cache key stable hash impl
 * @see prd.md §20.1 cache key behavior
 */

/**
 * JSON stringify deterministico — chiavi degli oggetti ordinate alfabeticamente.
 * Array NON ri-ordinati (preserve semantic order).
 *
 * @param value Input qualsiasi (tipicamente canonical payload post-mapper F2)
 * @returns Stringa JSON con object key ordering deterministico
 * @throws RangeError se cyclic structure (caller responsibility — F2 garantisce acyclic)
 */
export function stableStringify(value: unknown): string {
  if (value === null || typeof value !== 'object') return JSON.stringify(value)
  if (Array.isArray(value)) return `[${value.map(stableStringify).join(',')}]`
  const keys = Object.keys(value as Record<string, unknown>).sort()
  const parts = keys.map(
    (k) => `${JSON.stringify(k)}:${stableStringify((value as Record<string, unknown>)[k])}`,
  )
  return `{${parts.join(',')}}`
}

/**
 * FNV-1a 32-bit hash inline. Riferimento: http://www.isthe.com/chongo/tech/comp/fnv/
 *
 * ~10 LOC, no deps, ~1e-6 collision per 100k entries.
 * Output: 8-char hex string lowercase padded (`'a1b2c3d4'`).
 *
 * @param str Input string (tipicamente output di stableStringify)
 * @returns 8-char lowercase hex hash
 */
export function fnv1a32(str: string): string {
  let hash = 0x811c9dc5 // FNV offset basis 32-bit
  for (let i = 0; i < str.length; i++) {
    hash ^= str.charCodeAt(i)
    hash = Math.imul(hash, 0x01000193) // FNV prime 32-bit
  }
  return (hash >>> 0).toString(16).padStart(8, '0')
}

/**
 * Stable hash combinato: stableStringify + fnv1a32. Helper di alto livello per
 * il CacheHandler (06-03).
 *
 * @example
 * stableHash({ city: 'Roma' }) === stableHash({ city: 'Roma' })  // sempre true
 * stableHash({ a: 1, b: 2 }) === stableHash({ b: 2, a: 1 })      // sempre true (key ordering invariance)
 */
export function stableHash(value: unknown): string {
  return fnv1a32(stableStringify(value))
}

/**
 * Costruisce la cache key finale per un evento (D-155 default + D-156 scope).
 *
 * Formato:
 * - Senza scope: `${topic}::${hash8}` (es. `weather.requested::a1b2c3d4`)
 * - Con scope: `${scope}::${topic}::${hash8}` (es. `user-42::weather.requested::a1b2c3d4`)
 *
 * @param opts.topic BrokerEvent topic (es. `'weather.requested'`)
 * @param opts.payload Canonical payload post-mapper F2
 * @param opts.scope Optional user/tenant scope D-156 — anti cross-tenant leakage
 * @returns Cache key string deterministico
 *
 * @example
 * cacheKey({ topic: 'weather.requested', payload: { location: 'Roma' } })
 * // → 'weather.requested::a1b2c3d4'
 *
 * cacheKey({ topic: 'weather.requested', payload: {...}, scope: 'user-42' })
 * // → 'user-42::weather.requested::a1b2c3d4'
 *
 * @see RESEARCH §3.3 override route-level via RouteDefinition.cache.key
 * @see prd.md §20.1 cache key behavior
 */
export function cacheKey(opts: {
  readonly topic: string
  readonly payload: unknown
  readonly scope?: string | null
}): string {
  const baseKey = `${opts.topic}::${stableHash(opts.payload)}`
  return opts.scope ? `${opts.scope}::${baseKey}` : baseKey
}
```

```bash
# vitest run --filter stable-hash → tutti GREEN
git add packages/cache/src/stable-hash.ts
git commit -m "feat(06-02): GREEN stable-hash (D-155 default + D-156 scope prefix + FNV-1a)

stableStringify + fnv1a32 + stableHash + cacheKey utility pure (~60 LOC, zero deps,
analog F3 D-74 KeyBased dedupe-strategy.ts:65-90).

Pattern carryover D-74 esatto. Acyclic invariant documented (T-06-02-06).
Collision rate ~1e-6 per 100k entries (cite C1 RESEARCH §3.2).

15/15 test passing.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**Acceptance grep:**
- `grep -c "function stableStringify" packages/cache/src/stable-hash.ts` = 1
- `grep -c "function fnv1a32" packages/cache/src/stable-hash.ts` = 1
- `grep -c "function cacheKey" packages/cache/src/stable-hash.ts` = 1
- `grep -c "0x811c9dc5\|0x01000193" packages/cache/src/stable-hash.ts` = 2 (FNV-1a constants)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/cache test --run stable-hash 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `packages/cache/src/stable-hash.ts` ~60 LOC creato con 4 export pubblici
    - `packages/cache/src/stable-hash.test.ts` con 15+ test deterministici Tier-1 jsdom passing
    - 2 commit atomici TDD: RED test + GREEN feat
    - Coverage v8 sul file ≥90/80/90/90
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memory-cache-adapter.ts (RED→GREEN — LRU bounded D-158)</name>
  <files>packages/cache/src/memory-cache-adapter.ts, packages/cache/src/memory-cache-adapter.test.ts</files>
  <read_first>
    - packages/cache/src/types/cache-adapter.ts (creato in 06-01 — interface da implementare)
    - packages/cache/src/types/cache-entry.ts (creato in 06-01 — shape readonly)
    - packages/gateway/src/http/strategies/dedupe-strategy.ts (analog Map<key, ...> + cap pattern)
    - packages/gateway/src/http/strategies/backpressure-strategy.ts (analog cap + drop-oldest D-75 — pattern queue-bounded:138-156)
    - packages/worker/src/worker-registry.ts (analog Map registry + cap hard 8 D-128 + duplicate guard pattern)
    - .planning/phases/06-cache-tooling-avanzato/06-PATTERNS.md sezione "packages/cache/src/memory-cache-adapter.ts" (LOC 198-256) — pattern excerpt verbatim
    - .planning/phases/06-cache-tooling-avanzato/06-RESEARCH.md §2.2 LRU bounded implementazione + §15.7 LRU+TTL race
    - .planning/phases/06-cache-tooling-avanzato/06-CONTEXT.md D-158
  </read_first>
  <behavior>
    - Test 1: `createMemoryCacheAdapter()` ritorna CacheAdapter conforme interface (default maxEntries=1000)
    - Test 2: `createMemoryCacheAdapter({ maxEntries: 10 })` rispetta override
    - Test 3: `set(k, v)` poi `get(k)` ritorna `{ value, expiresAt: Infinity, setAt: <epoch ms> }` + stats.hits=1
    - Test 4: `get(missing)` ritorna `undefined` + stats.misses=1
    - Test 5: LRU eviction — cap=10, 11 set sequenziali (k0..k10) → primo k0 evicted (Map insertion order); stats.evictions=1
    - Test 6: LRU touch on get — cap=10, 10 set + `get(k0)` (k0 ri-ordinato in coda) + 1 set k10 → ora k1 evicted (NOT k0). stats.evictions=1
    - Test 7: TTL expiry — set(k, v, ttlMs=100) + advance fake time 200ms + `get(k)` ritorna undefined + stats.evictions=1 + stats.misses=1
    - Test 8: `delete(k)` esistente ritorna true + entry rimossa; `delete(missing)` ritorna false
    - Test 9: `invalidate('exact-key')` rimuove esattamente quella key + ritorna 1
    - Test 10: `invalidate(/^user-42::/)` con 5 entries match + 3 non-match → ritorna 5 + size scende di 5
    - Test 11: `invalidate({ prefix: 'weather.' })` startsWith + ritorna count
    - Test 12: `size()` ritorna numero entries correnti; `clear()` azzera + size=0
    - Test 13: `stats()` ritorna readonly { hits, misses, evictions, entries } cumulative
    - Test 14: `set` su key esistente NON triggera eviction (replace in-place) — cap=2, set(k1) set(k2) set(k1, newVal) → size=2
    - Test 15: TTL=undefined → `expiresAt: Number.POSITIVE_INFINITY` (mai scade)
  </behavior>
  <action>
**RED commit:**
```bash
cd "/Users/omarmarzio/programming/prova AI/SemBridge"
# Scrivi packages/cache/src/memory-cache-adapter.test.ts con 15 test descritti in <behavior>
# Usa fake timers vitest per Test 7 (TTL expiry deterministic)
git add packages/cache/src/memory-cache-adapter.test.ts
git commit -m "test(06-02): add RED test for MemoryCacheAdapter (D-158 LRU bounded + TTL + invalidate)

15 test deterministici Tier-1 jsdom: factory + override (2), get/set/stats baseline (2),
LRU eviction + LRU touch (2), TTL expiry fake timers (1), delete/clear (2), invalidate
3 dispatch (3), edge cases (3).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**GREEN commit:**

Crea `packages/cache/src/memory-cache-adapter.ts` (~120 LOC) replicando RESEARCH §2.2 + PATTERNS §198-256:

```typescript
/**
 * F6 MemoryCacheAdapter — LRU bounded cache adapter (D-158 default V1).
 *
 * **Pattern**: Map insertion order LRU (idiomatic JS, ECMAScript 2015 spec).
 * - Re-insert on get → LRU touch (re-orders entry to tail)
 * - Eviction → drop primo (oldest) via `cache.keys().next().value`
 * - TTL ortogonale a LRU (entry può essere evicted prima di scadenza TTL se cap)
 *
 * **Caratteristiche**:
 * - Zero dependency esterna
 * - Cap predictable (maxEntries=1000 default, NON bytes)
 * - Atomic single-thread JS event loop (no race condition F5 D-133 carryover)
 * - Lazy TTL expiration (no proactive sweeper — RESEARCH §15.7)
 *
 * **NON adottato `lru-cache@11.3.6`** (RESEARCH §2.3): zero-dep priority + budget
 * stretto.
 *
 * @example
 * const cache = createMemoryCacheAdapter({ maxEntries: 500 })
 * cache.set('key', { value: 42 }, 60_000)  // TTL 60s
 * const entry = cache.get('key')           // { value: { value: 42 }, expiresAt, setAt }
 * cache.stats()                             // { hits: 1, misses: 0, evictions: 0, entries: 1 }
 *
 * @see CacheAdapter interface in ./types/cache-adapter
 * @see RESEARCH §2 architettura cache + LRU implementation
 */

import type { CacheAdapter, CacheEntry, CacheStats } from './types'

const DEFAULT_MAX_ENTRIES = 1000 // D-158

export interface MemoryCacheAdapterOptions {
  readonly maxEntries?: number
}

export function createMemoryCacheAdapter(opts: MemoryCacheAdapterOptions = {}): CacheAdapter {
  const maxEntries = opts.maxEntries ?? DEFAULT_MAX_ENTRIES
  const cache = new Map<string, CacheEntry>()
  let hits = 0
  let misses = 0
  let evictions = 0

  return {
    get<T>(key: string): CacheEntry<T> | undefined {
      const entry = cache.get(key)
      if (!entry) {
        misses++
        return undefined
      }
      // TTL expiry check (atomic single-thread)
      if (entry.expiresAt < Date.now()) {
        cache.delete(key)
        evictions++
        misses++
        return undefined
      }
      // LRU touch: re-insert per portare in coda Map (insertion order = LRU order)
      cache.delete(key)
      cache.set(key, entry)
      hits++
      return entry as CacheEntry<T>
    },

    set<T>(key: string, value: T, ttlMs?: number): void {
      // Eviction LRU: se cap raggiunto e key NON esiste già → drop oldest
      if (cache.size >= maxEntries && !cache.has(key)) {
        const oldestKey = cache.keys().next().value
        if (oldestKey !== undefined) {
          cache.delete(oldestKey)
          evictions++
        }
      }
      const expiresAt = ttlMs !== undefined ? Date.now() + ttlMs : Number.POSITIVE_INFINITY
      cache.set(key, { value, expiresAt, setAt: Date.now() })
    },

    delete(key: string): boolean {
      return cache.delete(key)
    },

    invalidate(pattern: string | RegExp | { readonly prefix: string }): number {
      let count = 0
      // Exact string match
      if (typeof pattern === 'string') {
        if (cache.delete(pattern)) count++
        return count
      }
      // RegExp pattern
      if (pattern instanceof RegExp) {
        for (const k of Array.from(cache.keys())) {
          if (pattern.test(k)) {
            cache.delete(k)
            count++
          }
        }
        return count
      }
      // { prefix } object form
      if ('prefix' in pattern) {
        const prefix = pattern.prefix
        for (const k of Array.from(cache.keys())) {
          if (k.startsWith(prefix)) {
            cache.delete(k)
            count++
          }
        }
        return count
      }
      return count
    },

    size(): number {
      return cache.size
    },

    clear(): void {
      cache.clear()
    },

    stats(): CacheStats {
      return { hits, misses, evictions, entries: cache.size }
    },
  }
}
```

```bash
# vitest run --filter memory-cache-adapter → tutti GREEN
# Aggiorna packages/cache/src/index.ts:
#   export { createMemoryCacheAdapter, type MemoryCacheAdapterOptions } from './memory-cache-adapter'
#   export { stableStringify, fnv1a32, stableHash, cacheKey } from './stable-hash'

git add packages/cache/src/memory-cache-adapter.ts packages/cache/src/index.ts
git commit -m "feat(06-02): GREEN MemoryCacheAdapter LRU bounded + barrel exports (D-158)

Map insertion order LRU + TTL ortogonale + invalidate 3 dispatch (string/RegExp/{prefix})
+ stats cumulative. ~120 LOC zero deps esterne. Pattern Map ECMAScript 2015 spec
(idiomatic JS) — RESEARCH §2.2.

Barrel append: createMemoryCacheAdapter + stable-hash utility (cacheKey/stableHash/fnv1a32).

15/15 test passing. Coverage v8 ≥90/80/90/90.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**Acceptance grep:**
- `grep -c "function createMemoryCacheAdapter" packages/cache/src/memory-cache-adapter.ts` = 1
- `grep -c "DEFAULT_MAX_ENTRIES = 1000" packages/cache/src/memory-cache-adapter.ts` = 1 (D-158)
- `grep -c "Map insertion order\|LRU touch" packages/cache/src/memory-cache-adapter.ts` ≥1 (doc commento pattern)
- `grep -c "createMemoryCacheAdapter\|cacheKey\|stableHash" packages/cache/src/index.ts` ≥3 (barrel exports)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm -F @sembridge/cache test 2>&1 | tail -15 && pnpm -F @sembridge/cache build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `packages/cache/src/memory-cache-adapter.ts` ~120 LOC + `*.test.ts` 15+ test
    - `packages/cache/src/index.ts` aggiornato con runtime exports Wave 2 (createMemoryCacheAdapter, stable-hash utility)
    - 2 commit atomici TDD: RED test + GREEN feat (+ barrel append)
    - Coverage v8 sul file ≥90/80/90/90
    - Cross-package zero regression: full monorepo test passing
    - D-83 strict acceptance: `git diff main...HEAD packages/{core,mapper,routing,gateway,worker}/src/` exit 0 lines
  </done>
</task>

</tasks>

<verification>
- Tutti i 25+ test Tier-1 jsdom passing (15 stable-hash + 15 memory-cache-adapter)
- Coverage v8 sui 2 file ≥90/80/90/90 (target Phase D-92 carryover)
- Pattern carryover F3 D-74 esplicito nel doc commento di stable-hash.ts
- Pattern Map insertion order LRU esplicito nel doc commento di memory-cache-adapter.ts
- D-83 strict: zero modifiche runtime fuori `packages/cache/src/`
- File ownership disgiunta da 06-04 (parallel Wave 2): zero overlap su packages/{cache,devtools}/src/{multiplex-tap,tap-registry}.ts
</verification>

<success_criteria>
- [x] CacheAdapter interface (06-01) implementata da createMemoryCacheAdapter ✅
- [x] LRU eviction Map insertion order verified (Test 5) + LRU touch on get (Test 6) ✅
- [x] TTL expiry lazy verified (Test 7) ✅
- [x] invalidate 3 dispatch (string/RegExp/{prefix}) verified (Test 9-11) ✅
- [x] cacheKey({topic,payload[,scope]}) format verified D-155 + D-156 ✅
- [x] FNV-1a determinism + collision rate <0.1% verified ✅
- [x] CACHE-01 (in-memory + key configurabile per route/topic) — runtime done ✅
- [x] CACHE-02 (TTL + invalidate manuale/automatica) — runtime done ✅
- [x] D-83 strict carryover: zero diff packages/{core,mapper,routing,gateway,worker}/src/ ✅
- [x] Building blocks pronti per 06-03 (CacheHandler consume cacheKey + adapter) ✅
</success_criteria>

<output>
After completion, create `.planning/phases/06-cache-tooling-avanzato/06-02-SUMMARY.md` con:
- File creati (4 file: 2 source + 2 test) + LOC count
- Coverage v8 measured
- Test count breakdown (15+15)
- Pattern carryover documentation (F3 D-74 KeyBased + Map insertion order LRU)
- D-83 strict acceptance verified
- Threat coverage (T-06-02-01..06)
- Building blocks pronti per 06-03 (CacheHandler) + 06-08 (CacheBroker)
</output>