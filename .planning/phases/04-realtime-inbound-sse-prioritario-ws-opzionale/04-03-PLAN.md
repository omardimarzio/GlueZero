---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 03
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - packages/gateway/src/sse-ws/reconnect-strategy.ts
  - packages/gateway/src/sse-ws/reconnect-strategy.test.ts
autonomous: true
requirements:
  - RT-05
  - RT-07
must_haves:
  truths:
    - "createReconnectStrategy() ritorna un'interface ReconnectStrategy con nextDelayMs/recordFailure/recordSuccess/shouldFallback/fallback/getMode/isPermanentlyFailed/reset"
    - "nextDelayMs() applica formula full jitter: random(0, min(capMs, baseMs * 2^consecutiveFailures))"
    - "Dopo `fallbackThreshold` (default 3) fail consecutivi, shouldFallback() ritorna true"
    - "fallback() switcha mode SSE↔WS, resetta consecutiveFailures a 0, incrementa cycles"
    - "Dopo `globalCycleCap` (default 5) cycles raggiunti, isPermanentlyFailed() ritorna true e shouldFallback() ritorna false (no più fallback)"
    - "recordSuccess() resetta consecutiveFailures e cycles SOLO se trascorre `consolidationMs` dal precedente success (Q3 §6.2)"
    - "Math.random override-abile via options.random per test deterministici"
    - "Default initialMode: 'sse' (D-107 SSE-first)"
  artifacts:
    - path: "packages/gateway/src/sse-ws/reconnect-strategy.ts"
      provides: "createReconnectStrategy factory + ReconnectStrategy interface + ReconnectStrategyOptions"
      contains: "export function createReconnectStrategy"
      min_lines: 80
    - path: "packages/gateway/src/sse-ws/reconnect-strategy.test.ts"
      provides: "≥12 test deterministici (full jitter math, fallback threshold, cycle cap, consolidationMs guard)"
      contains: "describe('createReconnectStrategy"
  key_links:
    - from: "packages/gateway/src/sse-ws/reconnect-strategy.ts"
      to: "(consumer plan 04-05/04-06 adapter)"
      via: "named export createReconnectStrategy + ReconnectStrategy type"
      pattern: "export.*createReconnectStrategy"
    - from: "packages/gateway/src/sse-ws/reconnect-strategy.test.ts"
      to: "vi.useFakeTimers() per deterministic timing"
      via: "vi.advanceTimersByTime"
      pattern: "vi.useFakeTimers"
---

<objective>
Implementa la **state machine reconnect** per F4 con due responsabilità combinate:
1. **Full jitter backoff math** (D-109 / RT-05) — formula `delay = random(0, min(capMs, baseMs * 2^consecutiveFailures))` (RESEARCH §6.1, AWS Architecture Blog).
2. **Auto-fallback SSE↔WS state machine** (D-107) — dopo `fallbackThreshold` (3) fail consecutivi nel mode corrente switcha l'altro mode; dopo `globalCycleCap` (5) cicli totali pubblica fail permanente.
3. **Reset criteria con consolidationMs guard** (Q3 §6.2 default 5_000ms) — `recordSuccess()` resetta i counter SOLO se la connessione è rimasta up per ≥ `consolidationMs` (anti-flap detection).

Purpose: il `RealtimeChannel` (composito di SSE/WS adapter, manager) usa una istanza di `ReconnectStrategy` per-canale per decidere (a) quanto attendere prima del prossimo reconnect, (b) quando switchare mode in `'auto'`, (c) quando publish `system.realtime.failed` permanente.

Output:
- 1 nuovo file source (`reconnect-strategy.ts` — factory + interface + options)
- 1 nuovo file test (`reconnect-strategy.test.ts` — ≥12 test deterministici TDD RED→GREEN con `vi.useFakeTimers()` + `random` DI)

Pattern TDD obbligatorio (D-117): RED commit → GREEN commit. Co-located test sibling. Pattern factory + state machine identico a `circuit-breaker.ts` di F3 (`packages/gateway/src/http/strategies/circuit-breaker.ts`).

Q3 (RESEARCH §13.1) confermata: opzione B (`consolidationMs: 5_000` default) over-ridable via `options.consolidationMs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/src/http/strategies/circuit-breaker.ts
@packages/gateway/src/http/strategies/circuit-breaker.test.ts
@packages/gateway/src/http/strategies/retry-strategy.ts

<interfaces>
<!-- Tipi che 04-03 crea -->

From PATTERNS.md §2.6 (verbatim): l'interface ReconnectStrategy che 04-03 deve creare:
```typescript
export interface ReconnectStrategy {
  nextDelayMs(): number
  recordFailure(): void
  recordSuccess(): void
  shouldFallback(): boolean
  fallback(): 'sse' | 'websocket'
  getMode(): 'sse' | 'websocket'
  isPermanentlyFailed(): boolean
  reset(): void
}

export interface ReconnectStrategyOptions {
  readonly baseMs?: number          // default 1000 (D-109)
  readonly capMs?: number           // default 30_000 (D-109)
  readonly consolidationMs?: number // default 5_000 (Q3 §6.2)
  readonly maxAttempts?: number     // default Infinity (RT-05)
  readonly fallbackThreshold?: number // default 3 (D-107)
  readonly globalCycleCap?: number    // default 5 (D-107)
  readonly initialMode?: 'sse' | 'websocket' // default 'sse' (D-107)
  readonly random?: () => number    // default Math.random — DI per test
  readonly now?: () => number        // default Date.now — DI per test (NEW vs PATTERNS, per consolidationMs deterministic)
}
```

From packages/gateway/src/http/strategies/circuit-breaker.ts (analog state machine factory):
- Factory function pattern con `state: { ... }` closure
- Test pattern con `vi.useFakeTimers()` + `vi.advanceTimersByTime` deterministico

From packages/gateway/src/http/strategies/retry-strategy.ts (lines 144-156 — full jitter formula):
- `Math.floor(Math.random() * Math.min(maxDelay, base * 2 ** attempt))`
- Override `random` via options for test determinism
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crea reconnect-strategy.ts + .test.ts con TDD RED→GREEN</name>
  <files>
    packages/gateway/src/sse-ws/reconnect-strategy.ts
    packages/gateway/src/sse-ws/reconnect-strategy.test.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.6 reconnect-strategy INTERO + test pattern, §3.6 TDD)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§6.1 full jitter formula AWS, §6.2 reset criteria Q3, §6.5 cycle cap state machine)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-107 fallback, D-109 reconnect)
    - packages/gateway/src/http/strategies/circuit-breaker.ts (analog state machine + factory pattern)
    - packages/gateway/src/http/strategies/circuit-breaker.test.ts (analog test struct: describe + beforeEach vi.useFakeTimers)
    - packages/gateway/src/http/strategies/retry-strategy.ts (analog full jitter math line 144-156)
  </read_first>
  <behavior>
    - Test 1: nextDelayMs() con random=0.5, baseMs=1000, capMs=30000, consecutiveFailures=0 → 500 (0.5 * 1000)
    - Test 2: nextDelayMs() con random=0.5 dopo recordFailure() → 1000 (0.5 * 2000)
    - Test 3: nextDelayMs() con random=1.0 → cap a capMs - 1 (Math.floor(1 * 30000) = 29999 o 30000 a seconda exclusive)
    - Test 4: 3 fail consecutivi → shouldFallback() === true
    - Test 5: shouldFallback() false con < 3 fail consecutivi
    - Test 6: fallback() switcha 'sse' → 'websocket' e ritorna 'websocket'
    - Test 7: fallback() switcha 'websocket' → 'sse' (round-trip)
    - Test 8: fallback() resetta consecutiveFailures a 0 (counter reset al cambio mode)
    - Test 9: 5 cycle SSE↔WS senza success → isPermanentlyFailed() === true e shouldFallback() === false (cap reached)
    - Test 10: recordSuccess() entro consolidationMs (default 5000) NON resetta cycles (anti-flap)
    - Test 11: recordSuccess() dopo > consolidationMs resetta consecutiveFailures e cycles a 0
    - Test 12: maxAttempts cap raggiunto → isPermanentlyFailed() === true (totalAttempts ≥ maxAttempts)
    - Test 13: reset() riporta state alla configurazione iniziale (mode === initialMode, counters=0)
    - Test 14: getMode() ritorna initialMode di default ('sse')
    - Test 15: getMode() ritorna 'websocket' se initialMode override
  </behavior>
  <action>
**Step 1 (RED) — Crea `packages/gateway/src/sse-ws/reconnect-strategy.test.ts` PRIMA dell'implementazione**, replicando struttura `circuit-breaker.test.ts` con `vi.useFakeTimers()` + `random` DI:

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { createReconnectStrategy } from './reconnect-strategy'

describe('createReconnectStrategy (D-107 fallback + D-109 backoff + Q3 reset)', () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })
  afterEach(() => {
    vi.useRealTimers()
  })

  describe('full jitter math (D-109 + RT-05 + RESEARCH §6.1)', () => {
    it('Test 1: nextDelayMs base case — random=0.5, baseMs=1000, capMs=30000 → 500', () => {
      const r = createReconnectStrategy({ baseMs: 1000, capMs: 30_000, random: () => 0.5 })
      expect(r.nextDelayMs()).toBe(500) // floor(0.5 * (1000 * 2^0)) = 500
    })

    it('Test 2: nextDelayMs dopo 1 fail — random=0.5 → 1000', () => {
      const r = createReconnectStrategy({ baseMs: 1000, capMs: 30_000, random: () => 0.5 })
      r.recordFailure()
      expect(r.nextDelayMs()).toBe(1000) // floor(0.5 * (1000 * 2^1)) = 1000
    })

    it('Test 3: nextDelayMs cap a capMs — random=0.999, exp grande → capMs - small', () => {
      const r = createReconnectStrategy({ baseMs: 1000, capMs: 30_000, random: () => 0.999 })
      // dopo molti fail: 1000 * 2^20 = enorme, capped a 30000
      for (let i = 0; i < 20; i++) r.recordFailure()
      const delay = r.nextDelayMs()
      expect(delay).toBeLessThanOrEqual(30_000)
      expect(delay).toBeGreaterThanOrEqual(29_000) // floor(0.999 * 30000) ≈ 29970
    })
  })

  describe('auto-fallback state machine (D-107)', () => {
    it('Test 4: 3 fail consecutivi → shouldFallback() true', () => {
      const r = createReconnectStrategy({ fallbackThreshold: 3 })
      r.recordFailure()
      r.recordFailure()
      r.recordFailure()
      expect(r.shouldFallback()).toBe(true)
    })

    it('Test 5: <3 fail consecutivi → shouldFallback() false', () => {
      const r = createReconnectStrategy({ fallbackThreshold: 3 })
      r.recordFailure()
      r.recordFailure()
      expect(r.shouldFallback()).toBe(false)
    })

    it('Test 6: fallback() sse → websocket', () => {
      const r = createReconnectStrategy({ initialMode: 'sse' })
      expect(r.fallback()).toBe('websocket')
      expect(r.getMode()).toBe('websocket')
    })

    it('Test 7: fallback() websocket → sse (round-trip)', () => {
      const r = createReconnectStrategy({ initialMode: 'websocket' })
      r.fallback() // → sse
      r.fallback() // → websocket
      expect(r.getMode()).toBe('websocket')
    })

    it('Test 8: fallback() resetta consecutiveFailures (counter reset al cambio mode)', () => {
      const r = createReconnectStrategy({ fallbackThreshold: 3 })
      r.recordFailure()
      r.recordFailure()
      r.recordFailure()
      r.fallback()
      expect(r.shouldFallback()).toBe(false) // counter back to 0
    })

    it('Test 9: globalCycleCap=5 raggiunto → isPermanentlyFailed() true E shouldFallback() false', () => {
      const r = createReconnectStrategy({ fallbackThreshold: 1, globalCycleCap: 5 })
      for (let i = 0; i < 5; i++) {
        r.recordFailure()
        r.fallback()
      }
      expect(r.isPermanentlyFailed()).toBe(true)
      expect(r.shouldFallback()).toBe(false)
    })
  })

  describe('reset criteria con consolidationMs guard (Q3 §6.2)', () => {
    it('Test 10: recordSuccess() entro consolidationMs NON resetta cycles', () => {
      const fakeNow = vi.fn(() => 1_000_000)
      const r = createReconnectStrategy({ consolidationMs: 5_000, fallbackThreshold: 1, now: fakeNow })
      r.recordFailure()
      r.fallback() // cycles=1
      r.recordSuccess() // first success at t=1_000_000
      // Second success at t=1_001_000 (1s later — under consolidationMs)
      fakeNow.mockReturnValue(1_001_000)
      r.recordSuccess()
      // cycles NOT reset (anti-flap) — verifica indiretta: ulteriore failure-fallback fa raggiungere il cap
      // (V1: assert via getMode + counters esposti? PATTERNS lo definisce idiomatic, qui assert su isPermanentlyFailed sequence)
      // Concreto: ricreiamo un nuovo r ed enumeriamo cicli per verifica.
      // Più semplice: testiamo che dopo consolidation breve e poi nuovo failure, ancora 1 fail soltanto serve a fallback.
      r.recordFailure() // shouldFallback should be true (1 fail con threshold 1)
      expect(r.shouldFallback()).toBe(true)
    })

    it('Test 11: recordSuccess() dopo consolidationMs resetta consecutiveFailures e cycles', () => {
      const fakeNow = vi.fn(() => 1_000_000)
      const r = createReconnectStrategy({ consolidationMs: 5_000, fallbackThreshold: 3, globalCycleCap: 5, now: fakeNow })
      r.recordSuccess() // first success — establishes lastSuccessAt baseline
      // Accumulate 2 fail
      r.recordFailure()
      r.recordFailure()
      // Advance now beyond consolidationMs
      fakeNow.mockReturnValue(1_006_000) // 6s later
      r.recordSuccess() // SHOULD reset counters
      // Now nextDelayMs should be base case (random*1000)
      expect(r.shouldFallback()).toBe(false) // counter reset
      // Aggiungi 3 fail per verifica reset cycles
      r.recordFailure()
      r.recordFailure()
      r.recordFailure()
      expect(r.shouldFallback()).toBe(true)
    })

    it('Test 12: maxAttempts cap → isPermanentlyFailed() true', () => {
      const r = createReconnectStrategy({ maxAttempts: 3 })
      r.recordFailure()
      r.recordFailure()
      r.recordFailure()
      expect(r.isPermanentlyFailed()).toBe(true)
    })
  })

  describe('reset() + initialMode', () => {
    it('Test 13: reset() riporta state allo stato iniziale', () => {
      const r = createReconnectStrategy({ initialMode: 'sse', fallbackThreshold: 1 })
      r.recordFailure()
      r.fallback() // mode → ws, cycles=1
      r.reset()
      expect(r.getMode()).toBe('sse')
      expect(r.isPermanentlyFailed()).toBe(false)
      expect(r.shouldFallback()).toBe(false)
    })

    it('Test 14: getMode default initialMode "sse" (D-107 SSE-first)', () => {
      const r = createReconnectStrategy()
      expect(r.getMode()).toBe('sse')
    })

    it('Test 15: getMode override initialMode "websocket"', () => {
      const r = createReconnectStrategy({ initialMode: 'websocket' })
      expect(r.getMode()).toBe('websocket')
    })
  })
})
```

**Step 2 (GREEN) — Crea `packages/gateway/src/sse-ws/reconnect-strategy.ts`** copiando struttura da PATTERNS.md §2.6 con NOW DI aggiunto per testabilità:

```typescript
/**
 * Stato interno per-channel della reconnect state machine (D-107 + D-109).
 *
 * Encapsulated nel closure di `createReconnectStrategy` (factory pattern identico
 * a `createCircuitBreaker` di gateway/http/strategies/circuit-breaker.ts).
 */
interface ReconnectState {
  /** Modalità correntemente attiva (D-107 — SSE-first default). */
  mode: 'sse' | 'websocket'
  /** Counter fail consecutivi nel mode corrente — reset a success o a fallback(). */
  consecutiveFailures: number
  /** Counter cycle SSE↔WS effettuati — cap globale D-107 = 5. */
  cycles: number
  /** Timestamp ultima connessione successful (per consolidationMs reset — Q3 §6.2). */
  lastSuccessAt: number
  /** Counter total reconnect attempts (per maxAttempts cap RT-05). */
  totalAttempts: number
}

/** Opzioni override-abili per `createReconnectStrategy` (RT-05 + D-107 + D-109). */
export interface ReconnectStrategyOptions {
  /** Base delay per full jitter (D-109). Default 1_000ms. */
  readonly baseMs?: number
  /** Cap delay per full jitter (D-109). Default 30_000ms. */
  readonly capMs?: number
  /** Time minimo OPEN per resettare counter al recordSuccess (Q3 §6.2). Default 5_000ms. */
  readonly consolidationMs?: number
  /** Cap totale reconnect attempts (RT-05). Default Infinity. */
  readonly maxAttempts?: number
  /** Fail consecutivi nel mode corrente prima di fallback (D-107). Default 3. */
  readonly fallbackThreshold?: number
  /** Cap totale cicli SSE↔WS (D-107). Default 5. */
  readonly globalCycleCap?: number
  /** Mode iniziale (D-107 SSE-first). Default 'sse'. */
  readonly initialMode?: 'sse' | 'websocket'
  /** DI Math.random override per test deterministici (pattern retry-strategy.ts). */
  readonly random?: () => number
  /** DI Date.now override per test deterministici (consolidationMs guard). */
  readonly now?: () => number
}

/**
 * Public interface di `ReconnectStrategy` ritornata dal factory.
 *
 * Tutti i metodi sono sync e fanno mutate dello state interno chiuso (closure).
 * NON è thread-safe — pensato per single-channel single-thread browser context.
 */
export interface ReconnectStrategy {
  /**
   * Calcola il prossimo backoff delay (full jitter, D-109 / RESEARCH §6.1):
   *   `delay = floor(random() * min(capMs, baseMs * 2^consecutiveFailures))`
   */
  nextDelayMs(): number
  /** Registra fail dell'ultimo connect — incrementa counter + check fallback eligibility. */
  recordFailure(): void
  /** Registra success — reset counter (con consolidationMs guard Q3 §6.2). */
  recordSuccess(): void
  /** True se threshold fallback raggiunto E cycle cap NON exceeded (D-107). */
  shouldFallback(): boolean
  /** Switcha SSE↔WS mode + reset consecutiveFailures + incrementa cycles. Ritorna nuovo mode. */
  fallback(): 'sse' | 'websocket'
  /** Mode corrente. */
  getMode(): 'sse' | 'websocket'
  /** True se cap globale raggiunto (D-107) o maxAttempts (RT-05) — permanent failure. */
  isPermanentlyFailed(): boolean
  /** Reset completo a configurazione iniziale (per disconnect manuale + reconnect). */
  reset(): void
}

/**
 * Crea una `ReconnectStrategy` con full jitter + auto-fallback (D-107, D-109).
 *
 * Formula full jitter (RESEARCH §6.1, AWS Architecture Blog Marc Brooker 2015):
 *   `delay = random(0, min(capMs, baseMs * 2^attempt))`
 *
 * State machine: `sse[0..N fail] → ws[0..N fail] → sse[0..N fail] → ... → permanent`.
 *
 * Reset criteria Q3 §6.2 (RESEARCH "opzione B"): `recordSuccess()` resetta counter
 * SOLO se trascorre `consolidationMs` dal precedente success — anti-flap detection.
 *
 * @example
 * ```ts
 * const r = createReconnectStrategy({ initialMode: 'sse' })
 * while (!connected && !r.isPermanentlyFailed()) {
 *   await sleep(r.nextDelayMs())
 *   try { await connect(r.getMode()); r.recordSuccess(); connected = true }
 *   catch { r.recordFailure(); if (r.shouldFallback()) r.fallback() }
 * }
 * ```
 */
export function createReconnectStrategy(options: ReconnectStrategyOptions = {}): ReconnectStrategy {
  const baseMs = options.baseMs ?? 1_000
  const capMs = options.capMs ?? 30_000
  const consolidationMs = options.consolidationMs ?? 5_000
  const maxAttempts = options.maxAttempts ?? Number.POSITIVE_INFINITY
  const fallbackThreshold = options.fallbackThreshold ?? 3
  const globalCycleCap = options.globalCycleCap ?? 5
  const initialMode: 'sse' | 'websocket' =
    options.initialMode === 'websocket' ? 'websocket' : 'sse'
  const random = options.random ?? Math.random
  const now = options.now ?? Date.now

  const state: ReconnectState = {
    mode: initialMode,
    consecutiveFailures: 0,
    cycles: 0,
    lastSuccessAt: 0,
    totalAttempts: 0,
  }

  return {
    nextDelayMs(): number {
      const exponential = Math.min(capMs, baseMs * 2 ** state.consecutiveFailures)
      return Math.floor(random() * exponential)
    },
    recordFailure(): void {
      state.consecutiveFailures += 1
      state.totalAttempts += 1
    },
    recordSuccess(): void {
      const t = now()
      // Q3 §6.2 consolidationMs guard: reset counter SOLO se la connessione è rimasta
      // up per almeno consolidationMs (evita reset prematuro su connect-then-instant-fail).
      if (state.lastSuccessAt === 0 || t - state.lastSuccessAt >= consolidationMs) {
        state.consecutiveFailures = 0
        state.cycles = 0
      }
      state.lastSuccessAt = t
    },
    shouldFallback(): boolean {
      // Cap globale raggiunto → no più fallback (publish system.realtime.failed permanente).
      if (state.cycles >= globalCycleCap) return false
      return state.consecutiveFailures >= fallbackThreshold
    },
    fallback(): 'sse' | 'websocket' {
      state.mode = state.mode === 'sse' ? 'websocket' : 'sse'
      state.consecutiveFailures = 0 // counter reset al cambio mode (D-107)
      state.cycles += 1
      return state.mode
    },
    getMode(): 'sse' | 'websocket' {
      return state.mode
    },
    isPermanentlyFailed(): boolean {
      return state.cycles >= globalCycleCap || state.totalAttempts >= maxAttempts
    },
    reset(): void {
      state.mode = initialMode
      state.consecutiveFailures = 0
      state.cycles = 0
      state.lastSuccessAt = 0
      state.totalAttempts = 0
    },
  }
}
```

**Step 3 — Esegui RED→GREEN:**

1. Commit RED: `git add packages/gateway/src/sse-ws/reconnect-strategy.test.ts && git commit -m "test(04-03): add failing tests for createReconnectStrategy (D-107, D-109, Q3)"`
2. Verifica RED: test fallisce (file reconnect-strategy.ts non esiste)
3. Commit GREEN: `git add packages/gateway/src/sse-ws/reconnect-strategy.ts && git commit -m "feat(04-03): implement createReconnectStrategy state machine (D-107 fallback + D-109 full jitter + Q3 consolidationMs)"`
4. Verifica GREEN: tutti i test passing

**Anti-pattern AP-3 da evitare:** NON usare `reconnecting-websocket` library (vincolo PRD §31.3 + STACK.md). Tutto state-machine custom in questo file.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/reconnect-strategy.test.ts && pnpm --filter @sembridge/gateway exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` esiste e contiene `export function createReconnectStrategy`
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` esiste e contiene `export interface ReconnectStrategy`
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` esiste e contiene `export interface ReconnectStrategyOptions`
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` contiene formula full jitter `Math.floor(random() * Math.min(capMs, baseMs * 2 ** state.consecutiveFailures))`
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` contiene `globalCycleCap` (D-107 cap)
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` contiene `consolidationMs` (Q3)
    - File `packages/gateway/src/sse-ws/reconnect-strategy.ts` NON contiene import di `reconnecting-websocket` (anti-AP-3)
    - File `packages/gateway/src/sse-ws/reconnect-strategy.test.ts` contiene almeno 15 test (`grep -v '^#' packages/gateway/src/sse-ws/reconnect-strategy.test.ts | grep -c "it(" >= 15`)
    - File `packages/gateway/src/sse-ws/reconnect-strategy.test.ts` usa `vi.useFakeTimers()` per deterministic timing
    - `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/reconnect-strategy.test.ts` exit 0 con tutti i test passing
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
    - Cronologia git mostra 2 commit separati 04-03: RED + GREEN
  </acceptance_criteria>
  <done>State machine reconnect è implementata con full jitter (D-109), fallback SSE↔WS (D-107) e consolidationMs guard (Q3). 15 test deterministici passing con `random` + `now` DI. Plan 04-05 (sse-adapter.ts) e 04-06 (websocket-adapter.ts) possono importare `createReconnectStrategy` e `ReconnectStrategy` interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state machine internal | nessun untrusted input — solo invocazioni dal manager |
| timer environment | `Math.random` e `Date.now` injectabili via DI per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | thundering herd su server reboot — N client tutti reconnect simultanei senza jitter | mitigate | Full jitter `random(0, exp)` (RESEARCH §6.1 AWS pattern) garantisce distribuzione uniforme degli attempt nel tempo. Test 1-3 verificano formula. |
| T-04-03-02 | Denial of Service | reconnect storm permanente — client esegue reconnect infiniti consumando CPU/network | mitigate | `globalCycleCap: 5` (D-107) + `maxAttempts: ?` (RT-05) + `isPermanentlyFailed()` true → caller publish `system.realtime.failed` + stop. Test 9, 12 verificano. |
| T-04-03-03 | Tampering | client computa delay non-randomico (Math.random predicibile per attacker network observation) | accept | Math.random in browser è non-cryptographically-secure ma sufficiente per jitter spread. Attacker non guadagna nulla predicendo i delay. Documentato in DOC-04. |
| T-04-03-04 | Repudiation | timing dei reset counter ambiguo — flap detection può "perdere" eventi se consolidationMs troppo basso | mitigate | Q3 default 5_000ms scelto come bilanciamento (RESEARCH §6.2 — inspired da socket.io-client). Override-abile via `RealtimeChannelDef.reconnect.consolidationMs`. |
</threat_model>

<verification>
- File `reconnect-strategy.ts` con factory pure + 8 metodi nell'interface
- Formula full jitter implementata per `random(0, min(cap, base * 2^attempt))`
- Auto-fallback SSE↔WS con cap 5 (D-107) + reset counter al cambio mode
- ConsolidationMs guard (Q3 default 5000ms) anti-flap
- DI `random` + `now` per test determinism
- ≥15 test passing in jsdom tier-1
- Pattern TDD RED→GREEN visibile in git log (2 commit separati)
- Zero regressioni su core/mapper/routing/gateway-http
</verification>

<success_criteria>
1. `createReconnectStrategy({ random: () => 0.5 }).nextDelayMs() === 500` (deterministic full jitter)
2. Dopo 3 fail consecutivi `shouldFallback()` ritorna true; dopo `fallback()` mode switcha
3. Dopo 5 cicli SSE↔WS senza success `isPermanentlyFailed()` true e `shouldFallback()` false
4. `recordSuccess()` entro 5s da precedente success NON resetta cycles (anti-flap Q3)
5. ≥15 test deterministici passing con `vi.useFakeTimers()` + DI
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-03-SUMMARY.md` con:
- LOC count file source + test
- Output `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/reconnect-strategy.test.ts`
- Conferma TDD RED→GREEN: commit hash RED + commit hash GREEN
- Conferma anti-AP-3: `grep -v '^#' packages/gateway/src/sse-ws/reconnect-strategy.ts | grep -c "reconnecting-websocket"` === 0
- Hand-off note per 04-05/04-06 (adapter possono importare `createReconnectStrategy`)
</output>
