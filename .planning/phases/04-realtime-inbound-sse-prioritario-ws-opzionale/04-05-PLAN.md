---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 05
type: tdd
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - packages/gateway/src/sse-ws/sse-adapter.ts
  - packages/gateway/src/sse-ws/sse-adapter.test.ts
  - packages/gateway/src/sse-ws/test-utils/mock-event-source.ts
autonomous: true
requirements:
  - RT-01
  - RT-04
  - RT-05
  - RT-06
  - RT-07
  - ERR-02
must_haves:
  truths:
    - "SseAdapter.connect() chiama buildUrl() async (D-104) o usa url statico, e crea new EventSource(url, { withCredentials: true })"
    - "Su EventSource 'open' event publish system.realtime.connected con source.name='sse'"
    - "Su EventSource 'message' event applica backpressure (se configurato D-115) PRIMA di publishFn(BrokerEvent con source.type='server', source.name='sse')"
    - "Su EventSource 'error' event publish system.realtime.disconnected, chiude EventSource (RESEARCH §3.3 — disable native reconnect), record failure su ReconnectStrategy"
    - "Last-Event-ID gestito manualmente: l'adapter memorizza event.lastEventId su ogni message + lo inietta come query string ?lastEventId= via buildUrl in reconnect (RESEARCH §3.2 — chiusura RT-07)"
    - "Topic invalid (regex F1 fail) → publish network.error con category='protocol', NON crash (RESEARCH §3.7)"
    - "disconnect() chiama es.close() + abort controller + publish system.realtime.disconnected"
    - "checkFreshness(staleTimeoutMs) confronta Date.now() - lastEventReceivedAt < staleTimeoutMs"
    - "EventSourceCtor DI permette MockEventSource per test jsdom (RESEARCH §9.1)"
    - "SSE adapter supporta custom event types via def.eventTypes (W-4 SC-1 closure): se popolato, l'adapter registra addEventListener(eventType, ...) per ogni elemento e topic deriva da event field (NON da channel name); se omesso, fallback a 'message' con topic = def.name"
    - "Evento SSE custom server-side dichiarato in def.sseHeartbeatEventTypes (default ['heartbeat']) aggiorna lastEventReceivedAt SENZA pubblicare BrokerEvent (B-5 Q5 closure — freshness check con server heartbeat sintattici uniforme staleTimeoutMs WS=60s)"
    - "MockEventSource espone `static byChannelName: Map<string, MockEventSource>` populated nel constructor parsing query param `_channel` (B-NEW-2 fix iter 2 — abilita routing strict harness 04-08; owned da 04-05)"
  artifacts:
    - path: "packages/gateway/src/sse-ws/sse-adapter.ts"
      provides: "SseAdapter class + SseAdapterDeps interface"
      contains: "export class SseAdapter"
      min_lines: 150
    - path: "packages/gateway/src/sse-ws/sse-adapter.test.ts"
      provides: "≥10 test deterministici (connect/disconnect, message dispatch, error → reconnect record, freshness check, lastEventId tracking, topic validation)"
      contains: "describe('SseAdapter"
    - path: "packages/gateway/src/sse-ws/test-utils/mock-event-source.ts"
      provides: "MockEventSource class compatibile EventSource interface (jsdom non ha EventSource nativo) + static byChannelName Map per routing test-only (B-NEW-2)"
      contains: "export class MockEventSource"
  key_links:
    - from: "packages/gateway/src/sse-ws/sse-adapter.ts"
      to: "packages/gateway/src/sse-ws/reconnect-strategy.ts"
      via: "createReconnectStrategy({ initialMode: 'sse' })"
      pattern: "createReconnectStrategy"
    - from: "packages/gateway/src/sse-ws/sse-adapter.ts"
      to: "@gluezero/core BrokerError + nanoid"
      via: "import { createBrokerError } from '@gluezero/core'; import { nanoid } from 'nanoid'"
      pattern: "createBrokerError|nanoid"
    - from: "packages/gateway/src/sse-ws/sse-adapter.test.ts"
      to: "packages/gateway/src/sse-ws/test-utils/mock-event-source.ts"
      via: "EventSourceCtor DI"
      pattern: "MockEventSource"
---

<objective>
Implementa il **SSE adapter** (D-101 / D-104 / D-109 / D-113 — RT-01, RT-04, RT-06, RT-07) — wrapper di `EventSource` nativo con:
- Lifecycle (connect/disconnect/reconnect) governato da `ReconnectStrategy` di plan 04-03
- Last-Event-ID management manuale (RESEARCH §3.2 — il browser NON inietta header automaticamente quando custom reconnect; iniettiamo come query string `?lastEventId=` via `buildUrl()`)
- Auth-agnostic via `buildUrl()` async hook (D-104) con fallback a `url` statico
- Backpressure adapter-level (D-115) PRIMA di `publishFn` — riuso 1:1 di `BackpressureStrategy` di F3
- Eventi standard `system.realtime.connected/disconnected` (ERR-02 ext F4)
- Topic validation (regex F1) — frame con topic invalido → `network.error` con `category: 'protocol'`, NON crash
- AbortController cascade (D-112) — `externalSignal?.addEventListener('abort', () => disconnect(), { once: true })`
- Freshness check (D-110) — `lastEventReceivedAt` tracking + `checkFreshness(staleTimeoutMs)` API per visibility orchestration
- DI `EventSourceCtor` per testabilità (jsdom non supporta `EventSource` nativo — RESEARCH §9.1)

Output:
- 1 nuovo file source (`sse-adapter.ts` — class SseAdapter + SseAdapterDeps interface)
- 1 nuovo file test (`sse-adapter.test.ts` — ≥10 test deterministici TDD RED→GREEN)
- 1 nuovo file test util (`test-utils/mock-event-source.ts` — MockEventSource class per DI)

**Anti-pattern AP-2 da evitare** (PATTERNS.md §5): NON tentare di iniettare custom headers in EventSource (vincolo PRD §31.3 + D-105). Auth via `buildUrl()` query string only.

**Anti-pattern AP-4 implicito:** NON usare il native reconnect di EventSource — su `error` chiamare `es.close()` per forzare CLOSED state e applicare la nostra reconnect policy custom (RESEARCH §3.3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/src/http/http-gateway.ts
@packages/gateway/src/sse-ws/reconnect-strategy.ts
@packages/gateway/src/sse-ws/types/realtime-channel-def.ts
@packages/core/src/types/broker-error.ts
@packages/core/src/types/broker-event.ts

<interfaces>
<!-- Tipi che 04-05 importerà -->

From packages/core/src/types/broker-event.ts (BrokerEvent.source):
```typescript
export interface EventSourceDescriptor {
  readonly type: 'plugin' | 'broker' | 'server' | 'system'  // 'server' già supportato F1
  readonly id: string
  readonly name?: string
}
```

From packages/core/src/types/broker-error.ts (createBrokerError):
```typescript
export function createBrokerError(params: {
  code: string
  message: string
  category: 'config' | 'validation' | 'mapping' | 'network' | 'workflow' | 'auth' | 'protocol'
  details?: unknown
  // ...
}): BrokerError
```
NB: `'protocol'` è una category VALID di BrokerError (verificare con grep nel file). Se non c'è, usare `category: 'network'` con `code: 'realtime.frame.protocol-error'`.

From packages/gateway/src/sse-ws/reconnect-strategy.ts (Plan 04-03):
```typescript
export function createReconnectStrategy(opts?): ReconnectStrategy
```

From packages/gateway/src/sse-ws/types/realtime-channel-def.ts (Plan 04-01):
```typescript
export interface RealtimeChannelDef {
  readonly name: string
  readonly mode?: 'sse' | 'websocket' | 'auto'
  readonly buildUrl?: () => Promise<string>
  readonly url?: string
  readonly reconnect?: RealtimeReconnectConfig
  readonly backpressure?: BackpressurePolicyConfig
  // ...
}
```

From packages/gateway/src/http/strategies/backpressure-strategy.ts (riuso F3 D-115):
```typescript
export interface BackpressureStrategy {
  schedule(key: string, priority: 'critical' | 'high' | 'normal' | 'low', task: () => Promise<void>): Promise<void>
}
```

From PATTERNS.md §2.8 (sse-adapter excerpt completo da 700-789).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crea test-utils/mock-event-source.ts (helper riutilizzabile)</name>
  <files>
    packages/gateway/src/sse-ws/test-utils/mock-event-source.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.15 + §2.18 mock pattern, RESEARCH §9.1 mocking strategy)
  </read_first>
  <action>
Crea `packages/gateway/src/sse-ws/test-utils/mock-event-source.ts` come implementazione minimale compatibile con l'interfaccia `EventSource` necessaria al `SseAdapter` (RESEARCH §9.1):

```typescript
/**
 * MockEventSource — implementazione minimale compatibile `EventSource` per test jsdom.
 *
 * jsdom (vitest tier-1) NON ha `EventSource` nativo (RESEARCH §9.1). L'adapter accetta
 * `EventSourceCtor?` come DI per iniettare questa mock invece del nativo browser.
 *
 * Test helpers (`__open`, `__message`, `__error`) permettono di simulare il lifecycle
 * server-side senza un vero stream HTTP.
 */
export class MockEventSource implements Pick<EventSource,
  'addEventListener' | 'removeEventListener' | 'close' | 'readyState' | 'url' | 'withCredentials' | 'CONNECTING' | 'OPEN' | 'CLOSED'
> {
  static readonly CONNECTING = 0 as const
  static readonly OPEN = 1 as const
  static readonly CLOSED = 2 as const

  readonly url: string
  readonly withCredentials: boolean
  readyState: number = MockEventSource.CONNECTING
  readonly CONNECTING = 0
  readonly OPEN = 1
  readonly CLOSED = 2

  /** Per assertion test: ultimo costruttore invocato (instances tracking). */
  static lastInstance: MockEventSource | null = null
  static instances: MockEventSource[] = []

  /**
   * **B-NEW-2 fix (iter 2)** — Map test-only per routing strict per-canale dell'harness
   * (plan 04-08). Ogni canale di test che vuole essere indicizzato deve avere `?_channel=<name>`
   * nella URL: il constructor parsa il query param e si registra qui. Permette
   * `MockEventSource.byChannelName.get(name)` lookup deterministico senza fallback "ultimo
   * istanza" (causa di cross-canale pollution pre-fix B-2).
   */
  static byChannelName: Map<string, MockEventSource> = new Map()

  private listeners = new Map<string, Set<EventListener>>()

  constructor(url: string | URL, init?: EventSourceInit) {
    this.url = url.toString()
    this.withCredentials = init?.withCredentials ?? false
    MockEventSource.lastInstance = this
    MockEventSource.instances.push(this)

    // B-NEW-2 — parse `_channel=<name>` query param per indexing test-only.
    // Se l'URL è relativo o malformato, skip silenziosamente (non indicizzabile).
    try {
      const parsed = new URL(this.url, 'http://localhost')
      const channelName = parsed.searchParams.get('_channel')
      if (channelName) {
        MockEventSource.byChannelName.set(channelName, this)
      }
    } catch {
      // URL non parsabile: skip (no-op).
    }
  }

  addEventListener(type: string, fn: EventListener): void {
    if (!this.listeners.has(type)) this.listeners.set(type, new Set())
    this.listeners.get(type)!.add(fn)
  }

  removeEventListener(type: string, fn: EventListener): void {
    this.listeners.get(type)?.delete(fn)
  }

  close(): void {
    this.readyState = MockEventSource.CLOSED
  }

  // ---------- Test helpers (NOT part of EventSource spec) ----------

  /** Simula 'open' event server-side. */
  __open(): void {
    this.readyState = MockEventSource.OPEN
    this.dispatch('open', new Event('open'))
  }

  /**
   * Simula 'message' event server-side.
   * @param data - JSON string del payload
   * @param id - opzionale Last-Event-ID
   * @param eventType - default 'message', custom event type per `event:` field
   */
  __message(data: string, id?: string, eventType: string = 'message'): void {
    const ev = new MessageEvent(eventType, { data, lastEventId: id ?? '' })
    this.dispatch(eventType, ev)
  }

  /** Simula 'error' event server-side (network drop, server reboot, ...). */
  __error(): void {
    this.readyState = MockEventSource.CLOSED
    this.dispatch('error', new Event('error'))
  }

  /** Reset globale (chiamato da beforeEach). B-NEW-2 — pulisce anche byChannelName. */
  static __reset(): void {
    MockEventSource.lastInstance = null
    MockEventSource.instances = []
    MockEventSource.byChannelName.clear()
  }

  private dispatch(type: string, ev: Event): void {
    this.listeners.get(type)?.forEach((fn) => fn(ev))
  }
}
```

Il file NON è production code — è in `test-utils/` ed escluso dalla coverage (vedi 04-01 vitest.config.ts `'src/sse-ws/test-utils/**'`).
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm --filter @gluezero/gateway exec tsc --noEmit && test -f packages/gateway/src/sse-ws/test-utils/mock-event-source.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/test-utils/mock-event-source.ts` esiste
    - File contiene `export class MockEventSource`
    - File contiene `__open`, `__message`, `__error`, `__reset` test helpers
    - File contiene `static lastInstance: MockEventSource | null = null`
    - File contiene `static byChannelName: Map<string, MockEventSource> = new Map()` (B-NEW-2 fix iter 2 — owned da 04-05, non retrofittato da 04-08)
    - File contiene constructor che parsa `_channel` query param e registra l'instance in `byChannelName.set(channelName, this)` (B-NEW-2)
    - File contiene `__reset()` che pulisce anche `byChannelName.clear()` (B-NEW-2)
    - Test smoke (può essere in mock-event-source.test.ts o inline in sse-adapter.test.ts): `new MockEventSource('http://x?_channel=foo')` → `MockEventSource.byChannelName.get('foo')` === instance
    - `pnpm --filter @gluezero/gateway exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>MockEventSource utility disponibile per il test in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crea sse-adapter.ts + sse-adapter.test.ts con TDD RED→GREEN</name>
  <files>
    packages/gateway/src/sse-ws/sse-adapter.ts
    packages/gateway/src/sse-ws/sse-adapter.test.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.8 sse-adapter INTERO + §3.3 abort cascade + §3.4 backpressure + §3.6 TDD)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§3 SSE INTERO incluso 3.2 lastEventId, 3.3 native reconnect, 3.7 topic validation)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-104, D-105, D-109, D-113, D-115)
    - packages/gateway/src/http/http-gateway.ts (analog lifecycle pattern lines 100-298)
    - packages/gateway/src/sse-ws/reconnect-strategy.ts (plan 04-03 — useable)
    - packages/gateway/src/sse-ws/types/realtime-channel-def.ts (plan 04-01)
    - packages/core/src/types/broker-error.ts (createBrokerError + ErrorCategory union)
  </read_first>
  <behavior>
    - Test 1: `new SseAdapter(def, deps)` con `EventSourceCtor: MockEventSource` chiama buildUrl() e new MockEventSource(url, { withCredentials: true })
    - Test 2: `connect()` con buildUrl ritorna URL statico → MockEventSource creato con quello statico
    - Test 3: `connect()` senza buildUrl né url → throw BrokerError code='realtime.config.invalid'
    - Test 4: dopo connect + MockEventSource.__open() → publishFn invocato con BrokerEvent topic='system.realtime.connected', source.type='server', source.name='sse', payload.channel=def.name
    - Test 5: dopo connect + __message('{"city":"Roma"}', 'evt-1', 'weather.update') → publishFn invocato con topic derivato da event.type ('weather.update' o def.name), source.name='sse', id='evt-1' (Last-Event-ID propagato)
    - Test 6: lastEventId tracking — dopo __message con id='evt-1', un secondo connect() chiama buildUrl() e l'URL contiene `?lastEventId=evt-1` o append param (RESEARCH §3.2)
    - Test 7: __error() → publishFn invocato con topic='system.realtime.disconnected', es.close() invocato (readyState===CLOSED), reconnect.recordFailure() invocato
    - Test 8: backpressure DI — quando deps.backpressure è fornito, schedule(channelName, priority, fn) viene invocato PRIMA di publishFn (mock backpressure verify call ordering)
    - Test 9: topic validation — un __message con topic Custom event 'WEATHER.update' (case mixed, regex F1 fail) → publishFn riceve network.error con category 'protocol' E NO crash
    - Test 10: disconnect() — close MockEventSource + abort controller + publishFn riceve system.realtime.disconnected
    - Test 11: checkFreshness(60_000) ritorna true se Date.now() - lastEventReceivedAt < 60_000, false altrimenti
    - Test 12: AbortController cascade — externalSignal.abort() chiama disconnect() automaticamente (D-112)
    - Test 13 (W-4 SC-1 closure): def.eventTypes=['weather.update'] → server invia `event: weather.update\ndata: {"city":"Roma"}` → publishFn riceve BrokerEvent con topic='weather.update' (NOT def.name); listener su 'message' NON registrato per questo canale
    - Test 14 (B-5 Q5 closure): def.sseHeartbeatEventTypes=['heartbeat'] (default) → server invia `event: heartbeat` → lastEventReceivedAt aggiornato MA publishFn NON invocato per quell'evento; subsequent checkFreshness(60_000) ritorna true
  </behavior>
  <action>
**Step 1 (RED) — Crea `packages/gateway/src/sse-ws/sse-adapter.test.ts`** con test che usano `MockEventSource` (Task 1) come `EventSourceCtor`:

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { SseAdapter } from './sse-adapter'
import { MockEventSource } from './test-utils/mock-event-source'
import type { RealtimeChannelDef } from './types/realtime-channel-def'

describe('SseAdapter (D-101, D-104, D-109, D-113 — RT-01/04/06/07)', () => {
  let publishFn: ReturnType<typeof vi.fn>

  beforeEach(() => {
    publishFn = vi.fn()
    MockEventSource.__reset()
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('Test 1: connect() crea EventSource con URL da buildUrl + withCredentials true', async () => {
    const def: RealtimeChannelDef = { name: 'orders', buildUrl: async () => 'https://api.example.com/events' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    expect(MockEventSource.lastInstance).not.toBeNull()
    expect(MockEventSource.lastInstance!.url).toBe('https://api.example.com/events')
    expect(MockEventSource.lastInstance!.withCredentials).toBe(true)
  })

  it('Test 2: connect() con url statico (no buildUrl)', async () => {
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://static.example.com/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    expect(MockEventSource.lastInstance!.url).toBe('https://static.example.com/sse')
  })

  it('Test 3: connect() senza buildUrl né url → throw BrokerError realtime.config.invalid', async () => {
    const def: RealtimeChannelDef = { name: 'broken' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await expect(adapter.connect()).rejects.toMatchObject({ code: 'realtime.config.invalid' })
  })

  it('Test 4: __open() → publishFn riceve system.realtime.connected con source.name=sse', async () => {
    const def: RealtimeChannelDef = { name: 'orders', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    MockEventSource.lastInstance!.__open()
    expect(publishFn).toHaveBeenCalledWith(expect.objectContaining({
      topic: 'system.realtime.connected',
      source: expect.objectContaining({ type: 'server', name: 'sse' }),
      payload: expect.objectContaining({ channel: 'orders' }),
    }))
  })

  it('Test 5: __message() → publishFn riceve BrokerEvent con id da lastEventId', async () => {
    const def: RealtimeChannelDef = { name: 'weather', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    MockEventSource.lastInstance!.__open()
    MockEventSource.lastInstance!.__message('{"city":"Roma"}', 'evt-1', 'weather.update')
    expect(publishFn).toHaveBeenCalledWith(expect.objectContaining({
      id: 'evt-1',
      topic: 'weather.update',
      source: expect.objectContaining({ name: 'sse' }),
    }))
  })

  it('Test 6: lastEventId tracking — secondo connect appende ?lastEventId=evt-1 (RESEARCH §3.2)', async () => {
    const def: RealtimeChannelDef = { name: 'feed', buildUrl: async () => 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    MockEventSource.lastInstance!.__message('{}', 'evt-1', 'message')
    adapter.disconnect()
    await adapter.connect()
    expect(MockEventSource.lastInstance!.url).toContain('lastEventId=evt-1')
  })

  it('Test 7: __error() → publishFn riceve system.realtime.disconnected + es.close', async () => {
    const def: RealtimeChannelDef = { name: 'orders', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    const es = MockEventSource.lastInstance!
    es.__open()
    publishFn.mockClear()
    es.__error()
    expect(es.readyState).toBe(MockEventSource.CLOSED)
    expect(publishFn).toHaveBeenCalledWith(expect.objectContaining({ topic: 'system.realtime.disconnected' }))
  })

  it('Test 8: backpressure DI — schedule invocato PRIMA di publishFn per message events', async () => {
    const callOrder: string[] = []
    const backpressure = {
      schedule: vi.fn((_key: string, _pri: string, task: () => Promise<void>) => {
        callOrder.push('backpressure')
        return task()
      }),
    }
    publishFn.mockImplementation(() => callOrder.push('publish'))
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, {
      publishFn,
      backpressure: backpressure as never,
      EventSourceCtor: MockEventSource as unknown as typeof EventSource,
    })
    await adapter.connect()
    MockEventSource.lastInstance!.__open()
    callOrder.length = 0 // ignore connected event
    publishFn.mockClear()
    MockEventSource.lastInstance!.__message('{}', undefined, 'feed.tick')
    // Backpressure invocata almeno una volta per il message
    expect(backpressure.schedule).toHaveBeenCalled()
  })

  it('Test 9: topic invalid (regex F1 fail) → publishFn riceve network.error category protocol, no crash', async () => {
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    MockEventSource.lastInstance!.__open()
    publishFn.mockClear()
    // event:WEATHER.UPDATE viola la regex F1 /^[a-z0-9]+(\.[a-z0-9]+)*$/
    MockEventSource.lastInstance!.__message('{}', undefined, 'WEATHER.UPDATE')
    // Verify network.error pubblicato — il messaggio NON deve raggiungere subscriber utente
    const errorCall = publishFn.mock.calls.find(c => c[0]?.topic === 'network.error')
    expect(errorCall).toBeDefined()
  })

  it('Test 10: disconnect() chiude EventSource + publish disconnected', async () => {
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    MockEventSource.lastInstance!.__open()
    publishFn.mockClear()
    adapter.disconnect()
    expect(MockEventSource.lastInstance!.readyState).toBe(MockEventSource.CLOSED)
    expect(publishFn).toHaveBeenCalledWith(expect.objectContaining({ topic: 'system.realtime.disconnected' }))
  })

  it('Test 11: checkFreshness — < staleTimeoutMs ritorna true', async () => {
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    await adapter.connect()
    // Mai ricevuto message → checkFreshness ritorna true (default safe)
    expect(adapter.checkFreshness(60_000)).toBe(true)
    MockEventSource.lastInstance!.__message('{}', undefined, 'feed.tick')
    expect(adapter.checkFreshness(60_000)).toBe(true) // appena ricevuto
  })

  it('Test 12: external AbortSignal abort → disconnect chiamato (D-112 cascade)', async () => {
    const def: RealtimeChannelDef = { name: 'feed', url: 'https://x/sse' }
    const adapter = new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource as unknown as typeof EventSource })
    const ctrl = new AbortController()
    await adapter.connect(ctrl.signal)
    MockEventSource.lastInstance!.__open()
    publishFn.mockClear()
    ctrl.abort('test')
    expect(MockEventSource.lastInstance!.readyState).toBe(MockEventSource.CLOSED)
  })
})
```

**Step 2 (GREEN) — Crea `packages/gateway/src/sse-ws/sse-adapter.ts`** basato su PATTERNS.md §2.8 (verbatim adattato):

```typescript
import type { BrokerEvent } from '@gluezero/core'
import { createBrokerError } from '@gluezero/core'
import { nanoid } from 'nanoid'
import type { BackpressureStrategy } from '../http/strategies/backpressure-strategy'
import type { RealtimeChannelDef } from './types/realtime-channel-def'
import { createReconnectStrategy, type ReconnectStrategy } from './reconnect-strategy'

/** Regex F1 D-24 — topic naming: dot-separated lowercase alphanumeric segments. */
const TOPIC_REGEX = /^[a-z0-9]+(\.[a-z0-9]+)*$/

/** Funzione publish iniettata dal manager (loose coupling — no import diretto del Broker). */
export type RealtimePublishFn = (event: BrokerEvent) => void

/** Dipendenze SseAdapter — injection point per backpressure + EventSource Ctor (test). */
export interface SseAdapterDeps {
  /** Publish callback verso il broker (D-113 — pipeline §28 step 1 ingress). */
  readonly publishFn: RealtimePublishFn
  /** Backpressure strategy adapter-level (D-115 — riuso F3, opt-in). */
  readonly backpressure?: BackpressureStrategy
  /** DI EventSource constructor per test jsdom (RESEARCH §9.1). Default: globalThis.EventSource. */
  readonly EventSourceCtor?: typeof EventSource
}

/**
 * SSE adapter (D-101 / D-104 / D-109) — wrapper di `EventSource` nativo con custom
 * reconnect, Last-Event-ID tracking, backpressure adapter-level, ed eventi standard
 * `system.realtime.*` (ERR-02 ext F4).
 *
 * Pattern lifecycle replica `http-gateway.ts:100-298`:
 *   try { setup → register inFlight → attendi outcome → cleanup in finally }
 *
 * **Anti-pattern AP-2** (PATTERNS.md §5): NON tentare di iniettare custom headers in
 * EventSource (vincolo PRD §31.3 + D-105). Auth via `buildUrl()` query string only.
 *
 * **Anti-pattern AP-4 implicito**: NON usare native reconnect di EventSource — su error
 * forziamo `es.close()` per applicare la nostra reconnect policy custom (RESEARCH §3.3).
 *
 * @example
 * ```ts
 * const adapter = new SseAdapter({ name: 'orders', buildUrl: async () => '/sse?token=xyz' }, { publishFn })
 * await adapter.connect(externalSignal)
 * // ... eventi pubblicati via publishFn
 * adapter.disconnect()
 * ```
 */
export class SseAdapter {
  private es: EventSource | null = null
  private readonly controller = new AbortController()
  private readonly reconnect: ReconnectStrategy
  private lastEventId: string | undefined = undefined
  private lastEventReceivedAt = 0

  constructor(
    private readonly def: RealtimeChannelDef,
    private readonly deps: SseAdapterDeps,
  ) {
    this.reconnect = createReconnectStrategy({
      ...(def.reconnect?.baseMs !== undefined && { baseMs: def.reconnect.baseMs }),
      ...(def.reconnect?.capMs !== undefined && { capMs: def.reconnect.capMs }),
      ...(def.reconnect?.consolidationMs !== undefined && { consolidationMs: def.reconnect.consolidationMs }),
      ...(def.reconnect?.maxAttempts !== undefined && { maxAttempts: def.reconnect.maxAttempts }),
      ...(def.reconnect?.fallbackThreshold !== undefined && { fallbackThreshold: def.reconnect.fallbackThreshold }),
      ...(def.reconnect?.globalCycleCap !== undefined && { globalCycleCap: def.reconnect.globalCycleCap }),
      initialMode: 'sse',
    })
  }

  /**
   * Connette il canale SSE.
   *
   * @param externalSignal - signal di abort (cascade D-112). Se aborta → disconnect().
   */
  async connect(externalSignal?: AbortSignal): Promise<void> {
    if (this.controller.signal.aborted) return

    if (externalSignal) {
      // Cascade D-112: external abort → close EventSource + abort our controller.
      externalSignal.addEventListener('abort', () => this.disconnect('external.abort'), { once: true })
    }

    const baseUrl = this.def.buildUrl ? await this.def.buildUrl() : this.def.url
    if (!baseUrl) {
      throw createBrokerError({
        code: 'realtime.config.invalid',
        category: 'config',
        message: `SSE channel "${this.def.name}": neither buildUrl nor url provided`,
      })
    }

    // RESEARCH §3.2 — Last-Event-ID via query string (EventSource non supporta header custom D-105).
    const url = this.lastEventId !== undefined
      ? this.appendQueryParam(baseUrl, 'lastEventId', this.lastEventId)
      : baseUrl

    const Ctor = this.deps.EventSourceCtor ?? (globalThis as { EventSource?: typeof EventSource }).EventSource
    if (!Ctor) {
      throw createBrokerError({
        code: 'realtime.eventsource.unavailable',
        category: 'config',
        message: 'EventSource API not available in this environment',
      })
    }

    this.es = new Ctor(url, { withCredentials: true })

    this.es.addEventListener('open', () => {
      this.reconnect.recordSuccess()
      this.deps.publishFn({
        id: nanoid(),
        topic: 'system.realtime.connected',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'sse' },
        payload: { channel: this.def.name },
      } as BrokerEvent)
    })

    // W-4 fix — custom event types support (SC-1 ROADMAP scenario meteo `event: weather.update`).
    // Se def.eventTypes è popolato → registra listener per ogni eventType custom (topic derivato da event field).
    // Altrimenti → fallback a 'message' con topic = def.name.
    const eventTypes: readonly string[] = (this.def.eventTypes && this.def.eventTypes.length > 0)
      ? this.def.eventTypes
      : ['message']
    for (const eventType of eventTypes) {
      this.es.addEventListener(eventType, (ev: MessageEvent) => this.dispatchInbound(ev, eventType))
    }

    // B-5 fix — SSE heartbeat eventTypes (Q5 closure). Server-emitted custom event types
    // dichiarati in def.sseHeartbeatEventTypes (default ['heartbeat']) aggiornano
    // lastEventReceivedAt SENZA pubblicare BrokerEvent. Mantiene freshness check coerente
    // con WS staleTimeoutMs=60s anche se il server non emette messaggi reali.
    const heartbeatTypes: readonly string[] = this.def.sseHeartbeatEventTypes ?? ['heartbeat']
    for (const heartbeatType of heartbeatTypes) {
      // Skip se già registrato come eventType pubblicabile (evita doppio listener)
      if (eventTypes.includes(heartbeatType)) continue
      this.es.addEventListener(heartbeatType, () => {
        this.lastEventReceivedAt = Date.now()
        // NO publish — server-only heartbeat sintattico
      })
    }

    this.es.addEventListener('error', () => {
      // RESEARCH §3.3 — disable native reconnect: chiudi forziamo CLOSED state.
      this.reconnect.recordFailure()
      this.deps.publishFn({
        id: nanoid(),
        topic: 'system.realtime.disconnected',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'sse' },
        payload: { channel: this.def.name, reason: 'eventsource.error' },
      } as BrokerEvent)
      if (this.es) {
        this.es.close()
        this.es = null
      }
    })
  }

  /** Disconnect + cleanup (D-112 cascade). */
  disconnect(reason: string = 'manual'): void {
    if (this.es) {
      this.es.close()
      this.es = null
    }
    if (!this.controller.signal.aborted) {
      this.controller.abort(reason)
    }
    this.deps.publishFn({
      id: nanoid(),
      topic: 'system.realtime.disconnected',
      timestamp: Date.now(),
      source: { type: 'server', id: 'realtime-channel', name: 'sse' },
      payload: { channel: this.def.name, reason },
    } as BrokerEvent)
  }

  /** Freshness check invocato da Visibility API on-visible (D-110). */
  checkFreshness(staleTimeoutMs: number): boolean {
    if (this.lastEventReceivedAt === 0) return true // mai ricevuto, assume fresh
    return Date.now() - this.lastEventReceivedAt < staleTimeoutMs
  }

  /** Snapshot per debug / Inspector. */
  getDebugInfo(): { name: string; mode: 'sse'; readyState: number | null; lastEventId: string | undefined; lastEventReceivedAt: number } {
    return {
      name: this.def.name,
      mode: 'sse',
      readyState: this.es?.readyState ?? null,
      lastEventId: this.lastEventId,
      lastEventReceivedAt: this.lastEventReceivedAt,
    }
  }

  // ============================================================
  // Private helpers
  // ============================================================

  private dispatchInbound(ev: MessageEvent, eventType: string): void {
    this.lastEventReceivedAt = Date.now()
    if (ev.lastEventId) this.lastEventId = ev.lastEventId

    // Topic determination:
    // - Se `event:` field server-side è presente, ev.type === 'message' (sse default) o
    //   custom event type. V1: per i `'message'` event type, fallback a `def.name` come topic
    //   (consumer registra topic = def.name); per custom event types, usare ev.type.
    const topic = eventType === 'message' ? this.def.name : eventType

    // RESEARCH §3.7 — topic validation (regex F1 D-24). Topic invalid → network.error.
    if (!TOPIC_REGEX.test(topic)) {
      this.deps.publishFn({
        id: nanoid(),
        topic: 'network.error',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'sse' },
        payload: {
          category: 'protocol',
          code: 'realtime.topic.invalid',
          channel: this.def.name,
          rawTopic: topic,
        },
      } as BrokerEvent)
      return
    }

    // Build BrokerEvent (D-113 ingress step 1).
    const event: BrokerEvent = {
      id: ev.lastEventId || nanoid(),
      topic,
      timestamp: Date.now(),
      source: { type: 'server', id: 'realtime-channel', name: 'sse' },
      payload: this.tryParseJson(ev.data),
    } as BrokerEvent

    // D-115 — Backpressure adapter-level PRIMA del publish.
    if (this.deps.backpressure) {
      this.deps.backpressure
        .schedule(this.def.name, 'normal', () => Promise.resolve(this.deps.publishFn(event)))
        .catch(() => { /* drop logged — pattern http-gateway error swallow */ })
    } else {
      this.deps.publishFn(event)
    }
  }

  private tryParseJson(data: unknown): unknown {
    if (typeof data !== 'string') return data
    try { return JSON.parse(data) } catch { return data }
  }

  private appendQueryParam(url: string, key: string, value: string): string {
    try {
      const u = new URL(url)
      u.searchParams.set(key, value)
      return u.toString()
    } catch {
      // URL relativo o malformato — append manuale
      const sep = url.includes('?') ? '&' : '?'
      return `${url}${sep}${encodeURIComponent(key)}=${encodeURIComponent(value)}`
    }
  }
}
```

**Step 3 — RED→GREEN cycle:**

1. Commit RED: `git add packages/gateway/src/sse-ws/sse-adapter.test.ts packages/gateway/src/sse-ws/test-utils/mock-event-source.ts && git commit -m "test(04-05): add failing tests for SseAdapter (D-101, D-104, D-109, RT-01/04/06/07)"`
2. Verify RED fails (sse-adapter.ts non esiste)
3. Commit GREEN: `git add packages/gateway/src/sse-ws/sse-adapter.ts && git commit -m "feat(04-05): implement SseAdapter (EventSource wrapper + Last-Event-ID + backpressure + cascade abort)"`
4. Verify GREEN: 12/12 test passing

**Verifica anti-pattern:**
- `grep -v '^#' packages/gateway/src/sse-ws/sse-adapter.ts | grep -c "Authorization"` === 0 (no header injection AP-2)
- `grep -v '^#' packages/gateway/src/sse-ws/sse-adapter.ts | grep -c "fetch("` === 0 (no fetch+ReadableStream workaround in V1)

**Nota su BrokerError category 'protocol':** se l'union `ErrorCategory` di `@gluezero/core` NON include `'protocol'`, il payload `category: 'protocol'` viaggia come stringa nel field `payload` del `network.error` event (NON come `BrokerError.category`). L'adapter pubblica un `BrokerEvent` con `topic: 'network.error'` e `payload: { category: 'protocol', ... }` — questo è coerente con D-106 e Q2 RESEARCH.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm --filter @gluezero/gateway test packages/gateway/src/sse-ws/sse-adapter.test.ts && pnpm --filter @gluezero/gateway exec tsc --noEmit && [ $(grep -v '^#' packages/gateway/src/sse-ws/sse-adapter.ts | grep -c "Authorization") -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/sse-adapter.ts` esiste e contiene `export class SseAdapter`
    - File contiene `interface SseAdapterDeps` con `publishFn`, `backpressure?`, `EventSourceCtor?`
    - File contiene `new Ctor(url, { withCredentials: true })` (auth via cookie path)
    - File contiene `lastEventId` tracking + `appendQueryParam` per Last-Event-ID injection (RESEARCH §3.2)
    - File contiene `addEventListener('open'`, `addEventListener('message'`, `addEventListener('error'`
    - File contiene `createReconnectStrategy` invocazione con `initialMode: 'sse'`
    - File contiene `recordFailure()` invocato in 'error' handler
    - File contiene `recordSuccess()` invocato in 'open' handler
    - File contiene `es.close()` su error handler (RESEARCH §3.3 disable native reconnect)
    - File contiene topic validation con regex `/^[a-z0-9]+(\.[a-z0-9]+)*$/`
    - File contiene `topic: 'network.error'` per topic invalid
    - File contiene loop `for (const eventType of eventTypes)` con fallback `['message']` (W-4 SC-1 SSE custom event types)
    - File contiene loop `for (const heartbeatType of heartbeatTypes)` con default `['heartbeat']` che aggiorna `lastEventReceivedAt` senza publishFn (B-5 Q5 closure)
    - Test 13 verifica `event: weather.update\ndata:{...}` → BrokerEvent.topic === 'weather.update' (consegnato a subscriber del topic 'weather.update', NON di def.name)
    - Test 14 verifica `event: heartbeat` → checkFreshness(60_000) === true E publishFn NON chiamato per quell'evento
    - File contiene `source: { type: 'server', id: 'realtime-channel', name: 'sse' }` (PRD §18.5)
    - File contiene `externalSignal?.addEventListener('abort'` con `{ once: true }` (D-112 cascade)
    - File contiene `checkFreshness(staleTimeoutMs: number): boolean` method
    - File NON contiene `Authorization` literal (anti-AP-2 check)
    - File `packages/gateway/src/sse-ws/sse-adapter.test.ts` contiene almeno 12 test
    - `pnpm --filter @gluezero/gateway test packages/gateway/src/sse-ws/sse-adapter.test.ts` exit 0
    - `pnpm --filter @gluezero/gateway exec tsc --noEmit` exit 0
    - Cronologia git mostra ≥2 commit 04-05 (RED + GREEN)
  </acceptance_criteria>
  <done>SseAdapter è una class production-ready che incapsula EventSource lifecycle, Last-Event-ID tracking, backpressure, abort cascade e topic validation. 12 test deterministici passing con MockEventSource DI. Plan 04-07 (manager) può istanziare SseAdapter via `new SseAdapter(def, deps)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server↔client (untrusted) | server invia frame e topic — cliente normalizza/valida prima del publish |
| consumer code↔buildUrl | consumer fornisce funzione async che ritorna URL (potenziale URL injection) |
| browser EventSource API | trustato (host browser environment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | server invia event:`SYSTEM.REALTIME.CONNECTED` (uppercase, custom) per impersonare il broker | mitigate | Topic validation regex F1 forza lowercase `/^[a-z0-9]+(\.[a-z0-9]+)*$/`. Uppercase fallisce → network.error pubblicato, evento NON consegnato. Test 9 verifica. |
| T-04-05-02 | Spoofing | server invia topic `system.realtime.connected` valid lowercase per impersonare lifecycle event | accept (V1) | Documentato in DOC-04 plan 04-09: i topic `system.realtime.*` sono "convenzione interna adapter"; consumer non dovrebbero subscribire a quelli da server. V1.x può aggiungere strict filter "drop server-emitted system.* topics". |
| T-04-05-03 | URL injection | consumer buildUrl ritorna `https://malicious.com/...` come URL | accept | Consumer è responsabile per buildUrl (D-104 contract). Il package non valida URL — fa parte del contract documentato. |
| T-04-05-04 | Information Disclosure | token in URL query string appare in browser history / proxy log (PITFALL §11.2) | mitigate | DOC-04 best practices (plan 04-09): short-lived token (≤5min TTL), single-use, server invalida al disconnect. Cookie auth preferred dove possibile. |
| T-04-05-05 | Denial of Service | server invia frame storm 10K/sec — broker overload | mitigate | Backpressure DI (D-115) applica `'queue-bounded'` drop pre-publish. Test 8 verifica chiamata schedule. Critical bypass per `priority: 'critical'` (riuso F3). |
| T-04-05-06 | Memory leak | external AbortSignal aborts ma listener non rimossi → memory leak | mitigate | `{ once: true }` su `addEventListener('abort')` garantisce cleanup automatico. Test 12 verifica disconnect su external abort. |
| T-04-05-07 | Tampering | EventSource native reconnect attiva durante custom reconnect → 2 connection pendenti | mitigate | RESEARCH §3.3: `es.close()` su error forza CLOSED state e blocca native reconnect. Test 7 verifica readyState===CLOSED post-error. |
</threat_model>

<verification>
- File `sse-adapter.ts` con class SseAdapter + SseAdapterDeps interface
- Lifecycle EventSource (open/message/error/close) gestito + reconnect strategy integrata
- Last-Event-ID tracking manuale + injection via query string in reconnect URL
- Backpressure adapter-level pre-publish (D-115 riuso F3)
- Topic validation regex F1 — invalid → network.error category protocol, no crash
- AbortController cascade D-112 con `{ once: true }`
- DI EventSourceCtor per test (jsdom no native EventSource)
- ≥12 test deterministici passing con MockEventSource
- Pattern TDD RED→GREEN visibile in git log
- Anti-pattern AP-2 verificato: NO `Authorization` literal nel file
- Zero regressioni su core/mapper/routing/gateway-http
</verification>

<success_criteria>
1. `new SseAdapter(def, { publishFn, EventSourceCtor: MockEventSource })` + `connect()` → MockEventSource creato con URL da buildUrl + withCredentials true
2. `__open()` → publishFn riceve `system.realtime.connected` con `source: { type: 'server', name: 'sse' }`
3. `__message(data, id, type)` → publishFn riceve BrokerEvent con id da lastEventId + topic dal event.type
4. Reconnect dopo `__error` → buildUrl chiamato di nuovo + URL contiene `?lastEventId=<id>` (RT-07)
5. Topic invalid (uppercase) → publishFn riceve `network.error` category protocol, no crash
6. **W-4 SC-1 closure**: `def.eventTypes=['weather.update']` + server `event: weather.update` → BrokerEvent.topic === 'weather.update'
7. **B-5 Q5 closure**: `def.sseHeartbeatEventTypes=['heartbeat']` + server `event: heartbeat` → lastEventReceivedAt aggiornato, NO publish
8. ≥14 test deterministici passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-05-SUMMARY.md` con:
- LOC count source + test + mock
- Output `pnpm --filter @gluezero/gateway test packages/gateway/src/sse-ws/sse-adapter.test.ts`
- Conferma TDD RED→GREEN (commit hash)
- Conferma anti-AP-2: no `Authorization` in source
- Hand-off note per 04-07 (manager) sulla SseAdapter API
</output>
