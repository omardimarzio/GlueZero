---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 9
type: execute
wave: 6
depends_on:
  - 8
files_modified:
  - packages/gateway/README.md
  - packages/gateway/src/sse-ws/realtime-broker.ts
  - packages/gateway/src/sse-ws/public-factory.ts
  - packages/gateway/src/sse-ws/sse-adapter.ts
  - packages/gateway/src/sse-ws/websocket-adapter.ts
  - packages/gateway/src/sse-ws/realtime-channel-manager.ts
  - packages/gateway/vitest.config.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/TRACKER.md
autonomous: false
requirements:
  - RT-01
  - RT-02
  - RT-03
  - RT-04
  - RT-05
  - RT-06
  - RT-07
  - ERR-02
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "publint --strict passa per @gluezero/gateway (incluso subpath sse-ws)"
    - "attw --pack --profile=esm-only passa per @gluezero/gateway"
    - "Coverage v8 calibrata realistic post-implementation sui file packages/gateway/src/sse-ws/* (target ≥85% statements / ≥75% branches — F2/F3 lesson learned)"
    - "Tutti i 7 REQ-ID di F4 (RT-01..RT-07) flippati a 'Complete' in REQUIREMENTS.md"
    - "ROADMAP.md sezione Phase 4 marcata complete con closure date"
    - "STATE.md aggiornato: current_phase=5, completed includes 4"
    - "TRACKER.md aggiornato: stato F4 complete, prossimo step F5"
    - "DOC-04 README italiano esteso con sezione Realtime SSE/WS"
    - "JSDoc API pubblica completata su 5 file principali F4"
    - "Smoke test cross-package OK"
    - "Zero regressioni su core/mapper/routing/gateway-http test (D-83 strict carryover)"
    - "Open issue PRD §39 #9 chiuso: RT-07 documentato in DOC-04 (Last-Event-ID + ping app-level)"
    - "DOC-04 documenta esplicitamente Q1-Q6 closure rationale (RESEARCH §13.1): Q1 fetch+ReadableStream deferred V1.x, Q2 BrokerError category 'protocol' o payload field, Q3 reconnect consolidation 5s, Q4 wsSubprotocols opt-in, Q5 SSE staleTimeoutMs uniforme 60s + heartbeatEventTypes hook (B-5 closure), Q6 Chromium-only browser test V1"
    - "DOC-04 documenta D-108 (auto-fallback path differenti caveat: SSE e WS NON necessariamente stesso URL/path; consumer fornisce due endpoint server-side oppure endpoint unificato che gestisce upgrade)"
    - "DOC-04 documenta D-114 (mapper.mapToCanonical reuse — nessuna logica F4 specifica; gli adapter pubblicano via inner.publish e la pipeline §28 step 4 applica il mapper F2 esistente)"
    - "DOC-04 documenta D-116 (validation invariata — VAL-03/VAL-04 di F2 si applica al payload realtime senza modifiche)"
    - "DOC-04 documenta il Tier-2/Tier-3 test contract: server contract per MSW SSE replay (riconosce header `Last-Event-ID` E query `?lastEventId=`), Playwright launcher headless Chromium opt-in via `pnpm test:browser`"
  artifacts:
    - path: "packages/gateway/README.md"
      provides: "Documentazione utente F3 (HTTP) + F4 (Realtime SSE/WS) — italiano"
      contains: "Realtime SSE/WS"
      min_lines: 200
    - path: "packages/gateway/src/sse-ws/realtime-broker.ts"
      provides: "JSDoc completata"
      contains: "@example"
    - path: "packages/gateway/src/sse-ws/public-factory.ts"
      provides: "JSDoc completata"
      contains: "@example"
    - path: "packages/gateway/vitest.config.ts"
      provides: "Coverage thresholds calibrati post-implementation"
      contains: "thresholds"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RT-01..RT-07 status flippati a Complete + chiusura PRD §39 #9"
      contains: "RT-07"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 marcata complete + closure date + status COMPLETE"
      contains: "Phase 4"
    - path: ".planning/STATE.md"
      provides: "current_phase: 5, completed includes 4"
      contains: "current_phase"
    - path: ".planning/TRACKER.md"
      provides: "Stato post-F4 + prossimo step F5"
      contains: "Phase 4"
  key_links:
    - from: ".planning/REQUIREMENTS.md sezione Realtime"
      to: "RT-01..RT-07 + ERR-02 ext + TEST-01/02/03 subset"
      via: "table flip pending → complete"
      pattern: "RT-07.*Complete"
    - from: ".planning/ROADMAP.md Phase 4 entry"
      to: "list 9 plan + closure stats"
      via: "checkbox + status complete"
      pattern: "Phase 4.*COMPLETE"
---

<objective>
**Final gate Phase 4** (D-120, analogo 01-11 / 02-12 / 03-14) che chiude la fase con: lint, typecheck, build, test, coverage v8 calibrata, REQ matrix verifica, smoke-test cross-package, DOC-04 README esteso con sezione Realtime + auth patterns + frame envelope + reconnect contract + ping/pong contract + mobile caveats, JSDoc API pubblica completata, ROADMAP/STATE/REQUIREMENTS/TRACKER update, **flip RT-01..RT-07 → Complete**.

Purpose: certifica che F4 è production-ready, chiude formalmente l'open issue PRD §39 #9 (RT-07 — Last-Event-ID + ping app-level), garantisce zero regressioni F1-F3 (D-83 strict), prepara hand-off per Phase 5.

Output:
- README italiano esteso (sezione Realtime SSE/WS aggiunta dopo HTTP F3)
- JSDoc API pubblica completata
- Coverage thresholds calibrati post-implementation
- Update REQUIREMENTS.md / ROADMAP.md / STATE.md / TRACKER.md
- CI gates passano: publint, attw, lint, typecheck, test, coverage

`autonomous: false` per checkpoint:human-verify finale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/README.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TRACKER.md

<interfaces>
Plan finale precedenti come pattern reference:
- 01-11 (F1 closure): publint/attw/size-limit + DOC-01 + JSDoc + ROADMAP/STATE update
- 02-12 (F2 closure): coverage calibration + JSDoc + scenario meteo
- 03-14 (F3 closure): coverage v8 measured + thresholds calibrati + DOC-04 italiani routing+gateway
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coverage measurement + thresholds calibration</name>
  <files>
    packages/gateway/vitest.config.ts
  </files>
  <read_first>
    - packages/gateway/vitest.config.ts (esistente — modificato in plan 04-01)
    - .planning/phases/03-routing-server-gateway-http/03-14-SUMMARY.md (pattern coverage F3)
  </read_first>
  <action>
**Step 1 — Misura coverage v8:**

Run `pnpm --filter @gluezero/gateway test --coverage --reporter=verbose` e cattura le percentuali statements/branches/functions/lines per la sezione `src/sse-ws/`.

**Step 2 — Calibra thresholds:**

Se la coverage misurata è ≥ 90% statements/functions/lines e ≥ 80% branches per `src/sse-ws/`, mantieni i numeri F3 (85/75/88/87 globali). Altrimenti:
- Coverage < 85% statements: identificare branches non testati (defensive try/catch, unreachable error paths). Aggiungere 1-3 test target o accettare valore reale come threshold (lesson learned F2/F3: budget realistic post-implementation).
- Branches < 75%: pattern previsto F2/F3. Accettare 70-75% come threshold se restante è coperto da edge case non-testabili in jsdom.

Aggiorna `packages/gateway/vitest.config.ts` thresholds. Esempio V1 calibration:

```typescript
thresholds: {
  statements: 85,
  branches: 75,
  functions: 88,
  lines: 87,
},
```

Se coverage sse-ws è significantly higher (≥95% per state machine + parser puri), può sollevare i thresholds globali. Decisione lasciata al developer post-misurazione.

**Step 3 — Commit:** `git add packages/gateway/vitest.config.ts && git commit -m "test(04-09): calibrate coverage thresholds post-F4 implementation"`
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm --filter @gluezero/gateway test --coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Coverage v8 misurata e percentuali documentate nella SUMMARY
    - `packages/gateway/vitest.config.ts` thresholds aggiornati o confermati
    - `pnpm --filter @gluezero/gateway test --coverage` exit 0 (passa thresholds)
    - Commit di coverage calibration esiste
  </acceptance_criteria>
  <done>Coverage v8 misurata e thresholds calibrati. CI può eseguire coverage gate senza failure spurie.</done>
</task>

<task type="auto">
  <name>Task 2: CI gates — lint, typecheck, build, publint, attw, test</name>
  <files>
    (nessuna modifica file — solo esecuzione verifica gate)
  </files>
  <read_first>
    - packages/gateway/package.json (verifica scripts)
  </read_first>
  <action>
**Esegui tutti i CI gates per @gluezero/gateway:**

```bash
cd "/Users/omarmarzio/programming/prova AI/GlueZero"

# 1. Lint Biome
pnpm biome check packages/gateway/src/sse-ws/

# 2. Typecheck
pnpm --filter @gluezero/gateway typecheck

# 3. Build (multi-entry)
pnpm --filter @gluezero/gateway build

# 4. publint
pnpm --filter @gluezero/gateway publint

# 5. attw
pnpm --filter @gluezero/gateway attw

# 6. Full test suite
pnpm test
```

**Criteri:**
- Tutti exit 0
- `dist/sse-ws/index.js` + `dist/sse-ws/index.d.ts` + `dist/sse-ws/augment.js` + `dist/sse-ws/augment.d.ts` esistono
- publint 0 errori (warning OK)
- attw esm-only profile passa
- Test totali aumentati di stimati ~105 (4 augment + 15 frame-parser + 15 reconnect + 11 visibility + 12 sse + 15 ws + 12 manager + 16 broker+factory + 6 integration)

**Verifica D-83 strict:**
```bash
git diff main HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/
git diff main HEAD -- packages/gateway/src/http/
```
Aspettativa: VUOTO (zero modifiche runtime a F1-F3 + gateway/http).

**Se regressioni F1-F3:** BLOCKER, rollback immediato delle modifiche unintended.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm --filter @gluezero/gateway typecheck && pnpm --filter @gluezero/gateway build && pnpm --filter @gluezero/gateway publint && pnpm --filter @gluezero/gateway attw && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm biome check packages/gateway/src/sse-ws/` exit 0
    - `pnpm --filter @gluezero/gateway typecheck` exit 0
    - `pnpm --filter @gluezero/gateway build` exit 0
    - `dist/sse-ws/index.js`, `dist/sse-ws/index.d.ts`, `dist/sse-ws/augment.js`, `dist/sse-ws/augment.d.ts` esistono post-build
    - `pnpm --filter @gluezero/gateway publint` exit 0
    - `pnpm --filter @gluezero/gateway attw` exit 0 (esm-only profile)
    - `pnpm test` exit 0 con totale test aumentato di ≥80 nuovi
    - `git diff main HEAD -- packages/core/src/ packages/mapper/src/ packages/routing/src/` produce output VUOTO
    - `git diff main HEAD -- packages/gateway/src/http/` produce output VUOTO o solo whitespace
  </acceptance_criteria>
  <done>Tutti i CI gates passano. Zero regressioni F1-F3. D-83 strict carryover verificato.</done>
</task>

<task type="auto">
  <name>Task 3: DOC-04 README italiano — sezione Realtime SSE/WS</name>
  <files>
    packages/gateway/README.md
  </files>
  <read_first>
    - packages/gateway/README.md (esistente — sezione HTTP F3)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-104..D-115)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§3, §4, §11)
  </read_first>
  <action>
Estendi `packages/gateway/README.md` aggiungendo dopo la sezione HTTP F3 una nuova sezione `## Realtime SSE/WS (Phase 4)` con sotto-paragrafi:

1. **Quick start** — esempio createRealtimeBroker + connectRealtime + subscribe
2. **Auth patterns (D-104, D-105)** — tabella delle 4 strategie (cookie HttpOnly same/cross-origin, token query string, WS subprotocol). Best practice: short-lived ≤5min, single-use server-side
3. **Frame envelope contract (D-106)** — struttura `{topic, data, id?}`, regex F1 topic, fallback a `network.error category=protocol`. Note Q2 closure: `category` può viaggiare come field di `BrokerError.category` se l'union accetta 'protocol', altrimenti come field del payload `network.error`
4. **SSE custom event types (W-4 closure)** — `def.eventTypes?: readonly string[]` per topic dinamici via `event:` field SSE (es. `event: weather.update`); fallback a `'message'` con topic = `def.name` se omesso
5. **SSE heartbeat hook (B-5 + Q5 closure)** — `def.sseHeartbeatEventTypes?: readonly string[]` (default `['heartbeat']`): server invia `event: heartbeat` ogni ≤60s per mantenere freshness senza spam topic. `staleTimeoutMs` uniforme con WS=60_000 (Q5)
6. **Reconnect contract (RT-05 + D-109 + RT-07)** — full jitter formula, **Last-Event-ID query string injection** (chiusura PRD §39 #9), eventi standard `system.realtime.*` (incluso `system.realtime.reconnecting` + `system.realtime.failed`), server middleware example che riconosce ENTRAMBI header e query. Note Q3 closure: `consolidationMs: 5_000` (default) coalesce reconnect events ravvicinati
7. **Ping/pong contract WebSocket (D-111)** — heartbeat applicativo (`__ping__`/`__pong__`), `staleTimeoutMs: 60_000` (uniforme con SSE Q5), **strict-match topic riservati** (anti-AP-6)
8. **Auto-fallback SSE→WS (D-107 + D-108 + B-4 closure)** — tabella scheme switch, cap 5 cicli, **caveat path differenti (D-108)**: SSE e WS NON necessariamente stesso URL/path; consumer fornisce due endpoint distinti (es. `/sse` + `/ws`) oppure endpoint unificato che gestisce upgrade. **B-4 closure**: il `runReconnectLoop` interno orchestra il fallback effettivo; verificato in `auto-fallback.test.ts`. WS subprotocol opt-in `wsSubprotocols` (Q4 closure)
9. **Visibility-aware behavior (D-110)** — checkFreshnessAll on visible, tolerance ×3 hidden, mobile caveat
10. **Cascade cleanup (D-112 + LIFE-02 ext F4)** — lifecycle ownerId-based, disconnectByOwner pattern
11. **Backpressure adapter-level (D-115)** — riuso F3, default queue-bounded 1000, critical bypass
12. **Mapper + validation invariati (D-114 + D-116 closure W-2)** — gli adapter pubblicano via `inner.publish` e la pipeline §28 step 4-6 applica il mapper F2 + validation F2 SENZA logica F4 specifica. Esempio: server invia `{città, temp}` → subscriber riceve `{location, temperature_celsius}` se è registrato un `inputMap` F3. Documentare il pattern con esempio "scenario meteo" SC-1 ROADMAP
13. **Test contract D-118 3-tier (B-1 closure)** — Tier-1 jsdom (vitest default), Tier-2 MSW 2.x (`pnpm test:msw` opt-in: server mock SSE riconosce header `Last-Event-ID` E query `?lastEventId=`), Tier-3 Playwright headless Chromium (`pnpm test:browser` opt-in). Note Q6 closure: V1 è Chromium-only; Firefox/Safari deferred
14. **Limitazioni V1 documentate** — header EventSource, gap recovery, binary frames, outbound, multiplexing, browser test cross-engine

Lingua italiana per descrizioni, inglese per esempi codice/identifier. Lunghezza target ≥150 LOC nuove.

**Commit:** `git add packages/gateway/README.md && git commit -m "docs(04-09): extend README with Realtime SSE/WS section (DOC-04 + RT-07 closure)"`
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && grep -q "Realtime SSE/WS" packages/gateway/README.md && grep -q "Last-Event-ID" packages/gateway/README.md && grep -q "D-107" packages/gateway/README.md && grep -q "D-108" packages/gateway/README.md && grep -q "D-114" packages/gateway/README.md && grep -q "D-116" packages/gateway/README.md && grep -q "Tier-2" packages/gateway/README.md && grep -q "Tier-3" packages/gateway/README.md && grep -q "eventTypes" packages/gateway/README.md && grep -q "sseHeartbeatEventTypes" packages/gateway/README.md && wc -l packages/gateway/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/README.md` contiene sezione `## Realtime SSE/WS (Phase 4)`
    - Sezione contiene 14 sotto-paragrafi (Quick start, Auth, Frame envelope, SSE eventTypes W-4, SSE heartbeat B-5, Reconnect, Ping/pong, Auto-fallback D-107+D-108+B-4, Visibility, Cascade, Backpressure, Mapper+Validation D-114+D-116 W-2, Test contract D-118 3-tier B-1, Limitazioni)
    - Sezione menziona "Last-Event-ID" (chiusura PRD §39 #9 RT-07)
    - Sezione menziona "D-107" (auto-fallback) E "D-108" (path differenti caveat)
    - Sezione menziona "D-114" + "D-116" (mapper + validation reuse — W-2 closure)
    - Sezione menziona "Q1" + "Q2" + "Q3" + "Q4" + "Q5" + "Q6" closure rationale (RESEARCH §13.1)
    - Sezione menziona "Tier-1" + "Tier-2" + "Tier-3" test contract (B-1 D-118 closure)
    - Sezione menziona `eventTypes` (W-4) + `sseHeartbeatEventTypes` (B-5)
    - Lingua italiana per testo descrittivo, codice in inglese
    - LOC totale README ≥ 250 (incrementato da +50 per le 4 nuove sezioni W-2/W-4/B-1/B-5)
    - Commit di README extension esiste
  </acceptance_criteria>
  <done>DOC-04 README esteso. RT-07 chiusura PRD §39 #9 documentata.</done>
</task>

<task type="auto">
  <name>Task 4: JSDoc API pubblica completata su file principali F4</name>
  <files>
    packages/gateway/src/sse-ws/realtime-broker.ts
    packages/gateway/src/sse-ws/public-factory.ts
    packages/gateway/src/sse-ws/sse-adapter.ts
    packages/gateway/src/sse-ws/websocket-adapter.ts
    packages/gateway/src/sse-ws/realtime-channel-manager.ts
  </files>
  <read_first>
    - file source esistenti dei plan 04-05/06/07/08
    - packages/gateway/src/http/http-gateway.ts (pattern JSDoc F3)
  </read_first>
  <action>
Verifica e arricchisci JSDoc dei 5 file API pubblica F4. Per ognuno garantisci:

1. **Class/factory header** con descrizione + reference D-XX + `@example` codice eseguibile
2. **@param** per ogni parametro pubblico
3. **@returns** dove rilevante
4. **@throws** per metodi che possono throw `BrokerError`
5. **@see** per cross-reference (PRD section, decision ID, file analog)

Pattern reference: `packages/gateway/src/http/http-gateway.ts` (JSDoc completi F3).

Esempio per createRealtimeBroker — JSDoc completa con @param, @returns, @throws, @example multi-scenario:

```typescript
/**
 * Crea una nuova istanza RealtimeBroker.
 *
 * Pattern identico a createHttpGateway/createRouterBroker: factory pure function +
 * Valibot validation + Error con prefisso "Invalid ...". No singleton (D-30).
 *
 * Vincolo D-83 strict: zero modifiche runtime a F1-F3.
 *
 * @param config - RealtimeBrokerConfig (estende RouterBrokerConfig)
 * @returns Istanza RealtimeBroker pronta all'uso
 * @throws {Error} "Invalid RealtimeBrokerConfig: ..." se validation fail
 *
 * @example Quick start
 * (... esempio completo ...)
 *
 * @see {@link RealtimeBroker}
 */
```

**Build verification:**
```bash
pnpm --filter @gluezero/gateway build
grep -A 5 "createRealtimeBroker" packages/gateway/dist/sse-ws/index.d.ts | head -20
```

I JSDoc devono essere preservati nel `.d.ts` post-build.

**Commit:** `git add packages/gateway/src/sse-ws/{realtime-broker,public-factory,sse-adapter,websocket-adapter,realtime-channel-manager}.ts && git commit -m "docs(04-09): complete JSDoc on F4 public API (TypeDoc-ready)"`
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && pnpm --filter @gluezero/gateway build && grep -c "@example" packages/gateway/src/sse-ws/realtime-broker.ts packages/gateway/src/sse-ws/public-factory.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `realtime-broker.ts` contiene almeno 2 `@example` blocks
    - File `public-factory.ts` contiene almeno 1 `@example` + `@throws`
    - File `sse-adapter.ts` contiene `@example` o `@see`
    - File `websocket-adapter.ts` contiene `@example` o `@see`
    - File `realtime-channel-manager.ts` contiene `@example` o `@see`
    - `pnpm --filter @gluezero/gateway build` exit 0 e `dist/sse-ws/index.d.ts` preserva JSDoc
    - Commit di JSDoc completion esiste
  </acceptance_criteria>
  <done>JSDoc API pubblica completata. TypeDoc consolidation finale deferred a F6.</done>
</task>

<task type="auto">
  <name>Task 5: Update REQUIREMENTS / ROADMAP / STATE / TRACKER</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/TRACKER.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (sezione Realtime Fase 4)
    - .planning/ROADMAP.md (sezione Phase 4)
    - .planning/STATE.md
    - .planning/TRACKER.md
  </read_first>
  <action>
**Step 1 — REQUIREMENTS.md flip RT-01..RT-07 a Complete:**

Nella tabella "Realtime Inbound — Fase 4", aggiorna lo Status di tutti i 7 REQ-ID da `Pending` a `Complete`, con note per ciascuno:

- RT-01 Phase 4 Complete | SSE prioritario V1 — `SseAdapter` (plan 04-05)
- RT-02 Phase 4 Complete | WebSocket disponibile — `WebSocketAdapter` (plan 04-06)
- RT-03 Phase 4 Complete | `connectRealtime`/`disconnectRealtime` API (PRD §16.2) — `RealtimeBroker` (plan 04-08)
- RT-04 Phase 4 Complete | `source: { type: 'server', id: 'realtime-channel', name: 'sse'/'websocket' }` (D-113)
- RT-05 Phase 4 Complete | Full jitter cap 30s + heartbeat + visibility-aware — `createReconnectStrategy` (plan 04-03)
- RT-06 Phase 4 Complete | Mapper server->canonical riusato F2/F3 (D-114 attraverso RouterBroker)
- RT-07 Phase 4 Complete | **Closes PRD §39 #9**: Last-Event-ID via query (RESEARCH §3.2) + ping app-level (D-111)

Aggiorna anche le note cross-cutting:
- **ERR-02**: F4 ext annotato — `system.realtime.connected/disconnected/reconnecting`
- **LIFE-02**: F4 cascade D-112 verificato in `cascade-cleanup.test.ts`
- **TEST-01/02/03**: F4 subset documentato (plan 04-08 integration scenarios)

Sezione "Open Issues PRD §39 chiusura": annota #9 → Phase 4 plan 04-09.

**Step 2 — ROADMAP.md Phase 4 marcato complete:**

Sostituisci `### Phase 4: Realtime inbound` con:

```markdown
- [x] **Phase 4: Realtime inbound (SSE prioritario, WS opzionale)** ✅ **COMPLETE** (9/9 plans, ready for verifier) — Adapter SSE + WebSocket con reconnection policy unificata, normalizzazione canonica, auto-fallback SSE→WS, ping/pong applicativo, visibility-aware behavior, cascade cleanup ext F4. Closed: 2026-05-XX.
```

Aggiorna sezione dettagli Phase 4 con:
```
**Plans**: 9/9 complete
**Status**: ✅ COMPLETE — ready for verification
**Closure date**: 2026-05-XX
**Test coverage**: TBD test files / TBD nuovi test F4 in @gluezero/gateway/sse-ws
**CI gates**: publint ✅, attw ESM-only ✅, biome ✅, typecheck ✅
**Coverage v8**: sse-ws TBD%/TBD%/TBD%/TBD% (statements/branches/functions/lines)
**Open issues PRD §39 chiusi**: #9 (RT-07)
**Needs research**: yes (consumed)
```

Aggiorna "Progress" table riga Phase 4:
```
| 4. Realtime inbound | 9/9 | ✅ Complete & Verified | 2026-05-XX |
```

**Step 3 — STATE.md update:**

Aggiorna i campi:
```yaml
current_phase: 5
completed:
  - 1
  - 2
  - 3
  - 4
```

(Adatta alla forma reale di STATE.md attuale)

**Step 4 — TRACKER.md update:**

Sezione "Stato corrente":
```
| Fase | **Phase 5 — Worker Runtime — 🟢 NOT STARTED** |
| Wave | — |
| Plan progress F4 | 9 / 9 (✅ COMPLETE) |
| Plan progress globale | 46 / 46+ |
```

Sezione "Ultimo step completato":
```
- Step: **Phase 4 final gate** → CI gates pass + DOC-04 + RT-01..RT-07 → Complete
- Commit: <hash>
- File: .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-09-SUMMARY.md
```

Sezione "Prossimo step":
```
**Avvio Phase 5 (Worker Runtime):**
- /gsd-discuss-phase 5 OR /gsd-plan-phase 5 --research
- WK-01..WK-07 da REQUIREMENTS.md
- Comlink + WorkerBridge, structuredClone default, WK-07 chiude PRD §39 #11
```

**Step 5 — Commit unico atomic:**
```bash
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/TRACKER.md
git commit -m "docs(04-09): close Phase 4 — RT-01..RT-07 complete, RT-07 closes PRD §39 #9"
```
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/GlueZero" && grep -c "RT-0[1-7]" .planning/REQUIREMENTS.md && grep -E "Phase 4.*COMPLETE" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contiene almeno 7 occorrenze di pattern `RT-0X` (RT-01..RT-07)
    - `.planning/REQUIREMENTS.md` annota chiusura PRD §39 #9 per RT-07
    - `.planning/ROADMAP.md` Phase 4 entry contiene `[x]` + `COMPLETE` + closure date
    - `.planning/ROADMAP.md` Progress table riga Phase 4 mostra `9/9` + `Complete`
    - `.planning/STATE.md` current_phase: 5
    - `.planning/STATE.md` completed include 4
    - `.planning/TRACKER.md` stato attuale mostra Phase 4 ✅ COMPLETE
    - `.planning/TRACKER.md` prossimo step menziona Phase 5
    - Commit `docs(04-09): close Phase 4 ...` esiste
  </acceptance_criteria>
  <done>Tracking documenti aggiornati. F4 formalmente chiusa. F5 può iniziare con `/gsd-discuss-phase 5 --research`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 Realtime inbound (SSE/WS) è formalmente chiusa:
- 9 plans completati (Wave 1-6)
- ~105 nuovi test (unit + integration)
- Coverage v8 calibrata realistic post-implementation
- DOC-04 README esteso con sezione Realtime SSE/WS completa
- RT-01..RT-07 flippati a Complete in REQUIREMENTS.md
- PRD §39 open issue #9 (RT-07) chiuso
- ROADMAP.md / STATE.md / TRACKER.md aggiornati
- D-83 strict carryover verificato (zero modifiche runtime a core/mapper/routing/gateway-http)
  </what-built>
  <how-to-verify>
1. **Visual smoke check del README:**
   `cat packages/gateway/README.md | less` — verifica sezione "Realtime SSE/WS (Phase 4)" presente con 10 sotto-paragrafi.

2. **Smoke import cross-package** (compile-only se workspace impedisce npm install):
   `pnpm --filter @gluezero/gateway exec tsc --noEmit` — typecheck OK.

3. **Git status pulito:**
   `git status` — working tree clean.

4. **REQUIREMENTS.md flip verificato:**
   `grep -E "RT-0[1-7].*Complete" .planning/REQUIREMENTS.md` — 7 righe.

5. **Test full suite passing:**
   `pnpm test` — exit 0, totale ≥ pre-F4 + ~105 nuovi.

6. **Cronologia git pulita F4:**
   `git log --oneline | head -40` — ~25-35 commit del Phase 4.
  </how-to-verify>
  <resume-signal>Type "approved" se la verifica è ok, altrimenti descrivi gli issue per applicare fix prima del closure formale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer↔CI gates | publint/attw/lint sono gate automatici |
| package consumer↔README | documentazione utente — accuracy critica per security best-practice (auth) |
| tracking docs↔phase closure | flip RT status indica formal completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | DOC-04 README documenta token in URL pattern, consumer adopta senza short-lived | mitigate | README esplicita "best practice: short-lived ≤5min, single-use, server invalida al disconnect". Cookie auth menzionato come preferred. |
| T-04-09-02 | Repudiation | RT-01..RT-07 flippati senza coverage vera dei test | mitigate | Task 2 richiede pnpm test exit 0 con ≥80 nuovi test. Task 1 misura coverage v8 reale prima del flip. |
| T-04-09-03 | Tampering | qualcuno modifica F1/F2/F3 runtime "per fix coverage" violando D-83 | mitigate | Task 2 acceptance: git diff vuoto su core/mapper/routing/gateway/http. Verificato esplicitamente. |
| T-04-09-04 | Spoofing topic system.realtime.* | RT-04 chiusa senza filter "drop server-emitted system.* topics" | accept (V1) | Documentato in DOC-04 README come limitazione V1 + AP-12 PATTERNS.md. V1.x può aggiungere strict filter. |
</threat_model>

<verification>
- Coverage v8 calibrata realistic (≥85% statements + ≥75% branches sse-ws)
- publint --strict OK
- attw esm-only OK
- biome lint OK
- typecheck OK
- Build genera dist/sse-ws/{index,augment}.{js,d.ts}
- Full test suite exit 0 con ~105 nuovi test
- Zero regressioni F1/F2/F3 (D-83 strict)
- DOC-04 README sezione Realtime SSE/WS ≥150 righe in italiano
- JSDoc API pubblica con @example blocks su 5 file principali
- REQUIREMENTS.md tutti i 7 RT-XX → Complete
- ROADMAP.md Phase 4 → COMPLETE
- STATE.md current_phase: 5
- TRACKER.md stato Phase 4 ✅ + prossimo step F5
- PRD §39 #9 (RT-07) chiuso
- Checkpoint human-verify approved
</verification>

<success_criteria>
1. Tutti i CI gates passano (publint + attw + lint + typecheck + build + test + coverage)
2. Coverage v8 calibrata realistic post-implementation
3. DOC-04 README italiano esteso con 10 sotto-paragrafi sezione Realtime
4. JSDoc API pubblica completata con @example/throws/see su 5 file principali
5. RT-01..RT-07 flippati a Complete + chiusura PRD §39 #9 documentata
6. ROADMAP/STATE/TRACKER aggiornati per F4 closure + handoff F5
7. Zero regressioni su F1-F3 (git diff vuoto su packages F1-F3 + gateway/http)
8. Smoke import cross-package OK
9. Checkpoint human-verify approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-09-SUMMARY.md` con:
- Coverage v8 misurata: statements/branches/functions/lines per src/sse-ws/
- Test count totale: pre-F4 baseline → post-F4 final
- Output `pnpm publint && pnpm attw` per @gluezero/gateway
- LOC count totale aggiunto in F4 (source + test)
- LOC README aggiunto (sezione Realtime)
- D-83 strict carryover verificato: git diff vuoto su packages F1-F3
- 9 plan summary brief con commit hash di reference
- 11 PRD §39 open issues progress: 9 totali chiusi (F1: #7, F2: #1+#3+#4, F3: #5+#6+#8, F4: #9). Aperti: #2, #10 (F6), #11 (F5)
- Hand-off note Phase 5: Worker Runtime parallelizzabile, F5 può iniziare con /gsd-discuss-phase 5 --research
</output>
