---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 06
type: tdd
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - packages/gateway/src/sse-ws/websocket-adapter.ts
  - packages/gateway/src/sse-ws/websocket-adapter.test.ts
  - packages/gateway/src/sse-ws/test-utils/mock-websocket.ts
autonomous: true
requirements:
  - RT-02
  - RT-04
  - RT-05
  - RT-06
  - ERR-02
must_haves:
  truths:
    - "WebSocketAdapter.connect() chiama buildUrl() async (D-104) con scheme switch http(s)→ws(s) (D-107) o usa url statico"
    - "Su WS open event publish system.realtime.connected con source.name='websocket' + start heartbeat timer (D-111)"
    - "Su WS message event applica parseFrame (plan 04-02), filtra __ping__/__pong__ via isInternalTopic strict, applica backpressure D-115 PRIMA di publishFn"
    - "Heartbeat ping/pong applicativo: invia { topic: '__ping__', data: { ts } } ogni heartbeatIntervalMs (default 30000)"
    - "Stale detection: se Date.now() - lastPongAt > staleTimeoutMs (default 60000) → disconnect + reconnect record failure"
    - "wsSubprotocols (Q4) passati a new WebSocket(url, subprotocols) come passthrough opt-in"
    - "WS close event con code !== 1000 → publish system.realtime.disconnected con details.code/reason; reconnect record failure (RFC 6455 §7.4)"
    - "Frame parsing fail (parseFrame.ok===false) → publish network.error category protocol con reason + raw"
    - "MockWebSocket DI permette test jsdom (RESEARCH §9.1)"
    - "bufferedAmount > 64KB → ping skipped (RESEARCH §4.4)"
    - "MockWebSocket espone `static byChannelName: Map<string, MockWebSocket>` populated nel constructor parsing query param `_channel` (B-NEW-2 fix iter 2 — abilita routing strict harness 04-08; owned da 04-06)"
  artifacts:
    - path: "packages/gateway/src/sse-ws/websocket-adapter.ts"
      provides: "WebSocketAdapter class + WebSocketAdapterDeps interface"
      contains: "export class WebSocketAdapter"
      min_lines: 200
    - path: "packages/gateway/src/sse-ws/websocket-adapter.test.ts"
      provides: "≥15 test deterministici (connect, ping/pong, stale, frame parse fail, internal topics filter, close codes, subprotocols, bufferedAmount, anti-AP-6)"
      contains: "describe('WebSocketAdapter"
    - path: "packages/gateway/src/sse-ws/test-utils/mock-websocket.ts"
      provides: "MockWebSocket class compatibile WebSocket interface + static byChannelName Map per routing test-only (B-NEW-2)"
      contains: "export class MockWebSocket"
  key_links:
    - from: "packages/gateway/src/sse-ws/websocket-adapter.ts"
      to: "packages/gateway/src/sse-ws/frame-parser.ts (plan 04-02)"
      via: "import { parseFrame, isInternalTopic, INTERNAL_TOPICS }"
      pattern: "from './frame-parser'"
    - from: "packages/gateway/src/sse-ws/websocket-adapter.ts"
      to: "packages/gateway/src/sse-ws/reconnect-strategy.ts (plan 04-03)"
      via: "createReconnectStrategy({ initialMode: 'websocket' })"
      pattern: "createReconnectStrategy"
---

<objective>
Implementa il **WebSocket adapter** (D-101 / D-104 / D-106 / D-107 / D-109 / D-111 — RT-02, RT-04, RT-06) — wrapper di `WebSocket` nativo con:
- Lifecycle (connect/disconnect/reconnect) governato da `ReconnectStrategy` di plan 04-03 (`initialMode: 'websocket'`)
- **Envelope JSON parsing** via `parseFrame` di plan 04-02 (D-106)
- **Internal topics filter strict** (`__ping__`/`__pong__` via `isInternalTopic` — PITFALL §11.7 anti-AP-6)
- **Heartbeat ping/pong applicativo** (D-111) — invia `{topic:'__ping__',data:{ts}}` ogni `heartbeatIntervalMs`, watchdog `staleTimeoutMs` su `lastPongAt`
- **bufferedAmount check** (RESEARCH §4.4) — skip ping se buffer > 64KB
- **Scheme switch** (D-107) — `http(s)://` → `ws(s)://` automatico
- **WS subprotocols** (Q4 / PITFALL §11.3) — `def.wsSubprotocols` passthrough
- **Close codes routing** (RFC 6455 §7.4 / RESEARCH §4.2) — code !== 1000 → reconnect record failure
- **Backpressure adapter-level** (D-115) PRIMA di `publishFn` — riuso 1:1 di `BackpressureStrategy` F3
- **AbortController cascade** (D-112)
- **DI WebSocketCtor** per testabilità

Output:
- 1 nuovo file source (`websocket-adapter.ts`)
- 1 nuovo file test (`websocket-adapter.test.ts` — ≥15 test TDD RED→GREEN)
- 1 nuovo file test util (`test-utils/mock-websocket.ts`)

**Anti-pattern AP-3** (PATTERNS.md §5): NON usare `reconnecting-websocket`.
**Anti-pattern AP-4**: NON trustare `readyState === OPEN` come liveness — ping/pong app-level.
**Anti-pattern AP-6**: match strict per `__ping__`/`__pong__` (no prefix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/src/sse-ws/frame-parser.ts
@packages/gateway/src/sse-ws/reconnect-strategy.ts
@packages/gateway/src/sse-ws/sse-adapter.ts
@packages/gateway/src/sse-ws/types/realtime-channel-def.ts
@packages/core/src/types/broker-error.ts
@packages/core/src/types/broker-event.ts

<interfaces>
From plan 04-02 (`packages/gateway/src/sse-ws/frame-parser.ts`):
```typescript
export function parseFrame(raw: unknown): FrameParseResult
export function isInternalTopic(topic: string): boolean  // strict match
export const INTERNAL_TOPICS = Object.freeze({ PING: '__ping__', PONG: '__pong__' })
```

From plan 04-03 (`reconnect-strategy.ts`):
```typescript
export function createReconnectStrategy(opts?): ReconnectStrategy
```

From plan 04-05 (`sse-adapter.ts`): pattern shape SseAdapterDeps riutilizzabile per WS.

From PATTERNS.md §2.9 (websocket-adapter excerpt completo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crea test-utils/mock-websocket.ts</name>
  <files>
    packages/gateway/src/sse-ws/test-utils/mock-websocket.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.16, §2.18 mock pattern)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§9.1)
  </read_first>
  <action>
Crea `packages/gateway/src/sse-ws/test-utils/mock-websocket.ts` come MockWebSocket class minimale per DI in test:

```typescript
/**
 * MockWebSocket — implementazione minimale compatibile `WebSocket` per test jsdom
 * (RESEARCH §9.1 — jsdom non ha WebSocket nativo).
 *
 * Test helpers (`__open`, `__message`, `__error`, `__close`, `__setBufferedAmount`)
 * simulano lifecycle server-side. `sentFrames` traccia le `send()` per assertion.
 */
export class MockWebSocket implements Pick<WebSocket,
  'addEventListener' | 'removeEventListener' | 'close' | 'send' | 'readyState' | 'url' | 'protocol' | 'bufferedAmount' | 'CONNECTING' | 'OPEN' | 'CLOSING' | 'CLOSED'
> {
  static readonly CONNECTING = 0 as const
  static readonly OPEN = 1 as const
  static readonly CLOSING = 2 as const
  static readonly CLOSED = 3 as const

  readonly url: string
  readonly protocol: string
  readyState: number = MockWebSocket.CONNECTING
  bufferedAmount = 0
  readonly CONNECTING = 0
  readonly OPEN = 1
  readonly CLOSING = 2
  readonly CLOSED = 3

  static lastInstance: MockWebSocket | null = null
  static instances: MockWebSocket[] = []

  /**
   * **B-NEW-2 fix (iter 2)** — Map test-only per routing strict per-canale dell'harness
   * (plan 04-08). Ogni canale di test che vuole essere indicizzato deve avere `?_channel=<name>`
   * nella URL: il constructor parsa il query param e si registra qui. Permette
   * `MockWebSocket.byChannelName.get(name)` lookup deterministico.
   */
  static byChannelName: Map<string, MockWebSocket> = new Map()

  sentFrames: string[] = []

  private listeners = new Map<string, Set<EventListener>>()

  constructor(url: string | URL, protocols?: string | readonly string[]) {
    this.url = url.toString()
    this.protocol = Array.isArray(protocols) ? (protocols[0] ?? '') : (typeof protocols === 'string' ? protocols : '')
    MockWebSocket.lastInstance = this
    MockWebSocket.instances.push(this)

    // B-NEW-2 — parse `_channel=<name>` query param per indexing test-only.
    // Se l'URL è relativo o malformato, skip silenziosamente (non indicizzabile).
    try {
      const parsed = new URL(this.url, 'http://localhost')
      const channelName = parsed.searchParams.get('_channel')
      if (channelName) {
        MockWebSocket.byChannelName.set(channelName, this)
      }
    } catch {
      // URL non parsabile: skip (no-op).
    }
  }

  addEventListener(type: string, fn: EventListener): void {
    if (!this.listeners.has(type)) this.listeners.set(type, new Set())
    this.listeners.get(type)!.add(fn)
  }

  removeEventListener(type: string, fn: EventListener): void {
    this.listeners.get(type)?.delete(fn)
  }

  send(data: string | ArrayBuffer | Blob): void {
    if (typeof data === 'string') this.sentFrames.push(data)
  }

  close(code: number = 1000, reason: string = ''): void {
    if (this.readyState === MockWebSocket.CLOSED) return
    this.readyState = MockWebSocket.CLOSED
    const ev = new CloseEvent('close', { code, reason, wasClean: code === 1000 })
    this.dispatch('close', ev)
  }

  __open(): void {
    this.readyState = MockWebSocket.OPEN
    this.dispatch('open', new Event('open'))
  }

  __message(data: string): void {
    this.dispatch('message', new MessageEvent('message', { data }))
  }

  __error(): void {
    this.dispatch('error', new Event('error'))
  }

  __close(code: number, reason: string = '', wasClean: boolean = false): void {
    this.readyState = MockWebSocket.CLOSED
    const ev = new CloseEvent('close', { code, reason, wasClean })
    this.dispatch('close', ev)
  }

  __setBufferedAmount(bytes: number): void {
    this.bufferedAmount = bytes
  }

  static __reset(): void {
    MockWebSocket.lastInstance = null
    MockWebSocket.instances = []
    MockWebSocket.byChannelName.clear() // B-NEW-2
  }

  private dispatch(type: string, ev: Event): void {
    this.listeners.get(type)?.forEach((fn) => fn(ev))
  }
}
```

Il file è in `test-utils/`, escluso da coverage (vedi 04-01 vitest.config.ts).
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway exec tsc --noEmit && test -f packages/gateway/src/sse-ws/test-utils/mock-websocket.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/test-utils/mock-websocket.ts` esiste
    - File contiene `export class MockWebSocket`
    - File contiene helpers `__open`, `__message`, `__close`, `__setBufferedAmount`, `__reset`
    - File contiene `sentFrames: string[]`
    - File contiene `static byChannelName: Map<string, MockWebSocket> = new Map()` (B-NEW-2 fix iter 2 — owned da 04-06, non retrofittato da 04-08)
    - File contiene constructor che parsa `_channel` query param e registra l'instance in `byChannelName.set(channelName, this)` (B-NEW-2)
    - File contiene `__reset()` che pulisce anche `byChannelName.clear()` (B-NEW-2)
    - Test smoke (può essere in mock-websocket.test.ts o inline in websocket-adapter.test.ts): `new MockWebSocket('ws://x?_channel=bar')` → `MockWebSocket.byChannelName.get('bar')` === instance
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>MockWebSocket disponibile per Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crea websocket-adapter.ts + .test.ts con TDD RED→GREEN</name>
  <files>
    packages/gateway/src/sse-ws/websocket-adapter.ts
    packages/gateway/src/sse-ws/websocket-adapter.test.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.9 websocket-adapter INTERO + §3.3 abort cascade + §3.4 backpressure)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§4 WebSocket — 4.2 close codes, 4.3 ping/pong NOT exposed, 4.4 bufferedAmount, 4.5 onerror opaque, 4.6 OPEN zombie)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-106, D-107, D-111, D-115)
    - packages/gateway/src/sse-ws/frame-parser.ts (plan 04-02)
    - packages/gateway/src/sse-ws/reconnect-strategy.ts (plan 04-03)
    - packages/gateway/src/sse-ws/sse-adapter.ts (plan 04-05 — analog)
  </read_first>
  <behavior>
    - Test 1: scheme switch — buildUrl='https://api/x' → MockWebSocket creato con 'wss://api/x'
    - Test 2: wsSubprotocols passthrough — `['sembridge-v1']` → MockWebSocket.protocol === 'sembridge-v1'
    - Test 3: missing url+buildUrl → throw realtime.config.invalid
    - Test 4: __open() → publishFn system.realtime.connected con source.name='websocket' + heartbeat started
    - Test 5: __message envelope valido → publishFn BrokerEvent { topic, payload, id, source.name='websocket' }
    - Test 6: __message malformed → publishFn network.error category=protocol
    - Test 7: __message __ping__ → publishFn NON invocato per __ping__ (filter strict)
    - Test 8: __message __pong__ → lastPongAt aggiornato + publishFn NON invocato per __pong__
    - Test 9: heartbeat — advance timer di intervalMs → sentFrames contiene `__ping__`
    - Test 10: stale detection — advance timer staleTimeoutMs senza pong → disconnect + recordFailure
    - Test 11: __close(1006) → publishFn disconnected con details.code=1006 + recordFailure
    - Test 12: __close(1000) → publishFn disconnected ma reconnect NON record failure
    - Test 13: bufferedAmount > 64KB → ping skipped (sentFrames length non incrementa)
    - Test 14: external AbortSignal abort → disconnect cascade D-112
    - Test 15: PITFALL §11.7 — topic 'weather.__ping__' passa through (anti-AP-6 strict)
  </behavior>
  <action>
**Step 1 (RED) — Crea `packages/gateway/src/sse-ws/websocket-adapter.test.ts`** con i 15 test deterministici descritti in `<behavior>`. Pattern: `vi.useFakeTimers()` per heartbeat/stale, `MockWebSocket.__reset()` in beforeEach. Vedi `sse-adapter.test.ts` (plan 04-05) come template per struttura describe/it.

Il test file deve includere imports:
```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { WebSocketAdapter } from './websocket-adapter'
import { MockWebSocket } from './test-utils/mock-websocket'
import type { RealtimeChannelDef } from './types/realtime-channel-def'
```

Esempi di test concreti (dei 15 elencati in behavior):

```typescript
it('Test 1: scheme switch http→ws / https→wss (D-107)', async () => {
  const def: RealtimeChannelDef = { name: 'orders', buildUrl: async () => 'https://api/x' }
  const adapter = new WebSocketAdapter(def, { publishFn, WebSocketCtor: MockWebSocket as unknown as typeof WebSocket })
  await adapter.connect()
  expect(MockWebSocket.lastInstance!.url).toBe('wss://api/x')
})

it('Test 7: __ping__ filtrato strict (anti-AP-6)', async () => {
  const def: RealtimeChannelDef = { name: 'feed', url: 'wss://x' }
  const adapter = new WebSocketAdapter(def, { publishFn, WebSocketCtor: MockWebSocket as unknown as typeof WebSocket })
  await adapter.connect()
  MockWebSocket.lastInstance!.__open()
  publishFn.mockClear()
  MockWebSocket.lastInstance!.__message('{"topic":"__ping__","data":{}}')
  expect(publishFn).not.toHaveBeenCalledWith(expect.objectContaining({ topic: '__ping__' }))
})

it('Test 15: weather.__ping__ passa through (PITFALL §11.7)', async () => {
  const def: RealtimeChannelDef = { name: 'feed', url: 'wss://x' }
  const adapter = new WebSocketAdapter(def, { publishFn, WebSocketCtor: MockWebSocket as unknown as typeof WebSocket })
  await adapter.connect()
  MockWebSocket.lastInstance!.__open()
  publishFn.mockClear()
  MockWebSocket.lastInstance!.__message('{"topic":"weather.__ping__","data":1}')
  expect(publishFn).toHaveBeenCalledWith(expect.objectContaining({ topic: 'weather.__ping__' }))
})

it('Test 9: heartbeat invia __ping__ ogni intervalMs', async () => {
  const def: RealtimeChannelDef = { name: 'feed', url: 'wss://x', heartbeat: { intervalMs: 1000, staleTimeoutMs: 60_000 } }
  const adapter = new WebSocketAdapter(def, { publishFn, WebSocketCtor: MockWebSocket as unknown as typeof WebSocket })
  await adapter.connect()
  MockWebSocket.lastInstance!.__open()
  vi.advanceTimersByTime(1100)
  expect(MockWebSocket.lastInstance!.sentFrames.some(f => f.includes('"__ping__"'))).toBe(true)
})

it('Test 13: bufferedAmount > 64KB → ping skipped', async () => {
  const def: RealtimeChannelDef = { name: 'feed', url: 'wss://x', heartbeat: { intervalMs: 1000, staleTimeoutMs: 60_000 } }
  const adapter = new WebSocketAdapter(def, { publishFn, WebSocketCtor: MockWebSocket as unknown as typeof WebSocket })
  await adapter.connect()
  const ws = MockWebSocket.lastInstance!
  ws.__open()
  ws.__setBufferedAmount(100_000)
  const initial = ws.sentFrames.length
  vi.advanceTimersByTime(1100)
  expect(ws.sentFrames.length).toBe(initial)
})
```

Includi tutti i 15 test (1-15 numerati come elencato in `<behavior>`). NON omettere nessun test — il numero minimo per acceptance è 15.

**Step 2 (GREEN) — Crea `packages/gateway/src/sse-ws/websocket-adapter.ts`** completo basato su PATTERNS.md §2.9 + integrazione con `frame-parser.ts` di plan 04-02 + `reconnect-strategy.ts` di plan 04-03:

```typescript
import type { BrokerEvent } from '@sembridge/core'
import { createBrokerError } from '@sembridge/core'
import { nanoid } from 'nanoid'
import type { BackpressureStrategy } from '../http/strategies/backpressure-strategy'
import type { RealtimeChannelDef } from './types/realtime-channel-def'
import { createReconnectStrategy, type ReconnectStrategy } from './reconnect-strategy'
import { INTERNAL_TOPICS, isInternalTopic, parseFrame } from './frame-parser'

/** Threshold bufferedAmount (RESEARCH §4.4) — ping skip se buffer > 64KB. */
const BUFFERED_AMOUNT_PING_CAP = 64_000

export type RealtimePublishFn = (event: BrokerEvent) => void

export interface WebSocketAdapterDeps {
  readonly publishFn: RealtimePublishFn
  readonly backpressure?: BackpressureStrategy
  readonly WebSocketCtor?: typeof WebSocket
}

/**
 * RFC 6455 §7.4 — close codes che richiedono reconnect (RESEARCH §4.2).
 *
 * @returns true se il code suggerisce retry, false per chiusure intenzionali o bug.
 */
function shouldReconnectOnCloseCode(code: number): boolean {
  if (code === 1000) return false
  if (code === 1002 || code === 1003 || code === 1007 || code === 1009 || code === 1010 || code === 1015) return false
  return true
}

/**
 * WebSocket adapter (D-101 / D-104 / D-106 / D-107 / D-109 / D-111).
 *
 * Estende il pattern di `SseAdapter` con: envelope JSON parsing (D-106), internal
 * topics filter strict (D-111 + PITFALL §11.7 anti-AP-6), heartbeat ping/pong app-level,
 * stale detection, bufferedAmount cap, scheme switch http→ws.
 *
 * **Anti-pattern AP-3** (PATTERNS.md §5): NON usare `reconnecting-websocket`.
 * **Anti-pattern AP-4**: NON trustare `readyState === OPEN` come liveness.
 */
export class WebSocketAdapter {
  private ws: WebSocket | null = null
  private readonly controller = new AbortController()
  private readonly reconnect: ReconnectStrategy
  private heartbeatTimer: ReturnType<typeof setInterval> | null = null
  private lastPongAt = 0
  private lastEventReceivedAt = 0
  private readonly heartbeatIntervalMs: number
  private readonly staleTimeoutMs: number
  private intentionallyClosed = false

  constructor(
    private readonly def: RealtimeChannelDef,
    private readonly deps: WebSocketAdapterDeps,
  ) {
    this.reconnect = createReconnectStrategy({
      ...(def.reconnect?.baseMs !== undefined && { baseMs: def.reconnect.baseMs }),
      ...(def.reconnect?.capMs !== undefined && { capMs: def.reconnect.capMs }),
      ...(def.reconnect?.consolidationMs !== undefined && { consolidationMs: def.reconnect.consolidationMs }),
      ...(def.reconnect?.maxAttempts !== undefined && { maxAttempts: def.reconnect.maxAttempts }),
      ...(def.reconnect?.fallbackThreshold !== undefined && { fallbackThreshold: def.reconnect.fallbackThreshold }),
      ...(def.reconnect?.globalCycleCap !== undefined && { globalCycleCap: def.reconnect.globalCycleCap }),
      initialMode: 'websocket',
    })
    this.heartbeatIntervalMs = def.heartbeat?.intervalMs ?? 30_000
    this.staleTimeoutMs = def.heartbeat?.staleTimeoutMs ?? 60_000
  }

  async connect(externalSignal?: AbortSignal): Promise<void> {
    if (this.controller.signal.aborted) return
    if (externalSignal) {
      externalSignal.addEventListener('abort', () => this.disconnect('external.abort'), { once: true })
    }

    const baseUrl = this.def.buildUrl ? await this.def.buildUrl() : this.def.url
    if (!baseUrl) {
      throw createBrokerError({
        code: 'realtime.config.invalid',
        category: 'config',
        message: `WS channel "${this.def.name}": neither buildUrl nor url provided`,
      })
    }

    const wsUrl = this.switchScheme(baseUrl)

    const Ctor = this.deps.WebSocketCtor ?? (globalThis as { WebSocket?: typeof WebSocket }).WebSocket
    if (!Ctor) {
      throw createBrokerError({
        code: 'realtime.websocket.unavailable',
        category: 'config',
        message: 'WebSocket API not available in this environment',
      })
    }

    const subprotocols = this.def.wsSubprotocols
    this.ws = subprotocols !== undefined
      ? new Ctor(wsUrl, subprotocols as string | string[])
      : new Ctor(wsUrl)

    this.ws.addEventListener('open', () => {
      this.reconnect.recordSuccess()
      this.deps.publishFn({
        id: nanoid(),
        topic: 'system.realtime.connected',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'websocket' },
        payload: { channel: this.def.name },
      } as BrokerEvent)
      this.startHeartbeat()
    })

    this.ws.addEventListener('message', (ev) => this.dispatchInbound(ev as MessageEvent))

    // RESEARCH §4.5 — onerror è opaque, info utili arrivano da onclose.
    this.ws.addEventListener('error', () => { /* swallow — onclose racconta la verità */ })

    this.ws.addEventListener('close', (ev) => {
      const closeEv = ev as CloseEvent
      const code = closeEv.code
      const reason = closeEv.reason || `close.${code}`
      this.stopHeartbeat()
      if (shouldReconnectOnCloseCode(code) && !this.intentionallyClosed) {
        this.reconnect.recordFailure()
      }
      this.deps.publishFn({
        id: nanoid(),
        topic: 'system.realtime.disconnected',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'websocket' },
        payload: { channel: this.def.name, reason: `code:${code}.${reason}`, code },
      } as BrokerEvent)
    })
  }

  disconnect(reason: string = 'manual'): void {
    this.intentionallyClosed = true
    this.stopHeartbeat()
    if (this.ws) {
      try { this.ws.close(1000, reason) } catch { /* idempotent */ }
      this.ws = null
    }
    if (!this.controller.signal.aborted) {
      this.controller.abort(reason)
    }
  }

  checkFreshness(staleTimeoutMs: number): boolean {
    if (this.lastEventReceivedAt === 0) return true
    return Date.now() - this.lastEventReceivedAt < staleTimeoutMs
  }

  getDebugInfo(): { name: string; mode: 'websocket'; readyState: number | null; lastPongAt: number; lastEventReceivedAt: number } {
    return {
      name: this.def.name,
      mode: 'websocket',
      readyState: this.ws?.readyState ?? null,
      lastPongAt: this.lastPongAt,
      lastEventReceivedAt: this.lastEventReceivedAt,
    }
  }

  private dispatchInbound(ev: MessageEvent): void {
    const result = parseFrame(ev.data)
    if (!result.ok) {
      this.deps.publishFn({
        id: nanoid(),
        topic: 'network.error',
        timestamp: Date.now(),
        source: { type: 'server', id: 'realtime-channel', name: 'websocket' },
        payload: {
          category: 'protocol',
          code: 'realtime.frame.parse-failed',
          channel: this.def.name,
          reason: result.reason,
          raw: result.raw,
        },
      } as BrokerEvent)
      return
    }

    const { topic, data, id } = result.envelope

    // PITFALL §11.7 — match strict (anti-AP-6).
    if (isInternalTopic(topic)) {
      if (topic === INTERNAL_TOPICS.PONG) {
        this.lastPongAt = Date.now()
      }
      return
    }

    this.lastEventReceivedAt = Date.now()

    const event: BrokerEvent = {
      id: id ?? nanoid(),
      topic,
      timestamp: Date.now(),
      source: { type: 'server', id: 'realtime-channel', name: 'websocket' },
      payload: data,
    } as BrokerEvent

    if (this.deps.backpressure) {
      this.deps.backpressure
        .schedule(this.def.name, 'normal', () => Promise.resolve(this.deps.publishFn(event)))
        .catch(() => { /* drop logged */ })
    } else {
      this.deps.publishFn(event)
    }
  }

  private startHeartbeat(): void {
    this.lastPongAt = Date.now()
    this.heartbeatTimer = setInterval(() => {
      if (Date.now() - this.lastPongAt > this.staleTimeoutMs) {
        this.deps.publishFn({
          id: nanoid(),
          topic: 'system.realtime.disconnected',
          timestamp: Date.now(),
          source: { type: 'server', id: 'realtime-channel', name: 'websocket' },
          payload: { channel: this.def.name, reason: 'stale.no-pong' },
        } as BrokerEvent)
        this.reconnect.recordFailure()
        if (this.ws) {
          try { this.ws.close(1000, 'stale.no-pong') } catch { /* idempotent */ }
          this.ws = null
        }
        this.stopHeartbeat()
        return
      }
      if (!this.ws || this.ws.bufferedAmount > BUFFERED_AMOUNT_PING_CAP) return
      try {
        this.ws.send(JSON.stringify({ topic: INTERNAL_TOPICS.PING, data: { ts: Date.now() } }))
      } catch { /* swallow */ }
    }, this.heartbeatIntervalMs)
  }

  private stopHeartbeat(): void {
    if (this.heartbeatTimer !== null) {
      clearInterval(this.heartbeatTimer)
      this.heartbeatTimer = null
    }
  }

  private switchScheme(url: string): string {
    try {
      const u = new URL(url)
      if (u.protocol === 'https:') u.protocol = 'wss:'
      else if (u.protocol === 'http:') u.protocol = 'ws:'
      return u.toString()
    } catch {
      return url.replace(/^https:\/\//, 'wss://').replace(/^http:\/\//, 'ws://')
    }
  }
}
```

**Step 3 — RED→GREEN cycle:**

1. Commit RED: `git add packages/gateway/src/sse-ws/websocket-adapter.test.ts packages/gateway/src/sse-ws/test-utils/mock-websocket.ts && git commit -m "test(04-06): add failing tests for WebSocketAdapter (D-101/D-106/D-107/D-111, RT-02/04/06)"`
2. Verify RED fails (websocket-adapter.ts non esiste)
3. Commit GREEN: `git add packages/gateway/src/sse-ws/websocket-adapter.ts && git commit -m "feat(04-06): implement WebSocketAdapter (envelope JSON + ping/pong + stale + scheme switch + bufferedAmount cap)"`
4. Verify GREEN: tutti i 15 test passing

**Verifica anti-pattern post-implementation:**
- `grep -v '^#' packages/gateway/src/sse-ws/websocket-adapter.ts | grep -c "reconnecting-websocket"` deve essere 0 (anti-AP-3)
- `grep -v '^#' packages/gateway/src/sse-ws/websocket-adapter.ts | grep -c "startsWith('__')"` deve essere 0 (anti-AP-6 — usa solo isInternalTopic strict)
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/websocket-adapter.test.ts && pnpm --filter @sembridge/gateway exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/websocket-adapter.ts` esiste e contiene `export class WebSocketAdapter`
    - File contiene `interface WebSocketAdapterDeps` con `publishFn`, `backpressure?`, `WebSocketCtor?`
    - File contiene `import { parseFrame, isInternalTopic, INTERNAL_TOPICS } from './frame-parser'`
    - File contiene `createReconnectStrategy({ ..., initialMode: 'websocket' })`
    - File contiene scheme switch `https:` → `wss:` e `http:` → `ws:`
    - File contiene `setInterval` per heartbeat con `INTERNAL_TOPICS.PING`
    - File contiene check `bufferedAmount > BUFFERED_AMOUNT_PING_CAP` (64_000)
    - File contiene staleTimeoutMs check con `Date.now() - this.lastPongAt`
    - File contiene `source: { type: 'server', id: 'realtime-channel', name: 'websocket' }`
    - File contiene `wsSubprotocols` passthrough a `new Ctor(wsUrl, subprotocols)`
    - File contiene `shouldReconnectOnCloseCode` function con codes 1000/1002/1003/1007/1009/1010/1015 → false
    - File NON contiene `reconnecting-websocket` (anti-AP-3): `grep -c reconnecting-websocket` === 0
    - File NON contiene `startsWith('__')` (anti-AP-6): `grep -c "startsWith('__')"` === 0
    - File `packages/gateway/src/sse-ws/websocket-adapter.test.ts` contiene almeno 15 test (`grep -v '^#' | grep -c "it("` ≥ 15)
    - `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/websocket-adapter.test.ts` exit 0
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
    - Cronologia git mostra ≥2 commit 04-06 (RED + GREEN)
  </acceptance_criteria>
  <done>WebSocketAdapter è una class production-ready che incapsula WebSocket lifecycle, envelope JSON parsing (D-106), heartbeat ping/pong (D-111), stale detection, scheme switch (D-107), close codes routing (RFC 6455). 15 test deterministici passing con MockWebSocket DI. Plan 04-07 (manager) può istanziare WebSocketAdapter via `new WebSocketAdapter(def, deps)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server↔client (untrusted) | server invia frame JSON arbitrari — parsing e validation lato client |
| consumer↔buildUrl | URL injection responsabilità consumer |
| heartbeat timer↔OS scheduler | Tab freeze mobile può disabilitare timer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | server invia frame oversize 10MB → JSON.parse memory exhaustion | accept (V1) | parseFrame non impone byte cap (deferred V1.x). Mitigation deferred al backpressure (D-115) e al server side. Documentato in DOC-04 plan 04-09. |
| T-04-06-02 | Spoofing | server invia `{topic:"__pong__"}` per ingannare il client che sia "vivo" anche se TCP morta | accept | Il server può sempre rispondere con `__pong__`. Il client si fida del pong come prova di liveness (RESEARCH §4.6 — alternativa è zero, browser non espone ping/pong nativi). Documentato. |
| T-04-06-03 | Spoofing | server invia `{topic:"system.realtime.connected"}` per impersonare lifecycle event | mitigate (parziale) | Il topic `system.realtime.*` può tecnicamente essere pubblicato dal server. V1 NON aggiunge filter "drop server-emitted system.* topics" (deferred V1.x, AP-12 di PATTERNS.md). DOC-04 plan 04-09 documenta che consumer "non dovrebbero subscribe a `system.realtime.*` da source server". |
| T-04-06-04 | Tampering | URL injection via buildUrl ritorna `https://malicious.com` | accept | Consumer responsabilità (D-104 contract). Documentato in DOC-04. |
| T-04-06-05 | Memory exhaustion | bufferedAmount accumula oltre 64KB ping flooding | mitigate | RESEARCH §4.4 — check `bufferedAmount > 64_000` prima di send ping. Test 13 verifica skip. |
| T-04-06-06 | Memory leak | heartbeat timer non clearato su disconnect | mitigate | `stopHeartbeat()` chiamato in disconnect/close. Test 14 verifica AbortSignal cascade. |
| T-04-06-07 | Topic pollution | server invia topic `__ping__` per mascherare un envelope utente | mitigate | `isInternalTopic` strict match — `__ping__`/`__pong__` consumed dal client (no publish). PITFALL §11.7. Test 7 verifica. |
| T-04-06-08 | TCP zombie connection | `readyState===OPEN` ma TCP morta dietro NAT | mitigate | RESEARCH §4.6 — ping/pong app-level + staleTimeoutMs. Test 10 verifica. |
| T-04-06-09 | Resource leak su errore | onerror invocato + onclose race condition | mitigate | RESEARCH §4.5 — onerror swallow (info inutile), onclose unica fonte di truth. Test 11/12 verificano. |
</threat_model>

<verification>
- File `websocket-adapter.ts` con class WebSocketAdapter + WebSocketAdapterDeps interface
- Lifecycle WebSocket (open/message/error/close) gestito + reconnect strategy integrata (initialMode: 'websocket')
- Envelope JSON parsing via `parseFrame` + filter strict `__ping__`/`__pong__` via `isInternalTopic`
- Heartbeat ping/pong applicativo + stale detection con `staleTimeoutMs`
- bufferedAmount cap (64KB) pre-send
- Scheme switch http(s)→ws(s) automatico
- WS subprotocols passthrough opt-in
- Close codes routing (RFC 6455)
- AbortController cascade D-112
- DI WebSocketCtor per test
- ≥15 test deterministici passing con MockWebSocket
- Pattern TDD RED→GREEN visibile in git log
- Anti-pattern AP-3 (no reconnecting-websocket lib)
- Anti-pattern AP-6 (no startsWith('__'))
- Zero regressioni su core/mapper/routing/gateway-http
</verification>

<success_criteria>
1. `new WebSocketAdapter(def, deps)` + `connect()` → MockWebSocket creato con scheme switch correto
2. `__open()` → publishFn `system.realtime.connected` con source.name='websocket' + heartbeat started
3. `__message('{"topic":"x","data":1,"id":"e"}')` → publishFn BrokerEvent con id='e', topic='x'
4. `__ping__` filtrato strict, `weather.__ping__` passa through (anti-AP-6)
5. Heartbeat genera `__ping__` frames, stale detection chiude su no-pong
6. ≥15 test deterministici passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-06-SUMMARY.md` con:
- LOC count source + test + mock
- Output `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/websocket-adapter.test.ts`
- Conferma TDD RED→GREEN (commit hash)
- Conferma anti-AP-3 + anti-AP-6: grep counts === 0
- Hand-off note per 04-07 (manager) sulla WebSocketAdapter API
</output>
