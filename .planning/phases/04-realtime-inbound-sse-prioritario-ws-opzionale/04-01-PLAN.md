---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gateway/src/sse-ws/augment.ts
  - packages/gateway/src/sse-ws/types/realtime-config.ts
  - packages/gateway/src/sse-ws/types/realtime-channel-def.ts
  - packages/gateway/src/sse-ws/types/index.ts
  - packages/gateway/src/sse-ws/index.ts
  - packages/gateway/src/sse-ws/augment.test.ts
  - packages/gateway/package.json
  - packages/gateway/tsup.config.ts
  - packages/gateway/vitest.config.ts
  - packages/gateway/src/index.ts
autonomous: true
requirements:
  - RT-01
  - RT-02
  - RT-03
  - RT-05
must_haves:
  truths:
    - "Subpath @sembridge/gateway/sse-ws è risolvibile: import { __augmentSseWsLoaded } from '@sembridge/gateway/sse-ws' compila senza errori"
    - "BrokerConfig.realtime?: RealtimeConfig è disponibile (declaration merging via augment) — typecheck consumer non crasha su {realtime:{channels:[...]}}"
    - "PluginDescriptor.realtimeChannels?: readonly RealtimeChannelDef[] è disponibile sui descriptor — chiude il placeholder F1 in core/types/plugin.ts:50"
    - "RealtimeChannelDef.mode accetta 'sse'|'websocket'|'auto' come union literal con discriminator pattern"
    - "RealtimeChannelDef.eventTypes?: readonly string[] (W-4) e .sseHeartbeatEventTypes?: readonly string[] (B-5) sono dichiarati come campi opzionali — solo SSE"
  artifacts:
    - path: "packages/gateway/src/sse-ws/augment.ts"
      provides: "TS declaration merging BrokerConfig.realtime + PluginDescriptor.realtimeChannels + F4PipelineStep + __augmentSseWsLoaded marker"
      contains: "declare module '@sembridge/core'"
    - path: "packages/gateway/src/sse-ws/types/realtime-config.ts"
      provides: "RealtimeConfig + ReconnectDefaults + HeartbeatDefaults + VisibilityDefaults"
      contains: "interface RealtimeConfig"
    - path: "packages/gateway/src/sse-ws/types/realtime-channel-def.ts"
      provides: "RealtimeChannelDef + RealtimeMode + RealtimeReconnectConfig"
      contains: "interface RealtimeChannelDef"
    - path: "packages/gateway/src/sse-ws/types/index.ts"
      provides: "Barrel re-export tipi F4"
      exports: ["RealtimeConfig", "RealtimeChannelDef", "RealtimeMode"]
    - path: "packages/gateway/src/sse-ws/index.ts"
      provides: "Subpath barrel skeleton (in plan 04-08 viene popolato con runtime)"
      contains: "export { __augmentSseWsLoaded"
    - path: "packages/gateway/package.json"
      provides: "Subpath export ./sse-ws + sideEffects glob esistente copre new augment"
      contains: '"./sse-ws":'
    - path: "packages/gateway/tsup.config.ts"
      provides: "Build entry sse-ws/index + sse-ws/augment"
      contains: "'sse-ws/index'"
    - path: "packages/gateway/vitest.config.ts"
      provides: "Coverage exclude per sse-ws/{index,types,augment,test-utils}"
      contains: "src/sse-ws/types/**"
    - path: "packages/gateway/src/index.ts"
      provides: "Umbrella re-export __augmentSseWsLoaded + export * from './sse-ws'"
      contains: "__augmentSseWsLoaded"
  key_links:
    - from: "packages/gateway/src/index.ts"
      to: "packages/gateway/src/sse-ws/augment.ts"
      via: "side-effect import re-export __augmentSseWsLoaded"
      pattern: "__augmentSseWsLoaded.*from.*sse-ws/augment"
    - from: "packages/gateway/src/sse-ws/augment.ts"
      to: "@sembridge/core BrokerConfig + PluginDescriptor"
      via: "declare module '@sembridge/core'"
      pattern: "declare module '@sembridge/core'"
    - from: "packages/gateway/package.json exports"
      to: "packages/gateway/dist/sse-ws/index.{js,d.ts}"
      via: "subpath './sse-ws'"
      pattern: '"./sse-ws"'
---

<objective>
Bootstrap del subpath `@sembridge/gateway/sse-ws` per Phase 4. Crea `augment.ts` (declaration merging `BrokerConfig.realtime` + `PluginDescriptor.realtimeChannels` — chiusura placeholder F1 di `core/types/plugin.ts:50`), i type pubblici (`RealtimeConfig`, `RealtimeChannelDef`, `RealtimeMode`), il barrel skeleton, e aggiorna build/test config (`package.json` exports, `tsup.config.ts` entries, `vitest.config.ts` coverage exclude, umbrella `src/index.ts`). NESSUNA implementazione runtime — solo types + scaffolding.

Purpose: stabilire il "vocabolario" type-level F4 perché i plan 04-02/03/04 in Wave 2 abbiano i types pronti senza dipendenze ulteriori. Pattern identico a `gateway/augment.ts` di F3 (plan 03-04).

Output:
- 4 nuovi file source (augment + 3 type files + barrel skeleton)
- 1 test file (augment.test.ts — verifica __augmentSseWsLoaded === true + smoke decl merging)
- 4 file modificati (package.json, tsup.config.ts, vitest.config.ts, gateway/src/index.ts)

Vincolo D-83 / D-101: ZERO modifiche runtime a `packages/core/`, `packages/mapper/`, `packages/routing/`, `packages/gateway/src/http/`. Il commento `// F4 will add: realtimeChannels` in `packages/core/src/types/plugin.ts:50` è SOLO un placeholder documentale — NON va rimosso né modificato (anti-pattern AP-1 di PATTERNS.md §5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/src/augment.ts
@packages/gateway/src/index.ts
@packages/gateway/package.json
@packages/gateway/tsup.config.ts
@packages/gateway/vitest.config.ts
@packages/routing/src/augment.ts
@packages/core/src/types/plugin.ts

<interfaces>
<!-- Tipi esistenti che 04-01 importerà o estenderà via declaration merging -->

From packages/core/src/types/plugin.ts (lines 44-57):
```typescript
export interface PluginDescriptor {
  readonly id: string
  readonly version?: string
  readonly displayName?: string
  // F2 will add: inputMap, outputMap, requires, provides
  // F3 will add: routes
  // F4 will add: realtimeChannels       ← placeholder che 04-01 chiude via decl merging
  // F5 will add: workers

  onRegister?(ctx: PluginContext): void | Promise<void>
  onMount?(ctx: PluginContext): void | Promise<void>
  onUnmount?(ctx: PluginContext): void | Promise<void>
  onDestroy?(ctx: PluginContext): void
}
```

From packages/core/src/types/broker-config.ts (placeholder esistente per BrokerConfig):
```typescript
// BrokerConfig è una interface in @sembridge/core. F2 ha aggiunto canonicalModel/aliasRegistry/transforms,
// F3 ha aggiunto routes/routing (in @sembridge/routing/augment.ts) e gateway (in @sembridge/gateway/augment.ts).
// F4 aggiunge realtime?: RealtimeConfig via declaration merging in sse-ws/augment.ts.
```

From packages/routing/src/index.ts (export ESISTENTE da Wave 4-B):
```typescript
// BackpressurePolicyConfig è esportato come type da @sembridge/routing
export type { BackpressurePolicyConfig } from './...'
```

Pattern S1 marker (Anti tree-shaking) — riferimento packages/gateway/src/augment.ts:92:
```typescript
export const __augmentGatewayLoaded: true = true
// 04-01 deve creare il sibling __augmentSseWsLoaded
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crea augment.ts + types F4 + barrel skeleton</name>
  <files>
    packages/gateway/src/sse-ws/augment.ts
    packages/gateway/src/sse-ws/types/realtime-config.ts
    packages/gateway/src/sse-ws/types/realtime-channel-def.ts
    packages/gateway/src/sse-ws/types/index.ts
    packages/gateway/src/sse-ws/index.ts
    packages/gateway/src/sse-ws/augment.test.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.1 augment.ts, §2.2 realtime-config.ts, §2.3 realtime-channel-def.ts, §2.13 index.ts, §3.2 Pattern S1 anti tree-shaking)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-101..D-104, D-115)
    - packages/gateway/src/augment.ts (intero, 92 LOC — analog esatto)
    - packages/routing/src/augment.ts (lines 59-180 — pattern PluginDescriptor.routes simmetrico)
    - packages/gateway/src/augment.test.ts (test pattern per __augmentLoaded)
    - packages/core/src/types/plugin.ts (PluginDescriptor — placeholder F4 will add)
  </read_first>
  <behavior>
    - Test 1: `__augmentSseWsLoaded === true` (literal type) — verifica const ri-esportato.
    - Test 2: Decl merging smoke — un BrokerConfig literal `{ realtime: { channels: [...] } }` typecheck OK.
    - Test 3: Decl merging smoke — un PluginDescriptor literal `{ id: 'x', realtimeChannels: [{ name: 'a' }] }` typecheck OK.
    - Test 4: F4PipelineStep type accetta i 3 literal `'event.realtime.received'`, `'event.realtime.frame-parsed'`, `'event.realtime.reconnecting'`.
  </behavior>
  <action>
**Step 1 — Crea `packages/gateway/src/sse-ws/types/realtime-config.ts`** copiando struttura da PATTERNS.md §2.2:

```typescript
import type { RealtimeChannelDef } from './realtime-channel-def'

/** Default reconnect policy (D-109 — RT-05 override-abile per-canale). */
export interface ReconnectDefaults {
  readonly baseMs?: number          // default 1_000 (D-109)
  readonly capMs?: number           // default 30_000 (D-109)
  readonly consolidationMs?: number // default 5_000 (Q3 §6.2)
  readonly maxAttempts?: number     // default Infinity (RT-05)
  readonly fallbackThreshold?: number // default 3 (D-107)
  readonly globalCycleCap?: number    // default 5 (D-107)
}

/** Default heartbeat policy (D-111 — WS ping/pong + SSE freshness uniforme Q5). */
export interface HeartbeatDefaults {
  readonly intervalMs?: number     // default 30_000 (D-111)
  readonly staleTimeoutMs?: number // default 60_000 (D-111 + Q5 SSE uniforme)
}

/** Default visibility policy (D-110 — Visibility API integration). */
export interface VisibilityDefaults {
  readonly toleranceMultiplier?: number // default 3 (D-110)
}

/**
 * Configurazione realtime — root config dichiarata in `BrokerConfig.realtime`.
 *
 * Pattern identico a `GatewayConfig` di F3 (sub-sezioni opzionali). Le sezioni
 * `defaults.*` si applicano a tutti i canali che non le overridano per-canale
 * tramite `RealtimeChannelDef.{reconnect,heartbeat}`.
 *
 * @see {@link https://github.com/<TBD>/sembridge/blob/main/.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md | D-102, D-110, D-111}
 */
export interface RealtimeConfig {
  /** Default applicati a tutti i canali (RT-05 override-abile). */
  readonly defaults?: {
    readonly reconnect?: ReconnectDefaults
    readonly heartbeat?: HeartbeatDefaults
    readonly visibility?: VisibilityDefaults
  }
  /** Canali pre-registrati al boot (D-102 — analogo `routes` di F3 RouterBrokerConfig). */
  readonly channels?: readonly RealtimeChannelDef[]
}
```

**Step 2 — Crea `packages/gateway/src/sse-ws/types/realtime-channel-def.ts`** copiando da PATTERNS.md §2.3:

```typescript
import type { BackpressurePolicyConfig } from '@sembridge/routing'

/** Modalità di connessione realtime (D-107 — auto = SSE-first con auto-fallback a WS). */
export type RealtimeMode = 'sse' | 'websocket' | 'auto'

/** Per-channel reconnect override (D-109). Tutti override-abili da RealtimeConfig.defaults. */
export interface RealtimeReconnectConfig {
  readonly baseMs?: number
  readonly capMs?: number
  readonly consolidationMs?: number
  readonly maxAttempts?: number
  readonly fallbackThreshold?: number
  readonly globalCycleCap?: number
}

/**
 * Definizione di un canale realtime (D-102 — multi-channel topology).
 *
 * Indicizzato per `name` univoco nel `RealtimeChannelManager` (plan 04-07). Ogni canale
 * ha proprio adapter (SSE o WS, plan 04-05/04-06), proprio reconnect state, proprio
 * `buildUrl()` hook (D-104 auth-agnostic).
 *
 * Coerente con `RouteDefinitionBase` di F3 (id+topic+priority): `name` qui ha lo
 * stesso ruolo di `id` per un route, ma a livello di canale fisico.
 *
 * @example
 * ```ts
 * const def: RealtimeChannelDef = {
 *   name: 'orders',
 *   mode: 'auto',                                    // D-107 SSE-first + WS fallback
 *   buildUrl: async () => `/events?token=${await getToken()}`,  // D-104 auth-agnostic
 *   wsSubprotocols: ['sembridge-v1'],                // Q4 opt-in per auth handshake
 *   reconnect: { baseMs: 500, capMs: 15_000 },       // override default
 *   heartbeat: { intervalMs: 20_000 },                // override per-canale
 *   backpressure: { policy: 'queue-bounded', maxSize: 500 }, // D-115 riuso F3
 * }
 * ```
 */
export interface RealtimeChannelDef {
  /** Chiave univoca del canale nel Manager (D-102). NON vuota. */
  readonly name: string
  /** Modalità connessione (D-107). Default `'auto'` se omesso. */
  readonly mode?: RealtimeMode
  /**
   * Hook auth-agnostic invocato PRIMA di OGNI connect/reconnect (D-104).
   * Return value = URL completo (incluso eventuale token in query string).
   * Default fallback: `url` statico se omesso.
   */
  readonly buildUrl?: () => Promise<string>
  /** URL fallback se `buildUrl` non fornito (D-104). */
  readonly url?: string
  /** Subprotocols WS opzionali (Q4 / PITFALL §11.3 — passthrough a `new WebSocket(url, protocols)`). */
  readonly wsSubprotocols?: string | readonly string[]
  /** Override reconnect per-canale (D-109). */
  readonly reconnect?: RealtimeReconnectConfig
  /** Override heartbeat per-canale WS (D-111). */
  readonly heartbeat?: { readonly intervalMs?: number; readonly staleTimeoutMs?: number }
  /** Backpressure strategy adapter-level (D-115 — riuso 1:1 di F3 BackpressurePolicyConfig). */
  readonly backpressure?: BackpressurePolicyConfig
  /**
   * SSE custom event types (W-4 fix — chiusura SC-1 ROADMAP scenario meteo `event: weather.update`).
   *
   * Default `undefined` → adapter SSE registra solo listener `'message'` con topic = `def.name`.
   * Se popolato, l'adapter SSE registra `addEventListener(eventType, ...)` per ogni elemento e
   * il `BrokerEvent.topic` deriva dal field `event:` SSE (NOT da `def.name`).
   *
   * **Solo SSE** — i WebSocket non hanno custom event types.
   *
   * @example
   * ```ts
   * { name: 'meteo', url: '/sse', eventTypes: ['weather.update', 'weather.alert'] }
   * // Server invia `event: weather.update\ndata: {...}` → BrokerEvent.topic === 'weather.update'
   * ```
   */
  readonly eventTypes?: readonly string[]
  /**
   * SSE custom event types che aggiornano `lastEventReceivedAt` SENZA pubblicare BrokerEvent
   * (B-5 fix — Q5 closure freshness check con server heartbeat sintattici).
   *
   * Default `['heartbeat']` se omesso. Server SSE che invia `event: heartbeat` ogni N<60s
   * mantiene il canale "fresh" senza spam topic. Se il server usa un altro nome custom
   * (es. `keepalive`, `ping`), passare `['keepalive']` o `['heartbeat', 'keepalive']`.
   *
   * **Solo SSE** — WebSocket usa `__pong__` JSON message (D-111).
   */
  readonly sseHeartbeatEventTypes?: readonly string[]
}
```

**Step 3 — Crea `packages/gateway/src/sse-ws/augment.ts`** copiando struttura da PATTERNS.md §2.1 (verbatim):

```typescript
// augment.ts — TS declaration merging per estendere @sembridge/core con tipi F4 SSE/WS.
// (D-103 in 04-CONTEXT.md — replica simmetrica di gateway/augment.ts di F3 + routing/augment.ts)
//
// Vincolo D-83 / D-101: NESSUNA modifica a packages/core/src/, packages/mapper/src/,
// packages/routing/src/, packages/gateway/src/http/ runtime. Questo file è il PUNTO UNICO
// di chiusura del placeholder F1 in `packages/core/src/types/plugin.ts:50`
// ("F4 will add: realtimeChannels").
//
// Cosa estende:
//   - BrokerConfig (interface, da @sembridge/core) — aggiunge `realtime?: RealtimeConfig`
//     come campo opzionale (D-102). Sezione complementare a `gateway` (F3 — questo stesso
//     pkg) e `routes`/`routing` (F3 routing).
//   - PluginDescriptor (interface, da @sembridge/core) — aggiunge
//     `readonly realtimeChannels?: readonly RealtimeChannelDef[]` come campo opzionale
//     (D-103, RT-01). Pattern simmetrico con `routes?: readonly RouteDefinition[]` di F3
//     routing/augment.ts:75-78.
//
// Cosa NON estende qui:
//   - Nessuna augmentation di RouteDefinition con `type: 'realtime-inbound'` (PRD §17.5
//     lo prevede ma è scope F3 placeholder — F4 V1 usa il MANAGER, non un route handler).
//   - PipelineStep type alias non si può fare merging — esposto come `F4PipelineStep`
//     literal union additive (vedi sotto, pattern F2 `F2PipelineStep` / F3 `F3PipelineStep`).
//
// Side-effect import: `packages/gateway/src/sse-ws/index.ts` ri-esporta
// `__augmentSseWsLoaded` da questo file. Il `package.json` ha già il glob
// `sideEffects: ["**/augment.ts", "**/augment.js"]` che copre `dist/sse-ws/augment.js`.
//
// Threat coverage:
// - T-04-01-01 (Tampering — tree-shaker elimina dist/sse-ws/augment.js): mitigate via
//   sideEffects glob + __augmentSseWsLoaded re-export dal barrel.
// - T-04-01-02 (Spoofing — collision con augment F2/F3): accept. F4 augmenta SOLO
//   `BrokerConfig.realtime` e `PluginDescriptor.realtimeChannels` — campi DISGIUNTI dai
//   campi F2 (inputMap/outputMap/canonicalModel/aliasRegistry/transforms) e F3
//   (routes/routing/gateway). Test backward-compat smoke verifica coexistenza.

import type { RealtimeChannelDef } from './types/realtime-channel-def'
import type { RealtimeConfig } from './types/realtime-config'

declare module '@sembridge/core' {
  /**
   * F4 augmentation (D-102, PRD §27): aggiunge la sezione `realtime` a `BrokerConfig`.
   *
   * `realtime`: configurazione del realtime adapter SSE/WS centralizzato (D-102 multi-channel
   * topology). Pattern identico a `BrokerConfig.gateway` (F3) e `BrokerConfig.canonicalModel`
   * (F2) — sezione strutturata letta dal `RealtimeBroker` costruttore (plan 04-08) per
   * istanziare `RealtimeChannelManager` (plan 04-07).
   */
  interface BrokerConfig {
    /** Sezione `realtime` (D-102, PRD §16.2/§18.3-18.4): config canali SSE/WS multi-channel. */
    realtime?: RealtimeConfig
  }

  /**
   * F4 augmentation (D-103, RT-01): aggiunge il campo opzionale `realtimeChannels`
   * al PluginDescriptor pubblico — chiude il placeholder F1 in
   * `packages/core/src/types/plugin.ts:50` ("F4 will add: realtimeChannels").
   *
   * I canali sono auto-registrati al `registerPlugin` con `ownerId = pluginId`
   * (cascade D-26 ext F4 / D-112 — plan 04-07/04-08). Pattern simmetrico a
   * `routes?: readonly RouteDefinition[]` di routing/augment.ts:75-78.
   */
  interface PluginDescriptor {
    /** Canali realtime auto-registrati al `registerPlugin` con `ownerId = pluginId` (D-103). */
    readonly realtimeChannels?: readonly RealtimeChannelDef[]
  }
}

/** F4 PipelineStep — eventi step §28 emessi dagli adapter SSE/WS (D-113 ingress). */
export type F4PipelineStep =
  | 'event.realtime.received'      // step 1 ingress da adapter (D-113)
  | 'event.realtime.frame-parsed'  // step 1.b parser envelope (D-106)
  | 'event.realtime.reconnecting'  // diagnostic state machine (D-109)

/**
 * Marker const esportato per detection runtime del side-effect import.
 *
 * Pattern S1 (replica `__augmentLoaded` di routing/augment.ts:172 +
 * `__augmentGatewayLoaded` di gateway/augment.ts:92): export const literal `true`
 * ri-esportato dal barrel `sse-ws/index.ts` per evitare tree-shaking accidentale.
 *
 * Audit-able: `grep "__augmentSseWsLoaded" dist/` permette di confermare il
 * side-effect è presente nel bundle distribuito.
 */
export const __augmentSseWsLoaded: true = true
```

**Step 4 — Crea `packages/gateway/src/sse-ws/types/index.ts`** (barrel types-only):

```typescript
/** Barrel types F4 — re-export da `@sembridge/gateway/sse-ws`. */
export type {
  RealtimeConfig,
  ReconnectDefaults,
  HeartbeatDefaults,
  VisibilityDefaults,
} from './realtime-config'
export type {
  RealtimeChannelDef,
  RealtimeMode,
  RealtimeReconnectConfig,
} from './realtime-channel-def'
```

**Step 5 — Crea `packages/gateway/src/sse-ws/index.ts` skeleton** (sarà esteso in 04-08):

```typescript
/**
 * `@sembridge/gateway/sse-ws` — Subpath SSE/WebSocket realtime adapter (Phase 4).
 *
 * Plan 04-01: bootstrap types + augment + scaffolding. I runtime export
 * (parseFrame, createReconnectStrategy, SseAdapter, WebSocketAdapter,
 * RealtimeChannelManager, RealtimeBroker, createRealtimeBroker) verranno aggiunti
 * incrementalmente nei plan 04-02..04-08 via Wave-based parallelization.
 *
 * Vincolo D-101: composition wrapper di `RouterBroker` (F3). ZERO modifiche runtime
 * a F1/F2/F3 (D-83 ext F4).
 *
 * @packageDocumentation
 */

// Side-effect import — abilita TS declaration merging per BrokerConfig.realtime
// + PluginDescriptor.realtimeChannels (D-103). Pattern S1 anti tree-shaking
// (riferimento PATTERNS.md §3.2).
export { __augmentSseWsLoaded, type F4PipelineStep } from './augment'

// ---------- Type re-export: types/* ----------
export type {
  RealtimeConfig,
  ReconnectDefaults,
  HeartbeatDefaults,
  VisibilityDefaults,
  RealtimeChannelDef,
  RealtimeMode,
  RealtimeReconnectConfig,
} from './types'

// Plan 04-02..04-08 add: parseFrame, createReconnectStrategy, createVisibilityDetector,
// SseAdapter, WebSocketAdapter, RealtimeChannelManager, RealtimeBroker,
// createRealtimeBroker, FrameEnvelope, FrameParseResult.
```

**Step 6 — Crea `packages/gateway/src/sse-ws/augment.test.ts`** replica struttura `gateway/src/augment.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { __augmentSseWsLoaded, type F4PipelineStep } from './augment'
import type { RealtimeConfig } from './types/realtime-config'
import type { RealtimeChannelDef } from './types/realtime-channel-def'

describe('sse-ws augment.ts (D-103, plan 04-01)', () => {
  it('Test 1: __augmentSseWsLoaded marker is literal true', () => {
    expect(__augmentSseWsLoaded).toBe(true)
  })

  it('Test 2: F4PipelineStep accetta i 3 literal definiti', () => {
    const steps: F4PipelineStep[] = [
      'event.realtime.received',
      'event.realtime.frame-parsed',
      'event.realtime.reconnecting',
    ]
    expect(steps).toHaveLength(3)
  })

  it('Test 3: BrokerConfig decl merging — realtime?: RealtimeConfig accetta channels', () => {
    // Smoke: il typecheck è la vera verifica. A runtime verifichiamo solo che il literal compili.
    const channelDef: RealtimeChannelDef = { name: 'test', mode: 'auto' }
    const config: RealtimeConfig = { channels: [channelDef] }
    expect(config.channels).toHaveLength(1)
  })

  it('Test 4: PluginDescriptor decl merging — realtimeChannels è opzionale readonly', () => {
    // Smoke typecheck — il pattern è strutturalmente compatibile.
    const channels: readonly RealtimeChannelDef[] = [{ name: 'a', mode: 'sse' }]
    expect(channels[0]?.name).toBe('a')
  })
})
```

Tutti i file usano lingua italiana per JSDoc descrittivi (vincolo CLAUDE.md), inglese per identifier/code/test names.
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway exec tsc --noEmit && pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/augment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/augment.ts` esiste e contiene `declare module '@sembridge/core'`
    - File `packages/gateway/src/sse-ws/augment.ts` esiste e contiene `export const __augmentSseWsLoaded: true = true`
    - File `packages/gateway/src/sse-ws/augment.ts` esiste e contiene `interface BrokerConfig` (decl merging block)
    - File `packages/gateway/src/sse-ws/augment.ts` esiste e contiene `interface PluginDescriptor` (decl merging block)
    - File `packages/gateway/src/sse-ws/augment.ts` esiste e contiene `realtimeChannels?: readonly RealtimeChannelDef[]`
    - File `packages/gateway/src/sse-ws/types/realtime-config.ts` esiste e contiene `interface RealtimeConfig`
    - File `packages/gateway/src/sse-ws/types/realtime-channel-def.ts` esiste e contiene `interface RealtimeChannelDef`
    - File `packages/gateway/src/sse-ws/types/realtime-channel-def.ts` esiste e contiene `export type RealtimeMode = 'sse' | 'websocket' | 'auto'`
    - File `packages/gateway/src/sse-ws/types/realtime-channel-def.ts` esiste e contiene `readonly eventTypes?: readonly string[]` (W-4 SSE custom event types)
    - File `packages/gateway/src/sse-ws/types/realtime-channel-def.ts` esiste e contiene `readonly sseHeartbeatEventTypes?: readonly string[]` (B-5 SSE heartbeat hook)
    - File `packages/gateway/src/sse-ws/types/index.ts` esiste e re-esporta `RealtimeConfig` e `RealtimeChannelDef`
    - File `packages/gateway/src/sse-ws/index.ts` esiste e contiene `export { __augmentSseWsLoaded`
    - File `packages/gateway/src/sse-ws/augment.test.ts` esiste con almeno 4 test (smoke decl merging)
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
    - `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/augment.test.ts` exit 0 e mostra ≥4 test passing
  </acceptance_criteria>
  <done>I tipi F4 sono dichiarati e il declaration merging funziona via TS. Plan 04-02/03/04 possono importare `RealtimeChannelDef`, `RealtimeConfig`, `RealtimeMode` da `./types`. La sezione `BrokerConfig.realtime` e il campo `PluginDescriptor.realtimeChannels` sono visibili al typecheck consumer-side.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiorna build/test config + umbrella barrel</name>
  <files>
    packages/gateway/package.json
    packages/gateway/tsup.config.ts
    packages/gateway/vitest.config.ts
    packages/gateway/src/index.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.20 package.json, §2.21 tsup.config.ts, §2.22 vitest.config.ts, §2.23 umbrella src/index.ts)
    - packages/gateway/package.json (esistente — sezione exports + sideEffects)
    - packages/gateway/tsup.config.ts (esistente — entry list + commento "Phase 4 aggiungerà...")
    - packages/gateway/vitest.config.ts (esistente — coverage exclude + thresholds)
    - packages/gateway/src/index.ts (esistente — già `export * from './http'`)
  </read_first>
  <action>
**Step 1 — Modifica `packages/gateway/package.json`**: aggiungi subpath export `./sse-ws` parallelo a `./http` nella sezione `exports`. Il glob `sideEffects: ["**/augment.ts", "**/augment.js"]` esistente già copre `dist/sse-ws/augment.js` — NESSUNA modifica al campo `sideEffects`.

Modifica concreta: dopo il blocco `"./http": { ... }` aggiungi:

```jsonc
    "./sse-ws": {
      "types": "./dist/sse-ws/index.d.ts",
      "import": "./dist/sse-ws/index.js"
    },
```

Aggiorna anche il campo `description` sostituendo "(Phase 3 + Phase 4)" con "(Phase 3 HTTP + Phase 4 realtime SSE/WS)".

**Step 2 — Modifica `packages/gateway/tsup.config.ts`**: aggiungi 2 entry alla mappa `entry` (dopo `augment: 'src/augment.ts'`):

```typescript
    'sse-ws/index': 'src/sse-ws/index.ts',
    'sse-ws/augment': 'src/sse-ws/augment.ts',
```

Aggiorna il commento `// Phase 4 aggiungerà...` rimuovendolo o sostituendolo con `// Phase 4: subpath sse-ws (D-103 declaration merging + D-101 composition wrapper).`.

**Step 3 — Modifica `packages/gateway/vitest.config.ts`**: aggiungi alla lista `coverage.exclude` 4 nuovi pattern (dopo `'src/augment.ts'`):

```typescript
        'src/sse-ws/index.ts',
        'src/sse-ws/types/**',
        'src/sse-ws/augment.ts',
        'src/sse-ws/test-utils/**',
```

I `thresholds` rimangono invariati per ora (verranno calibrati in 04-09 dopo coverage measurement post-implementation).

**Step 4 — Modifica `packages/gateway/src/index.ts`**: aggiungi DOPO la linea `export { __augmentGatewayLoaded } from './augment'`:

```typescript
// PHASE 4 ADD: side-effect augment SSE/WS (D-103 BrokerConfig.realtime + PluginDescriptor.realtimeChannels)
export { __augmentSseWsLoaded } from './sse-ws/augment'
```

E DOPO la linea `export * from './http'`:

```typescript
// PHASE 4 ADD: re-export sub-modulo SSE/WS dall'umbrella.
export * from './sse-ws'
```

Non rimuovere né alterare i commenti JSDoc esistenti del file. Aggiorna se opportuno il commento `@packageDocumentation` per menzionare F4 (es. "Phase 3 (HTTP) + Phase 4 (SSE/WS realtime)").
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway build && pnpm --filter @sembridge/gateway exec tsc --noEmit && grep -q '"./sse-ws"' packages/gateway/package.json && grep -q "'sse-ws/index'" packages/gateway/tsup.config.ts && grep -q "src/sse-ws/types/\\*\\*" packages/gateway/vitest.config.ts && grep -q "__augmentSseWsLoaded" packages/gateway/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/gateway/package.json` contiene la stringa `"./sse-ws":` nella sezione exports
    - `packages/gateway/package.json` contiene `"types": "./dist/sse-ws/index.d.ts"` nel blocco `./sse-ws`
    - `packages/gateway/tsup.config.ts` contiene `'sse-ws/index': 'src/sse-ws/index.ts'`
    - `packages/gateway/tsup.config.ts` contiene `'sse-ws/augment': 'src/sse-ws/augment.ts'`
    - `packages/gateway/vitest.config.ts` contiene `'src/sse-ws/types/**'` nella `coverage.exclude` array
    - `packages/gateway/src/index.ts` contiene `export { __augmentSseWsLoaded } from './sse-ws/augment'`
    - `packages/gateway/src/index.ts` contiene `export * from './sse-ws'`
    - `pnpm --filter @sembridge/gateway build` exit 0 e produce `dist/sse-ws/index.js` + `dist/sse-ws/augment.js`
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
    - File `packages/gateway/dist/sse-ws/index.d.ts` esiste post-build
  </acceptance_criteria>
  <done>Subpath export `@sembridge/gateway/sse-ws` è risolvibile da consumer esterni. Build genera `dist/sse-ws/index.js` + `dist/sse-ws/augment.js`. Vitest config esclude correttamente i file non-runtime dalla coverage. Wave 2 (plan 04-02/03/04) può iniziare.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer↔package | TS declaration merging — augmenta interface esistenti (BrokerConfig, PluginDescriptor) di un altro package |
| build↔runtime | `sideEffects` glob `**/augment.ts` deve preservare il file augment dal tree-shaking aggressivo |
| package↔package | dependency cyclic: `gateway/sse-ws` importa `BackpressurePolicyConfig` da `routing` (già dep workspace, no nuovo ciclo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | tree-shaker elimina `dist/sse-ws/augment.js` → declaration merging non attivo a runtime ma TS non se ne accorge | mitigate | `package.json:sideEffects` glob `**/augment.{ts,js}` esistente copre il nuovo file (no modifica config). `__augmentSseWsLoaded` re-esportato dal barrel `sse-ws/index.ts` E dall'umbrella `gateway/src/index.ts` (Pattern S1) |
| T-04-01-02 | Spoofing | declaration merging accidentale collisioni con augment F2/F3 (es. `BrokerConfig.realtime` definito altrove) | accept | F4 augmenta SOLO `BrokerConfig.realtime` e `PluginDescriptor.realtimeChannels` — campi DISGIUNTI dai campi F1 (id/version), F2 (canonicalModel/aliasRegistry/transforms/inputMap/outputMap), F3 (routes/routing/gateway). Test smoke decl merging in 04-01 task1 verifica coexistenza. |
| T-04-01-03 | Tampering | augment estende interface non-additive (TS comportamento atteso ma accidente) | mitigate | TS interface merging è additive per costruzione. Il test 4 in `augment.test.ts` smoke verifica che `RealtimeChannelDef[]` sia readonly compatibile con descriptor minimali F1+F2+F3. |
| T-04-01-04 | Information Disclosure | URL del subpath `./sse-ws` esposto in package.json potrebbe rivelare struttura interna | accept | Comportamento standard del meccanismo "exports" — nessun secret esposto. URL relativi del package, niente endpoint server. |
| T-04-01-05 | Repudiation | augment scope ambiguous senza riferimento a placeholder F1 | mitigate | JSDoc esplicita "F4 augmentation" + reference D-101..D-104 + reference al placeholder F1 in `core/types/plugin.ts:50`. Pattern identico a F2/F3 augment header. |
</threat_model>

<verification>
- File `augment.ts` con `declare module '@sembridge/core'` per BrokerConfig + PluginDescriptor merging
- 3 type files (`realtime-config.ts`, `realtime-channel-def.ts`, `types/index.ts`) creati con tipi pubblici F4
- Barrel skeleton `sse-ws/index.ts` con side-effect import di `__augmentSseWsLoaded`
- `package.json` ha subpath export `./sse-ws`
- `tsup.config.ts` ha 2 entry `sse-ws/index` + `sse-ws/augment`
- `vitest.config.ts` esclude i file non-runtime sse-ws dalla coverage
- Umbrella `gateway/src/index.ts` ri-esporta `__augmentSseWsLoaded` E `export * from './sse-ws'`
- `pnpm --filter @sembridge/gateway build` produce `dist/sse-ws/index.js` + `dist/sse-ws/augment.js`
- `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
- `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/augment.test.ts` exit 0 con ≥4 test passing
- `pnpm --filter @sembridge/gateway test` exit 0 globalmente (zero regressioni su core/mapper/routing/gateway-http)
</verification>

<success_criteria>
1. Il consumer esterno può importare `import { __augmentSseWsLoaded } from '@sembridge/gateway/sse-ws'` senza errori
2. Il typecheck consumer accetta `const cfg: BrokerConfig = { realtime: { channels: [{ name: 'orders' }] } }`
3. Il typecheck consumer accetta `const desc: PluginDescriptor = { id: 'p', realtimeChannels: [{ name: 'a' }] }`
4. Build genera `dist/sse-ws/index.{js,d.ts}` + `dist/sse-ws/augment.{js,d.ts}` separati
5. Zero regressioni sui test esistenti core/mapper/routing/gateway-http (D-83 strict)
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-01-SUMMARY.md` con:
- Liste file creati/modificati con LOC count
- Conferma che `dist/sse-ws/{index,augment}.{js,d.ts}` sono prodotti
- Output di `pnpm --filter @sembridge/gateway test` (numero test pre/post)
- Conferma D-83 strict: zero modifiche a core/, mapper/, routing/, gateway/http/ runtime
- Hand-off note per Wave 2 (04-02/03/04 possono importare types/ + augment è loaded)
</output>
