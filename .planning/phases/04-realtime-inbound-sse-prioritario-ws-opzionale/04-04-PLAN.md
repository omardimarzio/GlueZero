---
phase: 04-realtime-inbound-sse-prioritario-ws-opzionale
plan: 04
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - packages/gateway/src/sse-ws/visibility-detector.ts
  - packages/gateway/src/sse-ws/visibility-detector.test.ts
autonomous: true
requirements:
  - RT-05
must_haves:
  truths:
    - "createVisibilityDetector({ onChange, document }) ritorna VisibilityDetector con start/stop/getState/isActive"
    - "start() registra UNA SOLA listener su document.addEventListener('visibilitychange') anche se invocato due volte (idempotente)"
    - "stop() rimuove la listener via removeEventListener — cleanup garantito (no leak)"
    - "Quando document è null o undefined (Worker context, RESEARCH §5.3), start() è no-op e getState() ritorna 'visible' default sicuro"
    - "Quando document.visibilityState cambia, onChange callback viene invocato con stato 'visible' o 'hidden'"
    - "isActive() ritorna true dopo start() e false dopo stop() (idempotenza-aware)"
  artifacts:
    - path: "packages/gateway/src/sse-ws/visibility-detector.ts"
      provides: "createVisibilityDetector factory + VisibilityDetector interface + VisibilityState type + VisibilityDetectorOptions"
      contains: "export function createVisibilityDetector"
      min_lines: 50
    - path: "packages/gateway/src/sse-ws/visibility-detector.test.ts"
      provides: "≥10 test deterministici (start/stop idempotente, DI guard null document, dispatch event)"
      contains: "describe('createVisibilityDetector"
  key_links:
    - from: "packages/gateway/src/sse-ws/visibility-detector.ts"
      to: "(consumer plan 04-07 realtime-channel-manager.ts)"
      via: "named export createVisibilityDetector"
      pattern: "export.*createVisibilityDetector"
    - from: "packages/gateway/src/sse-ws/visibility-detector.ts"
      to: "globalThis.document (browser API)"
      via: "document.addEventListener('visibilitychange', listener)"
      pattern: "addEventListener.*visibilitychange"
---

<objective>
Implementa il **wrapper Visibility API** (D-110, RESEARCH §5) come state machine event-driven con cleanup garantito + DI guard per environment senza `document` (SSR / Web Worker / iframe sandbox).

Purpose: il `RealtimeChannelManager` (plan 04-07) usa una singola istanza condivisa per orchestrare il "freshness check" su `visibilitychange → visible` di tutti i canali attivi. Pattern simile al cleanup pattern di `combine-signals.ts` di F3 (`addEventListener` + tracking listener per `removeEventListener` puntuale).

Output:
- 1 nuovo file source (`visibility-detector.ts` — factory + interface + DI guard)
- 1 nuovo file test (`visibility-detector.test.ts` — ≥10 test deterministici TDD RED→GREEN con jsdom)

Pattern TDD obbligatorio (D-117): RED commit → GREEN commit. Co-located test sibling.

**Anti-pattern AP-5 da evitare** (PATTERNS.md §5): NON usare `setInterval` per polling visibility — la API è event-driven, polling produce overhead inutile e non risolve il problema mobile freeze (RESEARCH §5.5).

Per testabilità (RESEARCH §5.3), l'options accetta `document?: Document | null`: `undefined` = use `globalThis.document` (browser/jsdom default), `null` = explicit disable (Worker/SSR), `Document mock` = test injection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md
@.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md
@CLAUDE.md
@packages/gateway/src/http/combine-signals.ts

<interfaces>
<!-- Tipi che 04-04 crea -->

From PATTERNS.md §2.7 (verbatim):
```typescript
export type VisibilityState = 'visible' | 'hidden'

export interface VisibilityDetectorOptions {
  readonly onChange: (state: VisibilityState) => void
  /** DI per environment senza Document (RESEARCH §5.3). Default: globalThis.document. Pass `null` per disabilitare. */
  readonly document?: Document | null
}

export interface VisibilityDetector {
  start(): void
  stop(): void
  getState(): VisibilityState
  isActive(): boolean
}
```

From packages/gateway/src/http/combine-signals.ts (lines 62-86 — analog cleanup pattern listener tracking):
- `addEventListener` + memoize listener per `removeEventListener` puntuale
- `if (!signal) return` guard per missing input

From RESEARCH §5.3 (DI guard pattern):
- Worker context = no `document` available → no-op detector
- jsdom default has `document` → real listener attach
- Test inject mock Document via `opts.document = mockDoc`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crea visibility-detector.ts + .test.ts con TDD</name>
  <files>
    packages/gateway/src/sse-ws/visibility-detector.ts
    packages/gateway/src/sse-ws/visibility-detector.test.ts
  </files>
  <read_first>
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-PATTERNS.md (§2.7 visibility-detector INTERO + §3.6 TDD pattern + §5 AP-5)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-RESEARCH.md (§5.1-5.3 Visibility API, §5.5 mobile freeze)
    - .planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-CONTEXT.md (D-110 visibility integration tolerance ×3)
    - packages/gateway/src/http/combine-signals.ts (analog cleanup pattern lines 51-86)
    - packages/gateway/src/http/combine-signals.test.ts (analog test pattern)
  </read_first>
  <behavior>
    - Test 1: `createVisibilityDetector({ onChange })` con jsdom default document → `isActive()` false prima di start()
    - Test 2: `start()` rende `isActive()` true; `stop()` rende `isActive()` false
    - Test 3: `start()` chiamato due volte (idempotente) registra UNA SOLA listener (mock document.addEventListener call count === 1)
    - Test 4: `stop()` chiamato due volte (idempotente) rimuove UNA SOLA volta (no double-cleanup error)
    - Test 5: dispatch `visibilitychange` event con `document.visibilityState = 'hidden'` invoca `onChange('hidden')`
    - Test 6: dispatch `visibilitychange` event con `document.visibilityState = 'visible'` invoca `onChange('visible')`
    - Test 7: `getState()` ritorna 'hidden' o 'visible' coerente con `document.visibilityState`
    - Test 8: con `document: null` (DI guard) → `start()` no-op, `getState()` ritorna 'visible' default
    - Test 9: con `document: null` → `isActive()` false anche dopo start()
    - Test 10: dispatch event DOPO `stop()` NON invoca onChange (listener rimosso)
    - Test 11: con custom Document mock injected → addEventListener invocato sul mock, NON sul globalThis.document
  </behavior>
  <action>
**Step 1 (RED) — Crea `packages/gateway/src/sse-ws/visibility-detector.test.ts` PRIMA dell'implementazione**, struttura simile a `combine-signals.test.ts`:

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { createVisibilityDetector, type VisibilityDetector, type VisibilityState } from './visibility-detector'

/**
 * Helper: crea un mock Document con visibilityState mutabile + dispatchEvent.
 * jsdom default ha già document.addEventListener funzionante, ma usiamo mock
 * per testare il DI path E il count delle invocazioni addEventListener.
 */
function createMockDocument(): Document & { __setState: (s: VisibilityState) => void; __dispatch: () => void } {
  const listeners = new Set<EventListener>()
  let state: VisibilityState = 'visible'
  const mock = {
    get visibilityState() {
      return state
    },
    addEventListener: vi.fn((type: string, fn: EventListener) => {
      if (type === 'visibilitychange') listeners.add(fn)
    }),
    removeEventListener: vi.fn((type: string, fn: EventListener) => {
      if (type === 'visibilitychange') listeners.delete(fn)
    }),
    __setState(s: VisibilityState): void {
      state = s
    },
    __dispatch(): void {
      const ev = new Event('visibilitychange')
      listeners.forEach((fn) => fn(ev))
    },
  } as unknown as Document & { __setState: (s: VisibilityState) => void; __dispatch: () => void }
  return mock
}

describe('createVisibilityDetector (D-110, RESEARCH §5)', () => {
  let onChange: ReturnType<typeof vi.fn>
  let mockDoc: ReturnType<typeof createMockDocument>

  beforeEach(() => {
    onChange = vi.fn()
    mockDoc = createMockDocument()
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('Test 1: prima di start() isActive() === false', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    expect(v.isActive()).toBe(false)
  })

  it('Test 2: start() rende isActive() true; stop() torna a false', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    expect(v.isActive()).toBe(true)
    v.stop()
    expect(v.isActive()).toBe(false)
  })

  it('Test 3: start() idempotente — registra UNA listener anche su 2 chiamate', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    v.start()
    expect(mockDoc.addEventListener).toHaveBeenCalledTimes(1)
  })

  it('Test 4: stop() idempotente — removeEventListener chiamata UNA SOLA volta', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    v.stop()
    v.stop()
    expect(mockDoc.removeEventListener).toHaveBeenCalledTimes(1)
  })

  it('Test 5: dispatch visibilitychange con state=hidden → onChange("hidden")', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    mockDoc.__setState('hidden')
    mockDoc.__dispatch()
    expect(onChange).toHaveBeenCalledWith('hidden')
  })

  it('Test 6: dispatch visibilitychange con state=visible → onChange("visible")', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    mockDoc.__setState('visible')
    mockDoc.__dispatch()
    expect(onChange).toHaveBeenCalledWith('visible')
  })

  it('Test 7: getState() coerente con mockDoc.visibilityState', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    mockDoc.__setState('hidden')
    expect(v.getState()).toBe('hidden')
    mockDoc.__setState('visible')
    expect(v.getState()).toBe('visible')
  })

  it('Test 8: DI guard — document=null → start() no-op, getState() default "visible"', () => {
    const v = createVisibilityDetector({ onChange, document: null })
    v.start()
    expect(v.isActive()).toBe(false)
    expect(v.getState()).toBe('visible')
  })

  it('Test 9: DI guard — document=null → isActive() false anche post start()', () => {
    const v = createVisibilityDetector({ onChange, document: null })
    v.start()
    expect(v.isActive()).toBe(false)
  })

  it('Test 10: dispatch DOPO stop() NON invoca onChange', () => {
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    v.stop()
    mockDoc.__setState('hidden')
    mockDoc.__dispatch()
    expect(onChange).not.toHaveBeenCalled()
  })

  it('Test 11: addEventListener invocato sul Document iniettato, NON sul globalThis.document', () => {
    const globalSpy = vi.spyOn(globalThis.document, 'addEventListener')
    const v = createVisibilityDetector({ onChange, document: mockDoc })
    v.start()
    expect(mockDoc.addEventListener).toHaveBeenCalled()
    expect(globalSpy).not.toHaveBeenCalledWith('visibilitychange', expect.anything())
  })
})
```

**Step 2 (GREEN) — Crea `packages/gateway/src/sse-ws/visibility-detector.ts`** copiando da PATTERNS.md §2.7 (verbatim):

```typescript
/**
 * Stato osservato della Visibility API (D-110, RESEARCH §5.1).
 *
 * Il W3C Page Visibility API L2 spec definisce anche `'prerender'` ma è raro.
 * F4 V1 collassa tutto su `'visible' | 'hidden'` (i due stati operativamente
 * rilevanti per il freshness check D-110).
 */
export type VisibilityState = 'visible' | 'hidden'

/** Opzioni del factory `createVisibilityDetector` (RESEARCH §5.3 DI guard). */
export interface VisibilityDetectorOptions {
  /** Callback invocato a OGNI transition `visibilitychange`. */
  readonly onChange: (state: VisibilityState) => void
  /**
   * DI per environment senza Document (RESEARCH §5.3 — Web Worker, SSR, iframe sandbox).
   * - `undefined` (default): use `globalThis.document` (browser/jsdom).
   * - `null`: explicit disable — `start()` no-op, `getState()` ritorna `'visible'` default sicuro.
   * - `Document` mock: test injection.
   */
  readonly document?: Document | null
}

/** Public interface ritornata dal factory. Tutti i metodi sono sync e idempotenti. */
export interface VisibilityDetector {
  /** Inizia ad osservare `visibilitychange`. Idempotente (no-op se già attivo). */
  start(): void
  /** Stop osservazione + cleanup listener (D-112 cascade cleanup). Idempotente. */
  stop(): void
  /** Stato corrente snapshot. Default sicuro `'visible'` se DI guard attivo. */
  getState(): VisibilityState
  /** True se `start()` invocato e detector attivo. */
  isActive(): boolean
}

/**
 * Crea un `VisibilityDetector` che astrae la Visibility API (D-110, RESEARCH §5).
 *
 * Pattern cleanup garantito (replica `combine-signals.ts:62-86` listener tracking):
 * - `start()` registra listener via `document.addEventListener('visibilitychange', fn)`.
 * - `stop()` chiama `document.removeEventListener` puntuale (cleanup garantito, no leak).
 * - Idempotente: `start()` chiamato 2x senza `stop()` registra UNA sola volta.
 *
 * DI guard (RESEARCH §5.3): se `document === null`, `start()` è no-op e `getState()`
 * ritorna `'visible'` per default sicuro (assumiamo "visibile" se non possiamo osservare).
 *
 * **Anti-pattern AP-5 (PATTERNS.md §5):** NON usare `setInterval` per polling — la
 * Visibility API è event-driven, polling produce overhead inutile.
 *
 * @example
 * ```ts
 * const v = createVisibilityDetector({
 *   onChange: (s) => { if (s === 'visible') manager.checkFreshnessAll() }
 * })
 * v.start()
 * // ... later
 * v.stop() // cleanup garantito
 * ```
 */
export function createVisibilityDetector(opts: VisibilityDetectorOptions): VisibilityDetector {
  // RESEARCH §5.3 DI guard:
  // - undefined → use globalThis.document (browser default).
  // - null → explicitly disabled (Worker/SSR).
  // - Document → test mock injection.
  const doc =
    opts.document === null
      ? null
      : opts.document !== undefined
        ? opts.document
        : typeof globalThis !== 'undefined' && 'document' in globalThis
          ? ((globalThis as { document?: Document }).document ?? null)
          : null

  let active = false
  let listener: (() => void) | null = null

  function read(): VisibilityState {
    if (!doc) return 'visible' // safe default (RESEARCH §5.3)
    return doc.visibilityState === 'hidden' ? 'hidden' : 'visible'
  }

  return {
    start(): void {
      if (active || !doc) return // idempotent + DI guard no-op
      listener = (): void => opts.onChange(read())
      doc.addEventListener('visibilitychange', listener)
      active = true
    },
    stop(): void {
      if (!active || !doc || listener === null) return
      doc.removeEventListener('visibilitychange', listener)
      listener = null
      active = false
    },
    getState(): VisibilityState {
      return read()
    },
    isActive(): boolean {
      return active
    },
  }
}
```

**Step 3 — RED→GREEN cycle:**

1. Commit RED: `git add packages/gateway/src/sse-ws/visibility-detector.test.ts && git commit -m "test(04-04): add failing tests for createVisibilityDetector (D-110, DI guard, idempotency)"`
2. Verify RED fails (file production non esiste)
3. Commit GREEN: `git add packages/gateway/src/sse-ws/visibility-detector.ts && git commit -m "feat(04-04): implement createVisibilityDetector with DI guard + cleanup pattern (D-110)"`
4. Verify GREEN: tutti i test passing

**Anti-pattern AP-5 check:** il file `visibility-detector.ts` NON deve contenere `setInterval` o `setTimeout` (event-driven only).
  </action>
  <verify>
    <automated>cd "/Users/omarmarzio/programming/prova AI/SemBridge" && pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/visibility-detector.test.ts && pnpm --filter @sembridge/gateway exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` esiste e contiene `export function createVisibilityDetector`
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` esiste e contiene `export interface VisibilityDetector`
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` esiste e contiene `export type VisibilityState = 'visible' | 'hidden'`
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` contiene `addEventListener('visibilitychange'`
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` contiene `removeEventListener('visibilitychange'`
    - File `packages/gateway/src/sse-ws/visibility-detector.ts` NON contiene `setInterval` (anti-AP-5) — verify con `grep -v '^#' packages/gateway/src/sse-ws/visibility-detector.ts | grep -c "setInterval" === 0`
    - File `packages/gateway/src/sse-ws/visibility-detector.test.ts` contiene almeno 11 test (`grep -v '^#' | grep -c "it(" >= 11`)
    - `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/visibility-detector.test.ts` exit 0 con tutti i test passing
    - `pnpm --filter @sembridge/gateway exec tsc --noEmit` exit 0
    - Cronologia git mostra 2 commit separati 04-04: RED + GREEN
  </acceptance_criteria>
  <done>VisibilityDetector è una factory event-driven con start/stop idempotente, cleanup garantito e DI guard per environment senza document. 11 test deterministici passing. Plan 04-07 (realtime-channel-manager.ts) può importare `createVisibilityDetector` per orchestrare freshness check on-visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser↔detector | Document API è trustato (host browser environment) |
| Worker/SSR↔detector | Document non disponibile — DI guard ritorna no-op detector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Document API mocked (consumer fornisce mock malicious) | accept | DI è esplicito — il consumer è responsabile della Document fornita. Test 11 verifica che `mockDoc.addEventListener` sia il target, non `globalThis.document`. |
| T-04-04-02 | Memory leak | start() chiamato N volte senza stop() corrispondente — listener accumulati | mitigate | start() idempotente: registra UNA sola listener anche su N invocazioni. Test 3 verifica. |
| T-04-04-03 | Memory leak | stop() non chiamato → listener mai rimossa, document leaked al detector | mitigate | Manager (plan 04-07) garantisce stop() su disconnectAll/teardownVisibility. Test 4+10 verifica cleanup. |
| T-04-04-04 | Information Disclosure | callback onChange espone stato visibility a terzi (non sensible per definizione) | accept | visibilityState è API pubblica browser, niente secret. |
| T-04-04-05 | Mobile freeze divergence | Safari iOS background freeze più aggressivo di desktop — listener potrebbe non scattare | accept | Documentato in DOC-04 plan 04-09 come limitazione platform-level. F4 fa best effort. |
</threat_model>

<verification>
- File `visibility-detector.ts` con factory + interface + DI guard
- start/stop idempotente (registra/rimuove listener UNA volta)
- DI guard `null` document → no-op + getState() default 'visible'
- ≥11 test passing in jsdom tier-1 (con mock Document custom)
- Pattern TDD RED→GREEN visibile in git log
- Zero usage di `setInterval` (anti-AP-5)
- Zero regressioni su core/mapper/routing/gateway-http
</verification>

<success_criteria>
1. `createVisibilityDetector({ onChange }).start()` registra UNA listener su `globalThis.document` (jsdom env)
2. `start()` chiamato 2x → addEventListener invocato 1x (idempotente)
3. `stop()` rimuove la listener via `removeEventListener` puntuale
4. `document: null` → start() no-op, getState() ritorna 'visible' default
5. dispatch `visibilitychange` post-stop NON invoca onChange (cleanup verificato)
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-inbound-sse-prioritario-ws-opzionale/04-04-SUMMARY.md` con:
- LOC count file source + test
- Output `pnpm --filter @sembridge/gateway test packages/gateway/src/sse-ws/visibility-detector.test.ts`
- Conferma TDD RED→GREEN: commit hash RED + commit hash GREEN
- Conferma anti-AP-5: `grep -v '^#' packages/gateway/src/sse-ws/visibility-detector.ts | grep -c "setInterval"` === 0
- Hand-off note per 04-07 (manager può importare `createVisibilityDetector`)
</output>
