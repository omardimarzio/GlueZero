{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .planning/scripts/update-tracker.sh 2>&1 | head -5 || true"
          }
        ]
      }
    ]
  }
}
