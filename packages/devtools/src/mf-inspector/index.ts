/**
 * `@gluezero/devtools/mf-inspector` — subpath additivo placeholder (D-V2-F16-19).
 *
 * **Stato W1 P01 (foundation):** questo file è un placeholder vuoto creato per
 * permettere a `tsup` di costruire l'entry `mf-inspector/index` e popolare
 * `dist/mf-inspector/index.{js,d.ts}` (cap 8 KB gzipped pre-imposto). Il
 * contenuto reale (`mfInspectorModule` factory + 17-fields aggregator + per-MF
 * ring buffer + 11 timings collector + pause/resume/flush) sarà aggiunto in
 * W2 P02.
 *
 * Esposto via `packages/devtools/package.json#exports['./mf-inspector']`.
 *
 * @see D-V2-F16-19 (tsup multi-entry subpath pattern carryover F7 theme-inspector)
 * @see D-V2-F16-16 (W2 P02 — content real)
 * @packageDocumentation
 */
export {}
