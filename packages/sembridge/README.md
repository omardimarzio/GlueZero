# @sembridge/sembridge

> Placeholder. Sviluppo previsto in Phase 2 (primo release pubblico aggregato). In Phase 1 questo pacchetto esiste solo come scaffold per il monorepo `pnpm workspaces`.

Pacchetto aggregato che re-esporta `@sembridge/core`, `@sembridge/mapper`, `@sembridge/gateway`, `@sembridge/routing`, `@sembridge/worker`, `@sembridge/cache`, `@sembridge/devtools`.

## Stato

- [x] Scaffold (Phase 1)
- [ ] Implementazione (Phase 2 — primo release pubblico aggregato)

## Riferimenti

- `prd.md` (root) §31 (packaging)
- `.planning/ROADMAP.md` Phase 2
