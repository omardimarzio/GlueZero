---
"@gluezero/core": major
"@gluezero/microfrontends": major
"@gluezero/mapper": major
"@gluezero/context": major
"@gluezero/mf-esm": major
"@gluezero/mf-web-component": major
"@gluezero/mf-iframe": major
"@gluezero/mf-module-federation": major
"@gluezero/mf-single-spa": major
"@gluezero/permissions": major
"@gluezero/compat": major
"@gluezero/isolation": major
"@gluezero/fallbacks": major
"@gluezero/devtools": major
"@gluezero/react": major
"@gluezero/web-components": major
---

# GlueZero v2.0.0 — General Availability

**Milestone Microfrontend Governance Layer GA** — 10 fasi v2.0 (F8-F17) complete, 132 REQ-IDs chiusi, 16 package npm pubblicati. Backward compatibility v1.x preservata bit-exact (BC §42 14 API + MF-PIPE-01 pipeline §28 ordine letterale).

## Highlight

- **`@gluezero/react`** NEW (F17 W2) — Provider `<GlueZeroProvider broker mfContext?>` con due React.Context separati internamente + 6 hooks (`useGlueZero`, `useGlueZeroPublish`, `useGlueZeroSubscribe`, `useRuntimeContext`, `useMicroFrontendContext`, `useGlueZeroBroker`) + factory `createReactMicroFrontendLifecycle(Component, options?)` compatibile `MicroFrontendRuntimeModule` F8 + `<GlueZeroErrorBoundary>` class component built-in (pubblica `microfrontend.runtime.failed` + delega a `SERVICE_FALLBACKS` F14) + React 19 StrictMode coalescing pattern. Peer optional `react`+`react-dom` `>=18.2.0 <20.0.0`. Bundle 1.53 KB gzipped (cap 10 KB).

- **`@gluezero/web-components`** NEW (F17 W3) — `GlueZeroElement` base class opzionale + cleanup automatico via `AbortController + signal` (delegato a `disconnectedCallback`) + helper context wiring (`glueZeroBroker`/`glueZeroContext` property assignment + setter trigger) + `this.publish`/`this.subscribe` instance method shortcuts con auto-iniezione `signal` + `metadata.microFrontendId`. **Subpath `@gluezero/web-components/lit`** two-tier: `GlueZeroController` (Lit `ReactiveController` 3.x) + `GlueZeroLitMixin<Base extends LitElement>(Base)` mixin ergonomico. Peer optional `lit: >=3.0.0 <4.0.0`. Bundle 609 B gzipped + /lit 480 B (cap 8 KB + 3 KB).

- **`@gluezero/core` + `@gluezero/microfrontends` + `@gluezero/mapper` + `@gluezero/context`** (F10) — Canonical Model esteso + MicroFrontendRegistry + RuntimeContext + 29 standard topics F8. `createBroker({})` rimane semantica v1.x bit-exact (zero breaking change Livello A). `createBroker({modules:[microFrontendModule()]})` abilita MF governance opt-in (Livello B). Bundle delta ≤ +350 B (D-V2-21 PASS F8-12).

- **`@gluezero/mf-esm`** (F9) — ESM loader dynamic import + lifecycle FSM mount/unmount/destroy + governance integration.

- **`@gluezero/permissions` + `@gluezero/compat` + `@gluezero/isolation` + `@gluezero/fallbacks`** (F11/F12/F13/F14) — Production MF Governance stack: permission topic ACL + compat policy matrix (block-mount/warn/allow) + shadow-dom isolation + ErrorBoundary fallback Service Locator pattern.

- **`@gluezero/mf-{web-component,iframe,module-federation,single-spa}`** (F15) — 4 loader experimental enterprise multi-team multi-MF SaaS.

- **`@gluezero/devtools`** (F16) — `mfInspectorModule()` MIN-3 SnapshotProvider Registry + 14 metriche per-MF + subpath `/mf-inspector` Tier-1 sc-closure 4 SC + JSDoc enrichment.

- **Test discipline closure** — MF-TEST-01..04 (tier discipline cross-fase F8-F17 + 17 categorie unit + 12+3 integration scenarios + tinybench CI hard gate <5%/<10% scenarios A/B).

- **Documentation** — 18 documenti PRD §41 in `docs/v2/` + 2 README italiano adapter (react + web-components) 13 sezioni + migration guide adoption levels A/B/C + customer-dashboard golden showcase end-to-end + 6 examples HTML CDN standalone + TypeDoc GitHub Pages auto-deploy `gluezero.github.io/v2/`.

- **Backward compatibility** — BC §42 14 API v1.x preservate bit-exact (`debug-snapshot-shape` + `get-metrics-shape` + 12 altre API). `createBroker({})` rimane semantica v1.x (zero breaking change Livello A).

## D-V2-F8-10 BLOCKING unlock

Closure milestone F17 verifier PASS — D-V2-F8-10 BLOCKING (npm publish 2.0.0 GA unlock) sbloccato. Sequenza staging:

1. **2.0.0-rc.0** publish `--tag next` (release candidate, 7 giorni soak window early adopters).
2. **7gg soak window** monitoring GitHub issue tracker, ZERO P0/P1 blocker accepted.
3. **2.0.0** promote `--tag latest` (post-soak no-blocker) + GitHub Release v2.0.0 annotated.

Vedi [docs/v2/17-migration-guide.md](./docs/v2/17-migration-guide.md) per migration v1.x → v2.0 (livelli A/B/C).

Vedi [examples/customer-dashboard/](./examples/customer-dashboard/) per golden showcase end-to-end realistico 1 host + 3 MF mixed (React + WC + iframe) + governance (permissions + compat + isolation + fallbacks + devtools).

## Breaking changes

**Nessuno** per Livello A (`createBroker({})` bit-exact v1.x). I 16 package vengono bumped major per allinearsi alla nuova milestone GA + Canonical Model esteso v2.0 (semantica module-aware), ma BC §42 garantisce API v1.x preservation.

Major bump rationale per-package:
- **core/mapper/microfrontends/context**: API extends with module-aware semantics + MF registry + RuntimeContext (BC §42 14 API preserved).
- **devtools**: extends with `mfInspectorModule()` + subpath `/mf-inspector` + 14 metriche MF (debug-snapshot-shape + get-metrics-shape D-V2-19 preserved).
- **mf-esm + permissions + compat + isolation + fallbacks + mf-{wc,iframe,mf,ss}**: NEW packages v2.0 (pre-bumped `2.0.0-alpha.0` durante development F8-F15).
- **react + web-components**: NEW packages v2.0 framework adapters (F17 W2/W3).
